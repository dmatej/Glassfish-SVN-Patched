<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>org.glassfish.core</groupId>
        <artifactId>core</artifactId>
        <version>10.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>glassfish</artifactId>
    <packaging>hk2-jar</packaging>
    <name>Appserver Core Bootstraping Classes</name>
    <description>Glassfish appserver boostraping classes</description>

    <developers>
        <developer>
            <id>dochez</id>
            <name>Jerome Dochez</name>
            <url>http://blogs.sun.com/dochez</url>
            <organization>Sun Microsystems, Inc.</organization>
            <roles>
                <role>lead</role>
                <role>developer</role>
            </roles>
        </developer>
    </developers>    
    <build>
        <plugins>
            <plugin>
                <groupId>com.sun.enterprise</groupId>
                <artifactId>hk2-maven-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.sun.enterprise.glassfish.bootstrap.Main</mainClass>
                        </manifest>
                        <!-- this specifies the bootstrap module -->
                        <manifestEntries>
                            <Bundle-Name>glassfish-bootstrap</Bundle-Name>
                            <Import-Bundles>org.glassfish.core:kernel</Import-Bundles>
                            <Class-Path>jars/hk2-${hk2.version}.jar jars/auto-depends-${hk2.version}.jar jars/config-${hk2.version}.jar jars/stax-api-1.0-2.jar jars/sjsxp-1.0.jar</Class-Path>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>com.sun.enterprise</groupId>
            <artifactId>hk2</artifactId>
            <version>${hk2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.enterprise</groupId>
            <artifactId>config</artifactId>
            <version>${hk2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.enterprise</groupId>
            <artifactId>auto-depends</artifactId>
            <version>${hk2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.xml.stream</groupId>
            <artifactId>sjsxp</artifactId>
            <version>1.0</version>
        </dependency>
    </dependencies>
</project>

