<?xml version="1.0" encoding="iso-8859-1"?>

<project name="trilead-ssh2 source integration" default="build" basedir="..">

    <!-- import build.xml for init target and inherited properties -->
    <import file="${basedir}/build.xml"/>

    <target name="checkout" depends="init,check.svn" unless="skipCheckout">
        <svn.checkout repo="${trilead-ssh2.svn.url}" dest="trilead-ssh2"/>
    </target>

    <target name="build" depends="checkout" >
        <mvn.install dir="${glassfish.external.dir}/trilead-ssh2"/>
    </target>

</project>
