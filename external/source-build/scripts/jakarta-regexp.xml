<?xml version="1.0" encoding="iso-8859-1"?>

<project name="Apache Jakarta Regexp Library source integration" default="build" basedir="..">

    <!-- import build.xml for init target and inherited properties -->
    <import file="${basedir}/build.xml"/>

    <target name="checkout" depends="init,check.svn" unless="skipCheckout">
        <svn.checkout repo="${jakarta-regexp.svn.url}" dest="jakarta-regexp"/>
    </target>

    <target name="build" depends="checkout">
        <ant dir="${glassfish.external.dir}/jakarta-regexp" />
        <mvn.publish.local
	    file="${glassfish.external.dir}/jakarta-regexp/build/jakarta-regexp-${jakarta-regexp.version}.jar"
            groupId="org.apache.jakarta-regexp"
            artifactId="jakarta-regexp"
            version="${jakarta-regexp.version}"/>
   </target>

</project>
