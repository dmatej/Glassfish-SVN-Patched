<?xml version="1.0" encoding="iso-8859-1"?>

<project name="tiger-types source integration" default="build" basedir="..">

    <!-- import build.xml for init target and inherited properties -->
    <import file="${basedir}/build.xml"/>

    <target name="checkout" depends="init,check.svn" unless="skipCheckout">
        <svn.checkout repo="${tiger-types.svn.url}" dest="tiger-types"/>
    </target>

    <target name="build" depends="checkout" >
        <mvn.install dir="${glassfish.external.dir}/tiger-types" />
    </target>

</project>
