<?xml version="1.0" encoding="iso-8859-1"?>

<project name="dbschema source integration" default="build" basedir="..">
    
    <!-- import build.xml for init target and inherited properties -->
    <import file="${basedir}/build.xml"/>

    <target name="checkout" depends="init,check.svn" unless="skipCheckout">
        <svn.checkout repo="${dbschema.svn.url}" dest="dbschema"/>
    </target>

    <target name="build" depends="checkout">
        <mvn.install dir="${glassfish.external.dir}/dbschema"/>
    </target>

    <target name="publish">
        <mvn2 dir="${glassfish.external.dir}/dbschema" goal="deploy">
            <args>
                <arg line="-DaltDeploymentRepository=${altDeploymentRepository}" />
            </args>
        </mvn2>
    </target>

</project>
