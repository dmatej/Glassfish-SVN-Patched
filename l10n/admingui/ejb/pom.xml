<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
<modelVersion>4.0.0</modelVersion>
<parent>
    <groupId>org.glassfish.l10n</groupId>
    <artifactId>l10n</artifactId>
    <version>3.0-SNAPSHOT</version>
  </parent>
<artifactId>console-ejb-plugin-l10n</artifactId>
<packaging>hk2-jar</packaging>
<name>Admin Console ejb Plugin localization</name>
<build>
    <plugins>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <configuration>
              <tasks>
                <echo message="Syncing English resource files"/>
                <sync overwrite="true" todir="src/main/resources_en">
                  <fileset dir="${GFV3_DIR}/admingui/ejb/src/main/resources">
                    <includesfile name="l10n.include"/>
                    <excludesfile name="l10n.exclude"/>
                  </fileset>
                </sync>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.glassfish.l10n</groupId>
        <artifactId>maven-l10n-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <goals>
              <goal>l10n-prepare</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <configuration>
                    <!-- Enable the plugin for hk2-jar packaging type -->
                    <supportedProjectTypes>
                        <supportedProjectType>hk2-jar</supportedProjectType>
                        <supportedProjectType>jar</supportedProjectType>
                        <supportedProjectType>bundle</supportedProjectType>
                    </supportedProjectTypes>
                    <instructions>
                        <!-- _include does not work. See Felix-620.
                        <_include>osgi.bundle</_include>
                        -->
                        <Export-Package>
                        org.glassfish.l10n.admingui
                        </Export-Package>
                        <!--<Fragment-Host>
                        org.glassfish.admingui.console-security-plugin
                        </Fragment-Host>
                        Implement as admingui plugin.
                        -->
                    </instructions>
                </configuration>
                <executions>
                    <execution>
                        <id>bundle-manifest</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>manifest</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
    </plugins>
    <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>**/*.jar</exclude>
                </excludes>
            </resource>
        </resources>
  </build>
  <dependencies>
        <dependency>
            <groupId>org.glassfish.admingui</groupId>
            <artifactId>console-plugin-service</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
