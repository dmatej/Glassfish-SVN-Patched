list-jvm-options(1)       asadmin 유틸리티 하위 명령       list-jvm-options(1)

제목
       list-jvm-options - Java 응용 프로그램 실행기 옵션 나열

시놉시스
           list-jvm-options [--help] [--target target]
           [--profiler={false|true}]

설명
       list-jvm-options 하위 명령은 GlassFish Server 시작 시 Java 응용
       프로그램 실행기로 전달되는 명령행 옵션 목록을 표시합니다.

       이 옵션은 관리 콘솔의 JVM 옵션 페이지를 통해 또는 create-jvm-options 및
       delete-jvm-options 하위 명령을 통해 관리됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           Java 응용 프로그램 실행기 옵션을 나열할 대상을 지정합니다.

           적합한 값은 다음과 같습니다.

           server
               DAS를 지정합니다(기본값).

           instance-name
               GlassFish Server 인스턴스를 지정합니다.

           cluster-name
               클러스터를 지정합니다.

           configuration-name
               명명된 구성을 지정합니다.

       --profiler
           나열할 Java 응용 프로그램 실행기 옵션이 프로파일러용인지 여부를
           지정합니다. 프로파일러를 구성한 경우에만 이 옵션을 true로
           설정합니다. 이 옵션을 true로 설정하고 프로파일러를 구성하지 않으면
           오류가 발생합니다. 기본값은 false입니다.

예제
       예 1, Java 응용 프로그램 실행기 옵션 나열
           이 예제는 Java 응용 프로그램 실행기에서 사용하는 옵션을 나열합니다.

               asadmin> list-jvm-options
               -Djava.security.auth.login.config=${com.sun.aas.instanceRoot}/config/login.conf
               -XX: LogVMOutput
               -XX: UnlockDiagnosticVMOptions
               -Dcom.sun.enterprise.config.config_environment_factory_class=
               com.sun.enterprise.config.serverbeans.AppserverConfigEnvironmentFactory
               -Djavax.net.ssl.keyStore=${com.sun.aas.instanceRoot}/config/keystore.jks
               -XX:NewRatio=2
               -DANTLR_USE_DIRECT_CLASS_LOADING=true
               -Djava.security.policy=${com.sun.aas.instanceRoot}/config/server.policy
               -Djdbc.drivers=org.apache.derby.jdbc.ClientDriver
               -Djavax.net.ssl.trustStore=${com.sun.aas.instanceRoot}/config/cacerts.jks
               -client
               -Djava.ext.dirs=${com.sun.aas.javaRoot}/lib/ext${path.separator}${
               com.sun.aas.javaRoot}/jre/lib/ext${path.separator}${com.sun.aas.instanceRoot}
               /lib/ext${path.separator}${com.sun.aas.derbyRoot}/lib
               -Xmx512m
               -XX:MaxPermSize=192m
               -Djava.endorsed.dirs=${com.sun.aas.installRoot}/lib/endorsed
               -XX:LogFile=${com.sun.aas.instanceRoot}/logs/jvm.log
               Command list-jvm-options executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jvm-options(1), delete-jvm-options(1)

       asadmin(1M)

       Java 응용 프로그램 실행기에 대한 자세한 내용은 사용 중인 운영 체제용
       참조 페이지를 참조하십시오.

       ·   Oracle Solaris 및 Linux: java - Java 응용 프로그램
           실행기(http://java.sun.com/javase/6/docs/technotes/tools/solaris/java.html)

       ·   Windows: java - Java 응용 프로그램
           실행기(http://java.sun.com/javase/6/docs/technotes/tools/windows/java.html)

Java EE 7                      2010년 12월 20일            list-jvm-options(1)
