delete-threadpool(1)    Unterbefehle des asadmin-Utili    delete-threadpool(1)

NAME
       delete-threadpool - entfernt einen Threadpool

ZUSAMMENFASSUNG
           delete-threadpool [--help] [--target target] threadpool-id

BESCHREIBUNG
       Entfernt den Threadpool mit der angegebenen ID. Dieser Unterbefehl wird
       nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, aus dem Sie den Threadpool
           entfernen.

           Folgende Werte sind gültig:

           server
               Löscht den Threadpool für die standardmäßige GlassFish
               Server-Instanz server und ist der Standardwert.

           configuration-name
               Löscht den Threadpool für die benannte Konfiguration.

           cluster-name
               Löscht den Threadpool für jede Instanz im Cluster.

           instance-name
               Löscht den Threadpool für eine bestimmte Instanz.

OPERANDEN
       threadpool-id
           Eine ID für die Arbeitsqueue, Beispiel: thread-pool1,
            threadpool-2 usw.

BEISPIELE
       Beispiel 1, Threadpools löschen
           In diesem Beispiel wird threadpool-l gelöscht.

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                        29. Nov. 2010            delete-threadpool(1)
