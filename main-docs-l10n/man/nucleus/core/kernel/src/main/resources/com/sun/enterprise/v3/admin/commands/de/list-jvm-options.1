list-jvm-options(1)     Unterbefehle des asadmin-Utili     list-jvm-options(1)

NAME
       list-jvm-options - listet Optionen für den Java-Anwendungs-Launcher auf

ZUSAMMENFASSUNG
           list-jvm-options [--help] [--target target]
           [--profiler={false|true}]

BESCHREIBUNG
       Der Unterbefehl list-jvm-options zeigt eine Liste von
       Befehlszeilenoptionen an, die an den Java-Anwendungs-Launcher übergeben
       werden, wenn GlassFish Server gestartet wird.

       Die Optionen werden über die Seite "JVM-Optionen" der
       Administrationskonsole oder über die Unterbefehle create-jvm-options
       und delete-jvm-options verwaltet.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, für das Sie die Optionen für den
           Java-Anwendungs-Launcher auflisten.

           Folgende Werte sind gültig:

           server
               Gibt den DAS an (Standardeinstellung).

           instance-name
               Gibt eine GlassFish Server-Instanz an.

           cluster-name
               Gibt ein Cluster an.

           configuration-name
               Gibt eine benannte Konfiguration an.

       --profiler
           Gibt an, ob die aufzulistenden Optionen des
           Java-Anwendungs-Launchers für den Profiler gelten. Setzen Sie diese
           Option nur auf "true", wenn ein Profiler konfiguriert wurde. Ist
           diese Option auf "true" gesetzt und kein Profiler konfiguriert,
           tritt ein Fehler auf. Der Standardwert ist "false".

BEISPIELE
       Beispiel 1, Optionen des Java-Anwendungs-Launchers auflisten
           In diesem Beispiel werden die Optionen aufgelistet, die vom
           Java-Anwendungs-Launcher verwendet werden.

               asadmin> list-jvm-options
               -Djava.security.auth.login.config=${com.sun.aas.instanceRoot}/config/login.conf
               -XX: LogVMOutput
               -XX: UnlockDiagnosticVMOptions
               -Dcom.sun.enterprise.config.config_environment_factory_class=
               com.sun.enterprise.config.serverbeans.AppserverConfigEnvironmentFactory
               -Djavax.net.ssl.keyStore=${com.sun.aas.instanceRoot}/config/keystore.jks
               -XX:NewRatio=2
               -DANTLR_USE_DIRECT_CLASS_LOADING=true
               -Djava.security.policy=${com.sun.aas.instanceRoot}/config/server.policy
               -Djdbc.drivers=org.apache.derby.jdbc.ClientDriver
               -Djavax.net.ssl.trustStore=${com.sun.aas.instanceRoot}/config/cacerts.jks
               -client
               -Djava.ext.dirs=${com.sun.aas.javaRoot}/lib/ext${path.separator}${
               com.sun.aas.javaRoot}/jre/lib/ext${path.separator}${com.sun.aas.instanceRoot}
               /lib/ext${path.separator}${com.sun.aas.derbyRoot}/lib
               -Xmx512m
               -XX:MaxPermSize=192m
               -Djava.endorsed.dirs=${com.sun.aas.installRoot}/lib/endorsed
               -XX:LogFile=${com.sun.aas.instanceRoot}/logs/jvm.log
               Command list-jvm-options executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jvm-options(1), delete-jvm-options(1)

       asadmin(1M)

       Weitere Informationen zum Java-Anwendungs-Launcher finden Sie auf der
       Referenzseite für das Betriebssystem, das Sie verwenden:

       ·   Oracle Solaris und Linux: Java - der Java-Anwendungs-Launcher
           (http://java.sun.com/javase/6/docs/technotes/tools/solaris/java.html)

       ·   Windows: Java - der Java-Anwendungs-Launcher
           (http://java.sun.com/javase/6/docs/technotes/tools/windows/java.html)

Java EE 7                        20. Dez. 2010             list-jvm-options(1)
