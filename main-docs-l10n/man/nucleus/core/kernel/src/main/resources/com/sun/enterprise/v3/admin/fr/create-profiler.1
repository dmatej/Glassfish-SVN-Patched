create-profiler(1)      Sous-commandes de l'utilitaire      create-profiler(1)

NOM
       create-profiler - crée l'élément d'analyseur de performances

SYNTAXE
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

DESCRIPTION
       La sous-commande create-profiler crée l'élément d'analyseur de
       performances. Une instance de serveur est liée à l'analyseur de
       performances par l'élément d'analyseur de performances de la
       configuration Java. Il ne peut exister qu'un seul analyseur de
       performances à la fois. Si vous tentez de créer un analyseur de
       performances alors qu'il en existe déjà un, un message d'erreur
       apparaît.

       Pour appliquer les modifications, vous devez redémarrer le serveur.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible sur laquelle vous créez l'analyseur
           de performances. Les valeurs valides sont les suivantes :

           server
               Cette valeur crée l'analyseur de performances pour l'instance
               de serveur par défaut. Il s'agit de la valeur par défaut.

           configuration_name
               Cette valeur crée l'analyseur de performances pour la
               configuration nommée.

           cluster_name
               Cette valeur crée l'analyseur de performances pour chaque
               instance de serveur du cluster.

           instance_name
               Cette valeur crée l'analyseur de performances pour une instance
               de serveur particulière.

       --classpath
           Chaîne de variable d'environnement CLASSPATH Java qui indique les
           classes nécessaires pour l'analyseur de performances.

       --nativelibpath
           Ce chemin est automatiquement créé sous la forme d'une
           concaténation du chemin relatif d'installation des bibliothèques
           partagées natives GlassFish Server, du chemin de la bibliothèque
           native JRE standard, du paramètre d'environnement shell
           (LD_LIBRARY_PATH sous UNIX) et de tout chemin indiqué dans
           l'élément d'analyseur de performances.

       --enabled
           Par défaut, l'analyseur de performances est activé.

       --property
           Paires nom/valeur des attributs propres au fournisseur.

OPÉRANDES
       profiler_name
           Nom de l'analyseur de performances.

EXEMPLES
       Exemple 1, Création d'un analyseur de performances
           Cet exemple montre comment créer un analyseur de performances nommé
           sample_profiler.

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-profiler(1)

       asadmin(1M)

Java EE 7                         22 déc 2010               create-profiler(1)
