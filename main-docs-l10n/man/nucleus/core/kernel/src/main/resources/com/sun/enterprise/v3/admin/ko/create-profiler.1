create-profiler(1)        asadmin 유틸리티 하위 명령        create-profiler(1)

제목
       create-profiler - 프로파일러 요소 생성

시놉시스
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

설명
       create-profiler 하위 명령은 프로파일러 요소를 생성합니다. 서버
       인스턴스는 Java 구성의 프로파일러 요소에 의해 프로파일러에 접속됩니다.
       한 번에 하나의 프로파일러만 존재합니다. 하나의 프로파일러가 존재하는
       상태에서 프로파일러를 생성하려고 시도하면 오류 메시지가 표시됩니다.

       변경 사항을 적용하려면 서버를 재시작해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 프로파일러를 생성하는 대상을 지정합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스에 대한 프로파일러를 생성합니다. 이 값은
               기본값입니다.

           configuration_name
               이름이 지정된 구성에 대한 프로파일러를 생성합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 프로파일러를
               생성합니다.

           instance_name
               특정 서버 인스턴스에 대한 프로파일러를 생성합니다.

       --classpath
           프로파일러에서 필요로 하는 클래스를 지정하는 Java 클래스 경로
           문자열.

       --nativelibpath
           이 경로는 원시 공유 라이브러리에 대한 GlassFish Server 설치의 상대
           경로, 표준 JRE 원시 라이브러리 경로, 셸 환경 설정(UNIX에서는
           LD_LIBRARY_PATH) 및 프로파일러 요소에 지정할 수 있는 모든 경로가
           결합되도록 자동으로 구성됩니다.

       --enabled
           프로파일러는 기본적으로 사용으로 설정되어 있습니다.

       --property
           제공자 관련 속성의 이름/값 쌍입니다.

피연산자
       profiler_name
           프로파일러의 이름.

예제
       예 1, 프로파일러 생성
           이 예제는 sample_profiler라는 프로파일러를 생성합니다.

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-profiler(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일             create-profiler(1)
