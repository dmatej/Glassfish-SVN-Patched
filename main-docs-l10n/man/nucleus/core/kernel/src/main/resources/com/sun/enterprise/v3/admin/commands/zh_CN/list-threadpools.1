list-threadpools(1)         asadmin 实用程序子命令         list-threadpools(1)

名称
       list-threadpools - 列出所有线程池

提要
           list-threadpools [--help] target

说明
       list-threadpools 子命令列出 GlassFish Server 线程池。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           此操作数指定要列出线程池的目标。此操作数是必需的。

           有效值如下：

           server
               列出默认 GlassFish Server 实例 server 的线程池。

           configuration-name
               列出命名配置的线程池。

           cluster-name
               列出集群中每个实例的线程池。

           instance-name
               列出特定实例的线程池。

示例
       Example 1, 列出线程池
           此示例列出默认实例 server 的当前线程池。

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 20 日          list-threadpools(1)
