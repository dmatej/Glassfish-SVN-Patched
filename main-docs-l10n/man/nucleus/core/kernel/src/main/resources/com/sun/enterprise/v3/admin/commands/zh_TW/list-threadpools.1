list-threadpools(1)         asadmin 公用程式子命令         list-threadpools(1)

參考名
       list-threadpools - 列出所有的繫線集區

概要
           list-threadpools [--help] target

描述
       list-threadpools 子命令會列出 GlassFish Server 繫線集區。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           此運算元可指定您要列出繫線集區的目標。需要使用此運算元。

           有效的值如下：

           server
               列出預設 GlassFish Server 執行處理 server 的繫線集區。

           configuration-name
               列出已命名組態的繫線集區。

           cluster-name
               列出叢集內每個執行處理的繫線集區。

           instance-name
               列出特定執行處理的繫線集區。

範例
       範例 1, 列出繫線集區
           此範例會列出預設執行處理 server 目前的繫線集區。

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 20 日          list-threadpools(1)
