list-system-properties(1Subcomandos de la Utilidad aslist-system-properties(1)

NOMBRE DE REFERENCIA
       list-system-properties - muestra las propiedades de sistema de un
       dominio, configuración, cluster o instancia de servidor.

SINOPSIS
           list-system-properties [--help] [target]

DESCRIPCIÓN
       El subcomando list-system-properties muestra las propiedades del
       sistema de un dominio, configuración, cluster o instancia de servidor.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Restringe el listado de las propiedades de sistema a un destino
           específico. Los valores válidos son:

           domain
               Muestra las propiedades de sistema definidas para el dominio.

           configuration_name
               Muestra las propiedades del sistema definidas para la
               configuración especificada, así como las que el cluster hereda
               del dominio.

           cluster_name
               Muestra las propiedades del sistema definidas para el cluster
               especificado, así como las que el cluster hereda de su
               configuración y del dominio.

           instance_name
               Muestra las propiedades del sistema definidas para la instancia
               de servidor especificada, así como las que el servidor heredan
               de su cluster (si la instancia está en cluster), su
               configuración y el dominio.

EJEMPLOS
       Ejemplo 1, Lista de Propiedades del Sistema
           En este ejemplo se muestran las propiedades de sistema de
           localhost.

               asadmin> list-system-properties
               http-listener-port=1088
               Command list-system-properties executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-system-properties(1), delete-system-property(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de 2010  list-system-properties(1)
