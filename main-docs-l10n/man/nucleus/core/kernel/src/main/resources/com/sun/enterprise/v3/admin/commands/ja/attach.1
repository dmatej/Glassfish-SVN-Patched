attach(1)             asadminユーティリティのサブコマンド            attach(1)

名前
       attach - asadmin
       --detachを使用して起動されたサブコマンドまたは進捗情報を含むサブコマンドにアタッチします

概要
           attach  [--help]
           job_id

説明
       attachサブコマンドは、asadminユーティリティ・オプション--detachを使用して起動されたサブコマンドまたは進行情報を含むサブコマンドにアタッチします。--detachオプションを使用すると、長時間実行中のサブコマンドはデタッチされ、デタッチ・モードでバックグラウンドで実行されます。

       サブコマンド(ジョブ)にはジョブIDが割り当てられ、このIDをジョブのステータスやその出力を表示するために使用できます。list-jobs(1)サブコマンドを使用してジョブおよびそのジョブIDを表示し、configure-managed-jobs(1)サブコマンドを使用して保持するジョブに関する情報の長さを構成します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       job_id
           ステータスおよび出力を表示するジョブのID。

例
       例 1, サブコマンドへのアタッチとそのステータスのチェック
           この例では、ジョブIDが20のdeployサブコマンドにアタッチし、そのジョブが終了したことを示します。サブコマンドがまだ進行中の場合、出力には、64%:
           Uploading bitsなどのように現在のステータスが表示されます。

               asadmin> attach 20
               Finished execution of deploy
               Command attach executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       list-jobs(1), configure-managed-jobs(1)

       asadmin(1M)

Java EE 7                        2013年1月12日                       attach(1)
