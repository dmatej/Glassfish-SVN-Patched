delete-threadpool(1)    Subcomandos do utilitário asad    delete-threadpool(1)

NOME
       delete-threadpool - remove um pool de threads

SINOPSE
           delete-threadpool [--help] [--target target] threadpool-id

DESCRIçãO
       Remove o pool de threads com o ID especificado. Esse subcomando só é
       suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Essa opção especifica o alvo do qual você está deletando o pool de
           threads.

           Os valores válidos são os seguintes:

           server
               Deleta o pool de threads para a instância do servidor default
               do GlassFish Server e é o valor default.

           configuration-name
               Deleta o pool de threads da configuração nomeada.

           cluster-name
               Deleta o pool de threads de cada instância do cluster.

           instance-name
               Deleta o pool de threads de determinada instância.

OPERANDOS
       threadpool-id
           Um ID para a fila de trabalho, por exemplo, thread-pool1,
            threadpool-2 e assim por diante.

EXEMPLOS
       Example 1, Deletando um Pool de Threads
           Esse exemplo deleta threadpool-l.

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                         29 nov 2010             delete-threadpool(1)
