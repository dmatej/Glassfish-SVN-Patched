set(1)                  Subcomandos do utilitário asad                  set(1)

NOME
       set - define os valores de atributos configuráveis

SINOPSE
           set [--help] attribute-name=value

DESCRIçãO
       O subcomando set utiliza nomes com pontos para modificar os valores de
       um ou mais atributos configuráveis.

       Atributos na hierarquia de monitoramento são de somente para leitura,
       mas atributos configuráveis podem ser modificados. Você pode usar o
       subcomando list(1) para exibir nomes com pontos que representem
       subsistemas e componentes individuais do servidor. Por exemplo, um nome
       com pontos pode ser server.applications.web-module. Depois de descobrir
       um componente ou subsistema específico, é possível então utilizar o
       subcomando get para acessar os atributos. Para obter informações mais
       detalhadas sobre nomes com pontos, consulte a página de ajuda
       dotted-names(5ASC).

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Caracteres que tenham        │
           │           significado especial para o  │
           │           shell ou o interpretador de  │
           │           comandos, como asterisco     │
           │           (*), deverão estar entre     │
           │           aspas ou caracteres de       │
           │           escape de forma apropriada   │
           │           ao shell, por exemplo,       │
           │           colocando o argumento entre  │
           │           aspas. Em multimodo, aspas   │
           │           são necessárias somente para │
           │           argumentos que incluam       │
           │           espaços, aspas ou barra      │
           │           invertida.                   │
           └────────────────────────────────────────┘

       Ao modificar atributos, é possível ativar e desativar serviços, além de
       personalizar a forma como um elemento existente funciona. Um subcomando
       asadmin é fornecido para atualizar alguns elementos. Por exemplo,
       update-password-alias. No entanto, para atualizar outros elementos, é
       preciso utilizar o comando set. Por exemplo, um pool de conexões JDBC
       pode ser criado utilizando o subcomando create-jdbc-connection-pool .
       Para alterar definições de atributos posteriormente, é utilizado o
       comando set .

       Qualquer alteração utilizando os subcomandos do utilitário asadmin ou a
       Console de Administração são automaticamente aplicados ao arquivo de
       configuração do GlassFish Server associado.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       attribute-name=value
           Identifica o nome com pontos completo do nome do atributo e seu
           valor.

EXEMPLOS
       Example 1, Definição de um atributo do pool de conexões JDBC
           Esse exemplo altera o tamanho estável do pool de conexões DerbyPool
           para 9.

               asadmin> set resources.jdbc-connection-pool.DerbyPool.steady-pool-size=9
               Command set executed successfully.

       Example 2, Ativando o Serviço de Monitoramento de um Objeto Monitorável
           Esse exemplo ativa o monitoramento da JVM.

               asadmin> set server.monitoring-service.module-monitoring-levels.jvm=HIGH
               Command set executed successfully.

       Example 3, Ativando a Recuperação Automática para o Serviço de
       Transações
           Esse exemplo ativa a recuperação automática do serviço de
           transações.

               asadmin> set server.transaction-service.automatic-recovery=true
               Command set executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       get(1), list(1)

       dotted-names(5ASC)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide

Java EE 7                         10 set 2009                           set(1)
