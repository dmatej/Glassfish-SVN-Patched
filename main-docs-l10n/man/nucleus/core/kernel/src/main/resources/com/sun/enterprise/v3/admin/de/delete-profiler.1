delete-profiler(1)      Unterbefehle des asadmin-Utili      delete-profiler(1)

NAME
       delete-profiler - entfernt das Profiler-Element

ZUSAMMENFASSUNG
           delete-profiler [--help] [--target target_name]

BESCHREIBUNG
       Der Unterbefehl delete-profiler löscht das Profiler-Element in der
       Java-Konfiguration. Es kann immer nur jeweils ein Profiler vorhanden
       sein. Wenn Sie versuchen, einen Profiler zu erstellen, wenn bereits ein
       Profiler vorhanden ist, wird eine Fehlermeldung angezeigt, und der
       vorhandene Profiler muss gelöscht werden.

       Damit die Änderungen wirksam werden, muss der Server neu gestartet
       werden.

       Dieser Befehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Mit dieser Option wird das zu löschende Ziel-Profiler-Element
           angegeben. Gültige Werte sind

           server
               Löscht das Profiler-Element für die standardmäßige
               Serverinstanz server und ist der Standardwert.

           configuration_name
               Löscht das Profiler-Element für die benannte Konfiguration.

           cluster_name
               Löscht das Profiler-Element für jede Serverinstanz im Cluster.

           instance_name
               Löscht das Profiler-Element für eine bestimmte Serverinstanz.

BEISPIELE
       Beispiel 1, Profile löschen
           In diesem Beispiel wird der Profiler namens sample_profiler
           gelöscht.

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-profiler(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010              delete-profiler(1)
