delete-profiler(1)          asadmin 实用程序子命令          delete-profiler(1)

名称
       delete-profiler - 删除分析器元素

提要
           delete-profiler [--help] [--target target_name]

说明
       delete-profiler 子命令删除 Java
       配置中的分析器元素。在同一时间只能存在一个分析器。如果在已经存在一个分析器时尝试再创建一个，会显示一条错误消息，提示您必须删除现有分析器。

       要使更改生效，需要重新启动服务器。

       该命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要删除的目标分析器元素。有效值为

           server
               删除默认服务器实例 server 的分析器元素，这是默认值。

           configuration_name
               删除命名配置的分析器元素。

           cluster_name
               删除集群中每个服务器实例的分析器元素。

           instance_name
               删除特定服务器实例的分析器元素。

示例
       Example 1, 删除配置文件
           此示例删除名为 sample_profiler 的分析器。

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-profiler(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日           delete-profiler(1)
