delete-threadpool(1)        asadmin 公用程式子命令        delete-threadpool(1)

參考名
       delete-threadpool - 移除繫線集區

概要
           delete-threadpool [--help] [--target target] threadpool-id

描述
       移除具有指定 ID 的繫線集區。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項可指定要從中移除繫線集區的目標。

           有效的值如下：

           server
               刪除預設 GlassFish Server 執行處理 server
               的繫線集區，此為預設值。

           configuration-name
               刪除指定之組態的繫線集區。

           cluster-name
               刪除叢集中每個執行處理的繫線集區。

           instance-name
               刪除特定執行處理的繫線集區。

運算元
       threadpool-id
           工作佇列的 ID，例如 thread-pool1、threadpool-2 等等。

範例
       範例 1, 刪除繫線集區
           此範例會刪除 threadpool-l。

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                     2010 年 11 月 29 日         delete-threadpool(1)
