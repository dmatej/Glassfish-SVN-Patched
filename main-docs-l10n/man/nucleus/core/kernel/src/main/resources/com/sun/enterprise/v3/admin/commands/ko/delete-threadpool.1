delete-threadpool(1)      asadmin 유틸리티 하위 명령      delete-threadpool(1)

제목
       delete-threadpool - 스레드 풀 제거

시놉시스
           delete-threadpool [--help] [--target target] threadpool-id

설명
       지정된 ID를 가진 스레드 풀을 제거합니다. 이 하위 명령은 원격 모드에서만
       지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 스레드 풀을 제거할 대상을 지정합니다.

           적합한 값은 다음과 같습니다.

           server
               기본 GlassFish Server 인스턴스 server에 대한 스레드 풀을
               삭제하고 이는 기본값입니다.

           configuration-name
               명명된 구성에 대한 스레드 풀을 삭제합니다.

           cluster-name
               클러스터에 있는 모든 인스턴스에 대한 스레드 풀을 삭제합니다.

           instance-name
               특정 인스턴스에 대한 스레드 풀을 삭제합니다.

피연산자
       threadpool-id
           작업 대기열의 ID입니다(예: thread-pool1, threadpool-2 등).

예제
       예 1, 스레드 풀 삭제
           이 예제는 threadpool-l을 삭제합니다.

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                      2010년 11월 29일           delete-threadpool(1)
