list-system-properties(1)   asadmin 公用程式子命令   list-system-properties(1)

參考名
       list-system-properties -
       子命令會列出網域、組態、叢集或伺服器執行處理的系統特性。

概要
           list-system-properties [--help] [target]

描述
       list-system-properties
       子命令會列出網域、組態、叢集或伺服器執行處理的系統特性。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           此選項會將清單限制為特定目標的系統特性。有效值包括

           domain
               列出針對網域所定義的系統特性。

           configuration_name
               列出已命名之組態的系統特性，以及叢集從網域繼承的系統特性。

           cluster_name
               列出針對已命名之叢集所定義的系統特性，以及叢集從其組態與網域繼承的系統特性。

           instance_name
               列出針對已命名之伺服器執行處理所定義的系統特性，以及伺服器從其叢集
               (如果該執行處理為叢集執行處理)、組態與網域繼承的系統特性。

範例
       範例 1, 列出系統特性
           此範例會列出 localhost 的系統特性。

               asadmin> list-system-properties
               http-listener-port=1088
               Command list-system-properties executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-system-properties(1), delete-system-property(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日    list-system-properties(1)
