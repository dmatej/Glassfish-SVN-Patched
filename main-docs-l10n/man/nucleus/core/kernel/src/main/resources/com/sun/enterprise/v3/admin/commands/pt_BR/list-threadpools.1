list-threadpools(1)     Subcomandos do utilitário asad     list-threadpools(1)

NOME
       list-threadpools - lista todos os pools de threads

SINOPSE
           list-threadpools [--help] target

DESCRIçãO
       O subcomando list-threadpools lista os pools de threads do GlassFish
       Server.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Este operando especifica o alvo para o qual você está listando
           pools de threads. Este operando é necessário.

           Os valores válidos são os seguintes:

           server
               Lista os pools de threads para a instância do servidor do
               GlassFish Server default.

           configuration-name
               Lista os pools de threads para a configuração nomeada.

           cluster-name
               Lista os pools de threads para cada instância do cluster.

           instance-name
               Lista os pools de threads para determinada instância.

EXEMPLOS
       Example 1, Listando Pools de Threads
           Este exemplo lista os pools de threads atuais para a instância do
           servidor default.

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                         20 dez 2010              list-threadpools(1)
