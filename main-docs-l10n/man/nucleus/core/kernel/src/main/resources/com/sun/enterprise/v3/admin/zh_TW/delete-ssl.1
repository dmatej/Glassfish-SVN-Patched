delete-ssl(1)               asadmin 公用程式子命令               delete-ssl(1)

參考名
       delete-ssl - 刪除所選取 HTTP 監聽程式、IIOP 監聽程式或 IIOP 服務中的
       SSL 元素

概要
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

描述
       delete-ssl 子命令會刪除選取 HTTP 監聽程式、IIOP 監聽程式或 IIOP
       服務中的 SSL 元素。

       如果 --type 為 iiop-service，則不需要 listener_id。

       只有遠端模式支援這個子命令。

選項
       如果選項有短選項名稱，則短選項在長選項名稱之前。短選項有一個破折號，而長選項則有兩個破折號。

       --help, -?
           顯示子命令的說明文字。

       --target
           指定要在其中組態 ssl 元素的目標。以下為有效值：

           server
               指定要在其中為 SSL 取消組態 iiop-service 或 HTTP/IIOP
               監聽程式的伺服器。

           config
               指定包含要為其取消組態 SSL 的 HTTP/IIOP 監聽程式或 iiop-service
               的組態。

           cluster
               指定要在其中為 SSL 取消組態 HTTP/IIOP 監聽程式或 iiop-service
               的叢集。叢集中的所有伺服器執行處理將對各自的監聽程式或
               iiop-service 取消 SSL 組態。

           instance
               指定要在其中為 SSL 取消組態 HTTP/IIOP 監聽程式或 iiop-service
               的執行處理。

       --type
           要刪除其 SSL 的服務或監聽程式的類型。類型必須是下列其中一種：

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

運算元
       listener_id
           要從中刪除 SSL 元素的監聽程式的 ID。

           如果 --type 為 iiop-service，則不需要 listener_id 運算元。

範例
       範例 1, 從 HTTP 監聽程式刪除 SSL 元素
           下列範例會顯示如何從名為 http-listener-1 的 HTTP 監聽程式刪除 SSL
           元素。

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-ssl(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日                delete-ssl(1)
