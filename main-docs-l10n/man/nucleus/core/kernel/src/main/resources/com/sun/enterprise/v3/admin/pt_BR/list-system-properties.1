list-system-properties(1Subcomandos do utilitário asalist-system-properties(1)

NOME
       list-system-properties - lista as propriedades do sistema do domínio,
       configuração, cluster ou instância do servidor

SINOPSE
           list-system-properties [--help] [target]

DESCRIçãO
       O subcomando list-system-properties lista as propriedades do sistema de
       um domínio, configuração, cluster ou instância do servidor.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Isso restringe a listagem para as propriedades do sistema para um
           alvo específico. Os valores válidos são:

           domain
               Lista as propriedades do sistema definidas para o domínio.

           configuration_name
               Lista as propriedades do sistema para a configuração nomeada,
               assim como aquelas que o cluster herda do domínio.

           cluster_name
               Lista as propriedades do sistema definidas para o cluster
               nomeado, assim como as do cluster herdam de sua configuração e
               do domínio.

           instance_name
               Lista as propriedades do sistema definidas para a instância do
               servidor nomeado, assim como aquelas que o servidor herda do
               seu cluster (se a instância for clusterizada), da sua
               configuração e do domínio.

EXEMPLOS
       Example 1, Listando Propriedades do Sistema
           Este exemplo lista as propriedades do sistema em localhost.

               asadmin> list-system-properties
               http-listener-port=1088
               Command list-system-properties executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-system-properties(1), delete-system-property(1)

       asadmin(1M)

Java EE 7                   22 de dezembro de 2010   list-system-properties(1)
