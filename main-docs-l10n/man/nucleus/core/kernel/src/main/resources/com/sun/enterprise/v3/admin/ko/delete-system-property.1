delete-system-property(1) asadmin 유틸리티 하위 명령 delete-system-property(1)

제목
       delete-system-property - 한 번에 하나씩 도메인, 구성, 클러스터 또는
       서버 인스턴스의 시스템 속성 제거

시놉시스
           delete-system-property [--help] [--target target_name ]
           [property_name]

설명
       delete-system-property 하위 명령은 도메인, 구성, 클러스터 또는 서버
       인스턴스의 시스템 속성을 삭제합니다. 시스템 속성을 삭제하기 전에 구성의
       다른 부분에서 해당 시스템 속성을 참조하고 있지 않은지 확인합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 시스템 속성을 삭제할 대상을 지정합니다. 이 하위 명령에
           대한 적합한 대상은 인스턴스, 클러스터, 구성, 도메인 및 서버입니다.
           기본 옵션은 서버입니다.

피연산자
       property_name
           제거할 시스템 속성의 이름.

예제
       예 1, 시스템 속성 삭제
           이 예제는 http-listener-port라는 시스템 속성을 삭제합니다.

               asadmin> delete-system-property http-listener-port
               Command delete-system-property executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-system-properties(1), list-system-properties(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일      delete-system-property(1)
