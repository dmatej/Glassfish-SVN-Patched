delete-profiler(1)      Sous-commandes de l'utilitaire      delete-profiler(1)

NOM
       delete-profiler - enlève l'élément d'analyseur de performances

SYNTAXE
           delete-profiler [--help] [--target target_name]

DESCRIPTION
       La sous-commande delete-profiler supprime l'élément d'analyseur de
       performances de la configuration Java. Un seul analyseur de
       performances peut exister à la fois. Si vous tentez de créer un
       analyseur de performances alors qu'il en existe déjà un, un message
       d'erreur apparaît et l'analyseur de performances existant doit être
       supprimé.

       Pour appliquer les modifications, vous devez redémarrer le serveur.

       Cette commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique l'élément d'analyseur de performances cible à
           supprimer. Les valeurs valides sont les suivantes :

           server
               Supprime l'élément d'analyseur de performances de l'instance de
               serveur par défaut (server). Il s'agit de la valeur par défaut.

           configuration_name
               Supprime l'élément d'analyseur de performances pour la
               configuration nommée.

           cluster_name
               Supprime l'élément d'analyseur de performances pour chaque
               instance de serveur du cluster.

           instance_name
               Supprime l'élément d'analyseur de performances pour une
               instance de serveur donnée.

EXEMPLES
       Exemple 1, Suppression d'un analyseur de performances
           Dans cet exemple, l'analyseur de performances nommé sample_profiler
           est supprimé.

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-profiler(1)

       asadmin(1M)

Java EE 7                         22 déc 2010               delete-profiler(1)
