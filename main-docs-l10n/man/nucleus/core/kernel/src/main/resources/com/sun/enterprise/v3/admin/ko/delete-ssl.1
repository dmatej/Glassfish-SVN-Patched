delete-ssl(1)             asadmin 유틸리티 하위 명령             delete-ssl(1)

제목
       delete-ssl - 선택한 HTTP 리스너, IIOP 리스너 또는 IIOP 서비스에서 SSL
       요소 삭제

시놉시스
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

설명
       delete-ssl 하위 명령은 선택한 HTTP 리스너, IIOP 리스너 또는 IIOP
       서비스에서 SSL 요소를 삭제합니다.

       --type이 iiop-service인 경우 listener_id가 필요하지 않습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       옵션에 짧은 옵션 이름이 있는 경우 짧은 옵션이 긴 옵션 이름보다
       우선합니다. 짧은 옵션은 대시가 하나인 반면 긴 옵션은 대시가 두
       개입니다.

       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           ssl 요소를 구성할 대상을 지정합니다. 적합한 값은 다음과 같습니다.

           server
               SSL에 대한 iiop-service 또는 HTTP/IIOP 리스너의 구성을 해제할
               서버를 지정합니다.

           config
               SSL의 구성을 해제할 HTTP/IIOP 리스너 또는 iiop-service가 포함된
               구성을 지정합니다.

           cluster
               SSL에 대한 HTTP/IIOP 리스너 또는 iiop-service의 구성을 해제할
               클러스터를 지정합니다. 클러스터에 있는 모든 서버 인스턴스는 각
               리스너 또는 iiop-service에 대해 구성이 해제된 SSL을 가져옵니다.

           instance
               SSL에 대한 HTTP/IIOP 리스너 또는 iiop-service의 구성을 해제할
               인스턴스를 지정합니다.

       --type
           SSL을 삭제할 서비스 또는 리스너 유형입니다. 이 유형은 다음 유형 중
           하나여야 합니다.

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

피연산자
       listener_id
           SSL 요소를 삭제할 리스너의 ID입니다.

           --type이 iiop-service인 경우 listener_id 피연산자가 필요하지
           않습니다.

예제
       예 1, HTTP 리스너에서 SSL 요소 삭제
           다음 예제는 http-listener-1이라는 HTTP 리스너에서 SSL 요소를
           삭제하는 방법을 보여줍니다.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-ssl(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일                  delete-ssl(1)
