delete-threadpool(1)  asadminユーティリティのサブコマンド delete-threadpool(1)

名前
       delete-threadpool - スレッド・プールを除去します

概要
           delete-threadpool [--help] [--target target] threadpool-id

説明
       指定したIDのスレッド・プールを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションでは、スレッド・プールを除去するターゲットを指定します。

           有効な値は次のとおりです:

           server
               デフォルトのGlassFish Server
               インスタンスであるserverのスレッド・プールを削除します。これはデフォルト値です。

           configuration-name
               名前付き構成のスレッド・プールを削除します。

           cluster-name
               クラスタ内のすべてのインスタンスのスレッド・プールを削除します。

           instance-name
               特定のインスタンスのスレッド・プールを削除します。

オペランド
       threadpool-id
           作業キューのID(例: thread-pool1、threadpool-2など)。

例
       例 1, スレッド・プールの削除
           この例では、threadpool-lを削除します。

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                       2010年11月29日            delete-threadpool(1)
