delete-profiler(1)        asadmin 유틸리티 하위 명령        delete-profiler(1)

제목
       delete-profiler - 프로파일러 요소 제거

시놉시스
           delete-profiler [--help] [--target target_name]

설명
       delete-profiler 하위 명령은 Java 구성에서 프로파일러 요소를 삭제합니다.
       한 번에 하나의 프로파일러만 존재할 수 있습니다. 하나의 프로파일러가
       존재하는 상태에서 프로파일러를 생성하려고 시도하면 오류 메시지가
       표시되므로 기존 프로파일러를 삭제해야 됩니다.

       변경 사항을 적용하려면 서버를 재시작해야 합니다.

       이 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 삭제하는 대상 프로파일러 요소를 지정합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스 server에 대한 프로파일러 요소를 삭제하고
               이는 기본값입니다.

           configuration_name
               이름이 지정된 구성에 대한 프로파일러 요소를 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 프로파일러 요소를
               삭제합니다.

           instance_name
               특정 서버 인스턴스에 대한 프로파일러 요소를 삭제합니다.

예제
       예 1, 프로파일 삭제
           이 예제는 sample_profiler라는 프로파일러를 삭제합니다.

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-profiler(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일             delete-profiler(1)
