create-profiler(1)      Subcomandos de la Utilidad asa      create-profiler(1)

NOMBRE DE REFERENCIA
       create-profiler - Crea el elemento analizador de perfiles.

SINOPSIS
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

DESCRIPCIÓN
       El subcomando create-profiler crear el elemento analizador de perfiles.
       Una instancia de servidor se vincula al analizador de perfiles a través
       del elemento analizador de perfiles en la configuración de Java. Sólo
       existe un analizador de perfiles a la vez. Si intenta crear un
       analizador de perfiles cuando ya existe uno, se mostrará un mensaje de
       error.

       Para que los cambios surtan efecto, el servidor debe reiniciarse.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           La opción especifica el destino en el que va a crear el analizador
           de perfiles. Los valores válidos son:

           server
               Crea el analizador de perfiles para la instancia de servidor
               por defecto. Este es el valor por defecto.

           configuration_name
               Crea el analizador de perfiles para la configuración con
               nombre.

           cluster_name
               Crea el analizador de perfiles para cada instancia de servidor
               en el cluster.

           instance_name
               Crea el analizador de perfiles para una determinada instancia
               de servidor.

       --classpath
           Cadena de classpath de Java que especifica las clases que necesita
           el analizador de perfiles.

       --nativelibpath
           Esta ruta de acceso se construye de manera automática para que sea
           una concatenación de la ruta de acceso relativa de instalación de
           GlassFish Server para sus bibliotecas nativas compartidas, la ruta
           de acceso de la biblioteca nativa de JRE estándar, el valor del
           entorno del shell (LD_LIBRARY_PATH en UNIX) y cualquier ruta de
           acceso que se pueda especificar en el elemento de perfil.

       --enabled
           El analizador de perfiles se activa por defecto.

       --property
           Pares de nombre/valor de atributos específicos del proveedor.

OPERANDOS
       profiler_name
           Nombre del analizador de perfiles.

EJEMPLOS
       Ejemplo 1, Creación de un Analizador de Perfiles
           En este ejemplo se crea un analizador de perfiles denominado
           sample_profiler.

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-profiler(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de 2010         create-profiler(1)
