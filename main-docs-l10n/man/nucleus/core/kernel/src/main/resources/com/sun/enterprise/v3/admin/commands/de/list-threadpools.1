list-threadpools(1)     Unterbefehle des asadmin-Utili     list-threadpools(1)

NAME
       list-threadpools - listet alle Threadpools auf

ZUSAMMENFASSUNG
           list-threadpools [--help] target

BESCHREIBUNG
       Der Unterbefehl list-threadpools listet die GlassFish
       Server-Threadpools auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Dieser Operand gibt das Ziel an, für das Sie Threadpools auflisten.
           Dieser Operand ist erforderlich.

           Folgende Werte sind gültig:

           server
               Listet die Threadpools für die standardmäßige GlassFish
               Server-Instanz server auf.

           configuration-name
               Listet die Threadpools für die benannte Konfiguration auf.

           cluster-name
               Listet die Threadpools für jede Instanz im Cluster auf.

           instance-name
               Listet die Threadpools für eine bestimmte Instanz auf.

BEISPIELE
       Beispiel 1, Threadpools auflisten
           In diesem Beispiel werden die aktuellen Threadpools für die
           Standardinstanz server aufgelistet.

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                        20. Dez. 2010             list-threadpools(1)
