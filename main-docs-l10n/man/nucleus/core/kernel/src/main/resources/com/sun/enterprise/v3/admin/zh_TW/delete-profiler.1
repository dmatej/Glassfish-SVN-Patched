delete-profiler(1)          asadmin 公用程式子命令          delete-profiler(1)

參考名
       delete-profiler - 移除效能評測器元素

概要
           delete-profiler [--help] [--target target_name]

描述
       delete-profiler 子命令會刪除 Java
       組態中的效能評測器元素。一次只能存在一個效能評測器。
       如果您嘗試在存在一個效能評測器時建立另一個效能評測器，則會顯示錯誤訊息並且必須刪除現有的效能評測器。

       要讓變更生效，必須重新啟動伺服器。

       只有遠端模式支援此命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項指定您要刪除的目標效能評測器元素。有效值包括

           server
               刪除預設伺服器執行處理 server 的效能評測器元素，且此為預設值。

           configuration_name
               刪除指定組態的效能評測器元素。

           cluster_name
               刪除叢集中每個伺服器執行處理的效能評測器元素。

           instance_name
               刪除特定伺服器執行處理的效能評測器元素。

範例
       範例 1, 刪除設定檔
           此範例會刪除名為 sample_profiler 的效能評測器。

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-profiler(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日           delete-profiler(1)
