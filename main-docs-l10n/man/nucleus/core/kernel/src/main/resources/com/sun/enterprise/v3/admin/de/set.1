set(1)                  Unterbefehle des asadmin-Utili                  set(1)

NAME
       set - legt die Werte konfigurierbarer Attribute fest

ZUSAMMENFASSUNG
           set [--help] attribute-name=value

BESCHREIBUNG
       Der Unterbefehl set verwendet Namen mit Punkten, um die Werte eines
       oder mehrerer konfigurierbarer Attribute zu modifizieren.

       Attribute aus der Überwachungshierarchie sind schreibgeschützt.
       Konfigurationsattribute können geändert werden. Sie können den
       Unterbefehl list(1) verwenden, um die Namen mit Punkten anzuzeigen, die
       einzelne Serverkomponenten und Subsysteme darstellen. Beispiel: Ein
       Name mit Punkten kann server.applications.web-module lauten. Nachdem
       Sie die Komponente oder das Subsystem ermittelt haben, verwenden Sie
       den Unterbefehl get, um auf die Attribute zuzugreifen. Ausführlichere
       Informationen zu Namen mit Punkten finden Sie auf der Hilfeseite
       dotted-names(5ASC).

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Zeichen wie das Sternchen    │
           │           (*), die eine spezielle      │
           │           Bedeutung für die Shell oder │
           │           den Befehls-Interpreter      │
           │           haben, sollten entsprechend  │
           │           in Anführungszeichen gesetzt │
           │           oder von Escape-Zeichen      │
           │           angeführt werden,            │
           │           beispielsweise indem das     │
           │           Argument in                  │
           │           Anführungszeichen            │
           │           eingeschlossen wird. Im      │
           │           Mehrfachmodus werden         │
           │           Anführungszeichen nur für    │
           │           Argumente benötigt, die      │
           │           Leerzeichen,                 │
           │           Anführungszeichen oder       │
           │           umgekehrte Schrägstriche     │
           │           enthalten.                   │
           └────────────────────────────────────────┘

       Indem Sie Attribute modifizieren, können Sie Services aktivieren und
       deaktivieren und die Funktionsweise eines vorhandenen Elements
       anpassen. Mit dem asadmin-Unterbefehl können einige Elemente
       aktualisiert werden. Beispiel: update-password-alias. Um andere
       Elemente zu aktualisieren, verwenden Sie den Befehl set. Sie können
       beispielsweise einen JDBC-Connection Pool erstellen, indem Sie den
       Unterbefehl create-jdbc-connection-pool verwenden. Um
       Attributeinstellungen später zu ändern, verwenden Sie den Befehl set.

       Änderungen, die mithilfe der Unterbefehle des Utilitys asadmin oder der
       Administrationskonsole vorgenommen werden, werden automatisch auf die
       verknüpfte GlassFish Server-Konfigurationsdatei angewendet.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       attribute-name=value
           Gibt den vollständigen Namen mit Punkten des Attributnamens und
           dessen Wert an.

BEISPIELE
       Example 1, Attribute des JDBC-Connection Pools festlegen
           In diesem Beispiel wird die konstante Poolgröße des Connection
           Pools DerbyPool auf 9 geändert.

               asadmin> set resources.jdbc-connection-pool.DerbyPool.steady-pool-size=9
               Command set executed successfully.

       Example 2, Überwachungsservice für ein überwachbares Objekt aktivieren
           In diesem Beispiel wird die Überwachung für die JVM aktiviert.

               asadmin> set server.monitoring-service.module-monitoring-levels.jvm=HIGH
               Command set executed successfully.

       Example 3, Automatisches Recovery für den Transaktionsservice
       aktivieren
           In diesem Beispiel wird automatisches Recovery für den
           Transaktionsservice aktiviert.

               asadmin> set server.transaction-service.automatic-recovery=true
               Command set executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       get(1), list(1)

       dotted-names(5ASC)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide

Java EE 7                        10. Sep. 2009                          set(1)
