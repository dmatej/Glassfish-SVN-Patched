list-system-properties(1Comandi secondari della utililist-system-properties(1)

NOME
       list-system-properties - elenca le proprietà di sistema di un dominio,
       una configurazione, un cluster o un'istanza server

SINTASSI
           list-system-properties [--help] [target]

DESCRIZIONE
       Il comando secondario list-system-properties elenca le proprietà di
       sistema di un dominio, una configurazione, un cluster o un'istanza
       server.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Limita la lista delle proprietà di sistema per la destinazione
           specificata. Di seguito sono riportati i valori validi.

           domain
               Elenca le proprietà di sistema definite per il dominio.

           configuration_name
               Elenca le proprietà di sistema per la configurazione
               specificata, nonché quelle che il cluster ereditate dal
               dominio.

           cluster_name
               Elenca le proprietà di sistema definite per il cluster
               specificato, nonché quelle che il cluster eredita dalla
               configurazione e dal dominio.

           instance_name
               Elenca le proprietà di sistema definite per l'istanza server
               specificata, nonché quelle che il server eredita dal cluster
               (se l'istanza è contenuta in un cluster), dalla configurazione
               e dal dominio.

ESEMPI
       Esempio 1, Visualizzazione della lista delle proprietà di sistema
           In questo esempio vengono elencate le proprietà di sistema di
           localhost.

               asadmin> list-system-properties
               http-listener-port=1088
               Command list-system-properties executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-system-properties(1), delete-system-property(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010      list-system-properties(1)
