delete-system-property(1Sous-commandes de l'utilitairdelete-system-property(1)

NOM
       delete-system-property - enlève une seule propriété système du domaine,
       de la configuration, du cluster ou de l'instance de serveur à la fois

SYNTAXE
           delete-system-property [--help] [--target target_name ]
           [property_name]

DESCRIPTION
       La sous-commande delete-system-properties supprime une propriété
       système d'un domaine, d'une configuration, d'un cluster ou d'une
       instance de serveur. Assurez-vous que la propriété système n'est
       référencée nulle part ailleurs dans la configuration avant de la
       supprimer.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible sur laquelle supprimer les
           propriétés système. Les cibles valides pour cette sous-commande
           sont les suivantes : instance, cluster, configuration, domain et
           server. L'option par défaut est server.

OPÉRANDES
       property_name
           Nom de la propriété système à enlever.

EXEMPLES
       Exemple 1, Suppression d'une propriété système
           Dans cet exemple, la propriété système nommée http-listener-port
           est supprimée.

               asadmin> delete-system-property http-listener-port
               Command delete-system-property executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-system-properties(1), list-system-properties(1)

       asadmin(1M)

Java EE 7                         22 déc 2010        delete-system-property(1)
