delete-ssl(1)           Comandi secondari della utilit           delete-ssl(1)

NOME
       delete-ssl - elimina l'elemento SSL nel listener HTTP, nel listener
       IIOP o nel servizio IIOP selezionato

SINTASSI
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

DESCRIZIONE
       Il comando secondario delete-ssl elimina l'elemento SSL nel listener
       HTTP, nel listener IIOP o nel servizio IIOP selezionato.

       L'operando listener_id non è necessario se l'opzione --type è impostata
       su iiop-service.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       Se per un'opzione è disponibile un'abbreviazione, questa precede il
       nome esteso. Le opzioni brevi presentano una lineetta, mentre le
       opzioni lunghe ne hanno due.

       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione in cui viene configurato l'elemento SSL.
           Di seguito sono riportati i valori validi.

           server
               Specifica il server in cui deve essere annullata la
               configurazione del servizio IIOP o del listener HTTP/IIOP per
               l'elemento SSL.

           config
               Specifica la configurazione che contiene il listener HTTP/IIOP
               o il servizio IIOP per cui deve essere annullata la
               configurazione dell'elemento SSL.

           cluster
               Specifica il cluster in cui deve essere annullata la
               configurazione del listener HTTP/IIOP o del servizio IIOP per
               l'elemento SSL. In tutte le istanze server del cluster verrà
               annullata la configurazione dell'elemento SSL per il listener o
               il servizio IIOP rispettivo.

           instance
               Specifica l'istanza in cui deve essere annullata la
               configurazione del listener HTTP/IIOP o del servizio IIOP per
               l'elemento SSL.

       --type
           Il tipo di servizio o di listener per cui viene eliminato
           l'elemento SSL. Il tipo deve essere uno dei tipi riportati di
           seguito.

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPERANDI
       listener_id
           L'ID del listener da cui deve essere eliminato l'elemento SSL.

           L'operando listener_id non è necessario se si imposta --type su
           iiop-service.

ESEMPI
       Esempio 1, Eliminazione di un elemento SSL da un listener HTTP
           Nell'esempio riportato di seguito viene illustrato come eliminare
           un elemento SSL da un listener HTTP denominato http-listener-1.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-ssl(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010                  delete-ssl(1)
