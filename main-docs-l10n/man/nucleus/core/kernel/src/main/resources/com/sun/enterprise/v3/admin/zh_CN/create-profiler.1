create-profiler(1)          asadmin 实用程序子命令          create-profiler(1)

名称
       create-profiler - 创建分析器元素

提要
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

说明
       create-profiler 子命令创建分析器元素。服务器实例通过 Java
       配置中的分析器元素绑定到分析器。在同一时间只能存在一个分析器。如果在已存在一个分析器时尝试再创建一个，将显示错误消息。

       要使更改生效，需要重新启动服务器。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定创建分析器的目标。有效值为

           server
               为默认的服务器实例创建分析器。这是默认值。

           configuration_name
               为命名配置创建分析器

           cluster_name
               为集群中的每个服务器实例创建分析器

           instance_name
               为特定服务器实例创建分析器

       --classpath
           指定分析器所需类的 Java 类路径字符串。

       --nativelibpath
           此路径自动构造成由以下路径串联而成：GlassFish Server
           安装本机共享库的相对路径、标准 JRE 本机库路径、shell 环境设置 (在
           UNIX 上为 LD_LIBRARY_PATH) 以及任何可在配置文件元素中指定的路径。

       --enabled
           默认情况下，分析器处于启用状态。

       --property
           特定于提供方的属性的名称/值对。

操作数
       profiler_name
           分析器的名称。

示例
       Example 1, 创建分析器
           此示例创建名为 sample_profiler 的分析器。

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-profiler(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日           create-profiler(1)
