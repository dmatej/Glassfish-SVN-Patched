delete-ssl(1)               asadmin 实用程序子命令               delete-ssl(1)

名称
       delete-ssl - 删除所选 HTTP 监听程序、IIOP 监听程序或 IIOP 服务中的 SSL
       元素

提要
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

说明
       delete-ssl 子命令删除所选 HTTP 监听程序、IIOP 监听程序或 IIOP 服务中的
       SSL 元素。

       如果 --type 为 iiop-service，则不需要 listener_id。

       该子命令仅在远程模式下受支持。

选项
       如果某个选项具有短选项名称，则短选项优先于长选项名称。短选项具有一个短划线，而长选项具有两个短划线。

       --help, -?
           显示子命令的帮助文本。

       --target
           指定配置 SSL 元素的目标。以下是有效值：

           server
               指定要在其中为 iiop-service 或 HTTP/IIOP 监听程序取消 SSL
               配置的服务器。

           config
               指定包含要取消 SSL 配置的 HTTP/IIOP 监听程序或 iiop-service
               的配置。

           cluster
               指定要在其中为 HTTP/IIOP 监听程序或 iiop-service 取消 SSL
               配置的集群。集群中所有服务器实例的相应监听程序或 iiop-service
               都将取消 SSL 配置。

           instance
               指定要在其中为 HTTP/IIOP 监听程序或 iiop-service 取消 SSL
               配置的实例。

       --type
           要删除 SSL 的服务或监听程序的类型。此类型必须是下列类型之一：

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

操作数
       listener_id
           要从中删除 SSL 元素的监听程序的 ID。

           如果 --type 为 iiop-service，则不需要 listener_id 操作数。

示例
       Example 1, 从 HTTP 监听程序删除 SSL 元素
           以下示例说明如何从名为 http-listener-1 的 HTTP 监听程序删除 SSL
           元素。

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-ssl(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日                delete-ssl(1)
