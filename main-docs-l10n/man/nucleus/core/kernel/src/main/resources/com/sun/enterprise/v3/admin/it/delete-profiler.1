delete-profiler(1)      Comandi secondari della utilit      delete-profiler(1)

NOME
       delete-profiler - rimuove l'elemento profiler

SINTASSI
           delete-profiler [--help] [--target target_name]

DESCRIZIONE
       Il comando secondario delete-profiler elimina l'elemento profiler nella
       configurazione Java. Può esistere un solo profiler. Se si tenta di
       creare un profiler quando ne esiste già uno, viene visualizzato un
       messaggio di errore e il profiler esistente deve essere eliminato.

       Per rendere effettive le modifiche, è necessario riavviare il server.

       Questo comando è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica l'elemento profiler di destinazione che si
           desidera eliminare. Di seguito sono riportati i valori validi.

           server
               Elimina l'elemento profiler dell'istanza server predefinita
               server ed è il valore predefinito.

           configuration_name
               Elimina l'elemento profiler della configurazione specificata.

           cluster_name
               Elimina l'elemento profiler di tutte le istanze server del
               cluster.

           instance_name
               Elimina l'elemento profiler per un'istanza server particolare.

ESEMPI
       Esempio 1, Eliminazione di un profiler
           In questo esempio viene eliminato il profiler denominato
           sample_profiler.

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-profiler(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010             delete-profiler(1)
