list-threadpools(1)     Comandi secondari della utilit     list-threadpools(1)

NOME
       list-threadpools - elenca tutti i pool di thread

SINTASSI
           list-threadpools [--help] target

DESCRIZIONE
       Il comando secondario list-threadpools elenca i pool di thread di
       GlassFish Server.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Questo operando indica la destinazione per cui si elencano i pool
           di thread. Questo operando è obbligatorio.

           Di seguito sono riportati i valori validi.

           server
               Elenca i pool di thread per l'istanza di GlassFish Server
               predefinita server.

           configuration-name
               Elenca i pool di thread per la configurazione specificata.

           cluster-name
               Elenca i pool di thread per tutte le istanze del cluster.

           instance-name
               Elenca i pool di thread per un'istanza particolare.

ESEMPI
       Esempio 1, Visualizzazione della lista dei pool di thread
           Questo esempio elenca i pool di thread correnti per l'istanza
           predefinita server.

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                      20 dicembre 2010            list-threadpools(1)
