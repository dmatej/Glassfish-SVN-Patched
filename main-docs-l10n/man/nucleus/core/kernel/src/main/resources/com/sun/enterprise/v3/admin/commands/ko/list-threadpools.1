list-threadpools(1)       asadmin 유틸리티 하위 명령       list-threadpools(1)

제목
       list-threadpools - 모든 스레드 풀 나열

시놉시스
           list-threadpools [--help] target

설명
       list-threadpools 하위 명령은 GlassFish Server 스레드 풀을 나열합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           이 피연산자는 스레드 풀을 나열할 대상을 지정합니다. 이 피연산자는
           필수입니다.

           적합한 값은 다음과 같습니다.

           server
               기본 GlassFish Server 인스턴스 server에 대한 스레드 풀을
               나열합니다.

           configuration-name
               명명된 구성에 대한 스레드 풀을 나열합니다.

           cluster-name
               클러스터에 있는 모든 인스턴스에 대한 스레드 풀을 나열합니다.

           instance-name
               특정 인스턴스에 대한 스레드 풀을 나열합니다.

예제
       예 1, 스레드 풀 나열
           이 예제는 기본 인스턴스 server의 현재 스레드 풀을 나열합니다.

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                      2010년 12월 20일            list-threadpools(1)
