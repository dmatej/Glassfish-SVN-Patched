delete-system-property(1Unterbefehle des asadmin-Utildelete-system-property(1)

NAME
       delete-system-property - entfernt jeweils eine Systemeigenschaft der
       Domain, der Konfiguration, des Clusters oder der Serverinstanz

ZUSAMMENFASSUNG
           delete-system-property [--help] [--target target_name ]
           [property_name]

BESCHREIBUNG
       Der Unterbefehl delete-system-property löscht eine Systemeigenschaft
       einer Domain, einer Konfiguration, eines Clusters oder einer
       Serverinstanz. Stellen Sie sicher, dass die Systemeigenschaft nicht an
       anderer Stelle in der Konfiguration referenziert ist, bevor Sie sie
       löschen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, auf dem Sie die Systemeigenschaften
           löschen. Die gültigen Ziele für diesen Unterbefehl sind "instance",
           "cluster", "configuration", "domain" und "server". Server ist die
           Standardoption.

OPERANDEN
       property_name
           Der Name der zu entfernenden Systemeigenschaft.

BEISPIELE
       Example 1, Systemeigenschaften löschen
           In diesem Beispiel wird die Systemeigenschaft namens
           http-listener-port gelöscht.

               asadmin> delete-system-property http-listener-port
               Command delete-system-property executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-system-properties(1), list-system-properties(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010       delete-system-property(1)
