delete-threadpool(1)        asadmin 实用程序子命令        delete-threadpool(1)

名称
       delete-threadpool - 删除线程池

提要
           delete-threadpool [--help] [--target target] threadpool-id

说明
       删除具有指定 ID 的线程池。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要从中删除线程池的目标。

           有效值如下：

           server
               删除默认 GlassFish Server 实例 server 的线程池，这是默认值。

           configuration-name
               删除命名配置的线程池。

           cluster-name
               删除集群中每个实例的线程池。

           instance-name
               删除特定实例的线程池。

操作数
       threadpool-id
           工作队列的 ID，例如，thread-pool1、threadpool-2 等等。

示例
       Example 1, 删除线程池
           此示例删除 threadpool-l。

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                     2010 年 11 月 29 日         delete-threadpool(1)
