delete-ssl(1)           Sous-commandes de l'utilitaire           delete-ssl(1)

NOM
       delete-ssl - supprime l'élément SSL dans le processus d'écoute HTTP, le
       processus d'écoute IIOP ou le service IIOP sélectionné

SYNTAXE
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

DESCRIPTION
       La sous-commande delete-ssl supprime l'élément SSL dans le processus
       d'écoute HTTP, le processus d'écoute IIOP ou le service IIOP
       sélectionné.

       L'élément listener_id n'est pas obligatoire si le type est
       iiop-service.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       Si une option possède un nom court, celui-ci précède le nom d'option
       long. Les options courtes contiennent un tiret alors que les options
       longues en contiennent deux.

       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible sur laquelle vous configurez
           l'élément SSL. Les valeurs valides sont les suivantes :

           server
               Spécifie le serveur sur lequel la configuration du service IIOP
               ou du processus d'écoute HTTP/IIOP doit être annulée pour SSL.

           config
               Spécifie la configuration contenant le processus d'écoute
               HTTP/IIOP ou le service IIOP pour lequel la configuration SSL
               doit être annulée.

           cluster
               Spécifie le cluster dans lequel la configuration du processus
               d'écoute HTTP/IIOP ou du service IIOP doit être annulée pour
               SSL. La configuration SSL sera annulée sur toutes les instances
               de serveur du cluster pour le processus d'écoute ou le service
               IIOP respectif.

           instance
               Spécifie l'instance dans laquelle la configuration du processus
               d'écoute HTTP/IIOP ou du service IIOP doit être annulée pour
               SSL.

       --type
           Type de service ou de processus d'écoute pour lequel SSL est
           supprimé. Il doit s'agir de l'un des types suivants :

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPÉRANDES
       listener_id
           ID du processus d'écoute duquel l'élément SSL doit être supprimé.

           L'opérande listener_id n'est pas requis si l'option --type a la
           valeur iiop-service.

EXEMPLES
       Exemple 1, Suppression d'un élément SSL d'un processus d'écoute HTTP
           Dans cet exemple, un élément SSL est supprimé d'un processus
           d'écoute HTTP nommé http-listener-1.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-ssl(1)

       asadmin(1M)

Java EE 7                         22 déc 2010                    delete-ssl(1)
