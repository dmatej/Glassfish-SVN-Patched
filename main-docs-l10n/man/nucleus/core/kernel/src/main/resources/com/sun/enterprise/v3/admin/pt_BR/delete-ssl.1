delete-ssl(1)           Subcomandos do utilitário asad           delete-ssl(1)

NOME
       delete-ssl - deleta o elemento SSL do listener HTTP, do listener IIOP
       ou do serviço IIOP selecionado

SINOPSE
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

DESCRIçãO
       O subcomando delete-ssl deleta o elemento SSL no listener HTTP,
       listener IIOP ou do serviço IIOP selecionado.

       O listener_id não será necessário se --type for iiop-service.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       Se uma opção tiver um nome de opção curto, a mesma precederá o nome de
       opção longo. As opções curtas possuem um traço, enquanto as opções
       longas possuem dois traços.

       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo no qual o elemento SSL está sendo configurado. Os
           valores a seguir são válidos:

           server
               Especifica o servidor no qual o serviço IIOP ou o listener
               HTTP/IIOP será desconfigurado para SSL.

           config
               Especifica a configuração que contém o listener HTTP/IIOP ou o
               serviço IIOP para o qual o SSL será desconfigurado.

           cluster
               Especifica o cluster no qual o listener HTTP/IIOP ou o serviço
               IIOP será desconfigurado para SSL. Todas as instâncias do
               servidor no cluster terão o SSL desconfigurado para o
               respectivo listener ou serviço IIOP.

           instance
               Especifica a instância na qual o listener HTTP/IIOP ou o
               serviço IIOP será desconfigurado para SSL.

       --type
           O tipo de serviço ou listener para o qual o SSL é deletado. O tipo
           deve ser um dos seguintes:

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPERANDOS
       listener_id
           O ID do listener do qual o elemento SSL será deletado.

           O operando listener_id não será obrigatório se --type for
           iiop-service.

EXEMPLOS
       Example 1, Deletando um elemento SSL de um listener HTTP
           O exemplo a seguir exibe como deletar um elemento SSL de um
           listener HTTP com o nome http-listener-1.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-ssl(1)

       asadmin(1M)

Java EE 7                   22 de dezembro de 2010               delete-ssl(1)
