delete-ssl(1)           Unterbefehle des asadmin-Utili           delete-ssl(1)

NAME
       delete-ssl - löscht das SSL-Element im gewählten HTTP-Listener,
       IIOP-Listener oder IIOP-Service

ZUSAMMENFASSUNG
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

BESCHREIBUNG
       Der Unterbefehl delete-ssl löscht das SSL-Element im gewählten
       HTTP-Listener, IIOP-Listener oder IIOP-Service.

       Die listener_id ist nicht erforderlich, wenn der Typ (--type)
       iiop-service ist.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       Wenn eine Option einen kurzen Optionsnamen hat, geht die kurze Option
       dem langen Optionsnamen voraus. Kurze Optionen haben einen Bindestrich,
       lange Optionen haben zwei Bindestriche.

       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, für das Sie das SSL-Element konfigurieren.
           Folgende Werte sind gültig:

           server
               Gibt den Server an, auf dem der IIOP-Service oder der
               HTTP/IIOP-Listener für SSL dekonfiguriert werden soll.

           config
               Gibt die Konfiguration an, die den HTTP/IIOP-Listener oder den
               IIOP-Service enthält, für den SSL dekonfiguriert werden soll.

           cluster
               Gibt das Cluster an, in dem der HTTP/IIOP-Listener oder der
               IIOP-Service für SSL dekonfiguriert werden soll. Auf allen
               Serverinstanzen im Cluster wird die Konfiguration von SSL für
               den entsprechenden Listener oder den entsprechenden
               IIOP-Service aufgehoben.

           instance
               Gibt die Instanz an, in der der HTTP/IIOP-Listener oder der
               IIOP-Service für SSL dekonfiguriert werden soll.

       --type
           Der Typ des Service oder Listeners, für den SSL gelöscht wird. Der
           Typ muss einer der folgenden Typen sein:

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPERANDEN
       listener_id
           Die ID des Listeners, aus dem das SSL-Element gelöscht werden soll.

           Der Operand listener_id ist nicht erforderlich, wenn der Typ
           (--type) iiop-service ist.

BEISPIELE
       Beispiel 1, SSL-Elemente aus einem HTTP-Listener löschen
           Das folgende Beispiel zeigt, wie ein SSL-Element aus einem
           HTTP-Listener namens http-listener-1 gelöscht wird.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-ssl(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010                   delete-ssl(1)
