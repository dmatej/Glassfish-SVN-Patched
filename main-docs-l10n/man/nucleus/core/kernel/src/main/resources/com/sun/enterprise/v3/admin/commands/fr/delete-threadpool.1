delete-threadpool(1)    Sous-commandes de l'utilitaire    delete-threadpool(1)

NOM
       delete-threadpool - enlève un pool de threads

SYNTAXE
           delete-threadpool [--help] [--target target] threadpool-id

DESCRIPTION
       Enlève le pool de threads portant l'ID spécifié. Cette sous-commande
       est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible de laquelle enlever le pool de
           threads.

           Les valeurs valides sont les suivantes :

           server
               Supprime le pool de threads pour l'instance GlassFish Server
               par défaut (server). Il s'agit de la valeur par défaut.

           configuration-name
               Supprime le pool de threads pour la configuration nommée.

           cluster-name
               Supprime le pool de threads pour chaque instance du cluster.

           instance-name
               Supprime le pool de threads pour une instance donnée.

OPÉRANDES
       threadpool-id
           ID de la file d'attente de travail, par exemple, thread-pool1,
           threadpool-2, et ainsi de suite.

EXEMPLES
       Exemple 1, Suppression d'un pool de threads
           Dans cet exemple, l'élément threadpool-l est supprimé.

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

Java EE 7                         29 nov 2010             delete-threadpool(1)
