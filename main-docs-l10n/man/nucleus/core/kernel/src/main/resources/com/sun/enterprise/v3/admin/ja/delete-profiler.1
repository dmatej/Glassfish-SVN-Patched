delete-profiler(1)    asadminユーティリティのサブコマンド   delete-profiler(1)

名前
       delete-profiler - プロファイラ要素を除去します

概要
           delete-profiler [--help] [--target target_name]

説明
       delete-profilerサブコマンドは、Java構成のプロファイラ要素を削除します。プロファイラは一度に1つしか存在できません。プロファイラがすでに存在するときにプロファイラを作成しようとすると、エラー・メッセージが表示され、既存のプロファイラを削除する必要があります。

       変更を有効にするには、サーバーを再起動する必要があります。

       このコマンドは、リモート・モードでのみサポートされます。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションには、削除するターゲット・プロファイラ要素を指定します。有効な値は次のとおりです

           server
               デフォルトのサーバー・インスタンスserverのプロファイラ要素を削除します。これはデフォルト値です。

           configuration_name
               名前付き構成のプロファイラ要素を削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのプロファイラ要素を削除します。

           instance_name
               特定のサーバー・インスタンスのプロファイラ要素を削除します。

例
       例 1, プロファイルの削除
           この例では、sample_profilerという名前のプロファイラを削除します。

               asadmin> delete-profiler sample_profiler
               Command delete-profiler executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-profiler(1)

       asadmin(1M)

Java EE 7                       2010年12月22日              delete-profiler(1)
