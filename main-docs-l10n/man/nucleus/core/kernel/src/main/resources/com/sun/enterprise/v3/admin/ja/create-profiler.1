create-profiler(1)    asadminユーティリティのサブコマンド   create-profiler(1)

名前
       create-profiler - プロファイラ要素を作成します

概要
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

説明
       create-profilerサブコマンドは、プロファイラ要素を作成します。サーバー・インスタンスは、Java構成のプロファイラ要素によってプロファイラに関連付けられています。プロファイラは一度に1つしか存在しません。プロファイラがすでに存在するときにプロファイラを作成しようとすると、エラー・メッセージが表示されます。

       変更を有効にするには、サーバーを再起動する必要があります。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションにはプロファイラを作成するターゲットを指定します。有効な値は次のとおりです

           server
               デフォルトのサーバー・インスタンスにプロファイラを作成します。この値がデフォルト値です。

           configuration_name
               名前付き構成のプロファイラを作成します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスにプロファイラを作成します。

           instance_name
               特定のサーバー・インスタンスにプロファイラを作成します。

       --classpath
           プロファイラが必要とするクラスを指定するJavaクラスパス文字列。

       --nativelibpath
           このパスは、GlassFish
           Serverインストールのネイティブ共有ライブラリの相対パス、標準JREネイティブ・ライブラリ・パス、シェル環境設定(UNIX上のLD_LIBRARY_PATH)、およびプロファイル要素に指定されている可能性のあるパスの連結として自動的に構築されます。

       --enabled
           プロファイラはデフォルトで使用可能になっています。

       --property
           プロバイダ固有の属性の名前と値のペア。

オペランド
       profiler_name
           プロファイラの名前。

例
       例 1, プロファイラの作成
           この例では、sample_profilerという名前のプロファイラを作成します。

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-profiler(1)

       asadmin(1M)

Java EE 7                       2010年12月22日              create-profiler(1)
