delete-ssl(1)         asadminユーティリティのサブコマンド        delete-ssl(1)

名前
       delete-ssl -
       選択したHTTPリスナー、IIOPリスナー、またはIIOPサービス内のSSL要素を削除します

概要
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

説明
       delete-sslサブコマンドは、選択したHTTPリスナー、IIOPリスナー、またはIIOPサービス内のSSL要素を削除します。

       --typeがiiop-serviceの場合、listener_idは必須ではありません。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       オプションに短いオプション名がある場合は、長いオプション名よりも前に来ます。短形式のオプションにはダッシュが(-)が、長形式のオプションには二重ダッシュ(--)が付いています。

       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           ssl要素を構成するターゲットを指定します。次の値が有効です。

           server
               iiop-serviceまたはHTTP/IIOPリスナーのSSLを構成解除するサーバーを指定します。

           config
               SSLを構成解除するHTTP/IIOPリスナーまたはiiop-serviceを含む構成を指定します。

           cluster
               HTTP/IIOPリスナーまたはiiop-serviceのSSLを構成解除するクラスタを指定します。クラスタ内のすべてのサーバー・インスタンスで、それぞれのリスナーまたはiiop-serviceのSSLが構成解除されます。

           instance
               HTTP/IIOPリスナーまたはiiop-serviceのSSLを構成解除するインスタンスを指定します。

       --type
           SSLを削除するサービスまたはリスナーのタイプ。typeには次のいずれかのタイプを指定する必要があります。

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

オペランド
       listener_id
           SSL要素を削除するリスナーのID。

           --typeがiiop-serviceの場合、listener_idオペランドは必須ではありません。

例
       例 1, HTTPリスナーからのSSL要素の削除
           次の例は、http-listener-1という名前のHTTPリスナーからSSL要素を削除する方法を示しています。

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-ssl(1)

       asadmin(1M)

Java EE 7                       2010年12月22日                   delete-ssl(1)
