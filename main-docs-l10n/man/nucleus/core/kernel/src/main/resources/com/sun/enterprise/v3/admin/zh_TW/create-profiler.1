create-profiler(1)          asadmin 公用程式子命令          create-profiler(1)

參考名
       create-profiler - 建立效能評測器元素

概要
           create-profiler [--help] [--target target_name]
           [--classpath classpath] [--nativelibpath native_library_path] [--enabled=true]
           [--property(name=value)[:name=value]*] profiler_name

描述
       create-profiler 子命令會建立效能評測器元素。伺服器執行處理會依據 Java
       組態中的效能評測器元素，連接至效能評測器。一次只能存在一個效能評測器。如果您試圖在已存在效能評測器時建立效能評測器，則會顯示錯誤訊息。

       要讓變更生效，必須重新啟動伺服器。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項指定要建立效能評測器的目標。有效值包括

           server
               建立預設伺服器執行處理的效能評測器。此為預設值。

           configuration_name
               為已命名的組態建立效能評測器

           cluster_name
               為叢集中的每個伺服器執行處理建立效能評測器

           instance_name
               為特定的伺服器執行處理建立效能評測器

       --classpath
           可指定效能評測器所需類別的 Java 類別路徑字串。

       --nativelibpath
           自動建構此路徑時，會串連其原生共用程式庫的 GlassFish Server
           安裝相對路徑、標準 JRE 原生程式庫路徑、shell 環境設定 (在 UNIX 上為
           LD_LIBRARY_PATH)，及設定檔元素中可指定的任何路徑。

       --enabled
           效能評測器預設值為啟用。

       --property
           提供者特定屬性的名/值配對。

運算元
       profiler_name
           效能評測器名稱。

範例
       範例 1, 建立效能評測器
           此範例會建立一個名為 sample_profiler 的效能評測器。

               asadmin> create-profiler --classpath /home/appserver/
               --nativelibpath /u/home/lib --enabled=false
               --property defaultuser=admin:password=adminadmin sample_profiler
               Created Profiler with id = sample_profiler

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-profiler(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日           create-profiler(1)
