delete-ssl(1)           Subcomandos de la Utilidad asa           delete-ssl(1)

NOMBRE DE REFERENCIA
       delete-ssl - Suprime el elemento SSL del listener HTTP, listener IIOP o
       servicio IIOP seleccionado.

SINOPSIS
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

DESCRIPCIÓN
       El subcomando delete-ssl suprime el elemento SSL del listener de HTTP,
       listener IIOP o servicio IIOP seleccionado.

       No es necesario especificar listener_id si --type es iiop-service.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       Si una opción tiene un nombre corto, este nombre tiene prioridad sobre
       el nombre largo. Las opciones cortas están marcadas con un guión; las
       largas, con dos guiones.

       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino en el que se va a configurar el elemento ssl.
           Los valores válidos son:

           server
               Especifica el servidor en el que el servicio IIOP o el listener
               HTTP o listener IIOP se va a desconfigurar para SSL.

           config
               Especifica la configuración que contiene el listener HTTP/IIOP
               o el servicio IIOP para el que se va a desconfigurar SSL.

           cluster
               Especifica el cluster en el que el listener HTTP/IIOP o el
               servicio IIOP se va a desconfigurar para SSL. En todas las
               instancias de servidor del cluster se desconfigurará SSL para
               los respectivos listeners o servicios IIOP.

           instance
               Especifica la instancia en la que el listener HTTP/IIOP o el
               servicio IIOP se va a desconfigurar para SSL.

       --type
           Tipo de servicio o listener para el que se va a suprimir el SSL. El
           tipo debe ser uno de los siguientes:

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPERANDOS
       listener_id
           ID del listener del que se va a suprimir el elemento SSL.

           No es necesario el operando listener_id si --type es iiop-service.

EJEMPLOS
       Ejemplo 1, Supresión de un Elemento SSL de un Listener HTTP
           En el ejemplo siguiente se muestra cómo suprimir un elemento SSL de
           un listener HTTP denominado http-listener-1.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-ssl(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de 2010              delete-ssl(1)
