list-threadpools(1)   asadminユーティリティのサブコマンド  list-threadpools(1)

名前
       list-threadpools - すべてのスレッド・プールをリストします

概要
           list-threadpools [--help] target

説明
       list-threadpoolsサブコマンドは、GlassFish
       Serverスレッド・プールをリストします。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           このオペランドでは、スレッド・プールをリストするターゲットを指定します。このオペランドは必須です。

           有効な値は次のとおりです:

           server
               デフォルトのGlassFish
               Serverインスタンスであるserverのスレッド・プールをリストします。

           configuration-name
               名前付き構成のスレッド・プールをリストします。

           cluster-name
               クラスタ内のすべてのインスタンスのスレッド・プールをリストします。

           instance-name
               特定のインスタンスのスレッド・プールをリストします。

例
       例 1, スレッド・プールのリスト
           この例では、デフォルト・インスタンスであるserverの現在のスレッド・プールをリストします。

               asadmin> list-threadpools server
               admin-thread-pool
               http-thread-pool
               thread-pool-1
               Command list-threadpools executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-threadpool(1), delete-threadpool(1)

       asadmin(1M)

Java EE 7                       2010年12月20日             list-threadpools(1)
