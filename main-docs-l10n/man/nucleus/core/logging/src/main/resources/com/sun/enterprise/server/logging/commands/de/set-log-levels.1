set-log-levels(1)       Unterbefehle des asadmin-Utili       set-log-levels(1)

NAME
       set-log-levels - legt die Logging-Ebene für einen oder mehrere Logger
       fest

ZUSAMMENFASSUNG
           set-log-levels [--help] logger-name=logger-level[:logger-name=logger-level]*
           [--target=target]

BESCHREIBUNG
       Der Unterbefehl set-log-levels legt die Logging-Ebene für einen oder
       mehrere Logger fest. Änderungen treten dynamisch in Kraft. Je nach
       festgelegten Logging-Ebenen ist möglicherweise ein Neustart des Servers
       erforderlich.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Die Serverinstanz oder das Cluster, für die bzw. das Logging-Ebenen
           festgelegt werden.

OPERANDEN
       target
           Die Serverinstanz oder das Cluster, für die bzw. das Logging-Ebenen
           festgelegt werden. Gültige Werte sind:

           ·   server_name: Standardziel ist server. Wenn kein Ziel angegeben
               wird, werden die Logging-Ebenen für den Server festgelegt.

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

       logger-name
           Der Name des Loggers. Mit dem Unterbefehl list-log-levels können
           Sie die Namen der aktuellen Logger auflisten.

       logger-level
           Die Ebene, die für den Logger festgelegt werden soll. Gültige
           Logging-Ebenenwerte sind SEVERE, WARNING, INFO, CONFIG,
            FINE, FINER und FINEST. Die Standardeinstellung ist INFO.

BEISPIELE
       Example 1, Logging-Ebene für einen Logger festlegen
           In diesem Beispiel wird die Logging-Ebene des Webcontainerloggers
           auf WARNING gesetzt.

               asadmin> set-log-levels javax.enterprise.system.container.web=WARNING
               Command set-log-level executed successfully.

       Example 2, Logging-Ebene für mehrere Logger festlegen
           In diesem Beispiel wird die Logging-Ebene des Webcontainerloggers
           auf FINE und die Logging-Ebene des EJB-Containerloggers auf SEVERE
           gesetzt:

               asadmin set-log-levels javax.enterprise.system.container.web=FINE:
               javax.enterprise.system.container.ejb=SEVERE
               Command set-log-level executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       collect-log-files(1), list-log-attributes(1), list-log-levels(1),
       rotate-log(1), set-log-attributes(1)

       asadmin(1M)

       "Administering the Logging Service" im Oracle GlassFish Server
       Administration Guide

Java EE 7                        12. Feb. 2011               set-log-levels(1)
