set-log-levels(1)           asadmin 公用程式子命令           set-log-levels(1)

參考名
       set-log-levels - 設定一或多個日誌的日誌層級

概要
           set-log-levels [--help]
           [--target=target]
           logger-name=logger-level[:logger-name=logger-level]*

描述
       set-log-levels
       子命令會設定一或多個日誌的日誌層級。變更會以動態方式生效。根據您設定的日誌層級，可能需要重新啟動伺服器。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           將設定日誌層級的伺服器執行處理或叢集。有效值包括

           ·   server -
               預設伺服器執行處理。若未指定目標，將會設定預設伺服器執行處理的日誌層級。

           ·   cluster-name - 叢集的名稱。

           ·   instance-name - 獨立伺服器執行處理的名稱。

運算元
       logger-name
           日誌的名稱。list-log-levels 子命令可以用來列出目前日誌的名稱。

       logger-level
           要為日誌設定的層級。日誌層級的值為
           嚴

範例
       範例 1, 設定日誌的日誌層級
           此範例會將 Web 容器日誌的日誌層級設定為 警

               asadmin> set-log-levels javax.enterprise.system.container.web=WARNING
               Command set-log-level executed successfully.

       範例 2, 設定多重日誌的日誌層級
           此範例中，將 Web 容器日誌的日誌層級設定為 詳
           容器日誌的日誌層級設定為 嚴

               asadmin set-log-levels javax.enterprise.system.container.web=FINE:
               javax.enterprise.system.container.ejb=SEVERE
               Command set-log-level executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       collect-log-files(1), list-log-attributes(1), list-log-levels(1),
       rotate-log(1), set-log-attributes(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering the
       Logging Service

Java EE 7                     2011 年 2 月 12 日             set-log-levels(1)
