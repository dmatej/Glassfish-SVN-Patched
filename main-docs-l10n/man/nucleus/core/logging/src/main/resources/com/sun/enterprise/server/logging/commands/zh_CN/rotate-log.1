rotate-log(1)               asadmin 实用程序子命令               rotate-log(1)

名称
       rotate-log - 滚动日志文件

提要
           rotate-log [--help]

说明
       rotate-log 子命令通过将文件以 server.log_date-and-time
       格式重命名为带有时间戳的名称并创建新的日志文件来滚动服务器日志。更改将动态生效，即无需重新启动服务器。

       日志队列的大小可通过 logging.properties
       文件进行配置。日志滚动基于文件大小或自上次日志滚动以来所经历的时间。在某些情况下，尤其是将日志级别设置为最
       子命令可用于立即滚动服务器日志。在方便的时候为滚动日志创建脚本时，该子命令也十分有用。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           将滚动日志的服务器、集群或服务器实例。如果省略该选项，则滚动默认服务器的日志。

操作数
       target
           有效值为：

           ·   server_name - 默认目标为
               server。如果未指定目标，则滚动服务器的日志。

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 滚动服务器日志
           此示例滚动服务器日志。

               asadmin> rotate-log
               Command rotate-log executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       collect-log-files(1), list-log-attributes(1), list-log-levels(1),
       set-log-attributes(1), set-log-levels(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering the
       Logging Service

Java EE 7                     2011 年 2 月 12 日                 rotate-log(1)
