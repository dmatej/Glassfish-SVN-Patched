set-log-attributes(1)     asadmin 유틸리티 하위 명령     set-log-attributes(1)

제목
       set-log-attributes - 하나 이상의 로거에 대한 로깅 속성 설정

시놉시스
           set-log-attributes [--help] [--target=target]
           attribute-name=attribute-value[:attribute-name=attribute-value]*

설명
       set-log-attributes 하위 명령은 하나 이상의 로거에 대한 로깅 속성을
       설정합니다. 설정할 수 있는 속성은 도메인의 logging.properties 파일에서
       사용할 수 있는 속성에 해당합니다. 설정하는 속성에 따라 서버를
       재시작해야 할 수도 있습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           로거 속성을 설정할 서버 도메인, 인스턴스 또는 클러스터입니다. 이
           옵션을 생략하면 기본 서버에 대한 속성이 설정됩니다.

피연산자
       target
           적합한 값은 다음과 같습니다.

           ·   server_name - 기본 대상은 server입니다. 지정한 대상이 없는 경우
               서버에 대한 로그 속성이 설정됩니다.

           ·   cluster_name- 대상 클러스터의 이름입니다.

           ·   instance_name- 대상 서버 인스턴스의 이름입니다.

       attribute-name
           로깅 속성의 전체 범위 이름입니다.  list-log-attributes 하위 명령을
           사용하여 현재 정의되어 있는 모든 속성의 이름을 나열할 수 있습니다.

       attribute-value
           지정된 속성에 적용할 값입니다.

           지정할 수 있는 속성값은 다음과 같습니다. 해당 값 각각에 대한 전체
           설명은 Oracle GlassFish Server Administration Guide의
           "Administering the Logging Service"를 참조하십시오.

           com.sun.enterprise.server.logging.GFFileHandler.alarms
               기본값은 false입니다.

           com.sun.enterprise.server.logging.GFFileHandler.excludeFields
               기본값은 빈 문자열입니다.

           com.sun.enterprise.server.logging.GFFileHandler.file
               기본값은 ${com.sun.aas.instanceRoot}/logs/server.log입니다.

           com.sun.enterprise.server.logging.GFFileHandler.flushFrequency
               기본값은 1입니다.

           com.sun.enterprise.server.logging.GFFileHandler.formatter
               기본값은
               com.sun.enterprise.server.logging.UniformLogFormatter입니다.

           com.sun.enterprise.server.logging.GFFileHandler.logtoConsole
               기본값은 false입니다.

           com.sun.enterprise.server.logging.GFFileHandler.maxHistoryFiles
               기본값은 0입니다.

           com.sun.enterprise.server.logging.GFFileHandler.multiLineMode
               기본값은 true입니다.

           com.sun.enterprise.server.logging.GFFileHandler.retainErrorsStasticsForHours
               기본값은 0입니다.

           com.sun.enterprise.server.logging.GFFileHandler.rotationLimitInBytes
               기본값은 2000000입니다.

           com.sun.enterprise.server.logging.GFFileHandler.rotationTimelimitInMinutes
               기본값은 0입니다.

           com.sun.enterprise.server.logging.SyslogHandler.useSystemLogging
               기본값은 false입니다.

           handlers
               기본값은 java.util.logging.ConsoleHandler입니다.

           java.util.logging.ConsoleHandler.formatter
               기본값은
               com.sun.enterprise.server.logging.UniformLogFormatter입니다.

           java.util.logging.FileHandler.count
               기본값은 1입니다.

           java.util.logging.FileHandler.formatter
               기본값은 java.util.logging.XMLFormatter입니다.

           java.util.logging.FileHandler.limit
               기본값은 50000입니다.

           java.util.logging.FileHandler.pattern
               기본값은 %h/java%u.log입니다.

           log4j.logger.org.hibernate.validator.util.Version
               기본값은 warn입니다.

예제
       예 1, 유지 관리할 최대 로그 내역 파일 수 설정
           이 예제는 서버에 대한 최대 로그 내역 파일 수를 전체적으로 8로
           설정합니다.

               asadmin> set-log-attributes --target=server \
               com.sun.enterprise.server.logging.GFFileHandler.maxHistoryFiles=8
               com.sun.enterprise.server.logging.GFFileHandler.maxHistoryFiles logging
               attribute set with value 8.
               These logging attributes are set for server.
               Command set-log-attributes executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       collect-log-files(1), list-log-attributes(1), list-log-levels(1),
       rotate-log(1), set-log-levels(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide의 "Administering the
       Logging Service"

Java EE 7                       2011년 2월 12일          set-log-attributes(1)
