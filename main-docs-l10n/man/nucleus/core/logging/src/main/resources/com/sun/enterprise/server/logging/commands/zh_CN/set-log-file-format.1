set-log-file-format(1)      asadmin 实用程序子命令      set-log-file-format(1)

名称
       set-log-file-format - 设置要用于服务器日志文件的格式化程序

提要
           set-log-file-format [--help]
           [--target target]
           [formatter]

说明
       set-log-file-format
       子命令可设置要用于指定目标的服务器日志文件的格式化程序。

       GlassFish Server 提供 ULF (UniformLogFormatter) 和 ODL (Oracle
       Diagnostics Logging) 格式化程序。也可以通过指定扩展
       java.util.logging.Formatter 类的类的全限定名称来使用定制格式化程序。

       定制格式化程序类可以打包在 JAR 文件中并置于 domain/lib/ext
       中，或者实现为 HK2 服务和 JAR (包含安装在 glassfish/modules
       中的格式化程序实现)。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要为其设置格式化程序的目标。指定服务器域、实例或集群的名称。如果省略该选项，则设置默认服务器的日志记录程序属性。

操作数
       formatter
           指定要用于服务器日志文件的格式化程序。有效值为 ULF、ODL
           或要用于呈现日志文件的定制格式化程序类的全限定名称。默认值为 ODL。

示例
       Example 1, 设置日志文件格式
           此示例将服务器日志文件格式设置为 server1 的 ULF。

               asadmin> set-log-file-format --target server1 ULF
               Command set-log-file-format executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-loggers(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering the
       Logging Service

Java EE 7                      2013 年 2 月 6 日        set-log-file-format(1)
