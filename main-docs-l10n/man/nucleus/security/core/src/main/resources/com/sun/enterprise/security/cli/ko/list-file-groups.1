list-file-groups(1)       asadmin 유틸리티 하위 명령       list-file-groups(1)

제목
       list-file-groups - 파일 그룹 나열

시놉시스
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

설명
       list-file-groups 하위 명령은 파일 영역 인증에서 지원되는 파일 사용자 및
       그룹을 나열합니다. 이 하위 명령은 파일 사용자가 사용할 수 있는 그룹을
       나열합니다.  --name 옵션이 지정되지 않은 경우 모든 그룹이 나열됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --name
           그룹을 나열할 파일 사용자의 이름을 식별합니다.

       --authrealmname
           사용 가능한 그룹을 나열할 인증 영역의 이름입니다.

       --target
           이 옵션은 나열할 수 있는 구성을 지정합니다. 적합한 대상은 다음과
           같습니다.

           server
               현재 서버의 파일 그룹을 나열합니다. 이 값은 기본값입니다.

           cluster_name
               클러스터의 파일 그룹을 나열합니다.

           instance_name
               특정 인스턴스의 파일 그룹을 나열합니다.

예제
       예 1, 모든 파일 영역의 그룹 나열
           이 예제는 서버에 대해 정의된 모든 파일 영역 그룹을 나열합니다.

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                       2010년 12월 1일            list-file-groups(1)
