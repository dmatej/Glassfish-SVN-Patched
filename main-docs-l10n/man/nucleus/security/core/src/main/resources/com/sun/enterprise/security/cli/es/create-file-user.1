create-file-user(1)     Subcomandos de la Utilidad asa     create-file-user(1)

NOMBRE DE REFERENCIA
       create-file-user - Crea un nuevo usuario de archivo.

SINOPSIS
           create-file-user [--help] [--authrealmnameauth_realm_name]
           [--target target
           [--groups user_groups[:user_groups]*] user_name

DESCRIPCIÓN
       El subcomando create-file-user crea una entrada en el archivo de claves
       con el nombre de usuario, contraseña y grupos especificados. Se pueden
       crear varios grupos separándolos con dos puntos (:). Si no se
       especifica auth_realm_name, se creará una entrada en el archivo de
       claves para el dominio por defecto. Si se especifica auth_realm_name,
       se creará una entrada en el archivo de claves utilizando
       auth_realm_name.

       Puede utilizar la opción --passwordfile del comando asadmin(1M) para
       especificar la contraseña para el usuario. La entrada del archivo de
       contraseña debe tener el formato AS_ADMIN_USERPASSWORD=user-password.

       Si no se proporciona una contraseña, este subcomando falla en caso de
       estar activada la administración segura y si el usuario que se crea es
       un usuario administrativo.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Nombre del destino en el que funciona el comando. Los destinos
           válidos son config, instance, cluster o server. Por defecto, el
           destino es el servidor (server).

       --groups
           Grupo asociado a este usuario de archivos.

       --authrealmname
           Nombre del dominio en el que se crea el nuevo usuario. Si no
           especifica esta opción, el usuario se crea en el dominio "archivo".

OPERANDOS
       user_name
           Nombre del usuario de archivos que se va a crear.

EJEMPLOS
       Ejemplo 1, Creación de un Usuario en el Dominio de Archivo
           Este ejemplo crea un usuario de dominio de archivo llamado
           sample_user. Se considera que ya se ha creado un dominio de
           autenticación mediante el subcomando create-auth-realm.

               asadmin> create-file-user
               --groups staff:manager
               --authrealmname auth-realm1 sample_user
               Command create-file-user executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-auth-realm(1), delete-file-user(1), list-file-users(1),
       update-file-user(1), list-file-groups(1)

       asadmin(1M)

Java EE 7                     03 de junio de 2011          create-file-user(1)
