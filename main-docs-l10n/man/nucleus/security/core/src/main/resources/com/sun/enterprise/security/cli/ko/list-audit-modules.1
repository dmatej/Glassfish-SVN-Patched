list-audit-modules(1)     asadmin 유틸리티 하위 명령     list-audit-modules(1)

제목
       list-audit-modules - 모든 감사 모듈 가져오기 및 표시

시놉시스
           list-audit-modules [--help] [target]

설명
       list-audit-modules 하위 명령은 모든 감사 모듈을 나열합니다. 이 하위
       명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           감사 모듈을 나열할 대상을 지정합니다. 적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스 server의 감사 모듈을 나열하며 기본값입니다.

           configuration_name
               명명된 구성의 감사 모듈을 나열합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스의 감사 모듈을 나열합니다.

           instance_name
               특정 서버 인스턴스의 감사 모듈을 나열합니다.

예제
       예 1, 감사 모듈 나열

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일          list-audit-modules(1)
