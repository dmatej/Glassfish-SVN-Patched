delete-audit-module(1)asadminユーティリティのサブコマンド

名前
       delete-audit-module - 名前付き監査モジュールを除去します

概要
           delete-audit-module [--help]
           [--target target]
           audit_module_name

説明
       このサブコマンドは、名前付き監査モジュールを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           監査モジュールを削除するターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverの監査モジュールを削除します。これはデフォルト値です。

           configuration_name
               名前付き構成の監査モジュールを削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスの監査モジュールを削除します。

           instance_name
               特定のサーバー・インスタンスの監査モジュールを削除します。

オペランド
       audit_module_name
           削除する監査モジュールの名前。

例
       例 1, 監査モジュールの削除

               asadmin> delete-audit-module sampleAuditModule
               Command delete-audit-module executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-audit-module(1), list-audit-modules(1)

       asadmin(1M)

Java EE 7                       2010年12月22日          delete-audit-module(1)
