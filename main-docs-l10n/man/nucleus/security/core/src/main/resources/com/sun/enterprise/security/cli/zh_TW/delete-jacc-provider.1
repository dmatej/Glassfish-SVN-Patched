delete-jacc-provider(1)     asadmin 公用程式子命令     delete-jacc-provider(1)

參考名
       delete-jacc-provider - 讓管理員可刪除針對網域定義的 JACC 提供者

概要
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

描述
       delete-jacc-provider 子命令可供管理員刪除針對網域定義的 JACC
       提供者。JACC 提供者在網域的 domain.xml 檔案中定義為 security-service
       元素中的 jacc-provider 元素。可使用 GlassFish Server 管理主控台或
       create-jacc-provider 子命令建立 JACC 提供者。

       預設 GlassFish Server 安裝包括名為 default 與 simple 的兩個 JACC
       提供者。不應刪除以下預設的提供者。

       GlassFish Server 針對授權而使用的 JACC 提供者會以 domain.xml 中
       security-service 的 jacc-provider 元素加以識別。因此，如果刪除
       jacc-provider 提供者時，請務必將 jacc-provider 變更為存在於
       security-service 之下的其他 JACC 提供者名稱。

       如果將 jacc-provider 元素變更為指向其他的 JACC 提供者，則必須重新啟動
       GlassFish Server。

       只有遠端模式支援這個子命令。

選項
       如果選項有短選項名稱，則短選項在長選項名稱之前。短選項有一個破折號，而長選項則有兩個破折號。

       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要從中刪除 JACC 提供者的目標。以下為有效值：

           server
               刪除預設伺服器執行處理上的 JACC 提供者。此為預設值。

           configuration_name
               刪除指定組態中的 JACC 提供者。

           cluster_name
               刪除指定叢集內的所有伺服器執行處理中的 JACC 提供者。

           instance_name
               刪除指定伺服器執行處理上的 JACC 提供者。

運算元
       jacc-provider-name
           要刪除的 JACC 提供者名稱。

範例
       範例 1, 刪除 JACC 提供者
           下列範例會顯示如何從預設網域中刪除名為 testJACC 的 JACC 提供者。

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 14 日       delete-jacc-provider(1)
