delete-message-security-prasadmin1유틸리티 delete-message-security-provider(1)

제목
       delete-message-security-provider - 관리자가 메시지 보안 제공자를 삭제할
       수 있음

시놉시스
           delete-message-security-provider [--help] [--target target]
           --layer message_layer
           provider_name

설명
       delete-message-security-provider 하위 명령을 사용하여 관리자는 메시지
       보안 제공자를 삭제할 수 있습니다.

       이 하위 명령이 실행될 때 발생하는 동작은 해당 메시지 계층에 대한
       provider-config 하위 요소(domain.xml의 message-security-config 요소)가
       삭제되는 것입니다.  domain.xml 파일은 매개변수 및 속성을 GlassFish
       Server에 지정합니다. 아래 목록에 지정되어 있는 옵션은 domain.xml 파일의
       message-security-config 및 provider-config 하위 요소 내 속성에
       적용됩니다.

       message-layer(message-security-config 속성)가 존재하지 않는 경우 생성된
       다음 그 아래에 provider-config가 생성됩니다.

       이 명령은 원격 모드에서만 지원됩니다.

옵션
       옵션에 짧은 옵션 이름이 있는 경우 짧은 옵션이 긴 옵션 이름보다
       우선합니다. 짧은 옵션은 대시가 하나인 반면 긴 옵션은 대시가 두
       개입니다.

       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           메시지 보안 제공자를 삭제할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스 server에서 메시지 보안 제공자를 삭제하고
               이는 기본값입니다.

           domain
               도메인에서 메시지 보안 제공자를 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에서 메시지 보안 제공자를
               삭제합니다.

           instance_name
               특정 서버 인스턴스에서 메시지 보안 제공자를 삭제합니다.

       --layer
           제공자를 삭제해야 할 message-layer입니다. 기본값은
           HttpServlet입니다.

피연산자
       provider_name
           provider-config 요소를 참조하는 데 사용되는 제공자의 이름입니다.

예제
       예 1, 메시지 보안 제공자 삭제
           다음 예제는 클라이언트에 대한 메시지 보안 제공자를 삭제하는 방법을
           보여줍니다.

               asadmin> delete-message-security-provider
               --layer SOAP mySecurityProvider

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-message-security-provider(1), list-message-security-providers(1)

       asadmin(1M)

Java EE 7                      2010년 12월 delete-message-security-provider(1)
