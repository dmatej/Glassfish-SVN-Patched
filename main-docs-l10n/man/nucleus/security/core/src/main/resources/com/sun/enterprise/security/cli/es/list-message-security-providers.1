list-message-security-prSubcomandos de la Utlist-message-security-providers(1)

NOMBRE DE REFERENCIA
       list-message-security-providers - muestra todos los proveedores de
       mensajes de seguridad para la capa de mensajes especificada.

SINOPSIS
           list-message-security-providers [--help]
           --layer message_layer
           [target]

DESCRIPCIÓN
       El subcomando list-message-security-providers permite a los
       administradores mostrar todos los proveedores de mensajes de seguridad
       (subelementos de provider-config) para la capa de mensajes especificada
       (elemento message-security-config de domain.xml).

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --layer
           Capa de mensajes para la que se debe mostrar el proveedor. El valor
           por defecto es HttpServlet.

OPERANDOS
       target
           Restringe el listado a los proveedores de seguridad de mensajes de
           un destino concreto. Los valores válidos son:

           server
               Muestra una lista de los proveedores para la instancia de
               servidor por defecto server y es el valor por defecto.

           domain
               Muestra una lista de los proveedores para el dominio.

           cluster
               Muestra una lista de los proveedores para las instancias de
               servidor del cluster.

           instance
               Muestra los proveedores para una instancia del servidor
               concreta.

EJEMPLOS
       Ejemplo 1, Listado de proveedores de mensajes de seguridad
           En el ejemplo siguiente se muestra cómo mostrar los proveedores de
           mensajes de seguridad para una capa de mensajes.

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                   23 de diciembre list-message-security-providers(1)
