delete-file-user(1)     Sous-commandes de l'utilitaire     delete-file-user(1)

NOM
       delete-file-user - enlève l'utilisateur de fichier nommé

SYNTAXE
           delete-file-user [--help][--authrealmname auth_realm_name]
           [--target target]
           username

DESCRIPTION
       La sous-commande delete-file-user supprime l'entrée du fichier de clés
       correspondant au nom utilisateur indiqué.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --authrealmname
           Nom du domaine d'authentification avec lequel l'utilisateur a été
           créé.

       --target
           Il s'agit du nom de la cible sur laquelle la commande agit. Les
           cibles valides sont les suivantes :

           server
               Cette valeur supprime l'utilisateur de fichier pour l'instance
               de serveur par défaut. Il s'agit de la valeur par défaut.

           domain
               Cette valeur supprime l'utilisateur de fichier dans le domaine.

           cluster_name
               Cette valeur supprime l'utilisateur de fichier de chaque
               instance de serveur du cluster.

           instance_name
               Cette valeur supprime l'utilisateur de fichier d'une instance
               de serveur particulière.

OPÉRANDES
       username
           Il s'agit du nom de l'utilisateur de fichier à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un utilisateur à partir d'un domaine de
       fichiers
           L'exemple suivant montre comment supprimer l'utilisateur nommé
           sample_user d'un domaine de fichiers.

               asadmin> delete-file-user
               sample_user
               Command delete-file-user executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-file-user(1), list-file-users(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                         1 déc 2010               delete-file-user(1)
