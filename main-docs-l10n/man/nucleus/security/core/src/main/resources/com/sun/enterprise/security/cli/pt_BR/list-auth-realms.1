list-auth-realms(1)     Subcomandos do utilitário asad     list-auth-realms(1)

NOME
       list-auth-realms - lista os realms de autenticação

SINOPSE
           list-auth-realms [--help] [target]

DESCRIçãO
       O subcomando list-auth-realms lista os realms de autenticação. Esse
       subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           O nome do alvo para o qual você deseja listar os realms de
           autenticação.

           server
               Lista os realms para a instância do servidor default server e é
               o valor default.

           configuration_name
               Lista os realms para a configuração nomeada.

           cluster_name
               Lista os realms para cada instância do servidor no cluster.

           instance_name
               Lista os realms para uma instância de servidor específica.

EXEMPLOS
       Example 1, Listando realms de autenticação

               asadmin> list-auth-realms
               file
               ldap
               certificate
               db
               Command list-auth-realms executed successfully
           Onde arquivo, ldap, certificado e db são os realms de autenticação
           disponíveis.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-auth-realm(1), delete-auth-realm(1)

       asadmin(1M)

Java EE 7                   22 de dezembro de 2010         list-auth-realms(1)
