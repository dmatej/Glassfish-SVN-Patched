disable-secure-admin(1)     asadmin 实用程序子命令     disable-secure-admin(1)

名称
       disable-secure-admin - 如果已启用安全管理，则禁用该功能。

提要
           disable-secure-admin [--help]

说明
       如果已启用安全管理，则 disable-secure-admin 子命令将禁用该功能。

           注意
           ┌────────────────────────────────────────────────────────────────────────────────────┐
           │注                                                                                  │
           │                                                                                    │
           │           在启用或禁用安全管理之后，必须重新启动域中任何正在运行的服务器。如果只有 │
           │           DAS                                                                      │
           │           在运行，则启用或禁用安全管理将更为简单，只需重新启动                     │
           │           DAS，然后再启动任何其他实例。                                            │
           └────────────────────────────────────────────────────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

示例
       Example 1, 禁用域的 secure admin
           以下示例说明如何为域禁用 secure admin。

               asadmin> disable-secure-admin
               server-config
               default-config

               Command disable-secure-admin executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-secure-admin(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 25 日       disable-secure-admin(1)
