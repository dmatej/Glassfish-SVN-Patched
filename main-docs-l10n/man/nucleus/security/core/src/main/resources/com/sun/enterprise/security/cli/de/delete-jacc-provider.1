delete-jacc-provider(1) Unterbefehle des asadmin-Utili delete-jacc-provider(1)

NAME
       delete-jacc-provider - ermöglicht Administratoren, die für eine Domain
       definierten JACC-Provider zu löschen

ZUSAMMENFASSUNG
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

BESCHREIBUNG
       Der Unterbefehl delete-jacc-provider ermöglicht Administratoren, die
       für eine Domain definierten JACC-Provider zu löschen. JACC-Provider
       werden als jacc-provider-Elemente im security-service-Element in der
       domain.xml-Datei der Domain definiert. JACC-Provider können mit der
       Admin-Konsole von GlassFish Server oder mit dem Unterbefehl
       create-jacc-provider erstellt werden.

       Die standardmäßige GlassFish Server-Installation enthält zwei
       JACC-Provider namens standard und simple. Diese Standardprovider
       sollten nicht gelöscht werden.

       Der von GlassFish Server verwendete JACC-Provider für die Autorisierung
       wird durch das jacc-provider-Element von security-service in domain.xml
       identifiziert. Vergewissern Sie sich daher beim Löschen des
       jacc-provider-Providers, dass Sie jacc-provider in den Namen eines
       anderen JACC-Providers unter security-service ändern.

       Wenn Sie das jacc-provider-Element ändern, um auf einen anderen
       JACC-Provider zu verweisen, müssen Sie GlassFish Server neu starten.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       Wenn eine Option einen kurzen Optionsnamen hat, geht die kurze Option
       dem langen Optionsnamen voraus. Kurze Optionen haben einen Bindestrich,
       lange Optionen haben zwei Bindestriche.

       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie den JACC-Provider löschen. Folgende
           Werte sind gültig:

           server
               Löscht den JACC-Provider der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           configuration_name
               Löscht den JACC-Provider in der angegebenen Konfiguration.

           cluster_name
               Löscht den JACC-Provider auf allen Serverinstanzen im
               angegebenen Cluster.

           instance_name
               Löscht den JACC-Provider auf einer angegebenen Serverinstanz.

OPERANDEN
       jacc-provider-name
           Der Name des JACC-Providers, der gelöscht werden soll.

BEISPIELE
       Example 1, JACC-Provider löschen
           Das folgende Beispiel zeigt, wie ein JACC-Provider namens testJACC
           aus der Standarddomain gelöscht wird.

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                        14. Jun. 2011         delete-jacc-provider(1)
