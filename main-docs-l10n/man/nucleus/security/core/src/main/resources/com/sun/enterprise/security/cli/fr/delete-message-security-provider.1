delete-message-security-Sous-commandes de ldelete-message-security-provider(1)

NOM
       delete-message-security-provider - permet aux administrateurs de
       supprimer un fournisseur de sécurité des messages

SYNTAXE
           delete-message-security-provider [--help] [--target target]
           --layer message_layer
           provider_name

DESCRIPTION
       La sous-commande delete-message-security-provider permet aux
       administrateurs de supprimer un fournisseur de sécurité des messages.

       Concernant les résultats de l'exécution de cette sous-commande, le
       sous-élément provider-config pour la couche Messages donnée (élément
       message-security-config du fichier domain.xml) est supprimé. Le fichier
       domain.xml spécifie les paramètres et les propriétés de GlassFish
       Server). Les options spécifiées dans la liste ci-dessous s'appliquent
       aux attributs dans les sous-éléments message-security-config et
       provider-config du fichier domain.xml.

       Si la couche message-layer (attribut message-security-config) n'existe
       pas, elle est créée, puis l'élément provider-config est créé sous cette
       couche.

       Cette commande est prise en charge en mode distant uniquement.

OPTIONS
       Si une option possède un nom court, celui-ci précède le nom d'option
       long. Les options courtes contiennent un tiret alors que les options
       longues en contiennent deux.

       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Spécifie la cible de laquelle supprimer le fournisseur de sécurité
           des messages. Les valeurs valides sont les suivantes :

           server
               Supprime le fournisseur de sécurité des messages de l'instance
               de serveur par défaut (server). Il s'agit de la valeur par
               défaut.

           domain
               Supprime le fournisseur de sécurité des messages du domaine.

           cluster_name
               Supprime le fournisseur de sécurité des messages de chaque
               instance de serveur du cluster.

           instance_name
               Supprime le fournisseur de sécurité des messages d'une instance
               de serveur donnée.

       --layer
           Couche message-layer de laquelle le fournisseur doit être supprimé.
           La valeur par défaut est HttpServlet.

OPÉRANDES
       provider_name
           Nom du fournisseur utilisé pour référencer l'élément
           provider-config.

EXEMPLES
       Exemple 1, Suppression d'un fournisseur de sécurité des messages
           Dans cet exemple, un fournisseur de sécurité des messages est
           supprimé pour un client.

               asadmin> delete-message-security-provider
               --layer SOAP mySecurityProvider

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-message-security-provider(1), list-message-security-providers(1)

       asadmin(1M)

Java EE 7                         22 déc 20delete-message-security-provider(1)
