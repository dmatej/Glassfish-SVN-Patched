delete-file-user(1)       asadmin 유틸리티 하위 명령       delete-file-user(1)

제목
       delete-file-user - 이름이 지정된 파일 사용자 제거

시놉시스
           delete-file-user [--help][--authrealmname auth_realm_name]
           [--target target]
           username

설명
       delete-file-user 하위 명령은 지정된 사용자 이름에 대한 키 파일의 항목을
       삭제합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --authrealmname
           사용자를 생성한 인증 영역의 이름입니다.

       --target
           명령이 작동할 대상의 이름입니다. 적합한 대상은 다음과 같습니다.

           server
               기본 서버 인스턴스에서 파일 사용자를 삭제합니다. 이 값은
               기본값입니다.

           domain
               도메인에서 파일 사용자를 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에서 파일 사용자를
               삭제합니다.

           instance_name
               특정 서버 인스턴스에서 파일 사용자를 삭제합니다.

피연산자
       username
           삭제할 파일 사용자의 이름입니다.

예제
       예 1, 파일 영역에서 사용자 삭제
           다음 예제는 파일 영역에서 sample_user라는 사용자를 삭제하는 방법을
           보여줍니다.

               asadmin> delete-file-user
               sample_user
               Command delete-file-user executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-file-user(1), list-file-users(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                       2010년 12월 1일            delete-file-user(1)
