list-message-security-prSubcomandos do utililist-message-security-providers(1)

NOME
       list-message-security-providers - lista todos os provedores da mensagem
       de segurança para a camada de mensagem especificada

SINOPSE
           list-message-security-providers [--help]
           --layer message_layer
           [target]

DESCRIçãO
       O subcomando list-message-security-providers permite que
       administradores listem todos os provedores de mensagem de segurança
       (subelementos provider-config) para a camada de mensagem fornecida
       (message-security-config elemento de domain.xml).

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --layer
           A camada de mensagem em que o provedor deve ser listado. O valor
           default é HttpServlet.

OPERANDOS
       target
           Restringe a listagem aos provedores de segurança de mensagem para
           um alvo especificado. Os valores válidos incluem:

           server
               Lista os provedores para a instância do servidor default e
               server é o valor default.

           domain
               Lista os provedores para o domínio.

           cluster
               Lista os provedores para cada instância do servidor no cluster.

           instance
               Lista os provedores de uma determinada instância do servidor.

EXEMPLOS
       Example 1, Listando provedores de segurança de mensagem
           O exemplo a seguir mostra como listar provedores de segurança de
           mensagem para uma camada de mensagem.

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                   22 de dezembro dlist-message-security-providers(1)
