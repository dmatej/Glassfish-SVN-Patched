delete-jacc-provider(1) Subcomandos do utilitário asad delete-jacc-provider(1)

NOME
       delete-jacc-provider - permite que os administradores deletem
       provedores JACC definidos para um domínio.

SINOPSE
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

DESCRIçãO
       O subcomando delete-jacc-provider permite que os administradores
       deletem provedores JACC definidos para um domínio. Os provedores JACC
       são definidos como elementos jacc-provider no elemento security-service
       no arquivo domain.xml do domínio. Os provedores JACC podem ser criados
       usando a Console de Administração do GlassFish Server ou o subcomando
       create-jacc-provider.

       A instalação default do &ProductName inclui dois provedores de JACC,
       com os nomes default e simple. Estes provedores default não devem ser
       deletados.

       O provedor JACC utilizado pelo GlassFish Server para autorização é
       identificado pelo elemento jacc-provider de security-service no
       domain.xml. Portanto, se o provedor jacc-provider for deletado,
       certifique-se de alterar o jacc-provider pelo nome de algum outro
       provedor JACC existente em security-service.

       Se alterar o elemento jacc-provider para que indique outro provedor
       JACC, você deverá reiniciar o GlassFish Server.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       Se uma opção tiver um nome de opção curto, a mesma precederá o nome de
       opção longo. As opções curtas possuem um traço, enquanto as opções
       longas possuem dois traços.

       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo a partir do qual está sendo deletado o provedor
           JACC. Os valores a seguir são válidos:

           server
               Deleta o provedor JACC na instância do servidor default. Este é
               o valor default.

           configuration_name
               Deleta o provedor JACC da configuração especificada.

           cluster_name
               Deleta o provedor JACC de todas as instâncias de servidor no
               cluster especificado.

           instance_name
               Deleta o provedor JACC da instância do servidor especificada.

OPERANDOS
       jacc-provider-name
           O nome do provedor JACC a ser deletado.

EXEMPLOS
       Example 1, Deletando um provedor JACC
           O exemplo a seguir mostra como deletar um provedor JACC com o nome
           testJACC do domínio default.

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                      14 de junho 2011        delete-jacc-provider(1)
