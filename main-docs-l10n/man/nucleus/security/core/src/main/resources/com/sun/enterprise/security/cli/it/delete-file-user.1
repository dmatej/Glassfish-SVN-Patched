delete-file-user(1)     Comandi secondari della utilit     delete-file-user(1)

NOME
       delete-file-user - rimuove l'utente file specificato

SINTASSI
           delete-file-user [--help][--authrealmname auth_realm_name]
           [--target target]
           username

DESCRIZIONE
       Il comando secondario delete-file-user consente di eliminare la voce
       nel keyfile per il nome utente specificato.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --authrealmname
           Il nome del realm di autenticazione con cui è stato creato
           l'utente.

       --target
           Il nome della destinazione su cui agisce il comando. Di seguito
           sono riportate le destinazioni valide.

           server
               Elimina l'utente file dall'istanza server predefinita. Questo è
               il valore predefinito.

           domain
               Elimina l'utente file nel dominio.

           cluster_name
               Elimina l'utente file da tutte le istanze server del cluster.

           instance_name
               Elimina l'utente file da un'istanza server particolare.

OPERANDI
       username
           Il nome dell'utente file da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un utente da un realm file
           Nell'esempio riportato di seguito vengono illustrate le modalità di
           eliminazione dell'utente denominato sample_user da un realm file.

               asadmin> delete-file-user
               sample_user
               Command delete-file-user executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-file-user(1), list-file-users(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                       1 dicembre 2010            delete-file-user(1)
