list-file-groups(1)     Unterbefehle des asadmin-Utili     list-file-groups(1)

NAME
       list-file-groups - listet Dateigruppen auf

ZUSAMMENFASSUNG
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

BESCHREIBUNG
       Der Unterbefehl list-file-groups listet die Dateibenutzer und Gruppen
       auf, die von der Datei-Realm-Authentifizierung unterstützt werden.
       Dieser Unterbefehl listet verfügbare Gruppen im Dateibenutzer auf. Ist
       die Option --name nicht angegeben, werden alle Gruppen aufgelistet.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Gibt den Namen des Dateibenutzers an, für den die Gruppen
           aufgelistet werden.

       --authrealmname
           Der Name der Authentifizierungs-Realm, für die verfügbare Gruppen
           aufgelistet werden sollen.

       --target
           Diese Option legt fest, welche Konfigurationen Sie auflisten
           können. Gültige Ziele sind:

           server
               Listet die Dateigruppen im aktuellen Server auf. Dies ist der
               Standardwert.

           cluster_name
               Listet die Dateigruppen in einem Cluster auf.

           instance_name
               Listet die Dateigruppen für eine bestimmte Instanz auf.

BEISPIELE
       Example 1, Gruppen in allen Datei-Realms auflisten
           In diesem Beispiel werden alle Datei-Realm-Gruppen aufgelistet, die
           für den Server definiert sind.

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                      01. Dezember 2010           list-file-groups(1)
