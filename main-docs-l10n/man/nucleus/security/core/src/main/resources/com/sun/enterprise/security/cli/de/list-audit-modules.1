list-audit-modules(1)   Unterbefehle des asadmin-Utili   list-audit-modules(1)

NAME
       list-audit-modules - ruft alle Auditmodule ab und zeigt diese an

ZUSAMMENFASSUNG
           list-audit-modules [--help] [target]

BESCHREIBUNG
       Der Unterbefehl list-audit-modules listet alle Auditmodule auf. Dieser
       Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Gibt das Ziel an, für das Sie die Auditmodule auflisten. Folgende
           Werte sind gültig:

           server
               Listet die Auditmodule für die standardmäßige Serverinstanz
               server auf und ist der Standardwert.

           configuration_name
               Listet die Auditmodule für die benannte Konfiguration auf.

           cluster_name
               Listet die Auditmodule für jede Serverinstanz im Cluster auf.

           instance_name
               Listet die Auditmodule für eine bestimmte Serverinstanz auf.

BEISPIELE
       Beispiel 1, Auditmodule auflisten

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010           list-audit-modules(1)
