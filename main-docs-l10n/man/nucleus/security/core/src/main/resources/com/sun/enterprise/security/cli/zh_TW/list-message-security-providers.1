list-message-security-providasadmin 公用程式list-message-security-providers(1)

參考名
       list-message-security-providers -
       可列出指定之訊息層的所有安全性訊息提供者

概要
           list-message-security-providers [--help]
           --layer message_layer
           [target]

描述
       list-message-security-providers 子命令可供管理員列出指定之訊息層
       (domain.xml 的 message-security-config 元素) 的所有安全性訊息提供者
       (provider-config 子元素)。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --layer
           必須列出提供者的訊息層。預設值為 HttpServlet。

運算元
       target
           將清單限制為特定目標的訊息安全性提供者。有效值包含：

           server
               列出預設伺服器執行處理 server 的提供者 (這是預設值)。

           domain
               列出網域的提供者。

           cluster
               列出叢集內伺服器執行處理的提供者。

           instance
               列出特定伺服器執行處理的提供者。

範例
       範例 1, 列出訊息安全性提供者
           下列範例會顯示如何列出訊息層的訊息安全性提供者。

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 list-message-security-providers(1)
