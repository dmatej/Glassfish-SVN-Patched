list-audit-modules(1)       asadmin 实用程序子命令       list-audit-modules(1)

名称
       list-audit-modules - 获取所有审计模块并显示

提要
           list-audit-modules [--help] [target]

说明
       list-audit-modules
       子命令列出所有审计模块。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           指定要列出审计模块的目标。有效值如下：

           server
               列出默认服务器实例 server 的审计模块，这是默认值。

           configuration_name
               列出命名配置的审计模块。

           cluster_name
               列出集群中每个服务器实例的审计模块。

           instance_name
               列出特定服务器实例的审计模块。

示例
       Example 1, 列出审计模块

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日        list-audit-modules(1)
