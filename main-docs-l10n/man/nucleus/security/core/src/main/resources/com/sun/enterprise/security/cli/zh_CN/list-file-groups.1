list-file-groups(1)         asadmin 实用程序子命令         list-file-groups(1)

名称
       list-file-groups - 列出文件组

提要
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

说明
       list-file-groups
       子命令列出文件领域验证支持的文件用户和组。此子命令列出文件用户中的可用组。如果未指定
       --name 选项，则列出所有组。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --name
           标识将要列出组的文件用户的名称。

       --authrealmname
           要列出可用组的验证领域的名称。

       --target
           该选项指定可以列出的配置。有效目标包括：

           server
               列出当前服务器中的文件组。这是默认值。

           cluster_name
               列出集群中的文件组。

           instance_name
               列出特定实例的文件组。

示例
       Example 1, 列出所有文件领域中的组
           此示例列出为服务器定义的所有文件领域组。

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 1 日           list-file-groups(1)
