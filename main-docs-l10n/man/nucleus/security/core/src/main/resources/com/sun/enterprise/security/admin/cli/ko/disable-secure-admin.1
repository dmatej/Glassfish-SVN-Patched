disable-secure-admin(1)   asadmin 유틸리티 하위 명령   disable-secure-admin(1)

제목
       disable-secure-admin - 이미 사용으로 설정된 경우 보안 관리를 사용
       안함으로 설정

시놉시스
           disable-secure-admin [--help]

설명
       disable-secure-admin 하위 명령은 이미 사용으로 설정된 경우 보안 관리를
       사용 안함으로 설정합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           보안 관리를 사용 또는 사용   │
           │           안함으로 설정한 후에         │
           │           도메인에서 실행 중인 서버를  │
           │           재시작해야 합니다. 간단히    │
           │           실행 중인 DAS에만 보안       │
           │           관리를 사용 또는 사용        │
           │           안함으로 설정하고, DAS를     │
           │           재시작한 후, 다른 인스턴스를 │
           │           시작하면 됩니다.             │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

예제
       예 1, 도메인에 대해 secure admin 사용 안함
           다음 예제는 도메인에 대해 secure admin을 사용 안함으로 설정하는
           방법을 보여줍니다.

               asadmin> disable-secure-admin
               server-config
               default-config

               Command disable-secure-admin executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       enable-secure-admin(1)

       asadmin(1M)

Java EE 7                       2011년 6월 25일        disable-secure-admin(1)
