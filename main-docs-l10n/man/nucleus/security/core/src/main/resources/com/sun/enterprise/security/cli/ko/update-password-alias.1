update-password-alias(1)  asadmin 유틸리티 하위 명령; update-password-alias(1)

제목
       update-password-alias - 비밀번호 별칭 업데이트

시놉시스
           update-password-alias [--help]
            aliasname

설명
       이 하위 명령은 명명된 대상의 비밀번호 별칭 ID를 업데이트합니다. 별칭은
       ${ALIAS=password-alias-password} 형식의 토큰입니다. 별칭 이름에
       해당하는 비밀번호는 암호화된 형식으로 저장됩니다.
       update-password-alias 하위 명령에는 대화식 보안 형식(사용자에게 모든
       정보를 입력하라는 프롬프트 표시)과 스크립트 형식(비밀번호가 명령행에서
       전달됨)이 모두 사용됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       aliasname
           domain.xml에 나타나는 비밀번호의 이름입니다.

예제
       예 1, 비밀번호 별칭 업데이트

               asadmin> update-password-alias jmspassword-alias
               Please enter the alias password>
               Please enter the alias password again>
               Command update-password-alias executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       delete-password-alias(1), list-password-aliases(1),
       create-password-alias(1)

       asadmin(1M)

Java EE 7                       2009년 8월 28일       update-password-alias(1)
