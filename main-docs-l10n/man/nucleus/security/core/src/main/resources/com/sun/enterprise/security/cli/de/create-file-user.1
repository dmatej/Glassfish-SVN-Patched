create-file-user(1)     Unterbefehle des asadmin-Utili     create-file-user(1)

NAME
       create-file-user - erstellt einen neuen Dateibenutzer

ZUSAMMENFASSUNG
           create-file-user [--help] [--authrealmnameauth_realm_name]
           [--target target
           [--groups user_groups[:user_groups]*] user_name

BESCHREIBUNG
       Der Unterbefehl create-file-user erstellt einen Eintrag in der
       Schlüsseldatei mit dem angegebenen Benutzernamen, dem Kennwort und den
       Gruppen. Es können mehrere Gruppen erstellt werden, die durch einen
       Doppelpunkt (:) voneinander getrennt werden müssen. Wenn
       auth_realm_name nicht angegeben ist, wird in der Schlüsseldatei ein
       Eintrag für die Standard-Realm erstellt. Wenn auth_realm_name angegeben
       ist, wird in der Schlüsseldatei ein Eintrag mit auth_realm_name
       erstellt.

       Sie können die Option --passwordfile des Befehls asadmin(1M) verwenden,
       um das Kennwort für den Benutzer anzugeben. Der Eintrag in der
       Kennwortdatei muss im Format AS_ADMIN_USERPASSWORD=user-password
       erfolgen.

       Wenn kein Kennwort angegeben wird, ist dieser Unterbefehl nicht
       erfolgreich, wenn die sichere Administration aktiviert und der zu
       erstellende Benutzer ein Admin-Benutzer ist.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Dies ist der Name des Ziels, dem der Befehl gilt. Die gültigen
           Ziele sind config, instance, cluster oder server. Standardmäßig ist
           das Ziel der Server.

       --groups
           Dies ist die Gruppe, die mit diesem Dateibenutzer verknüpft ist.

       --authrealmname
           Der Name der Realm, in der der neue Benutzer erstellt wird. Wenn
           Sie diese Option nicht angeben, wird der Benutzer in der Realm
           "file" erstellt.

OPERANDEN
       user_name
           Dies ist der Name des Dateibenutzers, der erstellt werden soll.

BEISPIELE
       Example 1, Benutzer in der Datei-Realm erstellen
           In diesem Beispiel wird ein Datei-Realm-Benutzer mit dem Namen
           sample_user erstellt. Es wird angenommen, dass mit dem Unterbefehl
           create-auth-realm bereits eine Authentifizierungs-Realm erstellt
           wurde.

               asadmin> create-file-user
               --groups staff:manager
               --authrealmname auth-realm1 sample_user
               Command create-file-user executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-auth-realm(1), delete-file-user(1), list-file-users(1),
       update-file-user(1), list-file-groups(1)

       asadmin(1M)

Java EE 7                        03. Juni 2011             create-file-user(1)
