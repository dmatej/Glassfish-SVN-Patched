disable-secure-admin(1) Unterbefehle des asadmin-Utili disable-secure-admin(1)

NAME
       disable-secure-admin - deaktiviert die sichere Administration, wenn sie
       bereits aktiviert wurde.

ZUSAMMENFASSUNG
           disable-secure-admin [--help]

BESCHREIBUNG
       Der Unterbefehl disable-secure-admin deaktiviert die sichere
       Administration, wenn sie bereits aktiviert wurde.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Sie müssen alle laufenden    │
           │           Server in der Domain neu     │
           │           starten, nachdem Sie die     │
           │           sichere Administration       │
           │           aktiviert oder deaktiviert   │
           │           haben. Es ist einfacher, die │
           │           sichere Administration zu    │
           │           aktivieren oder zu           │
           │           deaktivieren, wenn nur der   │
           │           DAS ausgeführt wird, und den │
           │           DAS neu zu starten, bevor    │
           │           Sie andere Instanzen         │
           │           starten.                     │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

BEISPIELE
       Beispiel 1, secure admin für eine Domain deaktivieren
           Im folgenden Beispiel wird beschrieben, wie Sie secure admin für
           eine Domain deaktivieren.

               asadmin> disable-secure-admin
               server-config
               default-config

               Command disable-secure-admin executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-secure-admin(1)

       asadmin(1M)

Java EE 7                        25. Jun. 2011         disable-secure-admin(1)
