list-jacc-providers(1)      asadmin 公用程式子命令      list-jacc-providers(1)

參考名
       list-jacc-providers - 子命令會讓管理員列出針對網域定義的 JACC 提供者。

概要
           list-jacc-providers [--help] [target]

描述
       list-jacc-providers 子命令會讓管理員列出針對網域定義的 JACC
       提供者。JACC 提供者在網域的 domain.xml 檔案中定義為 security-service
       元素中的 jacc-provider 元素。可使用 GlassFish Server 管理主控台或
       create-jacc-provider 子命令建立 JACC 提供者。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           指定您要列出 JACC 提供者的目標。以下為有效值：

           server
               列出預設伺服器執行處理上的 JACC 提供者。此為預設值。

           configuration_name
               列出指定組態中的 JACC 提供者。

           cluster_name
               列出指定叢集內所有伺服器執行處理上的 JACC 提供者。

           instance_name
               列出指定伺服器執行處理上的 JACC 提供者。

範例
       範例 1, 列出 JACC 提供者
           下列範例會顯示如何列出預設網域的 JACC 提供者。

               asadmin> list-jacc-providers
               default
               simple
               testJACC

               Command list-jacc-providers executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jacc-provider(1), delete-jacc-provider(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 21 日        list-jacc-providers(1)
