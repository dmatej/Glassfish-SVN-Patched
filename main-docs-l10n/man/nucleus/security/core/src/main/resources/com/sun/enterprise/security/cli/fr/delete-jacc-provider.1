delete-jacc-provider(1) Sous-commandes de l'utilitaire delete-jacc-provider(1)

NOM
       delete-jacc-provider - permet aux administrateurs de supprimer les
       fournisseurs JACC définis pour un domaine

SYNTAXE
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

DESCRIPTION
       La sous-commande delete-jacc-provider permet aux administrateurs de
       supprimer les fournisseurs JACC définis pour un domaine. Les
       fournisseurs JACC sont définis en tant que jacc-provider dans l'élément
       security-service du fichier domain.xml du domaine. Vous pouvez les
       créer à l'aide de la console d'administration GlassFish Server ou de la
       sous-commande create-jacc-provider.

       L'installation par défaut de GlassFish Server comprend deux
       fournisseurs JACC, nommés default et simple. Ces fournisseurs par
       défaut ne doivent pas être supprimés.

       Le fournisseur JACC utilisé par GlassFish Server pour l'autorisation
       est identifié par l'élément jacc-provider de security-service dans le
       fichier domain.xml. Par conséquent, si vous supprimez le fournisseur
       jacc-provider, veillez à modifier jacc-provider pour indiquer le nom
       d'un autre fournisseur JACC qui existe sous security-service.

       Si vous modifiez l'élément jacc-provider pour qu'il pointe vers un
       autre fournisseur JACC, vous devez redémarrer GlassFish Server.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       Si une option possède un nom court, celui-ci précède le nom d'option
       long. Les options courtes contiennent un tiret alors que les options
       longues en contiennent deux.

       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible pour laquelle vous supprimez le
           fournisseur JACC. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime le fournisseur JACC sur l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           configuration_name
               Cette valeur supprime le fournisseur JACC dans la configuration
               indiquée.

           cluster_name
               Cette valeur supprime le fournisseur JACC sur toutes les
               instances de serveur dans le cluster indiqué.

           instance_name
               Cette valeur supprime le fournisseur JACC sur l'instance de
               serveur indiquée.

OPÉRANDES
       jacc-provider-name
           Nom du fournisseur JACC à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un fournisseur JACC
           L'exemple suivant montre comment supprimer un fournisseur JACC
           nommé testJACC du domaine par défaut.

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                         14 jui 2011          delete-jacc-provider(1)
