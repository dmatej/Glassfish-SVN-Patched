list-message-security-provasadmin)유틸리티 하

제목
       list-message-security-providers - 제공된 메시지 층에 대한 모든 보안
       메시지 제공자 나열

시놉시스
           list-message-security-providers [--help]
           --layer message_layer
           [target]

설명
       list-message-security-providers 하위 명령을 통해 관리자는 제공된 메시지
       층(domain.xml의 message-security-config 요소)에 대한 모든 보안 메시지
       제공자(provider-config 하위 요소)를 나열할 수 있습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --layer
           제공자를 나열할 메시지 층입니다. 기본값은 HttpServlet입니다.

피연산자
       target
           특정 대상에 대한 메시지 보안 제공자만 나열되도록 제한합니다. 적합한
           값은 다음과 같습니다.

           server
               기본 서버 인스턴스 server의 제공자를 나열하며 기본값입니다.

           domain
               도메인의 제공자를 나열합니다.

           cluster
               클러스터에 있는 서버 인스턴스의 제공자를 나열합니다.

           instance
               특정 서버 인스턴스의 제공자를 나열합니다.

예제
       예 1, 메시지 보안 제공자 나열
           다음 예제는 메시지 층에 대한 메시지 보안 제공자 나열 방법을
           보여줍니다.

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                      2010년 12월 2list-message-security-providers(1)
