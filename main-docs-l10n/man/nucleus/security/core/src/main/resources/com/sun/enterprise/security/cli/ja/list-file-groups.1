list-file-groups(1)   asadminユーティリティのサブコマンド  list-file-groups(1)

名前
       list-file-groups - ファイル・グループをリストします

概要
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

説明
       list-file-groupsサブコマンドは、ファイル・レルム認証によってサポートされるファイル・ユーザーとファイル・グループをリストします。このサブコマンドは、ファイル・ユーザー内の使用可能なグループをリストします。--nameオプションを指定しない場合は、すべてのグループがリストされます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --name
           グループをリストするファイル・ユーザーの名前を識別します。

       --authrealmname
           使用可能なグループをリストする認証レルムの名前。

       --target
           このオプションは、リストできる構成を指定します。有効なターゲットは次のとおりです。

           server
               現在のサーバー内のファイル・グループをリストします。この値がデフォルト値です。

           cluster_name
               クラスタ内のファイル・グループをリストします。

           instance_name
               特定のインスタンスのファイル・グループをリストします。

例
       例 1, すべてのファイル・レルムのグループのリスト
           この例では、サーバーに対して定義されたすべてのファイル・レルムのグループをリストします。

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                        2010年12月1日             list-file-groups(1)
