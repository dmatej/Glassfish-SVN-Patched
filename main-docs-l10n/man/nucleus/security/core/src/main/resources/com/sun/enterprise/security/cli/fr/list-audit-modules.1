list-audit-modules(1)   Sous-commandes de l'utilitaire   list-audit-modules(1)

NOM
       list-audit-modules - permet d'obtenir tous les modules d'audit et de
       les afficher

SYNTAXE
           list-audit-modules [--help] [target]

DESCRIPTION
       La sous-commande list-audit-modules répertorie tous les modules
       d'audit. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Spécifie la cible pour laquelle répertorier les modules d'audit.
           Les valeurs valides sont les suivantes :

           server
               Répertorie les modules d'audit pour l'instance de serveur par
               défaut (server). Il s'agit de la valeur par défaut.

           configuration_name
               Répertorie les modules d'audit pour la configuration nommée.

           cluster_name
               Répertorie les modules d'audit pour chaque instance de serveur
               du cluster.

           instance_name
               Répertorie les modules d'audit pour une instance de serveur
               donnée.

EXEMPLES
       Exemple 1, Liste des modules d'audit

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

Java EE 7                         22 déc 2010            list-audit-modules(1)
