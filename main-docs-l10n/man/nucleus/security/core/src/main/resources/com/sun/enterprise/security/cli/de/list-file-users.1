list-file-users(1)      Unterbefehle des asadmin-Utili      list-file-users(1)

NAME
       list-file-users - listet die Dateibenutzer auf

ZUSAMMENFASSUNG
           list-file-users [--help] [--authrealmname auth_realm_name] [target]

BESCHREIBUNG
       Der Unterbefehl list-file-users zeigt eine Liste der Dateibenutzer an,
       die von der Datei-Realm-Authentifizierung unterstützt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --authrealmname
           Listet nur die Benutzer in der angegebenen Authentifizierungs-Realm
           auf.

OPERANDEN
       target
           Gibt das Ziel an, für das Sie Dateibenutzer auflisten möchten.
           Folgende Werte sind gültig:

           server
               Listet die Dateibenutzer in der standardmäßigen Serverinstanz
               auf. Dies ist der Standardwert.

           configuration_name
               Listet die Dateibenutzer in der angegebenen Konfiguration auf.

           cluster_name
               Listet die Dateibenutzer auf allen Serverinstanzen im
               angegebenen Cluster auf.

           instance_name
               Listet die Dateibenutzer in einer angegebenen Serverinstanz
               auf.

BEISPIELE
       Beispiel 1, Benutzer in einer bestimmten Datei-Realm auflisten
           Im folgenden Beispiel werden die Benutzer in der Datei-Realm mit
           dem Namen sample_file_realm aufgelistet.

               asadmin> list-file-users --authrealmname sample_file_realm
               sample_user05
               sample_user08
               sample_user12
               Command list-file-users executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-file-user(1), delete-file-user(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                        21. Jun. 2011              list-file-users(1)
