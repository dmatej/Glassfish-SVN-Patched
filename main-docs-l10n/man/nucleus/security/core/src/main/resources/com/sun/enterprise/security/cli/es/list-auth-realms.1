list-auth-realms(1)     Subcomandos de la Utilidad asa     list-auth-realms(1)

NOMBRE DE REFERENCIA
       list-auth-realms - Muestra los dominios de autenticación.

SINOPSIS
           list-auth-realms [--help] [target]

DESCRIPCIÓN
       El subcomando list-auth-dominios muestra una lista de los dominios de
       autenticación. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Nombre del destino para el que desea mostrar los dominios de
           autenticación.

           server
               Muestra los dominios para la instancia de servidor por defecto
               server y es el valor por defecto.

           configuration_name
               Muestra los dominios para la configuración especificada.

           cluster_name
               Muestra los dominios para todas las instancias de servidor del
               cluster.

           instance_name
               Muestra los dominios para una determinada instancia de
               servidor.

EJEMPLOS
       Ejemplo 1, Lista de los Dominios de Autenticación

               asadmin> list-auth-realms
               file
               ldap
               certificate
               db
               Command list-auth-realms executed successfully
           Donde file, ldap, certificate y db son los dominios de
           autenticación disponibles.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-auth-realm(1), delete-auth-realm(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de 2010        list-auth-realms(1)
