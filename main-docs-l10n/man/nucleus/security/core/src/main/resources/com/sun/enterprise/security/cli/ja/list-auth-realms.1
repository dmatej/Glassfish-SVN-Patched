list-auth-realms(1)   asadminユーティリティのサブコマンド  list-auth-realms(1)

名前
       list-auth-realms - 認証レルムをリストします

概要
           list-auth-realms [--help] [target]

説明
       list-auth-realmsサブコマンドは、認証レルムをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           認証レルムをリストするターゲットの名前。

           server
               デフォルトのサーバー・インスタンスserverのレルムをリストします(デフォルト値)。

           configuration_name
               名前付き構成のレルムをリストします。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのレルムをリストします。

           instance_name
               特定のサーバー・インスタンスのレルムをリストします。

例
       例 1, 認証レルムのリスト

               asadmin> list-auth-realms
               file
               ldap
               certificate
               db
               Command list-auth-realms executed successfully
           file、ldap、certificate、およびdbは、使用可能な認証レルムです。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-auth-realm(1), delete-auth-realm(1)

       asadmin(1M)

Java EE 7                       2010年12月22日             list-auth-realms(1)
