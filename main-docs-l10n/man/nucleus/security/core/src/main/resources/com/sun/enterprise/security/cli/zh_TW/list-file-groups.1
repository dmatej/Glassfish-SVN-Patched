list-file-groups(1)         asadmin 公用程式子命令         list-file-groups(1)

參考名
       list-file-groups - 列出檔案群組

概要
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

描述
       list-file-groups
       子命令會列出檔案範圍認證所支援的檔案使用者和群組。這個子命令會列出檔案使用者中的可用群組。如果未指定
       --name 選項，則會列出所有群組。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --name
           識別將列出群組之檔案使用者的名稱。

       --authrealmname
           列出可用群組之認證範圍的名稱。

       --target
           此選項可指定您能夠列出的組態。有效目標包括：

           server
               列出目前伺服器中的檔案群組。此為預設值。

           cluster_name
               列出叢集中的檔案群組。

           instance_name
               列出特定執行處理的檔案群組。

範例
       範例 1, 列出所有檔案範圍中的群組
           此範例會列出針對伺服器定義的所有檔案範圍群組。

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 1 日           list-file-groups(1)
