delete-message-security-Comandirsecondari ddelete-message-security-provider(1)

NOME
       delete-message-security-provider - consente agli amministratori di
       eliminare un provider di sicurezza messaggi

SINTASSI
           delete-message-security-provider [--help] [--target target]
           --layer message_layer
           provider_name

DESCRIZIONE
       Il comando secondario delete-message-security-provider consente agli
       amministratori di eliminare un provider di sicurezza messaggi.

       Quando viene eseguito questo comando secondario, viene eliminato
       l'elemento secondario provider-config per il layer di messaggio
       specificato (elemento message-security-config di domain.xml. Il file
       domain.xml specifica i parametri e le proprietà a GlassFish Server). Le
       opzioni specificate nella lista riportata di seguito si applicano agli
       attributi negli elementi secondari message-security-config e
       provider-config del file domain.xml.

       Se il layer di messaggio (attributo message-security-config) non
       esiste, viene creato e provider-config viene creato sotto di esso.

       Questo comando è supportato solo in modalità remota.

OPZIONI
       Se per un'opzione è disponibile un'abbreviazione, questa precede il
       nome esteso. Le opzioni brevi presentano una lineetta, mentre le
       opzioni lunghe ne hanno due.

       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione da cui eliminare il provider di sicurezza
           messaggi. Di seguito sono riportati i valori validi.

           server
               Elimina il provider di sicurezza messaggi dall'istanza server
               predefinita server ed è il valore predefinito.

           domain
               Elimina il provider di sicurezza messaggi dal dominio.

           cluster_name
               Elimina il provider di sicurezza messaggi da tutte le istanze
               server del cluster.

           instance_name
               Elimina il provider di sicurezza messaggi da un'istanza server
               particolare.

       --layer
           Il layer di messaggio da cui deve essere eliminato il provider. Il
           valore predefinito è HttpServlet.

OPERANDI
       provider_name
           Il nome del provider usato per fare riferimento all'elemento
           provider-config.

ESEMPI
       Esempio 1, Eliminazione di un provider di sicurezza messaggi
           Nell'esempio riportato di seguito viene illustrato come eliminare
           un provider di sicurezza messaggi per un client.

               asadmin> delete-message-security-provider
               --layer SOAP mySecurityProvider

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-message-security-provider(1), list-message-security-providers(1)

       asadmin(1M)

Java EE 7                      22 dicembre delete-message-security-provider(1)
