list-jacc-providers(1)      asadmin 实用程序子命令      list-jacc-providers(1)

名称
       list-jacc-providers - 允许管理员列出为域定义的 JACC 提供方

提要
           list-jacc-providers [--help] [target]

说明
       管理员可以使用 list-jacc-providers 子命令来列出为域定义的 JACC
       提供方。JACC 提供方以域的 domain.xml 文件中 security-service 元素的
       jacc-provider 元素来定义。可以使用 GlassFish Server 管理控制台或
       create-jacc-provider 子命令创建 JACC 提供方。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           指定要列出 JACC 提供方的目标。以下是有效值：

           server
               列出默认服务器实例上的 JACC 提供方。这是默认值。

           configuration_name
               列出指定配置中的 JACC 提供方。

           cluster_name
               列出指定集群中所有服务器实例上的 JACC 提供方。

           instance_name
               列出指定服务器实例上的 JACC 提供方。

示例
       Example 1, 列出 JACC 提供方
           以下示例显示如何列出默认域的 JACC 提供方。

               asadmin> list-jacc-providers
               default
               simple
               testJACC

               Command list-jacc-providers executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jacc-provider(1), delete-jacc-provider(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 21 日        list-jacc-providers(1)
