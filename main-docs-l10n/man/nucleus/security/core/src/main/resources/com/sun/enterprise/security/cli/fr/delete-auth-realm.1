delete-auth-realm(1)    Sous-commandes de l'utilitaire    delete-auth-realm(1)

NOM
       delete-auth-realm - enlève le domaine d'authentification nommé

SYNTAXE
           delete-auth-realm [--help]
           [--target target]
           auth_realm-name

DESCRIPTION
       La sous-commande delete-auth-realm enlève le domaine d'authentification
       nommé. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible sur laquelle vous supprimez le
           domaine d'authentification. Les valeurs valides sont les suivantes
           :

           server
               Cette valeur supprime le domaine pour l'instance de serveur par
               défaut server et constitue la valeur par défaut.

           configuration_name
               Cette valeur supprime le domaine pour la configuration nommée.

           cluster_name
               Cette valeur supprime le domaine pour chaque instance de
               serveur du cluster.

           instance_name
               Cette valeur supprime le domaine pour une instance de serveur
               particulière.

OPÉRANDES
       auth_realm_name
           Nom du domaine à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un domaine d'authentification
           Cet exemple montre comment supprimer le domaine d'authentification
           db.

               asadmin> delete-auth-realm db
               Command delete-auth-realm executed successfully

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-auth-realm(1), list-auth-realms(1)

       asadmin(1M)

Java EE 7                         22 déc 2010             delete-auth-realm(1)
