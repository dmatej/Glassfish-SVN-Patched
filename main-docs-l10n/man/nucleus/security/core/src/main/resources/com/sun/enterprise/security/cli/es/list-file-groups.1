list-file-groups(1)     Subcomandos de la Utilidad asa     list-file-groups(1)

NOMBRE DE REFERENCIA
       list-file-groups - Muestra los grupos de archivos.

SINOPSIS
           list-file-groups [--help] [--name username] [--authrealmname auth_realm_name]
           [--target target]

DESCRIPCIÓN
       El subcomando list-file-groups muestra los usuarios y grupos de
       archivos que soporta la autenticación del dominio de archivo. Este
       subcomando muestra los grupos disponibles en el usuario de archivos. Si
       no se ha especificado la opción --name, se muestran todos los grupos.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           Identifica el nombre del usuario de archivos para el que se van a
           enumerar los grupos.

       --authrealmname
           El nombre del dominio de autenticación para el que se deben
           enumerar los grupos disponibles.

       --target
           Este operando especifica qué configuraciones se pueden mostrar en
           una lista. Los destinos válidos son:

           server
               Muestra los grupos de archivos en el servidor actual. Este es
               el valor por defecto.

           cluster_name
               Muestra los grupos de archivos en un cluster.

           instance_name
               Enumera los grupos de archivos para una instancia concreta.

EJEMPLOS
       Ejemplo 1, Lista de Grupos en Todos los Dominios de Archivo
           Este ejemplo muestra todos los grupos de dominios de archivo
           definidos para el servidor.

               asadmin> list-file-groups
               staff
               manager
               Command list-file-groups executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-file-user(1), update-file-user(1), create-file-user(1),
       list-file-users(1)

       asadmin(1M)

Java EE 7                   01 de diciembre de 2010        list-file-groups(1)
