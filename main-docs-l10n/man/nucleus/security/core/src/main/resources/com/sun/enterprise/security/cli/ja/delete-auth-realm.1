delete-auth-realm(1)  asadminユーティリティのサブコマンド delete-auth-realm(1)

名前
       delete-auth-realm - 名前付き認証レルムを除去します

概要
           delete-auth-realm [--help]
           [--target target]
           auth_realm-name

説明
       delete-auth-realmサブコマンドは、名前付き認証レルムを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           認証レルムを削除するターゲットを指定します。有効な値は次のとおりです

           server
               デフォルトのサーバー・インスタンスserverのレルムを削除します。これはデフォルト値です。

           configuration_name
               名前付き構成のレルムを削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのレルムを削除します。

           instance_name
               特定のサーバー・インスタンスのレルムを削除します。

オペランド
       auth_realm_name
           削除するレルムの名前。

例
       例 1, 認証レルムの削除
           この例では、認証レルムdbを削除します。

               asadmin> delete-auth-realm db
               Command delete-auth-realm executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-auth-realm(1), list-auth-realms(1)

       asadmin(1M)

Java EE 7                       2010年12月22日            delete-auth-realm(1)
