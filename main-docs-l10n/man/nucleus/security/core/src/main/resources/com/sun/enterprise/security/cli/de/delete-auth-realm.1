delete-auth-realm(1)    Unterbefehle des asadmin-Utili    delete-auth-realm(1)

NAME
       delete-auth-realm - entfernt die benannte Authentifizierungs-Realm

ZUSAMMENFASSUNG
           delete-auth-realm [--help]
           [--target target]
           auth_realm-name

BESCHREIBUNG
       Der Unterbefehl delete-auth-realm entfernt die benannte
       Authentifizierungs-Realm. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie die Authentifizierungs-Realm
           entfernen. Gültige Werte sind

           server
               Löscht die Realm für die standardmäßige Serverinstanz server
               und ist der Standardwert.

           configuration_name
               Löscht die Realm für die benannte Konfiguration.

           cluster_name
               Löscht die Realm für jede Serverinstanz im Cluster.

           instance_name
               Löscht die Realm für eine bestimmte Serverinstanz.

OPERANDEN
       auth_realm_name
           Name der Realm, die gelöscht werden soll.

BEISPIELE
       Example 1, Authentifizierungs-Realms löschen
           In diesem Beispiel wird die Authentifizierungs-Realm db gelöscht.

               asadmin> delete-auth-realm db
               Command delete-auth-realm executed successfully

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-auth-realm(1), list-auth-realms(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010            delete-auth-realm(1)
