delete-message-security-Subcomandos de la Udelete-message-security-provider(1)

NOMBRE DE REFERENCIA
       delete-message-security-provider - Permite a los administradores
       suprimir un proveedor de seguridad de mensajes.

SINOPSIS
           delete-message-security-provider [--help] [--target target]
           --layer message_layer
           provider_name

DESCRIPCIÓN
       El subcomando delete-message-security-provider permite a los
       administradores suprimir un proveedor de seguridad de mensajes.

       Al ejecutar este subcomando, el subelemento provider-config del nivel
       de mensaje especificado (elemento message-security-config de
       domain.xml) se suprime. En el archivo domain.xml se especifican
       parámetros y propiedades de GlassFish Server. Las opciones
       especificadas en la siguiente lista se aplican a los atributos de los
       subelementos message-security-config y provider-config del archivo
       domain.xml .

       Si el nivel de mensaje (atributo message-security-config) no existe, se
       crea; a continuación se crea provider-config bajo el mismo.

       Este comando sólo está soportado en modo remoto.

OPCIONES
       Si una opción tiene un nombre corto, este nombre tiene prioridad sobre
       el nombre largo. Las opciones cortas están marcadas con un guión; las
       largas, con dos guiones.

       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va a suprimir el proveedor de
           seguridad de mensajes. Los valores válidos son:

           server
               Suprime el proveedor de seguridad de mensajes de la instancia
               de servidor por defecto
                server y es el valor por defecto.

           domain
               Suprime el proveedor de seguridad de mensajes del dominio.

           cluster_name
               Suprime el proveedor de seguridad de mensajes de todas las
               instancias de servidor del cluster.

           instance_name
               Suprime el proveedor de seguridad de mensajes de una instancia
               de servidor determinada.

       --layer
           Capa de mensaje del que se debe suprimir el proveedor. El valor por
           defecto es HttpServlet.

OPERANDOS
       provider_name
           El nombre del proveedor utilizado para hacer referencia al elemento
           provider-config.

EJEMPLOS
       Ejemplo 1, Supresión de un Proveedor de Seguridad de Mensajes
           En el ejemplo siguiente se muestra cómo suprimir un proveedor de
           seguridad de mensajes de un cliente.

               asadmin> delete-message-security-provider
               --layer SOAP mySecurityProvider

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-message-security-provider(1), list-message-security-providers(1)

       asadmin(1M)

Java EE 7                   23 de diciembredelete-message-security-provider(1)
