delete-jacc-provider(1)   asadmin 유틸리티 하위 명령   delete-jacc-provider(1)

제목
       delete-jacc-provider - 관리자가 도메인에 대해 정의된 JACC 제공자를
       삭제할 수 있음

시놉시스
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

설명
       delete-jacc-provider 하위 명령을 사용하여 관리자는 도메인에 대해 정의된
       JACC 제공자를 삭제할 수 있습니다. JACC 제공자는 도메인의 domain.xml
       파일에 있는 security-service 요소의 jacc-provider 요소로 정의됩니다.
       JACC 제공자는 GlassFish Server 관리 콘솔 또는 create-jacc-provider 하위
       명령을 사용하여 생성할 수 있습니다.

       기본 GlassFish Server 설치에는 default와 simple이라는 두 개의 JACC
       제공자가 포함됩니다. 이러한 기본 제공자를 삭제해서는 안됩니다.

       인증을 위해 GlassFish Server에서 사용하는 JACC 제공자는 domain.xml에
       있는 security-service의 jacc-provider 요소로 식별됩니다. 그러므로
       jacc-provider 제공자를 삭제하면 security-service에 존재하는 일부 다른
       JACC 제공자의 이름으로 jacc-provider를 변경해야 합니다.

       다른 JACC 제공자를 가리키도록 jacc-provider 요소를 변경하면 GlassFish
       Server을(를) 재시작해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       옵션에 짧은 옵션 이름이 있는 경우 짧은 옵션이 긴 옵션 이름보다
       우선합니다. 짧은 옵션은 대시가 하나인 반면 긴 옵션은 대시가 두
       개입니다.

       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           JACC 제공자를 삭제할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에서 JACC 제공자를 삭제합니다. 이 값은
               기본값입니다.

           configuration_name
               지정된 구성에서 JACC 제공자를 삭제합니다.

           cluster_name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 JACC 제공자를
               삭제합니다.

           instance_name
               지정된 서버 인스턴스에서 JACC 제공자를 삭제합니다.

피연산자
       jacc-provider-name
           삭제할 JACC 제공자의 이름입니다.

예제
       예 1, JACC 제공자 삭제
           다음 예제는 기본 도메인에서 testJACC라는 JACC 제공자를 삭제하는
           방법을 보여줍니다.

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                       2011년 6월 14일        delete-jacc-provider(1)
