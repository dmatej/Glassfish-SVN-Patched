delete-file-user(1)     Subcomandos do utilitário asad     delete-file-user(1)

NOME
       delete-file-user - remove o usuário do arquivo com o nome

SINOPSE
           delete-file-user [--help][--authrealmname auth_realm_name]
           [--target target]
           username

DESCRIçãO
       O subcomando delete-file-user deleta a entrada no arquivo-chave para o
       nome de usuário especificado.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --authrealmname
           O nome do realm de autenticação com o qual o usuário foi criado.

       --target
           É o nome do alvo no qual o comando opera. Os alvos válidos são:

           server
               Deleta o usuário do arquivo da instância de servidor default.
               Este é o valor default

           domain
               Deleta o usuário do arquivo no domínio.

           cluster_name
               Deleta o usuário do arquivo de cada instância de servidor do
               cluster.

           instance_name
               Deleta o usuário do arquivo de uma instância de servidor
               específica.

OPERANDOS
       username
           Esse é o nome do usuário do arquivo a ser deletado.

EXEMPLOS
       Example 1, Deletando um Usuário de um Realm de Arquivo
           O exemplo a seguir mostra como deletar o usuário com o nome
           sample_user de um realm do arquivo.

               asadmin> delete-file-user
               sample_user
               Command delete-file-user executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-file-user(1), list-file-users(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                         01 dez 2010              delete-file-user(1)
