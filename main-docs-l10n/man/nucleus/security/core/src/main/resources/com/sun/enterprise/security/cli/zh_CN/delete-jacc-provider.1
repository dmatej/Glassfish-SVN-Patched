delete-jacc-provider(1)     asadmin 实用程序子命令     delete-jacc-provider(1)

名称
       delete-jacc-provider - 允许管理员删除为域定义的 JACC 提供方

提要
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

说明
       delete-jacc-provider 子命令允许管理员删除为域定义的 JACC 提供方。JACC
       提供方以域的 domain.xml 文件中 security-service 元素的 jacc-provider
       元素来定义。可以使用 GlassFish Server 管理控制台或 create-jacc-provider
       子命令创建 JACC 提供方。

       默认 GlassFish Server 安装包括名为默
       提供方。不能删除这些默认提供方。

       GlassFish Server 用于授权的 JACC 提供方由 domain.xml 中
       security-service 的 jacc-provider 元素标识。因此，如果删除
       jacc-provider 提供方，请确保将 jacc-provider 更改为 security-service
       下存在的某些其他 JACC 提供方的名称。

       如果将 jacc-provider 元素更改为指向其他 JACC 提供方，必须重新启动
       GlassFish Server。

       该子命令仅在远程模式下受支持。

选项
       如果某个选项具有短选项名称，则短选项优先于长选项名称。短选项具有一个短划线，而长选项具有两个短划线。

       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从中删除 JACC 提供方的目标。以下是有效值：

           server
               删除默认服务器实例上的 JACC 提供方。这是默认值。

           configuration_name
               删除指定配置中的 JACC 提供方。

           cluster_name
               删除指定集群的所有服务器实例中的 JACC 提供方。

           instance_name
               删除指定服务器实例上的 JACC 提供方。

操作数
       jacc-provider-name
           要删除的 JACC 提供方的名称。

示例
       Example 1, 删除 JACC 提供方
           以下示例显示了如何从默认域中删除名为 testJACC 的 JACC 提供方。

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 14 日       delete-jacc-provider(1)
