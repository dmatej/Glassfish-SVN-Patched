list-message-security-providasadmin 实用程序list-message-security-providers(1)

名称
       list-message-security-providers - 列出指定消息层的所有安全消息提供方

提要
           list-message-security-providers [--help]
           --layer message_layer
           [target]

说明
       使用 list-message-security-providers 子命令，管理员可为给定消息层
       (domain.xml 的 message-security-config 元素) 列出所有安全性消息提供方
       (provider-config 子元素)。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --layer
           必须列出提供方的消息层。默认值为 HttpServlet。

操作数
       target
           将列出的内容限制为特定目标的消息安全提供方。有效值包括：

           server
               列出默认服务器实例 server 的提供方，这是默认值。

           domain
               列出域的提供方。

           cluster
               列出集群中服务器实例的提供方。

           instance
               列出特定服务器实例的提供方。

示例
       Example 1, 列出消息安全提供方
           以下示例说明如何为消息层列出消息安全提供方。

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 list-message-security-providers(1)
