list-auth-realms(1)       asadmin 유틸리티 하위 명령       list-auth-realms(1)

제목
       list-auth-realms - 인증 영역 나열

시놉시스
           list-auth-realms [--help] [target]

설명
       list-auth-realms 하위 명령은 인증 영역을 나열합니다. 이 하위 명령은
       원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           인증 영역을 나열할 대상의 이름입니다.

           server
               기본 서버 인스턴스 server의 영역을 나열하며 기본값입니다.

           configuration_name
               명명된 구성의 영역을 나열합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스의 영역을 나열합니다.

           instance_name
               특정 서버 인스턴스의 영역을 나열합니다.

예제
       예 1, 인증 영역 나열

               asadmin> list-auth-realms
               file
               ldap
               certificate
               db
               Command list-auth-realms executed successfully
           여기서 file, ldap, certificate 및 db는 사용 가능한 인증 영역입니다.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-auth-realm(1), delete-auth-realm(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일            list-auth-realms(1)
