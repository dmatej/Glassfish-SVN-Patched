delete-auth-realm(1)    Comandi secondari della utilit    delete-auth-realm(1)

NOME
       delete-auth-realm - rimuove il realm di autenticazione specificato

SINTASSI
           delete-auth-realm [--help]
           [--target target]
           auth_realm-name

DESCRIZIONE
       Il comando secondario delete-auth-realm consente di rimuovere il realm
       di autenticazione specificato. Questo comando secondario è supportato
       solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione da cui viene eliminato il realm di
           autenticazione. Di seguito sono riportati i valori validi.

           server
               Elimina il realm per l'istanza server predefinita server ed è
               il valore predefinito.

           configuration_name
               Elimina il realm per la configurazione specificata.

           cluster_name
               Elimina il realm per ogni istanza server del cluster.

           instance_name
               Elimina il realm per un'istanza server particolare.

OPERANDI
       auth_realm_name
           Il nome del realm da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un realm di autenticazione
           In questo esempio viene eliminato il realm di autenticazione db.

               asadmin> delete-auth-realm db
               Command delete-auth-realm executed successfully

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-auth-realm(1), list-auth-realms(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010           delete-auth-realm(1)
