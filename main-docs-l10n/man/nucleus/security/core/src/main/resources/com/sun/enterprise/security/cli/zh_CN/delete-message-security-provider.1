delete-message-security-provasadmin 实用程序

名称
       delete-message-security-provider - 允许管理员删除消息安全提供方

提要
           delete-message-security-provider [--help] [--target target]
           --layer message_layer
           provider_name

说明
       delete-message-security-provider 子命令允许管理员删除消息安全提供方。

       运行此子命令时，将删除给定消息层 (domain.xml 的 message-security-config
       元素) 的 provider-config 子元素。domain.xml 文件指定 GlassFish Server
       的参数和属性。以下列表中指定的选项适用于 domain.xml 文件的
       message-security-config 和 provider-config 子元素中的属性。

       如果消息层 (message-security-config 属性)
       不存在，则创建该属性，然后在该属性下创建 provider-config。

       该命令仅在远程模式下受支持。

选项
       如果某个选项具有短选项名称，则短选项优先于长选项名称。短选项具有一个短划线，而长选项具有两个短划线。

       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从中删除消息安全提供方的目标。有效值为

           server
               从默认服务器实例 server 中删除消息安全提供方，这是默认值

           domain
               从域中删除消息安全提供方。

           cluster_name
               从集群中的每个服务器实例中删除消息安全提供方。

           instance_name
               从特定服务器实例中删除消息安全提供方。

       --layer
           必须从中删除提供方的消息层。默认值为 HttpServlet。

操作数
       provider_name
           用于引用 provider-config 元素的提供方的名称。

示例
       Example 1, 删除消息安全提供方
           以下示例说明如何为客户机删除消息安全提供方。

               asadmin> delete-message-security-provider
               --layer SOAP mySecurityProvider

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-message-security-provider(1), list-message-security-providers(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月delete-message-security-provider(1)
