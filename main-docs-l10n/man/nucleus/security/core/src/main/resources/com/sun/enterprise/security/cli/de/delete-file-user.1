delete-file-user(1)     Unterbefehle des asadmin-Utili     delete-file-user(1)

NAME
       delete-file-user - entfernt den benannten Dateibenutzer

ZUSAMMENFASSUNG
           delete-file-user [--help][--authrealmname auth_realm_name]
           [--target target]
           username

BESCHREIBUNG
       Der Unterbefehl delete-file-user löscht den Eintrag in der
       Schlüsseldatei für den angegebenen Benutzernamen.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --authrealmname
           Der Name der Authentifizierungs-Realm, mit der der Benutzer
           erstellt wurde.

       --target
           Dies ist der Name des Ziels, dem der Befehl gilt. Gültige Werte
           sind:

           server
               Löscht den Dateibenutzer auf der standardmäßigen Serverinstanz.
               Dies ist der Standardwert

           domain
               Löscht den Dateibenutzer in der Domain.

           cluster_name
               Löscht den Dateibenutzer aus jeder Serverinstanz im Cluster.

           instance_name
               Löscht den Dateibenutzer aus einer bestimmten Serverinstanz.

OPERANDEN
       username
           Dies ist der Name des Dateibenutzers, der gelöscht werden soll.

BEISPIELE
       Example 1, Benutzer aus einer Datei-Realm löschen
           Das folgende Beispiel zeigt, wie ein Benutzer mit dem Namen
           sample_user aus einer Datei-Realm gelöscht wird.

               asadmin> delete-file-user
               sample_user
               Command delete-file-user executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-file-user(1), list-file-users(1), update-file-user(1),
       list-file-groups(1)

       asadmin(1M)

Java EE 7                      01. Dezember 2010           delete-file-user(1)
