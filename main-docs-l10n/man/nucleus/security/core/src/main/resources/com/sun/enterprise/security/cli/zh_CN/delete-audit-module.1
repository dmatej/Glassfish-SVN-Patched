delete-audit-module(1)      asadmin 实用程序子命令      delete-audit-module(1)

名称
       delete-audit-module - 删除命名的审计模块

提要
           delete-audit-module [--help]
           [--target target]
           audit_module_name

说明
       此子命令删除命名的审计模块。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要删除审计模块的目标。有效值如下：

           server
               删除默认服务器实例 server 的审计模块，这是默认值。

           configuration_name
               删除命名配置的审计模块。

           cluster_name
               删除集群中每个服务器实例的审计模块。

           instance_name
               删除特定服务器实例的审计模块。

操作数
       audit_module_name
           要删除的审计模块的名称。

示例
       Example 1, 删除审计模块

               asadmin> delete-audit-module sampleAuditModule
               Command delete-audit-module executed successfully

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-audit-module(1), list-audit-modules(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日       delete-audit-module(1)
