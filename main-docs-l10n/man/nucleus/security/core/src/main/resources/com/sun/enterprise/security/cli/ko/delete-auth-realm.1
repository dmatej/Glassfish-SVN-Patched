delete-auth-realm(1)      asadmin 유틸리티 하위 명령      delete-auth-realm(1)

제목
       delete-auth-realm - 이름이 지정된 인증 영역 제거

시놉시스
           delete-auth-realm [--help]
           [--target target]
           auth_realm-name

설명
       delete-auth-realm 하위 명령은 이름이 지정된 인증 영역을 제거합니다. 이
       하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           인증 영역을 삭제할 대상을 지정합니다. 적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스 server에 대한 영역을 삭제하고 이는
               기본값입니다.

           configuration_name
               이름이 지정된 구성에 대한 영역을 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 영역을 삭제합니다.

           instance_name
               특정 서버 인스턴스에 대한 영역을 삭제합니다.

피연산자
       auth_realm_name
           삭제할 영역의 이름입니다.

예제
       예 1, 인증 영역 삭제
           이 예제는 인증 영역 db를 삭제합니다.

               asadmin> delete-auth-realm db
               Command delete-auth-realm executed successfully

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-auth-realm(1), list-auth-realms(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일           delete-auth-realm(1)
