list-message-security-prUnterbefehle des asalist-message-security-providers(1)

NAME
       list-message-security-providers - listet alle
       Sicherheitsmeldungsprovider für die angegebene Meldungsschicht auf

ZUSAMMENFASSUNG
           list-message-security-providers [--help]
           --layer message_layer
           [target]

BESCHREIBUNG
       Der Unterbefehl list-message-security-providers ermöglicht es
       Administratoren, alle Sicherheitsmeldungsprovider
       (provider-config-Unterelemente) für die angegebene Meldungsschicht
       (message-security-config-Element von domain.xml) aufzulisten.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --layer
           Die Meldungsschicht, für die der Provider aufgelistet werden soll.
           Der Standardwert ist HttpServlet.

OPERANDEN
       target
           Beschränkt die Auflistung auf Meldungssicherheitsprovider für ein
           bestimmtes Ziel. Gültige Werte sind:

           server
               Listet Provider für die standardmäßige Serverinstanz server auf
               und ist der Standardwert.

           domain
               Listet Provider für die Domain auf.

           cluster
               Listet Provider für die Serverinstanzen im Cluster auf.

           instance
               Listet Provider für eine bestimmte Serverinstanz auf.

BEISPIELE
       Beispiel 1, Meldungssicherheitsprovider auflisten
           Das folgende Beispiel zeigt, wie Meldungssicherheitsprovider für
           eine Meldungsschicht aufgelistet werden.

               asadmin> list-message-security-providers
               --layer SOAP
               XWS_ClientProvider
               ClientProvider
               XWS_ServerProvider
               ServerProvider
               Command list-message-security-providers executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-message-security-provider(1),
       delete-message-security-provider(1)

       asadmin(1M)

Java EE 7                        22. Dez. 20list-message-security-providers(1)
