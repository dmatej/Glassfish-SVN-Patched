delete-audit-module(1)  Subcomandos de la Utilidad asa  delete-audit-module(1)

NOMBRE DE REFERENCIA
       delete-audit-module - Elimina el módulo de auditoría con nombre.

SINOPSIS
           delete-audit-module [--help]
           [--target target]
           audit_module_name

DESCRIPCIÓN
       Este subcomando elimina el módulo de auditoría con nombre. Este
       subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino en el que se va a suprimir el módulo de
           auditoría. Los valores válidos son los siguientes:

           server
               Suprime el módulo de auditoría de la instancia de servidor por
               defecto server y es el valor por defecto.

           configuration_name
               Suprime el módulo de auditoría de la configuración con nombre.

           cluster_name
               Suprime el módulo de auditoría para cada instancia de servidor
               en el cluster.

           instance_name
               Suprime el módulo de auditoría para una determinada instancia
               de servidor.

OPERANDOS
       audit_module_name
           Nombre del módulo de auditoría que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Módulo de Auditoría

               asadmin> delete-audit-module sampleAuditModule
               Command delete-audit-module executed successfully

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-audit-module(1), list-audit-modules(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de 2010     delete-audit-module(1)
