list-audit-modules(1) asadminユーティリティのサブコマンドlist-audit-modules(1)

名前
       list-audit-modules - すべての監査モジュールを取得して表示します

概要
           list-audit-modules [--help] [target]

説明
       list-audit-modulesサブコマンドはすべての監査モジュールをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           監査モジュールをリストするターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverの監査モジュールをリストします(デフォルト値)。

           configuration_name
               名前付き構成の監査モジュールをリストします。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスの監査モジュールをリストします。

           instance_name
               特定のサーバー・インスタンスの監査モジュールをリストします。

例
       例 1, 監査モジュールのリスト

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

Java EE 7                       2010年12月22日           list-audit-modules(1)
