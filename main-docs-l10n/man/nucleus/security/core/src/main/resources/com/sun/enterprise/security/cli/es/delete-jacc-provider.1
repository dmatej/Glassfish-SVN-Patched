delete-jacc-provider(1) Subcomandos de la Utilidad asa delete-jacc-provider(1)

NOMBRE DE REFERENCIA
       delete-jacc-provider - Permite a los administradores suprimir
       proveedores de JACC definidos para un dominio.

SINOPSIS
           delete-jacc-provider [--help]
           [--target target] jacc-provider-name

DESCRIPCIÓN
       El subcomando delete-jacc-provider permite a los administradores
       suprimir proveedores de JACC definidos para un dominio. Los proveedores
       JACC se definen como elementos jacc-provider del elemento
       security-service en el archivo dominio.xml del dominio. Los proveedores
       JACC se pueden crear utilizando la Consola de administración de
       GlassFish Server o el subcomando create-jacc-provider.

       La instalación por defecto de GlassFish Server incluye dos proveedores
       JACC denominados default y simple. Estos proveedores por defecto no se
       deben suprimir.

       El proveedor JACC utilizado porGlassFish Server para la autorización se
       identifica mediante el elemento jacc-provider del security-service en
       domain.xml. Por lo tanto, si suprime el proveedor jacc-provider,
       asegúrese de cambiar también jacc-provider para el nombre de algún otro
       proveedor de JACC que exista en security-service.

       Si cambia el elemento jacc-provider para que apunte a un proveedor JACC
       diferente, debe reiniciar GlassFish Server.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       Si una opción tiene un nombre corto, este nombre tiene prioridad sobre
       el nombre largo. Las opciones cortas están marcadas con un guión; las
       largas, con dos guiones.

       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va a suprimir el proveedor JACC.
           Los valores válidos son:

           server
               Suprime el proveedor JACC de la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration_name
               Suprime el proveedor JACC de la configuración especificada.

           cluster_name
               Suprime el proveedor JACC de todas las instancias de servidor
               del cluster especificado.

           instance_name
               Suprime el proveedor JACC de una instancia de servidor
               especificada.

OPERANDOS
       jacc-provider-name
           Nombre del proveedor JACC que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Proveedor JACC
           En el ejemplo siguiente se muestra cómo suprimir un proveedor JACC
           denominado testJACC del dominio por defecto.

               asadmin> delete-jacc-provider testJACC

               Command delete-jacc-provider executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jacc-provider(1), list-jacc-providers(1)

       asadmin(1M)

Java EE 7                     14 de junio de 2011      delete-jacc-provider(1)
