delete-local-instance(1)    asadmin 公用程式子命令    delete-local-instance(1)

參考名
       delete-local-instance - 在執行子命令的機器上刪除 GlassFish Server
       執行處理

概要
           delete-local-instance [--help]
           [--nodedir node-dir] [--node node-name]
           [instance-name]

描述
       delete-local-instance 子命令會在執行此子命令的機器上刪除 GlassFish
       Server 執行處理。這個子命令不需要組態分散式元件物件模型 (DCOM)
       遠端協定或安全 Shell (SSH)。您必須從執行處理常駐的機器執行此命令。

       無論 GlassFish Server
       執行處理的建立方式為何，這個子命令都能夠刪除任何執行處理。例如，此子命令可以刪除使用
       create-instance(1) 子命令建立的執行處理。

       要刪除的執行處理不得處於執行狀態。否則，將發生錯誤。

       此子命令透過執行下列動作刪除執行處理：

       ·   從網域管理伺服器 (DAS) 的組態移除執行處理

       ·   從檔案系統刪除執行處理檔案

       如果要刪除的執行處理是唯一使用節點目錄的執行處理，則也會移除該目錄。

       如果刪除獨立執行處理，則也會刪除該執行處理的獨立組態。參照名為
       instance-name-config
       的組態，且沒有其他叢集或非叢集的組態參照的獨立執行處理。

       delete-local-instance 子命令不會連絡 DAS
       來確定執行處理所在的節點。若要確定執行處理所在的節點，該子命令會搜尋包含節點目錄的目錄。如果存在多個節點目錄，則必須將此節點指定為子命令的選項。

       如果未指定運算元且只有一個執行處理位於指定節點上，則子命令會刪除該執行處理。如果未指定運算元且有多個執行處理位於節點上，便會發生錯誤。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --nodedir
           指定包含執行處理節點目錄的目錄。執行處理檔案儲存在執行處理的節點目錄中。預設值為
           as-install/nodes。

       --node
           指定執行處理所在的節點。僅在 --nodedir
           選項指定的目錄只包含一個節點目錄時，才可忽略此選項。否則，需要使用此選項。

運算元
       instance-name
           要刪除的執行處理名稱。如果只有一個執行處理位於指定節點上，才有可能會省略此運算元。否則，需要有此運算元。

範例
       範例 1, 刪除執行處理
           此範例會刪除執行處理 pmdsainst。

               asadmin> delete-local-instance pmdsainst

               Command delete-local-instance executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-instance(1), create-local-instance(1), delete-instance(1),
       start-instance(1), start-local-instance(1), stop-instance(1),
       stop-local-instance(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日      delete-local-instance(1)
