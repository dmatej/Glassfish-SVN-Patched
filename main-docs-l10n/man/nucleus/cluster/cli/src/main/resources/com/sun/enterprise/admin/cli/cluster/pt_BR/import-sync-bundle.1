import-sync-bundle(1)   Subcomandos do utilitário asad   import-sync-bundle(1)

NOME
       import-sync-bundle - Importa os dados de configuração de uma instância
       clusterizada ou standalone de um arquivo compactado

SINOPSE
           import-sync-bundle [--help]
           --instance instance-name
           [--nodedir node-dir] [--node node-name]
           file-name

DESCRIçãO
       O subcomando import-sync-bundle importa os dados de configuração de uma
       instância clusterizada ou de uma instância standalone de um arquivo
       compactado criado pelo subcomando export-sync-bundle(1).

       É preciso executar este subcomando no host onde a instância reside.
       Para contatar o servidor de administração de domínios (DAS), este
       subcomando requer o nome do host onde este DAS está em execução. Se uma
       porta não default for utilizada para a administração, este subcomando
       também requer o número da porta. É preciso fornecer essas informações
       por meio da opção --host e da opção --port do utilitário asadmin(1M).

       A importação dos dados da configuração de uma instância, transfere os
       dados a um host para uma instância sem a necessidade de ser capaz de se
       comunicar com o DAS. A importação dos dados da configuração de uma
       instância é normalmente necessária pelos seguintes motivos:

       ·   Para restabelecer a instância após um upgrade

       ·   Para sincronizar manualmente a instância com o servidor de
           administração de domínios (DAS) quando a instância não pode
           contatar o DAS

       O subcomando importa os dados da configuração de uma instância ao
       executar as seguintes operações:

       ·   Criando ou atualizando arquivos e diretórios da instância

       ·   Tentando registrar a instância com o DAS

       Se a tentativa de registrar a instância com o DAS falhar, o subcomando
       não falhará. Em vez disso, o subcomando exibirá uma advertência de que
       a tentativa falhou. A advertência contém o comando a ser executado para
       registrar a instância com o DAS.

       O subcomando import-sync-bundle não contata o DAS para determinar o nó
       no qual a instância reside. Se o nó não for especificado como uma opção
       do subcomando, o subcomando determinará o nó da configuração DAS no
       arquivo compactado.

       Este subcomando é suportado só no modo local.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --instance
           A instância para a qual os dados da configuração estão sendo
           importados. A instância já precisa existir na configuração DAS. O
           arquivo compactado do qual os dados estão sendo importados precisa
           conter dados para a instância especificada.

       --nodedir
           O diretório que contém o diretório do nó da instância. Os arquivos
           da instância são armazenados no diretório do nó da instância. O
           default é as-install/nodes.

       --node
           O nó no qual a instância reside. Se essa opção for omitida, o
           subcomando determinará o nó da configuração DAS no arquivo
           compactado.

OPERANDOS
       file-name
           O nome do arquivo compactado, incluindo o caminho, que contém o
           arquivo compactado a ser importado. Este operando é necessário.

EXEMPLOS
       Example 1, Importando Dados da Configuração para uma Instância
       Clusterizada
           Este exemplo importa a configuração para a instância clusterizada
           ymli2 no nó sj02 do arquivo compactado
           /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip.

           O comando é executado no host sj02, que é o host que o nó sj02
           representa. O DAS está sendo executado no host sr04 e usa a porta
           HTTP default para a administração.

               sj02# asadmin --host sr04 import-sync-bundle --node sj02 --instance ymli2
               /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip
               Command import-sync-bundle executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       export-sync-bundle(1)

       asadmin(1M)

Java EE 7                         7 dez 2010             import-sync-bundle(1)
