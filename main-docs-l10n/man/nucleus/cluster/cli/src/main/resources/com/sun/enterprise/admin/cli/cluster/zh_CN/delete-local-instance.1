delete-local-instance(1)    asadmin 实用程序子命令    delete-local-instance(1)

名称
       delete-local-instance - 删除运行此子命令的计算机上的 GlassFish Server
       实例

提要
           delete-local-instance [--help]
           [--nodedir node-dir] [--node node-name]
           [instance-name]

说明
       delete-local-instance 子命令删除运行此子命令的计算机上的 GlassFish
       Server 实例。此子命令不要求配置分布式组件对象模型 (DCOM) 远程协议或安全
       shell (SSH)。必须从实例所在的计算机运行此命令。

       此子命令可删除任何 GlassFish Server
       实例，而不考虑实例的创建方式。例如，此子命令可删除使用
       create-instance(1) 子命令创建的实例。

       要删除的实例不能处于运行状态。否则，会出现错误。

       该子命令通过执行以下操作删除实例：

       ·   从域管理服务器 (DAS) 的配置删除实例

       ·   从文件系统删除实例的文件

       如果要删除的实例是唯一使用节点目录的实例，该目录也将一同删除。

       如果删除独立实例，该实例的独立配置也将删除。独立实例引用名为
       instance-name-config 的配置，任何其他集群或非集群实例都没有引用该配置。

       delete-local-instance 子命令不会联系 DAS
       来确定该实例所在的节点。为了确定该实例所在的节点，该子命令将搜索包含节点目录的目录。如果存在多个节点目录，必须将相应节点指定为该子命令的一个选项。

       如果未指定操作数且仅有一个实例位于指定节点，则该子命令将删除该实例。如果未指定操作数且有多个实例位于该节点，将会出现错误。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --nodedir
           指定包含该实例的节点目录的目录。该实例的文件存储在实例的节点目录中。默认值为
           as-install/nodes。

       --node
           指定该实例所在的节点。只有当 --nodedir
           选项指定的目录仅包含一个节点目录时，才能忽略此选项。否则，该选项是必需的。

操作数
       instance-name
           要删除的实例的名称。如果仅有一个实例位于指定节点，则可省略该操作数。否则，该操作数是必需的。

示例
       Example 1, 删除实例
           此示例删除 pmdsainst 实例。

               asadmin> delete-local-instance pmdsainst

               Command delete-local-instance executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-instance(1), create-local-instance(1), delete-instance(1),
       start-instance(1), start-local-instance(1), stop-instance(1),
       stop-local-instance(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日      delete-local-instance(1)
