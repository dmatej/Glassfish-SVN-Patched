setup-local-dcom(1)         asadmin 实用程序子命令         setup-local-dcom(1)

名称
       setup-local-dcom - 在主机上设置 DCOM

提要
           setup-local-dcom [--help] [--verbose={false|true}]
           --force={false|true}

说明
       setup-local-dcom 子命令可在运行该子命令的主机上设置分布式组件对象模型
       (DCOM) 远程协议。DCOM 远程协议仅在 Windows 系统上可用。

       必须由 operating-system-level 管理员用户在设置 DCOM
       的主机上运行该子命令。在运行该命令之前，请确保如 Oracle GlassFish
       Server High Availability Administration Guide 中的 Windows Operating
       System Settings 所述设置了 Windows 操作系统中的各项。

       在某台主机上设置 DCOM
       将确保可从远程主机在该主机上运行脚本。要从远程主机在该主机上运行脚本，必须允许
       Windows 用户完全控制某些 Windows 注册表项。

       在某些 Windows 版本中，只有用户 NT SERVICE\TrustedInstaller 拥有对这些
       Windows 注册表项的完全控制权限。如果主机上的 Windows
       版本以这种方式进行了配置，则该子命令将修改这些注册表项以允许 Windows
       用户完全控制它们。

           小心
           ┌──────────────────────────────────────────────────────────┐
           │注意                                                      │
           │                                                          │
           │           该子命令可能会修改 Windows                     │
           │           注册表中某些项的权限。在运行该命令之前，请备份 │
           │           Windows 注册表。                               │
           └──────────────────────────────────────────────────────────┘

       默认情况下，该子命令会提示您确认要更新 Windows
       注册表。要在无提示情况下运行该子命令，请将 --force 选项设置为真

       该子命令仅在本地模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --verbose, -v
           指定该子命令是否显示有关为在主机上设置 DCOM 而执行的操作的信息。

           可能的值如下：

           假
               不显示有关为设置 DCOM 而执行的操作的信息 (默认值)。

           真
               该子命令显示有关为在主机上设置 DCOM 而执行的操作的信息。

       --force, -f
           指定该子命令是否提示您确认要更新 Windows 注册表。

           可能的值如下：

           假
               会提示您确认要更新 Windows 注册表 (默认值)。

           真
               该子命令强制更新 Windows 注册表而不提示您。

示例
       Example 1, 在主机上设置 DCOM
           此示例在运行子命令的主机上设置 DCOM。

               asadmin> setup-local-dcom
               Caution: This command might modify the permissions of some keys in the Windows
               registry.
               Before running this command, back up the Windows registry.
               The modification allows the Windows user full control over these keys.

               Are you sure that you want to edit the Windows registry? If so, type yes in full: yes
               Command setup-local-dcom executed successfully.

       Example 2, 强制在主机上设置 DCOM
           此示例设置 DCOM 而不提示确认用户要更新 Windows 注册表。

               asadmin> setup-local-dcom --force=true
               Command setup-local-dcom executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       asadmin(1M)

       Oracle GlassFish Server High Availability Administration Guide 中的
       Windows Operating System Settings

Java EE 7                     2012 年 2 月 10 日           setup-local-dcom(1)
