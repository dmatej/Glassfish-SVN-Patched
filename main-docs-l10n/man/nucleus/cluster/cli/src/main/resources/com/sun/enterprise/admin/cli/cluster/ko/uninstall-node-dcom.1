uninstall-node-dcom(1)    asadmin 유틸리티 하위 명령    uninstall-node-dcom(1)

제목
       uninstall-node-dcom - 지정된 DCOM 사용 호스트에서 GlassFish Server
       소프트웨어 설치 해제

시놉시스
           uninstall-node-dcom [--help]
           [--installdir as-install-parent]
           [--windowsuser windows-user] [--windowsdomain windows-domain]
           [--force={false|true}]
           host-list

설명
       uninstall-node-dcom 하위 명령은 하위 명령의 피연산자로 지정된
       호스트에서 GlassFish Server 소프트웨어의 설치를 해제합니다. 이 하위
       명령을 사용하려면 하위 명령이 실행되는 호스트와 GlassFish Server
       소프트웨어의 설치를 해제할 각 호스트에 DCOM(Distributed Component
       Object Model) 원격 프로토콜이 구성되어 있어야 합니다.

           참고
           ┌───────────────────────────────────────┐
           │주                                     │
           │                                       │
           │           SSH(보안 셸)를 사용하는     │
           │           호스트에서 GlassFish Server │
           │           소프트웨어의 설치를         │
           │           해제하려면                  │
           │           uninstall-node-ssh(1) 하위  │
           │           명령을 사용하십시오.        │
           └───────────────────────────────────────┘

       기본적으로 미리 정의된 노드 localhost-domain을 제외한 노드가 GlassFish
       Server 소프트웨어를 제거하는 호스트에 상주하는 경우 하위 명령을
       실패합니다. 사용자 정의 노드가 상주하는 호스트에서 GlassFish Server
       소프트웨어를 제거하려면 --force 옵션을 true로 설정합니다.  --force
       옵션이 true이면 하위 명령이 기본 설치 디렉토리의 상위의 전체 내용을
       제거합니다.

       기본 설치 디렉토리의 상위에 있는 파일이 열려 있는 경우 하위 명령을
       실패합니다.

       여러 호스트를 지정한 경우 다음 항목의 구성이 모든 호스트에서 동일해야
       합니다.

       ·   GlassFish Server 소프트웨어에 대한 기본 설치 디렉토리의 상위

       ·   Windows 사용자

       ·   Windows 도메인

       하위 명령은 DAS(도메인 관리 서버)의 구성을 수정하지 않습니다.

       이 하위 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --installdir
           각 호스트에 GlassFish Server 소프트웨어를 설치할 기본 설치
           디렉토리의 상위에 대한 절대 경로(예: C:\glassfish3)입니다.

           이 하위 명령을 실행할 사용자가 지정된 디렉토리에 대한 쓰기 권한을
           가지고 있어야 합니다. 그렇지 않으면 오류가 발생합니다.

           지정된 디렉토리에 호스트의 GlassFish Server 소프트웨어가 설치가
           포함되어야 합니다. 그렇지 않으면 오류가 발생합니다.

           기본값은 이 하위 명령이 실행되는 호스트에 있는 GlassFish Server
           소프트웨어에 대한 기본 설치 디렉토리의 상위입니다.

       --windowsuser, -w
           DCOM을 통해 호스트에 접속하기 위한 프로세스를 실행할 이 노드의
           호스트 사용자입니다. 기본값은 DAS 프로세스를 실행할 사용자입니다.

       --windowsdomain, -d
           --windowsuser 옵션에서 지정된 사용자가 포함되는 Windows 도메인의
           이름입니다. 기본값은 하위 명령이 실행되는 호스트의 이름입니다.

       --force
           사용자 정의 노드가 호스트에 상주하는 경우에도 하위 명령이
           호스트에서 GlassFish Server 소프트웨어의 설치를 해제할지 여부를
           지정합니다. 가능한 값은 다음과 같습니다.

           false
               사용자 정의 노드가 호스트에 상주하는 경우 소프트웨어가 제거되지
               않고 하위 명령을 실패합니다(기본값).

               --force 옵션이 false이면 하위 명령이 GlassFish Server
               소프트웨어 파일만 제거합니다. 구성 파일 등 기본 설치 디렉토리의
               상위에 있는 기타 콘텐츠는 제거되지 않습니다.

           true
               사용자 정의 노드가 호스트에 상주하는 경우에도 하위 명령이
               호스트에서 GlassFish Server 소프트웨어의 설치를 해제합니다.

               --force 옵션이 true이면 하위 명령이 기본 설치 디렉토리의 상위의
               전체 내용을 제거합니다.

피연산자
       host-list
           GlassFish Server 소프트웨어의 설치를 해제할 공백으로 구분된 호스트
           이름 목록입니다.

예제
       예 1, 기본 위치에서 GlassFish Server 소프트웨어 설치 해제
           이 예제는 호스트 wpmdl1.example.com 및 wpmdl2.example.com의 기본
           위치에서 GlassFish Server 소프트웨어의 설치를 해제합니다.

               asadmin> uninstall-node-dcom wpmdl1 wpmdl2
               Command uninstall-node-dcom executed successfully.

       예 2, 강제로 GlassFish Server 소프트웨어 설치 해제
           이 예제는 호스트 xkyd.example.com에서 GlassFish Server 소프트웨어의
           설치를 해제합니다.

           사용자 정의 노드가 호스트에 상주하는 경우에도 소프트웨어의 설치가
           해제됩니다.  C:\glassfish3 디렉토리의 전체 콘텐츠가 제거됩니다.

               asadmin> uninstall-node-dcom --force --installdir C:\glassfish3 xkyd.example.com
               Command uninstall-node-dcom executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       install-node-dcom(1), uninstall-node-ssh(1)

       asadmin(1M)

Java EE 7                       2012년 1월 5일          uninstall-node-dcom(1)
