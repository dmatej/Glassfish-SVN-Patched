import-sync-bundle(1)     asadmin 유틸리티 하위 명령     import-sync-bundle(1)

제목
       import-sync-bundle - 아카이브 파일에서 클러스터된 인스턴스 또는 독립형
       인스턴스의 구성 데이터 임포트

시놉시스
           import-sync-bundle [--help]
           --instance instance-name
           [--nodedir node-dir] [--node node-name]
           file-name

설명
       import-sync-bundle 하위 명령은 export-sync-bundle(1) 하위 명령으로
       생성된 아카이브 파일에서 클러스터된 인스턴스 또는 독립형 인스턴스의
       구성 데이터를 임포트합니다.

       인스턴스가 상주하는 호스트에서 이 하위 명령을 실행해야 합니다.
       DAS(도메인 관리 서버)에 접속하려면 이 하위 명령에 DAS가 실행되고 있는
       호스트의 이름이 필요합니다. 기본 포트가 아닌 포트가 관리에 사용되면 이
       하위 명령에 포트 번호도 필요합니다.  asadmin(1M) 유틸리티의 --host 옵션
       및 --port 옵션을 통해 이 정보를 제공해야 합니다.

       인스턴스의 구성 데이터를 임포트하면 인스턴스가 DAS와 통신할 필요 없이
       데이터가 인스턴스에 대한 호스트로 전송됩니다. 일반적으로 인스턴스의
       구성 데이터를 임포트하는 이유는 다음과 같습니다.

       ·   업그레이드 후 인스턴스를 다시 설정하기 위해

       ·   인스턴스가 DAS(도메인 관리 서버)에 접속할 수 없을 때 인스턴스를
           수동으로 DAS와 동기화하기 위해

       이 하위 명령은 다음 작업을 수행하여 인스턴스의 구성 데이터를
       임포트합니다.

       ·   인스턴스의 파일 및 디렉토리 생성 또는 업데이트

       ·   인스턴스를 DAS에 등록하려고 시도

       인스턴스를 DAS에 등록하려는 시도가 실패하는 경우 하위 명령이 실패하지
       않습니다. 대신, 하위 명령은 시도 실패를 알리는 경고를 표시합니다.
       경고에는 인스턴스를 DAS에 등록하기 위해 실행할 수 있는 명령이
       포함됩니다.

       import-sync-bundle 하위 명령은 인스턴스가 상주하는 노드를 확인하기 위해
       DAS에 접속하지 않습니다. 노드를 하위 명령의 옵션으로 지정하지 않은 경우
       하위 명령이 아카이브 파일의 DAS 구성에서 노드를 확인합니다.

       이 하위 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --instance
           구성 데이터를 임포트할 인스턴스입니다. 인스턴스가 DAS 구성에
           존재해야 합니다. 데이터를 임포트할 아카이브 파일에 지정된
           인스턴스에 대한 데이터가 있어야 합니다.

       --nodedir
           인스턴스의 노드 디렉토리가 포함된 디렉토리입니다. 인스턴스의 파일은
           인스턴스의 노드 디렉토리에 저장됩니다. 기본값은
           as-install/nodes입니다.

       --node
           인스턴스가 상주하는 노드입니다. 이 옵션을 생략하면 하위 명령이
           아카이브 파일의 DAS 구성에서 노드를 확인합니다.

피연산자
       file-name
           임포트할 아카이브 파일이 포함된 파일의 이름(경로 포함)입니다. 이
           피연산자는 필수입니다.

예제
       예 1, 클러스터된 인스턴스에 대한 구성 데이터 임포트
           이 예제는 아카이브 파일
           /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip에서
           노드 sj02의 클러스터된 인스턴스 ymli2에 대한 구성을 임포트합니다.

           명령은 노드 sj02가 나타내는 호스트인 호스트 sj02에서 실행됩니다.
           DAS는 호스트 sr04에서 실행되고 있으며 기본 HTTP 포트를 관리에
           사용합니다.

               sj02# asadmin --host sr04 import-sync-bundle --node sj02 --instance ymli2
               /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip
               Command import-sync-bundle executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       export-sync-bundle(1)

       asadmin(1M)

Java EE 7                       2010년 12월 7일          import-sync-bundle(1)
