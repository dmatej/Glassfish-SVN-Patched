start-cluster(1)        Unterbefehle des asadmin-Utili        start-cluster(1)

NAME
       start-cluster - startet ein Cluster

ZUSAMMENFASSUNG
           start-cluster [--help] [--autohadboverride={true|false}]
           [--verbose={false|true}] cluster-name

BESCHREIBUNG
       Der Unterbefehl start-cluster startet ein Cluster, indem alle GlassFish
       Server-Instanzen im Cluster gestartet werden, die noch nicht ausgeführt
       werden. Für diesen Unterbefehl muss das Distributed Component Object
       Model-(DCOM-)-Remote-Protokoll oder Secure Shell (SSH) auf dem Host
       konfiguriert sein, auf dem der Domainadministrationsserver (DAS)
       ausgeführt wird, und auf allen Hosts, auf denen sich Instanzen im
       Cluster befinden.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Wenn sich alle Instanzen auf │
           │           demselben Host wie der DAS   │
           │           befinden, ist DCOM oder SSH  │
           │           nicht erforderlich.          │
           │           Gegebenenfalls muss ein      │
           │           Cluster gestartet werden, in │
           │           dem sich Instanzen auf Hosts │
           │           befinden, auf denen DCOM     │
           │           oder SSH nicht konfiguriert  │
           │           ist und die entfernt vom DAS │
           │           liegen. Führen Sie in diesem │
           │           Fall den Unterbefehl         │
           │           start-local-instance(1) für  │
           │           jede Instanz auf dem Host    │
           │           aus, auf dem sich die        │
           │           Instanz befindet.            │
           └────────────────────────────────────────┘

       Sie können diesen Unterbefehl über jeden Host ausführen, der sich mit
       dem DAS in Verbindung setzen kann.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --autohadboverride
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

       --verbose
           Gibt an, ob zusätzliche Statusinformationen angezeigt werden, wenn
           das Cluster gestartet wird.

           Folgende Werte sind gültig:

           true
               Zeigt den Befehl zum Starten jeder Instanz im Cluster an. Zeigt
               außerdem an, ob der Versuch, jede Instanz zu starten,
               erfolgreich war.

           false
               Zeigt keine zusätzlichen Statusinformationen an (Standard).

OPERANDEN
       cluster-name
           Der Name des Clusters, das gestartet werden soll.

BEISPIELE
       Example 1, Cluster starten
           In diesem Beispiel wird das Cluster ymlcluster gestartet. Beim
           Starten des Clusters werden zusätzliche Statusinformationen
           angezeigt.

               asadmin> start-cluster --verbose ymlcluster
               start-instance yml-i-sr1-usca-02
               start-instance yml-i-sr1-usca-01

               The command start-instance executed successfully for:
               yml-i-sr1-usca-02 yml-i-sr1-usca-01

               Command start-cluster executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-cluster(1), delete-cluster(1), list-clusters(1), setup-ssh(1),
       start-local-instance(1), stop-cluster(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                        8. Dez. 2011                 start-cluster(1)
