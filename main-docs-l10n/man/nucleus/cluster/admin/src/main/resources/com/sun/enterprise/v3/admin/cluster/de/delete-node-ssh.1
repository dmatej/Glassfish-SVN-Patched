delete-node-ssh(1)      Unterbefehle des asadmin-Utili      delete-node-ssh(1)

NAME
       delete-node-ssh - löscht einen Knoten, der für die Kommunikation über
       SSH aktiviert ist

ZUSAMMENFASSUNG
           delete-node-ssh [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

BESCHREIBUNG
       Der Unterbefehl delete-node-ssh löscht einen Knoten, der für die
       Kommunikation über Secure Shell (SSH) aktiviert ist, aus der Domain.
       Für diesen Unterbefehl muss SSH nicht konfiguriert sein.

       Dieser Unterbefehl kann nur Knoten löschen, die für die Kommunikation
       über SSH aktiviert sind. Der Unterbefehl zum Löschen anderer
       Knotentypen hängt vom Typ des Knotens ab:

       ·   Ein Knoten, der für die Kommunikation über das Distributed
           Component Object Model-(DCOM-)Remote-Protokoll aktiviert ist, muss
           mit dem Unterbefehl delete-node-dcom(1) gelöscht werden.

       ·   Ein Knoten, der nicht für die Remote-Kommunikation aktiviert ist,
           muss mit dem Unterbefehl delete-node-config(1) gelöscht werden.

       Um zu bestimmen, ob ein Knoten für die Kommunikation über SSH aktiviert
       ist, verwenden Sie den Unterbefehl list-nodes(1).

       Auf dem Knoten, der gelöscht wird, dürfen sich keine GlassFish
       Server-Instanzen befinden. Andernfalls ist der Unterbefehl nicht
       erfolgreich. Bevor Sie diesen Unterbefehl ausführen, löschen Sie alle
       Instanzen, die sich auf dem Knoten befinden, indem Sie beispielsweise
       den Unterbefehl delete-instance(1) oder den Unterbefehl
       delete-local-instance(1) verwenden.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --uninstall
           Gibt an, ob die GlassFish Server-Software von dem Host
           deinstalliert wird, den der Knoten darstellt.

           Mögliche Werte:

           false
               Die GlassFish Server-Software wird nicht vom Host deinstalliert
               (Standard).

           true
               Die GlassFish Server-Software wird vom Host deinstalliert.
               Befindet sich ein anderer Knoten als der vordefinierte Knoten
               localhost-domain auf einem Host, von dem die GlassFish
               Server-Software deinstalliert wird, ist der Unterbefehl
               standardmäßig nicht erfolgreich. Um die GlassFish
               Server-Software von einem Host zu deinstallieren, auf dem sich
               benutzerdefinierte Knoten befinden, setzen Sie die Option
               --force auf true. Ist die Option --force auf true gesetzt,
               entfernt der Unterbefehl den gesamten Content aus dem
               übergeordneten Verzeichnis des
               Basisinstallationsverzeichnisses.

       --force
           Wenn --uninstall auf "true" gesetzt ist, wird angegeben, ob der
           Unterbefehl die GlassFish Server-Software von einem Host
           deinstalliert, auch wenn sich ein benutzerdefinierter Knoten auf
           dem Host befindet. Mögliche Werte:

           false
               Befindet sich ein benutzerdefinierter Knoten auf einem Host,
               wird die Software nicht deinstalliert, und der Unterbefehl ist
               nicht erfolgreich (Standard).

               Wenn die Option --force auf false gesetzt ist, entfernt der
               Unterbefehl nur die GlassFish Server-Softwaredateien. Anderer
               Content aus dem übergeordneten Verzeichnis des
               Basisinstallationsverzeichnisses, wie Konfigurationsdateien,
               wird nicht entfernt.

           true
               Der Unterbefehl deinstalliert die GlassFish Server-Software
               auch dann von dem Host, wenn sich ein benutzerdefinierter
               Knoten auf dem Host befindet.

               Ist die Option --force auf true gesetzt, entfernt der
               Unterbefehl den gesamten Content aus dem übergeordneten
               Verzeichnis des Basisinstallationsverzeichnisses.

OPERANDEN
       node-name
           Der Name des Knotens, der gelöscht werden soll. Der Knoten muss für
           die Kommunikation über SSH aktiviert sein. Andernfalls tritt ein
           Fehler auf.

BEISPIELE
       Example 1, Knoten löschen, der für die Kommunikation über SSH aktiviert
       ist
           In diesem Beispiel wird der Knoten eg1 gelöscht, der für die
           Kommunikation über SSH aktiviert ist.

               asadmin> delete-node-ssh eg1
               Command delete-node-ssh executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-node-dcom(1), create-node-ssh(1), delete-node-config(1),
       delete-node-dcom(1), delete-instance(1), delete-local-instance(1),
       install-node(1), install-node-dcom(1), install-node-ssh(1),
       list-nodes(1), uninstall-node(1), uninstall-node-dcom(1),
       uninstall-node-ssh(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                        8. Dez. 2011               delete-node-ssh(1)
