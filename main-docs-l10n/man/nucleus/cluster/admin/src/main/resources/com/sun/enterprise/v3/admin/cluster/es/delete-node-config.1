delete-node-config(1)   Subcomandos de la Utilidad asa   delete-node-config(1)

NOMBRE DE REFERENCIA
       delete-node-config - Suprime un nodo que no está activado para la
       comunicación remota.

SINOPSIS
           delete-node-config [--help] node-name

DESCRIPCIÓN
       El subcomando delete-node-config suprime un nodo que no esté activado
       para la comunicación remota desde el dominio. Este subcomando no
       requiere la configuración del protocolo remoto Distributed Component
       Object Model (DCOM) ni del shell seguro (SSH).

       Este subcomando sólo puede suprimir un nodo que no esté activado para
       la comunicación remota. El subcomando para suprimir otros tipos de
       nodos depende del tipo de nodo:

       ·   Un nodo que esté activado para la comunicación remota a través de
           DCOM se debe suprimir mediante el subcomando delete-node-dcom(1).

       ·   Un nodo que esté activado para la comunicación remota a través de
           SSH se debe suprimir mediante el subcomando delete-node-ssh(1).

       Para determinar si un nodo está activado para la comunicación remota,
       utilice el subcomando list-nodes(1).

       En el nodo que se va a suprimir no debe residir ninguna instancia de
       GlassFish Server. De lo contrario, el subcomando falla. Antes de
       ejecutar este subcomando, suprima las instancias que residan en el nodo
       mediante, por ejemplo, el subcomando delete-instance(1) o
       delete-local-instance(1).

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           El nodo predefinido          │
           │           localhost-domain no se puede │
           │           suprimir.                    │
           └────────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       node-name
           Nombre del nodo que se va a suprimir. El nodo no debe estar
           activado para la comunicación a través de DCOM ni SSH. De lo
           contrario, se produce un error.

EJEMPLOS
       Ejemplo 1, Supresión de un Nodo no Activado para Comunicación Remota
           En este ejemplo se suprime el nodo sj03, que no está activado para
           la comunicación remota.

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                   8 de diciembre de 2011       delete-node-config(1)
