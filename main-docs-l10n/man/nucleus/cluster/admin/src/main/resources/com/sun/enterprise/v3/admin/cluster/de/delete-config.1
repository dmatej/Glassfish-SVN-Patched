delete-config(1)        Unterbefehle des asadmin-Utili        delete-config(1)

NAME
       delete-config - löscht eine vorhandene benannte Konfiguration

ZUSAMMENFASSUNG
           delete-config [--help] configuration-name

BESCHREIBUNG
       Der Unterbefehl delete-config löscht eine vorhandene benannte
       Konfiguration aus der Konfiguration des Domainadministrationsservers
       (DAS). Eine Konfiguration kann nur dann gelöscht werden, wenn keine
       GlassFish Server-Instanzen oder -Cluster auf die Konfiguration
       verweisen. Eine Standalone-Konfiguration wird automatisch gelöscht,
       wenn die auf die Konfiguration verweisenden Instanzen oder Cluster
       gelöscht werden. Die Konfiguration default-config, die zum Erstellen
       von Standalone-Konfigurationen kopiert wird, kann nicht gelöscht
       werden.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       configuration-name
           Der Name der Konfiguration, die gelöscht werden soll.

BEISPIELE
       Example 1, Benannte Konfigurationen löschen
           In diesem Beispiel wird die benannte Konfiguration pmdconfig
           gelöscht.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                        7. Dez. 2010                 delete-config(1)
