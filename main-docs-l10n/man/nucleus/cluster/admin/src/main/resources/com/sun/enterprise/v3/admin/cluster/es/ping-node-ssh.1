ping-node-ssh(1)        Subcomandos de la Utilidad asa        ping-node-ssh(1)

NOMBRE DE REFERENCIA
       ping-node-ssh - comprueba si se puede utilizar un nodo que está
       activado para la comunicación por SSH.

SINOPSIS
           ping-node-ssh [--help]
           [--validate={false|true}] node-name

DESCRIPCIÓN
       El subcomando ping-node-ssh comprueba si se puede utilizar un nodo que
       está activado para la comunicación sobre Secure Shell (SSH) se puede
       utilizar. Este subcomando requiere que se configure Secure Shell (SSH)
       en el equipo en el que se ejecuta el servidor de administración de
       dominio (DAS) y en los equipos en los que reside el nodo. Puede
       ejecutar este subcomando desde cualquier máquina que pueda establecer
       contacto con el DAS.

       Si el nodo se puede utilizar, el subcomando muestra una confirmación de
       que el subcomando pudo conectar con el nodo a través de SSH. Esta
       confirmación incluye el nombre del host que representa el nodo.

       Si lo desea, el subcomando también puede validar el nodo para
       determinar si la utilidad asadmin(1M) se puede ejecutar en el host que
       representa el nodo. Para validar un nodo, el subcomando ejecuta el
       subcomando version(1). Si el nodo es válido, el subcomando muestra la
       versión que devuelve el subcomando version.

       El nodo que se especifica como el operando de este subcomando debe
       estar activado para la comunicación a través de SSH. Si el nodo no está
       activado para la comunicación a través de SSH, se produce un error.
       Para determinar si un nodo está activado para la comunicación a través
       de SSH, utilice el subcomando list-nodes(1).

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Para comprobar si un nodo    │
           │           que está activado para la    │
           │           comunicación a través del    │
           │           protocolo remoto Distributed │
           │           Component Object Model       │
           │           (DCOM) se puede utilizar,    │
           │           use el subcomando            │
           │           ping-node-dcom(1).           │
           └────────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --validate
           Especifica si el subcomando valida el nodo.

           Los valores posibles son los siguientes:

           true
               El nodo se valida.

           false
               El nodo no se valida (por defecto).

OPERANDOS
       node-name
           El nombre del nodo que se debe comprobar. El nodo debe estar
           activado para la comunicación a través de SSH. De lo contrario, se
           produce un error.

EJEMPLOS
       Ejemplo 1, Comprobación de si un Nodo Activado para SSH se Puede
       Utilizar
           En este ejemplo se comprueba si el nodo activado para SSHsj03-node
           se puede utilizar.

               asadmin> ping-node-ssh sj03-node
               Successfully made SSH connection to node sj03-node (sj03.example.com)
               Command ping-node-ssh executed successfully.

       Ejemplo 2, Validación de un Nodo Activado para SSH
           En este ejemplo se valida el nodo activado para SSHadc-node.

               asadmin> ping-node-ssh --validate=true adc-node
               Successfully made SSH connection to node adcnode (adc.example.com)
               GlassFish version found at /export/glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1 (build 40)
               Command version executed successfully.
               Command ping-node-ssh executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-node-ssh(1), delete-node-ssh(1), list-nodes(1),
       ping-node-dcom(1), setup-ssh(1), update-node-ssh(1), version(1)

       asadmin(1M)

Java EE 7                   8 de diciembre de 2011            ping-node-ssh(1)
