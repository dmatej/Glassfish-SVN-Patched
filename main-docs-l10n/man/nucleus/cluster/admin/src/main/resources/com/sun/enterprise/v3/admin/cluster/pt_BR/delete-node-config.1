delete-node-config(1)   Subcomandos do utilitário asad   delete-node-config(1)

NOME
       delete-node-config - deleta um nó que não está ativado para comunicação
       remota

SINOPSE
           delete-node-config [--help] node-name

DESCRIçãO
       O subcomando delete-node-config deleta um nó que não está ativado para
       comunicação remota a partir do domínio. Este subcomando não requer o
       protocolo DCOM (Distributed Component Object Model) ou shell seguro
       (SSH) para ser configurado.

       O subcomando só pode deletar um nó que não está ativado para
       comunicação remota. O subcomando para deletar outros tipos de nós
       depende do tipo do nó:

       ·   Um nó que está ativado para comunicação por meio do DCOM deve ser
           deletado utilizando o subcomando delete-node-dcom(1).

       ·   Um nó que está ativado para comunicação por meio do SSH deve ser
           deletado utilizando o subcomando delete-node-ssh(1).

       Para determinar se um nó está ativado para comunicação remota, utilize
       o subcomando list-nodes(1).

       Nenhuma GlassFish Server instância devem residir no nó que está sendo
       deletado. Caso contrário, o subcomando irá falhar. Antes de executar
       este subcomando, delete quaisquer instâncias que residem no nó
       utilizando, por exemplo, o subcomandodelete-instance(1) ou o subcomando
       delete-local-instance(1).

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           O nó predefinido localhost │
           │           não pode ser deletado.     │
           └──────────────────────────────────────┘

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       node-name
           O nome do nó a ser deletado. O nó não deve ser ativado para
           comunicação por meio do DCOM ou do SSH. Caso contrário, ocorrerá um
           erro.

EXEMPLOS
       Example 1, Deletando um Nó que não Está Ativado para Comunicação Remota
           Este exemplo deleta o nó sj03, que não está ativado para
           comunicação remota.

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                         8 dez 2011             delete-node-config(1)
