list-persistence-types(1Subcomandos de la Utilidad aslist-persistence-types(1)

NOMBRE DE REFERENCIA
       list-persistence-types - muestra los tipos de persistencia registrados
       para las sesiones HTTP y las instancias SFSB.

SINOPSIS
           list-persistence-types [--help] --type={web|ejb}

DESCRIPCIÓN
       El subcomando list-persistence-types muestra los tipos de persistencia
       registrados para las sesiones HTTP y las instancias de un bean de
       sesión con estado (SFSB). Los tipos de persistencia incorporados son
       memoria , archivo y replicados. El tipo memoria no se aplica a
       instancias SFSB.

       Otros tipos de persistencia se pueden agregar mediante la clase
       StrategyBuilder . Para obtener más información, consulte Oracle
       GlassFish Server Add-On Component Development Guide.

       Para definir el tipo de persistencia para las sesiones HTTP, utilice el
       subcomando set para definir el atributopersistence-type . Por ejemplo:

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       Para definir el tipo de persistencia de instancias SFSB sin la opción
       de disponibilidad activada, utilice el subcomando set para definir el
       atributo sfsb-persistence-type. Por ejemplo:

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       Para definir el tipo de persistencia de instancias SFSB con la opción
       de disponibilidad activada, utilice el subcomando set para definir el
       atributo sfsb-ha-persistence-type. Por ejemplo:

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --type
           Especifica el tipo de sesiones para las que se muestran los tipos
           de persistencia. Los valores permitidos son los siguientes:

           ·   web: — muestra los tipos de persistencia para las sesiones
               HTTP.

           ·   ejb: — muestra una lista de los tipos de persistencia para
               instancias SFSB.

EJEMPLOS
       Ejemplo 1, Lista de los Tipos de Persistencia para Sesiones HTTP
           En este ejemplo se muestra una lista de los tipos de persistencia
           para las sesiones HTTP.

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                    15 de febrero de 2010   list-persistence-types(1)
