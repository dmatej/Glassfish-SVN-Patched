delete-config(1)        Comandi secondari della utilit        delete-config(1)

NOME
       delete-config - elimina una configurazione denominata esistente

SINTASSI
           delete-config [--help] configuration-name

DESCRIZIONE
       Il comando secondario delete-config elimina una configurazione
       denominata esistente dalla configurazione del server di amministrazione
       del dominio (DAS). L'eliminazione di una configurazione è possibile
       solo se non esistono istanze o cluster di GlassFish Server che vi fanno
       riferimento. Una configurazione standalone viene eliminata
       automaticamente quando l'istanza o il cluster a cui si riferiscono
       vengono eliminati. È impossibile eliminare la configurazione
       default-config copiata per creare configurazioni standalone.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       configuration-name
           Il nome della configurazione che viene eliminata.

ESEMPI
       Esempio 1, Eliminazione di una configurazione denominata
           In questo esempio viene eliminata la configurazione denominata
           pmdconfig.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                       7 dicembre 2010               delete-config(1)
