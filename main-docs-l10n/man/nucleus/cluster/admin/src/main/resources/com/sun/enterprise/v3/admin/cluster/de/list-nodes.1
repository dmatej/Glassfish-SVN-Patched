list-nodes(1)           Unterbefehle des asadmin-Utili           list-nodes(1)

NAME
       list-nodes - listet alle GlassFish Server-Knoten in einer Domain auf

ZUSAMMENFASSUNG
           list-nodes [--help] [--long={false|true}]

BESCHREIBUNG
       Der Unterbefehl list-nodes listet alle GlassFish Server-Knoten in einer
       Domain auf.

       Standardmäßig zeigt der Unterbefehl für jeden aufgelisteten Knoten die
       folgenden Informationen an:

       ·   den Namen des Knotens

       ·   den Knotentyp, wobei folgende Typen verfügbar sind:

           CONFIG
               Der Knoten unterstützt keine Remote-Kommunikation.

           DCOM
               Der Knoten unterstützt die Kommunikation über das Distributed
               Component Object Model-(DCOM-)Remote-Protokoll.

           SSH
               Der Knoten unterstützt die Kommunikation über Secure Shell
               (SSH).

       ·   den Namen des Hosts, den der Knoten darstellt

       Die Option --long des Unterbefehls gibt an, ob die Knoten im Langformat
       aufgelistet werden. Im Langformat werden zu jedem Knoten die folgenden
       zusätzlichen Informationen angezeigt:

       ·   der Pfad zum übergeordneten Verzeichnis des
           Basisinstallationsverzeichnisses von GlassFish Server auf dem Host,
           den der Knoten darstellt

       ·   eine durch Komma getrennte Liste der Namen der GlassFish
           Server-Instanzen, die sich auf dem Knoten befinden

       Wenn die Option --terse des Utilitys asadmin(1M) auf true gesetzt und
       die Option --long des Unterbefehls auf false gesetzt ist, listet der
       Unterbefehl nur den Namen jedes Knotens auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --long, -l
           Gibt an, ob die Knoten im Langformat aufgelistet werden.

           Mögliche Werte:

           true
               Die Knoten werden im Langformat aufgelistet.

           false
               Die Knoten werden im Kurzformat aufgelistet (Standard).

BEISPIELE
       Example 1, GlassFish Server-Knoten auflisten
           In diesem Beispiel werden Name, Typ und Host für alle GlassFish
           Server-Knoten in der Domain domain1 angezeigt.

               asadmin> list-nodes
               localhost-domain1  CONFIG  localhost
               sj02  SSH  sj02.example.com
               sj01  SSH  sj01.example.com
               devnode  CONFIG  localhost
               Command list-nodes executed successfully.

       Example 2, Nur die Namen der GlassFish Server-Knoten auflisten
           In diesem Beispiel wird die Option --terse des Utilitys asadmin
           verwendet, um nur die Namen der GlassFish Server-Knoten in der
           Domain domain1 aufzulisten.

               asadmin> list-nodes --terse=true
               localhost-domain1
               sj02
               sj01
               devnode

       Example 3, GlassFish Server-Knoten im Langformat auflisten
           In diesem Beispiel werden die GlassFish Server-Knoten in der Domain
           domain1 im Langformat aufgelistet.

               asadmin> list-nodes --long=true
               NODE NAME           TYPE     NODE HOST          INSTALL DIRECTORY     REFERENCED BY
               localhost-domain1   CONFIG   localhost          /export/glassfish3
               sj02                SSH      sj02.example.com   /export/glassfish3    pmd-i2, yml-i2
               sj01                SSH      sj01.example.com   /export/glassfish3    pmd-i1, yml-i1
               devnode             CONFIG   localhost          /export/glassfish3    pmdsa1
               Command list-nodes executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-node-config(1), create-node-dcom(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes-config(1), list-nodes-dcom(1), list-nodes-ssh(1)

       asadmin(1M)

Java EE 7                        8. Dez. 2011                    list-nodes(1)
