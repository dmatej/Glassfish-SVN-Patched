delete-node-dcom(1)         asadmin 实用程序子命令         delete-node-dcom(1)

名称
       delete-node-dcom - 删除允许通过 DCOM 进行通信的节点

提要
           delete-node-dcom [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

说明
       delete-node-dcom 子命令从域中删除允许通过分布式组件对象模型 (DCOM)
       远程协议进行通信的节点。DCOM 协议仅在 Windows
       系统上可用。此子命令不要求配置 DCOM。

       此子命令只能删除允许通过 DCOM
       进行通信的节点。用于删除其他类型节点的子命令取决于节点的类型：

       ·   必须使用 delete-node-ssh(1) 子命令才能删除允许通过安全 shell (SSH)
           进行通信的节点。

       ·   必须使用 delete-node-config(1) 子命令才能删除不允许进行通信的节点。

       要确定是否允许某个节点通过 DCOM 进行通信，请使用 list-nodes(1) 子命令。

       要删除的节点上不能有任何 GlassFish Server
       实例。否则，该子命令将失败。在运行此子命令之前，请使用
       delete-instance(1) 或 delete-local-instance(1)
       这类子命令删除节点上的任何实例。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --uninstall
           指定是否从节点表示的主机中卸载 GlassFish Server 软件。

           可能的值如下：

           假
               不从该主机中卸载 GlassFish Server 软件 (默认值)。

           真
               从该主机中卸载 GlassFish Server
               软件。默认情况下，如果有预定义节点 localhost-domain
               以外的任何节点位于正在从中卸载 GlassFish Server
               软件的主机上，此子命令将失败。要从用户定义节点所在的主机中卸载
               GlassFish Server 软件，请将 --force 选项设置为真
               选项为真

       --force
           指定 --uninstall
           为“真”时，即使用户定义节点位于主机上，此子命令是否仍从该主机中卸载
           GlassFish Server 软件。可能的值如下：

           假
               如果主机上驻留了用户定义的节点，则不卸载软件且该子命令失败
               (默认值)。

               如果 --force 选项为假
               软件文件。而不会删除基础安装目录的父目录中的其他内容，例如配置文件。

           真
               即使主机上驻留了用户定义的节点，该子命令仍从该主机中卸载
               GlassFish Server 软件。

               如果 --force
               选项为真

操作数
       node-name
           要删除的节点的名称。必须允许此节点通过 DCOM
           进行通信。否则，会出现错误。

示例
       Example 1, 删除允许通过 DCOM 进行通信的节点
           此示例删除允许通过 DCOM 进行通信的节点 xkyd。

               asadmin> delete-node-dcom xkyd
               Command delete-node-dcom executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-node-dcom(1), delete-node-config(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node-dcom(1),
       list-nodes(1), uninstall-node-dcom(1), update-node-dcom(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 5 日           delete-node-dcom(1)
