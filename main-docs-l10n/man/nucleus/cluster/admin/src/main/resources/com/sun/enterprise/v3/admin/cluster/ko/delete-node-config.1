delete-node-config(1)     asadmin 유틸리티 하위 명령     delete-node-config(1)

제목
       delete-node-config - 원격 통신에 대해 사용으로 설정되지 않은 노드 삭제

시놉시스
           delete-node-config [--help] node-name

설명
       delete-node-config 하위 명령은 도메인에서 원격 통신에 대해 사용으로
       설정되지 않은 노드를 삭제합니다. 이 하위 명령을 사용하기 위해
       DCOM(Distributed Component Object Model) 원격 프로토콜 또는 SSH(보안
       셸)를 구성할 필요는 없습니다.

       이 하위 명령은 원격 통신에 대해 사용으로 설정되지 않은 노드만 삭제할 수
       있습니다. 다른 유형의 노드를 삭제하는 하위 명령은 노드 유형에 따라
       다릅니다.

       ·   DCOM을 통한 원격 통신에 대해 사용으로 설정된 노드는
           delete-node-dcom(1) 하위 명령을 사용하여 삭제해야 합니다.

       ·   SSH를 통한 원격 통신에 대해 사용으로 설정된 노드는
           delete-node-ssh(1) 하위 명령을 사용하여 삭제해야 합니다.

       노드가 원격 통신에 대해 사용으로 설정되어 있는지를 확인하려면
       list-nodes(1) 하위 명령을 사용합니다.

       삭제할 노드에는 GlassFish Server 인스턴스가 없어야 합니다. 그렇지
       않으면 하위 명령을 실패합니다. 이 하위 명령을 실행하기 전에
       delete-instance(1) 하위 명령 또는 delete-local-instance(1) 하위 명령
       등을 사용하여 노드에 있는 모든 인스턴스를 삭제합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           미리 정의된 노드             │
           │           localhost-domain은 삭제할 수 │
           │           없습니다.                    │
           └────────────────────────────────────────┘

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       node-name
           삭제할 노드의 이름입니다. 이 노드는 DCOM 또는 SSH를 통한 통신에
           대해 사용으로 설정되어 있지 않아야 합니다. 그렇지 않으면 오류가
           발생합니다.

예제
       예 1, 원격 통신에 대해 사용으로 설정되지 않은 노드 삭제
           이 예제는 원격 통신에 대해 사용으로 설정되지 않은 노드 sj03을
           삭제합니다.

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                       2011년 12월 8일          delete-node-config(1)
