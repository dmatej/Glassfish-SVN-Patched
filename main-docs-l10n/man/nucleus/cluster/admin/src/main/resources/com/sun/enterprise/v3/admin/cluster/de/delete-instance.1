delete-instance(1)      Unterbefehle des asadmin-Utili      delete-instance(1)

NAME
       delete-instance - löscht eine GlassFish Server-Instanz

ZUSAMMENFASSUNG
           delete-instance [--help] instance-name

BESCHREIBUNG
       Der Unterbefehl delete-instance löscht eine GlassFish Server-Instanz.
       Für diesen Unterbefehl muss das Distributed Component Object
       Model-(DCOM-)Remote-Protokoll oder Secure Shell (SSH) auf dem Host
       konfiguriert sein, auf dem der Domainadministrationsserver (DAS)
       ausgeführt wird, und auf dem Host, der von dem Knoten repräsentiert
       wird, auf dem sich die Instanz befindet.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           DCOM oder SSH ist nicht      │
           │           erforderlich, wenn sich die  │
           │           Instanz auf einem Knoten des │
           │           Typs CONFIG befindet, der    │
           │           den lokalen Host darstellt.  │
           │           Ein Knoten des Typs CONFIG   │
           │           ist nicht für die            │
           │           Remote-Kommunikation über    │
           │           DCOM oder SSH aktiviert.     │
           └────────────────────────────────────────┘

       Sie können diesen Unterbefehl über jeden Host ausführen, der sich mit
       dem DAS in Verbindung setzen kann.

       Der Unterbefehl kann jede GlassFish Server-Instanz löschen, unabhängig
       davon, wie sie erstellt wurde. Dieser Unterbefehl kann beispielsweise
       eine Instanz löschen, die mithilfe des Unterbefehls
       create-local-instance(1) erstellt wurde.

       Die Instanz, die gelöscht wird, darf nicht ausgeführt werden.
       Andernfalls tritt ein Fehler auf.

       Der Unterbefehl löscht eine Instanz, indem folgende Aktionen ausgeführt
       werden:

       ·   Entfernen der Instanz aus der Konfiguration des DAS (Domain
           Administration Server)

       ·   Löschen der Dateien der Instanz aus dem System

       Wenn die Instanz, die gelöscht wird, die einzige Instanz ist, die das
       Knotenverzeichnis verwendet, wird das Verzeichnis ebenfalls entfernt.

       Wenn eine Standalone-Instanz gelöscht wird, wird die
       Standalone-Konfiguration der Instanz ebenfalls gelöscht. Eine
       Standalone-Instanz bezieht sich auf eine Konfiguration namens
       instance-name-config, auf die sich keine anderen Cluster oder nicht
       geclusterten Instanzen beziehen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       instance-name
           Der Name der Instanz, die gelöscht werden soll.

BEISPIELE
       Beispiel 1, GlassFish Server-Instanzen löschen
           In diesem Beispiel wird die GlassFish Server-Instanz pmdsainst
           gelöscht.

               asadmin> delete-instance pmdsainst

               Command delete-instance executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-instance(1), create-local-instance(1), delete-local-instance(1),
       setup-ssh(1), start-instance(1), start-local-instance(1),
       stop-instance(1), stop-local-instance(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                        08. Dez. 2011              delete-instance(1)
