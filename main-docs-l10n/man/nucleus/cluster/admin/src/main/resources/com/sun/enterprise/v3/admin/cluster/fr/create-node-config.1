create-node-config(1)   Sous-commandes de l'utilitaire   create-node-config(1)

NOM
       create-node-config - crée un noeud qui n'est pas activé pour les
       communications à distance

SYNTAXE
           create-node-config [--help]
           [--nodehost node-host]
           [--installdir as-install-parent] [--nodedir node-dir] node-name

DESCRIPTION
       La sous-commande create-node-config crée un noeud non activé pour les
       communications à distance. Elle n'exige pas que le protocole distant
       DCOM (Distributed Component Object Model) ou que le protocole SSH
       (Secure Shell) soit configuré pour créer le noeud.

       Un noeud représente un hôte sur lequel le logiciel GlassFish Server est
       installé. Un noeud doit exister au niveau de chaque hôte sur lequel
       résident des instances GlassFish Server.

           Note
           ┌────────────────────────────────────────┐
           │Remarque                                │
           │                                        │
           │           GlassFish Server fournit le  │
           │           noeud prédéfini              │
           │           localhost-domain, qui        │
           │           représente l'hôte sur lequel │
           │           le serveur DAS est exécuté.  │
           │           Le noeud prédéfini           │
           │           localhost-domain n'est pas   │
           │           activé pour les              │
           │           communications à distance.   │
           └────────────────────────────────────────┘

       Toutes les tâches d'administration des instances réalisées sur un noeud
       non activé pour les communications à distance doivent être effectuées
       sur l'hôte que le noeud représente. Le serveur d'administration de
       domaine (DAS) d'un hôte distant ne peut pas communiquer avec le noeud.
       Pour administrer les instances sur un noeud qui représente un hôte
       distant par rapport au serveur DAS, vous devez utiliser les
       sous-commandes suivantes :

       ·   create-local-instance(1)

       ·   delete-local-instance(1)

       ·   start-local-instance(1)

       Cependant, vous pouvez utiliser stop-local-instance(1) ou
       stop-instance(1) pour arrêter les instances.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --nodehost
           Nom de l'hôte que le noeud représente. Si cette option est omise,
           aucun hôte n'est spécifié pour le noeud.

       --installdir
           Chemin d'accès complet au parent du répertoire d'installation de
           base du logiciel GlassFish Server sur l'hôte (par exemple,
           /export/glassfish3/). Si cette option est omise, aucun parent du
           répertoire d'installation de base du logiciel GlassFish Server
           n'est indiqué pour le noeud.

       --nodedir
           Chemin d'accès complet au répertoire devant contenir les instances
           de GlassFish Server créées sur le noeud. Tout chemin relatif
           indiqué doit se baser sur le répertoire as-install. Si cette option
           est omise, aucun répertoire d'instances n'est spécifié pour le
           noeud.

OPÉRANDES
       node-name
           Nom du noeud.

           Le nom doit respecter les exigences suivantes :

           ·   Il ne peut contenir que des caractères ASCII.

           ·   Il doit commencer par une lettre, un chiffre ou un trait de
               soulignement.

           ·   Il ne peut contenir que les caractères suivants :

               ·   Lettres minuscules

               ·   Lettres majuscules

               ·   Chiffres

               ·   Trait d'union

               ·   Point

               ·   Trait de soulignement

           ·   Le nom du noeud doit être unique dans le domaine ; il ne doit
               pas appartenir à un autre noeud, à une configuration nommée, à
               un cluster ou à une instance de GlassFish Server.

           ·   Le nom ne doit pas être domain, server ou tout autre mot-clé
               réservé par GlassFish Server.

EXEMPLES
       Exemple 1, Création d'un noeud non activé pour les communications à
       distance
           Cet exemple montre comment créer le noeud sj03 pour l'hôte
           sj03.example.com. Le noeud n'est pas activé pour les communications
           à distance.

               asadmin> create-node-config --nodehost sj03.example.com sj03

               Command create-node-config executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-local-instance(1), create-node-dcom(1), create-node-ssh(1),
       delete-local-instance(1), delete-node-config(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       start-local-instance(1), stop-instance(1), stop-local-instance(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                         8 déc 2011             create-node-config(1)
