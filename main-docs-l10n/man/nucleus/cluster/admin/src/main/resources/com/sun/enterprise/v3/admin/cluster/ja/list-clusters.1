list-clusters(1)      asadminユーティリティのサブコマンド     list-clusters(1)

名前
       list-clusters - ドメイン内の既存のクラスタをリストします

概要
           list-clusters [--help] [target]

説明
       list-clustersサブコマンドは、ドメイン内の既存のクラスタをリストします。リストは、クラスタ、インスタンス、ノード、または構成でフィルタできます。表示された各クラスタに対して、サブコマンドによってクラスタが実行中かどうかをリストできます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           クラスタをリストするターゲットを指定することで、クラスタのリストをフィルタします。有効な値は次のとおりです:

           domain
               ドメイン内のすべてのクラスタをリストします(デフォルト)。

           cluster-name
               指定したクラスタのみをリストします。

           instance-name
               指定したインスタンスがメンバーであるクラスタをリストします。

           node-name
               指定したノードに存在するインスタンスを含むクラスタをリストします。たとえば、クラスタ
               pmdc内のインスタンスpmdi1と、クラスタymlc内のインスタンスymli1がノードn1に存在する場合は、pmdcと
               ymlcがリストされます。

           configuration-name
               名前付き構成によって構成が定義されたインスタンスを含むすべてのクラスタをリストします。

例
       例 1, ドメイン内のすべてのクラスタのリスト
           この例では、現在のドメイン内のすべてのクラスタをリストします。

               asadmin> list-clusters
               pmdclust not running
               ymlclust not running

               Command list-clusters executed successfully.

       例 2, クラスタのステータスの表示
           この例では、実行されていないクラスタymlclustのステータスを表示します。

               asadmin> list-clusters ymlclust
               ymlclust not running

               Command list-clusters executed successfully.

       例 3, ノードに関連付けられたすべてのクラスタのリスト
           この例では、ノードsj02上にあるインスタンスを含むクラスタをリストします。

               asadmin> list-clusters sj02
               ymlclust not running

               Command list-clusters executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-cluster(1), delete-cluster(1), start-cluster(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                        2010年9月9日                 list-clusters(1)
