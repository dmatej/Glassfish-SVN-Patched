delete-node-config(1)       asadmin 公用程式子命令       delete-node-config(1)

參考名
       delete-node-config - 刪除未啟用遠端通訊的節點

概要
           delete-node-config [--help] node-name

描述
       delete-node-config
       子命令會從網域中刪除未啟用遠端通訊的節點。這個子命令不需要組態分散式元件物件模型
       (DCOM) 遠端協定或安全 Shell (SSH)。

       此子命令只能刪除未啟用遠端通訊的節點。刪除其他類型節點的子命令取決於節點的類型：

       ·   透過 DCOM 啟用遠端通訊的節點必須使用 delete-node-dcom(1)
           子命令刪除。

       ·   透過 SSH 啟用遠端通訊的節點必須使用 delete-node-ssh(1) 子命令刪除。

       若要判斷節點是否已啟用遠端通訊，請使用 list-nodes(1) 子命令。

       要刪除的節點中不得有任何 GlassFish Server
       執行處理。否則子命令會失敗。在執行此子命令之前，請刪除位於該節點的所有執行處理，例如使用
       delete-instance(1) 子命令或 delete-local-instance(1) 子命令。

           注意
           ┌──────────────────────────────────┐
           │備註                              │
           │                                  │
           │           無法刪除預先定義的節點 │
           │           localhost-domain。     │
           └──────────────────────────────────┘

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       node-name
           要刪除的節點名稱。此節點不得啟用透過 DCOM 或 SSH
           通訊。否則，將發生錯誤。

範例
       範例 1, 刪除未啟用遠端通訊的節點
           此範例會刪除節點 sj03，此節點未啟用遠端通訊。

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日         delete-node-config(1)
