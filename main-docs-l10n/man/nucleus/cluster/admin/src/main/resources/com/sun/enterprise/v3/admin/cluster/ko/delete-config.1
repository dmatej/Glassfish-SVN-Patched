delete-config(1)          asadmin 유틸리티 하위 명령          delete-config(1)

제목
       delete-config - 기존의 명명된 구성 삭제

시놉시스
           delete-config [--help] configuration-name

설명
       delete-config 하위 명령은 DAS(도메인 관리 서버)의 구성에서 기존의
       명명된 구성을 삭제합니다. 구성을 참조하는 GlassFish Server 인스턴스
       또는 클러스터가 없는 경우에만 구성을 삭제할 수 있습니다. 독립형 구성은
       해당 구성을 참조하는 인스턴스 또는 클러스터가 삭제될 때 자동으로
       삭제됩니다. 독립형 구성을 생성하기 위해 복사된 default-config 구성은
       삭제할 수 없습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       configuration-name
           삭제할 구성의 이름입니다.

예제
       예 1, 명명된 구성 삭제
           이 예제는 이름이 지정된 구성 pmdconfig를 삭제합니다.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                       2010년 12월 7일               delete-config(1)
