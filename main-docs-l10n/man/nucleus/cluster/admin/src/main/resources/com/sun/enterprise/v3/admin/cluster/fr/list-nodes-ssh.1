list-nodes-ssh(1)       Sous-commandes de l'utilitaire       list-nodes-ssh(1)

NOM
       list-nodes-ssh - répertorie tous les noeuds GlassFish Server qui
       prennent en charge la communication via SSH dans un domaine

SYNTAXE
           list-nodes-ssh [--help] [--long={false|true}]

DESCRIPTION
       La sous-commande list-nodes-ssh répertorie tous les noeuds GlassFish
       Server qui prennent en charge la communication via le protocole SSH
       (Secure Shell) dans un domaine.

           Note
           ┌────────────────────────────────────────┐
           │Remarque                                │
           │                                        │
           │           Pour obtenir la liste de     │
           │           tous les noeuds dans un      │
           │           domaine, quel que soit le    │
           │           type de noeud, exécutez la   │
           │           sous-commande list-nodes(1). │
           └────────────────────────────────────────┘

       Par défaut, la sous-commande affiche les informations suivantes pour
       chaque noeud répertorié :

       ·   Le nom du noeud

       ·   Le type de noeud, à savoir SSH

       ·   Le nom de l'hôte représenté par le noeud

       L'option --long de la sous-commande indique si les noeuds sont
       répertoriés au format long. Au format long, les informations
       supplémentaires suivantes relatives à chaque noeud sont affichées :

       ·   Le chemin du parent du répertoire d'installation de base de
           GlassFish Server sur l'hôte représenté par le noeud

       ·   La liste des noms des instances GlassFish Server résidant sur le
           noeud, séparés par des virgules

       Si l'option --terse de l'utilitaire asadmin(1M) a la valeur true et que
       l'option --long de la sous-commande a la valeur false, la sous-commande
       répertorie uniquement le nom de chaque noeud.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Indique si les noeuds sont répertoriés au format long.

           Les valeurs possibles sont les suivantes :

           true
               Les noeuds sont répertoriés au format long.

           false
               Les noeuds sont répertoriés au format court (par défaut).

EXEMPLES
       Exemple 1, Liste des noeuds GlassFish Server
           Dans cet exemple, le nom, le type et l'hôte de tous les noeuds
           GlassFish Server qui prennent en charge la communication via SSH
           dans un domaine sont affichés.

               asadmin> list-nodes-ssh
               sj02  SSH  sj02.example.com
               sj01  SSH  sj01.example.com
               Command list-nodes-ssh executed successfully.

       Exemple 2, Liste des noms des noeuds GlassFish Server uniquement
           Dans cet exemple, l'option --terse de l'utilitaire asadmin est
           utilisée afin de répertorier uniquement les noms des noeuds
           GlassFish Server qui prennent en charge la communication via SSH
           dans un domaine.

               asadmin> list-nodes-ssh --terse=true
               sj02
               sj01

       Exemple 3, Liste des noeuds GlassFish Server au format long
           Dans cet exemple, les noeuds GlassFish Server qui prennent en
           charge la communication via SSH dans un domaine sont répertoriés au
           format long.

               asadmin> list-nodes-ssh --long=true
               NODE NAME   TYPE   NODE HOST          INSTALL DIRECTORY    REFERENCED BY
               sj02        SSH    sj02.example.com   /export/glassfish3   pmd-i-sj02, yml-i-sj02
               sj01        SSH    sj01.example.com   /export/glassfish3   pmd-i-sj01, yml-i-sj01
               Command list-nodes-ssh executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-node-dcom(1), create-node-config(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes(1), list-nodes-config(1), list-nodes-dcom(1)

       asadmin(1M)

Java EE 7                         8 déc 2011                 list-nodes-ssh(1)
