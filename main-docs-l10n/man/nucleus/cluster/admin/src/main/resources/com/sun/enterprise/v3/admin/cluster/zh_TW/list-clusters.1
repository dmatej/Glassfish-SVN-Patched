list-clusters(1)            asadmin 公用程式子命令            list-clusters(1)

參考名
       list-clusters - 會列出網域中現有的叢集

概要
           list-clusters [--help] [target]

描述
       list-clusters
       子命令會列出網域中的現有叢集。該清單可依叢集、執行處理、節點或組態進行篩選。對於列出的每個叢集，子命令會指出叢集是否正在執行。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           透過指定要列出叢集的目標來篩選叢集清單。有效的值如下：

           domain
               列出網域中的所有叢集 (預設值)。

           cluster-name
               只列出指定的叢集。

           instance-name
               列出指定執行處理所隸屬的叢集。

           node-name
               列出所包含的執行處理位於指定節點上的叢集。例如，如果叢集 pmdc
               中的執行處理 pmdi1 和叢集 ymlc 中的執行處理 ymli1 位於節點 n1
               上，則會列出 pmdc 和 ymlc。

           configuration-name
               列出包含已命名組態定義其組態之執行處理的所有叢集。

範例
       範例 1, 列出網域中的所有叢集
           此範例會列出目前網域中的所有叢集。

               asadmin> list-clusters
               pmdclust not running
               ymlclust not running

               Command list-clusters executed successfully.

       範例 2, 顯示叢集的狀態
           此範例會顯示目前未執行之叢集 ymlclust 的狀態。

               asadmin> list-clusters ymlclust
               ymlclust not running

               Command list-clusters executed successfully.

       範例 3, 列出與節點相關聯的所有叢集
           此範例會列出包含位於節點 sj02 之執行處理的叢集。

               asadmin> list-clusters sj02
               ymlclust not running

               Command list-clusters executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-cluster(1), delete-cluster(1), start-cluster(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                      2010 年 9 月 9 日              list-clusters(1)
