delete-config(1)        Subcomandos de la Utilidad asa        delete-config(1)

NOMBRE DE REFERENCIA
       delete-config - Suprime una configuración con nombre existente.

SINOPSIS
           delete-config [--help] configuration-name

DESCRIPCIÓN
       El subcomando delete-config suprime una configuración con nombre
       existente de la configuración del servidor de administración de dominio
       (DAS). Sólo puede suprimir una configuración si ninguna instancia o
       cluster de GlassFish Server hace referencia a la configuración. Las
       configuraciones independientes se suprimen automáticamente cuando se
       suprime la instancia o el cluster al que se hace referencia. No puede
       suprimir la configuración default-config que se ha copiado para crear
       configuraciones independientes.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       configuration-name
           Nombre de la configuración que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de una Configuración con Nombre
           En este ejemplo se suprime la configuración con nombre pmdconfig.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                   7 de diciembre de 2010            delete-config(1)
