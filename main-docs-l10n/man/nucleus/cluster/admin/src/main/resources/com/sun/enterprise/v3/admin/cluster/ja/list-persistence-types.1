list-persistence-typesasadminユーティリティのサブコマlist-persistence-types(1)

名前
       list-persistence-types -
       HTTPセッションおよびSFSBインスタンスに対して登録されている永続性タイプをリストします

概要
           list-persistence-types [--help] --type={web|ejb}

説明
       list-persistence-typesサブコマンドは、HTTPセッションおよびステートフル・セッションBean
       (SFSB)インスタンスに対して登録されている永続性タイプをリストします。組込みの永続性タイプは、memory、file、およびreplicatedです。memoryタイプはSFSBインスタンスには適用されません。

       その他の永続性タイプは、StrategyBuilderクラスを使用して追加できます。詳細は、Oracle
       GlassFish Serverア

       HTTPセッションに対して永続性タイプを設定するには、setサブコマンドを使用してpersistence-type属性を設定します。例:

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       可用性を有効にすることなくSFSBインスタンスに対して永続性タイプを設定するには、setサブコマンドを使用してsfsb-persistence-type属性を設定します。例:

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       可用性を有効にしてSFSBインスタンスに対する永続性タイプを設定するには、setサブコマンドを使用してsfsb-ha-persistence-type属性を設定します。例:

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --type
           永続性タイプをリストするセッションのタイプを指定します。使用できる値は次のとおりです。

           ·   web — HTTPセッションの永続性タイプをリストします。

           ·   ejb — SFSBインスタンスの永続性タイプをリストします。

例
       例 1, HTTPセッションの永続性タイプのリスト
           この例では、HTTPセッションの永続性タイプをリストします。

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年12月15日       list-persistence-types(1)
