copy-config(1)              asadmin 实用程序子命令              copy-config(1)

名称
       copy-config - 复制现有的命名配置以创建其他配置

提要
           copy-config [--help]
           [--systemproperties (name=value)[:name=value]*]
           source-configuration-name destination-configuration-name

说明
       copy-config 子命令通过复制现有的配置，在域管理服务器 (DAS)
       的配置中创建命名配置。新配置与复制的配置相同 (您在 --systemproperties
       选项中指定的属性除外)。

       创建独立服务器实例或独立集群时将复制 default-config 配置。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --systemproperties
           配置的可选属性名称/值对。这些属性将覆盖配置中的端口设置。

           以下属性可用：

           ASADMIN_LISTENER_PORT
               该属性指定 DAS 在连接到实例以管理实例时使用的 HTTP 端口或 HTTPS
               端口的端口号。有效值为 1-65535。在 UNIX 上，创建用于监听 1 到
               1024 端口的套接字需要超级用户权限。

           HTTP_LISTENER_PORT
               此属性可指定用于监听 HTTP 请求的端口的端口号。有效值为
               1-65535。在 UNIX 上，创建用于监听 1 到 1024
               端口的套接字需要超级用户权限。

           HTTP_SSL_LISTENER_PORT
               此属性可指定用于监听 HTTPS 请求的端口的端口号。有效值为
               1-65535。在 UNIX 上，创建用于监听 1 到 1024
               端口的套接字需要超级用户权限。

           IIOP_LISTENER_PORT
               此属性可指定用于 IIOP 连接的端口的端口号。有效值为 1-65535。在
               UNIX 上，创建用于监听 1 到 1024 端口的套接字需要超级用户权限。

           IIOP_SSL_LISTENER_PORT
               此属性可指定用于安全 IIOP 连接的端口的端口号。有效值为
               1-65535。在 UNIX 上，创建用于监听 1 到 1024
               端口的套接字需要超级用户权限。

           IIOP_SSL_MUTUALAUTH_PORT
               此属性可指定用于带有客户机验证功能的安全 IIOP
               连接的端口的端口号。有效值为 1-65535。在 UNIX 上，创建用于监听
               1 到 1024 端口的套接字需要超级用户权限。

           JAVA_DEBUGGER_PORT
               此属性指定用于连接到 Java 平台调试器体系结构 (JPDA)
               (http://java.sun.com/javase/technologies/core/toolsapis/jpda/)
               调试器的端口的端口号。有效值为 1-65535。在 UNIX
               上，创建用于监听 1 到 1024 端口的套接字需要超级用户权限。

           JMS_PROVIDER_PORT
               此属性指定 Java 消息服务提供方的端口号。有效值为 1-65535。在
               UNIX 上，创建用于监听 1 到 1024 端口的套接字需要超级用户权限。

           JMX_SYSTEM_CONNECTOR_PORT
               该属性指定 JMX 连接器监听的端口号。有效值为 1-65535。在 UNIX
               上，创建用于监听 1 到 1024 端口的套接字需要超级用户权限。

           OSGI_SHELL_TELNET_PORT
               此属性指定用于连接到 Apache Felix Remote Shell
               (http://felix.apache.org/site/apache-felix-remote-shell.html)
               的端口的端口号。此 Shell 使用 Felix Shell 服务与 OSGi
               模块管理子系统进行交互。有效值为 1-65535。在 UNIX
               上，创建用于监听 1 到 1024 端口的套接字需要超级用户权限。

操作数
       source-configuration-name
           您正在复制的配置的名称。

       destination-configuration-name
           通过复制源配置创建的配置的名称。

           名称必须满足以下要求：

           ·   名称中只能包含 ASCII 字符。

           ·   名称开头必须是字母、数字或下划线。

           ·   名称只能包含以下字符：

               ·   小写字母

               ·   大写字母

               ·   数字

               ·   连字符

               ·   句点

               ·   下划线

           ·   名称在域中必须是唯一的，而且不能是另一个命名配置、集群、GlassFish
               Server 实例或节点的名称。

           ·   名称不得是 domain、server 或 GlassFish Server
               保留的任何其他关键字。

示例
       Example 1, 复制配置
           本示例将 default-config 配置复制到 pmdsaconfig
           配置，覆盖以下端口的设置：

           ·   HTTP 监听程序端口

           ·   HTTPS 监听程序端口

               asadmin> copy-config
               --systemproperties HTTP_LISTENER_PORT=2000:HTTP_SSL_LISTENER_PORT=3000
               default-config pmdsaconfig

               Command copy-config executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-config(1), list-configs(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 14 日                copy-config(1)
