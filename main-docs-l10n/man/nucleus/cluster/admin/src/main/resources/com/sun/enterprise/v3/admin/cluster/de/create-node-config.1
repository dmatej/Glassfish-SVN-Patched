create-node-config(1)   Unterbefehle des asadmin-Utili   create-node-config(1)

NAME
       create-node-config - erstellt einen Knoten, der nicht für die
       Remote-Kommunikation aktiviert ist

ZUSAMMENFASSUNG
           create-node-config [--help]
           [--nodehost node-host]
           [--installdir as-install-parent] [--nodedir node-dir] node-name

BESCHREIBUNG
       Der Unterbefehl create-node-config erstellt einen Knoten, der nicht für
       die Remote-Kommunikation aktiviert ist. Der Unterbefehl
       create-node-config erfordert weder das DCOM-(Distributed Component
       Object Model-) noch das SSH-(Secure Shell-)Remote-Protokoll, um für das
       Erstellen des Knotens konfiguriert zu werden.

       Ein Knoten stellt einen Host dar, auf dem die GlassFish Server-Software
       installiert ist. Ein Knoten muss auf jedem Host vorhanden sein, auf dem
       sich GlassFish Server-Instanzen befinden.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Um den Host darzustellen,    │
           │           auf dem der DAS ausgeführt   │
           │           wird, gibt GlassFish Server  │
           │           den vordefinierten Knoten    │
           │           localhost-domain an. Der     │
           │           vordefinierte Knoten         │
           │           localhost-domain ist nicht   │
           │           für die Remote-Kommunikation │
           │           aktiviert.                   │
           └────────────────────────────────────────┘

       Die gesamte Administration der Instanzen auf einem Knoten, der nicht
       für die Remote-Kommunikation aktiviert ist, muss auf dem Host
       vorgenommen werden, den der Knoten darstellt. Der DAS (Domain
       Administration Server) auf einem Remote-Host kann den Knoten nicht
       kontaktieren. Um Instanzen auf einem Knoten zu verwalten, der für den
       DAS einen Remote-Host darstellt, müssen Sie folgende Unterbefehle
       verwenden:

       ·   create-local-instance(1)

       ·   delete-local-instance(1)

       ·   start-local-instance(1)

       Jedoch können Sie die Instanzen mit stop-local-instance(1) oder
       stop-instance(1) stoppen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --nodehost
           Der Name des Hosts, den der Knoten darstellt. Wenn diese Option
           ausgelassen wird, wird kein Host für den Knoten angegeben.

       --installdir
           Der vollständige Pfad zum übergeordneten Verzeichnis des
           Basisinstallationsverzeichnisses der GlassFish Server-Software auf
           dem Host, Beispiel: /export/glassfish3. Wenn diese Option
           ausgelassen wird, wird kein übergeordnetes Verzeichnis des
           Basisinstallationsverzeichnisses der GlassFish Server-Software
           angegeben.

       --nodedir
           Der Pfad zu dem Verzeichnis, das die GlassFish Server-Instanzen
           enthalten soll, die auf dem Knoten erstellt werden. Wenn ein
           relativer Pfad angegeben wird, ist der Pfad relativ zum Verzeichnis
           as-install. Wenn diese Option ausgelassen wird, wird für den Knoten
           kein Verzeichnis für Instanzen angegeben.

OPERANDEN
       node-name
           Der Name des Knotens.

           Der Name muss den folgenden Anforderungen entsprechen:

           ·   Der Name darf nur ASCII-Zeichen enthalten.

           ·   Der Name muss mit einem Buchstaben, einer Ziffer oder einem
               Unterstrich beginnen.

           ·   Der Name darf nur die folgenden Zeichen enthalten:

               ·   Kleinbuchstaben

               ·   Großbuchstaben

               ·   Ziffern

               ·   Bindestriche

               ·   Punkte

               ·   Unterstriche

           ·   Der Name muss in der Domain eindeutig sein und darf nicht der
               Name eines anderen Knotens, eines Clusters, einer benannten
               Konfiguration oder einer GlassFish Server-Instanz sein.

           ·   Bei dem Namen darf es sich nicht um domain, server oder ein
               anderes Schlüsselwort handeln, das von GlassFish Server
               reserviert ist.

BEISPIELE
       Beispiel 1, Knoten, die nicht für die Remote-Kommunikation aktiviert
       sind, erstellen
           In diesem Beispiel wird der Knoten sj03 für Host sj03.example.com
           erstellt. Der Knoten ist nicht für die Remote-Kommunikation
           aktiviert.

               asadmin> create-node-config --nodehost sj03.example.com sj03

               Command create-node-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-local-instance(1), create-node-dcom(1), create-node-ssh(1),
       delete-local-instance(1), delete-node-config(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       start-local-instance(1), stop-instance(1), stop-local-instance(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                        08. Dez. 2011           create-node-config(1)
