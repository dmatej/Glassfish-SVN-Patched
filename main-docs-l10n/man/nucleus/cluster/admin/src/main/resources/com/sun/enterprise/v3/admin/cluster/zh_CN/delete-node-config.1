delete-node-config(1)       asadmin 实用程序子命令       delete-node-config(1)

名称
       delete-node-config - 删除不允许进行远程通信的节点

提要
           delete-node-config [--help] node-name

说明
       delete-node-config
       子命令从域中删除不允许进行远程通信的节点。此子命令不要求配置分布式组件对象模型
       (DCOM) 远程协议或安全 shell (SSH)。

       此子命令只能删除不允许进行远程通信的节点。用于删除其他类型节点的子命令取决于节点的类型：

       ·   必须使用 delete-node-dcom(1) 子命令才能删除允许通过 DCOM
           进行远程通信的节点。

       ·   必须使用 delete-node-ssh(1) 子命令才能删除允许通过 SSH
           进行远程通信的节点。

       要确定是否允许某个节点进行远程通信，请使用 list-nodes(1) 子命令。

       要删除的节点上不能有任何 GlassFish Server
       实例。否则，该子命令将失败。在运行此子命令之前，请使用
       delete-instance(1) 或 delete-local-instance(1)
       这类子命令删除节点上的任何实例。

           注意
           ┌───────────────────────────────────┐
           │注                                 │
           │                                   │
           │           无法删除预定义的        │
           │           localhost-domain 节点。 │
           └───────────────────────────────────┘

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       node-name
           要删除的节点的名称。不能允许此节点通过 DCOM 或 SSH
           进行通信。否则，会出现错误。

示例
       Example 1, 删除不允许进行远程通信的节点
           此示例删除不允许进行远程通信的节点 sj03。

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日         delete-node-config(1)
