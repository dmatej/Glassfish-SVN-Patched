list-configs(1)       asadminユーティリティのサブコマンド      list-configs(1)

名前
       list-configs - 名前付き構成をリストします

概要
           list-configs [--help] [target]

説明
       list—
       configsサブコマンドは、ドメイン管理サーバー(DAS)の構成内の名前付き構成をリストします。リストは、クラスタ、インスタンス、または名前付き構成でフィルタできます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           構成のリストをフィルタします。有効な値は次のとおりです:

           domain
               現在のドメイン内のすべての名前付き構成をリストします。

           cluster-name
               指定したクラスタ内のインスタンスの構成を定義する名前付き構成をリストします。

           instance-name
               指定したインスタンスの構成を定義する名前付き構成をリストします。

           configuration-name
               指定した名前付き構成をリストします。このオプションを使用して、名前付き構成が存在するかを確認します。

例
       例 1, ドメイン内のすべての名前付き構成のリスト
           この例では、現在のドメイン内のすべての名前付き構成をリストします。

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       copy-config(1), delete-config(1)

       asadmin(1M)

       構成(5ASC)

Java EE 7                       2010年12月20日                 list-configs(1)
