delete-config(1)            asadmin 实用程序子命令            delete-config(1)

名称
       delete-config - 删除现有的命名配置

提要
           delete-config [--help] configuration-name

说明
       delete-config 子命令从域管理服务器 (DAS)
       的配置中删除现有的命名配置。仅当没有任何 GlassFish Server
       实例或集群引用某个配置时，才可以删除该配置。当引用独立配置的实例或集群被删除时，该独立配置会自动被删除。不能删除已被复制的用于创建独立配置的
       default-config 配置。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       configuration-name
           要删除的配置的名称。

示例
       Example 1, 删除命名配置
           此示例删除命名配置 pmdconfig。

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                     2010 年 12 月 7 日              delete-config(1)
