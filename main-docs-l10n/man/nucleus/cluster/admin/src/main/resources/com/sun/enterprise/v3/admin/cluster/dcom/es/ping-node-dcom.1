ping-node-dcom(1)       Subcomandos de la Utilidad asa       ping-node-dcom(1)

NOMBRE DE REFERENCIA
       ping-node-dcom - comprueba si se puede utilizar un nodo que está
       activado para la comunicación a través de DCOM se puede utilizar.

SINOPSIS
           ping-node-dcom [--help]
           [--validate={false|true}] node-name

DESCRIPCIÓN
       El subcomando ping-node-dcom comprueba si se puede utilizar un nodo que
       está activado para la comunicación sobre el protocolo remoto
       Distributed Component Object Model (DCOM). El protocolo DCOM sólo está
       disponible para los sistemas Windows. Este subcomando requiere que se
       configure DCOM en el equipo en el que se ejecuta el servidor de
       administración de dominio (DAS) y en el equipo en el que reside el
       nodo. Puede ejecutar este subcomando desde cualquier máquina que pueda
       establecer contacto con el DAS.

       Si el nodo se puede utilizar, el subcomando muestra una confirmación de
       que el subcomando pudo conectar con el nodo a través de DCOM. Esta
       confirmación incluye el nombre del host que representa el nodo.

       Si lo desea, el subcomando también puede validar el nodo para
       determinar si la utilidad asadmin(1M) se puede ejecutar en el host que
       representa el nodo. Para validar un nodo, el subcomando ejecuta el
       subcomando version(1). Si el nodo es válido, el subcomando muestra la
       versión que devuelve el subcomando version.

       El nodo que se especifica como el operando de este subcomando debe
       estar activado para la comunicación a través de DCOM. Si el nodo no
       está activado para la comunicación a través de DCOM, se produce un
       error. Para determinar si un nodo está activado para la comunicación a
       través de DCOM, utilice el subcomando list-nodes(1).

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Para comprobar si se puede  │
           │           utilizar un nodo que está   │
           │           activado para la            │
           │           comunicación a través de    │
           │           Secure Shell (SSH), utilice │
           │           el subcomando               │
           │           ping-node-ssh(1).           │
           └───────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --validate
           Especifica si el subcomando valida el nodo.

           Los valores posibles son los siguientes:

           true
               El nodo se valida.

           false
               El nodo no se valida (por defecto).

OPERANDOS
       node-name
           El nombre del nodo que se debe comprobar. El nodo debe estar
           activado para la comunicación a través de DCOM. De lo contrario, se
           produce un error.

EJEMPLOS
       Ejemplo 1, Comprobación de si se Puede Utilizar un Nodo Activado para
       DCOM
           En este ejemplo se comprueba si se puede utilizar el nodo activado
           para DCOM wpmd2.

               asadmin> ping-node-dcom wpmdl2
               Successfully made DCOM connection to node wpmdl2 (wpmdl2.example.com)
               Command ping-node-dcom executed successfully.

       Ejemplo 2, Validación de un Nodo Activado para DCOM
           En este ejemplo se valida el nodo activado para DCOM adc-node.

               asadmin> ping-node-dcom --validate=true adc-node
               Successfully made DCOM connection to node adc-node (adc-node.example.com)
               GlassFish version found at C:\glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1.2 (build 18)
               Command version executed successfully.
               Command ping-node-dcom executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-node-dcom(1), delete-node-dcom(1), list-nodes(1),
       ping-node-ssh(1), update-node-dcom(1), validate-dcom(1), version(1)

       asadmin(1M)

Java EE 7                   05 de diciembre de 2011          ping-node-dcom(1)
