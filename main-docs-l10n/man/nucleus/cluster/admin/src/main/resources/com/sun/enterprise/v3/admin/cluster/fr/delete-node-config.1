delete-node-config(1)   Sous-commandes de l'utilitaire   delete-node-config(1)

NOM
       delete-node-config - supprime un noeud non activé pour les
       communications à distance

SYNTAXE
           delete-node-config [--help] node-name

DESCRIPTION
       La sous-commande delete-node-config supprime un noeud qui n'est pas
       activé pour les communications à distance à partir du domaine. Elle
       n'exige pas que le protocole distant DCOM (Distributed Component Object
       Model) ou que le protocole SSH (Secure Shell) soit configuré.

       Cette sous-commande peut supprimer uniquement un noeud qui n'est pas
       activé pour les communications à distance. La sous-commande permettant
       de supprimer d'autres types de noeud dépend du type du noeud :

       ·   Un noeud activé pour les communications à distance via DCOM doit
           être supprimé à l'aide de la sous-commande delete-node-dcom(1).

       ·   Un noeud activé pour les communications à distance via SSH doit
           être supprimé à l'aide de la sous-commande delete-node-ssh(1).

       Pour déterminer si un noeud est activé pour les communications à
       distance, utilisez la sous-commande list-nodes(1).

       Aucune instance GlassFish Server ne doit résider sur le noeud à
       supprimer. Sinon, la sous-commande échoue. Avant d'exécuter cette
       sous-commande, supprimez toutes les instances qui résident sur le noeud
       à l'aide, par exemple, de la sous-commande delete-instance(1) ou
       delete-local-instance(1).

           Note
           ┌────────────────────────────────────────┐
           │Remarque                                │
           │                                        │
           │           Le noeud prédéfini           │
           │           localhost-domain ne peut pas │
           │           être supprimé.               │
           └────────────────────────────────────────┘

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       node-name
           Nom du noeud à supprimer. Le noeud ne doit pas être activé pour la
           communication via DCOM ou SSH. Sinon, une erreur survient.

EXEMPLES
       Exemple 1, Suppression d'un noeud non activé pour les communications à
       distance
           Dans cet exemple, le noeud sj03, qui n'est pas activé pour les
           communications à distance, est supprimé.

               asadmin> delete-node-config sj03

               Command delete-node-config executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-node-config(1), create-node-dcom(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node(1),
       install-node-dcom(1), install-node-ssh(1), list-nodes(1),
       uninstall-node(1), uninstall-node-dcom(1), uninstall-node-ssh(1),
       update-node-config(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                         8 déc 2011             delete-node-config(1)
