list-configs(1)         Subcomandos do utilitário asad         list-configs(1)

NOME
       list-configs - lista as configurações nomeadas

SINOPSE
           list-configs [--help] [target]

DESCRIçãO
       O subcomando list—configs lista as configurações nomeadas na
       configuração do servidor de administração de domínios (DAS). A lista
       pode ser filtrada pelo cluster, instância ou configuração nomeada.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Filtra a lista de configurações. Os valores válidos são os
           seguintes:

           domain
               Lista as configurações nomeadas do domínio atual.

           cluster-name
               Lista a configuração nomeada que define a configuração de
               instâncias no cluster especificado.

           instance-name
               Lista a configuração nomeada que define a configuração da
               instância especificada.

           configuration-name
               Lista a configuração nomeada especificada. Use esta opção para
               determinar se a configuração nomeada existe.

EXEMPLOS
       Example 1, Listando Todas as Configurações Nomeadas em um Domínio
           Este exemplo lista todas as configurações nomeadas no domínio
           atual.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                         20 dez 2010                  list-configs(1)
