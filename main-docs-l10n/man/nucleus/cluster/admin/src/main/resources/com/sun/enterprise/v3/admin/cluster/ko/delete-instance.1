delete-instance(1)        asadmin 유틸리티 하위 명령        delete-instance(1)

제목
       delete-instance - GlassFish Server 인스턴스 삭제

시놉시스
           delete-instance [--help] instance-name

설명
       delete-instance 하위 명령은 GlassFish Server 인스턴스를 삭제합니다. 이
       하위 명령을 사용하려면 DAS(도메인 관리 서버)가 실행되고 있는 호스트 및
       인스턴스가 상주하는 노드가 나타내는 호스트에 DCOM(Distributed Component
       Object Model) 원격 프로토콜 또는 SSH(보안 셸)가 구성되어 있어야 합니다.

           참고
           ┌───────────────────────────────────────┐
           │주                                     │
           │                                       │
           │           인스턴스가 로컬 호스트를    │
           │           나타내는 CONFIG 유형의      │
           │           노드에 상주하는 경우 DCOM   │
           │           또는 SSH가 필요하지         │
           │           않습니다.  CONFIG 유형의    │
           │           노드는 DCOM 또는 SSH를 통한 │
           │           원격 통신에는 사용할 수     │
           │           없습니다.                   │
           └───────────────────────────────────────┘

       DAS에 접속할 수 있는 모든 호스트에서 이 하위 명령을 실행할 수 있습니다.

       이 하위 명령은 인스턴스가 생성된 방법에 상관없이 모든 GlassFish Server
       인스턴스를 삭제할 수 있습니다. 예를 들어 이 하위 명령은
       create-local-instance(1) 하위 명령을 사용하여 생성된 인스턴스를 삭제할
       수 있습니다.

       삭제할 인스턴스는 실행 중이 아니어야 합니다. 그렇지 않으면 오류가
       발생합니다.

       이 하위 명령은 다음 작업을 수행하여 인스턴스를 삭제합니다.

       ·   DAS(도메인 관리 서버)의 구성에서 인스턴스 제거

       ·   파일 시스템에서 인스턴스의 파일 삭제

       삭제할 인스턴스가 노드 디렉토리를 사용하고 있는 유일한 인스턴스인 경우
       해당 디렉토리도 제거됩니다.

       독립형 인스턴스가 삭제된 경우 인스턴스의 독립형 구성도 삭제됩니다.
       독립형 인스턴스는 다른 클러스터 또는 클러스터되지 않은 인스턴스에서
       참조하지 않는 instance-name-config라는 구성을 참조합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       instance-name
           삭제할 인스턴스의 이름입니다.

예제
       예 1, GlassFish Server 인스턴스 삭제
           이 예제는 GlassFish Server 인스턴스 pmdsainst를 삭제합니다.

               asadmin> delete-instance pmdsainst

               Command delete-instance executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-instance(1), create-local-instance(1), delete-local-instance(1),
       setup-ssh(1), start-instance(1), start-local-instance(1),
       stop-instance(1), stop-local-instance(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                       2011년 12월 8일             delete-instance(1)
