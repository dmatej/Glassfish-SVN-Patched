ping-node-dcom(1)       Unterbefehle des asadmin-Utili       ping-node-dcom(1)

NAME
       ping-node-dcom - prüft, ob ein für die Kommunikation über DCOM
       aktivierter Knoten verfügbar ist

ZUSAMMENFASSUNG
           ping-node-dcom [--help]
           [--validate={false|true}] node-name

BESCHREIBUNG
       Der Unterbefehl ping-node-dcom prüft, ob ein Knoten, der für die
       Kommunikation über das DCOM-(Distributed Component Object
       Model-)Remote-Protokoll aktiviert wurde, verfügbar ist. Das
       DCOM-Protokoll ist nur bei Windows-Systemen verfügbar. Dieser
       Unterbefehl erfordert die Konfiguration von DCOM auf dem Rechner, auf
       dem der DAS (Domain Administration Server) ausgeführt wird, und auf dem
       Rechner, auf dem sich der Knoten befindet. Sie können diesen Befehl
       über jeden Rechner ausführen, der sich mit dem DAS in Verbindung setzen
       kann.

       Ist der Knoten verfügbar, zeigt der Unterbefehl eine Bestätigung an,
       dass er eine Verbindung zum Knoten über DCOM herstellen konnte. Diese
       Bestätigung beinhaltet den Namen des Hosts, den der Knoten darstellt.

       Optional kann der Unterbefehl auch den Knoten validieren, um zu
       ermitteln, ob das Utility asadmin(1M) auf dem Host ausgeführt werden
       kann, den der Knoten darstellt. Zum Validieren eines Knotens führt der
       Unterbefehl den Unterbefehl version(1) aus. Ist der Knoten gültig,
       zeigt der Unterbefehl die Version an, die der Unterbefehl version
       zurückgibt.

       Der Knoten, der als Operand dieses Unterbefehls angegeben ist, muss für
       die Kommunikation über DCOM aktiviert sein. Ist der Knoten nicht für
       die Kommunikation über DCOM aktiviert, tritt ein Fehler auf. Um
       festzustellen, ob ein Knoten für die Kommunikation über DCOM aktiviert
       ist, verwenden Sie den Unterbefehl list-nodes(1).

           Anmerkung
           ┌──────────────────────────────────────┐
           │Hinweis                               │
           │                                      │
           │           Verwenden Sie den          │
           │           Unterbefehl                │
           │           ping-node-ssh(1), um zu    │
           │           prüfen, ob ein Knoten      │
           │           verfügbar ist, der für die │
           │           Kommunikation über SSH     │
           │           (Secure Shell) aktiviert   │
           │           wurde.                     │
           └──────────────────────────────────────┘

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --validate
           Gibt an, ob der Unterbefehl den Knoten validiert.

           Mögliche Werte:

           true
               Der Knoten wird validiert.

           false
               Der Knoten wird nicht validiert (Standard).

OPERANDEN
       node-name
           Der Name des Knotens, der geprüft werden soll. Der Knoten muss für
           die Kommunikation über DCOM aktiviert sein. Andernfalls tritt ein
           Fehler auf.

BEISPIELE
       Beispiel 1, Prüfen, ob ein Knoten verfügbar ist, der für die
       Kommunikation über DCOM aktiviert wurde
           In diesem Beispiel wird geprüft, ob der für DCOM aktivierte Knoten
           wpmd2 verfügbar ist.

               asadmin> ping-node-dcom wpmdl2
               Successfully made DCOM connection to node wpmdl2 (wpmdl2.example.com)
               Command ping-node-dcom executed successfully.

       Beispiel 2, Einen für DCOM aktivierten Knoten validieren
           In diesem Beispiel wird der für DCOM aktivierte Knoten adc-node
           validiert.

               asadmin> ping-node-dcom --validate=true adc-node
               Successfully made DCOM connection to node adc-node (adc-node.example.com)
               GlassFish version found at C:\glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1.2 (build 18)
               Command version executed successfully.
               Command ping-node-dcom executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-node-dcom(1), delete-node-dcom(1), list-nodes(1),
       ping-node-ssh(1), update-node-dcom(1), validate-dcom(1), version(1)

       asadmin(1M)

Java EE 7                        05. Dez. 2011               ping-node-dcom(1)
