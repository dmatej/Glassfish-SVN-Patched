list-configs(1)             asadmin 实用程序子命令             list-configs(1)

名称
       list-configs - 列出指定的配置

提要
           list-configs [--help] [target]

说明
       list—configs 子命令可列出域管理服务器 (DAS)
       配置中的命名配置。可按照集群、实例或命名的配置对列表进行过滤。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           过滤配置列表。有效值如下：

           domain
               列出当前域中的所有命名配置。

           cluster-name
               列出定义指定集群中的实例配置的命名配置。

           instance-name
               列出用来定义指定实例配置的命名配置。

           configuration-name
               列出指定的命名配置。使用该选项可以确定某个命名配置是否存在。

示例
       Example 1, 列出域中的所有命名配置
           此示例列出当前域中的所有命名配置。

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                     2010 年 12 月 20 日              list-configs(1)
