list-configs(1)         Sous-commandes de l'utilitaire         list-configs(1)

NOM
       list-configs - répertorie les configurations nommées

SYNTAXE
           list-configs [--help] [target]

DESCRIPTION
       La sous-commande list—configs répertorie les configurations nommées
       dans la configuration du serveur d'administration de domaine (serveur
       DAS). La liste peut être filtrée par cluster, instance ou configuration
       nommée.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Filtre la liste des configurations. Les valeurs valides sont les
           suivantes :

           domain
               Répertorie toutes les configurations nommées du domaine en
               cours.

           cluster-name
               Répertorie la configuration nommée qui définit la configuration
               des instances dans le cluster spécifié.

           instance-name
               Répertorie la configuration nommée qui définit la configuration
               de l'instance spécifiée.

           configuration-name
               Répertorie la configuration nommée spécifiée. Utilisez cette
               option pour déterminer si une configuration nommée existe.

EXEMPLES
       Exemple 1, Liste de toutes les configurations nommées d'un domaine
           Dans cet exemple, toutes les configurations nommées du domaine en
           cours sont répertoriées.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                        20 déc. 2010                  list-configs(1)
