delete-node-ssh(1)      Subcomandos do utilitário asad      delete-node-ssh(1)

NOME
       delete-node-ssh - deleta um nó que está ativado para comunicação por
       meio de SSH

SINOPSE
           delete-node-ssh [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

DESCRIçãO
       O subcomando delete-node-ssh deleta um nó que é ativado para a
       comunicação através do shell seguro (SSH) do domínio. O subcomando não
       requer que SSH seja configurado.

       Este subcomando pode deletar apenas um nó que está ativado para a
       comunicação por meio de SSH. O subcomando para deletar outros tipos de
       nós depende do tipo do nó:

       ·   Um nó que está ativado para comunicação por meio do protocolo
           remoto DCOM (Distributed Component Object Model) deve ser deletado
           usando o subcomando delete-node-dcom(1).

       ·   Um nó que não está ativado para a comunicação remota deve ser
           deletado utilizando o subcomando delete-node-config(1).

       Para determinar se um nó está ativado para comunicação por meio de SSH,
       utilize o subcomando list-nodes(1).

       Nenhuma GlassFish Server instância devem residir no nó que está sendo
       deletado. Caso contrário, o subcomando irá falhar. Antes de executar
       este subcomando, delete quaisquer instâncias que residem no nó
       utilizando, por exemplo, o subcomandodelete-instance(1) ou o subcomando
       delete-local-instance(1).

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --uninstall
           Especifica se o software GlassFish Server será desinstalado do host
           que o nó representa.

           Os valores possíveis são os seguintes:

           false
               O software GlassFish Server não foi desinstalado do host
               (default).

           true
               O software GlassFish Server foi desinstalado do host. Por
               default, se qualquer nó, exceto o nó predefinido
               localhost-domain residir em qualquer host do qual o software
               GlassFish Server estiver sendo desinstalado, o subcomando
               falhará. Para desinstalar o software GlassFish Server de um
               host no qual residem nós definidos pelo usuário, defina a opção
               --force como true. Se a opção --force for true, o subcomando
               removerá todo o conteúdo do pai do diretório-base de
               instalação.

       --force
           Se a opção --uninstall for verdadeira, especificará se o subcomando
           desinstalará o software GlassFish Server de um host, mesmo se um nó
           definido pelo usuário residir no host. Os valores possíveis são os
           seguintes:

           false
               Se um nó definido pelo usuário residir em um host, o software
               não será desinstalado e o subcomando falhará (default).

               Se a opção --force for false, o subcomando só removerá os
               arquivos do software GlassFish Server. Outro conteúdo do
               principal do diretório-base de instalação, tal como arquivos de
               configuração, não é removido.

           true
               O subcomando desinstala o software GlassFish Server do host,
               mesmo um nó definido pelo usuário reside no host.

               Se a opção --force for true, o subcomando removerá todo o
               conteúdo do pai do diretório-base de instalação.

OPERANDOS
       node-name
           O nome do nó a ser deletado. O nó deve ser ativado para comunicação
           por meio do SSH. Caso contrário, ocorrerá um erro.

EXEMPLOS
       Example 1, Deletando um nó que está Ativado para Comunicação Através do
       SSH
           Esse exemplo deleta o nó eg1, o qual está ativado para comunicação
           por meio do SSH.

               asadmin> delete-node-ssh eg1
               Command delete-node-ssh executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-node-dcom(1), create-node-ssh(1), delete-node-config(1),
       delete-node-dcom(1), delete-instance(1), delete-local-instance(1),
       install-node(1), install-node-dcom(1), install-node-ssh(1),
       list-nodes(1), uninstall-node(1), uninstall-node-dcom(1),
       uninstall-node-ssh(1), update-node-dcom(1), update-node-ssh(1)

       asadmin(1M)

Java EE 7                         8 dez 2011                delete-node-ssh(1)
