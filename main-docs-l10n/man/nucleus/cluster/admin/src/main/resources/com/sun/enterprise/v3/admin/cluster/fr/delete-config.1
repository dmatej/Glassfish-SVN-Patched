delete-config(1)        Sous-commandes de l'utilitaire        delete-config(1)

NOM
       delete-config - supprime une configuration nommée existante

SYNTAXE
           delete-config [--help] configuration-name

DESCRIPTION
       La sous-commande delete-config supprime une configuration nommée
       existante dans la configuration du serveur d'administration de domaine
       (DAS). Vous pouvez supprimer une configuration uniquement si aucune
       instance et aucun cluster GlassFish Server n'y fait référence. Une
       configuration autonome est automatiquement supprimée lors de la
       suppression de l'instance ou du cluster qui y fait référence. Vous ne
       pouvez pas supprimer la configuration default-config copiée pour créer
       des configurations autonomes.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       configuration-name
           Nom de la configuration que vous supprimez.

EXEMPLES
       Exemple 1, Suppression d'une configuration nommée
           Cet exemple montre comment supprimer la configuration nommée
           pmdconfig.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                         7 déc 2010                  delete-config(1)
