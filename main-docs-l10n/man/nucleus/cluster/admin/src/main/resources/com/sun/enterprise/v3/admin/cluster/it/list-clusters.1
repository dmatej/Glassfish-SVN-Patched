list-clusters(1)        Comandi secondari della utilit        list-clusters(1)

NOME
       list-clusters - elenca i cluster esistenti in un dominio

SINTASSI
           list-clusters [--help] [target]

DESCRIZIONE
       Il comando secondario list-clusters elenca i cluster esistenti in un
       dominio. La lista può essere filtrata per cluster, istanza, nodo o
       configurazione. Per ogni cluster elencato, il comando secondario indica
       se è in esecuzione.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Filtra la lista dei cluster specificando la destinazione per la
           quale è necessario elencare i cluster. Di seguito sono riportati i
           valori validi.

           domain
               Elenca tutti i cluster nel dominio (impostazione predefinita).

           cluster-name
               Elenca solo il cluster specificato.

           instance-name
               Elenca il cluster di cui l'istanza specificata è un membro.

           node-name
               Elenca i cluster che contengono un'istanza che risiede nel nodo
               specificato. Ad esempio, se l'istanza pmdi1 nel cluster pmdc e
               l'istanza ymli1 nel cluster ymlc risiedono nel nodo n1, gli
               elementi pmdc e ymlc vengono elencati.

           configuration-name
               Elenca tutti i cluster che contengono istanze la cui
               configurazione è definita dalla configurazione denominata.

ESEMPI
       Esempio 1, Visualizzazione della lista di tutti i cluster in un dominio
           In questo esempio vengono elencati tutti i cluster nel dominio
           corrente.

               asadmin> list-clusters
               pmdclust not running
               ymlclust not running

               Command list-clusters executed successfully.

       Esempio 2, Visualizzazione dello stato di un cluster
           In questo esempio viene visualizzato lo stato del cluster ymlclust,
           che non è in esecuzione.

               asadmin> list-clusters ymlclust
               ymlclust not running

               Command list-clusters executed successfully.

       Esempio 3, Visualizzazione della lista di tutti i cluster associati a
       un nodo
           In questo esempio vengono elencati i cluster che contengono
           un'istanza residente nel nodo sj02.

               asadmin> list-clusters sj02
               ymlclust not running

               Command list-clusters executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-cluster(1), delete-cluster(1), start-cluster(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                      9 settembre 2010               list-clusters(1)
