delete-config(1)            asadmin 公用程式子命令            delete-config(1)

參考名
       delete-config - 刪除現有指定的組態

概要
           delete-config [--help] configuration-name

描述
       delete-config 子命令會刪除網域管理伺服器 (DAS)
       組態中現有指定的組態。只有在沒有任何 GlassFish Server
       執行處理或叢集參照組態時，方可刪除該組態。在刪除參照它的執行處理或叢集時，也會自動刪除獨立組態。您無法將複製用來建立獨立組態的
       default-config 組態刪除。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       configuration-name
           您要刪除的組態名稱。

範例
       範例 1, 刪除指定的組態
           此範例會刪除指定的組態 pmdconfig。

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                     2010 年 12 月 7 日              delete-config(1)
