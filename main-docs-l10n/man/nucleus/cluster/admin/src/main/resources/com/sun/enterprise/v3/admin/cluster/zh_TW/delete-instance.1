delete-instance(1)          asadmin 公用程式子命令          delete-instance(1)

參考名
       delete-instance - 刪除 GlassFish Server 執行處理

概要
           delete-instance [--help] instance-name

描述
       delete-instance 子命令會刪除 GlassFish Server
       執行處理。這個子命令需要在執行網域管理伺服器 (DAS)
       的主機上，以及由執行處理所在節點代表的主機上，組態分散式元件物件模型
       (DCOM) 遠端協定或安全 Shell (SSH)。

           注意
           ┌────────────────────────────────────────────────────────────────────────┐
           │備註                                                                    │
           │                                                                        │
           │           如果執行處理位於代表本機主機且類型為組
           │           DCOM 或                                                      │
           │           SSH。未啟用類型為組
           │           DCOM 或 SSH 進行遠端通訊。                                   │
           └────────────────────────────────────────────────────────────────────────┘

       您可以從任何可連絡 DAS 的主機執行此子命令。

       無論 GlassFish Server
       執行處理的建立方式為何，這個子命令都能夠刪除任何執行處理。例如，此子命令可以刪除使用
       create-local-instance(1) 子命令建立的執行處理。

       要刪除的執行處理不得處於執行狀態。否則，將發生錯誤。

       此子命令透過執行下列動作刪除執行處理：

       ·   從網域管理伺服器 (DAS) 的組態移除執行處理

       ·   從檔案系統刪除執行處理檔案

       如果要刪除的執行處理是唯一使用節點目錄的執行處理，則也會移除該目錄。

       如果刪除獨立執行處理，則也會刪除該執行處理的獨立組態。參照名為
       instance-name-config
       的組態，且沒有其他叢集或非叢集的組態參照的獨立執行處理。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       instance-name
           要刪除的執行處理名稱。

範例
       範例 1, 刪除 GlassFish Server 執行處理
           此範例會刪除 GlassFish Server 執行處理
            pmdsainst。

               asadmin> delete-instance pmdsainst

               Command delete-instance executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-instance(1), create-local-instance(1), delete-local-instance(1),
       setup-ssh(1), start-instance(1), start-local-instance(1),
       stop-instance(1), stop-local-instance(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日            delete-instance(1)
