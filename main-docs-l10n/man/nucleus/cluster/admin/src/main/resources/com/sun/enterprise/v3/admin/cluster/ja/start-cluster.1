start-cluster(1)      asadminユーティリティのサブコマンド     start-cluster(1)

名前
       start-cluster - クラスタを起動します

概要
           start-cluster [--help] [--autohadboverride={true|false}]
           [--verbose={false|true}] cluster-name

説明
       start-clusterサブコマンドは、クラスタ内のまだ実行されていないすべてのGlassFish
       Serverインスタンスを起動することで、クラスタを起動します。このサブコマンドを使用する場合は、ドメイン管理サーバー(DAS)が稼働しているホスト上およびクラスタ内のインスタンスが存在するすべてのホスト上に、分散コンポーネント・オブジェクト・モデル(DCOM)リモート・プロトコルまたはセキュア・シェル(SSH)を構成する必要があります。

           注記
           ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注意                                                                                                                                                                                                                                                                                                                                                           │
           │                                                                                                                                                                                                                                                                                                                                                               │
           │           すべてのインスタンスがDASと同じホストにある場合、DCOMまたはSSHは必要ありません。DCOMまたはSSHが構成されていないDASからのリモート・ホスト上にインスタンスがあり、そのインスタンスが含まれたクラスタの起動が必要な場合があります。この状況では、インスタンスが存在するホストから、インスタンスごとにstart-local-instance(1)サブコマンドを実行します。 │
           └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       このサブコマンドは、DASにアクセス可能な任意のホストから実行できます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --autohadboverride
           このオプションは指定しないでください。このオプションは、旧リリースとの互換性のために保持されています。このオプションを指定した場合、構文エラーは発生しません。かわりに、サブコマンドが正常に実行され、オプションが無視されたことを示す警告メッセージが表示されます。

       --verbose
           クラスタが起動したときに追加のステータス情報を表示するかを指定します。

           有効な値は次のとおりです:

           true
               クラスタ内の各インスタンスを起動するためのコマンド、および各インスタンスの起動が成功したかどうかが表示されます。

           false
               追加のステータス情報は表示されません(デフォルト)。

オペランド
       cluster-name
           起動するクラスタの名前。

例
       例 1, クラスタの起動
           この例では、クラスタymlclusterを起動します。クラスタが起動すると、その他のステータス情報が表示されます。

               asadmin> start-cluster --verbose ymlcluster
               start-instance yml-i-sr1-usca-02
               start-instance yml-i-sr1-usca-01

               The command start-instance executed successfully for:
               yml-i-sr1-usca-02 yml-i-sr1-usca-01

               Command start-cluster executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-cluster(1), delete-cluster(1), list-clusters(1), setup-ssh(1),
       start-local-instance(1), stop-cluster(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                        2011年12月8日                start-cluster(1)
