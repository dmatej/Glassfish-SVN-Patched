delete-node-dcom(1)         asadmin 公用程式子命令         delete-node-dcom(1)

參考名
       delete-node-dcom - 刪除已啟用透過 DCOM 通訊的節點

概要
           delete-node-dcom [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

描述
       delete-node-dcom 子命令會刪除網域中已啟用透過分散式元件物件模型 (DCOM)
       遠端協定進行通訊的節點。DCOM 協定僅在 Windows
       系統上提供。此子命令不需要組態 DCOM。

       這個子命令只能刪除透過 DCOM
       啟用通訊的節點。刪除其他類型節點的子命令取決於節點的類型：

       ·   透過安全 Shell (SSH) 啟用通訊的節點必須使用 delete-node-ssh(1)
           子命令刪除。

       ·   未啟用通訊的節點必須使用 delete-node-config(1) 子命令刪除。

       若要判斷節點是否啟用透過 DCOM 通訊，請使用 list-nodes(1) 子命令。

       要刪除的節點中不得有任何 GlassFish Server
       執行處理。否則子命令會失敗。在執行此子命令之前，請刪除位於該節點的所有執行處理，例如使用
       delete-instance(1) 子命令或 delete-local-instance(1) 子命令。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --uninstall
           指定是否要解除安裝節點所代表之主機上的 GlassFish Server 軟體。

           可能值如下：

           false
               不解除安裝主機上的 GlassFish Server 軟體 (預設值)。

           true
               解除安裝主機上的 GlassFish Server 軟體。依預設，如果要解除安裝
               GlassFish Server 軟體的任何主機上有預先定義節點
               localhost-domain
               以外的任何節點，此子命令便會失敗。若要解除安裝使用者定義之節點所在主機上的
               GlassFish Server 軟體，請將 --force 選項設為 true。如果 --force
               選項為 true，則子命令會移除基底安裝目錄之父系目錄的所有內容。

       --force
           如果 --uninstall 為
           true，請指定即使主機上有使用者定義的節點，子命令是否仍要解除安裝該主機上的
           GlassFish Server 軟體。可能值如下：

           false
               如果主機上有使用者定義的節點，則不會解除安裝該軟體，且子命令會失敗
               (預設值)。

               如果 --force 選項為 false，子命令只會移除 GlassFish Server
               軟體檔案。不會移除基底安裝目錄之父系目錄的其他內容，例如組態檔案。

           true
               即使主機上有使用者定義的節點，該子命令仍會解除安裝 GlassFish
               Server 軟體。

               如果 --force 選項為
               true，則子命令會移除基底安裝目錄之父系目錄的所有內容。

運算元
       node-name
           要刪除的節點名稱。此節點必須啟用透過 DCOM 通訊。否則，將發生錯誤。

範例
       範例 1, 刪除啟用透過 DCOM 通訊的節點
           此範例會刪除節點 xkyd，此節點已啟用透過 DCOM 通訊。

               asadmin> delete-node-dcom xkyd
               Command delete-node-dcom executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-node-dcom(1), delete-node-config(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node-dcom(1),
       list-nodes(1), uninstall-node-dcom(1), update-node-dcom(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 5 日           delete-node-dcom(1)
