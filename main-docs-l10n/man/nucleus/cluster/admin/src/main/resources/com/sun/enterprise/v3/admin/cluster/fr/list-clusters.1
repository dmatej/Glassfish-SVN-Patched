list-clusters(1)        Sous-commandes de l'utilitaire        list-clusters(1)

NOM
       list-clusters - répertorie les clusters existants dans un domaine

SYNTAXE
           list-clusters [--help] [target]

DESCRIPTION
       La sous-commande list-clusters répertorie les clusters existants dans
       un domaine. La liste peut être filtrée par cluster, instance, noeud ou
       configuration. Pour chaque cluster répertorié, la sous-commande indique
       si le cluster est en cours d'exécution.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Filtre la liste des clusters en spécifiant la cible pour laquelle
           les clusters doivent être répertoriés. Les valeurs valides sont les
           suivantes :

           domain
               Répertorie tous les clusters du domaine (par défaut).

           cluster-name
               Répertorie uniquement le cluster spécifié.

           instance-name
               Répertorie le cluster dont l'instance spécifiée est membre.

           node-name
               Répertorie les clusters contenant une instance qui réside sur
               le noeud spécifié. Par exemple, si l'instance pmdi1 du cluster
               pmdc et l'instance ymli1 du cluster ymlc résident sur le noeud
               n1, les clusters pmdc et ymlc sont répertoriés.

           configuration-name
               Répertorie tous les clusters qui contiennent des instances dont
               la configuration est définie par la configuration nommée.

EXEMPLES
       Exemple 1, Liste de tous les clusters d'un domaine
           Dans cet exemple, tous les clusters du domaine en cours sont
           répertoriés.

               asadmin> list-clusters
               pmdclust not running
               ymlclust not running

               Command list-clusters executed successfully.

       Exemple 2, Affichage du statut d'un cluster
           Dans cet exemple, le statut du cluster ymlclust, qui n'est pas en
           cours d'exécution, est affiché.

               asadmin> list-clusters ymlclust
               ymlclust not running

               Command list-clusters executed successfully.

       Exemple 3, Liste de tous les clusters associés à un noeud
           Dans cet exemple, les clusters contenant une instance qui réside
           sur le noeud sj02 sont répertoriés.

               asadmin> list-clusters sj02
               ymlclust not running

               Command list-clusters executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-cluster(1), delete-cluster(1), start-cluster(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                         9 sep 2010                  list-clusters(1)
