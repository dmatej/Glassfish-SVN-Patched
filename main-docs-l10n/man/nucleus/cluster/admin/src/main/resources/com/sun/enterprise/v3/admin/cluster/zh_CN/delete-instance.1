delete-instance(1)          asadmin 实用程序子命令          delete-instance(1)

名称
       delete-instance - 删除 GlassFish Server 实例

提要
           delete-instance [--help] instance-name

说明
       delete-instance 子命令删除 GlassFish Server
       实例。此子命令要求在运行域管理服务器 (DAS)
       的主机上，以及由实例所在节点表示的主机上配置分布式组件对象模型 (DCOM)
       远程协议或安全 shell (SSH)。

           注意
           ┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注                                                                                                                                │
           │                                                                                                                                  │
           │           如果实例位于表示本地主机的、配
           └──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       可以从任何可联系 DAS 的主机运行此子命令。

       此子命令可删除任何 GlassFish Server
       实例，而不考虑实例的创建方式。例如，此子命令可删除使用
       create-local-instance(1) 子命令创建的实例。

       要删除的实例不能处于运行状态。否则，会出现错误。

       该子命令通过执行以下操作删除实例：

       ·   从域管理服务器 (DAS) 的配置删除实例

       ·   从文件系统删除实例的文件

       如果要删除的实例是唯一使用节点目录的实例，该目录也将一同删除。

       如果删除独立实例，该实例的独立配置也将删除。独立实例引用名为
       instance-name-config 的配置，任何其他集群或非集群实例都没有引用该配置。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       instance-name
           要删除的实例的名称。

示例
       Example 1, 删除 GlassFish Server 实例
           此示例删除 GlassFish Server 实例 pmdsainst。

               asadmin> delete-instance pmdsainst

               Command delete-instance executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-instance(1), create-local-instance(1), delete-local-instance(1),
       setup-ssh(1), start-instance(1), start-local-instance(1),
       stop-instance(1), stop-local-instance(1), validate-dcom(1)

       asadmin(1M)

Java EE 7                     2011 年 12 月 8 日            delete-instance(1)
