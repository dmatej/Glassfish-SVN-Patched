ping-node-ssh(1)        Subcomandos do utilitário asad        ping-node-ssh(1)

NOME
       ping-node-ssh - testa se um nó que está ativado para a comunicação por
       meio de SSH é utilizável

SINOPSE
           ping-node-ssh [--help]
           [--validate={false|true}] node-name

DESCRIçãO
       O subcomando ping-node-ssh testa se um nó que está ativado para a
       comunicação por meio do shell seguro (SSH) é utilizável. Este
       subcomando requer que um shell seguro (SSH) seja configurado na máquina
       onde o servidor de administração de domínios (DAS) está em execução e
       na máquina onde o nó reside. É possível executar esse comando a partir
       de qualquer máquina que possa entrar em contato com o DAS.

       Se o nó for utilizável, o subcomando exibe uma confirmação de que o
       subcomando pôde se comunicar com o nó através do SSH. Esta confirmação
       inclui o nome do host que o nó representa.

       Opcionalmente, o subcomando também pode validar o nó para determinar se
       o utilitário asadmin(1M) pode ser executado no host que o nó
       representa. Para validar um nó, o subcomando executa o subcomando
       version(1). Se o nó for válido, o subcomando exibirá a versão que o
       subcomando version retorna.

       O nó que é especificado como o operando deste subcomando precisa ser
       ativado para a comunicação por meio do SSH. Se o nó não for ativado
       para a comunicação por meio do SSH, ocorre um erro. Para determinar se
       um nó está ativado para comunicação por meio de SSH, utilize o
       subcomando list-nodes(1).

           Nota
           ┌───────────────────────────────────────┐
           │Observação                             │
           │                                       │
           │           Para testar se um nó que    │
           │           está ativado para           │
           │           comunicação por meio do     │
           │           protocolo remoto DCOM       │
           │           (Distributed Component      │
           │           Object Model) pode ser      │
           │           utilizado, use o subcomando │
           │           ping-node-dcom(1).          │
           └───────────────────────────────────────┘

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --validate
           Especifica se o subcomando valida o nó.

           Os valores possíveis são os seguintes:

           true
               O nó é validado.

           false
               O nó não é validado (default).

OPERANDOS
       node-name
           O nome do nó a ser testado. O nó deve ser ativado para comunicação
           por meio de SSH. Caso contrário, ocorrerá um erro.

EXEMPLOS
       Example 1, Testando se um Nó Ativado para o SSH é Utilizável
           Este exemplo testa se o nó sj03-node ativado para o SSH é
           utilizável.

               asadmin> ping-node-ssh sj03-node
               Successfully made SSH connection to node sj03-node (sj03.example.com)
               Command ping-node-ssh executed successfully.

       Example 2, Validando um Nó Ativado para o SSH
           Este exemplo valida o nó adc-node ativado para o SSH.

               asadmin> ping-node-ssh --validate=true adc-node
               Successfully made SSH connection to node adcnode (adc.example.com)
               GlassFish version found at /export/glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1 (build 40)
               Command version executed successfully.
               Command ping-node-ssh executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-node-ssh(1), delete-node-ssh(1), list-nodes(1),
       ping-node-dcom(1), setup-ssh(1), update-node-ssh(1), version(1)

       asadmin(1M)

Java EE 7                         8 dez 2011                  ping-node-ssh(1)
