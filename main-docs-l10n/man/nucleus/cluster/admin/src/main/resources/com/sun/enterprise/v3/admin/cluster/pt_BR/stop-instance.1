stop-instance(1)        Subcomandos do utilitário asad        stop-instance(1)

NOME
       stop-instance - interrompe uma instância do GlassFish Server em
       execução

SINOPSE
           stop-instance [--help]
           [--force={false|true}] [--kill={false|true}]
           instance-name

DESCRIçãO
       O subcomando stop-instance interrompe a instância do GlassFish Server
       em execução.

       O subcomando pode interromper qualquer instância do GlassFish Server,
       independentemente de como a instância foi criada. Por exemplo, esse
       subcomando pode interromper uma instância que foi criada usando o
       subcomando create-local-instance(1).

       Este comando é suportado só no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --force
           Especifica se a instância é interrompida de maneira forçada
           imediatamente.

           Os valores possíveis são os seguintes:

           true
               A instância é interrompida de maneira forçada imediatamente
               (default).

           false
               O subcomando espera até que todos os threads que estão
               associados com a instância sejam fechados antes de parar a
               instância.

       --kill
           Especifica se a instância é desativada usando a funcionalidade do
           sistema operacional para encerrar o processo da instância.

           Os valores possíveis são os seguintes:

           false
               A instância não foi desativada. O subcomando usa a
               funcionalidade da plataforma Java para encerrar o processo de
               instância (default).

           true
               A instância foi desativada. O subcomando usa a funcionalidade
               do sistema operacional para encerrar o processo da instância.

OPERANDOS
       instance-name
           Este é o nome da instância do GlassFish Server a ser interrompida.

EXEMPLOS
       Este exemplo interrompe a instância yml-i-sj01 do GlassFish Server.

       Example 1, Interrompendo uma Instância do GlassFish Server

               asadmin> stop-instance yml-i-sj01
               The instance, yml-i-sj01, was stopped.

               Command stop-instance executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-instance(1), create-local-instance(1), delete-instance(1),
       delete-local-instance(1), start-domain(1), start-instance(1),
       start-local-instance(1), stop-domain(1), stop-local-instance(1)

       asadmin(1M)

Java EE 7                   16 de dezembro de 2010            stop-instance(1)
