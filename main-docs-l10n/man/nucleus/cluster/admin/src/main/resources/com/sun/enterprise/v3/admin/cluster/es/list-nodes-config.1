list-nodes-config(1)    Subcomandos de la Utilidad asa    list-nodes-config(1)

NOMBRE DE REFERENCIA
       list-nodes-config - muestra todos los nodos de GlassFish Server que no
       aceptan comunicación remota en un dominio.

SINOPSIS
           list-nodes-config [--help] [--long={false|true}]

DESCRIPCIÓN
       El subcomando list-nodes-config muestra todos los nodos de GlassFish
       Server que no soportan comunicación remota en un dominio.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Para mostrar todos los nodos │
           │           de un dominio                │
           │           independientemente del tipo  │
           │           del nodo, ejecute el         │
           │           subcomando list-nodes(1).    │
           └────────────────────────────────────────┘

       Por defecto, el subcomando muestra la siguiente información para cada
       nodo que aparece en la lista:

       ·   El nombre del nodo

       ·   El tipo de nodo, que es CONFIG

       ·   El nombre del host que el nodo representa

       La opción --long del subcomando especifica si los nodos se muestran en
       la lista en formato largo. En formato largo, se muestra la siguiente
       información adicional sobre cada nodo:

       ·   La ruta de acceso al principal del directorio de instalación básica
           de GlassFish Server en el host que representa el nodo

       ·   Una lista separada por comas de los nombres de las instancias de
           GlassFish Server que residen en el nodo

       Si la opción --terse de la utilidad asadmin(1M) es true y la opción
       --long del subcomando es false, el subcomando muestra sólo el nombre de
       cada nodo.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --long, -l
           Especifica si los nodos se muestran en la lista en formato largo.

           Los valores posibles son los siguientes:

           true
               Los nodos se muestran en la lista en formato largo.

           false
               Los nodos se muestran en la lista en formato corto (valor por
               defecto).

EJEMPLOS
       Ejemplo 1, Lista de Nodos de GlassFish Server
           En este ejemplo se muestran el nombre, el tipo y el host para todos
           los nodos de GlassFish Server que no soportan comunicación remota
           en el dominio domain1.

               asadmin> list-nodes-config
               localhost-domain1  CONFIG  localhost
               devnode  CONFIG  localhost
               Command list-nodes-config executed successfully.

       Ejemplo 2, Lista sólo de los Nombres de Nodos de GlassFish Server
           En este ejemplo se utiliza la opción --terse de la utilidad asadmin
           para mostrar sólo los nombres de los nodos de GlassFish Server que
           no soportan comunicación remota en el dominio domain1.

               asadmin> list-nodes-config --terse=true
               localhost-domain1
               devnode

       Ejemplo 3, Lista de Nodos de GlassFish Server en Formato Largo
           En este ejemplo se muestra una lista de los nodos de GlassFish
           Server que no soportan comunicación remota en el dominio domain1 en
           formato largo.

               asadmin> list-nodes-config --long=true
               NODE NAME           TYPE     NODE HOST   INSTALL DIRECTORY    REFERENCED BY
               localhost-domain1   CONFIG   localhost   /export/glassfish3
               devnode             CONFIG   localhost   /export/glassfish3   pmdsa1
               Command list-nodes-config executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-node-config(1), create-node-dcom(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes(1), list-nodes-dcom(1), list-nodes-ssh(1)

       asadmin(1M)

Java EE 7                   8 de diciembre de 2011        list-nodes-config(1)
