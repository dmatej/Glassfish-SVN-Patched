list-persistence-types(1)   asadmin 实用程序子命令   list-persistence-types(1)

名称
       list-persistence-types - 列出 HTTP 会话和 SFSB 实例的已注册持久类型

提要
           list-persistence-types [--help] --type={web|ejb}

说明
       list-persistence-types 子命令列出 HTTP 会话和有状态会话 Bean (SFSB)
       实例的已注册持久类型。内置的持久类型为 memory、file 和
       replicated。memory 类型不适用于 SFSB 实例。

       可以使用 StrategyBuilder 类添加其他持久类型。有关详细信息，请参阅
       Oracle GlassFish Server Add-On Component Development Guide。

       要设置 HTTP 会话的持久类型，请使用 set 子命令设置 persistence-type
       属性。例如：

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       要设置未启用可用性的 SFSB 实例的持久类型，请使用 set 子命令设置
       sfsb-persistence-type 属性。例如：

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       要设置已启用可用性的 SFSB 实例的持久类型，请使用 set 子命令设置
       sfsb-ha-persistence-type 属性。例如：

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --type
           指定要列出持久类型的会话的类型。允许的值包括：

           ·   web - 列出 HTTP 会话的持久类型。

           ·   ejb - 列出 SFSB 实例的持久类型。

示例
       Example 1, 列出 HTTP 会话的持久类型
           此示例列出 HTTP 会话的持久类型。

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                     2010 年 12 月 15 日    list-persistence-types(1)
