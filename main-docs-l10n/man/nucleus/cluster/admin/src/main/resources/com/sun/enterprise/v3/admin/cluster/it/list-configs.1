list-configs(1)         Comandi secondari della utilit         list-configs(1)

NOME
       list-configs - elenca le configurazioni denominate

SINTASSI
           list-configs [--help] [target]

DESCRIZIONE
       Il comando secondario list—configs elenca le configurazioni denominate
       nella configurazione del server di amministrazione del dominio (DAS).
       La lista può essere filtrata per cluster, istanza o configurazione
       denominata.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Filtra la lista delle configurazioni. Di seguito sono riportati i
           valori validi.

           domain
               Elenca tutte le configurazioni denominate nel dominio corrente.

           cluster-name
               Elenca la configurazione denominata che definisce la
               configurazione delle istanze nel cluster specificato.

           instance-name
               Elenca la configurazione denominata che definisce la
               configurazione dell'istanza specificata.

           configuration-name
               Elenca la configurazione denominata specificata. Utilizzare
               questa opzione per determinare se una configurazione denominata
               esiste.

ESEMPI
       Esempio 1, Visualizzazione della lista di tutte le configurazioni
       denominate in un dominio
           In questo esempio vengono elencate tutte le configurazioni
           denominate nel dominio corrente.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                      20 dicembre 2010                list-configs(1)
