list-persistence-types(1Subcomandos do utilitário asalist-persistence-types(1)

NOME
       list-persistence-types - lista tipos de persistência registrados para
       sessões HTTP e instâncias SFSB

SINOPSE
           list-persistence-types [--help] --type={web|ejb}

DESCRIçãO
       O subcomando list-persistence-types lista os tipos registrados de
       persistências para as sessões HTTP e para instâncias de bean de sessão
       com estado (SFSB). Os tipos de persistências incorporados são memory ,
       file e replicated. O tipo memory não se aplica às instâncias SFSB.

       Outros tipos de persistências podem ser adicionados usando a classe
       StrategyBuilder. Para obter mais informações, consulte o Oracle
       GlassFish Server Add-On Component Development Guide.

       Para definir o tipo de persistência para as sessões HTTP, use o
       subcomando set para definir o atributo persistence-type. Por exemplo:

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       Para definir o tipo de persistência para instâncias SFSB sem a
       disponibilidade ativada, use o subcomando set para definir o atributo
       sfsb-persistence-type . Por exemplo:

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       Para definir o tipo de persistência para instâncias SFSB com a
       disponibilidade ativada, use o subcomando set para definir o atributo
       sfsb-ha-persistence-type . Por exemplo:

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --type
           Especifica os tipos de sessões para os quais os tipos de
           persistências são listados. Os valores permitidos são os seguintes:

           ·   web — Lista os tipos de persistência das sessões HTTP.

           ·   ejb — Lista os tipos de persistências das instâncias SFSB.

EXEMPLOS
       Example 1, Listando os Tipos de Persistências para Sessões HTTP
           Este exemplo lista os tipos de persistências para sessões HTTP.

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                         15 dez 2010        list-persistence-types(1)
