stop-cluster(1)             asadmin 实用程序子命令             stop-cluster(1)

名称
       stop-cluster - 停止 GlassFish Server 集群

提要
           stop-cluster [--help]
           [--verbose={false|true}]
           [--kill={false|true}]
           [--autohadboverride={true|false}]
           cluster-name

说明
       stop-cluster 子命令通过停止某个 GlassFish Server 集群中所有正在运行的
       GlassFish Server 实例来停止该集群。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --verbose
           指定在停止集群时，是否显示有关该集群中实例状态的其他进度消息。

           可能的值如下：

           真
               显示有关该集群中实例状态的其他进度消息。

           假
               不显示有关该集群中实例状态的消息。

       --kill
           指定是否使用操作系统的功能来终止实例进程，从而中止集群中的每个实例。

           可能的值如下：

           假
               不中止实例。该子命令将使用 Java 平台的功能来终止每个实例进程
               (默认值)。

           真
               中止每个实例。该子命令将使用操作系统的功能来终止每个实例进程。

       --autohadboverride
           不指定此选项。为了与早期发行版兼容而保留此选项。如果指定此选项，则不会出现语法错误。相反，子命令会成功运行，并显示忽略了此选项的警告消息。

操作数
       cluster-name
           要停止的集群的名称。

示例
       Example 1, 停止集群
           该示例将停止集群
           pmdcluster。在停止集群时显示有关该集群中实例状态的其他进度消息。

               asadmin> stop-cluster --verbose pmdcluster
               stop-instance pmd-i-sj01
               stop-instance pmd-i-sj02

               The command stop-instance executed successfully for: pmd-i-sj01 pmd-i-sj02

               Command stop-cluster executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-cluster(1), delete-cluster(1), list-clusters(1),
       start-cluster(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 16 日              stop-cluster(1)
