delete-node-dcom(1)       asadmin 유틸리티 하위 명령       delete-node-dcom(1)

제목
       delete-node-dcom - DCOM을 통한 통신에 대해 사용으로 설정된 노드 삭제

시놉시스
           delete-node-dcom [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

설명
       delete-node-dcom 하위 명령은 DCOM(Distributed Component Object Model)
       원격 프로토콜을 통한 통신에 대해 사용으로 설정된 노드를 도메인에서
       삭제합니다. DCOM 프로토콜은 Windows 시스템에서만 사용할 수 있습니다. 이
       하위 명령을 사용하기 위해 DCOM을 구성할 필요는 없습니다.

       이 하위 명령은 DCOM을 통한 통신에 대해 사용으로 설정된 노드만 삭제할 수
       있습니다. 다른 유형의 노드를 삭제하는 하위 명령은 노드 유형에 따라
       다릅니다.

       ·   SSH(보안 셸)를 통한 통신에 대해 사용으로 설정된 노드는
           delete-node-ssh(1) 하위 명령을 사용하여 삭제해야 합니다.

       ·   통신에 대해 사용으로 설정되지 않은 노드는 delete-node-config(1)
           하위 명령을 사용하여 삭제해야 합니다.

       노드가 DCOM을 통한 통신에 대해 사용으로 설정되어 있는지를 확인하려면
       list-nodes(1) 하위 명령을 사용합니다.

       삭제할 노드에는 GlassFish Server 인스턴스가 없어야 합니다. 그렇지
       않으면 하위 명령을 실패합니다. 이 하위 명령을 실행하기 전에
       delete-instance(1) 하위 명령 또는 delete-local-instance(1) 하위 명령
       등을 사용하여 노드에 있는 모든 인스턴스를 삭제합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --uninstall
           노드가 나타내는 호스트에서 GlassFish Server 소프트웨어를 제거할지를
           지정합니다.

           가능한 값은 다음과 같습니다.

           false
               호스트에서 GlassFish Server 소프트웨어가 제거되지
               않습니다(기본값).

           true
               호스트에서 GlassFish Server 소프트웨어가 제거됩니다. 기본적으로
               미리 정의된 노드 localhost-domain을 제외한 노드가 GlassFish
               Server 소프트웨어를 제거하는 호스트에 상주하는 경우 하위 명령을
               실패합니다. 사용자 정의 노드가 상주하는 호스트에서 GlassFish
               Server 소프트웨어를 제거하려면 --force 옵션을 true로
               설정합니다.  --force 옵션이 true이면 하위 명령이 기본 설치
               디렉토리의 상위의 전체 내용을 제거합니다.

       --force
           --uninstall이 true이면 사용자 정의 노드가 호스트에 상주하더라도
           하위 명령이 호스트에서 GlassFish Server 소프트웨어를 제거하는지를
           지정합니다. 가능한 값은 다음과 같습니다.

           false
               사용자 정의 노드가 호스트에 상주하는 경우 소프트웨어가 제거되지
               않고 하위 명령을 실패합니다(기본값).

               --force 옵션이 false이면 하위 명령이 GlassFish Server
               소프트웨어 파일만 제거합니다. 구성 파일 등 기본 설치 디렉토리의
               상위에 있는 기타 콘텐츠는 제거되지 않습니다.

           true
               사용자 정의 노드가 호스트에 상주하는 경우에도 하위 명령이
               호스트에서 GlassFish Server 소프트웨어의 설치를 해제합니다.

               --force 옵션이 true이면 하위 명령이 기본 설치 디렉토리의 상위의
               전체 내용을 제거합니다.

피연산자
       node-name
           삭제할 노드의 이름입니다. 이 노드는 DCOM을 통한 통신에 대해
           사용으로 설정되어 있어야 합니다. 그렇지 않으면 오류가 발생합니다.

예제
       예 1, DCOM을 통한 통신에 대해 사용으로 설정된 노드 삭제
           이 예제는 DCOM을 통한 통신에 대해 사용으로 설정된 노드 xkyd를
           삭제합니다.

               asadmin> delete-node-dcom xkyd
               Command delete-node-dcom executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-node-dcom(1), delete-node-config(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node-dcom(1),
       list-nodes(1), uninstall-node-dcom(1), update-node-dcom(1)

       asadmin(1M)

Java EE 7                       2011년 12월 5일            delete-node-dcom(1)
