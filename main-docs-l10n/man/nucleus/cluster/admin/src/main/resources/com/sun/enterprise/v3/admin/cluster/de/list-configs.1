list-configs(1)         Unterbefehle des asadmin-Utili         list-configs(1)

NAME
       list-configs - listet benannte Konfigurationen auf

ZUSAMMENFASSUNG
           list-configs [--help] [target]

BESCHREIBUNG
       Der Unterbefehl list—configs listet benannte Konfigurationen in der
       Konfiguration des DAS (Domain Administration Server) auf. Die Liste
       kann nach Cluster, Instanz oder benannter Konfiguration gefiltert
       werden.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Filtert die Liste der Konfigurationen. Folgende Werte sind gültig:

           domain
               Listet alle benannten Konfigurationen in der aktuellen Domain
               auf.

           cluster-name
               Listet die benannte Konfiguration auf, die die Konfiguration
               der Instanzen im angegebenen Cluster definiert.

           instance-name
               Listet die benannte Konfiguration auf, die die Konfiguration
               der angegebenen Instanz definiert.

           configuration-name
               Listet die angegebene benannte Konfiguration auf. Verwenden Sie
               diese Option, um zu ermitteln, ob eine benannte Konfiguration
               beendet wird.

BEISPIELE
       Beispiel 1, Alle benannten Konfigurationen in einer Domain auflisten
           In diesem Beispiel werden alle benannten Konfigurationen in der
           aktuellen Domain aufgelistet.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                        20. Dez. 2010                 list-configs(1)
