list-nodes-config(1)    Unterbefehle des asadmin-Utili    list-nodes-config(1)

NAME
       list-nodes-config - listet alle GlassFish Server-Knoten auf, die keine
       Remote-Kommunikation in einer Domain unterstützen

ZUSAMMENFASSUNG
           list-nodes-config [--help] [--long={false|true}]

BESCHREIBUNG
       Der Unterbefehl list-nodes-config listet alle GlassFish Server-Knoten
       auf, die keine Remote-Kommunikation in einer Domain unterstützen.

           Anmerkung
           ┌──────────────────────────────────────┐
           │Hinweis                               │
           │                                      │
           │           Um alle Knoten in einer    │
           │           Domain unabhängig vom Typ  │
           │           des Knotens aufzulisten,   │
           │           führen Sie den Unterbefehl │
           │           list-nodes(1) aus.         │
           └──────────────────────────────────────┘

       Standardmäßig zeigt der Unterbefehl für jeden aufgelisteten Knoten die
       folgenden Informationen an:

       ·   den Namen des Knotens

       ·   den Typ des Knotens (CONFIG)

       ·   den Namen des Hosts, den der Knoten darstellt

       Die Option --long des Unterbefehls gibt an, ob die Knoten im Langformat
       aufgelistet werden. Im Langformat werden zu jedem Knoten die folgenden
       zusätzlichen Informationen angezeigt:

       ·   der Pfad zum übergeordneten Verzeichnis des
           Basisinstallationsverzeichnisses von GlassFish Server auf dem Host,
           den der Knoten darstellt

       ·   eine durch Komma getrennte Liste der Namen der GlassFish
           Server-Instanzen, die sich auf dem Knoten befinden

       Wenn die Option --terse des Utilitys asadmin(1M) auf true gesetzt und
       die Option --long des Unterbefehls auf false gesetzt ist, listet der
       Unterbefehl nur den Namen jedes Knotens auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --long, -l
           Gibt an, ob die Knoten im Langformat aufgelistet werden.

           Mögliche Werte:

           true
               Die Knoten werden im Langformat aufgelistet.

           false
               Die Knoten werden im Kurzformat aufgelistet (Standard).

BEISPIELE
       Beispiel 1, GlassFish Server-Knoten auflisten
           Dieses Beispiel zeigt Namen, Typ und Host für alle GlassFish
           Server-Knoten an, die keine Remote-Kommunikation in der Domain
           domain1 unterstützen.

               asadmin> list-nodes-config
               localhost-domain1  CONFIG  localhost
               devnode  CONFIG  localhost
               Command list-nodes-config executed successfully.

       Beispiel 2, Nur die Namen der GlassFish Server-Knoten auflisten
           In diesem Beispiel wird die Option --terse des Utilitys asadmin
           verwendet, um nur die Namen der GlassFish Server-Knoten
           aufzulisten, die keine Remote-Kommunikation in der Domain domain1
           unterstützen.

               asadmin> list-nodes-config --terse=true
               localhost-domain1
               devnode

       Beispiel 3, GlassFish Server-Knoten im Langformat auflisten
           In diesem Beispiel werden die GlassFish Server-Knoten, die keine
           Remote-Kommunikation in der Domain domain1 unterstützen, im
           Langformat aufgelistet.

               asadmin> list-nodes-config --long=true
               NODE NAME           TYPE     NODE HOST   INSTALL DIRECTORY    REFERENCED BY
               localhost-domain1   CONFIG   localhost   /export/glassfish3
               devnode             CONFIG   localhost   /export/glassfish3   pmdsa1
               Command list-nodes-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-node-config(1), create-node-dcom(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes(1), list-nodes-dcom(1), list-nodes-ssh(1)

       asadmin(1M)

Java EE 7                        8. Dez. 2011             list-nodes-config(1)
