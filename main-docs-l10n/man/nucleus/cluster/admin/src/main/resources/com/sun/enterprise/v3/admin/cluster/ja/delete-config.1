delete-config(1)      asadminユーティリティのサブコマンド     delete-config(1)

名前
       delete-config - 既存の名前付き構成を削除します

概要
           delete-config [--help] configuration-name

説明
       delete-configサブコマンドは、ドメイン管理サーバー(DAS)の構成から既存の名前付き構成を削除します。構成を削除できるのは、その構成をGlassFish
       Server
       インスタンスやクラスタが参照していない場合のみです。スタンドアロンの構成は、その構成を参照しているインスタンスまたはクラスタが削除されると自動的に削除されます。スタンドアロンの構成を作成するためにコピーされたdefault-config構成は削除できません。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       configuration-name
           削除する構成の名前。

例
       例 1, 名前付き構成の削除
           この例では、名前付き構成pmdconfigを削除します。

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                        2010年12月7日                delete-config(1)
