list-persistence-types(1Unterbefehle des asadmin-Utillist-persistence-types(1)

NAME
       list-persistence-types - listet registrierte Persistenztypen für
       HTTP-Sessions und SFSB-Instanzen auf

ZUSAMMENFASSUNG
           list-persistence-types [--help] --type={web|ejb}

BESCHREIBUNG
       Der Unterbefehl list-persistence-types listet registrierte
       Persistenztypen für HTTP-Sessions und Stateful Session
       Bean-(SFSB-)Instanzen auf. Die integrierten Persistenztypen sind memory
       , file und replicated. Der Typ memory gilt nicht für SFSB-Instanzen.

       Andere Persistenztypen können mithilfe der Klasse StrategyBuilder
       hinzugefügt werden. Weitere Informationen finden Sie im Oracle
       GlassFish Server Add-On Component Development Guide.

       Den Persistenztyp für HTTP-Sessions können Sie festlegen, indem Sie mit
       dem Unterbefehl set das Attribut persistence-type festlegen. Beispiel:

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       Sie können den Persistenztyp für SFSB-Instanzen ohne aktivierte
       Verfügbarkeit festlegen, indem Sie mit dem Unterbefehl set das Attribut
       sfsb-persistence-type festlegen. Beispiel:

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       Sie können den Persistenztyp für SFSB-Instanzen mit aktivierter
       Verfügbarkeit festlegen, indem Sie mit dem Unterbefehl set das Attribut
       sfsb-ha-persistence-type festlegen. Beispiel:

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --type
           Gibt den Sessiontyp an, für den Persistenztypen aufgelistet werden.
           Folgende Werte sind zulässig:

           ·   web: Listet Persistenztypen für HTTP-Sessions auf.

           ·   ejb: Listet Persistenztypen für SFSB-Instanzen auf.

BEISPIELE
       Example 1, Persistenztypen für HTTP-Sessions auflisten
           In diesem Beispiel werden Persistenztypen für HTTP-Sessions
           aufgelistet.

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                        15. Dez. 2010       list-persistence-types(1)
