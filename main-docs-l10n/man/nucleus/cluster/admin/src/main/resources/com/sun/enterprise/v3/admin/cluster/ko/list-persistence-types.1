list-persistence-types(1) asadmin 유틸리티 하위 명령 list-persistence-types(1)

제목
       list-persistence-types - HTTP 세션 및 SFSB 인스턴스에 대해 등록된
       지속성 유형 나열

시놉시스
           list-persistence-types [--help] --type={web|ejb}

설명
       list-persistence-types 하위 명령은 HTTP 세션 및 SFSB(Stateful 세션Bean)
       인스턴스에 대해 등록된 지속성 유형을 나열합니다. 내장된 지속성 유형은
       memory, file 및 replicated입니다.  memory 유형은 SFSB 인스턴스에
       적용되지 않습니다.

       다른 지속성 유형은 StrategyBuilder 클래스를 사용하여 추가할 수
       있습니다. 자세한 내용은 Oracle GlassFish Server Add-On Component
       Development Guide을(를) 참조하십시오.

       HTTP 세션에 대한 지속성 유형을 설정하려면 set 하위 명령을 사용하여
       persistence-type 속성을 설정합니다. 예를 들면 다음과 같습니다.

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       가용성을 사용으로 설정하지 않은 상태에서 SFSB 인스턴스에 대한 지속성
       유형을 설정하려면 set 하위 명령을 사용하여 sfsb-persistence-type 속성을
       설정합니다. 예를 들면 다음과 같습니다.

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       가용성을 사용으로 설정한 상태에서 SFSB 인스턴스에 대한 지속성 유형을
       설정하려면 set 하위 명령을 사용하여 sfsb-ha-persistence-type 속성을
       설정합니다. 예를 들면 다음과 같습니다.

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --type
           지속성 유형을 나열할 세션 유형을 지정합니다. 허용되는 값은 다음과
           같습니다.

           ·   web — HTTP 세션에 대한 지속성 유형을 나열합니다.

           ·   ejb — SFSB 인스턴스에 대한 지속성 유형을 나열합니다.

예제
       예 1, HTTP 세션에 대한 지속성 유형 나열
           이 예제는 HTTP 세션에 대한 지속성 유형을 나열합니다.

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                      2010년 12월 15일      list-persistence-types(1)
