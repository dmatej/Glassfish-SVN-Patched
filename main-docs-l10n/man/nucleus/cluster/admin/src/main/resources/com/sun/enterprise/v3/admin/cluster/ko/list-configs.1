list-configs(1)           asadmin 유틸리티 하위 명령           list-configs(1)

제목
       list-configs - 명명된 구성 나열

시놉시스
           list-configs [--help] [target]

설명
       list—configs 하위 명령은 DAS(도메인 관리 서버)의 구성에서 명명된 구성을
       나열합니다. 목록은 클러스터, 인스턴스 또는 명명된 구성별로 필터링할 수
       있습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           구성 목록을 필터링합니다. 적합한 값은 다음과 같습니다.

           domain
               현재 도메인의 모든 명명된 구성을 나열합니다.

           cluster-name
               지정된 클러스터에서 인스턴스의 구성을 정의하는 명명된 구성을
               나열합니다.

           instance-name
               지정된 인스턴스의 구성을 정의하는 명명된 구성을 나열합니다.

           configuration-name
               지정된 명명된 구성을 나열합니다. 명명된 구성이 있는지
               확인하려면 이 옵션을 사용합니다.

예제
       예 1, 도메인의 모든 명명된 구성 나열
           이 예제는 현재 도메인에서 명명된 구성을 모두 나열합니다.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                      2010년 12월 20일                list-configs(1)
