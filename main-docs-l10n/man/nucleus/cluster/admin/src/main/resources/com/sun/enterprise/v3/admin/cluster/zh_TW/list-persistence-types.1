list-persistence-types(1)   asadmin 公用程式子命令   list-persistence-types(1)

參考名
       list-persistence-types - 列出 HTTP 階段作業和 SFSB
       執行處理的註冊永久性類型

概要
           list-persistence-types [--help] --type={web|ejb}

描述
       list-persistence-types 子命令會列出 HTTP 階段作業和有狀態的階段作業
       Bean (SFSB) 執行處理的已註冊持續性類型。內建的持續性類型有 memory、file
       和 replicated。memory 類型不適用於 SFSB 執行處理。

       其他持續性類型可以使用 StrategyBuilder 類別增加。如需詳細資訊，請參閱
       Oracle GlassFish Server Add-On Component Development Guide。

       若要設定 HTTP 階段作業的持續性類型，請使用 set 子命令設定
       persistence-type 屬性。例如︰

           asadmin> set c1-config.availability-service.web-container-availability.persistence-type=file

       若要在不啟用可用性的狀況下設定 SFSB 執行處理的持續性類型，請使用 set
       子命令設定 sfsb-persistence-type 屬性。例如︰

           asadmin> set c1-config.availability-service.ejb-container-availability.sfsb-persistence-type=file

       若要在啟用可用性的狀況下設定 SFSB 執行處理的持續性類型，請使用 set
       子命令設定 sfsb-ha-persistence-type 屬性。例如︰

           asadmin> set
           c1-config.availability-service.ejb-container-availability.sfsb-ha-persistence-type=replicated

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --type
           指定要列出持續性類型的階段作業類型。允許的值包括：

           ·   web — 列出 HTTP 階段作業持續性類型。

           ·   ejb — 列出 SFSB 執行處理的持續性類型。

範例
       範例 1, 列出 HTTP 階段作業的持續性類型
           此範例會列出 HTTP 階段作業的持續性類型。

               asadmin> list-persistence-types --type=web
               memory
               file
               replicated

               Command list-persistence-types executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       get(1), set(1), list(1)

       asadmin(1M)

       Oracle GlassFish Server Add-On Component Development Guide

Java EE 7                     2010 年 12 月 15 日    list-persistence-types(1)
