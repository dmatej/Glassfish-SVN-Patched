delete-node-dcom(1)     Subcomandos de la Utilidad asa     delete-node-dcom(1)

NOMBRE DE REFERENCIA
       delete-node-dcom - Suprime un nodo que está activado para la
       comunicación a través de DCOM.

SINOPSIS
           delete-node-dcom [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

DESCRIPCIÓN
       El subcomando delete-node-dcom suprime un nodo que está activado para
       la comunicación a través del protocolo remoto Distributed Component
       Object Model (DCOM) del dominio. El protocolo DCOM sólo está disponible
       para los sistemas Windows. Este subcomando no necesita que esté
       configurado DCOM.

       Este subcomando sólo puede suprimir un nodo que esté activado para la
       comunicación a través de DCOM. El subcomando para suprimir otros tipos
       de nodos depende del tipo de nodo:

       ·   Para suprimir un nodo que no esté activado para la comunicación a
           través de shell seguro (SSH) se debe utilizar el subcomando
           delete-node-ssh(1).

       ·   Para suprimir un nodo que no esté activado para la comunicación se
           debe utilizar el subcomando delete-node-config(1).

       Para determinar si un nodo está activado para la comunicación a través
       de DCOM, utilice el subcomando list-nodes(1).

       En el nodo que se va a suprimir no debe residir ninguna instancia de
       GlassFish Server. De lo contrario, el subcomando falla. Antes de
       ejecutar este subcomando, suprima las instancias que residan en el nodo
       mediante, por ejemplo, el subcomando delete-instance(1) o
       delete-local-instance(1).

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --uninstall
           Especifica si el software GlassFish Server se debe desinstalar del
           host que representa el nodo .

           Los valores posibles son los siguientes:

           false
               El software GlassFish Server no se desinstala del host (por
               defecto).

           true
               El software GlassFish Server se desinstala del host. Por
               defecto, si un nodo, a excepción del nodo predefinido
               localhost-domain, reside en un host del que se va a desinstalar
               el software GlassFish Server, falla el subcomando. Para
               desinstalar el software GlassFish Server de un host en el que
               residen nodos definidos por el usuario, defina la opción
               --force en true. Si la opción --force se define en true, el
               subcomando elimina todo el contenido del principal del
               directorio de instalación base.

       --force
           Si --unistall se define en true, especifica si el subcomando
           desinstala el software GlassFish Server de un host incluso si en el
           host reside un nodo definido por el usuario. Los valores posibles
           son los siguientes:

           false
               Si en el host reside un nodo definido por el usuario, el
               software no se desinstala y el subcomando falla (por defecto).

               Si la opción --force se define en false, el subcomando sólo
               elimina los archivos del software GlassFish Server. No se
               elimina otro contenido del principal del directorio de
               instalación base, como los archivos de configuración.

           true
               El subcomando desinstala el software GlassFish Server del host
               incluso aunque en el host resida un nodo definido por el
               usuario.

               Si la opción --force se define en true, el subcomando elimina
               todo el contenido del principal del directorio de instalación
               base.

OPERANDOS
       node-name
           Nombre del nodo que se va a suprimir. El nodo debe estar activado
           para la comunicación a través de DCOM. De lo contrario, se produce
           un error.

EJEMPLOS
       Ejemplo 1, Supresión de un Nodo Activado para la Comunicación a Través
       de DCOM
           En este ejemplo se suprime el nodo xkyd, que está activado para la
           comunicación a través de DCOM.

               asadmin> delete-node-dcom xkyd
               Command delete-node-dcom executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-node-dcom(1), delete-node-config(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node-dcom(1),
       list-nodes(1), uninstall-node-dcom(1), update-node-dcom(1)

       asadmin(1M)

Java EE 7                   05 de diciembre de 2011        delete-node-dcom(1)
