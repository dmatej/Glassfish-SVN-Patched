ping-node-ssh(1)        Unterbefehle des asadmin-Utili        ping-node-ssh(1)

NAME
       ping-node-ssh - testet, ob ein für die Kommunikation über SSH
       aktivierter Knoten verwendbar ist

ZUSAMMENFASSUNG
           ping-node-ssh [--help]
           [--validate={false|true}] node-name

BESCHREIBUNG
       Der Unterbefehl ping-node-ssh testet, ob ein für die Kommunikation über
       Secure Shell (SSH) aktivierter Knoten verwendbar ist. Für diesen
       Unterbefehl muss Secure Shell (SSH) auf dem Rechner konfiguriert sein,
       auf dem der Domainadministrationsserver (DAS) ausgeführt wird, und auf
       dem Rechner, auf dem sich der Knoten befindet. Sie können diesen Befehl
       über jeden Rechner ausführen, der sich mit dem DAS in Verbindung setzen
       kann.

       Ist der Knoten verwendbar, zeigt der Unterbefehl eine Bestätigung an,
       dass er eine Verbindung zum Knoten über SSH herstellen konnte. Diese
       Bestätigung beinhaltet den Namen des Hosts, den der Knoten darstellt.

       Optional kann der Unterbefehl auch den Knoten validieren, um zu
       ermitteln, ob das Utility asadmin(1M) auf dem Host ausgeführt werden
       kann, den der Knoten darstellt. Zum Validieren eines Knotens führt der
       Unterbefehl den Unterbefehl version(1) aus. Ist der Knoten gültig,
       zeigt der Unterbefehl die Version an, die der Unterbefehl version
       zurückgibt.

       Der Knoten, der als Operand dieses Unterbefehls angegeben ist, muss für
       die Kommunikation über SSH aktiviert sein. Ist der Knoten nicht für die
       Kommunikation über SSH aktiviert, tritt ein Fehler auf. Um zu
       bestimmen, ob ein Knoten für die Kommunikation über SSH aktiviert ist,
       verwenden Sie den Unterbefehl list-nodes(1).

           Note
           ┌─────────────────────────────────────────┐
           │Hinweis                                  │
           │                                         │
           │           Um zu testen, ob ein für die  │
           │           Kommunikation über das        │
           │           Distributed Component Object  │
           │           Model-(DCOM-)Remote-Protokoll │
           │           aktivierter Knoten            │
           │           verwendbar ist, verwenden     │
           │           Sie den Unterbefehl           │
           │           ping-node-dcom(1).            │
           └─────────────────────────────────────────┘

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --validate
           Gibt an, ob der Unterbefehl den Knoten validiert.

           Mögliche Werte:

           true
               Der Knoten wird validiert.

           false
               Der Knoten wird nicht validiert (Standard).

OPERANDEN
       node-name
           Der Name des Knotens, der geprüft werden soll. Der Knoten muss für
           die Kommunikation über SSH aktiviert sein. Andernfalls tritt ein
           Fehler auf.

BEISPIELE
       Example 1, Testen, ob ein SSH-fähiger Knoten verwendbar ist
           In diesem Beispiel wird getestet, ob der SSH-fähige Knoten
           sj03-node verwendbar ist.

               asadmin> ping-node-ssh sj03-node
               Successfully made SSH connection to node sj03-node (sj03.example.com)
               Command ping-node-ssh executed successfully.

       Example 2, SSH-fähigen Knoten validieren
           In diesem Beispiel wird der SSH-fähige Knoten adc-node validiert.

               asadmin> ping-node-ssh --validate=true adc-node
               Successfully made SSH connection to node adcnode (adc.example.com)
               GlassFish version found at /export/glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1 (build 40)
               Command version executed successfully.
               Command ping-node-ssh executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-node-ssh(1), delete-node-ssh(1), list-nodes(1),
       ping-node-dcom(1), setup-ssh(1), update-node-ssh(1), version(1)

       asadmin(1M)

Java EE 7                        8. Dez. 2011                 ping-node-ssh(1)
