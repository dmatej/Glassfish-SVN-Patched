list-configs(1)             asadmin 公用程式子命令             list-configs(1)

參考名
       list-configs - 列出已命名的組態

概要
           list-configs [--help] [target]

描述
       list—configs 子命令會列出網域管理伺服器 (DAS)
       之組態中的指定組態。該清單可依叢集、執行處理或已命名組態進行篩選。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           篩選組態清單。有效的值如下：

           domain
               列出目前網域中的所有已命名組態。

           cluster-name
               列出可定義指定叢集中執行處理之組態的已命名組態。

           instance-name
               列出可定義指定執行處理之組態的已命名組態。

           configuration-name
               列出指定的已命名組態。使用此選項可判定已命名組態是否結束。

範例
       範例 1, 列出網域中的所有已命名組態
           此範例會列出目前網域中所有已命名的組態。

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       copy-config(1), delete-config(1)

       asadmin(1M)

       configuration(5ASC)

Java EE 7                     2010 年 12 月 20 日              list-configs(1)
