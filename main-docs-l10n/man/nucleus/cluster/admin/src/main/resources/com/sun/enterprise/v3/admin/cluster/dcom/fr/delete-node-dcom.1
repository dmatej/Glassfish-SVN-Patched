delete-node-dcom(1)     Sous-commandes de l'utilitaire     delete-node-dcom(1)

NOM
       delete-node-dcom - supprime un noeud activé pour la communication via
       DCOM

SYNTAXE
           delete-node-dcom [--help]
           [--uninstall={false|true}] [--force={false|true}]
           node-name

DESCRIPTION
       La sous-commande delete-node-dcom supprime un noeud activé pour la
       communication via le protocole distant DCOM (Distributed Component
       Object Model) du domaine. Le protocole DCOM est disponible uniquement
       pour les systèmes Windows. Cette sous-commande ne nécessite pas que le
       protocole DCOM soit configuré.

       Cette sous-commande peut uniquement supprimer un noeud activé pour la
       communication via DCOM. La sous-commande permettant de supprimer
       d'autres types de noeud dépend du type du noeud :

       ·   Un noeud activé pour la communication via le protocole SSH (Secure
           Shell) doit être supprimé à l'aide de la sous-commande
           delete-node-ssh(1).

       ·   Un noeud qui n'est pas activé pour la communication doit être
           supprimé à l'aide de la sous-commande delete-node-config(1).

       Pour déterminer si un noeud est activé pour la communication via DCOM,
       utilisez la sous-commande list-nodes(1).

       Aucune instance GlassFish Server ne doit résider sur le noeud à
       supprimer. Sinon, la sous-commande échoue. Avant d'exécuter cette
       sous-commande, supprimez toutes les instances qui résident sur le noeud
       à l'aide, par exemple, de la sous-commande delete-instance(1) ou
       delete-local-instance(1).

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --uninstall
           Indique si le logiciel GlassFish Server est désinstallé de l'hôte
           représenté par le noeud.

           Les valeurs possibles sont les suivantes :

           false
               Le logiciel GlassFish Server n'est pas désinstallé de l'hôte
               (par défaut).

           true
               Le logiciel GlassFish Server est désinstallé de l'hôte. Par
               défaut, si l'un des noeuds, à l'exception du noeud prédéfini
               localhost-domain, réside sur un hôte à partir duquel le
               logiciel GlassFish Server doit être désinstallé, la
               sous-commande échoue. Pour désinstaller le logiciel GlassFish
               Server à partir d'un hôte sur lequel résident des noeuds
               définis par l'utilisateur, définissez l'option --force sur
               true. Si l'option --force a la valeur true, la sous-commande
               enlève l'intégralité du contenu du parent du répertoire
               d'installation de base.

       --force
           Si l'option --uninstall a la valeur True, indique si la
           sous-commande désinstalle le logiciel GlassFish Server à partir
           d'un hôte, même si un noeud défini par l'utilisateur réside sur
           l'hôte. Les valeurs possibles sont les suivantes :

           false
               Si un noeud défini par l'utilisateur réside sur un hôte, le
               logiciel n'est pas désinstallé et la sous-commande échoue (par
               défaut).

               Si l'option --force a la valeur false, la sous-commande enlève
               uniquement les fichiers du logiciel GlassFish Server. Aucun
               autre type de contenu du parent du répertoire d'installation de
               base, comme les fichiers de configuration, n'est enlevé.

           true
               La sous-commande désinstalle le logiciel GlassFish Server de
               l'hôte, même si un noeud défini par l'utilisateur réside sur
               l'hôte.

               Si l'option --force a la valeur true, la sous-commande enlève
               l'intégralité du contenu du parent du répertoire d'installation
               de base.

OPÉRANDES
       node-name
           Nom du noeud à supprimer. Le noeud doit être activé pour la
           communication via DCOM. Sinon, une erreur survient.

EXEMPLES
       Exemple 1, Suppression d'un noeud activé pour la communication via DCOM
           Dans cet exemple, le noeud xkyd, qui est activé pour la
           communication via DCOM, est supprimé.

               asadmin> delete-node-dcom xkyd
               Command delete-node-dcom executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-node-dcom(1), delete-node-config(1), delete-node-ssh(1),
       delete-instance(1), delete-local-instance(1), install-node-dcom(1),
       list-nodes(1), uninstall-node-dcom(1), update-node-dcom(1)

       asadmin(1M)

Java EE 7                        05 déc. 2011              delete-node-dcom(1)
