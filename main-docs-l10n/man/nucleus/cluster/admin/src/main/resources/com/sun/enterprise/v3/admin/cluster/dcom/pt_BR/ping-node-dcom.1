ping-node-dcom(1)       Subcomandos do utilitário asad       ping-node-dcom(1)

NOME
       ping-node-dcom - testa se um nó que está ativado para comunicação pelo
       DCOM pode ser utilizado

SINOPSE
           ping-node-dcom [--help]
           [--validate={false|true}] node-name

DESCRIçãO
       O subcomando ping-node-dcom testa se um nó que está ativado para
       comunicação usando o protocolo remoto DCOM (Distributed Component
       Object Model) pode ser utilizado. O protocolo DCOM só está disponível
       nos sistemas Windows. Este subcomando requer que o DCOM seja
       configurado na máquina em que o servidor de administração de domínios
       (DAS) está em execução e na máquina em que o nó reside. É possível
       executar esse comando a partir de qualquer máquina que possa entrar em
       contato com o DAS.

       Se o nó for utilizável, o subcomando exibirá uma confirmação de que o
       subcomando pôde se conectar com o nó por meio do DCOM. Esta confirmação
       inclui o nome do host que o nó representa.

       Opcionalmente, o subcomando também pode validar o nó para determinar se
       o utilitário asadmin(1M) pode ser executado no host que o nó
       representa. Para validar um nó, o subcomando executa o subcomando
       version(1). Se o nó for válido, o subcomando exibirá a versão que o
       subcomando version retorna.

       O nó que é especificado como o operando deste subcomando precisa ser
       ativado para a comunicação por meio do DCOM. Se o nó não for ativado
       para a comunicação por meio do DCOM, ocorrerá um erro. Para determinar
       se um nó está ativado para comunicação por meio de DCOM, use o
       subcomando list-nodes(1).

           Nota
           ┌───────────────────────────────────────┐
           │Observação                             │
           │                                       │
           │           Para testar se um nó que    │
           │           está ativado para           │
           │           comunicação por meio de     │
           │           shell seguro (SSH) pode ser │
           │           utilizado, use o subcomando │
           │           the ping-node-ssh(1).       │
           └───────────────────────────────────────┘

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --validate
           Especifica se o subcomando valida o nó.

           Os valores possíveis são os seguintes:

           true
               O nó é validado.

           false
               O nó não é validado (default).

OPERANDOS
       node-name
           O nome do nó a ser testado. O nó deve estar ativado para
           comunicação por meio de DCOM. Caso contrário, ocorrerá um erro.

EXEMPLOS
       Example 1, Testando se um Nó Ativado para DCOM Pode Ser Utilizado
           Este exemplo testa se o nó wpmd2 ativado para DCOM é utilizável.

               asadmin> ping-node-dcom wpmdl2
               Successfully made DCOM connection to node wpmdl2 (wpmdl2.example.com)
               Command ping-node-dcom executed successfully.

       Example 2, Validando um Nó Ativado para DCOM
           Este exemplo valida o nó adc-node ativado para DCOM.

               asadmin> ping-node-dcom --validate=true adc-node
               Successfully made DCOM connection to node adc-node (adc-node.example.com)
               GlassFish version found at C:\glassfish3:
               Using locally retrieved version string from version class.
               Version = GlassFish Server Open Source Edition 3.1.2 (build 18)
               Command version executed successfully.
               Command ping-node-dcom executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-node-dcom(1), delete-node-dcom(1), list-nodes(1),
       ping-node-ssh(1), update-node-dcom(1), validate-dcom(1), version(1)

       asadmin(1M)

Java EE 7                         05 dez 2011                ping-node-dcom(1)
