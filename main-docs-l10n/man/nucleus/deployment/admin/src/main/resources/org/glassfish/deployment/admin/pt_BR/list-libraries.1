list-libraries(1)       Subcomandos do utilitário asad       list-libraries(1)

NOME
       list-libraries - lista arquivos JAR da biblioteca no GlassFish Server

SINOPSE
           list-libraries [--help] [--type={common|ext|app}]

DESCRIçãO
       O subcomando list-libraries lista arquivos compactados da biblioteca no
       GlassFish Server.

       A opção --type especifica o tipo de biblioteca e o diretório do
       GlassFish Server para o qual as bibliotecas devem ser listadas.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --type
           Especifica o tipo de biblioteca e o diretório do GlassFish Server
           para o qual as bibliotecas são listadas. Os valores válidos são os
           seguintes:

           common
               Lista os arquivos da biblioteca para o diretório de
               carregadores de classe domain-dir/lib. Esse é o default.

           ext
               Lista os arquivos da biblioteca para o diretório do pacote
               opcional Java, domain-dir/lib/ext.

           app
               Lista os arquivos da biblioteca para o diretório de carreadores
               de classe específico da aplicação, domain-dir/lib/applibs.

           Para obter mais informações sobre esses diretórios, consulte "Class
           Loaders" no Oracle GlassFish Server Application Development Guide.

EXEMPLOS
       Example 1, Listando Bibliotecas
           Este exemplo lista as bibliotecas no diretório de carregadores de
           classe específicas da aplicação na instância do servidor default.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       add-library(1), remove-library(1)

       asadmin(1M)

       "Carregadores de Classe" no Oracle GlassFish Server Application
       Development Guide

Java EE 7                   16 de novembro de 2011           list-libraries(1)
