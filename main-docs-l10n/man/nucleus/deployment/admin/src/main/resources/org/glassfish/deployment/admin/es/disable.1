disable(1)              Subcomandos de la Utilidad asa              disable(1)

NOMBRE DE REFERENCIA
       disable - Desactiva el componente.

SINOPSIS
           disable [--help] [--target target_name] component_name

DESCRIPCIÓN
       El subcomando disable desactiva inmediatamente el complemento
       desplegado especificado. Si el componente no se ha desplegado, se
       devolverá un mensaje de error.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Esta opción especifica el destino en el que se va a desactivar el
           componente. Los valores válidos son:

           server
               Desactiva el componente en la instancia de servidor por defecto
                server y es el valor por defecto.

           domain_name
               Desactiva el componente en el dominio especificado.

           cluster_name
               Desactiva el componente en todas las instancias de servidor del
               cluster.

           instance_name
               Desactiva el componente en una instancia del servidor
               determinada en cluster o independiente.

OPERANDOS
       component_name
           Nombre del componente que se va a desactivar.

           El nombre puede incluir un identificador de versión opcional, que
           va a continuación del nombre y está separado de él por dos puntos
           (:). El identificador de versión debe comenzar por una letra o
           número. Puede contener caracteres alfanuméricos más los caracteres
           de subrayado (_), guión (-) y punto (.). Para desactivar varias
           versiones, puede utilizar un asterisco (*) como carácter comodín.
           Para obtener más información sobre las versiones de módulo y de
           aplicación, consulte el apartado sobre las versiones de módulo y de
           aplicación en Oracle GlassFish Server Application Deployment Guide.

EJEMPLOS
       Ejemplo 1, Desactivación de un Componente
           En este ejemplo se desactiva el componente desplegado sampleApp.

               asadmin> disable sampleApp
               Command disable executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                   17 de noviembre de 2010                 disable(1)
