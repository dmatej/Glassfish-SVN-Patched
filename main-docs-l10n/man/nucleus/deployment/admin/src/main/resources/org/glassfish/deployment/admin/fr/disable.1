disable(1)              Sous-commandes de l'utilitaire              disable(1)

NOM
       disable - désactive le composant

SYNTAXE
           disable [--help] [--target target_name] component_name

DESCRIPTION
       La sous-commande disable désactive immédiatement le composant déployé
       spécifié. Si le composant n'a pas été déployé, un message d'erreur est
       renvoyé.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible sur laquelle désactiver le
           composant. Les valeurs valides sont les suivantes :

           server
               Désactive le composant sur l'instance de serveur par défaut
               (server). Il s'agit de la valeur par défaut.

           domain_name
               Désactive le composant sur le domaine nommé.

           cluster_name
               Désactive le composant sur chaque instance de serveur du
               cluster.

           instance_name
               Désactive le composant sur une instance de serveur autonome ou
               incluse dans un cluster donnée.

OPÉRANDES
       component_name
           Nom du composant à désactiver.

           Le nom peut être suivi d'un identificateur de version facultatif
           dont il est séparé par un signe deux-points (:). L'identificateur
           de version doit commencer par une lettre ou un chiffre. Il peut
           contenir des caractères alphanumériques, des traits de soulignement
           (_), des tirets (-) et des points (.). Pour désactiver plusieurs
           versions, vous pouvez utiliser un astérisque (*) comme caractère
           générique. Pour plus d'informations sur les versions des modules et
           des applications, reportez-vous à Module and Application Versions
           dans le manuel Oracle GlassFish Server Application Deployment
           Guide.

EXEMPLES
       Exemple 1, Désactivation d'un composant
           Dans cet exemple, le composant déployé sampleApp est désactivé.

               asadmin> disable sampleApp
               Command disable executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         17 nov 2010                       disable(1)
