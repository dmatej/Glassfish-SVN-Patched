undeploy(1)           asadminユーティリティのサブコマンド          undeploy(1)

名前
       undeploy - デプロイ済のコンポーネントを除去します

概要
           undeploy [--help] [--target target] [--droptables={true|false}]
           [--cascade={false|true}] name

説明
       undeployサブコマンドは、デプロイ済のアプリケーションまたはモジュールをアンインストールし、リポジトリから除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --cascade
           trueに設定すると、デプロイされていないリソース・アダプタに関連付けられているすべての接続プールおよびコネクタ・リソースが削除されます。falseに設定した場合は、いずれかのプールおよびリソースがリソース・アダプタに関連付けられていると、アンデプロイに失敗します。この場合は、これらのプールおよびリソースを明示的に削除するか、またはこのオプションをtrueに設定する必要があります。このオプションをfalseに設定した場合に、リソース・アダプタに関連付けられているプールおよびリソースが1つもない場合は、そのリソース・アダプタはアンデプロイされます。このオプションは、コネクタ(リソース・アダプタ)およびアプリケーションに適用できます。デフォルト値はfalseです。

       --droptables
           trueに設定すると、デプロイメントの際にアプリケーションによりCMP
           Beansを使用して作成された表が削除されます。falseに設定すると、表は削除されません。指定されない場合は、glassfish-ejb-jar.xmlファイルのcmp-resource要素にあるdrop-tables-at-deployエントリの値によって、表を削除するかを判断します。デフォルト値はtrueです。

       --target
           アンデプロイを行うターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverからコンポーネントをアンデプロイします(デフォルト値)。

           domain
               ドメインからコンポーネントをアンデプロイします。

           cluster_name
               クラスタ内の各サーバー・インスタンスからコンポーネントをアンデプロイします。

           instance_name
               特定のスタンドアロン・サーバー・インスタンスからコンポーネントをアンデプロイします。

オペランド
       name
           デプロイ済コンポーネントの名前。

           名前には、オプションのバージョン識別子を含めることができます。このバージョン識別子は名前の後に続き、コロン(:)で名前と区切ります。バージョン識別子は、文字または数値で始まる必要があります。これには、英数字とアンダースコア(_)、ダッシュ(-)、およびピリオド(.)を含めることができます。複数のバージョンを削除する場合は、アスタリスク(*)をワイルドカード文字として使用できます。モジュールとアプリケーションのバージョンについては、Oracle
           GlassFish
           Serverア

例
       例 1, エンタープライズ・アプリケーションのアンデプロイ
           この例では、Cart.earという名前のエンタープライズ・アプリケーションをアンデプロイします。

               asadmin> undeploy Cart
               Command undeploy executed successfully.

       例 2,
       コンテナ管理による永続性(CMP)を使用するエンタープライズBeanのアンデプロイ
           この例では、myejbという名前のCMP
           Beanをアンデプロイし、対応するデータベース表を削除します。

               asadmin> undeploy --droptables=true myejb
               Command undeploy executed successfully.

       例 3, コネクタ(リソース・アダプタ)のアンデプロイ
           この例では、jdbcraという名前のコネクタ・モジュールをアンデプロイし、カスケード削除を実行して、関連付けられているリソースおよび接続プールを除去します。

               asadmin> undeploy --cascade=true jdbcra
               Command undeploy executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       deploy(1), redeploy(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月17日                     undeploy(1)
