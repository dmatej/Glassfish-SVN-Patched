list-applications(1)  asadminユーティリティのサブコマンド list-applications(1)

名前
       list-applications - デプロイされたアプリケーションをリストします

概要
           list-applications [--help]
           [--long={false|true}] [--resources] [--subcomponents]
           [--type type] [target]

説明
       list-applicationsサブコマンドはデプロイされたJava
       EEアプリケーションと各アプリケーションのタイプをリストします。

       --typeオプションを指定しない場合は、すべてのアプリケーションが表示されます。typeオプションを指定した場合は、タイプを指定する必要があります。指定できるタイプは、このヘルプ・ページの「オプション」の項にリストされています。

       複数のバージョンのモジュールまたはアプリケーションがデプロイされている場合、このサブコマンドはすべてのバージョンをリストします。有効なバージョンをリストするには、--longオプションをtrueに設定します。モジュールとアプリケーションのバージョンについては、Oracle
       GlassFish
       Serverア

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --long
           trueの場合、リストされる各モジュールまたはアプリケーションが有効になっているかどうかが表示されます。デフォルトはfalseです。

       --resources
           各アプリケーションのアプリケーション・スコープのリソースをリストします。--subcomponentsオプションも使用した場合は、アプリケーション内のコンポーネントごとにアプリケーション・スコープのリソースをリストします。

       --subcomponents
           各アプリケーションのサブコンポーネントをリストします。リストされるサブコンポーネントはアプリケーション・タイプによって異なります。たとえば、Java
           EEアプリケーション(EARファイル)では、モジュールがリストされます。Webアプリケーションでは、サーブレットとJSPページがリストされます。EJBモジュールでは、EJBサブコンポーネントがリストされます。

       --type
           リストするアプリケーション・タイプを指定します。オプションは次のとおりです:

           ·   application

           ·   appclient

           ·   connector

           ·   ejb

           ·   web

           ·   webservice

           タイプを指定しない場合は、すべてのアプリケーションがリストされます。

オペランド
       --target
           サブコマンドの操作対象のターゲットの名前です。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverのアプリケーションをリストします(デフォルト値)。

           domain
               ドメインのアプリケーションをリストします。

           cluster_name
               クラスタのアプリケーションをリストします。

           instance_name
               特定のスタンドアロン・サーバー・インスタンスのアプリケーションをリストします。

例
       例 1, Webアプリケーションのリスト

               asadmin> list-applications --type web
               hellojsp <web>
               Command list-applications executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       list-components(1), list-sub-components(1), show-component-status(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月29日            list-applications(1)
