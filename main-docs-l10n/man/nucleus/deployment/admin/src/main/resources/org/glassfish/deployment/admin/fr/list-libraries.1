list-libraries(1)       Sous-commandes de l'utilitaire       list-libraries(1)

NOM
       list-libraries - répertorie les fichiers JAR de bibliothèque sur
       GlassFish Server

SYNTAXE
           list-libraries [--help] [--type={common|ext|app}]

DESCRIPTION
       La sous-commande list-libraries répertorie les fichiers d'archive de
       bibliothèque sur GlassFish Server.

       L'option --type indique le type de bibliothèque et le répertoire
       GlassFish Server pour lequel les bibliothèques doivent être
       répertoriées.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --type
           Indique le type de bibliothèque et le répertoire GlassFish Server
           pour lequel les bibliothèques doivent être répertoriées. Les
           valeurs valides sont les suivantes :

           common
               Répertorie les fichiers de bibliothèque du répertoire du
               chargeur de classe commun, domain-dir/lib. Il s'agit de la
               valeur par défaut.

           ext
               Répertorie les fichiers de bibliothèque du répertoire de
               package Java facultatif, domain-dir/lib/ext.

           app
               Répertorie les fichiers de bibliothèque du répertoire du
               chargeur de classe propre à l'application,
               domain-dir/lib/applibs.

           Pour plus d'informations sur ces répertoires, reportez-vous à Class
           Loaders dans le manuel Oracle GlassFish Server Application
           Development Guide.

EXEMPLES
       Exemple 1, Liste des bibliothèques
           Dans cet exemple, les bibliothèques du répertoire du chargeur de
           classe propre à l'application sont répertoriées pour l'instance de
           serveur par défaut.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       add-library(1), remove-library(1)

       asadmin(1M)

       Class Loaders dans le manuel Oracle GlassFish Server Application
       Development Guide

Java EE 7                         16 nov 2011                list-libraries(1)
