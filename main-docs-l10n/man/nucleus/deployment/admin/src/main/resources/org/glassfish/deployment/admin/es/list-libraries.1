list-libraries(1)       Subcomandos de la Utilidad asa       list-libraries(1)

NOMBRE DE REFERENCIA
       list-libraries - Muestra los archivos JAR de biblioteca en GlassFish
       Server.

SINOPSIS
           list-libraries [--help] [--type={common|ext|app}]

DESCRIPCIÓN
       El subcomando list-libraries muestra los archivos de almacenamiento de
       biblioteca de GlassFish Server.

       La opción --type especifica el tipo de biblioteca y el directorio de
       GlassFish Server para el que deben mostrarse las bibliotecas.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --type
           Especifica el tipo de biblioteca y el directorio de GlassFish
           Server para el que se muestran las bibliotecas. Los valores válidos
           son los siguientes:

           common
               Muestra los archivos de biblioteca para el directorio del
               cargador de clase Común, domain-dir/lib. Éste es el valor por
               defecto.

           ext
               Muestra los archivos de biblioteca para el directorio opcional
               de paquetes de Java, domain-dir/lib/ext.

           app
               Muestra los archivos de biblioteca para el directorio del
               cargador de clases específicas de la aplicación,
               domain-dir/lib/applibs.

           Para obtener más información sobre estos directorios, consulte la
           sección sobre cargadores de clases en Oracle GlassFish Server
           Application Development Guide.

EJEMPLOS
       Ejemplo 1, Lista de Bibliotecas
           En este ejemplo se muestran las bibliotecas del directorio carga de
           clases específico de la aplicación en la instancia de servidor por
           defecto.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       add-library(1), remove-library(1)

       asadmin(1M)

       Sección sobre cargadores de clases en Oracle GlassFish Server
       Application Development Guide

Java EE 7                   16 de noviembre de 2011          list-libraries(1)
