delete-application-ref(1Comandi secondari della utilidelete-application-ref(1)

NOME
       delete-application-ref - rimuove un riferimento a un'applicazione

SINTASSI
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

DESCRIZIONE
       Il comando secondario delete-application-ref rimuove un riferimento a
       un'applicazione da un cluster o da un'istanza server non in cluster. In
       questo modo viene annullata la distribuzione dell'elemento
       applicazione, che non risulta più disponibile nell'istanza o nel
       cluster di destinazione.

       Per la corretta esecuzione di questo comando secondario non è
       necessario che l'istanza o le istanze di destinazione che compongono il
       cluster siano in esecuzione. Se una o più istanze non sono disponibili,
       non caricheranno più l'applicazione all'avvio successivo.

       La rimozione del riferimento non comporta la rimozione
       dell'applicazione dal dominio. I bit vengono rimossi solo con il
       comando secondario undeploy.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione da cui viene rimosso il riferimento
           all'applicazione. Di seguito sono riportati i valori validi.

           ·   server: specifica l'istanza server predefinita come
               destinazione.
                server è il nome dell'istanza server predefinita ed è il
               valore predefinito.

           ·   cluster_name: specifica un cluster particolare come
               destinazione.

           ·   instance_name: specifica un'istanza server standalone
               particolare come destinazione.

       --cascade
           Per un modulo connettore, indica se devono essere eliminate in modo
           ricorsivo anche le risorse che dipendono dal modulo. Il valore
           predefinito è false. Il modulo connettore può essere un file RAR
           standalone o un modulo in un file EAR.

OPERANDI
       reference_name
           Il nome dell'applicazione o del modulo, che può essere un modulo di
           applicazione Java EE, un modulo Web, un modulo EJB, un modulo
           connettore, un modulo client dell'applicazione o un modulo ciclo di
           vita.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per eliminare i
           riferimenti a più versioni, è possibile utilizzare il carattere
           jolly asterisco (*). Per ulteriori informazioni sulle versioni di
           moduli e applicazioni, vedere "Module and Application Versions" in
           Oracle GlassFish Server Application Deployment Guide.

ESEMPI
       Esempio 1, Eliminazione di un riferimento a un'applicazione
           Nell'esempio riportato di seguito viene rimosso un riferimento al
           modulo Web
            MyWebApp dall'istanza server non in cluster NewServer.

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      17 novembre 2010      delete-application-ref(1)
