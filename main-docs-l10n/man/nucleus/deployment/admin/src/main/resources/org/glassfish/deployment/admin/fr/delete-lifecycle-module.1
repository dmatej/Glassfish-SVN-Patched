delete-lifecycle-module(Sous-commandes de l'utilitaidelete-lifecycle-module(1)

NOM
       delete-lifecycle-module - enlève le module de cycle de vie

SYNTAXE
           delete-lifecycle-module [--help] [--target target] module_name

DESCRIPTION
       La sous-commande delete-lifecycle-module enlève un module de cycle de
       vie. Un module de cycle de vie permet d'exécuter une tâche Java de
       courte ou longue durée à un stade bien précis du cycle de vie du
       serveur. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Indique l'emplacement à partir duquel le module de cycle de vie
           doit être supprimé. Les valeurs valides sont les suivantes :

           ·   server : spécifie l'instance de serveur par défaut comme cible
               pour la suppression du module de cycle de vie.  server
               représente le nom de l'instance de serveur par défaut. Il
               s'agit de la valeur par défaut de cette option.

           ·   cluster_name : spécifie un cluster particulier comme cible pour
               la suppression du module de cycle de vie.

           ·   instance_name : spécifie une instance de serveur particulière
               comme cible pour la suppression du module de cycle de vie.

OPÉRANDES
       module_name
           Cet opérande est un identificateur unique pour le module de
           processus d'écoute d'événements de cycle de vie de serveur déployé.

EXEMPLES
       Exemple 1, Suppression d'un module de cycle de vie
           Dans cet exemple, le module de cycle de vie nommé customSetup est
           supprimé.

               asadmin> delete-lifecycle-module customSetup
               Command delete-lifecycle-module executed successfully

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-lifecycle-module(1), list-lifecycle-modules(1)

       asadmin(1M)

Java EE 7                        16 nov. 2010       delete-lifecycle-module(1)
