disable(1)            asadminユーティリティのサブコマンド           disable(1)

名前
       disable - コンポーネントを無効にします

概要
           disable [--help] [--target target_name] component_name

説明
       disableサブコマンドは、指定のデプロイされたコンポーネントをただちに無効にします。コンポーネントがデプロイ済でない場合は、エラー・メッセージが返されます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションでは、コンポーネントを無効にするターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverのコンポーネントを無効にします。これはデフォルト値です。

           domain_name
               指定したドメインのコンポーネントを無効にします。

           cluster_name
               クラスタ内のすべてのサーバーのコンポーネントを無効にします。

           instance_name
               特定のクラスタ化されたサーバー・インスタンスまたはスタンドアロン・サーバー・インスタンスのコンポーネントを無効にします。

オペランド
       component_name
           無効にするコンポーネントの名前。

           名前には、オプションのバージョン識別子を含めることができます。このバージョン識別子は名前の後に続き、コロン(:)で名前と区切ります。バージョン識別子は、文字または数値で始まる必要があります。これには、英数字とアンダースコア(_)、ダッシュ(-)、およびピリオド(.)を含めることができます。複数のバージョンを無効にするには、ワイルドカード文字としてアスタリスク(*)を使用できます。モジュールとアプリケーションのバージョンについては、Oracle
           GlassFish
           Serverア

例
       例 1, コンポーネントの無効化
           この例では、デプロイされたコンポーネントsampleAppを無効にします。

               asadmin> disable sampleApp
               Command disable executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月17日                      disable(1)
