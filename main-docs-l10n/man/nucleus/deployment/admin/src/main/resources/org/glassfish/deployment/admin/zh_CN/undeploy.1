undeploy(1)                 asadmin 实用程序子命令                 undeploy(1)

名称
       undeploy - 删除已部署的组件

提要
           undeploy [--help] [--target target] [--droptables={true|false}]
           [--cascade={false|true}] name

说明
       undeploy 子命令卸载已部署的应用程序或模块，并将其从资料档案库中删除。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --cascade
           如果设置为真
           (资源适配器) 和应用程序。默认值为“假”。

       --droptables
           如果设置为“真”，则删除应用程序在部署期间使用 CMP Bean
           创建的表。如果设置为“假”，则不会删除表。如果未指定，则
           glassfish-ejb-jar.xml 文件的 cmp-resource 元素中的
           drop-tables-at-deploy 条目的值将确定是否删除表。默认值为“真”。

       --target
           指定要从中取消部署的目标。有效值为：

           server
               从默认服务器实例 server 中取消部署组件，且为默认值。

           domain
               从域中取消部署组件。

           cluster_name
               从集群的每个服务器实例中取消部署组件。

           instance_name
               从特定的独立服务器实例中取消部署组件。

操作数
       name
           已部署的组件的名称。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.) 等字符。要删除多个版本，可以使用星号 (*)
           作为通配符。有关模块和应用程序版本的详细信息，请参阅 Oracle
           GlassFish Server Application Deployment Guide 中的 Module and
           Application Versions。

示例
       Example 1, 取消部署企业应用程序
           该示例取消部署名为 Cart.ear 的企业应用程序。

               asadmin> undeploy Cart
               Command undeploy executed successfully.

       Example 2, 取消部署带有容器管理的持久性 (CMP) 的企业 Bean
           该示例取消部署名为 myejb 的 CMP Bean，并删除对应的数据库表。

               asadmin> undeploy --droptables=true myejb
               Command undeploy executed successfully.

       Example 3, 取消部署连接器 (资源适配器)
           该示例取消部署名为 jdbcra
           的连接器模块，执行级联删除，从而删除关联的资源和连接池。

               asadmin> undeploy --cascade=true jdbcra
               Command undeploy executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       deploy(1), redeploy(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日                  undeploy(1)
