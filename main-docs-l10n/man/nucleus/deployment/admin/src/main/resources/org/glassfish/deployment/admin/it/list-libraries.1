list-libraries(1)       Comandi secondari della utilit       list-libraries(1)

NOME
       list-libraries - elenca i file JAR di libreria su GlassFish Server

SINTASSI
           list-libraries [--help] [--type={common|ext|app}]

DESCRIZIONE
       Il comando secondario list-libraries elenca i file di archivio libreria
       su GlassFish Server.

       L'opzione --type specifica il tipo di libreria e la directory di
       GlassFish Server per la quale devono essere elencate le librerie.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --type
           Specifica il tipo di libreria e la directory di GlassFish Server
           per la quale vengono elencate le librerie. Di seguito sono
           riportati i valori validi.

           common
               Elenca i file di libreria per la directory domain-dir/lib del
               classloader Common. Questo è il valore predefinito.

           ext
               Elenca i file di libreria per la directory domain-dir/lib/ext
               del package opzionale Java.

           app
               Elenca i file di libreria per la directory
               domain-dir/lib/applibs del classloader specifico
               dell'applicazione.

           Per ulteriori informazioni su queste directory, vedere "Class
           Loaders" in Oracle GlassFish Server Application Development Guide.

ESEMPI
       Esempio 1, Visualizzazione della lista delle librerie
           In questo esempio vengono elencate le librerie nella directory del
           classloader specifico dell'applicazione sull'istanza server
           predefinita.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       add-library(1), remove-library(1)

       asadmin(1M)

       "Class Loaders" in Oracle GlassFish Server Application Development
       Guide

Java EE 7                      16 novembre 2011              list-libraries(1)
