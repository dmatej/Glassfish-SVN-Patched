list-libraries(1)           asadmin 公用程式子命令           list-libraries(1)

參考名
       list-libraries - 列出 GlassFish Server 上的程式庫 JAR 檔案

概要
           list-libraries [--help] [--type={common|ext|app}]

描述
       list-libraries 子命令會列出 GlassFish Server 上的程式庫存檔。

       --type 選項會指定程式庫類型以及程式庫未列出的 GlassFish Server 目錄。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --type
           指定程式庫類型以及列出程式庫的 GlassFish Server
           目錄。有效的值如下：

           common
               列出 Common 類別載入器目錄 domain-dir/lib
               的程式庫檔案。此為預設值。

           ext
               列出 Java 選用套裝軟體目錄 domain-dir/lib/ext 的程式庫檔案。

           app
               列出應用程式特定的類別載入器目錄 domain-dir/lib/applibs
               的程式庫檔案。

           如需有關這些目錄的詳細資訊，請參閱 Oracle GlassFish Server
           Application Development Guide 中的 Class Loaders。

範例
       範例 1, 列出程式庫
           此範例會列出預設伺服器執行處理上，應用程式特定的類別載入器目錄中的程式庫。

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       add-library(1), remove-library(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide 中的Class Loaders

Java EE 7                     2011 年 11 月 16 日            list-libraries(1)
