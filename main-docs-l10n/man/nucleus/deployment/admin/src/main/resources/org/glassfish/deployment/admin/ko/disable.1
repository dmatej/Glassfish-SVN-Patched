disable(1)                asadmin 유틸리티 하위 명령                disable(1)

제목
       disable - 구성 요소를 사용 안함으로 설정

시놉시스
           disable [--help] [--target target_name] component_name

설명
       disable 하위 명령은 지정한 배치된 구성 요소를 바로 사용 안함으로
       설정합니다. 구성 요소가 배치되어 있지 않은 경우에는 오류 메시지가
       반환됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 구성 요소를 사용 안함으로 설정할 대상을 지정합니다.
           적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스 server에서 구성 요소를 사용 안함으로
               설정하며 기본값입니다.

           domain_name
               명명된 도메인에서 구성 요소를 사용 안함으로 설정합니다.

           cluster_name
               클러스터의 모든 서버 인스턴스에서 구성 요소를 사용 안함으로
               설정합니다.

           instance_name
               특정 클러스터된 또는 독립형 서버 인스턴스에서 구성 요소를 사용
               안함으로 설정합니다.

피연산자
       component_name
           사용 안함으로 설정할 구성 요소의 이름입니다.

           이름에는 버전 식별자(옵션)가 포함될 수 있으며, 이 식별자는 이름
           다음에 오고 콜론(:)으로 이름과 구분됩니다. 버전 식별자는 문자 또는
           숫자로 시작해야 합니다. 영숫자 문자와 밑줄(_), 대시(-) 및 마침표(.)
           문자가 사용될 수 있습니다. 여러 버전을 사용 안함으로 설정하려면
           별표(*)를 와일드 카드 문자로 사용할 수 있습니다. 모듈 및 응용
           프로그램 버전에 대한 자세한 내용은 Oracle GlassFish Server
           Application Deployment Guide의 "Module and Application Versions"를
           참조하십시오.

예제
       예 1, 구성 요소 사용 안함
           이 예제는 배치된 구성 요소 sampleApp를 사용 안함으로 설정합니다.

               asadmin> disable sampleApp
               Command disable executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      2010년 11월 17일                     disable(1)
