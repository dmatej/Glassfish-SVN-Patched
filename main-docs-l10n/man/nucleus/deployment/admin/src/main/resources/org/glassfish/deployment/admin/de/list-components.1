list-components(1)      Unterbefehle des asadmin-Utili      list-components(1)

NAME
       list-components - listet bereitgestellte Komponenten auf

ZUSAMMENFASSUNG
           list-components [--help]
           [--long={false|true}] [--resources] [--subcomponents]
           [--type type] [target]

BESCHREIBUNG
           Anmerkung
           ┌───────────────────────────────────────┐
           │Hinweis                                │
           │                                       │
           │           Der Unterbefehl             │
           │           list-components ist         │
           │           veraltet. Verwenden Sie     │
           │           stattdessen den Unterbefehl │
           │           list-applications.          │
           └───────────────────────────────────────┘

       Der Unterbefehl list-components listet alle bereitgestellten Java
       EE-Komponenten auf.

       Ist die Option --type nicht angegeben, werden alle Komponenten
       aufgelistet. Ist die Option "--type" angegeben, müssen Sie einen Typ
       angeben. Die verfügbaren Typen sind im Abschnitt "Optionen" dieser
       Hilfeseite aufgelistet.

       Wenn mehrere Versionen eines Moduls oder einer Anwendung bereitgestellt
       sind, listet dieser Unterbefehl alle Versionen auf. Um aufzulisten,
       welche Versionen aktiviert sind, setzen Sie die Option --long auf true.
       Weitere Informationen zu Modul- und Anwendungsversionen finden Sie
       unter "Module and Application Versions" im Oracle GlassFish Server
       Application Deployment Guide.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --long
           Wenn dies true ist, wird angezeigt, ob ein Modul oder eine
           Anwendung aktiviert ist. Der Standardwert ist false.

       --resources
           Listet die anwendungsspezifischen Ressourcen für jede Komponente
           auf. Wenn die Option --subcomponents ebenfalls verwendet wird,
           werden anwendungsspezifische Ressourcen für jede Komponente
           innerhalb einer Anwendung aufgelistet.

       --subcomponents
           Listet die Unterkomponenten jeder Komponente auf. Welche
           Unterkomponenten aufgelistet werden, hängt vom Komponententyp ab.
           Beispielsweise werden bei einer Java EE-Anwendung (EAR-Datei)
           Module aufgelistet. Bei einer Webanwendung werden Servlets und
           JSP-Seiten aufgelistet. Bei einem EJB-Modul werden
           EJB-Unterkomponenten aufgelistet.

       --type
           Gibt den Typ der Komponenten an, die aufgelistet werden sollen.
           Folgende Optionen stehen zur Auswahl:

           ·   application

           ·   appclient

           ·   connector

           ·   ejb

           ·   web

           ·   webservice

           Ist kein Typ angegeben, werden alle Komponenten aufgelistet.

OPERANDEN
       target
           Dies ist der Name des Ziels, dem der Unterbefehl gilt. Gültige
           Werte sind:

           server
               Listet die Komponenten für die standardmäßige Serverinstanz
               server auf und ist der Standardwert.

           domain
               Listet die Komponenten für die Domain auf.

           cluster_name
               Listet die Komponenten für das Cluster auf.

           instance_name
               Listet die Komponenten für eine bestimmte
               Standalone-Serverinstanz auf.

BEISPIELE
       Beispiel 1, Komponenten auflisten
           In diesem Beispiel werden die Connector-Komponenten aufgelistet.
           (cciblackbox-tx.rar wurde bereitgestellt.)

               asadmin> list-components --type connector
               cciblackbox-tx <connector>
               Command list-components executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       list-applications(1), show-component-status(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        29. Nov. 2010              list-components(1)
