remove-library(1)       Comandi secondari della utilit       remove-library(1)

NOME
       remove-library - rimuove uno o più file JAR di libreria da GlassFish
       Server

SINTASSI
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

DESCRIZIONE
       Il comando secondario remove-library rimuove uno o più file JAR di
       libreria da GlassFish Server.

       L'opzione --type specifica il tipo della libreria e la directory di
       GlassFish Server dalla quale viene rimossa la libreria.

       L'operando library-name è il nome del file JAR che contiene la libreria
       da rimuovere. Per specificare più librerie, fornire più nomi separati
       da spazi.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Il file di archivio della    │
           │           libreria viene rimosso dal   │
           │           server DAS. Per le librerie  │
           │           comuni e di estensioni, è    │
           │           necessario riavviare il      │
           │           server DAS in modo che le    │
           │           rimozioni delle librerie     │
           │           vengano rilevate dal runtime │
           │           del server. Per rimuovere le │
           │           librerie da altre istanze    │
           │           server, sincronizzare queste │
           │           ultime con il server DAS     │
           │           eseguendone il riavvio.      │
           │                                        │
           │           Questo comando non è         │
           │           supportato sui sistemi       │
           │           operativi Windows.           │
           └────────────────────────────────────────┘

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --type
           Specifica il tipo della libreria e la directory di GlassFish Server
           dalla quale viene rimossa la libreria. Di seguito sono riportati i
           valori validi.

           common
               Rimuove i file di libreria dalla directory domain-dir/lib del
               classloader Common. Questo è il valore predefinito.

           ext
               Rimuove i file di libreria dalla directory domain-dir/lib/ext
               del package opzionale Java.

           app
               Rimuove i file di libreria dalla directory
               domain-dir/lib/applibs del classloader specifico
               dell'applicazione.

           Per ulteriori informazioni su queste directory, vedere "Class
           Loaders" in Oracle GlassFish Server Application Development Guide.

OPERANDI
       library-name
           I nomi dei file JAR che contengono le librerie da rimuovere.

ESEMPI
       Esempio 1, Rimozione delle librerie
           In questo esempio la libreria nel file di archivio mylib.jar viene
           rimossa dalla directory del classloader specifico dell'applicazione
           sull'istanza server predefinita.

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       add-library(1), list-libraries(1)

       asadmin(1M)

       "Class Loaders" in Oracle GlassFish Server Application Development
       Guide

Java EE 7                      16 novembre 2011              remove-library(1)
