list-applications(1)        asadmin 实用程序子命令        list-applications(1)

名称
       list-applications - 列出已部署应用程序

提要
           list-applications [--help]
           [--long={false|true}] [--resources] [--subcomponents]
           [--type type] [target]

说明
       list-applications 子命令列出部署的 Java EE
       应用程序和所列出的每个应用程序的类型。

       如果未指定 --type 选项，则将列出所有应用程序。如果指定了 type
       选项，还必须指定一种类型。本帮助页的“选项”部分中列出了可能的类型。

       如果部署了模块或应用程序的多个版本，该子命令将列出所有版本。要列出启用了哪个版本，请将
       --long 选项设置为真
       GlassFish Server Application Deployment Guide 中的 Module and
       Application Versions。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --long
           如果为真

       --resources
           为每个应用程序列出应用程序范围的资源。如果还使用了 --subcomponents
           选项，则将为应用程序中的每个组件列出应用程序范围的资源。

       --subcomponents
           列出每个应用程序的子组件。列出的子组件取决于应用程序类型。例如，对于
           Java EE 应用程序 (EAR 文件)，将列出模块。对于 Web 应用程序，将列出
           servlet 和 JSP 页。对于 EJB 模块，将列出 EJB 子组件。

       --type
           指定要列出的应用程序的类型。这些选项如下所示：

           ·   application

           ·   appclient

           ·   connector

           ·   ejb

           ·   web

           ·   webservice

           如果未指定类型，则列出所有应用程序。

操作数
       --target
           这是子命令要执行操作的目标的名称。有效值如下所示：

           server
               列出默认服务器实例 server 的应用程序，这是默认值。

           domain
               列出域的应用程序。

           cluster_name
               列出集群的应用程序。

           instance_name
               列出特定独立服务器实例的应用程序。

示例
       Example 1, 列出 Web 应用程序

               asadmin> list-applications --type web
               hellojsp <web>
               Command list-applications executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-components(1), list-sub-components(1), show-component-status(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 29 日         list-applications(1)
