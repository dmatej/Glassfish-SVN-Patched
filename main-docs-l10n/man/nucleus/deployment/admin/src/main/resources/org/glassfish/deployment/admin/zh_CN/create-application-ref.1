create-application-ref(1)   asadmin 实用程序子命令   create-application-ref(1)

名称
       create-application-ref - 创建对应用程序的引用

提要
           create-application-ref [--help] [--target target]
           [--virtualservers virtual_servers] [--enabled=true]
           [--lbenabled=true] reference_name

说明
       create-application-ref
       子命令创建从集群或非集群服务器实例到之前部署的应用程序元素 (例如，Java
       EE 应用程序、Web 模块或企业 Bean 模块)
       的引用。这将有效地使应用程序元素得以部署并使其在目标实例或集群中可用。

       无需为了成功运行此子命令而运行或提供构成集群的目标实例。如果一个或多个实例不可用，下次启动它们时将收到新的应用程序元素。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要创建应用程序引用的目标。有效值为

           ·   server - 将默认服务器实例指定为应用程序引用的创建目标。server
               为默认服务器实例的名称，并且是该选项的默认值。

           ·   cluster_name - 将特定集群指定为应用程序引用的创建目标。

           ·   instance_name -
               将特定的独立服务器实例指定为应用程序引用的创建目标。

       --virtualservers
           指定要进行部署的虚拟服务器 ID 的逗号分隔列表。该选项仅适用于 Web
           模块 (独立情况下或在 Java EE
           应用程序中)。如果未指定该选项，则应用程序将部署到除管理服务器
           __asadmin 以外的所有虚拟服务器上。

       --enabled
           表明是否应启用 (即加载)
           应用程序。只有在全局级别启用应用程序，该值才会生效。默认值为真

       --lbenabled
           控制已部署的应用程序是否可用于负载平衡。默认值为“真”。

操作数
       reference_name
           应用程序或模块的名称，可以是 Java EE 应用程序、Web 模块、EJB
           模块、连接器模块、应用程序客户机模块或生命周期模块。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.) 等字符。如果 --enabled
           选项设置为“假”，可通过将星号 (*)
           用作通配符来创建对多个禁用版本的引用。有关模块和应用程序版本的详细信息，请参阅
           Oracle GlassFish Server Application Deployment Guide 中的 Module
           and Application Versions。

示例
       Example 1, 创建应用程序引用
           以下示例创建对非集群服务器实例 NewServer 上 Web 模块 MyWebApp
           的引用。

               asadmin> create-application-ref --target NewServer MyWebApp
               Command create-application-ref executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-application-ref(1), list-application-refs(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日    create-application-ref(1)
