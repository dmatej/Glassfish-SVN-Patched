disable(1)              Comandi secondari della utilit              disable(1)

NOME
       disable - disabilita il componente

SINTASSI
           disable [--help] [--target target_name] component_name

DESCRIZIONE
       Il comando secondario disable disabilita immediatamente il componente
       distribuito specificato. Se il componente non è stato distribuito,
       viene restituito un messaggio di errore.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica la destinazione in cui viene disabilitato
           il componente. Di seguito sono riportati i valori validi.

           server
               Disabilita il componente nell'istanza server predefinita server
               ed è il valore predefinito.

           domain_name
               Disabilita il componente nel dominio specificato.

           cluster_name
               Disabilita il componente in tutte le istanze server del
               cluster.

           instance_name
               Disabilita il componente in un'istanza server standalone o in
               cluster specifica.

OPERANDI
       component_name
           Il nome del componente da disabilitare.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per disabilitare più
           versioni, è possibile utilizzare un asterisco (*) come carattere
           jolly. Per ulteriori informazioni sulle versioni di moduli e
           applicazioni, vedere "Module and Application Versions" in Oracle
           GlassFish Server Application Deployment Guide.

ESEMPI
       Esempio 1, Disabilitazione di un componente
           In questo esempio viene disabilitato il componente distribuito
           sampleApp.

               asadmin> disable sampleApp
               Command disable executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      17 novembre 2010                     disable(1)
