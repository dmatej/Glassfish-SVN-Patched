remove-library(1)         asadmin 유틸리티 하위 명령         remove-library(1)

제목
       remove-library - GlassFish Server에서 하나 이상의 라이브러리 JAR 파일
       제거

시놉시스
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

설명
       remove-library 하위 명령은 GlassFish Server에서 하나 이상의 라이브러리
       JAR 파일을 제거합니다.

       --type 옵션은 라이브러리 유형 및 라이브러리를 제거할 GlassFish Server
       디렉토리를 지정합니다.

       library-name 피연산자는 제거할 라이브러리를 포함하는 JAR 파일의
       이름입니다. 여러 라이브러리를 지정하려면 공백으로 이름을 구분하여
       지정합니다.

           참고
           ┌───────────────────────────────────────┐
           │주                                     │
           │                                       │
           │           라이브러리 아카이브 파일은  │
           │           DAS에서 제거됩니다. 공통 및 │
           │           확장 라이브러리의 경우 서버 │
           │           런타임에 라이브러리 제거가  │
           │           선택되도록 DAS를 재시작해야 │
           │           합니다. 다른 서버           │
           │           인스턴스에서 라이브러리를   │
           │           제거하려면 해당 인스턴스를  │
           │           재시작하여 DAS와            │
           │           동기화하십시오.             │
           │                                       │
           │           이 명령은 Windows 운영      │
           │           체제에서 지원되지 않습니다. │
           └───────────────────────────────────────┘

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --type
           라이브러리 유형 및 라이브러리를 제거할 GlassFish Server 디렉토리를
           지정합니다. 적합한 값은 다음과 같습니다.

           common
               공통 클래스 로더 디렉토리인 domain-dir/lib에서 라이브러리
               파일을 제거합니다. 이 값은 기본값입니다.

           ext
               Java 선택적 패키지 디렉토리인 domain-dir/lib/ext에서 라이브러리
               파일을 제거합니다.

           app
               응용 프로그램별 클래스 로더 디렉토리인
               domain-dir/lib/applibs에서 라이브러리 파일을 제거합니다.

           이러한 디렉토리에 대한 자세한 내용은 Oracle GlassFish Server
           Application Development Guide의 "Class Loaders"를 참조하십시오.

피연산자
       library-name
           제거할 라이브러리를 포함하는 JAR 파일의 이름입니다.

예제
       예 1, 라이브러리 제거
           이 예제는 기본 서버 인스턴스의 응용 프로그램별 클래스 로더
           디렉토리에서 아카이브 파일 mylib.jar의 라이브러리를 제거합니다.

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       add-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide의 "Class Loaders"

Java EE 7                      2011년 11월 16일              remove-library(1)
