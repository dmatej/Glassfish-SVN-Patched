delete-application-ref(1Unterbefehle des asadmin-Utildelete-application-ref(1)

NAME
       delete-application-ref - entfernt eine Referenz auf eine Anwendung

ZUSAMMENFASSUNG
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

BESCHREIBUNG
       Der Unterbefehl delete-application-ref entfernt eine Referenz von einem
       Cluster oder einer nicht geclusterten Serverinstanz auf eine Anwendung.
       Dadurch wird das Deployment des Anwendungselements aufgehoben, das
       damit nicht mehr auf der Zielinstanz oder im Cluster zur Verfügung
       steht.

       Die Zielinstanz oder die Instanzen, aus denen das Cluster besteht,
       müssen nicht ausgeführt werden oder verfügbar sein, damit dieser
       Unterbefehl erfolgreich ausgeführt werden kann. Wenn eine oder mehrere
       Instanzen nicht verfügbar sind, laden sie nicht mehr die Anwendung,
       wenn sie das nächste Mal gestartet werden.

       Durch das Entfernen der Referenz wird die Anwendung nicht aus der
       Domain entfernt. Die Bits werden nur durch den Unterbefehl undeploy
       entfernt.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie die Anwendungsreferenz entfernen.
           Gültige Werte sind

           ·   server: Gibt die standardmäßige Serverinstanz als Ziel an.
               server ist der Name der standardmäßigen Serverinstanz und der
               Standardwert.

           ·   cluster_name: Gibt ein bestimmtes Cluster als Ziel an.

           ·   instance_name: Gibt eine bestimmte Standalone-Serverinstanz als
               Ziel an.

       --cascade
           Zeigt bei einem Connector-Modul an, ob die vom Modul abhängigen
           Ressourcen ebenfalls rekursiv gelöscht werden sollen. Der
           Standardwert ist false. Das Connector-Modul kann entweder eine
           Standalone-RAR-Datei oder ein Modul innerhalb einer EAR-Datei sein.

OPERANDEN
       reference_name
           Der Name der Anwendung oder des Moduls. Hierbei kann es sich um ein
           Java EE-Anwendungsmodul, ein Webmodul, ein EJB-Modul, ein
           Connector-Modul, ein Anwendungsclientmodul oder ein
           Lebenszyklusmodul handeln.

           Der Name kann eine optionale Versions-ID enthalten, die dem Namen
           folgt und von diesem durch einen Doppelpunkt (:) getrennt ist. Die
           Versions-ID muss mit einem Buchstaben oder einer Zahl beginnen. Er
           kann alphanumerische Zeichen sowie Unterstriche (_), Bindestriche
           (-) und Punkte (.) enthalten. Um Verweise auf mehrere Versionen zu
           löschen, können Sie ein Sternchen (*) als Platzhalterzeichen
           verwenden. Weitere Informationen zu Modul- und Anwendungsversionen
           finden Sie unter "Module and Application Versions" im Oracle
           GlassFish Server Application Deployment Guide.

BEISPIELE
       Beispiel 1, Anwendungsreferenzen löschen
           Im folgenden Beispiel wird eine Referenz zum Webmodul MyWebApp aus
           der nicht geclusterten Serverinstanz NewServer entfernt.

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        17. Nov. 2010       delete-application-ref(1)
