disable(1)                  asadmin 实用程序子命令                  disable(1)

名称
       disable - 禁用组件

提要
           disable [--help] [--target target_name] component_name

说明
       disable
       子命令立即禁用指定的已部署组件。如果尚未部署相应组件，则会返回错误消息。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要禁用组件的目标。有效值为：

           server
               禁用默认服务器实例 server 上的组件，这是默认值。

           domain_name
               禁用命名域中的组件。

           cluster_name
               禁用集群中每个服务器实例上的组件。

           instance_name
               禁用特定集群服务器实例或独立服务器实例上的组件。

操作数
       component_name
           要禁用的组件的名称。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.) 等字符。要禁用多个版本，可以使用星号 (*)
           作为通配符。有关模块和应用程序版本的详细信息，请参阅 Oracle
           GlassFish Server Application Deployment Guide 中的 Module and
           Application Versions。

示例
       Example 1, 禁用组件
           此示例禁用已部署的组件 sampleApp。

               asadmin> disable sampleApp
               Command disable executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日                   disable(1)
