delete-application-refasadminユーティリティのサブコマdelete-application-ref(1)

名前
       delete-application-ref - アプリケーションへの参照を除去します

概要
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

説明
       delete-application-refサブコマンドは、クラスタまたはクラスタ化されていないサーバー・インスタンスからアプリケーションへの参照を除去します。これによって、アプリケーション要素がアンデプロイされて、ターゲットのインスタンスやクラスタで使用できなくなります。

       このサブコマンドを正常に実行するために、クラスタを構成しているターゲットのインスタンスが実行中または使用可能である必要はありません。1つ以上のインスタンスが使用できない場合は、次にインスタンスを起動したときにアプリケーションはロードされません。

       参照を除去しても、アプリケーションはドメインから削除されません。ビットは、undeployサブコマンドでのみ除去されます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           アプリケーション参照を除去するターゲットを指定します。有効な値は次のとおりです

           ·   server-
               デフォルトのサーバー・インスタンスをターゲットとして指定します。serverは、デフォルトのサーバー・インスタンスの名前で、デフォルト値です。

           ·   cluster_name- 特定のクラスタをターゲットとして指定します。

           ·   iinstance_name-
               特定のスタンドアロン・サーバー・インスタンスをターゲットとして指定します。

       --cascade
           コネクタ・モジュールについて、モジュールに依存するリソースも再帰的に削除するかを指定します。デフォルトはfalseです。コネクタ・モジュールは、スタンドアロンのRARファイルまたはEARファイル内のモジュールにすることができます。

オペランド
       reference_name
           アプリケーションまたはモジュールの名前。Java
           EEアプリケーション・モジュール、Webモジュール、EJBモジュール、コネクタ・モジュール、アプリケーション・クライアント・モジュール、ライフサイクル・モジュールのいずれかを指定できます。

           名前には、オプションのバージョン識別子を含めることができます。このバージョン識別子は名前の後に続き、コロン(:)で名前と区切ります。バージョン識別子は、文字または数値で始まる必要があります。これには、英数字とアンダースコア(_)、ダッシュ(-)、およびピリオド(.)を含めることができます。複数のバージョンへの参照を削除するには、ワイルドカード文字としてアスタリスク(*)を使用できます。モジュールとアプリケーションのバージョンについては、Oracle
           GlassFish
           Serverア

例
       例 1, アプリケーション参照の削除
           次の例では、クラスタ化されていないサーバー・インスタンスNewServer上のWebモジュールMyWebAppへの参照を除去します。

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月17日       delete-application-ref(1)
