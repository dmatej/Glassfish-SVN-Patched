enable(1)             asadminユーティリティのサブコマンド            enable(1)

名前
       enable - コンポーネントを有効にします

概要
           enable [--help] [--target target_name] component_name

説明
       enableサブコマンドは、指定のデプロイされたコンポーネントを有効にします。コンポーネントがすでに有効になっている場合は、再有効化されます。コンポーネントがデプロイ済でない場合は、エラー・メッセージが返されます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションでは、コンポーネントを有効にするターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverを有効にします。これはデフォルト値です。

           domain_name
               指定したドメインを有効にします。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスを有効にします。

           instance_name
               特定のクラスタ化されたサーバー・インスタンスまたはスタンドアロン・サーバー・インスタンスを有効にします。

オペランド
       component_name
           有効にするコンポーネントの名前。

           名前には、オプションのバージョン識別子を含めることができます。このバージョン識別子は名前の後に続き、コロン(:)で名前と区切ります。バージョン識別子は、文字または数値で始まる必要があります。これには、英数字とアンダースコア(_)、ダッシュ(-)、およびピリオド(.)を含めることができます。モジュールとアプリケーションのバージョンについては、Oracle
           GlassFish
           Serverア

           サーバー・インスタンスで、モジュールまたはアプリケーションの1つのバージョンのみを有効にできます。その他のすべてのバージョンは無効になります。1つのバージョンを有効にすると、他のすべてのバージョンは自動的に無効になります。

例
       例 1, コンポーネントの有効化
           この例では、無効化されたコンポーネントsampleAppを有効にします。

               asadmin> enable sampleApp
               Command enable executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       deploy(1), , undeploy(1), disable(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月17日                       enable(1)
