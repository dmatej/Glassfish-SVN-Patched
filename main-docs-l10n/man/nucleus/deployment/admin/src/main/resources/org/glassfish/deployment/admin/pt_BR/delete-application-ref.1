delete-application-ref(1Subcomandos do utilitário asadelete-application-ref(1)

NOME
       delete-application-ref - remove uma referência de uma aplicação

SINOPSE
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

DESCRIçãO
       O subcomando delete-application-ref remove uma referência de uma
       instância do servidor clusterizado ou não clusterizado para uma
       aplicação. Isso efetivamente resulta na desimplantação do elemento da
       aplicação, que deixa de estar disponível na instância ou cluster do
       alvo.

       A instância ou instâncias do alvo que compõem o cluster não precisam
       estar em execução ou disponíveis para que esse subcomando seja bem
       sucedido. Se uma ou mais instâncias não estiverem disponíveis, elas não
       carregarão a aplicação na próxima vez que forem iniciadas.

       A remoção da referência não resulta na remoção da aplicação do domínio.
       Os bits são removidos apenas através do subcomando undeploy.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo do qual a referência da aplicação está sendo
           removida. Os valores válidos são

           ·   server- Especifica a instância do servidor default como o alvo.
                server é o nome da instância de servidor default e é o valor
               default.

           ·   cluster_name - Especifica um certo cluster como o alvo.

           ·   instance_name- Especifica uma determinada instância do servidor
               standalone como o alvo.

       --cascade
           Para um módulo do conector, indica se os recursos dependentes do
           módulo também devem ser deletados recursivamente. O default é
           false. O módulo do conector pode ser um arquivo RAR independente ou
           um módulo em um arquivo EAR.

OPERANDOS
       reference_name
           O nome da aplicação ou do módulo, que pode ser um módulo da
           aplicação Java EE, do módulo Web, do módulo EJB, do módulo
           conector, módulo cliente da aplicação ou do módulo de ciclo de
           vida.

           O nome pode incluir um identificador de versão opcional, que segue
           o nome e é separado do nome por dois-pontos (:). O identificador de
           versão deve começar com uma letra ou número. Pode conter caracteres
           alfanuméricos e sublinhado (_), traço (-), e ponto (.). Para
           deletar referências em várias versões, é possível utilizar um
           asterisco (*) como caractere curinga. Para obter mais informações
           sobre versões de módulo e da aplicação, consulte "Module and
           Application Versions" no Oracle GlassFish Server Application
           Deployment Guide.

EXEMPLOS
       Example 1, Deletando uma Referência da Aplicação
           O exemplo a seguir remove uma referência ao módulo Web
            MyWebApp da instância de servidor não clusterizado NewServer .

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                   17 de novembro de 2010   delete-application-ref(1)
