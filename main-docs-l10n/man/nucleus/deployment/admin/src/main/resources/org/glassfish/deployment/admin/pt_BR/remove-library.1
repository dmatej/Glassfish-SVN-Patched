remove-library(1)       Subcomandos do utilitário asad       remove-library(1)

NOME
       remove-library - remove um ou mais arquivos JAR da biblioteca do
       GlassFish Server

SINOPSE
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

DESCRIçãO
       O subcomando remove-library remove um ou mais arquivos JAR da
       biblioteca do GlassFish Server.

       A opção --type especifica o tipo de biblioteca e o diretório do
       GlassFish Server do qual a biblioteca é removida.

       O operando library-name corresponde ao nome do arquivo JAR que contém a
       biblioteca a ser removida. Para especificar várias bibliotecas,
       especifique vários nomes separados por espaços.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           O arquivo compactado da      │
           │           biblioteca é removido do     │
           │           DAS. Em bibliotecas common e │
           │           de extensões, é necessário   │
           │           reiniciar o DAS para que as  │
           │           remoções de bibliotecas      │
           │           sejam selecionadas pelo      │
           │           runtime do servidor. Para    │
           │           remover as bibliotecas de    │
           │           outras instâncias do         │
           │           servidor, sincronize as      │
           │           instâncias com o DAS         │
           │           reiniciando-as.              │
           │                                        │
           │           Este comando não é suportado │
           │           no sistema operacional       │
           │           Windows.                     │
           └────────────────────────────────────────┘

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --type
           Especifica o tipo da biblioteca e o diretório do GlassFish Server
           do qual a biblioteca é removida. Os valores válidos são os
           seguintes:

           common
               Remove os arquivos da biblioteca do diretório de carregadores
               de classe Common, domain-dir/lib. Esse é o default.

           ext
               Remove os arquivos da biblioteca do diretório do pacote
               opcional Java, domain-dir/lib/ext.

           app
               Remove os arquivos da biblioteca do diretório de carregadores
               de classe específico da aplicação, domain-dir/lib/applibs.

           Para obter mais informações sobre esses diretórios, consulte "Class
           Loaders" no Oracle GlassFish Server Application Development Guide.

OPERANDOS
       library-name
           Os nomes dos arquivos JAR files que contêm as bibliotecas que serão
           removidas.

EXEMPLOS
       Example 1, Removendo Bibliotecas
           Este exemplo remove a biblioteca do arquivo compactado mylib.jar do
           diretório de carregadores de classe específicas da aplicação na
           instância do servidor default.

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       add-library(1), list-libraries(1)

       asadmin(1M)

       "Carregadores de Classe" no Oracle GlassFish Server Application
       Development Guide

Java EE 7                   16 de novembro de 2011           remove-library(1)
