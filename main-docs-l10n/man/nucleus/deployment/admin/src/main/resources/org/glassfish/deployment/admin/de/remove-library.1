remove-library(1)       Unterbefehle des asadmin-Utili       remove-library(1)

NAME
       remove-library - entfernt eine oder mehrere Library-JAR-Dateien aus
       GlassFish Server

ZUSAMMENFASSUNG
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

BESCHREIBUNG
       Der Unterbefehl remove-library entfernt eine oder mehrere
       Library-JAR-Dateien aus GlassFish Server.

       Die Option --type gibt den Library-Typ und das GlassFish
       Server-Verzeichnis an, aus dem die Library entfernt wird.

       Der Operand library-name ist der Name der JAR-Datei, die die zu
       entfernende Library enthält. Um mehrere Librarys anzugeben, geben Sie
       mehrere Namen durch Leerzeichen getrennt ein.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Die Library-Archivdatei wird │
           │           aus dem DAS entfernt. Bei    │
           │           allgemeinen und              │
           │           Erweiterungs-Librarys müssen │
           │           Sie den DAS neu starten,     │
           │           damit die Entfernung der     │
           │           Librarys von der             │
           │           Serverlaufzeit               │
           │           berücksichtigt wird. Um die  │
           │           Librarys von anderen         │
           │           Serverinstanzen zu           │
           │           entfernen, synchronisieren   │
           │           Sie die Instanzen mit dem    │
           │           DAS, indem Sie sie neu       │
           │           starten.                     │
           │                                        │
           │           Dieser Befehl wird unter dem │
           │           Windows-Betriebssystem nicht │
           │           unterstützt.                 │
           └────────────────────────────────────────┘

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --type
           Gibt den Library-Typ und das GlassFish Server-Verzeichnis an, aus
           dem die Library entfernt wird. Folgende Werte sind gültig:

           common
               Entfernt die Library-Dateien aus dem gemeinsamen Class
               Loader-Verzeichnis domain-dir/lib. Dies ist die
               Standardeinstellung.

           ext
               Entfernt die Library-Dateien aus dem optionalen
               Java-Packageverzeichnis domain-dir/lib/ext.

           app
               Entfernt die Library-Dateien aus dem anwendungsspezifischen
               Class Loader-Verzeichnis domain-dir/lib/applibs.

           Weitere Informationen zu diesen Verzeichnissen finden Sie unter
           "Class Loaders" im Oracle GlassFish Server Application Development
           Guide.

OPERANDEN
       library-name
           Die Namen der JAR-Dateien, die die zu entfernenden Librarys
           enthalten.

BEISPIELE
       Example 1, Librarys entfernen
           In diesem Beispiel wird die Library in der Archivdatei mylib.jar
           aus dem anwendungsspezifischen Class Loader-Verzeichnis auf der
           standardmäßigen Serverinstanz entfernt.

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       add-library(1), list-libraries(1)

       asadmin(1M)

       "Class Loaders" im Oracle GlassFish Server Application Development
       Guide

Java EE 7                        16. Nov. 2011               remove-library(1)
