delete-application-ref(1)   asadmin 实用程序子命令   delete-application-ref(1)

名称
       delete-application-ref - 删除对应用程序的引用

提要
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

说明
       delete-application-ref
       子命令从集群或非集群服务器实例中删除对应用程序的引用。这将有效地使应用程序元素取消部署并使其在目标实例或集群中不再可用。

       无需为了成功运行此子命令而运行或提供构成集群的目标实例。如果一个或多个实例不可用，则在下次启动实例时将不再加载应用程序。

       删除引用不会导致从域中删除应用程序。这些位仅能通过 undeploy
       子命令删除。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从中删除应用程序引用的目标。有效值为

           ·   server - 将默认服务器实例指定为目标。server
               是默认服务器实例的名称，并且是默认值。

           ·   cluster_name - 将特定集群指定为目标。

           ·   instance_name - 将特定的独立服务器实例指定为目标。

       --cascade
           对于连接器模块，指示是否也应递归地删除依赖于模块的资源。默认值为假
           RAR 文件，也可以是 EAR 文件内的模块。

操作数
       reference_name
           应用程序或模块的名称，可以是 Java EE 应用程序模块、Web 模块、EJB
           模块、连接器模块、应用程序客户机模块或生命周期模块。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.)
           等字符。要删除对多个版本的引用，可以使用星号 (*)
           作为通配符。有关模块和应用程序版本的详细信息，请参阅 Oracle
           GlassFish Server Application Deployment Guide 中的 Module and
           Application Versions。

示例
       Example 1, 删除应用程序引用
           以下示例从非集群服务器实例 NewServer 中删除对 Web 模块 MyWebApp
           的引用。

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日    delete-application-ref(1)
