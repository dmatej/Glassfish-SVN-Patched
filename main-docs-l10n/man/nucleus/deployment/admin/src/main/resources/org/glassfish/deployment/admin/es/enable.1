enable(1)               Subcomandos de la Utilidad asa               enable(1)

NOMBRE DE REFERENCIA
       enable - Activa el componente.

SINOPSIS
           enable [--help] [--target target_name] component_name

DESCRIPCIÓN
       El subcomando enable activa el componente desplegado que se especifica.
       Si el componente está activado, se activa de nuevo. Si no se ha
       activado, se devolverá un mensaje de error.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Esta opción especifica el destino en el que se va a activar el
           componente. Los valores válidos son:

           server
               Activa la instancia de servidor por defecto server y se trata
               del valor por defecto.

           domain_name
               Activa el dominio con nombre.

           cluster_name
               Activa todas las instancias de servidor del cluster.

           instance_name
               Activa una instancia del servidor en cluster o independiente
               determinada.

OPERANDOS
       component_name
           Nombre del componente que se va a activar.

           El nombre puede incluir un identificador de versión opcional, que
           va a continuación del nombre y está separado de él por dos puntos
           (:). El identificador de versión debe comenzar por una letra o
           número. Puede contener caracteres alfanuméricos más los caracteres
           de subrayado (_), guión (-) y punto (.). Para obtener más
           información sobre las versiones de módulo y de aplicación, consulte
           el apartado sobre las versiones de módulo y de aplicación en Oracle
           GlassFish Server Application Deployment Guide.

           Cómo máximo puede estar activada una versión de un módulo o
           aplicación en una instancia del servidor. El resto de versiones se
           desactivan. La activación de una versión desactiva automáticamente
           todas las demás.

EJEMPLOS
       Ejemplo 1, Activación de un Componente
           En este ejemplo se activa el componente desactivado sampleApp.

               asadmin> enable sampleApp
               Command enable executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       deploy(1), , undeploy(1), disable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                   17 de noviembre de 2010                  enable(1)
