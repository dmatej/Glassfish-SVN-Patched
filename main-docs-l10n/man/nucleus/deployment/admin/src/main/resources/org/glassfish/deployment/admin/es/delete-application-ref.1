delete-application-ref(1Subcomandos de la Utilidad asdelete-application-ref(1)

NOMBRE DE REFERENCIA
       delete-application-ref - Elimina una referencia a una aplicación.

SINOPSIS
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

DESCRIPCIÓN
       El subcomando delete-application-ref elimina una referencia desde un
       cluster o una instancia de servidor sin cluster a una aplicación. El
       resultado es que se anula el despliegue del elemento de aplicación y
       éste deja de estar disponible en la instancia o cluster de destino.

       No es necesario que la instancia o instancias de destino que
       constituyen el cluster se estén ejecutando o estén disponibles para que
       este subcomando se ejecute correctamente. Si una o más instancias no
       están disponibles, éstas no cargarán la aplicación la próxima vez que
       se inicie.

       Al eliminar la referencia no se elimina la aplicación del dominio. Los
       bits sólo se eliminan con el subcomando undeploy.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se eliminará la referencia de la
           aplicación. Los valores válidos son:

           ·   server: especifica como destino la instancia de servidor por
               defecto.  server es el nombre de la instancia de servidor por
               defecto y el valor por defecto.

           ·   cluster_name: especifica un cluster determinado como destino.

           ·   instance_name: especifica una determinada instancia de servidor
               independiente como destino.

       --cascade
           Para un módulo de conector, indica si los recursos dependientes del
           módulo también se deben suprimir de forma recursiva. El valor por
           defecto es false. El módulo del conector puede ser un archivo RAR
           independiente o un módulo de un archivo EAR.

OPERANDOS
       reference_name
           El nombre de la aplicación o módulo, que puede ser un módulo de
           aplicación Java EE, un módulo web, un módulo EJB, un módulo de
           conector, un módulo de cliente de aplicaciones o un módulo de ciclo
           de vida.

           El nombre puede incluir un identificador de versión opcional, que
           va a continuación del nombre y está separado de él por dos puntos
           (:). El identificador de versión debe comenzar por una letra o
           número. Puede contener caracteres alfanuméricos más los caracteres
           de subrayado (_), guión (-) y punto (.). Para suprimir referencias
           a varias versiones, puede utilizar un asterisco (*) como carácter
           comodín. Para obtener más información sobre las versiones de módulo
           y de aplicación, consulte el apartado sobre las versiones de módulo
           y de aplicación en Oracle GlassFish Server Application Deployment
           Guide.

EJEMPLOS
       Ejemplo 1, Supresión de una Referencia de Aplicación
           En el ejemplo siguiente se elimina una referencia al módulo web
            MyWebApp de la instancia de servidor sin cluster NewServer .

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                   17 de noviembre de 2010  delete-application-ref(1)
