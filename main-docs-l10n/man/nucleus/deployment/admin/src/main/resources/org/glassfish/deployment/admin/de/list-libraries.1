list-libraries(1)       Unterbefehle des asadmin-Utili       list-libraries(1)

NAME
       list-libraries - listet Library-JAR-Dateien auf GlassFish Server auf

ZUSAMMENFASSUNG
           list-libraries [--help] [--type={common|ext|app}]

BESCHREIBUNG
       Der Unterbefehl list-libraries listet Library-Archivdateien auf
       GlassFish Server auf.

       Mit der Option --type werden der Library-Typ und das GlassFish
       Server-Verzeichnis angegeben, für das Librarys aufgelistet werden
       sollen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --type
           Gibt den Library-Typ und das GlassFish Server-Verzeichnis an, für
           das Librarys aufgelistet werden sollen. Folgende Werte sind gültig:

           common
               Listet die Library-Dateien für das gemeinsame Class
               Loader-Verzeichnis domain-dir/lib auf. Dies ist die
               Standardeinstellung.

           ext
               Listet die Library-Dateien für das optionale
               Java-Packageverzeichnis domain-dir/lib/ext auf.

           app
               Listet die Library-Dateien für das anwendungsspezifische Class
               Loader-Verzeichnis domain-dir/lib/applibs auf.

           Weitere Informationen zu diesen Verzeichnissen finden Sie unter
           "Class Loaders" im Oracle GlassFish Server Application Development
           Guide.

BEISPIELE
       Example 1, Librarys auflisten
           In diesem Beispiel werden die Librarys im anwendungsspezifischen
           Class Loader-Verzeichnis auf der standardmäßigen Serverinstanz
           aufgelistet.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       add-library(1), remove-library(1)

       asadmin(1M)

       "Class Loaders" im Oracle GlassFish Server Application Development
       Guide

Java EE 7                        16. Nov. 2011               list-libraries(1)
