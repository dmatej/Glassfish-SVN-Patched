list-libraries(1)           asadmin 实用程序子命令           list-libraries(1)

名称
       list-libraries - 列出 GlassFish Server 上的库 JAR 文件

提要
           list-libraries [--help] [--type={common|ext|app}]

说明
       list-libraries 子命令列出 GlassFish Server 上的库档案文件。

       --type 选项指定库类型和要列出库的 GlassFish Server 目录。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --type
           指定库类型和要列出库的 GlassFish Server 目录。有效值如下：

           common
               列出通用类加载器目录 domain-dir/lib 的库文件。这是默认选项。

           ext
               列出 Java 可选程序包目录 domain-dir/lib/ext 的库文件。

           app
               列出应用程序特定的类加载器目录 domain-dir/lib/applibs
               的库文件。

           有关这些目录的详细信息，请参阅 Oracle GlassFish Server Application
           Development Guide 中的 Class Loaders。

示例
       Example 1, 列出库
           此示例列出默认服务器实例上应用程序特定类加载器目录中的库。

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       add-library(1), remove-library(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide 中的 Class
       Loaders

Java EE 7                     2011 年 11 月 16 日            list-libraries(1)
