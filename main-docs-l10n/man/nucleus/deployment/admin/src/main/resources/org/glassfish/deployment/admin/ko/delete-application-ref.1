delete-application-ref(1) asadmin 유틸리티 하위 명령 delete-application-ref(1)

제목
       delete-application-ref - 응용 프로그램에 대한 참조 제거

시놉시스
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

설명
       delete-application-ref 하위 명령은 클러스터 또는 클러스터되지 않은 서버
       인스턴스에서 응용 프로그램에 대한 참조를 제거합니다. 이는 배치를
       해제하여 대상 인스턴스 또는 클러스터에서 더 이상 사용할 수 없도록 할
       응용 프로그램 요소에서 효과적으로 작용합니다.

       클러스터를 구성하는 대상 인스턴스(하나 또는 여러 개)가 실행 중이거나
       사용 가능한 상태가 아니어도 이 하위 명령은 성공적으로 실행됩니다. 하나
       이상의 인스턴스를 사용할 수 없는 경우 해당 인스턴스는 다음 시작 시 응용
       프로그램을 더 이상 로드하지 않습니다.

       참조를 제거하더라도 도메인에서 응용 프로그램이 제거되지는 않습니다.
       비트는 undeploy 하위 명령에 의해서만 제거됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           응용 프로그램 참조를 제거할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           ·   server- 기본 서버 인스턴스를 대상으로 지정합니다.  server는
               기본 서버 인스턴스의 이름이며 기본값입니다.

           ·   cluster_name- 특정 클러스터를 대상으로 지정합니다.

           ·   instance_name- 특정 독립형 서버 인스턴스를 대상으로 지정합니다.

       --cascade
           커넥터 모듈에 대해 모듈에 종속되는 리소스도 재귀적으로 삭제해야
           되는지를 나타냅니다. 기본값은 false입니다. 커넥터 모듈은 독립형 RAR
           파일이거나 EAR 파일 내의 모듈일 수 있습니다.

피연산자
       reference_name
           응용 프로그램 또는 모듈(Java EE 응용 프로그램 모듈, 웹 모듈, EJB
           모듈, 커넥터 모듈, 응용 프로그램 클라이언트 모듈 또는 수명 주기
           모듈일 수 있음)의 이름입니다.

           이름에는 버전 식별자(옵션)가 포함될 수 있으며, 이 식별자는 이름
           다음에 오고 콜론(:)으로 이름과 구분됩니다. 버전 식별자는 문자 또는
           숫자로 시작해야 합니다. 영숫자 문자와 밑줄(_), 대시(-) 및 마침표(.)
           문자가 사용될 수 있습니다. 여러 버전에 대한 참조를 삭제하려면
           별표(*)를 와일드 카드 문자로 사용합니다. 모듈 및 응용 프로그램
           버전에 대한 자세한 내용은 Oracle GlassFish Server Application
           Deployment Guide의 "Module and Application Versions"를
           참조하십시오.

예제
       예 1, 응용 프로그램 참조 삭제
           다음 예제는 클러스터되지 않은 서버 인스턴스 NewServer에서 웹 모듈
           MyWebApp에 대한 참조를 제거합니다.

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      2010년 11월 17일      delete-application-ref(1)
