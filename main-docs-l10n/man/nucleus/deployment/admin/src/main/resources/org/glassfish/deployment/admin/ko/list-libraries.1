list-libraries(1)         asadmin 유틸리티 하위 명령         list-libraries(1)

제목
       list-libraries - GlassFish Server의 라이브러리 JAR 파일 나열

시놉시스
           list-libraries [--help] [--type={common|ext|app}]

설명
       list-libraries 하위 명령은 GlassFish Server의 라이브러리 아카이브
       파일을 나열합니다.

       --type 옵션은 라이브러리 유형 및 라이브러리를 나열할 GlassFish Server
       디렉토리를 지정합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --type
           라이브러리 유형 및 라이브러리를 나열할 GlassFish Server 디렉토리를
           지정합니다. 적합한 값은 다음과 같습니다.

           common
               공통 클래스 로더 디렉토리인 domain-dir/lib의 라이브러리 파일을
               나열합니다. 이 값은 기본값입니다.

           ext
               Java 선택적 패키지 디렉토리인 domain-dir/lib/ext의 라이브러리
               파일을 나열합니다.

           app
               응용 프로그램별 클래스 로더 디렉토리인 domain-dir/lib/applibs의
               라이브러리 파일을 나열합니다.

           이러한 디렉토리에 대한 자세한 내용은 Oracle GlassFish Server
           Application Development Guide의 "Class Loaders"를 참조하십시오.

예제
       예 1, 라이브러리 나열
           이 예제는 기본 서버 인스턴스에 있는 응용 프로그램별 클래스 로더
           디렉토리의 라이브러리를 나열합니다.

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       add-library(1), remove-library(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide의 "Class Loaders"

Java EE 7                      2011년 11월 16일              list-libraries(1)
