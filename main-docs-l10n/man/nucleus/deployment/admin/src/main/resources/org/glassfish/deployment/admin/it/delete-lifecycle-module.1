delete-lifecycle-module(Comandi secondari della utildelete-lifecycle-module(1)

NOME
       delete-lifecycle-module - rimuove il modulo del ciclo di vita

SINTASSI
           delete-lifecycle-module [--help] [--target target] module_name

DESCRIZIONE
       Il comando secondario delete-lifecycle-module rimuove un modulo del
       ciclo di vita. Un modulo del ciclo di vita consente di eseguire un task
       basato su Java di breve o lunga durata in una fase specifica del ciclo
       di vita del server. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Indica la posizione in cui deve essere eliminato il modulo del
           ciclo di vita. Di seguito sono riportati i valori validi.

           ·   server: specifica l'istanza server predefinita come
               destinazione per l'eliminazione del modulo del ciclo di vita.
               server è il nome dell'istanza server predefinita ed è il valore
               predefinito per questa opzione.

           ·   cluster_name: specifica un cluster particolare come
               destinazione di eliminazione del modulo del ciclo di vita.

           ·   instance_name: specifica un'istanza server particolare come
               destinazione per l'eliminazione del modulo del ciclo di vita.

OPERANDI
       module_name
           Questo operando è un identificativo univoco per il modulo listener
           di eventi del ciclo di vita del server distribuito.

ESEMPI
       Esempio 1, Eliminazione di un modulo del ciclo di vita
           Nell'esempio riportato di seguito viene eliminato un modulo del
           ciclo di vita denominato customSetup.

               asadmin> delete-lifecycle-module customSetup
               Command delete-lifecycle-module executed successfully

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-lifecycle-module(1), list-lifecycle-modules(1)

       asadmin(1M)

Java EE 7                      16 novembre 2010     delete-lifecycle-module(1)
