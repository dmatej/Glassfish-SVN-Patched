enable(1)                   asadmin 实用程序子命令                   enable(1)

名称
       enable - 启用组件

提要
           enable [--help] [--target target_name] component_name

说明
       enable
       子命令启用指定的已部署组件。如果组件已启用，则会重新启用它。如果尚未部署组件，则会返回错误消息。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要启用组件的目标。有效值为：

           server
               启用默认服务器实例 server，这是默认值。

           domain_name
               启用命名域。

           cluster_name
               启用集群中的每个服务器实例。

           instance_name
               启用特定的集群服务器实例或独立服务器实例。

操作数
       component_name
           要启用的组件的名称。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.)
           等字符。有关模块和应用程序版本的详细信息，请参阅 Oracle GlassFish
           Server Application Deployment Guide 中的 Module and Application
           Versions。

           通常，可以在服务器实例上启用模块或应用程序的一个版本。将禁用所有其他版本。启用一个版本会自动禁用其他版本。

示例
       Example 1, 启用组件
           此示例启用已禁用的组件 sampleApp。

               asadmin> enable sampleApp
               Command enable executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       deploy(1), , undeploy(1), disable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日                    enable(1)
