list-application-refs(1)Subcomandos do utilitário asadlist-application-refs(1)

NOME
       list-application-refs - lista as referências de aplicação existentes

SINOPSE
           list-application-refs [--help] [--long={false|true}] [target]

DESCRIçãO
       O subcomando list-application-refs lista todas as referências de
       aplicações de um cluster ou de uma instância de servidor não
       clusterizada. Essa opção lista de maneira eficaz todos os módulos
       implantados no alvo especificado (por exemplo, aplicações J2EE, módulos
       Web e módulos de enterprise bean).

       Se várias versões de um módulo ou aplicação forem implantadas, esse
       subcomando listará todas as versões. Para listar qual versão está
       ativada, defina a opção --long como true. Para obter mais informações
       sobre versões de módulo e da aplicação, consulte "Module and
       Application Versions" no Oracle GlassFish Server Application Deployment
       Guide.

       A instância ou instâncias do alvo que compõem o cluster não precisam
       estar em execução ou disponíveis para que esse subcomando seja bem
       sucedido.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --long
           Se essa opção for definida como true, exibe se cada módulo ou
           aplicação listado está ativado. O default é false.

OPERANDOS
       target
           O alvo para o qual você está listando as referências de aplicações.
           Os valores válidos são

           ·   server- Especifica a instância do servidor default como o alvo.
                server é o nome da instância de servidor default e é o valor
               default.

           ·   cluster_name - Especifica um certo cluster como o alvo.

           ·   instance_name- Especifica determinada instância do servidor
               como o alvo.

EXEMPLOS
       Example 1, Listando Referências de Aplicações
           O exemplo a seguir lista as referências de aplicações para a
           instância de servidor não clusterizada NewServer.

               asadmin> list-application-refs NewServer
               ClientSessionMDBApp
               MEjbApp
               __ejb_container_timer_app
               Command list-application-refs executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-application-ref(1), delete-application-ref(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         22 set 2010         list-application-refs(1)
