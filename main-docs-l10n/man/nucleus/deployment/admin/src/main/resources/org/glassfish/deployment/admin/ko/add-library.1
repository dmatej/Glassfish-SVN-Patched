add-library(1)            asadmin 유틸리티 하위 명령            add-library(1)

제목
       add-library - GlassFish Server에 하나 이상의 라이브러리 JAR 파일 추가

시놉시스
           add-library [--help] [--type={common|ext|app}] [--upload={false|true}]

           library-file-path [library-file-path ... ]

설명
       add-library 하위 명령은 GlassFish Server에 하나 이상의 라이브러리
       아카이브 파일을 추가합니다.

       --type 옵션은 라이브러리 유형 및 라이브러리를 추가할 GlassFish Server
       디렉토리를 지정합니다.

       library-file-path 피연산자는 추가할 라이브러리를 포함하는 JAR 파일의
       경로입니다. 여러 라이브러리를 지정하려면 공백으로 경로를 구분하여
       지정합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           라이브러리 아카이브 파일은   │
           │           DAS에 추가됩니다. 공통 및    │
           │           확장 라이브러리의 경우 서버  │
           │           런타임에 라이브러리가        │
           │           선택되도록 DAS를 재시작해야  │
           │           합니다. 다른 서버 인스턴스에 │
           │           라이브러리를 추가하려면 해당 │
           │           인스턴스를 재시작하여 DAS와  │
           │           동기화하십시오.              │
           └────────────────────────────────────────┘

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --type
           라이브러리 유형 및 라이브러리를 추가할 GlassFish Server 디렉토리를
           지정합니다. 적합한 값은 다음과 같습니다.

           common
               공통 클래스 로더 디렉토리인 domain-dir/lib에 라이브러리 파일을
               추가합니다. 이 값은 기본값입니다.

           ext
               Java 선택적 패키지 디렉토리인 domain-dir/lib/ext에 라이브러리
               파일을 추가합니다.

           app
               응용 프로그램별 클래스 로더 디렉토리인 domain-dir/lib/applibs에
               라이브러리 파일을 추가합니다.

           이러한 디렉토리에 대한 자세한 내용은 Oracle GlassFish Server
           Application Development Guide의 "Class Loaders"를 참조하십시오.

       --upload
           하위 명령이 파일을 DAS에 업로드하는지를 지정합니다. 대부분의 경우
           이 옵션을 생략할 수 있습니다.

           적합한 값은 다음과 같습니다.

           false
               하위 명령이 파일을 업로드하지 않고 지정된 파일 이름을 통해
               파일에 액세스하려고 시도합니다. DAS가 파일에 액세스할 수 없는
               경우 하위 명령을 실패합니다.

               예를 들어, DAS가 관리 사용자 이외의 다른 사용자로 실행 중이며
               해당 파일에 대한 읽기 액세스 권한을 가지고 있지 않을 수
               있습니다. 이 경우 --upload 옵션이 false이면 하위 명령을
               실패합니다.

           true
               하위 명령이 네트워크 접속을 통해 파일을 DAS에 업로드합니다.

           기본값은 DAS가 하위 명령이 실행되는 호스트에 있는지 또는 원격
           호스트에 있는지에 따라 다릅니다.

           ·   DAS가 하위 명령이 실행되는 호스트에 있는 경우 기본값은
               false입니다.

           ·   DAS가 원격 호스트에 있는 경우 기본값은 true입니다.

           디렉토리 filepath가 지정된 경우 이 옵션이 무시됩니다.

피연산자
       library-file-path
           추가할 라이브러리를 포함하는 아카이브 파일의 경로입니다. 절대 경로
           또는 상대 경로를 지정할 수 있습니다.

           --upload 옵션이 true로 설정된 경우 로컬 클라이언트 시스템에 있는
           파일 경로입니다.  --upload 옵션이 false로 설정된 경우 서버 시스템에
           있는 파일 경로입니다.

예제
       예 1, 라이브러리 추가
           이 예제는 기본 서버 인스턴스의 응용 프로그램별 클래스 로더
           디렉토리에 아카이브 파일 mylib.jar의 라이브러리를 추가합니다.

               asadmin> add-library --type app /tmp/mylib.jar
               Command add-library executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       remove-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide의 "Class Loaders"

Java EE 7                      2011년 11월 16일                 add-library(1)
