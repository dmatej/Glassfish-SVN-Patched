list-application-refs(1)    asadmin 实用程序子命令;   list-application-refs(1)

名称
       list-application-refs - 列出现有应用程序引用

提要
           list-application-refs [--help] [--long={false|true}] [target]

说明
       list-application-refs
       子命令列出集群或非集群服务器实例中的所有应用程序引用。该子命令可有效地列出指定目标上部署的所有模块
       (例如，J2EE 应用程序、Web 模块以及企业 Bean 模块)。

       如果部署了模块或应用程序的多个版本，该子命令将列出所有版本。要列出启用了哪个版本，请将
       --long 选项设置为真
       GlassFish Server Application Deployment Guide 中的 Module and
       Application Versions。

       无需为了成功运行此子命令而运行或提供构成集群的目标实例。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --long
           如果为真

操作数
       target
           要列出应用程序引用的目标。有效值为

           ·   server - 将默认服务器实例指定为目标。server
               是默认服务器实例的名称，并且是默认值。

           ·   cluster_name - 将特定集群指定为目标。

           ·   instance_name - 将特定服务器实例指定为目标。

示例
       Example 1, 列出应用程序引用
           以下示例列出非集群服务器实例 NewServer 的应用程序引用。

               asadmin> list-application-refs NewServer
               ClientSessionMDBApp
               MEjbApp
               __ejb_container_timer_app
               Command list-application-refs executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-application-ref(1), delete-application-ref(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 9 月 22 日      list-application-refs(1)
