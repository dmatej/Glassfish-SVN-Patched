delete-application-ref(1)   asadmin 公用程式子命令   delete-application-ref(1)

參考名
       delete-application-ref - 移除應用程式的參照

概要
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

描述
       delete-application-ref
       子命令會從叢集或非叢集的伺服器執行處理移除應用程式的參照。這會有效導致應用程式元素取消建置，並且在目標執行處理或叢集上不再可用。

       這個子命令不需要目標執行處理或構成叢集的執行處理，更不需執行它們，便可成功執行。如果一或多個執行處理無法使用，則在下次啟動時不會載入應用程式。

       移除參照不會導致應用程式從網域移除。只有使用 undeploy
       子命令才能移除位元。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要從中移除應用程式參照的目標。有效值包括

           ·   server- 指定預設伺服器執行處理為目標。server
               為預設伺服器執行處理的名稱，且此為預設值。

           ·   cluster_name - 指定特定叢集作為目標。

           ·   instance_name - 指定特定獨立伺服器執行處理作為目標。

       --cascade
           對於連線器模組，指出是否也要遞迴地刪除依賴此模組的資源。預設值為
           false。連線器模組可以是獨立的 RAR 檔案或是 EAR 檔案內的模組。

運算元
       reference_name
           應用程式或模組的名稱，可以是 Java EE 應用程式模組、Web 模組、EJB
           模組、連線器模組、應用程式從屬端模組或生命週期模組。

           名稱中可以包含一個選擇性的版本識別碼，後面接著名稱，兩者間用冒號
           (:) 隔開。
           版本識別碼必須以字母或數字開頭。當中可以包含英數字元，也可以加上底線
           (_)、破折號 (-) 以及句號 (.)
           字元。若要刪除對多個版本的參照，您可以使用星號 (*)
           作為萬用字元。如需有關模組與應用程式版本的更多資訊，請參閱 Oracle
           GlassFish Server Application Deployment Guide 中的 Module and
           Application Versions。

範例
       範例 1, 刪除應用程式參照
           下列範例會從非叢集的伺服器執行處理 NewServer 移除 Web 模組 MyWebApp
           的參照。

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 17 日    delete-application-ref(1)
