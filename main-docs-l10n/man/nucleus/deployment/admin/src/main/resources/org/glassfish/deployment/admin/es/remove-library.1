remove-library(1)       Subcomandos de la Utilidad asa       remove-library(1)

NOMBRE DE REFERENCIA
       remove-library - elimina uno o más archivos JAR de biblioteca de
       GlassFish Server

SINOPSIS
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

DESCRIPCIÓN
       El subcomando remove-library elimina uno o más archivos JAR de
       biblioteca de GlassFish Server.

       La opción --type especifica el tipo de biblioteca y el directorio
       GlassFish Server del que se elimina la biblioteca.

       El operando library-name es el nombre del archivo JAR que contiene la
       biblioteca que se va a eliminar. Para especificar varias bibliotecas,
       especifique varios nombres separados por espacios.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           El archivo de almacenamiento │
           │           de biblioteca se elimina del │
           │           DAS. En el caso de           │
           │           bibliotecas comunes y de     │
           │           extensión, debe reiniciar el │
           │           DAS para que el tiempo de    │
           │           ejecución del servidor       │
           │           seleccione las eliminaciones │
           │           de la biblioteca. Para       │
           │           eliminar las bibliotecas de  │
           │           otras instancias del         │
           │           servidor, reinicie las       │
           │           instancias para              │
           │           sincronizarlas con el DAS.   │
           │                                        │
           │           Este comando no está         │
           │           soportado en el sistema      │
           │           operativo Windows.           │
           └────────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --type
           Especifica el tipo de biblioteca y el directorio GlassFish Server
           del que se elimina la biblioteca. Los valores válidos son los
           siguientes:

           common
               Elimina los archivos de biblioteca del directorio del cargador
               de clase Común, domain-dir/lib. Éste es el valor por defecto.

           ext
               Elimina los archivos de biblioteca del directorio opcional de
               paquetes de Java, domain-dir/lib/ext.

           app
               Elimina los archivos de biblioteca del directorio del cargador
               de clases específicas de la aplicación, domain-dir/lib/applibs.

           Para obtener más información sobre estos directorios, consulte la
           sección sobre cargadores de clases en Oracle GlassFish Server
           Application Development Guide.

OPERANDOS
       library-name
           Nombres de los archivos JAR que contienen las bibliotecas que se
           van a eliminar.

EJEMPLOS
       Ejemplo 1, Eliminación de Bibliotecas
           En este ejemplo se elimina la biblioteca del archivo de
           almacenamiento mylib.jar del directorio de cargador de clase
           específico de la aplicación en la instancia de servidor por
           defecto.

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       add-library(1), list-libraries(1)

       asadmin(1M)

       Sección sobre cargadores de clases en Oracle GlassFish Server
       Application Development Guide

Java EE 7                   16 de noviembre de 2011          remove-library(1)
