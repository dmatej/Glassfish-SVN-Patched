list-application-refs(1)  asadmin 유틸리티 하위 명령; list-application-refs(1)

제목
       list-application-refs - 기존 응용 프로그램 참조 나열

시놉시스
           list-application-refs [--help] [--long={false|true}] [target]

설명
       list-application-refs 하위 명령은 클러스터 또는 클러스터화되지 않은
       서버 인스턴스의 모든 응용 프로그램 참조를 나열합니다. 이 명령은 지정된
       대상에 배치된 모든 모듈(예: J2EE 응용 프로그램, 웹 모듈 및 Enterprise
       Bean 모듈)을 효과적으로 나열합니다.

       여러 버전의 모듈이나 응용 프로그램이 배치된 경우 이 하위 명령은 모든
       버전을 나열합니다. 사용으로 설정된 버전을 나열하려면 --long 옵션을
       true로 설정합니다. 모듈 및 응용 프로그램 버전에 대한 자세한 내용은
       Oracle GlassFish Server Application Deployment Guide의 "Module and
       Application Versions"를 참조하십시오.

       클러스터를 구성하는 대상 인스턴스(하나 또는 여러 개)가 실행 중이거나
       사용 가능한 상태가 아니어도 이 하위 명령은 성공적으로 실행됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --long
           true이면 나열된 각 모듈 또는 응용 프로그램이 사용으로 설정되었는지
           여부를 표시합니다. 기본값은 false입니다.

피연산자
       target
           응용 프로그램 참조를 나열할 대상입니다. 적합한 값은 다음과
           같습니다.

           ·   server- 기본 서버 인스턴스를 대상으로 지정합니다.  server는
               기본 서버 인스턴스의 이름이며 기본값입니다.

           ·   cluster_name- 특정 클러스터를 대상으로 지정합니다.

           ·   instance_name- 특정 서버 인스턴스를 대상으로 지정합니다.

예제
       예 1, 응용 프로그램 참조 나열
           다음 예제는 클러스터화되지 않은 서버 인스턴스 NewServer에 대한 응용
           프로그램 참조를 나열합니다.

               asadmin> list-application-refs NewServer
               ClientSessionMDBApp
               MEjbApp
               __ejb_container_timer_app
               Command list-application-refs executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-application-ref(1), delete-application-ref(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                       2010년 9월 22일       list-application-refs(1)
