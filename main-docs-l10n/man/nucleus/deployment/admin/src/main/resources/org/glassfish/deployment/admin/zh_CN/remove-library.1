remove-library(1)           asadmin 实用程序子命令           remove-library(1)

名称
       remove-library - 从 GlassFish Server 中删除一个或多个库 JAR 文件

提要
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

说明
       remove-library 子命令从 GlassFish Server 中删除一个或多个库 JAR 文件。

       --type 选项指定库类型和从中删除库的 GlassFish Server 目录。

       library-name 操作数是包含要删除的库的 JAR
       文件名称。要指定多个库，请指定多个以空格分隔的名称。

           注意
           ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注                                                                                                 │
           │                                                                                                   │
           │           从 DAS                                                                                  │
           │           中删除库档案文件。对于通用库和扩展库，必须重新启动                                      │
           │           DAS，服务器运行时才能选取库删除。要从其他服务器实例删除库，请通过重新启动这些实例将其与 │
           │           DAS 同步。                                                                              │
           │                                                                                                   │
           │           Windows                                                                                 │
           │           操作系统不支持此命令。                                                                  │
           └───────────────────────────────────────────────────────────────────────────────────────────────────┘

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --type
           指定库类型和从中删除库的 GlassFish Server 目录。有效值如下：

           common
               从通用类加载器目录 domain-dir/lib 中删除库文件。这是默认选项。

           ext
               从 Java 可选程序包目录 domain-dir/lib/ext 中删除库文件。

           app
               从应用程序特定的类加载器目录 domain-dir/lib/applibs
               中删除库文件。

           有关这些目录的详细信息，请参阅 Oracle GlassFish Server Application
           Development Guide 中的 Class Loaders。

操作数
       library-name
           包含要删除的库的 JAR 文件的名称。

示例
       Example 1, 删除库
           此示例从默认服务器实例的应用程序特定类加载器目录中删除档案文件
           mylib.jar 中的库。

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       add-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide 中的 Class
       Loaders

Java EE 7                     2011 年 11 月 16 日            remove-library(1)
