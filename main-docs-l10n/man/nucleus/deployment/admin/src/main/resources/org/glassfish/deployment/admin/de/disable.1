disable(1)              Unterbefehle des asadmin-Utili              disable(1)

NAME
       disable - deaktiviert die Komponente

ZUSAMMENFASSUNG
           disable [--help] [--target target_name] component_name

BESCHREIBUNG
       Der Unterbefehl disable deaktiviert sofort die angegebene
       bereitgestellte Komponente. Wenn die Komponente nicht bereitgestellt
       wurde, wird eine Fehlermeldung ausgegeben.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, auf dem Sie die Komponente
           deaktivieren. Gültige Werte sind:

           server
               Deaktiviert die Komponente auf der standardmäßigen
               Serverinstanz server und ist der Standardwert.

           domain_name
               Deaktiviert die Komponente auf der benannten Domain.

           cluster_name
               Deaktiviert die Komponente auf jeder Serverinstanz im Cluster.

           instance_name
               Deaktiviert die Komponente auf einer bestimmten geclusterten
               oder Standalone-Serverinstanz.

OPERANDEN
       component_name
           Name der Komponente, die deaktiviert werden soll.

           Der Name kann eine optionale Versions-ID enthalten, die dem Namen
           folgt und von diesem durch einen Doppelpunkt (:) getrennt ist. Die
           Versions-ID muss mit einem Buchstaben oder einer Zahl beginnen. Er
           kann alphanumerische Zeichen sowie Unterstriche (_), Bindestriche
           (-) und Punkte (.) enthalten. Um mehrere Versionen zu deaktivieren,
           können Sie ein Sternchen (*) als Platzhalterzeichen verwenden.
           Weitere Informationen zu Modul- und Anwendungsversionen finden Sie
           unter "Module and Application Versions" im Oracle GlassFish Server
           Application Deployment Guide.

BEISPIELE
       Beispiel 1, Komponenten deaktivieren
           In diesem Beispiel wird die bereitgestellte Komponente sampleApp
           deaktiviert.

               asadmin> disable sampleApp
               Command disable executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        17. Nov. 2010                      disable(1)
