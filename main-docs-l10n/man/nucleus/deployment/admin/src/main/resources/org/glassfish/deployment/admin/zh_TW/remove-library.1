remove-library(1)           asadmin 公用程式子命令           remove-library(1)

參考名
       remove-library - 從 GlassFish Server 移除一或多個程式庫 JAR 檔案

概要
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

描述
       remove-library 子命令可從 GlassFish Server 移除一或多個程式庫 JAR
       檔案。

       --type 選項指定程式庫以及要從其中移除程式庫的 GlassFish Server 目錄。

       library-name 運算元為 JAR
       檔案的名稱，該檔案中包含要移除的程式庫。若要指定多個程式庫，請指定以空格分隔的多個名稱。

           注意
           ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │備註                                                                                                                 │
           │                                                                                                                     │
           │           從 DAS                                                                                                    │
           │           移除程式庫存檔檔案。對於一般及擴充程式庫，您必須重新啟動                                                  │
           │           DAS，伺服器執行階段才會提取程式庫移除項。若要從其他伺服器執行處理移除程式庫，請將執行處理重新啟動，以便與 │
           │           DAS 同步化。                                                                                              │
           │                                                                                                                     │
           │           Windows                                                                                                   │
           │           作業系統不支援此命令。                                                                                    │
           └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --type
           從移除的程式庫位置指定程式庫類型和 GlassFish Server
           目錄。有效的值如下：

           common
               從 Common 類別載入器目錄 domain-dir/lib
               中移除程式庫檔案。此為預設值。

           ext
               從 Java 選用套裝軟體目錄 domain-dir/lib/ext 中移除程式庫檔案。

           app
               從應用程式特定的類別載入器目錄 domain-dir/lib/applibs
               中移除程式庫檔案。

           如需有關這些目錄的詳細資訊，請參閱 Oracle GlassFish Server
           Application Development Guide 中的 Class Loaders。

運算元
       library-name
           包含即將移除之程式庫的 JAR 檔案名稱。

範例
       範例 1, 移除程式庫
           此範例會從預設伺服器執行處理上應用程式特定的類別載入器目錄，移除存檔檔案
           mylib.jar 中的程式庫。

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       add-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Server Application Development Guide 中的Class Loaders

Java EE 7                     2011 年 11 月 16 日            remove-library(1)
