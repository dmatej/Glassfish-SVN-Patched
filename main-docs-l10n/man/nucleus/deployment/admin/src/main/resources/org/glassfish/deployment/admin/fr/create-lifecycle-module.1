create-lifecycle-module(Sous-commandes de l'utilitaicreate-lifecycle-module(1)

NOM
       create-lifecycle-module - crée un module de cycle de vie

SYNTAXE
           create-lifecycle-module [--help] --classname classname
            [--enabled={true|false}] [--target target]
            [--classpath classpath] [--loadorder loadorder]
            [--failurefatal={false|true} ] [--description description]
            [--property (name=value)[:name=value]*]
            module_name

DESCRIPTION
       La sous-commande create-lifecycle-module crée un module de cycle de
       vie. Un module de cycle de vie permet d'exécuter une tâche Java de
       courte ou longue durée à un stade bien précis du cycle de vie du
       serveur. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --classname
           Il s'agit du nom qualifié complet de la classe de démarrage.

       --target
           Cette option indique l'emplacement auquel le module de cycle de vie
           doit être créé. Les valeurs valides sont les suivantes :

           ·   server : cette valeur indique l'instance de serveur par défaut
               en tant que cible pour la création du module de cycle de vie.
               server est le nom de l'instance de serveur par défaut et
               constitue la valeur par défaut de cette option.

           ·   cluster_name : cette valeur indique un cluster particulier en
               tant que cible pour la création du module de cycle de vie.

           ·   instance_name : cette valeur indique une instance de serveur
               autonome particulière en tant que cible pour la création du
               module de cycle de vie.

       --classpath
           Cette option indique l'emplacement du module de cycle de vie. Il
           s'agit d'une variable d'environnement CLASSPATH au format standard
           : les valeurs des répertoires et des fichiers JAR sont séparées par
           le signe deux-points (:) (Unix) ou par le signe point-virgule (;)
           (Windows). Les répertoires et les fichiers JAR référencés ne sont
           pas téléchargés vers l'instance de serveur.

       --loadorder
           Cette option représente un entier qui peut être utilisé pour forcer
           l'ordre dans lequel les modules de cycle de vie déployés sont
           chargés au démarrage du serveur. Les modules dont le numéro est le
           plus bas sont chargés en premier. L'ordre n'est pas spécifié si
           plusieurs modules de cycle de vie ont la même valeur d'ordre de
           chargement. La valeur par défaut est Integer.MAX_VALUE, ce qui
           signifie que le module de cycle de vie est chargé en dernier.

       --failurefatal
           Cette option indique au système les actions à effectuer si le
           module de cycle de vie n'est pas chargé correctement. Lorsque cette
           option a la valeur True, le système abandonne le démarrage du
           serveur si ce module n'est pas chargé correctement. La valeur par
           défaut est False.

       --enabled
           Cette option détermine si le module de cycle de vie est activé lors
           de l'exécution. La valeur par défaut est True.

       --description
           Texte décrivant le module de cycle de vie.

       --property
           Il s'agit d'un attribut facultatif contenant les paires nom/valeur
           utilisées pour configurer le module de cycle de vie.

OPÉRANDES
       module_name
           Cet opérande est un identificateur unique pour le module de
           processus d'écoute d'événements de cycle de vie de serveur déployé.

EXEMPLES
       Exemple 1, Création d'un module de cycle de vie
           L'exemple suivant montre comment créer un module de cycle de vie
           nommé customSetup.

               asadmin> create-lifecycle-module --classname "com.acme.CustomSetup"
               --classpath "/export/customSetup" --loadorder 1 --failurefatal=true
               --description "this is a sample customSetup"
               --property rmi="Server\=acme1\:7070":timeout=30 customSetup
               Command create-lifecycle-module executed successfully
           Le caractère d'échappement \ est utilisé dans l'option de propriété
           pour indiquer que le signe égal (=) et le signe deux-points (:)
           font partie de la valeur de propriété rmi.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       delete-lifecycle-module(1), list-lifecycle-modules(1)

       asadmin(1M)

       Developing Lifecycle Listeners dans le manuel Oracle GlassFish Server
       Application Development Guide

Java EE 7                         18 nov 2010       create-lifecycle-module(1)
