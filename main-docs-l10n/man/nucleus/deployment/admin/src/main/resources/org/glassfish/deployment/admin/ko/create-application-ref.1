create-application-ref(1) asadmin 유틸리티 하위 명령 create-application-ref(1)

제목
       create-application-ref - 응용 프로그램에 대한 참조 생성

시놉시스
           create-application-ref [--help] [--target target]
           [--virtualservers virtual_servers] [--enabled=true]
           [--lbenabled=true] reference_name

설명
       create-application-ref 하위 명령은 클러스터 또는 클러스터되지 않은 서버
       인스턴스에서 이전에 배치된 응용 프로그램 요소(예: Java EE 응용
       프로그램, 웹 모듈 또는 엔터프라이즈 bean 모듈)로의 참조를 생성합니다.
       이는 배치하여 대상 인스턴스 또는 클러스터에서 사용할 수 있도록 할 응용
       프로그램 요소에서 효과적으로 작용합니다.

       클러스터를 구성하는 대상 인스턴스(하나 또는 여러 개)가 실행 중이거나
       사용 가능한 상태가 아니어도 이 하위 명령은 성공적으로 실행됩니다. 하나
       이상의 인스턴스를 사용할 수 없는 경우 해당 인스턴스는 다음 시작 시 새
       응용 프로그램 요소를 받게 됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           응용 프로그램 참조를 생성할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           ·   server- 기본 서버 인스턴스를 응용 프로그램 참조를 생성하기 위한
               대상으로 지정합니다.  server는 기본 서버 인스턴스의 이름이며
               이는 이 옵션의 기본값입니다.

           ·   cluster_name- 특정 클러스터를 응용 프로그램 참조를 생성하기
               위한 대상으로 지정합니다.

           ·   instance_name- 특정 독립형 서버 인스턴스를 응용 프로그램 참조를
               생성하기 위한 대상으로 지정합니다.

       --virtualservers
           배치할 가상 서버 ID의 쉼표로 구분된 목록을 지정합니다. 이 옵션은 웹
           모듈(독립형 또는 Java EE 응용 프로그램에서)에만 적용됩니다. 이
           옵션을 지정하지 않은 경우 응용 프로그램이 관리 서버 __asadmin을
           제외한 모든 가상 서버에 배치됩니다.

       --enabled
           응용 프로그램을 사용으로 설정(즉, 로드)해야 하는지를 나타냅니다.
           응용 프로그램이 전역 레벨로 사용으로 설정되어 있는 경우에만 이 값이
           적용됩니다. 기본값은 true입니다.

       --lbenabled
           배치된 응용 프로그램을 로드 균형 조정에 사용할 수 있는지를
           제어합니다. 기본값은 true입니다.

피연산자
       reference_name
           응용 프로그램 또는 모듈(Java EE 응용 프로그램, 웹 모듈, EJB 모듈,
           커넥터 모듈, 응용 프로그램 클라이언트 모듈 또는 수명 주기 모듈일 수
           있음)의 이름입니다.

           이름에는 버전 식별자(옵션)가 포함될 수 있으며, 이 식별자는 이름
           다음에 오고 콜론(:)으로 이름과 구분됩니다. 버전 식별자는 문자 또는
           숫자로 시작해야 합니다. 영숫자 문자와 밑줄(_), 대시(-) 및 마침표(.)
           문자가 사용될 수 있습니다.  --enabled 옵션을 false로 설정한 경우
           별표(*)를 와일드 카드 문자로 사용하여 사용 안함으로 설정된 여러
           버전에 대한 참조를 생성할 수 있습니다. 모듈 및 응용 프로그램 버전에
           대한 자세한 내용은 Oracle GlassFish Server Application Deployment
           Guide의 "Module and Application Versions"를 참조하십시오.

예제
       예 1, 응용 프로그램 참조 생성
           다음 예제는 클러스터되지 않은 서버 인스턴스 NewServer에서 웹 모듈
           MyWebApp에 대한 참조를 생성합니다.

               asadmin> create-application-ref --target NewServer MyWebApp
               Command create-application-ref executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       delete-application-ref(1), list-application-refs(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      2010년 11월 17일      create-application-ref(1)
