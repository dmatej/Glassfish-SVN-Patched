list-libraries(1)     asadminユーティリティのサブコマンド    list-libraries(1)

名前
       list-libraries - GlassFish ServerのライブラリJARファイルをリストします

概要
           list-libraries [--help] [--type={common|ext|app}]

説明
       list-librariesサブコマンドは、GlassFish
       Serverのライブラリ・アーカイブ・ファイルをリストします。

       --typeオプションは、ライブラリ・タイプおよびライブラリをリストするGlassFish
       Serverディレクトリを指定します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --type
           ライブラリ・タイプおよびライブラリをリストするGlassFish
           Serverディレクトリを指定します。有効な値は次のとおりです:

           common
               Commonクラス・ローダー・ディレクトリdomain-dir/libのライブラリ・ファイルをリストします。これはデフォルトです。

           ext
               Javaオプション・パッケージ・ディレクトリdomain-dir/lib/extのライブラリ・ファイルをリストします。

           app
               アプリケーション固有のクラス・ローダー・ディレクトリdomain-dir/lib/applibsのライブラリ・ファイルをリストします。

           これらのディレクトリの詳細は、Oracle GlassFish
           Serverア

例
       例 1, ライブラリのリスト
           この例では、デフォルトのサーバー・インスタンスに対するアプリケーション固有のクラス・ローダーのディレクトリにあるライブラリをリストします。

               asadmin> list-libraries --type app
               mylib.jar
               xlib.jar
               ylib.jar
               zlib.jar
               Command list-libraries executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       add-library(1), remove-library(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2011年11月16日               list-libraries(1)
