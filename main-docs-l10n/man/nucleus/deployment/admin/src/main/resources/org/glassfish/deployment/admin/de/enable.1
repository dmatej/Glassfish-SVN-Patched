enable(1)               Unterbefehle des asadmin-Utili               enable(1)

NAME
       enable - aktiviert die Komponente

ZUSAMMENFASSUNG
           enable [--help] [--target target_name] component_name

BESCHREIBUNG
       Der Unterbefehl enable aktiviert die angegebene bereitgestellte
       Komponente. Ist die Komponente bereits aktiviert, wird sie erneut
       aktiviert. Wenn die Komponente nicht bereitgestellt wurde, wird eine
       Fehlermeldung ausgegeben.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, auf dem Sie die Komponente
           aktivieren. Gültige Werte sind:

           server
               Aktiviert die standardmäßige Serverinstanz server und ist der
               Standardwert.

           domain_name
               Aktiviert die benannte Domain.

           cluster_name
               Aktiviert jede Serverinstanz im Cluster.

           instance_name
               Aktiviert eine bestimmte geclusterte oder
               Standalone-Serverinstanz.

OPERANDEN
       component_name
           Name der Komponente, die aktiviert werden soll.

           Der Name kann eine optionale Versions-ID enthalten, die dem Namen
           folgt und von diesem durch einen Doppelpunkt (:) getrennt ist. Die
           Versions-ID muss mit einem Buchstaben oder einer Zahl beginnen. Er
           kann alphanumerische Zeichen sowie Unterstriche (_), Bindestriche
           (-) und Punkte (.) enthalten. Weitere Informationen zu Modul- und
           Anwendungsversionen finden Sie unter "Module and Application
           Versions" im Oracle GlassFish Server Application Deployment Guide.

           Auf einer Serverinstanz kann höchstens eine Version eines Moduls
           oder einer Anwendung aktiviert werden. Alle anderen Versionen
           werden deaktiviert. Wird eine Version aktiviert, werden alle
           anderen Versionen automatisch deaktiviert.

BEISPIELE
       Example 1, Komponenten aktivieren
           In diesem Beispiel wird die Komponente sampleApp aktiviert und
           deaktiviert.

               asadmin> enable sampleApp
               Command enable executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       deploy(1), , undeploy(1), disable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        17. Nov. 2010                       enable(1)
