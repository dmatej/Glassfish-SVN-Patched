add-library(1)          Unterbefehle des asadmin-Utili          add-library(1)

NAME
       add-library - fügt mindestens eine Library-JAR-Datei zu GlassFish
       Server hinzu

ZUSAMMENFASSUNG
           add-library [--help] [--type={common|ext|app}] [--upload={false|true}]

           library-file-path [library-file-path ... ]

BESCHREIBUNG
       Der Unterbefehl add-library fügt mindestens ein Library-Archiv zu
       GlassFish Server hinzu.

       Die Option --type gibt den Library-Typ und das GlassFish
       Server-Verzeichnis an, dem die Library hinzugefügt wird.

       Der Operand library-file-path ist der Pfad zur JAR-Datei, die die
       hinzuzufügende Library enthält. Um mehrere Librarys anzugeben, geben
       Sie mehrere, durch Leerzeichen getrennte Dateien an.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Die Library-Archivdatei wird │
           │           dem DAS hinzugefügt. Bei     │
           │           allgemeinen und              │
           │           Erweiterungs-Librarys müssen │
           │           Sie den DAS neu starten,     │
           │           damit die Librarys von der   │
           │           Serverlaufzeit               │
           │           berücksichtigt werden. Um    │
           │           die Librarys anderen         │
           │           Serverinstanzen              │
           │           hinzuzufügen,                │
           │           synchronisieren Sie die      │
           │           Instanzen mit dem DAS, indem │
           │           Sie sie neu starten.         │
           └────────────────────────────────────────┘

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --type
           Diese Option gibt den Library-Typ und das GlassFish
           Server-Verzeichnis an, dem die Library hinzugefügt wird. Folgende
           Werte sind gültig:

           common
               Fügt die Library-Dateien zu dem gemeinsamen Class
               Loader-Verzeichnis domain-dir/lib hinzu. Dies ist die
               Standardeinstellung.

           ext
               Fügt die Library-Dateien zu dem optionalen
               Java-Packageverzeichnis domain-dir/lib/ext hinzu.

           app
               Fügt die Library-Dateien zu dem anwendungsspezifischen Class
               Loader-Verzeichnis domain-dir/lib/applibs hinzu.

           Weitere Informationen zu diesen Verzeichnissen finden Sie unter
           "Class Loaders" im Oracle GlassFish Server Application Development
           Guide.

       --upload
           Gibt an, ob der Unterbefehl die Datei auf den DAS hochlädt. In den
           meisten Fällen kann diese Option ausgelassen werden.

           Folgende Werte sind gültig:

           false
               Der Unterbefehl lädt die Datei nicht hoch, sondern versucht,
               über den angegebenen Dateinamen darauf zuzugreifen. Wenn der
               DAS nicht auf die Datei zugreifen kann, ist der Unterbefehl
               nicht erfolgreich.

               Beispielsweise kann der DAS unter einem anderen Benutzer als
               dem Admin-Benutzer ausgeführt werden, der keinen Lesezugriff
               auf die Datei besitzt. In diesem Fall ist der Unterbefehl nicht
               erfolgreich, wenn die Option --upload auf false gesetzt ist.

           true
               Der Unterbefehl lädt die Datei über die Netzwerkverbindung auf
               den DAS hoch.

           Der Standardwert hängt davon ab, ob sich der DAS auf dem Host
           befindet, auf dem der Unterbefehl ausgeführt wird, oder ob er auf
           einem Remote-Host liegt.

           ·   Befindet sich der DAS auf dem Host, auf dem der Unterbefehl
               ausgeführt wird, ist die Standardeinstellung false.

           ·   Befindet sich der DAS auf einem Remote-Host, ist die
               Standardeinstellung true.

           Wenn das Verzeichnis filepath angegeben ist, wird diese Option
           ignoriert.

OPERANDEN
       library-file-path
           Der Pfad zu den Archivdateien, die die hinzuzufügenden Librarys
           enthalten. Sie können einen absoluten Pfad oder einen relativen
           Pfad angeben.

           Ist die Option --upload auf true gesetzt, ist dies der Pfad zur
           Datei auf dem lokalen Clientrechner. Ist die Option --upload auf
           false gesetzt, ist dies der Pfad zur Datei auf dem Serverrechner.

BEISPIELE
       Beispiel 1, Librarys hinzufügen
           In diesem Beispiel wird die Library in der Archivdatei mylib.jar
           dem anwendungsspezifischen Class Loader-Verzeichnis auf der
           Standardserverinstanz hinzugefügt.

               asadmin> add-library --type app /tmp/mylib.jar
               Command add-library executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       remove-library(1), list-libraries(1)

       asadmin(1M)

       "Class Loaders" im Oracle GlassFish Server Application Development
       Guide

Java EE 7                        16. Nov. 2011                  add-library(1)
