list-components(1)      Subcomandos do utilitário asad      list-components(1)

NOME
       list-components - lista componentes implantados

SINOPSE
           list-components [--help]
           [--long={false|true}] [--resources] [--subcomponents]
           [--type type] [target]

DESCRIçãO
           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           O subcomando list-components │
           │           é obsoleto. Utilize o        │
           │           subcomando                   │
           │           list-applications.           │
           └────────────────────────────────────────┘

       O subcomando list-components lista todos os componentes do Java EE
       implantados.

       Se a opção --type não for especificada; todos os componentes serão
       listados. Se a opção de tipo estiver especificada, é necessário
       especificar um tipo. Os tipos possíveis são listas da seção Opções
       desta página de ajuda.

       Se várias versões de um módulo ou aplicação forem implantadas, esse
       subcomando listará todas as versões. Para listar qual versão está
       ativada, defina a opção --long como true. Para obter mais informações
       sobre versões de módulo e da aplicação, consulte "Module and
       Application Versions" no Oracle GlassFish Server Application Deployment
       Guide.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --long
           Se essa opção for definida como true, exibe se cada módulo ou
           aplicação listado está ativado. O default é false.

       --resources
           Lista os recursos de escopo da aplicação para cada componente. Se a
           opção --subcomponents também for utilizada, o comando listará os
           recursos no escopo da aplicação para cada componente de uma
           aplicação.

       --subcomponents
           Lista os subcomponentes de cada componente. Os subcomponentes
           listados dependem do tipo de componente. Por exemplo, para uma
           aplicação Java EE (arquivo EAR), são listados os módulos. Para uma
           aplicação Web, servlets e páginas JSP são listados. Para um módulo
           EJB, são listados subcomponentes do EJB.

       --type
           Especifica o tipo de componentes que serão listados. As opções são
           as seguintes:

           ·   application

           ·   appclient

           ·   connector

           ·   ejb

           ·   web

           ·   webservice

           Se nenhum tipo for especificado, todos os componentes serão
           listados.

OPERANDOS
       target
           Esse é o nome do alvo no qual o subcomando opera. Os valores
           válidos são:

           server
               Lista os componentes para a instância do servidor default
               server e é o valor default.

           domain
               Lista os componentes para o domínio.

           cluster_name
               Lista os componentes do cluster.

           instance_name
               Lista os componentes para uma determinada instância
               independente do servidor.

EXEMPLOS
       Example 1, Listando Componentes
           Este exemplo lista os componentes do conector. (cciblackbox-tx.rar
           foi implantado.)

               asadmin> list-components --type connector
               cciblackbox-tx <connector>
               Command list-components executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       list-applications(1), show-component-status(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         29 nov 2010               list-components(1)
