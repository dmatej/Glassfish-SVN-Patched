delete-application-ref(1Sous-commandes de l'utilitairdelete-application-ref(1)

NOM
       delete-application-ref - enlève la référence à une application

SYNTAXE
           delete-application-ref [--help] [--target target]
            [--cascade=false] reference_name

DESCRIPTION
       La sous-commande delete-application-ref enlève une référence à une
       application figurant dans un cluster ou une instance de serveur non
       incluse dans un cluster. Le déploiement de l'élément d'application est
       alors annulé et ce dernier n'est plus disponible sur le cluster ou
       l'instance cible.

       Il n'est pas nécessaire que les instances cible formant le cluster
       soient en cours d'exécution ou disponibles pour lancer cette
       sous-commande. Si certaines instances ne sont pas disponibles, elles ne
       chargeront plus l'application au prochain démarrage.

       La suppression de la référence n'entraîne pas la suppression de
       l'application du domaine. Les bits sont enlevés uniquement par la
       sous-commande undeploy.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible sur laquelle vous enlevez la
           référence à l'application. Les valeurs valides sont les suivantes :

           ·   server : cette valeur indique l'instance de serveur par défaut
               comme cible.  server est le nom de l'instance de serveur par
               défaut et constitue la valeur par défaut.

           ·   cluster_name : cette valeur indique un cluster particulier
               comme cible.

           ·   instance_name : cette valeur indique une instance de serveur
               autonome particulière comme cible.

       --cascade
           Pour un module connecteur, cette option indique si les ressources
           dépendant du module doivent également être supprimées de façon
           récursive. La valeur par défaut est false. Le module connecteur
           peut être un fichier RAR autonome ou un module au sein d'un fichier
           EAR.

OPÉRANDES
       reference_name
           Nom de l'application ou du module, qui peut être un module
           d'application Java EE, un module Web, un module EJB, un module
           connecteur, un module client d'application ou un module de cycle de
           vie.

           Le nom peut être suivi d'un identificateur de version facultatif
           dont il est séparé par un signe deux-points (:). L'identificateur
           de version doit commencer par une lettre ou un chiffre. Il peut
           contenir des caractères alphanumériques, des traits de soulignement
           (_), des tirets (-) et des points (.). Pour supprimer des
           références à plusieurs versions, vous pouvez utiliser un astérisque
           (*) comme caractère générique. Pour plus d'informations sur les
           versions des modules et des applications, reportez-vous à Module
           and Application Versions dans le manuel Oracle GlassFish Server
           Application Deployment Guide.

EXEMPLES
       Exemple 1, Suppression d'une référence à une application
           L'exemple suivant montre comment enlever une référence au module
           Web MyWebApp dans l'instance de serveur non incluse dans un cluster
           NewServer.

               asadmin> delete-application-ref --target NewServer MyWebApp
               Command delete-application-ref executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-application-ref(1), list-application-refs(1), undeploy(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         17 nov 2010        delete-application-ref(1)
