disable(1)              Subcomandos do utilitário asad              disable(1)

NOME
       disable - desativa o componente

SINOPSE
           disable [--help] [--target target_name] component_name

DESCRIçãO
       O subcomando disable desativa imediatamente o componente implantado
       especificado. Se o componente não foi implantado, uma mensagem de erro
       aparece.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Esta opção especifica o alvo no qual o componente está sendo
           desativado. Os valores válidos são:

           server
               Desativa o componente na instância do servidor default server e
               é o valor default.

           domain_name
               Desativa o componente no domínio nomeado.

           cluster_name
               Desativa o componente em cada instância do servidor no cluster.

           instance_name
               Desativa o componente em uma determinada instância standalone
               ou clusterizada do servidor.

OPERANDOS
       component_name
           nome do componente a ser desativado.

           O nome pode incluir um identificador de versão opcional, que segue
           o nome e é separado do nome por dois-pontos (:). O identificador de
           versão deve começar com uma letra ou número. Pode conter caracteres
           alfanuméricos e sublinhado (_), traço (-), e ponto (.). Para
           desativar várias versões, é possível utilizar um asterisco (*) como
           caractere curinga. Para obter mais informações sobre versões de
           módulo e da aplicação, consulte "Module and Application Versions"
           no Oracle GlassFish Server Application Deployment Guide.

EXEMPLOS
       Example 1, Desativando um Componente
           Este exemplo desativa o componente sampleApp implantado.

               asadmin> disable sampleApp
               Command disable executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       deploy(1), undeploy(1), enable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                   17 de novembro de 2010                  disable(1)
