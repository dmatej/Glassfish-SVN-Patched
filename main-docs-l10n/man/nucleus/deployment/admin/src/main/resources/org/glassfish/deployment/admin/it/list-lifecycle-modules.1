list-lifecycle-modules(1Comandi secondari della utililist-lifecycle-modules(1)

NOME
       list-lifecycle-modules - elenca i moduli del ciclo di vita

SINTASSI
           list-lifecycle-modules [--help] [target]

DESCRIZIONE
       Il comando secondario list-lifecycle-modules elenca i moduli del ciclo
       di vita. Un modulo del ciclo di vita consente di eseguire un task
       basato su Java di breve o lunga durata in una fase specifica del ciclo
       di vita del server. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Indica la posizione in cui devono essere elencati i moduli del
           ciclo di vita. Di seguito sono riportati i valori validi.

           ·   server: specifica l'istanza server predefinita come
               destinazione per la creazione della lista dei moduli del ciclo
               di vita.  server è il nome dell'istanza server predefinita ed è
               il valore predefinito per questo operando.

           ·   cluster_name: specifica un cluster particolare come
               destinazione per la creazione della lista dei moduli del ciclo
               di vita.

           ·   instance_name: specifica un'istanza server particolare come
               destinazione per la creazione della lista dei moduli del ciclo
               di vita.

ESEMPI
       Esempio 1, Visualizzazione della lista dei moduli del ciclo di vita

               asadmin> list-lifecycle-modules
               WSTCPConnectorLCModule
               Command list-lifecycle-modules executed successfully
           WSTCPConnectorLCModule è l'unico modulo del ciclo di vita elencato
           per la destinazione predefinita server.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-lifecycle-module(1), delete-lifecycle-module(1)

       asadmin(1M)

Java EE 7                      16 novembre 2010      list-lifecycle-modules(1)
