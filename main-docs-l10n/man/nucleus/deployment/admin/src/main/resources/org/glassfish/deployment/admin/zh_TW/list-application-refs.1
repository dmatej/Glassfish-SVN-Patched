list-application-refs(1)    asadmin 公用程式子命令;   list-application-refs(1)

參考名
       list-application-refs - 列出現有的應用程式參照

概要
           list-application-refs [--help] [--long={false|true}] [target]

描述
       list-application-refs
       子命令會列出叢集或非叢集伺服器執行處理中的所有應用程式參照。這個子命令會有效列出建置在特定目標的所有模組
       (例如，J2EE 應用程式、Web 模組及企業 Bean 模組)。

       如果建置的模組或應用程式有多重版本，這個子命令會列出所有版本。若要列出啟用的版本，請將
       --long 選項設為 true。如需有關模組與應用程式版本的更多資訊，請參閱
       Oracle GlassFish Server Application Deployment Guide 中的 Module and
       Application Versions。

       這個子命令不需要目標執行處理或構成叢集的執行處理，更不需執行它們，便可成功執行。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --long
           如果為 true，則會顯示列出的各模組或應用程式是否已啟用。預設值為
           false。

運算元
       target
           列出應用程式參照的目標。有效值包括

           ·   server- 指定預設伺服器執行處理為目標。server
               為預設伺服器執行處理的名稱，且此為預設值。

           ·   cluster_name - 指定特定叢集作為目標。

           ·   instance_name - 指定特定伺服器執行處理作為目標。

範例
       範例 1, 列出應用程式參照
           下列範例會列出非叢集伺服器執行處理 NewServer 的應用程式參照。

               asadmin> list-application-refs NewServer
               ClientSessionMDBApp
               MEjbApp
               __ejb_container_timer_app
               Command list-application-refs executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-application-ref(1), delete-application-ref(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 9 月 22 日      list-application-refs(1)
