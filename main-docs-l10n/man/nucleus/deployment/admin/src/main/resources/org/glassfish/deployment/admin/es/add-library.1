add-library(1)          Subcomandos de la Utilidad asa          add-library(1)

NOMBRE DE REFERENCIA
       add-library - Agrega uno o más archivos JAR de biblioteca a GlassFish
       Server.

SINOPSIS
           add-library [--help] [--type={common|ext|app}] [--upload={false|true}]

           library-file-path [library-file-path ... ]

DESCRIPCIÓN
       El subcomando add-library agrega uno o más archivos de almacenamiento
       de biblioteca a GlassFish Server.

       La opción --type especifica el tipo de biblioteca y el directorio de
       GlassFish Server al que se agrega la biblioteca.

       El operando library-file-path es la ruta de acceso del archivo JAR que
       contiene la biblioteca que se va a agregar. Para especificar varias
       bibliotecas, especifique varias rutas de acceso separadas por espacios.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           El archivo de almacenamiento │
           │           de biblioteca se agrega al   │
           │           DAS. En el caso de           │
           │           bibliotecas comunes y de     │
           │           extensiones, debe reiniciar  │
           │           el DAS para que el tiempo de │
           │           ejecución del servidor las   │
           │           seleccione debidamente. Para │
           │           agregar las bibliotecas a    │
           │           otras instancias del         │
           │           servidor, reinicie las       │
           │           instancias para              │
           │           sincronizarlas con el DAS.   │
           └────────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --type
           Especifica el tipo de biblioteca y el directorio de GlassFish
           Server al que se agrega la biblioteca. Los valores válidos son los
           siguientes:

           common
               Agrega los archivos de biblioteca al directorio del cargador de
               clases común, domain-dir/lib. Éste es el valor por defecto.

           ext
               Agrega los archivos de biblioteca al directorio opcional de
               paquetes de Java, domain-dir/lib/ext.

           app
               Agrega los archivos de biblioteca al directorio del cargador de
               clases específico de la aplicación, domain-dir/lib/applibs.

           Para obtener más información sobre estos directorios, consulte la
           sección sobre cargadores de clases en Oracle GlassFish Server
           Application Development Guide.

       --upload
           Especifica si el subcomando carga el archivo en el DAS. En la
           mayoría de las situaciones, esta opción se puede omitir.

           Los valores válidos son los siguientes:

           false
               El subcomando no carga el archivo y trata de acceder a él a
               través del nombre del archivo especificado. Si el DAS no puede
               acceder al archivo, el subcomando falla.

               Por ejemplo, el DAS puede ejecutarse como un usuario distinto
               del usuario de administración y no tener acceso de lectura al
               archivo. En esta situación, el subcomando falla si la opción
               --upload es false.

           true
               El subcomando carga el archivo en el DAS a través de la
               conexión de red.

           El valor por defecto depende de si el DAS está en el host en el que
           se ejecuta el subcomando o está en un host remoto.

           ·   Si el DAS está en el host en el que se ejecuta el subcomando,
               el valor por defecto es false.

           ·   Si el DAS está en un host remoto, el valor por defecto es true.

           Si se ha especificado un directorio filepath, esta opción se
           ignora.

OPERANDOS
       library-file-path
           Rutas de acceso a los archivos de almacenamiento que contienen las
           bibliotecas que se van a agregar. Puede especificar una ruta de
           acceso absoluta o relativa.

           Si la opción --upload se define en true, ésta es la ruta de acceso
           al archivo en la máquina cliente local. Si la opción --upload se
           define en false, ésta es la ruta de acceso al archivo en la máquina
           servidor.

EJEMPLOS
       Ejemplo 1, Adición de bibliotecas
           Este ejemplo agrega la biblioteca del archivo de almacenamiento
           mylib.jar al directorio del cargador de clases específico de la
           aplicación en la instancia del servidor por defecto.

               asadmin> add-library --type app /tmp/mylib.jar
               Command add-library executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       remove-library(1), list-libraries(1)

       asadmin(1M)

       Sección sobre cargadores de clases en Oracle GlassFish Server
       Application Development Guide

Java EE 7                   16 de noviembre de 2011             add-library(1)
