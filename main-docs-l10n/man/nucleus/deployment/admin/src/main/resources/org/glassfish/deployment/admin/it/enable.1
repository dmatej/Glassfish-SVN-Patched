enable(1)               Comandi secondari della utilit               enable(1)

NOME
       enable - abilita il componente

SINTASSI
           enable [--help] [--target target_name] component_name

DESCRIZIONE
       Il comando secondario enable abilita il componente distribuito
       specificato. Se è già abilitato, il componente viene riabilitato. Se
       invece il componente non è stato distribuito, viene restituito un
       messaggio di errore.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica la destinazione in cui viene abilitato il
           componente. Di seguito sono riportati i valori validi.

           server
               Abilita l'istanza server predefinita server ed è il valore
               predefinito.

           domain_name
               Abilita il dominio specificato.

           cluster_name
               Abilita tutte le istanze server del cluster.

           instance_name
               Abilita un'istanza server in cluster o standalone particolare.

OPERANDI
       component_name
           Il nome del componente da abilitare.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per ulteriori
           informazioni sulle versioni di moduli e applicazioni, vedere
           "Module and Application Versions" in Oracle GlassFish Server
           Application Deployment Guide.

           In un'istanza server è possibile abilitare al massimo una versione
           di un modulo o di un'applicazione. Tutte le altre versioni vengono
           disabilitate. L'abilitazione di una versione comporta la
           disabilitazione automatica di tutte le altre.

ESEMPI
       Esempio 1, Abilitazione di un componente
           In questo esempio viene abilitato il componente disabilitato
           sampleApp.

               asadmin> enable sampleApp
               Command enable executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       deploy(1), , undeploy(1), disable(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      17 novembre 2010                      enable(1)
