remove-library(1)     asadminユーティリティのサブコマンド    remove-library(1)

名前
       remove-library - 1つ以上のライブラリJARファイルをGlassFish
       Serverから除去します

概要
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

説明
       remove-libraryサブコマンドは、1つ以上のライブラリJARファイルをGlassFish
       Serverから除去します。

       --typeオプションは、ライブラリ・タイプおよびライブラリを除去するGlassFish
       Serverディレクトリを指定します。

       library-nameオペランドは、除去するライブラリが格納されているJARファイルの名前です。複数のライブラリを指定するには、それらの名前をスペースで区切って指定します。

           注記
           ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注意                                                                                                                                                                                                                                                                                                       │
           │                                                                                                                                                                                                                                                                                                           │
           │           ライブラリ・アーカイブ・ファイルはDASから除去されます。共通ライブラリおよび拡張ライブラリの場合は、サーバーの実行時にライブラリの除去が選択されるように、DASを再起動する必要があります。ライブラリを他のサーバー・インスタンスから除去するには、そのインスタンスを再起動してDASと同期させます。 │
           │                                                                                                                                                                                                                                                                                                           │
           │           このコマンドは、Windowsオペレーティング・システムではサポートされていません。                                                                                                                                                                                                                   │
           └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --type
           ライブラリ・タイプおよびライブラリを除去するGlassFish
           Serverディレクトリを指定します。有効な値は次のとおりです:

           common
               Commonクラス・ローダー・ディレクトリdomain-dir/libからライブラリ・ファイルを除去します。これはデフォルトです。

           ext
               Javaオプション・パッケージ・ディレクトリdomain-dir/lib/extからライブラリ・ファイルを除去します。

           app
               アプリケーション固有のクラス・ローダー・ディレクトリdomain-dir/lib/applibsからライブラリ・ファイルを除去します。

           これらのディレクトリの詳細は、Oracle GlassFish
           Serverア

オペランド
       library-name
           除去対象のライブラリが格納されているJARファイルの名前。

例
       例 1, ライブラリの除去
           この例では、デフォルトのサーバー・インスタンスにあるアプリケーション固有のクラス・ローダー・ディレクトリから、アーカイブ・ファイルmylib.jarのライブラリを除去します。

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       add-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Serverア

Java EE 7                       2011年11月16日               remove-library(1)
