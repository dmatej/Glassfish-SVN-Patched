list-jndi-entries(1)  asadminユーティリティのサブコマンド list-jndi-entries(1)

名前
       list-jndi-entries - JNDIツリーの参照および問合せを行います

概要
           list-jndi-entries [--help]
           [--context context_name]
           [target]

説明
       このサブコマンドを使用して、JNDIツリーの参照および問合せを行います。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --context
           JNDIコンテキストまたはサブコンテキストの名前。コンテキストが指定されていない場合、ネーム・サービスのすべてのエントリが返されます。コンテキスト(ejbなど)が指定されている場合、それらのエントリがすべて返されます。

オペランド
       target
           このオペランドでは、参照するJNDIツリーを指定します。

           使用可能な値は次のとおりです:

           server
               デフォルトのGlassFish
               ServerインスタンスのJNDIツリーを参照します(デフォルト)。デフォルトのインスタンスは、ドメイン管理サーバー(DAS)です。

           domain
               現在のドメインのJNDIツリーを参照します。

           cluster-name
               指定したクラスタのJNDIツリーを参照します。

           instance-name
               指定したGlassFish ServerインスタンスのJNDIツリーを参照します。

例
       例 1, JNDIツリーの参照
           この例では、デフォルトのGlassFish
           ServerインスタンスのJNDIツリーを参照します。

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                        2010年12月7日            list-jndi-entries(1)
