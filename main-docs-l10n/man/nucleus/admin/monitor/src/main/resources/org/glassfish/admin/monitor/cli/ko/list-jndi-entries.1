list-jndi-entries(1)      asadmin 유틸리티 하위 명령      list-jndi-entries(1)

제목
       list-jndi-entries - JNDI 트리 찾아보기 및 질의

시놉시스
           list-jndi-entries [--help]
           [--context context_name]
           [target]

설명
       이 하위 명령을 사용하여 JNDI 트리를 찾아보고 질의합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --context
           JNDI 컨텍스트나 하위 컨텍스트의 이름입니다. 컨텍스트를 지정하지
           않은 경우 명명 서비스의 모든 항목이 반환됩니다. 컨텍스트(예: ejb)를
           지정한 경우 해당하는 모든 항목이 반환됩니다.

피연산자
       target
           이 피연산자는 찾아볼 JNDI 트리를 지정합니다.

           가능한 값은 다음과 같습니다.

           server
               기본 GlassFish Server 인스턴스에 대한 JNDI 트리를
               찾아봅니다(기본값). 기본 인스턴스는 DAS(도메인 관리
               서버)입니다.

           domain
               현재 도메인에 대한 JNDI 트리를 찾아봅니다.

           cluster-name
               지정된 클러스터에 대한 JNDI 트리를 찾아봅니다.

           instance-name
               지정된 GlassFish Server 인스턴스에 대한 JNDI 트리를 찾아봅니다.

예제
       예 1, JNDI 트리 찾아보기
           이 예제는 기본 GlassFish Server 인스턴스에 대한 JNDI 트리를
           찾아봅니다.

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                       2010년 12월 7일           list-jndi-entries(1)
