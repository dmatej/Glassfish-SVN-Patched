list-jndi-entries(1)    Unterbefehle des asadmin-Utili    list-jndi-entries(1)

NAME
       list-jndi-entries - durchsucht die JNDI-Baumstruktur und fragt sie ab

ZUSAMMENFASSUNG
           list-jndi-entries [--help]
           [--context context_name]
           [target]

BESCHREIBUNG
       Verwenden Sie diesen Unterbefehl, um die JNDI-Struktur zu durchsuchen
       und abzufragen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --context
           Der Name des JNDI-Kontextes oder Subkontextes. Ist kein Kontext
           angegeben, werden alle Einträge im Naming Service zurückgegeben.
           Ist Kontext (wie beispielsweise ejb) angegeben, werden alle diese
           Einträge zurückgegeben.

OPERANDEN
       target
           Dieser Operand gibt die zu durchsuchende JNDI-Baumstruktur an.

           Mögliche Werte:

           server
               Durchsucht den JNDI-Baum nach der standardmäßigen GlassFish
               Server-Instanz (Standard). Die Standardinstanz ist der DAS
               (Domain Administration Server).

           domain
               Durchsucht die JNDI-Baumstruktur nach der aktuellen Domain.

           cluster-name
               Durchsucht die JNDI-Baumstruktur nach dem angegebenen Cluster.

           instance-name
               Durchsucht die JNDI-Baumstruktur nach der angegebenen GlassFish
               Server-Instanz.

BEISPIELE
       Example 1, JNDI-Baumstruktur durchsuchen
           In diesem Beispiel wird die JNDI-Baumstruktur nach der
           standardmäßigen GlassFish Server-Instanz durchsucht.

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                        7. Dez. 2010             list-jndi-entries(1)
