list-jndi-entries(1)    Sous-commandes de l'utilitaire    list-jndi-entries(1)

NOM
       list-jndi-entries - parcourt et interroge l'arborescence JNDI

SYNTAXE
           list-jndi-entries [--help]
           [--context context_name]
           [target]

DESCRIPTION
       Utilisez cette sous-commande pour parcourir et interroger
       l'arborescence JNDI.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --context
           Nom du contexte ou sous-contexte JNDI. Si aucun contexte n'est
           spécifié, toutes les entrées dans le service de noms sont
           renvoyées. Si un contexte (comme ejb) est spécifié, toutes ces
           entrées sont renvoyées.

OPÉRANDES
       target
           Cet opérande spécifie l'arborescence JNDI à parcourir.

           Les valeurs possibles sont les suivantes :

           server
               Parcourt l'arborescence JNDI de l'instance GlassFish Server par
               défaut (valeur par défaut). L'instance par défaut est le
               serveur d'administration de domaine (serveur DAS).

           domain
               Parcourt l'arborescence JNDI pour le domaine en cours.

           cluster-name
               Parcourt l'arborescence JNDI pour le cluster spécifié.

           instance-name
               Parcourt l'arborescence JNDI pour l'instance GlassFish Server
               spécifiée.

EXEMPLES
       Exemple 1, Navigation dans l'arborescence JNDI
           Dans cet exemple, le système parcourt l'arborescence JNDI de
           l'instance GlassFish Server par défaut.

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                         7 déc 2010              list-jndi-entries(1)
