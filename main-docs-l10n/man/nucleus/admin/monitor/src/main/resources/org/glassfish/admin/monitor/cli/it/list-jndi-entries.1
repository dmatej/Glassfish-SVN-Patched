list-jndi-entries(1)    Comandi secondari della utilit    list-jndi-entries(1)

NOME
       list-jndi-entries - sfoglia ed esegue query sulla struttura JNDI

SINTASSI
           list-jndi-entries [--help]
           [--context context_name]
           [target]

DESCRIZIONE
       Utilizzare questo comando secondario per sfogliare ed eseguire query
       sulla struttura JNDI.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --context
           Il nome del contesto o del contesto secondario JNDI. Se non si
           specifica un contesto, vengono restituite tutte le voci del
           servizio di denominazione. Se si specifica il contesto, ad esempio
           ejb, vengono restituite tutte le voci corrispondenti.

OPERANDI
       target
           Questo operando specifica la struttura JNDI da sfogliare.

           Di seguito sono riportati i valori possibili.

           server
               Sfoglia la struttura JNDI per l'istanza predefinita di
               GlassFish Server (impostazione predefinita). L'istanza
               predefinita è il server di amministrazione del dominio (DAS).

           domain
               Sfoglia la struttura JNDI per il dominio corrente.

           cluster-name
               Sfoglia la struttura JNDI per il cluster specificato.

           instance-name
               Sfoglia la struttura JNDI per l'istanza di GlassFish Server
               specificata.

ESEMPI
       Esempio 1, Esplorazione della struttura JNDI
           In questo esempio viene sfogliata la struttura JNDI per l'istanza
           predefinita di GlassFish Server.

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                       7 dicembre 2010           list-jndi-entries(1)
