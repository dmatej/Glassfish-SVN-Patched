list-jndi-entries(1)    Subcomandos de la Utilidad asa    list-jndi-entries(1)

NOMBRE DE REFERENCIA
       list-jndi-entries - Examina y consulta el árbol de JNDI.

SINOPSIS
           list-jndi-entries [--help]
           [--context context_name]
           [target]

DESCRIPCIÓN
       Utilice este comando para examinar y consultar el árbol JNDI.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --context
           El nombre del contexto o subcontexto JNDI. Si no se especifica el
           contexto, se devuelven todas las entradas en el servicio de
           nombres. Si se especifica el contexto (por ejemplo, ejb), se
           devuelven las entradas correspondientes.

OPERANDOS
       target
           Este operando especifica el árbol de JNDI que se va a examinar.

           Los valores posibles son los siguientes:

           server
               Examina el árbol de JNDI para encontrar la instancia de
               GlassFish Server por defecto (valor por defecto). La instancia
               por defecto es el servidor de administración de dominio (DAS).

           domain
               Examina el árbol de JNDI para encontrar el dominio actual.

           cluster-name
               Examina el árbol de JNDI para el cluster especificado.

           instance-name
               Examina el árbol de JNDI para encontrar la instancia de
               GlassFish Server especificada.

EJEMPLOS
       Ejemplo 1, Examen del árbol de JNDI
           En este ejemplo se examina el árbol de JNDI para encontrar la
           instancia de GlassFish Server por defecto.

               asadmin> list-jndi-entries
               java:global: com.sun.enterprise.naming.impl.TransientContext
               jdbc: com.sun.enterprise.naming.impl.TransientContext
               ejb: com.sun.enterprise.naming.impl.TransientContext
               com.sun.enterprise.container.common.spi.util.InjectionManager:
               com.sun.enterprise.container.common.impl.util.InjectionManagerImpl

               Command list-jndi-entries executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jndi-resource(1), delete-jndi-resource(1),
       list-jndi-resources(1)

       asadmin(1M)

Java EE 7                   7 de diciembre de 2010        list-jndi-entries(1)
