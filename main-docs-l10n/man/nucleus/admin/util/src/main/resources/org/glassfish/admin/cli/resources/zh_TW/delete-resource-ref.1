delete-resource-ref(1)      asadmin 公用程式子命令      delete-resource-ref(1)

參考名
       delete-resource-ref - 移除資源的參照

概要
           delete-resource-ref [--help] [--target target] reference_name

描述
       delete-resource-ref 子命令會從叢集或非叢集的伺服器執行處理移除資源
       (例如，JDBC 資源) 的參照。這會導致從目標執行處理或叢集的 JNDI
       樹狀結構移除資源。

       這個子命令不需要目標執行處理或構成叢集的執行處理，更不需執行它們，便可成功執行。如果一個或多個執行處理無法使用，它們將在下次啟動時不再載入
       JNDI 樹狀結構中的資源。

       移除參照不會導致資源從網域移除。此資源只能使用該資源的 delete
       子命令移除 (例如，delete-jdbc-resource)。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要從中移除資源參照的目標。 有效值包括

           server
               從預設伺服器執行處理 server 移除資源參照，且此為預設值。

           cluster_name
               從叢集中的每個伺服器執行處理移除資源參照。

           instance_name
               從指定的非叢集的伺服器執行處理移除資源參照。

運算元
       reference_name
           資源的名稱或 JNDI 名稱。

範例
       範例 1, 移除資源的參照
           此範例會移除叢集 cluster1 上 JMS 目的地資源 jms/Topic 的參照。

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 20 日        delete-resource-ref(1)
