delete-resource-ref(1)    asadmin 유틸리티 하위 명령    delete-resource-ref(1)

제목
       delete-resource-ref - 리소스에 대한 참조 제거

시놉시스
           delete-resource-ref [--help] [--target target] reference_name

설명
       delete-resource-ref 하위 명령은 클러스터 또는 클러스터되지 않은 서버
       인스턴스에서 리소스(예: JDBC 리소스)에 대한 참조를 제거합니다. 이는
       대상 인스턴스 또는 클러스터의 JNDI 트리에서 리소스를 제거하는 데
       효과적으로 작용합니다.

       클러스터를 구성하는 대상 인스턴스(하나 또는 여러 개)가 실행 중이거나
       사용 가능한 상태가 아니어도 이 하위 명령은 성공적으로 실행됩니다. 하나
       이상의 인스턴스를 사용할 수 없는 경우 해당 인스턴스는 다음 시작 시 JNDI
       트리의 리소스를 더 이상 로드하지 않습니다.

       참조를 제거하더라도 도메인에서 리소스가 제거되지는 않습니다. 리소스는
       해당 리소스에 대한 delete 하위 명령(예: delete-jdbc-resource)에
       의해서만 제거됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           리소스 참조를 제거할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스 server에서 리소스 참조를 제거하고 이는
               기본값입니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에서 리소스 참조를
               제거합니다.

           instance_name
               이름이 지정된 클러스터되지 않은 서버 인스턴스에서 리소스 참조를
               제거합니다.

피연산자
       reference_name
           리소스의 이름 또는 JNDI 이름입니다.

예제
       예 1, 리소스에 대한 참조 제거
           이 예제는 클러스터 cluster1에서 JMS 대상 리소스 jms/Topic에 대한
           참조를 제거합니다.

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                       2011년 6월 20일         delete-resource-ref(1)
