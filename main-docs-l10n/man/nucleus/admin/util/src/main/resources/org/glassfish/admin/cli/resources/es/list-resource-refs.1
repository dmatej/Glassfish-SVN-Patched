list-resource-refs(1)   Subcomandos de la Utilidad asa   list-resource-refs(1)

NOMBRE DE REFERENCIA
       list-resource-refs - muestra las referencias existentes a un dominio.

SINOPSIS
           list-resource-refs [--help] [target]

DESCRIPCIÓN
       El subcomando list-resource-refs muestra todas las referencias de
       recursos de un cluster o de una instancia de servidor sin cluster. El
       resultado es el listado de todos los recursos (por ejemplo, recursos
       JDBC) disponibles en el árbol JNDI del destino especificado.

       No es necesario que la instancia o instancias de destino del cluster se
       estén ejecutando o estén disponibles para que este subcomando se
       ejecute correctamente.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           El destino del que se enumeran las referencias de recursos. Los
           destinos válidos son los siguientes:

           server
               Muestra las referencias de recursos para la instancia de
               servidor por defecto y es el destino por defecto.

           cluster_name
               Muestra las referencias de recursos para todas las instancias
               de servidor del cluster.

           instance_name
               Muestra las referencias de recursos de la instancia de servidor
               sin cluster especificada.

EJEMPLOS
       Ejemplo 1, Lista de Referencias de Recursos para un Cluster
           En este ejemplo se muestran las referencias de recursos para el
           cluster cluster1.

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                     20 de junio de 2011        list-resource-refs(1)
