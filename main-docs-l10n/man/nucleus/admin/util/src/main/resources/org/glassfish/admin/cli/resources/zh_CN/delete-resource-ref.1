delete-resource-ref(1)      asadmin 实用程序子命令      delete-resource-ref(1)

名称
       delete-resource-ref - 删除资源引用

提要
           delete-resource-ref [--help] [--target target] reference_name

说明
       delete-resource-ref 子命令从集群或非集群服务器实例删除对资源 (例如 JDBC
       资源) 的引用。这将有效地从目标实例或集群的 JNDI 树中删除资源。

       无需为了成功运行此子命令而运行或提供构成集群的目标实例。如果一个或多个实例不可用，则在下次实例时将不再加载
       JNDI 树中的资源。

       删除引用不会导致从域中删除资源。仅当对资源使用 delete 子命令
       (例如，delete-jdbc-resource) 时，才会删除该资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从中删除资源引用的目标。有效值为

           server
               从默认服务器实例 server 删除资源引用，这是默认值。

           cluster_name
               从集群中的每个服务器实例删除资源引用。

           instance_name
               从命名的非集群服务器实例删除资源引用。

操作数
       reference_name
           资源的名称或 JNDI 名称。

示例
       Example 1, 删除资源引用
           此示例删除对集群 cluster1 上 JMS 目标资源 jms/Topic 的引用。

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 20 日        delete-resource-ref(1)
