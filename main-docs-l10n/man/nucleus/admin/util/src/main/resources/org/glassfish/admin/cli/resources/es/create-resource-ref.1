create-resource-ref(1)  Subcomandos de la Utilidad asa  create-resource-ref(1)

NOMBRE DE REFERENCIA
       create-resource-ref - Crea una referencia a un recurso.

SINOPSIS
           create-resource-ref [--help] [--target target]
           [--enabled={false|true}] reference_name

DESCRIPCIÓN
       El subcomando create-resource-ref crea una referencia desde un cluster
       o desde una instancia de servidor sin cluster a un recurso creado
       anteriormente; por ejemplo, un recurso JDBC creado mediante el
       subcomando create-jdbc-resource. El resultado es que el recurso queda
       disponible en el árbol JNDI del cluster o de la instancia.

       No es necesario que la instancia o instancias de destino que
       constituyen el cluster se estén ejecutando o estén disponibles para que
       este subcomando se ejecute correctamente. Si una o más instancias no
       están disponibles, recibirán el nuevo recurso la próxima vez que se
       inicien.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Los resource-ref sólo se   │
           │           pueden crear para recursos │
           │           vinculables, como          │
           │           jdbc-resource,             │
           │           connector-resource ,       │
           │           admin-object-resource,     │
           │           mail-resource,             │
           │            custom-resource o         │
           │           jndi-resource.             │
           │                                      │
           │           Las aplicaciones no hacen  │
           │           referencia directamente a  │
           │           los                        │
           │            jdbc-connection-pool ni a │
           │           los                        │
           │           connector-connection-pool. │
           │           En su lugar, se les hace   │
           │           referencia a través de un  │
           │           jdbc-resource o un         │
           │           connector-resource,        │
           │           respectivamente.           │
           └──────────────────────────────────────┘

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino para el que va a crear la referencia de
           recurso. Los destinos válidos son los siguientes:

           server
               Crea la referencia de recurso para la instancia de servidor por
               defecto. Éste es el destino por defecto.

           cluster_name
               Crea la referencia de recurso para cada instancia de servidor
               en el cluster.

           instance_name
               Crea la referencia de recurso para la instancia de servidor sin
               cluster con nombre.

       --enabled
           Indica si el recurso debe activarse. Este valor sólo se aplicará si
           el recurso está activado a nivel global. El valor por defecto es
           true.

OPERANDOS
       reference_name
           El nombre o nombre de JNDI del recurso.

EJEMPLOS
       Ejemplo 1, Creación de una Referencia a un Recurso de Destino de JMS
           En este ejemplo se crea una referencia al recurso de destino de JMS
           jms/Topic en el cluster Cluster1.

               asadmin> create-resource-ref --target Cluster1 jms/Topic
               resource-ref jms/Topic created successfully.
               Command create-resource-ref executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                     20 de junio de 2011       create-resource-ref(1)
