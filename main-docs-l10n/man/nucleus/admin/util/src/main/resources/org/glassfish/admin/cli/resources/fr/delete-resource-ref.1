delete-resource-ref(1)  Sous-commandes de l'utilitaire  delete-resource-ref(1)

NOM
       delete-resource-ref - enlève une référence à une ressource

SYNTAXE
           delete-resource-ref [--help] [--target target] reference_name

DESCRIPTION
       La sous-commande delete-resource-ref enlève une référence à une
       ressource (par exemple, une ressource JDBC) d'un cluster ou d'une
       instance de serveur non incluse dans un cluster. Cela entraîne la
       suppression de la ressource dans l'arborescence JNDI de l'instance ou
       du cluster cible.

       Il n'est pas nécessaire que les instances cible formant le cluster
       soient en cours d'exécution ou disponibles pour lancer cette
       sous-commande. Si certaines instances ne sont pas disponibles, elles ne
       chargeront plus la ressource dans l'arborescence JNDI lors de leur
       prochain démarrage.

       La suppression de la référence n'entraîne pas la suppression de la
       ressource du domaine. La ressource est enlevée uniquement par la
       sous-commande delete correspondant à cette ressource (par exemple,
       delete-jdbc-resource).

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Spécifie la cible de laquelle enlever la référence à la ressource.
           Les valeurs valides sont les suivantes :

           server
               Enlève la référence à la ressource de l'instance de serveur par
               défaut ( server). Il s'agit de la valeur par défaut.

           cluster_name
               Enlève la référence à la ressource de chaque instance de
               serveur du cluster.

           instance_name
               Enlève la référence à la ressource de l'instance de serveur
               nommée non incluse dans un cluster.

OPÉRANDES
       reference_name
           Nom ou nom JNDI de la ressource.

EXEMPLES
       Exemple 1, Suppression d'une référence à une ressource
           Dans cet exemple, une référence à la ressource de destination JMS
           jms/Topic sur le cluster cluster1 est enlevée.

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                         20 jui 2011           delete-resource-ref(1)
