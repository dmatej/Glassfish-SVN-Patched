list-resource-refs(1)   Sous-commandes de l'utilitaire   list-resource-refs(1)

NOM
       list-resource-refs - répertorie les références existantes à une
       ressource

SYNTAXE
           list-resource-refs [--help] [target]

DESCRIPTION
       La sous-commande list-resource-refs répertorie toutes les références à
       une ressource dans un cluster ou une instance de serveur non incluse
       dans un cluster. Elle répertorie en réalité toutes les ressources (par
       exemple, les ressources JDBC) disponibles dans l'arborescence JNDI de
       la cible spécifiée.

       Les instances cible du cluster n'ont pas besoin d'être en cours
       d'exécution ou disponibles pour que cette sous-commande soit
       correctement exécutée.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Cible pour laquelle répertorier les références à une ressource. Les
           valeurs valides sont les suivantes :

           server
               Répertorie les références à une ressource pour l'instance de
               serveur par défaut. Il s'agit de la cible par défaut.

           cluster_name
               Répertorie les références à une ressource pour chaque instance
               de serveur du cluster.

           instance_name
               Répertorie les références à une ressource pour l'instance de
               serveur non incluse dans un cluster nommée.

EXEMPLES
       Exemple 1, Liste des références à une ressource pour un cluster
           Dans cet exemple, les références à une ressource sont répertoriées
           pour le cluster cluster1.

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                         20 jui 2011            list-resource-refs(1)
