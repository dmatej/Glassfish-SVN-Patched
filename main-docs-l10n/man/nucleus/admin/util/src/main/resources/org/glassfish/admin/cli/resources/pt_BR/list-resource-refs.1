list-resource-refs(1)   Subcomandos do utilitário asad   list-resource-refs(1)

NOME
       list-resource-refs - lista as referências existentes a um recurso

SINOPSE
           list-resource-refs [--help] [target]

DESCRIçãO
       O subcomando list-resource-refs lista todas as referências de recurso
       em um cluster ou em uma instância de servidor não clusterizado. Isso
       efetivamente lista todos os recursos (por exemplo, recursos JDBC)
       disponíveis na árvore JNDI do alvo especificado.

       A instância ou instâncias do alvo do cluster não precisam estar em
       execução ou disponíveis para que esse subcomando seja bem sucedido.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           O alvo para o qual você está listando as referências de recurso. Os
           alvos válidos são:

           server
               Lista as referências de recurso para a instância do servidor
               default e é o alvo default.

           cluster_name
               Lista as referências de recurso para cada instância no cluster.

           instance_name
               Lista as referências de recurso para a instância do servidor
               não clusterizado nomeado.

EXEMPLOS
       Example 1, Listando Referências de Recurso de um Cluster
           Este exemplo lista as referências do recurso do cluster cluster1.

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                         20 jun 2011            list-resource-refs(1)
