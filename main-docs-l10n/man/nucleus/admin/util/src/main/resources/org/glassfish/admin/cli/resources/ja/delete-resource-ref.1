delete-resource-ref(1)asadminユーティリティのサブコマンド

名前
       delete-resource-ref - リソースへの参照を除去します

概要
           delete-resource-ref [--help] [--target target] reference_name

説明
       delete-resource-refサブコマンドは、クラスタまたはクラスタ化されていないサーバー・インスタンスからリソース(JDBCリソースなど)への参照を除去します。これによって、リソースがターゲットのインスタンスやクラスタのJNDIツリーから除去できるようになります。

       このサブコマンドを正常に実行するために、クラスタを構成しているターゲットのインスタンスが実行中または使用可能である必要はありません。1つ以上のインスタンスが使用できない場合は、次にインスタンスを起動したときにJNDIツリーにリソースはロードされません。

       参照を除去しても、リソースはドメインから除去されません。リソースは、そのリソースのdeleteサブコマンド(delete-jdbc-resourceなど)でのみ除去されます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           リソース参照を除去するターゲットを指定します。有効な値は次のとおりです

           server
               デフォルトのサーバー・インスタンスserverからリソース参照を除去します。これはデフォルト値です。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスからリソース参照を除去します。

           instance_name
               名前付きのクラスタ化されていないサーバー・インスタンスからリソース参照を除去します。

オペランド
       reference_name
           リソースの名前またはJNDI名。

例
       例 1, リソースへの参照の除去
           この例では、JMS宛先リソースjms/Topicへの参照をクラスタcluster1から除去します。

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                        2011年6月20日          delete-resource-ref(1)
