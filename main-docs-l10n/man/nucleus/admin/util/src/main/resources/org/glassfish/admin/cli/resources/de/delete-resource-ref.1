delete-resource-ref(1)  Unterbefehle des asadmin-Utili  delete-resource-ref(1)

NAME
       delete-resource-ref - entfernt eine Referenz auf eine Ressource

ZUSAMMENFASSUNG
           delete-resource-ref [--help] [--target target] reference_name

BESCHREIBUNG
       Der Unterbefehl delete-resource-ref entfernt eine Referenz aus einem
       Cluster oder einer nicht geclusterten Serverinstanz zu einer Ressource
       (beispielsweise eine JDBC-Ressource). Dadurch wird die Ressource aus
       der JNDI-Baumstruktur der Zielinstanz oder des Clusters entfernt.

       Die Zielinstanz oder die Instanzen, aus denen das Cluster besteht,
       müssen nicht ausgeführt werden oder verfügbar sein, damit dieser
       Unterbefehl erfolgreich ausgeführt werden kann. Wenn eine oder mehrere
       Instanzen nicht verfügbar sind, laden sie nicht mehr die Ressource in
       die JNDI-Baumstruktur, wenn sie das nächste Mal gestartet werden.

       Durch das Entfernen der Referenz wird die Ressource nicht aus der
       Domain entfernt. Die Ressource wird nur durch den jeweiligen
       Unterbefehl delete für die entsprechende Ressource (beispielsweise
       delete-jdbc-resource) entfernt.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie die Ressourcenreferenz entfernen.
           Gültige Werte sind

           server
               Entfernt die Ressourcenreferenz aus der standardmäßigen
               Serverinstanz server und ist der Standardwert.

           cluster_name
               Entfernt die Ressourcenreferenz aus jeder Serverinstanz im
               Cluster.

           instance_name
               Entfernt die Ressourcenreferenz aus der benannten nicht
               geclusterten Serverinstanz.

OPERANDEN
       reference_name
           Der Name bzw. JNDI-Name der Ressource.

BEISPIELE
       Example 1, Referenzen zu einer Ressource entfernen
           In diesem Beispiel wird eine Referenz auf die JMS-Zielressource
           jms/Topic im Cluster cluster1 entfernt.

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                        20. Jun. 2011          delete-resource-ref(1)
