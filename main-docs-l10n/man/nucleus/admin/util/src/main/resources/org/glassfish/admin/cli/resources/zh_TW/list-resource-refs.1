list-resource-refs(1)       asadmin 公用程式子命令       list-resource-refs(1)

參考名
       list-resource-refs - 列出現有的資源參照

概要
           list-resource-refs [--help] [target]

描述
       list-resource-refs
       子命令會列出叢集或非叢集伺服器執行處理中的所有資源參照。此子命令會有效地列出指定目標之
       JNDI 樹狀結構中，所有可用的資源 (例如 JDBC 資源)。

       這個子命令不需要執行或供叢集中的目標執行處理使用，即可成功執行。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           列出資源參照的目標。有效的目標如下：

           server
               列出預設伺服器執行處理的資源參照，且為預設目標。

           cluster_name
               列出叢集中每個伺服器執行處理的資源參照。

           instance_name
               列出已命名的非叢集伺服器執行處理之資源參照。

範例
       範例 1, 列出叢集的資源參照
           此範例會列出叢集 cluster1 的資源參照。

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 20 日         list-resource-refs(1)
