list-resource-refs(1)       asadmin 实用程序子命令       list-resource-refs(1)

名称
       list-resource-refs - 列出现有的资源引用

提要
           list-resource-refs [--help] [target]

说明
       list-resource-refs
       子命令列出集群或非集群服务器实例中的所有资源引用。这可以有效地列出指定目标的
       JNDI 树中可用的所有资源 (例如，JDBC 资源)。

       无需为了成功运行此子命令而运行或提供集群中的目标实例。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           要列出资源引用的目标。有效目标如下：

           server
               列出默认服务器实例的资源引用，并且这是默认目标。

           cluster_name
               列出集群中每个服务器实例的资源引用。

           instance_name
               列出命名的非集群服务器实例的资源引用。

示例
       Example 1, 列出集群的资源引用
           此示例列出了集群 cluster1 的资源引用。

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 20 日         list-resource-refs(1)
