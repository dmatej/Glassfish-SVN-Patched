delete-resource-ref(1)  Subcomandos de la Utilidad asa  delete-resource-ref(1)

NOMBRE DE REFERENCIA
       delete-resource-ref - Elimina una referencia a un recurso.

SINOPSIS
           delete-resource-ref [--help] [--target target] reference_name

DESCRIPCIÓN
       El subcomando delete-resource-ref elimina una referencia a un recurso
       (por ejemplo, un recurso JDBC) de un cluster o una instancia de
       servidor sin cluster. El resultado es la eliminación del recurso del
       árbol JNDI de la instancia o un cluster de destino.

       No es necesario que la instancia o instancias de destino que
       constituyen el cluster se estén ejecutando o estén disponibles para que
       este subcomando se ejecute correctamente. Si una o más instancias no
       están disponibles, éstas no cargarán el recurso en el árbol JNDI la
       próxima vez que se inicien.

       La eliminación de la referencia no conlleva la eliminación del recurso
       en el dominio. El recurso se elimina únicamente mediante el subcomando
       delete de ese recurso (por ejemplo, delete-jdbc-resource).

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va a eliminar la referencia de
           recurso. Los valores válidos son:

           server
               Elimina la referencia de recurso de la instancia de servidor
               por defecto server y es el valor por defecto.

           cluster_name
               Elimina la referencia de recurso de todas las instancias de
               servidor del cluster.

           instance_name
               Elimina la referencia de recurso de la instancia de servidor
               sin cluster especificada.

OPERANDOS
       reference_name
           El nombre o nombre de JNDI del recurso.

EJEMPLOS
       Ejemplo 1, Eliminación de una Referencia a un Recurso
           Este ejemplo elimina una referencia al recurso de destino de JMS
           jms/Topic en el cluster cluster1.

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

Java EE 7                     20 de junio de 2011       delete-resource-ref(1)
