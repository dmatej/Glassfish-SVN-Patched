list-resource-refs(1)     asadmin 유틸리티 하위 명령     list-resource-refs(1)

제목
       list-resource-refs - 기존 리소스 참조 나열

시놉시스
           list-resource-refs [--help] [target]

설명
       list-resource-refs 하위 명령은 클러스터 또는 클러스터화되지 않은 서버
       인스턴스의 모든 리소스 참조를 나열합니다. 이 하위 명령은 지정된 대상의
       JNDI 트리에서 사용 가능한 모든 리소스(예: JDBC 리소스)를 효과적으로
       나열합니다.

       이 하위 명령을 성공적으로 수행하기 위해 클러스터의 대상 인스턴스(하나
       또는 여러 개)가 실행 중이거나 사용 가능해야 할 필요는 없습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           리소스 참조를 나열할 대상입니다. 적합한 대상은 다음과 같습니다.

           server
               기본 서버 인스턴스의 리소스 참조를 나열하며 기본 대상입니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스의 리소스 참조를 나열합니다.

           instance_name
               명명된 클러스터화되지 않은 서버 인스턴스에 대한 리소스 참조를
               나열합니다.

예제
       예 1, 클러스터에 대한 리소스 참조 나열
           이 예제는 클러스터 cluster1에 대한 리소스 참조를 나열합니다.

               asadmin> list-resource-refs cluster1
               jms/Topic
               Command list-resource-refs executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-resource-ref(1), delete-resource-ref(1)

       asadmin(1M)

Java EE 7                       2011년 6월 20일          list-resource-refs(1)
