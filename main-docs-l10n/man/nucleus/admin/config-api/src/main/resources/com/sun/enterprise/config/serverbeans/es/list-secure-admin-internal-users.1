list-secure-admin-internSubcomandos de la Ulist-secure-admin-internal-users(1)

NOMBRE DE REFERENCIA
       list-secure-admin-internal-users - muestra los nombres de usuario que
       el DAS y las instancias de GlassFish Server utilizan para autenticarse
       y para autorizar operaciones administrativas.

SINOPSIS
           list-secure-admin-internal-users [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

DESCRIPCIÓN
       El subcomando list-secure-admin-internal-users muestra los nombres de
       usuario que el DAS y las instancias de GlassFish Server utilizan para
       autenticarse y para autorizar operaciones administrativas.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --long, -l
           Muestra información detallada sobre cada usuario interno. El valor
           por defecto es false.

       --output, -o
           Muestra detalles específicos sobre cada usuario interno. Utilice
           una lista separada por comas para especificar los detalles que
           desea mostrar y el orden de las mismas. Los valores son sensibles a
           mayúsculas/minúsculas.

           Los valores posibles son los siguientes:

           username
               Muestra el nombre de usuario para el usuario interno.

           password-alias
               Muestra el alias de contraseña para el usuario interno.

       --header, -h
           Especifica si los encabezados de columna se muestran cuando se usa
           la opción --long. El valor por defecto es true. Para suprimir los
           encabezados, defina la opción --header en false.

OPERANDOS
       name
           El nombre de usuario para el usuario interno para el que desea
           mostrar los detalles.

EJEMPLOS
       Ejemplo 1, Visualización del nombre de usuario para administración
       segura
           En este ejemplo se muestran los nombres de usuario que el DAS y las
           instancias de GlassFish Server utilizan para autenticarse y para
           autorizar operaciones administrativas.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                    13 de febrero list-secure-admin-internal-users(1)
