create-module-config(1)   asadmin 유틸리티 하위 명령   create-module-config(1)

제목
       create-module-config - domain.xml에 모듈의 기본 구성 추가

시놉시스
           create-module-config [--help]
           [--dryrun[={false|true}]]
           [--all[={false|true}]]
           [--target target]
           servicename

설명
       create-module-config 하위 명령은 domain.xml에 모듈의 기본 구성을
       추가합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --dryrun
           모듈의 기본 구성을 표시하지만 domain.xml에 추가하지 않습니다.

           기본값은 false입니다.

       --all
           수행하지 않은 경우 모듈의 모든 기본 구성을 domain.xml에 추가합니다.

           기본값은 false입니다.

       --target
           모듈의 기본 구성을 추가할 대상을 지정합니다.

           가능한 값은 다음과 같습니다.

           server
               모듈의 기본 구성을 기본 서버 인스턴스에 대한 domain.xml에
               추가합니다. 이 값은 기본값입니다.

           domain
               모듈의 기본 구성을 기본 도메인에 대한 domain.xml에 추가합니다.

           cluster-name
               모듈의 기본 구성을 지정된 클러스터에 대한 domain.xml에
               추가합니다.

           instance-name
               모듈의 기본 구성을 지정된 인스턴스에 대한 domain.xml에
               추가합니다.

피연산자
       servicename
           모듈의 이름입니다. 모듈의 기본 구성을 대상의 domain.xml에
           추가합니다.

예제
       예 1, domain.xml에 기본 구성 추가
           이 예제는 웹 컨테이너 모듈의 기본 구성을 server-config(기본 구성)에
           있는 domain1의 domain.xml 파일에 추가합니다.  domain.xml에 추가하기
           전에 구성을 미리 보려면 --dryrun 옵션을 사용합니다.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                       2013년 2월 6일         create-module-config(1)
