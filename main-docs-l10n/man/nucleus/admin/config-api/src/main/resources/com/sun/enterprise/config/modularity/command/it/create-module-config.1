create-module-config(1) Comandi secondari della utilit create-module-config(1)

NOME
       create-module-config - aggiunge la configurazione predefinita di un
       modulo al file domain.xml

SINTASSI
           create-module-config [--help]
           [--dryrun[={false|true}]]
           [--all[={false|true}]]
           [--target target]
           servicename

DESCRIZIONE
       Il comando secondario create-module-config aggiunge la configurazione
       predefinita di un modulo al file domain.xml.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --dryrun
           Visualizza la configurazione predefinita di un modulo senza
           aggiungerla al file domain.xml.

           Il valore predefinito è false.

       --all
           Aggiunge tutte le configurazioni predefinite dei moduli al file
           domain.xml se il file non le contiene già.

           Il valore predefinito è false.

       --target
           Specifica la destinazione alla quale verrà aggiunta la
           configurazione predefinita del modulo.

           Di seguito sono riportati i valori possibili.

           server
               Aggiunge la configurazione predefinita di un modulo al file
               domain.xml per l'istanza server predefinita. Questo è il valore
               predefinito.

           domain
               Aggiunge la configurazione predefinita di un modulo al file
               domain.xml per il dominio predefinito.

           cluster-name
               Aggiunge la configurazione predefinita di un modulo al file
               domain.xml per il cluster specificato.

           instance-name
               Aggiunge la configurazione predefinita di un modulo al file
               domain.xml per l'istanza specificata.

OPERANDI
       servicename
           Il nome del modulo. La configurazione predefinita del modulo verrà
           aggiunta al file domain.xml della destinazione.

ESEMPI
       Esempio 1, Aggiunta di una configurazione predefinita al file
       domain.xml
           In questo esempio viene aggiunta la configurazione predefinita del
           modulo contenitore Web al file domain.xml di domain1 in
           server-config (la configurazione predefinita). Per visualizzare in
           anteprima la configurazione prima di aggiungerla al file
           domain.xml, usare l'opzione --dryrun.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                       6 febbraio 2013        create-module-config(1)
