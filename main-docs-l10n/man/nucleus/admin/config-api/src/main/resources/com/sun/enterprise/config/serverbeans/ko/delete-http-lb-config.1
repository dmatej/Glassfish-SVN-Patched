delete-http-lb-config(1)  asadmin 유틸리티 하위 명령  delete-http-lb-config(1)

제목
       delete-http-lb-config - 로드 밸런서 구성 삭제

시놉시스
           delete-http-lb-config [--help] config_name

설명
       로드 밸런서 구성을 삭제하려면 delete-http-lb-config 하위 명령을
       사용합니다. 로드 밸런서 구성은 로드 균형 조정에 대해 사용으로 설정된
       클러스터 또는 서버 인스턴스를 참조하지 않아야 합니다. 또한 물리적 로드
       밸런서에서 로드 밸런서 구성을 참조하지 않아야 합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       config_name
           삭제할 로드 밸런서 구성의 이름입니다. 구성은 로드 균형 조정에 대해
           사용으로 설정되거나 물리적 로드 밸런서에서 사용하는 클러스터 또는
           서버 인스턴스를 참조하지 않아야 합니다.

예제
       예 1, 로드 밸런서 구성 삭제
           이 예제는 mylbconfig라는 로드 밸런서 구성을 삭제합니다.

               asadmin> delete-http-lb-config mylbconfig
               Command delete-http-lb-config executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                       2010년 8월 25일       delete-http-lb-config(1)
