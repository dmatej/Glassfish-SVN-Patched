delete-http-lb-config(1)Unterbefehle des asadmin-Utilidelete-http-lb-config(1)

NAME
       delete-http-lb-config - löscht eine Load Balancer-Konfiguration

ZUSAMMENFASSUNG
           delete-http-lb-config [--help] config_name

BESCHREIBUNG
       Verwenden Sie den Unterbefehl delete-http-lb-config, um eine Load
       Balancer-Konfiguration zu löschen. Die Load Balancer-Konfiguration darf
       keine Cluster oder Serverinstanzen referenzieren, die für Load
       Balancing aktiviert sind. Außerdem dürfen physische Load Balancer nicht
       die Load Balancer-Konfiguration referenzieren.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       config_name
           Der Name der Load Balancer-Konfiguration, die gelöscht werden soll.
           Die Konfiguration darf keine Cluster oder Serverinstanzen
           referenzieren, die für Load Balancing aktiviert sind, und darf
           nicht für physische Load Balancer verwendet werden.

BEISPIELE
       Beispiel 1, Load Balancer-Konfigurationen löschen
           In diesem Beispiel wird eine Load Balancer-Konfiguration mit dem
           Namen mylbconfig gelöscht.

               asadmin> delete-http-lb-config mylbconfig
               Command delete-http-lb-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                        25. Aug. 2010        delete-http-lb-config(1)
