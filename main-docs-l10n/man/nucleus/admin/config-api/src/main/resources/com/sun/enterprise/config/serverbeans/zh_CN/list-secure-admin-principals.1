list-secure-admin-principalsasadmin 实用程序子命

名称
       list-secure-admin-principals - 列出使 GlassFish Server
       接受来自客户机的管理请求的证书。

提要
           list-secure-admin-principals [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

说明
       list-secure-admin-principals 子命令列出使 GlassFish Server
       接受来自客户机的管理请求的证书。

选项
       --help, -?
           显示子命令的帮助文本。

       --long, -l
           显示有关使 GlassFish Server
           接受来自客户机的管理请求的证书详细信息。默认值为假

       --output, -o
           显示有关使 GlassFish Server
           接受来自客户机的管理请求的证书特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。值不区分大小写。

           可能的值如下：

           DN
               显示每个证书的唯一判别名 (DN)。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

操作数
       name
           证书的唯一判别名，使用位于引号中的逗号分隔列表指定。例如："CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US"。

示例
       Example 1, 列出证书
           此示例列出使 GlassFish Server 接受来自客户机的管理请求的证书。

               asadmin> list-secure-admin-principals
               CN=localhost,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               CN=localhost-instance,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               Command list-secure-admin-principals executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日
