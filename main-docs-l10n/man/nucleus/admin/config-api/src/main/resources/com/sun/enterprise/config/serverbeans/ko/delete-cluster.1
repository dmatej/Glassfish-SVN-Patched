delete-cluster(1)         asadmin 유틸리티 하위 명령         delete-cluster(1)

제목
       delete-cluster - GlassFish Server 클러스터 삭제

시놉시스
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

설명
       delete-cluster 하위 명령은 GlassFish Server 클러스터를 삭제합니다.
       클러스터에 GlassFish Server 인스턴스가 없는 경우에만 클러스터를 삭제할
       수 있습니다. 삭제할 클러스터에 인스턴스가 있는 경우 클러스터를 삭제하기
       전에 인스턴스를 정지 및 삭제합니다.

       클러스터의 명명된 구성이 클러스터에 대해 자동으로 생성되었고 다른
       클러스터나 클러스터되지 않은 인스턴스가 구성을 참조하지 않으면 클러스터
       삭제 시 구성이 삭제됩니다. 클러스터에 대해 자동으로 생성되는 구성은
       명명된 cluster-name-config입니다. 여기서 cluster-name은 클러스터의
       이름입니다.

       이 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --autohadboverride
           이 옵션은 지정하지 마십시오. 이 옵션은 이전 릴리스와의 호환성을
           위해 보존됩니다. 이 옵션을 지정하면 구문 오류가 발생하지 않습니다.
           대신, 하위 명령이 성공적으로 실행되고 옵션이 무시되었다는 경고
           메시지가 표시됩니다.

       --nodeagent
           이 옵션은 지정하지 마십시오. 이 옵션은 이전 릴리스와의 호환성을
           위해 보존됩니다. 이 옵션을 지정하면 구문 오류가 발생하지 않습니다.
           대신, 하위 명령이 성공적으로 실행되고 옵션이 무시되었다는 경고
           메시지가 표시됩니다.

피연산자
       cluster-name
           삭제할 클러스터의 이름입니다.

예제
       예 1, GlassFish Server 클러스터 삭제
           이 예제는 GlassFish Server 클러스터 adccluster를 삭제합니다.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                       2010년 12월 7일              delete-cluster(1)
