delete-http-lb-config(1)    asadmin 实用程序子命令    delete-http-lb-config(1)

名称
       delete-http-lb-config - 删除负载平衡器配置

提要
           delete-http-lb-config [--help] config_name

说明
       使用 delete-http-lb-config
       子命令删除负载平衡器配置。该负载平衡器配置不能引用为实现负载平衡而启用的任何集群或服务器实例。此外，该负载平衡器配置不能由任何物理负载平衡器引用。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       config_name
           要删除的负载平衡器配置的名称。此配置不能引用为实现负载平衡而启用的任何集群或服务器实例，也不能由任何物理负载平衡器使用。

示例
       Example 1, 删除负载平衡器配置
           此示例删除名为 mylbconfig 的负载平衡器配置

               asadmin> delete-http-lb-config mylbconfig
               Command delete-http-lb-config executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日      delete-http-lb-config(1)
