disable-secure-admin-intComandissecondaridisable-secure-admin-internal-user(1)

NOME
       disable-secure-admin-internal-user - Comunica al server DAS e alle
       istanze di GlassFish Server di non utilizzare l'utente amministratore
       specificato per eseguire l'autenticazione reciproca e per autorizzare
       le operazioni di amministrazione.

SINTASSI
           disable-secure-admin-internal-user
           [--help]
           admin-username

DESCRIZIONE
       Il comando secondario disable-secure-admin-internal-user disabilita
       l'amministrazione sicura in modo che non venga utilizzato il nome
       utente (invece del certificato SSL) per eseguire l'autenticazione
       reciproca del server DAS e delle istanze e per autorizzare le
       operazioni di amministrazione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       admin-username
           Il nome dell'utente amministratore che GlassFish Server non deve
           utilizzare per eseguire l'autenticazione reciproca del server DAS e
           delle istanze e per autorizzare le operazioni di amministrazione.

ESEMPI
       Esempio 1, Disabilitazione di un nome utente per l'amministrazione
       sicura
           Nell'esempio seguente viene descritto come disabilitare
           l'amministrazione sicura in modo da non utilizzare il nome utente
           tester per eseguire l'autenticazione reciproca del server DAS e
           delle istanze e per autorizzare le operazioni di amministrazione.

               asadmin> disable-secure-admin-internal-user tester

               Command disable-secure-admin-internal-user executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                       25 giugnodisable-secure-admin-internal-user(1)
