delete-module-config(1) Subcomandos de la Utilidad asa delete-module-config(1)

NOMBRE DE REFERENCIA
       delete-module-config - Elimina la configuración de un módulo de
       domain.xml

SINOPSIS
           delete-module-config [--help]
           [--target target]
           service_name

DESCRIPCIÓN
       El subcomando delete-module-config elimina la configuración de un
       módulo de domain.xml y hace que el módulo use la configuración por
       defecto incluida en el módulo.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va suprimir la configuración.

           Los valores posibles son los siguientes:

           server
               Suprime la configuración de la instancia de servidor por
               defecto. Este es el valor por defecto.

           domain
               Suprime la configuración del dominio por defecto.

           cluster-name
               Suprime la configuración del cluster especificado.

           instance-name
               Suprime la configuración de la instancia especificada.

OPERANDOS
       service_name
           Nombre del módulo para el que se va a eliminar la configuración.

EJEMPLOS
       Ejemplo 1, Supresión de una configuración por defecto de domain.xml
           En este ejemplo se suprime la configuración del módulo de
           contenedor web de domain1 en server-config (la configuración por
           defecto).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                    06 de febrero de 2013     delete-module-config(1)
