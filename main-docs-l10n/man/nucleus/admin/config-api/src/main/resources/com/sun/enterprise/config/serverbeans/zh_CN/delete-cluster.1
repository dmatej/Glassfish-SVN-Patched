delete-cluster(1)           asadmin 实用程序子命令           delete-cluster(1)

名称
       delete-cluster - 删除 GlassFish Server 集群

提要
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

说明
       delete-cluster 子命令删除 GlassFish Server 集群。只有集群不包含任何
       GlassFish Server
       实例时，才能删除该集群。如果您要删除的集群包含任何实例，应先停止并删除这些实例，再删除该集群。

       如果为集群自动创建了集群命名配置且没有任何其他集群或非集群实例引用该配置，则在删除该集群时将删除该配置。为集群自动创建的配置将命名为
       cluster-name-config，其中 cluster-name 是该集群的名称。

       该命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --autohadboverride
           不指定此选项。为了与早期发行版兼容而保留此选项。如果指定此选项，则不会出现语法错误。相反，子命令会成功运行，并显示忽略了此选项的警告消息。

       --nodeagent
           不指定此选项。为了与早期发行版兼容而保留此选项。如果指定此选项，则不会出现语法错误。相反，子命令会成功运行，并显示忽略了此选项的警告消息。

操作数
       cluster-name
           要删除的集群的名称。

示例
       Example 1, 删除 GlassFish Server 集群
           此示例删除 GlassFish Server 集群 adccluster。

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 7 日             delete-cluster(1)
