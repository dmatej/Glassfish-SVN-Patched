list-secure-admin-internal-uasadmin 公用程式

參考名
       list-secure-admin-internal-users - 列出 GlassFish Server DAS
       和執行處理在進行相互認證以及授權管理作業時會用到的使用者名稱。

概要
           list-secure-admin-internal-users [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

描述
       list-secure-admin-internal-users 子命令會列出 GlassFish Server DAS
       和執行處理進行相互認證以及授權管理作業時會用到的使用者名稱。

選項
       --help, -?
           顯示子命令的說明文字。

       --long, -l
           顯示與每個內部使用者有關的詳細資訊。預設值為 false。

       --output, -o
           顯示與每個內部使用者有關的特定詳細資訊。使用以逗號區隔的清單來指定您要顯示的詳細資訊和顯示順序。值有大小寫之分。

           可能值如下：

           username
               顯示內部使用者的使用者名稱。

           password-alias
               顯示內部使用者的密碼別名。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

運算元
       name
           您要顯示其詳細資訊之內部使用者的使用者名稱。

範例
       範例 1, 列出安全管理的使用者名稱
           此範例會列出 GlassFish Server DAS
           和執行處理進行相互認證以及授權管理作業時會用到的使用者名稱。

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 list-secure-admin-internal-users(1)
