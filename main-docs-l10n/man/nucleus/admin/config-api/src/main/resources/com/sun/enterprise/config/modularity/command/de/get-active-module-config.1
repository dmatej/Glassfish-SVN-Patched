get-active-module-configUnterbefehle des asadmin-Utget-active-module-config(1)

NAME
       get-active-module-config - zeigt die derzeit aktive Konfiguration eines
       Service oder einer Instanz an

ZUSAMMENFASSUNG
           get-active-module-config [--help]
           [--target target]
           [--all[={false|true}]]
           [servicename]

BESCHREIBUNG
       Der Unterbefehl get-active-module-config zeigt die derzeit aktive
       Konfiguration eines Service oder einer Instanz an.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel für den Unterbefehl an.

           Mögliche Werte:

           server
               Zeigt die derzeit aktive Konfiguration eines Service für die
               Standardserverinstanz an. Dies ist der Standardwert.

           domain
               Zeigt die derzeit aktive Konfiguration eines Service für die
               Standarddomain an.

           cluster-name
               Zeigt die derzeit aktive Konfiguration eines Service für das
               angegebene Cluster an.

           instance-name
               Zeigt die derzeit aktive Konfiguration eines Service für die
               angegebene Instanz an.

       --all
           Zeigt alle derzeit aktiven Konfigurationen.

           Der Standardwert ist false.

OPERANDEN
       servicename
           Der Name des Moduls.

BEISPIELE
       Example 1, Derzeit aktive Konfiguration anzeigen
           In diesem Beispiel wird die derzeit aktive Konfiguration für den
           JMS-Service in server-config (der Standardkonfiguration) angezeigt.

               asadmin> get-active-module-config
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                        06. Feb. 2013     get-active-module-config(1)
