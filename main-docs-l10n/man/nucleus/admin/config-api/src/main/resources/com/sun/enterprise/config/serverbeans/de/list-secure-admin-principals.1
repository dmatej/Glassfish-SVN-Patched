list-secure-admin-princiUnterbefehle des asadmilist-secure-admin-principals(1)

NAME
       list-secure-admin-principals - listet die Zertifikate auf, für die
       GlassFish Server Admin-Anforderungen von Clients akzeptiert.

ZUSAMMENFASSUNG
           list-secure-admin-principals [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

BESCHREIBUNG
       Der Unterbefehl list-secure-admin-principals listet die Zertifikate
       auf, für die GlassFish Server Admin-Anforderungen von Clients
       akzeptiert.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --long, -l
           Zeigt detaillierte Informationen zu den Zertifikaten an, für die
           GlassFish Server Admin-Anforderungen von Clients akzeptiert. Der
           Standardwert ist false.

       --output, -o
           Zeigt bestimmte Details zu den Zertifikaten an, für die GlassFish
           Server Admin-Anforderungen von Clients akzeptiert. Mithilfe einer
           durch Komma getrennten Liste können Sie die anzuzeigenden Details
           und deren Reihenfolge festlegen. Die Werte sind von Groß- und
           Kleinschreibung unabhängig.

           Mögliche Werte:

           DN
               Zeigt den Distinguished Name (DN) jedes Zertifikats an.

       --header, -h
           Legt fest, ob bei Verwendung der Option --long Spaltenüberschriften
           angezeigt werden. Der Standardwert ist true. Um die Überschriften
           zu unterdrücken, legen Sie für die Option --header den Wert false
           fest.

OPERANDEN
       name
           Der Distinguished Name (DN) des Zertifikats wird als eine durch
           Komma getrennte Liste in Anführungszeichen angegeben. Beispiel:
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US".

BEISPIELE
       Example 1, Zertifikate auflisten
           Das folgende Beispiel zeigt, wie Zertifikate aufgelistet werden,
           für die GlassFish Server Admin-Anforderungen von Clients
           akzeptiert.

               asadmin> list-secure-admin-principals
               CN=localhost,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               CN=localhost-instance,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               Command list-secure-admin-principals executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                        13. Feb. 2013 list-secure-admin-principals(1)
