get-active-module-configSubcomandos do utilitário aget-active-module-config(1)

NOME
       get-active-module-config - Exibe a configuração ativa atual de um
       serviço ou instância

SINOPSE
           get-active-module-config [--help]
           [--target target]
           [--all={false|true}]
           [service_name]

DESCRIçãO
       O subcomando get-active-module-config exibe a configuração ativa atual
       de um serviço ou instância.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo cuja configuração ativa atual de um serviço
           específico ou de uma instância inteira você deseja exibir.

           Os valores possíveis são os seguintes:

           server
               Exibe a configuração ativa atual da instância default do
               servidor. Este é o valor default.

           domain
               Exibe a configuração ativa atual do domínio default.

           cluster-name
               Exibe a configuração ativa atual do cluster especificado.

           instance-name
               Exibe a configuração ativa atual da instância especificada.

       --all
           Exibe todas as configurações ativas atuais.

           O valor default é false.

OPERANDOS
       nome_do_serviço
           O nome do módulo cuja configuração ativa atual você deseja ver.

EXEMPLOS
       Example 1, Exibindo a Configuração Ativa Atual
           Este exemplo exibe a configuração ativa atual do serviço JMS no
           server-config (a configuração default).

               asadmin> get-active-module-config jms-service
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                   6 de fevereiro de 2013 get-active-module-config(1)
