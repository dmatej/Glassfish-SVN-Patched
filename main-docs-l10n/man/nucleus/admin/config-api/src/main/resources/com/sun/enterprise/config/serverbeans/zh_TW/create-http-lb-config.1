create-http-lb-config(1)    asadmin 公用程式子命令    create-http-lb-config(1)

參考名
       create-http-lb-config - 建立負載平衡程式組態

概要
           create-http-lb-config [--help] [--responsetimeout 60]
            [httpsrouting=false] [--reloadinterval 60]
            [--monitor=false] [--property (name=value)[:name=value]*]
            --target target | config_name

描述
       使用 create-http-lb-config 子命令建立負載平衡程式組態。此組態會套用至
       HTTP 路徑中的負載平衡。使用這個子命令建立負載平衡程式組態檔案後，請執行
       create-http-lb 來建立負載平衡程式。

       您必須指定目標或組態名稱或兩者都指定。如果您未指定目標，便會建立不具目標的組態，而您往後可使用
       create-http-lb-ref
       增加目標。如果您未指定組態名稱，則會根據目標名稱建立名稱。如果您指定兩者，則會以指定的名稱建立組態，參照指定的目標。

           注意
           ┌────────────────────────────────────────┐
           │備註                                    │
           │                                        │
           │           此子命令僅適用於 Oracle      │
           │           GlassFish                    │
           │           Server。此子命令不適用於     │
           │           GlassFish Server Open Source │
           │           Edition。                    │
           └────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --responsetimeout
           伺服器執行處理需傳回回應的時限
           (以秒為單位)。如果在該時間段內未收到任何回應，則認為伺服器處於異常狀態。如果設定為正數，則請求為等冪請求，可以進行重試。如果該請求不是等冪請求，則會傳回一個錯誤頁。如果設定為
           0，則不使用任何逾時。預設值為 60。

       --httpsrouting
           如果設定為 true，則傳送到負載平衡程式的 HTTPS 請求會導致將 HTTPS
           請求傳送到伺服器執行處理。如果設定為 false，則傳送到負載平衡程式的
           HTTPS 請求會導致將 HTTP 請求傳送到伺服器執行處理。預設值為 false。

       --reloadinterval
           間隔檢查負載平衡程式組態檔案 loadbalancer.xml
           是否有變更的時間間隔。當檢查偵測到變更時，系統將重新載入組態檔案。值為
           0 時會停用重新載入。

       --monitor
           指定是否啟用監視。預設值為 false。

       --routecookie
           已停用此選項。此值一定是 true。

       --property
           用於組態負載平衡程式的選擇性屬性名/值配對。

       --target
           指定套用此負載平衡程式組態的目標。如果您不指定目標，則會建立沒有目標的負載平衡程式組態。您往後可以使用子命令
           create-http-lb-ref 來指定目標。

           有效值包括

           ·   cluster_name - 指定此叢集的請求將由負載平衡程式處理。

           ·   stand-alone_instance_name -
               指定此獨立執行處理的請求將由負載平衡程式處理。

運算元
       config_name
           新負載平衡程式組態的名稱。此名稱不得與網域中的任何其他負載平衡程式群組、代理程式、組態、叢集或伺服器執行處理相衝突。如果您不指定名稱，則負載平衡程式組態名稱會以目標名稱
           target_name-http-lb-config 為基礎。

範例
       範例 1, 建立負載平衡程式組態
           此範例會在名稱為 mycluster 的目標和名為 mylbconfigname
           的負載平衡程式組態上建立負載平衡程式組態。

               asadmin> create-http-lb-config --target mycluster mylbconfigname

               Command create-http-lb-config executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-http-lb-config(1), list-http-lb-configs(1), create-http-lb(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日      create-http-lb-config(1)
