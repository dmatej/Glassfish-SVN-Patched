list-secure-admin-internal-uasadmin 实用程序

名称
       list-secure-admin-internal-users - 列出 GlassFish Server DAS
       和实例用于相互验证和授权执行管理操作的用户名。

提要
           list-secure-admin-internal-users [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

说明
       list-secure-admin-internal-users 子命令列出 GlassFish Server DAS
       和实例用于相互验证和授权执行管理操作的用户名。

选项
       --help, -?
           显示子命令的帮助文本。

       --long, -l
           显示有关每个内部用户的详细信息。默认值为假

       --output, -o
           显示有关每个内部用户的特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。值不区分大小写。

           可能的值如下：

           username
               显示内部用户的用户名。

           password-alias
               显示内部用户的口令别名。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

操作数
       name
           要显示其详细信息的内部用户的用户名。

示例
       Example 1, 列出用于安全管理的用户名
           以下示例列出 GlassFish Server DAS
           和实例用于相互验证和授权执行管理操作的用户名。

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 list-secure-admin-internal-users(1)
