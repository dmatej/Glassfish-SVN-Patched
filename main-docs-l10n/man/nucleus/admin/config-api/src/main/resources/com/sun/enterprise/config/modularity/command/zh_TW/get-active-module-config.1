get-active-module-config(1) asadmin 公用程式子命令 get-active-module-config(1)

參考名
       get-active-module-config - 顯示服務或執行處理的目前使用中組態

概要
           get-active-module-config [--help]
           [--target target]
           [--all[={false|true}]]
           [servicename]

描述
       get-active-module-config 子命令會顯示服務或執行處理的目前使用中組態。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定子命令的目標。

           可能值如下：

           server
               顯示預設伺服器執行處理之服務的目前使用中組態。此為預設值。

           domain
               顯示預設網域之服務的目前使用中組態。

           cluster-name
               顯示指定叢集之服務的目前使用中組態。

           instance-name
               顯示指定執行處理之服務的目前使用中組態。

       --all
           顯示所有目前使用中組態。

           預設值為 false。

運算元
       servicename
           模組名稱。

範例
       範例 1, 顯示目前使用中組態
           此範例會顯示 server-config (預設組態) 中 JMS 服務的目前使用中組態。

               asadmin> get-active-module-config
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 6 日   get-active-module-config(1)
