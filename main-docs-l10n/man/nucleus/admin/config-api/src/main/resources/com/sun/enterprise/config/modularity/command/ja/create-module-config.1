create-module-config(1asadminユーティリティのサブコマンcreate-module-config(1)

名前
       create-module-config -
       モジュールのデフォルト構成をdomain.xmlに追加します

概要
           create-module-config [--help]
           [--dryrun[={false|true}]]
           [--all[={false|true}]]
           [--target target]
           servicename

説明
       create-module-configサブコマンドは、モジュールのデフォルト構成をdomain.xmlに追加します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --dryrun
           モジュールのデフォルト構成を表示しますが、domain.xmlには追加しません。

           デフォルト値はfalseです。

       --all
           まだ追加されていない場合は、モジュールのすべてのデフォルト構成をdomain.xmlに追加します。

           デフォルト値はfalseです。

       --target
           モジュールのデフォルト構成を追加するターゲットを指定します。

           使用可能な値は次のとおりです:

           server
               モジュールのデフォルト構成を、デフォルトのサーバー・インスタンスのdomain.xmlに追加します。この値がデフォルト値です。

           domain
               モジュールのデフォルト構成を、デフォルトのドメインのdomain.xmlに追加します。

           cluster-name
               モジュールのデフォルト構成を、指定したクラスタのdomain.xmlに追加します。

           instance-name
               モジュールのデフォルト構成を、指定したインスタンスのdomain.xmlに追加します。

オペランド
       servicename
           モジュールの名前。モジュールのデフォルト構成がターゲットのdomain.xmlに追加されます。

例
       例 1, domain.xmlへのデフォルト構成の追加
           この例では、Webコンテナ・モジュールのデフォルト構成を、server-config
           (デフォルト構成)のdomain1のdomain.xmlファイルに追加します。domain.xmlに追加する前に構成をプレビューするには、--dryrunオプションを使用します。

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                        2013年2月6日          create-module-config(1)
