delete-cluster(1)       Subcomandos de la Utilidad asa       delete-cluster(1)

NOMBRE DE REFERENCIA
       delete-cluster - Suprime un cluster de GlassFish Server.

SINOPSIS
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

DESCRIPCIÓN
       El subcomando delete-cluster suprime un cluster de GlassFish Server. Un
       cluster sólo se puede suprimir si no contiene instancias de GlassFish
       Server. Si un cluster que desea suprimir contiene alguna instancia,
       pare y suprima las instancias antes de suprimir el cluster.

       Si la configuración con nombre del cluster se ha creado automáticamente
       para éste y ningún otro cluster o instancia sin cluster hace referencia
       a la configuración, ésta se suprime al mismo tiempo que el cluster. Las
       configuraciones que se crean automáticamente para un cluster se
       denominan cluster-name-config, donde cluster-name es el nombre del
       cluster.

       Este comando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --autohadboverride
           No especifica esta opción. Esta opción se mantienen para su
           compatibilidad con versiones anteriores. Si especifica esta opción,
           no se produce un error de sintaxis. En su lugar, el subcomando se
           ejecuta correctamente y muestra una advertencia que indica que se
           ha ignorado la opción.

       --nodeagent
           No especifica esta opción. Esta opción se mantienen para su
           compatibilidad con versiones anteriores. Si especifica esta opción,
           no se produce un error de sintaxis. En su lugar, el subcomando se
           ejecuta correctamente y muestra una advertencia que indica que se
           ha ignorado la opción.

OPERANDOS
       cluster-name
           Nombre del cluster que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Cluster de GlassFish Server
           Este ejemplo suprime el cluster de GlassFish Server adccluster.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                   7 de diciembre de 2010           delete-cluster(1)
