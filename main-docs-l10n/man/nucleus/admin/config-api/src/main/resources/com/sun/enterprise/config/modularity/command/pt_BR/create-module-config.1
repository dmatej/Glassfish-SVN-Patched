create-module-config(1) Subcomandos do utilitário asad create-module-config(1)

NOME
       create-module-config - adiciona a configuração default de um módulo ao
       domain.xml

SINOPSE
           create-module-config [--help]
           [--dryrun={false|true}]
           [--all={false|true}]
           [--target target]
           service_name

DESCRIçãO
       O subcomando create-module-config adiciona a configuração default de um
       módulo para domain.xml.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --dryrun
           Exibe a configuração default de um módulo, mas não adiciona-o em
           domain.xml. O valor default é false.

       --all
           Adiciona todas as configurações default de módulos em domain.xml se
           ainda não estiverem nele. O valor default é false.

       --target
           Especifica o alvo aos quais a configuração default deve ser
           adicionada.

           Os valores possíveis são os seguintes:

           server
               Adiciona a configuração default à instância default do
               servidor. Este é o valor default.

           domain
               Adiciona a configuração default ao domínio default.

           cluster-name
               Adiciona a configuração default ao cluster especificado.

           instance-name
               Adiciona a configuração default à instância especificada.

OPERANDOS
       nome_do_serviço
           O nome do módulo ao qual a configuração default deve ser
           adicionada.

EXEMPLOS
       Example 1, Adicionando um Configuração Default em domain.xml
           Este exemplo adiciona a configuração default do módulo do contêiner
           da Web a domain1 em server-config (a configuração default). Use a
           opção --dryrun para visualizar a configuração antes de ela ser
           adicionada.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                   6 de fevereiro de 2013     create-module-config(1)
