delete-module-config(1) Comandi secondari della utilit delete-module-config(1)

NOME
       delete-module-config - elimina la configurazione predefinita di un
       modulo dal file domain.xml

SINTASSI
           delete-module-config [--help]
           [--target target]
           servicename

DESCRIZIONE
       Il comando secondario delete-module-config elimina la configurazione
       predefinita di un modulo dal file domain.xml e indice il modulo a
       utilizzare la configurazione predefinita inclusa nel modulo.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione dalla quale verrà eliminata la
           configurazione predefinita del modulo.

           Di seguito sono riportati i valori possibili.

           server
               Elimina la configurazione predefinita di un modulo dal file
               domain.xml per l'istanza server predefinita. Questo è il valore
               predefinito.

           domain
               Elimina la configurazione predefinita di un modulo dal file
               domain.xml per il dominio predefinito.

           cluster-name
               Elimina la configurazione predefinita di un modulo dal file
               domain.xml del cluster specificato.

           instance-name
               Elimina la configurazione predefinita di un modulo dal file
               domain.xml per l'istanza specificata.

OPERANDI
       servicename
           Il nome del modulo. La configurazione predefinita del modulo verrà
           eliminata dal file domain.xml della destinazione.

ESEMPI
       Esempio 1, Eliminazione di una configurazione predefinita dal file
       domain.xml
           In questo esempio viene eliminata la configurazione predefinita del
           modulo contenitore Web dal file domain.xml di domain1 in
           server-config (la configurazione predefinita).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                       6 febbraio 2013        delete-module-config(1)
