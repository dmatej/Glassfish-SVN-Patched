create-http-lb-config(1)Comandi secondari della utilitcreate-http-lb-config(1)

NOME
       create-http-lb-config - crea una configurazione per il load balancer

SINTASSI
           create-http-lb-config [--help] [--responsetimeout 60]
            [httpsrouting=false] [--reloadinterval 60]
            [--monitor=false] [--property (name=value)[:name=value]*]
            --target target | config_name

DESCRIZIONE
       Usare il comando secondario create-http-lb-config per creare una
       configurazione per un load balancer. Questa configurazione si applica
       al bilanciamento del carico nel percorso HTTP. Dopo aver utilizzato
       questo comando secondario per creare il file di configurazione del load
       balancer, creare il load balancer eseguendo create-http-lb.

       È necessario specificare una destinazione o un nome per la
       configurazione, o entrambi. Se non si specifica una destinazione, la
       configurazione viene creata senza una destinazione, che potrà essere
       aggiunta successivamente mediante il comando create-http-lb-ref. Se non
       si specifica un nome per la configurazione, ne viene creato uno in base
       al nome della destinazione. Se si specificano entrambi, la
       configurazione viene creata con il nome specificato e fa riferimento
       alla destinazione specificata.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --responsetimeout
           Il periodo di tempo in secondi entro il quale un'istanza server
           deve restituire una risposta. Se non si riceve una risposta entro
           il periodo di tempo specificato, il server viene considerato come
           non integro. Se il valore impostato è un numero positivo e la
           richiesta è idempotente, viene eseguito un nuovo tentativo per la
           risposta. Se la richiesta non è idempotente, viene restituita una
           pagina di errore. Se il valore impostato è 0, non viene applicato
           alcun timeout. Il valore predefinito è 60.

       --httpsrouting
           Se si imposta true, le richieste HTTPS al load balancer danno luogo
           a richieste HTTPS all'istanza server. Se si imposta false, le
           richieste HTTPS al load balancer danno luogo a richieste HTTP
           all'istanza server. Il valore predefinito è false.

       --reloadinterval
           L'intervallo tra i controlli finalizzati al rilevamento di
           modifiche nel file di configurazione loadbalancer.xml del load
           balancer. Quando vengono rilevate modifiche, il file di
           configurazione viene ricaricato. Il valore 0 disabilita il
           ricaricamento.

       --monitor
           Specifica se il monitoraggio è abilitato. Il valore predefinito è
           false.

       --routecookie
           Questa opzione non è più valida. Il valore è sempre true.

       --property
           Coppie nome-valore di attributo facoltative per la configurazione
           del load balancer.

       --target
           Specifica la destinazione a cui si applica la configurazione del
           load balancer. Se non si specifica alcuna destinazione, la
           configurazione del load balancer viene creata senza una
           destinazione. Le destinazioni possono essere specificate in un
           secondo tempo, mediante il comando secondario create-http-lb-ref.

           Di seguito sono riportati i valori validi.

           ·   cluster_name: specifica che le richieste per questo cluster
               verranno gestite dal load balancer.

           ·   stand-alone_instance_name: specifica che le richieste per
               questa istanza standalone verranno gestite dal load balancer.

OPERANDI
       config_name
           Il nome della nuova configurazione del load balancer. Il nome non
           deve essere in conflitto con altri gruppi di load balancer, agenti,
           configurazioni, cluster o istanze server presenti nel dominio. Se
           il nome non viene specificato, il nome della configurazione del
           load balancer sarà basato sul nome della destinazione,
           target_name-http-lb-config.

ESEMPI
       Esempio 1, Creazione di una configurazione del load balancer
           In questo esempio vengono create una configurazione del load
           balancer in una destinazione denominata mycluster e una
           configurazione del load balancer denominata mylbconfigname.

               asadmin> create-http-lb-config --target mycluster mylbconfigname

               Command create-http-lb-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-http-lb-config(1), list-http-lb-configs(1), create-http-lb(1)

       asadmin(1M)

Java EE 7                       25 agosto 2010        create-http-lb-config(1)
