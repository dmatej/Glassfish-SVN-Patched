disable-secure-admin-intUnterbefehle)des disable-secure-admin-internal-user(1)

NAME
       disable-secure-admin-internal-user - Weist den GlassFish Server-DAS und
       die Instanzen an, den angegebenen Admin-Benutzer nicht für die
       gegenseitige Authentifizierung und die Autorisierung von
       Admin-Vorgängen zu verwenden.

ZUSAMMENFASSUNG
           disable-secure-admin-internal-user
           [--help]
           admin-username

BESCHREIBUNG
       Der Unterbefehl disable-secure-admin-internal-user legt fest, dass die
       sichere Administration nicht den Benutzernamen (anstelle des
       SSL-Zertifikats) für die gegenseitige Authentifizierung des DAS und der
       Instanzen und die Autorisierung von Admin-Vorgängen verwendet.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       admin-username
           Der Admin-Benutzername, den GlassFish Server nicht zur
           gegenseitigen Authentifizierung des DAS und der Instanzen und zur
           Autorisierung von Admin-Vorgängen verwenden soll.

BEISPIELE
       Beispiel 1, Benutzernamen für sichere Administration deaktivieren
           Im folgenden Beispiel wird festgelegt, dass die sichere
           Administration nicht den Benutzernamen tester für die gegenseitige
           Authentifizierung des DAS und der Instanzen und die Autorisierung
           von Admin-Vorgängen verwendet.

               asadmin> disable-secure-admin-internal-user tester

               Command disable-secure-admin-internal-user executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                        25. Jun.disable-secure-admin-internal-user(1)
