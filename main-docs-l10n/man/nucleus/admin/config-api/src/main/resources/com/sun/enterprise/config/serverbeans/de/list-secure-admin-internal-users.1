list-secure-admin-internUnterbefehle des aslist-secure-admin-internal-users(1)

NAME
       list-secure-admin-internal-users - listet die Benutzernamen auf, die
       der GlassFish Server-DAS und die Instanzen zur gegenseitigen
       Authentifizierung und Autorisierung von Admin-Vorgängen verwenden.

ZUSAMMENFASSUNG
           list-secure-admin-internal-users [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

BESCHREIBUNG
       Der Unterbefehl list-secure-admin-internal-users listet die
       Benutzernamen auf, die der GlassFish Server-DAS und die Instanzen zur
       gegenseitigen Authentifizierung und Autorisierung von Admin-Vorgängen
       verwenden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --long, -l
           Zeigt detaillierte Informationen zu jedem internen Benutzer an. Der
           Standardwert ist false.

       --output, -o
           Zeigt bestimmte Details zu jedem internen Benutzer an. Mithilfe
           einer durch Komma getrennten Liste können Sie die anzuzeigenden
           Details und deren Reihenfolge festlegen. Die Werte sind von Groß-
           und Kleinschreibung unabhängig.

           Mögliche Werte:

           username
               Zeigt den Benutzernamen für den internen Benutzer an.

           password-alias
               Zeigt den Kennwortalias für den internen Benutzer an.

       --header, -h
           Legt fest, ob bei Verwendung der Option --long Spaltenüberschriften
           angezeigt werden. Der Standardwert ist true. Um die Überschriften
           zu unterdrücken, legen Sie für die Option --header den Wert false
           fest.

OPERANDEN
       name
           Der Benutzername für den internen Benutzer, für den Sie Details
           anzeigen wollen.

BEISPIELE
       Example 1, Benutzernamen für die sichere Administration auflisten
           Im folgenden Beispiel werden die Benutzernamen aufgelistet, die der
           GlassFish Server-DAS und die Instanzen zur gegenseitigen
           Authentifizierung und Autorisierung von Admin-Vorgängen verwenden.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                        13. Feb. 2list-secure-admin-internal-users(1)
