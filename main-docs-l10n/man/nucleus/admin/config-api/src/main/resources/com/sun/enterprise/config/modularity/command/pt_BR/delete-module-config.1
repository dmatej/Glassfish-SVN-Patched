delete-module-config(1) Subcomandos do utilitário asad delete-module-config(1)

NOME
       delete-module-config - remove a configuração de um módulo de domain.xml

SINOPSE
           delete-module-config [--help]
           [--target target]
           service_name

DESCRIçãO
       O subcomando delete-module-config remove a configuração de um módulo de
       domain.xml e faz com que o módulo utilize a configuração default
       incluída no módulo.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo do qual a configuração deve ser deletada.

           Os valores possíveis são os seguintes:

           server
               Deleta a configuração da instância do servidor default. Este é
               o valor default.

           domain
               Deleta a configuração do domínio default.

           cluster-name
               Deleta a configuração do cluster especificado.

           instance-name
               Deleta a configuração da instância especificada.

OPERANDOS
       nome_do_serviço
           O nome do módulo cuja configuração deve ser removida.

EXEMPLOS
       Example 1, Deletando uma Configuração Default do domain.xml
           Este exemplo deleta a configuração do módulo do contêiner da Web de
           domain1 em server-config (a configuração default).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                   6 de fevereiro de 2013     delete-module-config(1)
