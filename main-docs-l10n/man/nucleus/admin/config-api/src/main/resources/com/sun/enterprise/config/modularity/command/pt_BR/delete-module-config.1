delete-module-config(1) Subcomandos do utilitário asad delete-module-config(1)

NOME
       delete-module-config - deleta a configuração default de um módulo do
       domain.xml

SINOPSE
           delete-module-config [--help]
           [--target target]
           servicename

DESCRIçãO
       O subcomando delete-module-config deleta a configuração de um módulo do
       domain.xml e faz com que o módulo utilize a configuração default
       incluída no módulo.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo do qual a configuração default do módulo será
           deletada.

           Os valores possíveis são os seguintes:

           server
               Deleta a configuração default de um módulo para o domain.xml da
               instância do servidor default. Este é o valor default.

           domain
               Deleta a configuração default de um módulo do domain.xml do
               domínio default.

           cluster-name
               Deleta a configuração default de um módulo para o domain.xml do
               cluster especificado.

           instance-name
               Deleta a configuração default de um módulo do domain.xml da
               instância especificada.

OPERANDOS
       servicename
           O nome do módulo. A configuração default do módulo será deletada do
           domain.xml do alvo.

EXEMPLOS
       Example 1, Deletando uma Configuração Default do domain.xml
           Este exemplo deleta a configuração default do módulo de contêiner
           da Web do arquivo domain.xml do domain1 no server-config (a
           configuração default).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                   6 de fevereiro de 2013     delete-module-config(1)
