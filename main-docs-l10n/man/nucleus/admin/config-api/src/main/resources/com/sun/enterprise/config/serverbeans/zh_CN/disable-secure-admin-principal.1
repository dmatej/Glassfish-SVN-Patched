disable-secure-admin-principasadmin 实用程序子

名称
       disable-secure-admin-principal - 禁止将证书用于安全管理中的访问授权。

提要
           disable-secure-admin-principal
           [--help]
           --alias aliasname | DN

说明
       disable-secure-admin-principal
       子命令禁用了证书，使其无法在安全管理中用于授予访问权限。

       必须指定 --alias 选项或 DN。

选项
       --help, -?
           显示子命令的帮助文本。

       --alias
           信任存储中证书的别名。GlassFish Server
           使用该别名在信任存储中查找证书，如果找到该证书，则禁止对应的 DN
           用于安全管理。由于 alias-name
           必须是与信任存储中当前存在的证书关联的别名，因此，对于自签名证书这非常有用。

操作数
       DN
           证书的唯一判别名，使用位于引号中的逗号分隔列表指定。例如，"CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US"。

示例
       Example 1, 禁止信任 DN 用于安全管理
           以下示例演示如何禁止信任 DN 在安全管理中用于授予访问权限。

               asadmin> disable-secure-admin-principal
               "CN=system.amer.oracle.com,OU=GlassFish,
               O=Oracle Corporation,L=Santa Clara,ST=California,C=US"

               Command disable-secure-admin-principal executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 25disable-secure-admin-principal(1)
