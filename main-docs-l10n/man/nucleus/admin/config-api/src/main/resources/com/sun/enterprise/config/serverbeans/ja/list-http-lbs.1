list-http-lbs(1)      asadminユーティリティのサブコマンド     list-http-lbs(1)

名前
       list-http-lbs - ロード・バランサをリストします

概要
           list-http-lbs [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

説明
       list-http-lbsサブコマンドを使用して、物理ロード・バランサをリストします。

           注記
           ┌───────────────────────────────────────────────────────────────────┐
           │注意                                                               │
           │                                                                   │
           │           このサブコマンドは、Oracle                              │
           │           GlassFish                                               │
           │           Serverにのみ適用されます。このサブコマンドは、GlassFish │
           │           Server Open Source                                      │
           │           Editionには適用されません。                             │
           └───────────────────────────────────────────────────────────────────┘

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --long, -l
           各ロード・バランサに関する詳細情報を表示します。デフォルト値はfalseです。

       --output, -o
           各ロード・バランサの特定の詳細を表示します。表示する詳細とその順序を指定するには、カンマ区切りのリストを使用します。たとえば、list-http-lbs
           --output
           name,device-host,device-portのようにします。この値では大文字と小文字が区別されません。

           使用可能な値は次のとおりです:

           device-host
               ロード・バランシング・デバイスのデバイス・ホストまたはIPアドレスを表示します。このホストまたはIPに物理ロード・バランサが存在します。

           name
               ロード・バランサの名前を表示します。

           auto-apply-enabled
               自動適用機能が有効化されているかどうかを表示します。

           lb-config-name
               ロード・バランサ構成の名前を表示します。

           device-port
               ロード・バランシング・デバイスとの通信に使用されるポートを表示します。

       --header, -h
           --longオプションが使用されている場合に、列ヘッダーを表示するかどうかを指定します。デフォルト値はtrueです。ヘッダーを表示しないようにするには、--headerオプションをfalseに設定します。

オペランド
       name
           詳細を表示するロード・バランサの名前。

例
       例 1, ドメインの物理ロード・バランサのリスト
           この例では、ドメインに対して定義されたすべての物理ロード・バランサをリストします。

               asadmin> list-http-lbs

               lb1
               lb2
               Command list-http-lbs executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-http-lb(1), delete-http-lb(1)

       asadmin(1M)

Java EE 7                        2013年2月12日                list-http-lbs(1)
