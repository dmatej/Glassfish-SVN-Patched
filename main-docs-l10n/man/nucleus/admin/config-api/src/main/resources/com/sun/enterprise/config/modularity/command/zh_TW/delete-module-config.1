delete-module-config(1)     asadmin 公用程式子命令     delete-module-config(1)

參考名
       delete-module-config - 從 domain.xml 刪除模組的預設組態

概要
           delete-module-config [--help]
           [--target target]
           servicename

描述
       delete-module-config 子命令會從 domain.xml
       刪除模組的預設組態，讓模組使用包含在模組中的預設組態。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定要刪除模組預設組態的目標。

           可能值如下：

           server
               針對預設的伺服器執行處理，刪除 domain.xml
               中的模組預設組態。此為預設值。

           domain
               針對預設的網域，刪除 domain.xml 中的模組預設組態。

           cluster-name
               在指定的叢集中，刪除 domain.xml 中的模組預設組態。

           instance-name
               針對指定的執行處理，刪除 domain.xml 中的模組預設組態。

運算元
       servicename
           模組名稱。模組的預設組態將自目標的 domain.xml 刪除。

範例
       範例 1, 刪除 domain.xml 的預設組態
           此範例會將 Web 容器模組的預設組態自 server-config (預設組態) 中
           domain1 的 domain.xml 檔案刪除。

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 6 日       delete-module-config(1)
