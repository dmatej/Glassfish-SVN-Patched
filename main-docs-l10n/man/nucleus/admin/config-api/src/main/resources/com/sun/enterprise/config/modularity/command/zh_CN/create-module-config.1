create-module-config(1)     asadmin 实用程序子命令     create-module-config(1)

名称
       create-module-config - 将模块的默认配置添加到 domain.xml

提要
           create-module-config [--help]
           [--dryrun[={false|true}]]
           [--all[={false|true}]]
           [--target target]
           servicename

说明
       create-module-config 子命令将模块的默认配置添加到 domain.xml。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --dryrun
           显示模块的默认配置但不将其添加到 domain.xml。

           默认值为假

       --all
           将模块的所有默认配置添加到 domain.xml (如果它们尚未在其中)。

           默认值为假

       --target
           指定将为其添加模块的默认配置的目标。

           可能的值如下：

           server
               将模块的默认配置添加到默认服务器实例的 domain.xml
               中。这是默认值。

           domain
               将模块的默认配置添加到默认域的 domain.xml 中。

           cluster-name
               将模块的默认配置添加到指定集群的 domain.xml 中。

           instance-name
               将模块的默认配置添加到指定实例的 domain.xml 中。

操作数
       servicename
           模块的名称。模块的默认配置将添加到目标的 domain.xml 中。

示例
       Example 1, 将默认配置添加到 domain.xml
           此示例将 Web 容器模块的默认配置添加到 server-config (默认配置) 中
           domain1 的 domain.xml 文件中。要在将配置添加到 domain.xml
           之前预览它，请使用 --dryrun 选项。

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 6 日       create-module-config(1)
