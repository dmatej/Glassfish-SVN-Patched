delete-module-config(1) Sous-commandes de l'utilitaire delete-module-config(1)

NOM
       delete-module-config - supprime la configuration par défaut d'un module
       du fichier domain.xml

SYNTAXE
           delete-module-config [--help]
           [--target target]
           servicename

DESCRIPTION
       La sous-commande delete-module-config supprime la configuration par
       défaut d'un module du fichier domain.xml, ce qui force le module à
       utiliser la configuration par défaut qu'il contient.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Spécifie la cible de laquelle supprimer la configuration par défaut
           du module.

           Les valeurs possibles sont les suivantes :

           server
               Supprime la configuration par défaut d'un module du fichier
               domain.xml pour l'instance de serveur par défaut. Il s'agit de
               la valeur par défaut.

           domain
               Supprime la configuration par défaut d'un module du fichier
               domain.xml pour le domaine par défaut.

           cluster-name
               Supprime la configuration par défaut d'un module du fichier
               domain.xml pour le cluster spécifié.

           instance-name
               Supprime la configuration par défaut d'un module du fichier
               domain.xml pour l'instance spécifiée.

OPÉRANDES
       servicename
           Nom du module. La configuration par défaut du module est supprimée
           du fichier domain.xml de la cible.

EXEMPLES
       Exemple 1, Suppression d'une configuration par défaut du fichier
       domain.xml
           Dans cet exemple, la configuration par défaut du module de
           conteneur Web est supprimée du fichier domain.xml de domain1 dans
           server-config (configuration par défaut).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                         6 fév 2013           delete-module-config(1)
