delete-module-config(1) Sous-commandes de l'utilitaire delete-module-config(1)

NOM
       delete-module-config - enlève la configuration d'un module de
       domain.xml

SYNTAXE
           delete-module-config [--help]
           [--target target]
           service_name

DESCRIPTION
       La sous-commande delete-module-config enlève la configuration d'un
       module du fichier domain.xml, ce qui force le module à utiliser la
       configuration par défaut qu'il contient.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible de laquelle la configuration doit
           être supprimée.

           Les valeurs possibles sont les suivantes :

           server
               Supprime la configuration de l'instance de serveur par défaut.
               Il s'agit de la valeur par défaut.

           domain
               Supprime la configuration du domaine par défaut.

           cluster-name
               Supprime la configuration du cluster indiqué.

           instance-name
               Supprime la configuration de l'instance indiquée.

OPÉRANDES
       service_name
           Nom du module pour lequel la configuration doit être enlevée.

EXEMPLES
       Exemple 1, Suppression d'une configuration par défaut du fichier
       domain.xml
           Cet exemple montre comment supprimer la configuration du module de
           conteneur Web de domain1 dans server-config (configuration par
           défaut).

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                         6 fév 2013           delete-module-config(1)
