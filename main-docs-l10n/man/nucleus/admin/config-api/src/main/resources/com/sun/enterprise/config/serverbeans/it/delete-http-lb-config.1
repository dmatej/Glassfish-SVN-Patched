delete-http-lb-config(1)Comandi secondari della utilitdelete-http-lb-config(1)

NOME
       delete-http-lb-config - elimina una configurazione del load balancer

SINTASSI
           delete-http-lb-config [--help] config_name

DESCRIZIONE
       Usare il comando secondario delete-http-lb-config per eliminare una
       configurazione del load balancer. La configurazione del load balancer
       non deve fare riferimento a cluster o istanze server abilitate per il
       bilanciamento del carico. Inoltre, nessun load balancer fisico deve
       fare riferimento alla configurazione del load balancer in questione.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       config_name
           Il nome della configurazione del load balancer da eliminare. La
           configurazione non deve fare riferimento a cluster o istanze server
           abilitate per il bilanciamento del carico, né essere utilizzata da
           load balancer fisici.

ESEMPI
       Esempio 1, Eliminazione di una configurazione del load balancer
           In questo esempio viene eliminata una configurazione del load
           balancer denominata mylbconfig

               asadmin> delete-http-lb-config mylbconfig
               Command delete-http-lb-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                       25 agosto 2010        delete-http-lb-config(1)
