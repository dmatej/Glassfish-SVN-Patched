list-http-lbs(1)            asadmin 实用程序子命令            list-http-lbs(1)

名称
       list-http-lbs - 列出负载平衡器

提要
           list-http-lbs [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

说明
       使用 list-http-lbs 子命令列出物理负载平衡器。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --long, -l
           显示有关每个负载平衡器的详细信息。默认值为假

       --output, -o
           显示有关每个负载平衡器的特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。例如，list-http-lbs
           --output name,device-host,device-port。值不区分大小写。

           可能的值如下：

           device-host
               显示负载平衡设备的设备主机或 IP 地址。该主机或 IP
               为物理负载平衡器驻留的位置。

           name
               显示负载平衡器的名称。

           auto-apply-enabled
               显示是否启用了“自动应用”功能。

           lb-config-name
               显示负载平衡器配置的名称。

           device-port
               显示用于与负载平衡设备通信的端口。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

操作数
       name
           要显示其详细信息的负载平衡器的名称。

示例
       Example 1, 列出域的物理负载平衡器
           此示例列出针对域定义的所有物理负载平衡器。

               asadmin> list-http-lbs

               lb1
               lb2
               Command list-http-lbs executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb(1), delete-http-lb(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 12 日              list-http-lbs(1)
