create-module-config(1) Sous-commandes de l'utilitaire create-module-config(1)

NOM
       create-module-config - ajoute la configuration par défaut d'un module
       au fichier domain.xml

SYNTAXE
           create-module-config [--help]
           [--dryrun={false|true}]
           [--all={false|true}]
           [--target target]
           service_name

DESCRIPTION
       La sous-commande create-module-config ajoute la configuration par
       défaut d'un module au fichier domain.xml.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --dryrun
           Cette option affiche la configuration par défaut d'un module sans
           l'ajouter au fichier domain.xml. La valeur par défaut est false.

       --all
           Cette option ajoute toutes les configurations par défaut des
           modules au fichier domain.xml si elles n'y figurent pas encore. La
           valeur par défaut est false.

       --target
           Cette option indique la cible à laquelle la configuration par
           défaut doit être ajoutée.

           Les valeurs possibles sont les suivantes :

           server
               Cette valeur ajoute la configuration par défaut à l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           domain
               Cette valeur ajoute la configuration par défaut au domaine par
               défaut.

           cluster-name
               Cette valeur ajoute la configuration par défaut au cluster
               spécifié.

           instance-name
               Cette valeur ajoute la configuration par défaut à l'instance
               spécifiée.

OPÉRANDES
       service_name
           Nom du module pour lequel la configuration par défaut doit être
           ajoutée.

EXEMPLES
       Exemple 1, Ajout d'une configuration par défaut au fichier domain.xml
           Cet exemple montre comment ajouter la configuration par défaut du
           module de conteneur Web à domain1 dans server-config (configuration
           par défaut). Utilisez l'option --dryrun pour prévisualiser la
           configuration avant de l'ajouter.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                         6 fév 2013           create-module-config(1)
