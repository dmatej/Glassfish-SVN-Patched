get-active-module-config(1) asadmin 实用程序子命令 get-active-module-config(1)

名称
       get-active-module-config - 显示服务或实例的当前活动配置

提要
           get-active-module-config [--help]
           [--target target]
           [--all={false|true}]
           [service_name]

说明
       get-active-module-config 子命令可显示服务或实例的当前活动配置。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要查看特定服务或整个实例的当前活动配置的目标。

           可能的值如下：

           server
               显示默认服务器实例的当前活动配置。这是默认值。

           domain
               显示默认域的当前活动配置。

           cluster-name
               显示指定集群的当前活动配置。

           instance-name
               显示指定实例的当前活动配置。

       --all
           显示所有的当前活动配置。

           默认值为假

操作数
       service_name
           要显示其当前活动配置的模块的名称。

示例
       Example 1, 显示当前活动配置
           此示例显示 server-config (默认配置) 中 JMS 服务的当前活动配置。

               asadmin> get-active-module-config jms-service
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 6 日   get-active-module-config(1)
