enable-secure-admin-prinComandi)secondari dellenable-secure-admin-principal(1)

NOME
       enable-secure-admin-principal - Quando l'amministrazione sicura è
       abilitata, comunica a GlassFish Server di accettare le richieste di
       amministrazione da client identificati dai certificati SSL specificati.

SINTASSI
           enable-secure-admin-principal
           [--help]
           --alias aliasname | DN

DESCRIZIONE
       Il comando secondario enable-secure-admin-principal comunica a
       GlassFish Server di accettare le richieste di amministrazione
       accompagnate da un certificato SSL con il nome distinto (DN)
       specificato. Se si utilizza il formato "--alias aliasname", GlassFish
       Server cerca nel truststore un certificato con l'alias specificato e
       utilizza il DN associato al certificato. In caso contrario, GlassFish
       Server registra il valore specificato come DN.

       È necessario specificare l'opzione --alias o il DN.

       È possibile eseguire enable-secure-admin-principal più volte in modo
       che GlassFish Server accetti le richieste di amministrazione da un
       client che invia un certificato con uno qualsiasi dei DN specificati.

       Quando si esegue enable-secure-admin, GlassFish Server registra
       automaticamente i DN per l'alias di amministrazione e dell'istanza, sia
       che si specifichino questi valori che si utilizzino i valori
       predefiniti. Non è necessario eseguire manualmente
       enable-secure-admin-principal per questi certificati. Oltre che per i
       certificati, enable-secure-admin-principal deve essere eseguito per
       qualsiasi altro DN che GlassFish Server deve autorizzare all'invio
       delle richieste di amministrazione. Ciò include i DN che corrispondono
       ai certificati sicuri, ovvero quelli con una catena di certificati per
       un'autorità sicura.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --alias
           Il nome alias del certificato nel truststore. GlassFish Server
           cerca il certificato nel truststore utilizzando un alias e, se lo
           trova, memorizza il DN corrispondente come valido per
           l'amministrazione sicura. Poiché alias-name deve essere un alias
           associato a un certificato presente nel truststore, ciò è molto
           utile per i certificati con firma automatica.

OPERANDI
       DN
           Il nome distinto del certificato, specificato come lista separata
           da virgole tra virgolette. Ad esempio,
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US".

ESEMPI
       Esempio 1, Accettazione di un DN per l'amministrazione sicura
           L'esempio seguente mostra come specificare un DN per autorizzare
           l'accesso nell'amministrazione sicura.

               asadmin> enable-secure-admin-principal
               "CN=system.amer.oracle.com,OU=GlassFish,
               O=Oracle Corporation,L=Santa Clara,ST=California,C=US"

               Command enable-secure-admin-principal executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable-secure-admin(1)

       disable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                       25 giugno 2011enable-secure-admin-principal(1)
