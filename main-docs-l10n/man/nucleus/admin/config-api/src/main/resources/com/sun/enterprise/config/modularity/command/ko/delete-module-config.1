delete-module-config(1)   asadmin 유틸리티 하위 명령   delete-module-config(1)

제목
       delete-module-config - domain.xml에서 모듈 구성 제거

시놉시스
           delete-module-config [--help]
           [--target target]
           service_name

설명
       delete-module-config 하위 명령은 domain.xml에서 모듈의 구성을 제거하고
       모듈에서 모듈에 포함된 기본 구성을 사용하도록 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           구성을 삭제할 대상을 지정합니다.

           가능한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스에서 구성을 삭제합니다. 이 값은 기본값입니다.

           domain
               기본 도메인에서 구성을 삭제합니다.

           cluster-name
               지정된 클러스터에서 구성을 삭제합니다.

           instance-name
               지정된 인스턴스에서 구성을 삭제합니다.

피연산자
       service_name
           구성을 제거할 모듈 이름입니다.

예제
       예 1, domain.xml에서 기본 구성 삭제
           이 예제에서는 웹 컨테이너 모듈의 구성을 server-config(기본 구성)에
           있는 domain1에서 삭제합니다.

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                       2013년 2월 6일         delete-module-config(1)
