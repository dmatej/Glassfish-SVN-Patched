list-secure-admin-principalsasadmin 公用程式子命

參考名
       list-secure-admin-principals - 列出 GlassFish Server
       接受從屬端管理要求時所依據的憑證。

概要
           list-secure-admin-principals [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

描述
       list-secure-admin-principals 子命令會列出 GlassFish Server
       接受從屬端管理要求時所依據的憑證。

選項
       --help, -?
           顯示子命令的說明文字。

       --long, -l
           顯示與 GlassFish Server
           接受從屬端管理要求時所依據之憑證的詳細資訊。預設值為 false。

       --output, -o
           顯示與 GlassFish Server
           接受從屬端管理要求時所依據之憑證的特定詳細資訊。使用以逗號區隔的清單來指定您要顯示的詳細資訊和顯示順序。值有大小寫之分。

           可能值如下：

           DN
               顯示每個憑證的辨別名稱 (DN)。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

運算元
       name
           憑證的辨別名稱，指定方式為用括號括住以逗號分隔的清單。例如："CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US"。

範例
       範例 1, 列出憑證
           以下範例顯示如何列出 GlassFish Server
           接受從屬端管理要求時所依據的憑證。

               asadmin> list-secure-admin-principals
               CN=localhost,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               CN=localhost-instance,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               Command list-secure-admin-principals executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日
