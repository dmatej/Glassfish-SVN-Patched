create-module-config(1) Subcomandos de la Utilidad asa create-module-config(1)

NOMBRE DE REFERENCIA
       create-module-config - Agrega la configuración por defecto de un módulo
       a domain.xml.

SINOPSIS
           create-module-config [--help]
           [--dryrun={false|true}]
           [--all={false|true}]
           [--target target]
           service_name

DESCRIPCIÓN
       El subcomando create-module-config agrega la configuración por defecto
       de un módulo a domain.xml.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --dryrun
           Muestra la configuración por defecto de un módulo pero no la agrega
           a domain.xml. El valor por defecto es false.

       --all
           Agrega todas las configuraciones por defecto de módulos a
           domain.xml si aún no están en ella. El valor por defecto es false.

       --target
           Especifica el destino al que se agregará la configuración por
           defecto.

           Los valores posibles son los siguientes:

           server
               Agrega la configuración por defecto a la instancia de servidor
               por defecto. Este es el valor por defecto.

           domain
               Agrega la configuración por defecto al dominio por defecto.

           cluster-name
               Agrega la configuración por defecto al cluster especificado.

           instance-name
               Agrega la configuración por defecto a la instancia
               especificada.

OPERANDOS
       service_name
           Nombre del módulo al que se va a agregar la configuración por
           defecto.

EJEMPLOS
       Ejemplo 1, Adición de una configuración por defecto a domain.xml
           En este ejemplo se agrega la configuración por defecto del módulo
           de contenedor web a domain1 en server-config (la configuración por
           defecto). Utilice la opción --dryrun para obtener una vista previa
           de la configuración antes de que se agregue.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                    06 de febrero de 2013     create-module-config(1)
