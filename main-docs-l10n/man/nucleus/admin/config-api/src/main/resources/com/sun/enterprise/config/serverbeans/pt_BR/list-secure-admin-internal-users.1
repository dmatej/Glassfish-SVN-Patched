list-secure-admin-internSubcomandos do utillist-secure-admin-internal-users(1)

NOME
       list-secure-admin-internal-users - lista os nomes de usuários
       utilizados pelo DAS do GlassFish Server e pelas instâncias para
       autenticação recíproca e para autorizar operações admin.

SINOPSE
           list-secure-admin-internal-users [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

DESCRIçãO
       O subcomando list-secure-admin-internal-users lista os nomes de
       usuários utilizados pelo DAS do GlassFish Server e pelas instâncias
       para autenticação recíproca e para autorizar operações admin.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --long, -l
           Exibe informações detalhadas sobre cada usuário interno. O valor
           default é false.

       --output, -o
           Exibe detalhes específicos sobre cada usuário interno. Use uma
           lista separada por vírgulas para especificar os detalhes que você
           deseja exibir e sua ordem. Os valores diferenciam maiúsculas de
           minúsculas.

           Os valores possíveis são os seguintes:

           username
               Exibe o nome de usuário para o usuário interno.

           password-alias
               Exibe o alias de senha do usuário interno.

       --header, -h
           Especifica se os cabeçalhos da coluna são exibidos quando a opção
           --long for usada. O valor default é verdadeiro. Para suprimir os
           cabeçalhos, defina a opção --header como false.

OPERANDOS
       name
           O nome do usuário do usuário interno para o qual você deseja exibir
           detalhes.

EXEMPLOS
       Example 1, Lista o nome do usuário para administração segura
           O exemplo a seguir lista os nomes de usuários utilizados pelo DAS
           do GlassFish Server e pelas instâncias para autenticação recíproca
           e para autorizar operações admin.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                         13 fev 20list-secure-admin-internal-users(1)
