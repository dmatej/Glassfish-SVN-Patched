delete-module-config(1)     asadmin 实用程序子命令     delete-module-config(1)

名称
       delete-module-config - 从 domain.xml 中删除模块的配置

提要
           delete-module-config [--help]
           [--target target]
           service_name

说明
       delete-module-config 子命令可从 domain.xml
       中删除模块的配置并导致模块使用模块中包含的默认配置。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从中删除配置的目标。

           可能的值如下：

           server
               从默认服务器实例中删除配置。这是默认值。

           domain
               从默认域中删除配置。

           cluster-name
               从指定的集群中删除配置。

           instance-name
               从指定的实例中删除配置。

操作数
       service_name
           要删除其配置的模块的名称。

示例
       Example 1, 从 domain.xml 中删除默认配置
           此示例从 server-config (默认配置) 中的 domain1 中删除 Web
           容器模块的配置。

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 6 日       delete-module-config(1)
