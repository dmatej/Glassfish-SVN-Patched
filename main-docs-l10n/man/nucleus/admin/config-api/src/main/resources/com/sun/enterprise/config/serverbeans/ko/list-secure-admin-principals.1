list-secure-admin-principaasadmin 유틸리티 하위list-secure-admin-principals(1)

제목
       list-secure-admin-principals - GlassFish Server에서 클라이언트의 관리
       요청을 승인하는 데 사용하는 인증서 나열

시놉시스
           list-secure-admin-principals [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

설명
       list-secure-admin-principals 하위 명령은 GlassFish Server에서
       클라이언트의 관리 요청을 승인하는 데 사용하는 인증서를 나열합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --long, -l
           GlassFish Server에서 클라이언트의 관리 요청을 승인하는 데 사용하는
           인증서에 대한 세부 정보를 표시합니다. 기본값은 false입니다.

       --output, -o
           GlassFish Server에서 클라이언트의 관리 요청을 승인하는 데 사용하는
           인증서에 대한 특정 세부 정보를 표시합니다. 표시할 세부 정보 및
           순서를 지정하려면 쉼표로 구분된 목록을 사용합니다. 이러한 값은
           대소문자를 구분하지 않습니다.

           가능한 값은 다음과 같습니다.

           DN
               각 인증의 DN(식별 이름)을 표시합니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

피연산자
       name
           인증서의 식별 이름으로, 따옴표 안에 쉼표로 구분된 목록으로
           지정됩니다. 예를 들어,
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US"입니다.

예제
       예 1, 인증서 나열
           이 예제에서는 GlassFish Server에서 클라이언트의 관리 요청을
           승인하는 데 사용하는 인증서를 나열합니다.

               asadmin> list-secure-admin-principals
               CN=localhost,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               CN=localhost-instance,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               Command list-secure-admin-principals executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                       2013년 2월 13일list-secure-admin-principals(1)
