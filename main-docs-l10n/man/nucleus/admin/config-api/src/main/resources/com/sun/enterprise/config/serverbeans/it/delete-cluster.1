delete-cluster(1)       Comandi secondari della utilit       delete-cluster(1)

NOME
       delete-cluster - elimina un cluster di GlassFish Server

SINTASSI
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

DESCRIZIONE
       Il comando secondario delete-cluster consente di eliminare un cluster
       GlassFish Server. È possibile eliminare solo i cluster che non
       contengono istanze di GlassFish Server. Se il cluster che si sta
       eliminando contiene istanze, interrompere il processo ed eliminare le
       istanze prima di eliminare il cluster.

       Se la configurazione specifica del cluster è stata creata in modo
       automatico per il cluster e non esistono altri cluster o istanze non in
       cluster che vi fanno riferimento, la configurazione verrà eliminata
       insieme al cluster. A una configurazione creata in modo automatica per
       un cluster viene assegnato il nome nome-cluster-config, dove
       nome-cluster è il nome del cluster.

       Questo comando è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --autohadboverride
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

       --nodeagent
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

OPERANDI
       cluster-name
           Il nome del cluster da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un cluster di GlassFish Server
           In questo esempio viene eliminato il cluster di GlassFish Server
           adccluster.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                       7 dicembre 2010              delete-cluster(1)
