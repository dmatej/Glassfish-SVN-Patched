disable-secure-admin-priComandi1secondari deldisable-secure-admin-principal(1)

NOME
       disable-secure-admin-principal - disabilita il certificato per
       autorizzare l'accesso nell'amministrazione sicura

SINTASSI
           disable-secure-admin-principal
           [--help]
           --alias aliasname | DN

DESCRIZIONE
       Il comando secondario disable-secure-admin-principal disabilita la
       validità del certificato per autorizzare l'accesso nell'amministrazione
       sicura.

       È necessario specificare l'opzione --alias o il DN.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --alias
           Il nome alias del certificato nel truststore. GlassFish Server
           cerca il certificato nel truststore utilizzando un alias e, se lo
           trova, disabilita il DN corrispondente come valido per
           l'amministrazione sicura. Poiché alias-name deve essere un alias
           associato a un certificato presente nel truststore, ciò è molto
           utile per i certificati con firma automatica.

OPERANDI
       DN
           Il nome distinto del certificato, specificato come lista separata
           da virgole tra virgolette. Ad esempio,
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US" .

ESEMPI
       Esempio 1, Disabilita l'accettazione di un DN per l'amministrazione
       sicura.
           L'esempio seguente mostra come disabilitare l'accettazione di un DN
           per autorizzare l'accesso nell'amministrazione sicura.

               asadmin> disable-secure-admin-principal
               "CN=system.amer.oracle.com,OU=GlassFish,
               O=Oracle Corporation,L=Santa Clara,ST=California,C=US"

               Command disable-secure-admin-principal executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                       25 giugno 201disable-secure-admin-principal(1)
