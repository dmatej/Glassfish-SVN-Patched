list-secure-admin-internalasadmin1유틸리티 list-secure-admin-internal-users(1)

제목
       list-secure-admin-internal-users - GlassFish Server DAS 및 인스턴스가
       상호 인증 및 관리 작업 권한 부여에 사용하는 사용자 이름 나열

시놉시스
           list-secure-admin-internal-users [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

설명
       list-secure-admin-internal-users 하위 명령은 GlassFish Server DAS 및
       인스턴스가 상호 인증 및 관리 작업 권한 부여에 사용하는 사용자 이름을
       나열합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --long, -l
           각 내부 사용자에 대한 세부 정보를 표시합니다. 기본값은 false입니다.

       --output, -o
           각 내부 사용자에 대한 특정 세부 정보를 표시합니다. 표시할 세부 정보
           및 순서를 지정하려면 쉼표로 구분된 목록을 사용합니다. 이러한 값은
           대소문자를 구분하지 않습니다.

           가능한 값은 다음과 같습니다.

           username
               내부 사용자에 대한 사용자 이름을 표시합니다.

           password-alias
               내부 사용자에 대한 비밀번호 별칭을 표시합니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

피연산자
       name
           세부 정보를 표시할 내부 사용자의 사용자 이름입니다.

예제
       예 1, 보안 관리에 사용되는 사용자 이름 나열
           이 예제에서는 GlassFish Server DAS 및 인스턴스가 상호 인증 및 관리
           작업 권한 부여에 사용하는 사용자 이름을 나열합니다.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                       2013년 2월 list-secure-admin-internal-users(1)
