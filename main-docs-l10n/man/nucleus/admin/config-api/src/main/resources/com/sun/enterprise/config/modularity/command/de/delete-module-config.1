delete-module-config(1) Unterbefehle des asadmin-Utili delete-module-config(1)

NAME
       delete-module-config - löscht die Standardkonfiguration eines Moduls
       aus domain.xml

ZUSAMMENFASSUNG
           delete-module-config [--help]
           [--target target]
           servicename

BESCHREIBUNG
       Der Unterbefehl delete-module-config löscht die Standardkonfiguration
       eines Moduls aus domain.xml und veranlasst das Modul, die im Modul
       enthaltene Standardkonfiguration zu verwenden.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem die Standardkonfiguration des Moduls
           gelöscht wird.

           Mögliche Werte:

           server
               Löscht die Standardkonfiguration eines Moduls für die
               Standardserverinstanz aus domain.xml. Dies ist der
               Standardwert.

           domain
               Löscht die Standardkonfiguration eines Moduls für die
               Standarddomain aus domain.xml.

           cluster-name
               Löscht die Standardkonfiguration eines Moduls aus domain.xml
               aus dem angegebenen Cluster.

           instance-name
               Löscht die Standardkonfiguration eines Moduls für die
               angegebene Instanz aus domain.xml.

OPERANDEN
       servicename
           Der Name des Moduls. Die Standardkonfiguration des Moduls wird aus
           der Datei domain.xml des Ziels gelöscht.

BEISPIELE
       Example 1, Standardkonfigurationen aus domain.xml löschen
           In diesem Beispiel wird die Standardkonfiguration des
           Webcontainermoduls aus der Datei domain.xml von domain1 in
           server-config (die Standardkonfiguration) gelöscht.

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                        06. Feb. 2013         delete-module-config(1)
