delete-cluster(1)       Sous-commandes de l'utilitaire       delete-cluster(1)

NOM
       delete-cluster - supprime un cluster GlassFish Server

SYNTAXE
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

DESCRIPTION
       La sous-commande delete-cluster supprime un cluster GlassFish Server.
       Vous pouvez uniquement supprimer les clusters ne contenant aucune
       instance GlassFish Server. Si vous supprimez un cluster qui contient
       des instances, arrêtez-les et supprimez-les avant de supprimer le
       cluster.

       Si la configuration nommée du cluster a été créée automatiquement pour
       celui-ci et qu'aucun autre cluster ou aucune autre instance non incluse
       dans un cluster ne fait référence à la configuration, celle-ci est
       supprimée lors de la suppression du cluster. La configuration créée
       automatiquement pour un cluster est nommée cluster-name-config, où
       cluster-name est le nom du cluster.

       Cette commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --autohadboverride
           Ne spécifiez pas cette option. Elle est conservée pour assurer la
           compatibilité avec les versions antérieures. Si vous spécifiez
           cette option, aucune erreur de syntaxe ne survient. Au lieu de
           cela, la sous-commande est exécutée et affiche un message
           d'avertissement indiquant que l'option est ignorée.

       --nodeagent
           Ne spécifiez pas cette option. Elle est conservée pour assurer la
           compatibilité avec les versions antérieures. Si vous spécifiez
           cette option, aucune erreur de syntaxe ne survient. Au lieu de
           cela, la sous-commande est exécutée et affiche un message
           d'avertissement indiquant que l'option est ignorée.

OPÉRANDES
       cluster-name
           Nom du cluster à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un cluster GlassFish Server
           Cet exemple montre comment supprimer le cluster GlassFish Server
           adccluster.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                         7 déc 2010                 delete-cluster(1)
