create-module-config(1) Unterbefehle des asadmin-Utili create-module-config(1)

NAME
       create-module-config - fügt die Standardkonfiguration eines Moduls zu
       domain.xml hinzu

ZUSAMMENFASSUNG
           create-module-config [--help]
           [--dryrun[={false|true}]]
           [--all[={false|true}]]
           [--target target]
           servicename

BESCHREIBUNG
       Der Unterbefehl create-module-config fügt die Standardkonfiguration
       eines Moduls zu domain.xml hinzu.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --dryrun
           Zeigt die Standardkonfiguration eines Moduls an, fügt sie jedoch
           nicht zu domain.xml hinzu.

           Der Standardwert ist false.

       --all
           Fügt alle Standardkonfigurationen von Modulen zu domain.xml hinzu,
           sofern sie noch nicht darin enthalten sind.

           Der Standardwert ist false.

       --target
           Gibt das Ziel an, für das die Standardkonfiguration des Moduls
           hinzugefügt wird.

           Mögliche Werte:

           server
               Fügt die Standardkonfiguration eines Moduls für die
               Standardserverinstanz zu domain.xml hinzu. Dies ist der
               Standardwert.

           domain
               Fügt die Standardkonfiguration eines Moduls für die
               Standarddomain zu domain.xml hinzu.

           cluster-name
               Fügt die Standardkonfiguration eines Moduls für das angegebene
               Cluster zu domain.xml hinzu.

           instance-name
               Fügt die Standardkonfiguration eines Moduls für die angegebene
               Instanz zu domain.xml hinzu.

OPERANDEN
       servicename
           Der Name des Moduls. Die Standardkonfiguration des Moduls wird zur
           Datei domain.xml des Ziels hinzugefügt.

BEISPIELE
       Example 1, Standardkonfigurationen zu domain.xml hinzufügen
           In diesem Beispiel wird die Standardkonfiguration des
           Webcontainermoduls zur Datei domain.xml von domain1 in
           server-config (die Standardkonfiguration) hinzugefügt. Um eine
           Vorschau der Konfiguration anzuzeigen, bevor diese zu domain.xml
           hinzugefügt wird, verwenden Sie die Option --dryrun.

               asadmin> create-module-config web-container
               Command create-module-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                        06. Feb. 2013         create-module-config(1)
