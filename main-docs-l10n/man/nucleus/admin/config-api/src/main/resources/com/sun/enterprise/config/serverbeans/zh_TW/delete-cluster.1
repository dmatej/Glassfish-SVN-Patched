delete-cluster(1)           asadmin 公用程式子命令           delete-cluster(1)

參考名
       delete-cluster - 刪除 GlassFish Server 叢集

概要
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

描述
       delete-cluster 子命令會刪除 GlassFish Server 叢集。僅在叢集不含
       GlassFish Server
       執行處理時方可刪除叢集。如果您要刪除的叢集含有任何執行處理，請先停止並刪除執行處理，然後再刪除叢集。

       如果叢集有為其自動建立的已命名組態，且沒有任何其他叢集或非叢集執行處理參照該組態，則會在刪除叢集時將該組態刪除。為叢集自動建立的組態名為
       cluster-name -config，其中 cluster-name 為叢集的名稱。

       只有遠端模式支援此命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --autohadboverride
           請勿指定此選項。保留這個選項是為了兼顧與舊版本的相容性。如果您指定此選項，並不會發生語法錯誤。相反地，子命令會順利執行，並顯示已忽略該選項的警告訊息。

       --nodeagent
           請勿指定此選項。保留這個選項是為了兼顧與舊版本的相容性。如果您指定此選項，並不會發生語法錯誤。相反地，子命令會順利執行，並顯示已忽略該選項的警告訊息。

運算元
       cluster-name
           要刪除的叢集名稱。

範例
       範例 1, 刪除 GlassFish Server 叢集
           此範例會刪除 GlassFish Server 叢集 adccluster。

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 7 日             delete-cluster(1)
