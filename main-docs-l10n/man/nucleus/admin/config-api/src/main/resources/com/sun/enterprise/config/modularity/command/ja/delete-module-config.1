delete-module-config(1asadminユーティリティのサブコマンdelete-module-config(1)

名前
       delete-module-config -
       domain.xmlからモジュールのデフォルト構成を削除します

概要
           delete-module-config [--help]
           [--target target]
           servicename

説明
       delete-module-configサブコマンドはdomain.xmlからモジュールのデフォルト構成を削除して、モジュール内に含まれるデフォルト構成が使用されるようにします。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           モジュールのデフォルト構成が削除されるターゲットを指定します。

           使用可能な値は次のとおりです:

           server
               モジュールのデフォルト構成を、デフォルトのサーバー・インスタンスのdomain.xmlから削除します。この値がデフォルト値です。

           domain
               モジュールのデフォルト構成を、デフォルトのドメインのdomain.xmlから削除します。

           cluster-name
               モジュールのデフォルト構成を、指定したクラスタのdomain.xmlから削除します。

           instance-name
               モジュールのデフォルト構成を、指定したインスタンスのdomain.xmlから削除します。

オペランド
       servicename
           モジュールの名前。モジュールのデフォルト構成がターゲットのdomain.xmlから削除されます。

例
       例 1, domain.xmlからのデフォルト構成の削除
           この例では、Webコンテナ・モジュールのデフォルト構成を、server-config
           (デフォルト構成)のdomain1のdomain.xmlファイルから削除します。

               asadmin> delete-module-config web-container
               Command delete-module-config executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-module-config(1), get-active-module-config(1)

       asadmin(1M)

Java EE 7                        2013年2月6日          delete-module-config(1)
