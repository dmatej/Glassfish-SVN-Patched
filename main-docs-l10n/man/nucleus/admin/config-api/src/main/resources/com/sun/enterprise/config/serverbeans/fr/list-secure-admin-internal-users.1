list-secure-admin-internSous-commandes de llist-secure-admin-internal-users(1)

NOM
       list-secure-admin-internal-users - répertorie les noms utilisateur que
       le serveur DAS et les instances GlassFish Server utilisent pour
       s'authentifier mutuellement et pour autoriser les opérations
       d'administration

SYNTAXE
           list-secure-admin-internal-users [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

DESCRIPTION
       La sous-commande list-secure-admin-internal-users répertorie les noms
       utilisateur que le serveur DAS et les instances GlassFish Server
       utilisent pour s'authentifier mutuellement et pour autoriser les
       opérations d'administration.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Affiche des informations détaillées sur chaque utilisateur interne.
           La valeur par défaut est false.

       --output, -o
           Affiche des détails spécifiques relatifs à chaque utilisateur
           interne. Utilisez une liste d'éléments séparés par des virgules
           pour indiquer les détails à afficher et leur ordre. Les valeurs ne
           respectent pas les majuscules/minuscules.

           Les valeurs possibles sont les suivantes :

           username
               Affiche le nom utilisateur de l'utilisateur interne.

           password-alias
               Affiche l'alias de mot de passe de l'utilisateur interne.

       --header, -h
           Indique si les en-têtes de colonne sont affichés lorsque l'option
           --long est utilisée. La valeur par défaut est true. Pour supprimer
           les en-têtes, définissez l'option --header sur false.

OPÉRANDES
       name
           Nom utilisateur de l'utilisateur interne pour lequel afficher des
           détails.

EXEMPLES
       Exemple 1, Liste des noms utilisateur pour l'administration sécurisée
           Dans cet exemple, les noms utilisateur que le serveur DAS et les
           instances GlassFish Server utilisent pour s'authentifier
           mutuellement et pour autoriser les opérations d'administration sont
           répertoriés.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                        13 fév. 20list-secure-admin-internal-users(1)
