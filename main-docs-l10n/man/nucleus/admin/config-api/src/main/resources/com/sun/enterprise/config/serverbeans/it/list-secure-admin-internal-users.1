list-secure-admin-internComandissecondari dlist-secure-admin-internal-users(1)

NOME
       list-secure-admin-internal-users - elenca i nomi utente che il server
       DAS e le istanze di GlassFish Server utilizzano per eseguire
       l'autenticazione reciproca e per autorizzare le operazioni di
       amministrazione.

SINTASSI
           list-secure-admin-internal-users [--help]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [name]

DESCRIZIONE
       Il comando secondario list-secure-admin-internal-users elenca i nomi
       utente che il server DAS e le istanze di GlassFish Server utilizzano
       per eseguire l'autenticazione reciproca e per autorizzare le operazioni
       di amministrazione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --long, -l
           Visualizza informazioni dettagliate su ciascun utente interno. Il
           valore predefinito è false.

       --output, -o
           Visualizza dettagli specifici su ciascun utente interno. Utilizzare
           una lista separata da virgole per specificare i dettagli da
           visualizzare e l'ordine di visualizzazione preferito. I valori non
           fanno distinzione tra maiuscole e minuscole.

           Di seguito sono riportati i valori possibili.

           username
               Visualizza il nome dell'utente interno.

           password-alias
               Visualizza l'alias di password dell'utente interno.

       --header, -h
           Specifica se le intestazioni di colonna vengono visualizzate quando
           si utilizza l'opzione --long. Il valore predefinito è true. Per
           eliminare le intestazioni, impostare l'opzione --header su false.

OPERANDI
       name
           Il nome utente dell'utente interno per il quale si desidera
           visualizzare i dettagli.

ESEMPI
       Esempio 1, Visualizzazione della lista di nomi utente per
       l'amministrazione sicura
           In questo esempio vengono elencati i nomi utente che il server DAS
           e le istanze di GlassFish Server utilizzano per eseguire
           l'autenticazione reciproca e per autorizzare le operazioni di
           amministrazione.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

Java EE 7                      13 febbraio list-secure-admin-internal-users(1)
