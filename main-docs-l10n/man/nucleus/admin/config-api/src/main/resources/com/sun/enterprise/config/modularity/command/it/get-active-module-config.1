get-active-module-configComandi secondari della utiget-active-module-config(1)

NOME
       get-active-module-config - visualizza la configurazione attiva corrente
       di un servizio o di un'istanza

SINTASSI
           get-active-module-config [--help]
           [--target target]
           [--all[={false|true}]]
           [servicename]

DESCRIZIONE
       Il comando secondario get-active-module-config visualizza la
       configurazione attiva corrente di un servizio o di un'istanza.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione per il comando secondario.

           Di seguito sono riportati i valori possibili.

           server
               Visualizza la configurazione attiva corrente di un servizio per
               l'istanza server predefinita. Questo è il valore predefinito.

           domain
               Visualizza la configurazione attiva corrente di un servizio per
               il dominio predefinito.

           cluster-name
               Visualizza la configurazione attiva corrente di un servizio per
               il cluster specificato.

           instance-name
               Visualizza la configurazione attiva corrente di un servizio per
               l'istanza specificata.

       --all
           Visualizza tutte le configurazioni attive correnti.

           Il valore predefinito è false.

OPERANDI
       servicename
           Il nome del modulo.

ESEMPI
       Esempio 1, Visualizzazione della configurazione attiva corrente
           In questo esempio viene visualizzata la configurazione attiva
           corrente per il servizio JMS in server-config (la configurazione
           predefinita).

               asadmin> get-active-module-config
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                       6 febbraio 2013    get-active-module-config(1)
