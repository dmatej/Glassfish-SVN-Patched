disable-secure-admin-priUnterbefehle des asaddisable-secure-admin-principal(1)

NAME
       disable-secure-admin-principal - deaktiviert das Zertifikat, das
       Zugriff im Rahmen einer sicheren Administration gewährt.

ZUSAMMENFASSUNG
           disable-secure-admin-principal
           [--help]
           --alias aliasname | DN

BESCHREIBUNG
       Der Unterbefehl disable-secure-admin-principal deaktiviert das als
       gültig geltende Zertifikat für eine Zugriffsautorisierung als Teil
       einer sicheren Administration.

       Sie müssen entweder die Option --alias oder den DN angeben.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --alias
           Der Aliasname des Zertifikats im Truststore GlassFish Server sucht
           im Truststore anhand des Aliasnamens nach dem Zertifikat. Wird es
           gefunden, deaktiviert GlassFish Server die Gültigkeit des
           entsprechenden DN für eine sichere Administration. Da alias-name
           ein Aliasname sein muss, der mit einem Zertifikat im Truststore
           verknüpft ist, können selbstsignierte Zertifikate sehr hilfreich
           sein.

OPERANDEN
       DN
           Der Distinguished Name (DN) des Zertifikats wird als eine durch
           Komma getrennte Liste in Anführungszeichen angegeben. Beispiel:
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US" .

BEISPIELE
       Example 1, Deaktiviert die Vertrauenswürdigkeit eines DN für eine
       sichere Administration
           Das folgende Beispiel zeigt, wie Sie die Vertrauenswürdigkeit eines
           DN für die Zugriffsautorisierung im Rahmen einer sicheren
           Administration deaktivieren.

               asadmin> disable-secure-admin-principal
               "CN=system.amer.oracle.com,OU=GlassFish,
               O=Oracle Corporation,L=Santa Clara,ST=California,C=US"

               Command disable-secure-admin-principal executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                        25. Jun. 201disable-secure-admin-principal(1)
