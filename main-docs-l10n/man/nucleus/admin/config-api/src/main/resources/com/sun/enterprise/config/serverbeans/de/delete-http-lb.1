delete-http-lb(1)       Unterbefehle des asadmin-Utili       delete-http-lb(1)

NAME
       delete-http-lb - löscht einen Load Balancer

ZUSAMMENFASSUNG
           delete-http-lb [--help] load_balancer_name

BESCHREIBUNG
       Verwenden Sie den Unterbefehl delete-http-lb, um einen physischen Load
       Balancer zu löschen.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       load_balancer_name
           Der Name des Load Balancers, der gelöscht werden soll.

BEISPIELE
       Beispiel 1, Load Balancer-Konfigurationen löschen
           In diesem Beispiel wird eine Load Balancer-Konfiguration namens
           mylb gelöscht.

               asadmin> delete-http-lb mylb

               Command delete-http-lb executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb(1), list-http-lbs(1)

       asadmin(1M)

Java EE 7                        25. Aug. 2010               delete-http-lb(1)
