disable-secure-admin-princasadmin 유틸리티 하disable-secure-admin-principal(1)

제목
       disable-secure-admin-principal - 보안 관리에서 액세스 권한 부여에
       인증서를 사용 안함으로 설정

시놉시스
           disable-secure-admin-principal
           [--help]
           --alias aliasname | DN

설명
       disable-secure-admin-principal 하위 명령은 보안 관리의 일부로 액세스
       권한 부여에 적합하도록 인증서를 사용 안함으로 설정합니다.

       --alias 옵션 또는 DN을 지정해야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --alias
           트러스트 저장소에서 인증서의 별칭 이름입니다. GlassFish
           Server은(는) 해당 별칭을 사용하여 트러스트 저장소에서 인증서를
           조회하고, 발견한 경우 보안 관리에 적합하도록 해당 DN을 사용
           안함으로 설정합니다.  alias-name은 현재 트러스트 저장소에 있는
           인증서와 연관된 별칭이어야 하므로 자체 서명된 인증서에 매우 유용할
           수 있습니다.

피연산자
       DN
           인증서의 식별 이름으로, 따옴표 안에 쉼표로 구분된 목록으로
           지정됩니다. 예를 들어,
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US"입니다.

예제
       예 1, 보안 관리에 DN 신뢰 사용 안함
           다음 예제는 보안 관리에서 액세스 권한 부여에 DN 신뢰를 사용
           안함으로 설정하는 방법을 보여줍니다.

               asadmin> disable-secure-admin-principal
               "CN=system.amer.oracle.com,OU=GlassFish,
               O=Oracle Corporation,L=Santa Clara,ST=California,C=US"

               Command disable-secure-admin-principal executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                       2011년 6월 25disable-secure-admin-principal(1)
