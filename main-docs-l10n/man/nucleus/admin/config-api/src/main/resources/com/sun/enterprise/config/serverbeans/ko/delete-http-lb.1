delete-http-lb(1)         asadmin 유틸리티 하위 명령         delete-http-lb(1)

제목
       delete-http-lb - 로드 밸런서 삭제

시놉시스
           delete-http-lb [--help] load_balancer_name

설명
       물리적 로드 밸런서를 삭제하려면 delete-http-lb 하위 명령을 사용합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       load_balancer_name
           삭제할 로드 밸런서의 이름입니다.

예제
       예 1, 로드 밸런서 구성 삭제
           이 예제는 mylb라는 로드 밸런서 구성을 삭제합니다.

               asadmin> delete-http-lb mylb

               Command delete-http-lb executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb(1), list-http-lbs(1)

       asadmin(1M)

Java EE 7                       2010년 8월 25일              delete-http-lb(1)
