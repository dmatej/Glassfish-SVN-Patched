delete-cluster(1)       Subcomandos do utilitário asad       delete-cluster(1)

NOME
       delete-cluster - deleta um cluster do GlassFish Server

SINOPSE
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

DESCRIçãO
       O subcomando delete-cluster deleta o cluster do GlassFish Server. Um
       cluster pode ser deletado apenas se não contiver instâncias do
       GlassFish Server. Se um cluster que você estiver deletando contiver
       quaisquer instâncias, pare e delete a instância antes de deletar o
       cluster.

       Se a configuração nomeada do cluster foi criada automaticamente para o
       cluster e nenhum outro cluster ou instância não clusterizada fizer
       referência a ela, ela será deletada quando o cluster for deletado. Uma
       configuração que seja automaticamente criada para um cluster com o nome
       cluster-name-config, onde cluster-name é o nome do cluster.

       Este comando é suportado só no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --autohadboverride
           Não especificar essa opção. Esta opção é retida para
           compatibilidade com versões anteriores. se você especificar esta
           opção, um erro de sintaxe não ocorrerá. Em vez disso, o subcomando
           é executado com sucesso e exibe uma mensagem de advertência
           informando que a opção será ignorada.

       --nodeagent
           Não especificar essa opção. Esta opção é retida para
           compatibilidade com versões anteriores. se você especificar esta
           opção, um erro de sintaxe não ocorrerá. Em vez disso, o subcomando
           é executado com sucesso e exibe uma mensagem de advertência
           informando que a opção será ignorada.

OPERANDOS
       cluster-name
           O nome do cluster a ser deletado.

EXEMPLOS
       Example 1, Deletando um Cluster do GlassFish Server
           Este exemplo deleta o cluster adccluster do GlassFish Server.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                         7 dez 2010                 delete-cluster(1)
