list-secure-admin-princiSous-commandes de l'utilist-secure-admin-principals(1)

NOM
       list-secure-admin-principals - répertorie les certificats pour lesquels
       GlassFish Server accepte les demandes d'administration provenant des
       clients

SYNTAXE
           list-secure-admin-principals [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]
           [name]

DESCRIPTION
       La sous-commande list-secure-admin-principals répertorie les
       certificats pour lesquels GlassFish Server accepte les demandes
       d'administration provenant des clients.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Affiche des informations détaillées sur les certificats pour
           lesquels GlassFish Server accepte les demandes d'administration
           provenant des clients. La valeur par défaut est false.

       --output, -o
           Affiche des détails spécifiques relatifs aux certificats pour
           lesquels GlassFish Server accepte les demandes d'administration
           provenant des clients. Utilisez une liste d'éléments séparés par
           des virgules pour indiquer les détails à afficher et leur ordre.
           Les valeurs ne respectent pas les majuscules/minuscules.

           Les valeurs possibles sont les suivantes :

           DN
               Affiche le nom distinctif de chaque certificat.

       --header, -h
           Indique si les en-têtes de colonne sont affichés lorsque l'option
           --long est utilisée. La valeur par défaut est true. Pour supprimer
           les en-têtes, définissez l'option --header sur false.

OPÉRANDES
       name
           Nom distinctif du certificat, indiqué sous la forme d'une liste de
           valeurs séparées par des virgules, entre guillemets. Par exemple :
           "CN=system.amer.oracle.com,OU=GlassFish,O=Oracle
           Corporation,L=Santa Clara,ST=California,C=US".

EXEMPLES
       Exemple 1, Liste des certificats
           Dans cet exemple, les certificats pour lesquels GlassFish Server
           accepte les demandes d'administration provenant des clients sont
           répertoriés.

               asadmin> list-secure-admin-principals
               CN=localhost,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               CN=localhost-instance,OU=GlassFish,O=Oracle Corporation,L=Santa Clara,ST=California,C=US
               Command list-secure-admin-principals executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       enable-secure-admin(1)

       enable-secure-admin-principal(1)

       asadmin(1M)

Java EE 7                        13 fév. 2013  list-secure-admin-principals(1)
