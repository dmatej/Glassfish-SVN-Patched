get-active-module-config(1asadmin 유틸리티 하위 명령

제목
       get-active-module-config - 서비스 또는 인스턴스의 현재 활성 구성 표시

시놉시스
           get-active-module-config [--help]
           [--target target]
           [--all={false|true}]
           [service_name]

설명
       get-active-module-config 하위 명령은 서비스 또는 인스턴스의 현재 활성
       구성을 표시합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           특정 서비스 또는 전체 인스턴스의 현재 활성 구성을 볼 대상을
           지정합니다.

           가능한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스에 대한 현재 활성 구성을 표시합니다. 이 값은
               기본값입니다.

           domain
               기본 도메인에 대한 현재 활성 구성을 표시합니다.

           cluster-name
               지정된 클러스터에 대한 현재 활성 구성을 표시합니다.

           instance-name
               지정된 인스턴스에 대한 현재 활성 구성을 표시합니다.

       --all
           현재 활성 구성을 모두 표시합니다.

           기본값은 false입니다.

피연산자
       service_name
           현재 활성 구성을 표시할 모듈 이름입니다.

예제
       예 1, 현재 활성 구성 표시
           이 예제는 server-config(기본 구성)의 JMS 서비스에 대한 현재 활성
           구성을 표시합니다.

               asadmin> get-active-module-config jms-service
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                       2013년 2월 6일     get-active-module-config(1)
