get-active-module-confasadminユーティリティのサブコget-active-module-config(1)

名前
       get-active-module-config -
       サービスまたはインスタンスの現在アクティブな構成を表示します

概要
           get-active-module-config [--help]
           [--target target]
           [--all={false|true}]
           [service_name]

説明
       get-active-module-configサブコマンドは、サービスまたはインスタンスの現在アクティブな構成を表示します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           特定のサービスまたはインスタンス全体の現在アクティブな構成を表示するターゲットを指定します。

           使用可能な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスの現在アクティブな構成を表示します。これがデフォルト値です。

           domain
               デフォルトのドメインの現在アクティブな構成を表示します。

           cluster-name
               指定したクラスタの現在アクティブな構成を表示します。

           instance-name
               指定したインスタンスの現在アクティブな構成を表示します。

       --all
           現在アクティブな構成をすべて表示します。

           デフォルト値はfalseです。

オペランド
       service_name
           現在アクティブな構成を表示するモジュール名。

例
       例 1, 現在アクティブな構成の表示
           この例では、server-config
           (デフォルト構成)のJMSサービスの現在アクティブな構成を表示します。

               asadmin> get-active-module-config jms-service
               At location: domain/configs/config[server-config]
               <jms-service default-jms-host="default_JMS_host" type="EMBEDDED"
                 <jms-host port="7676" host="localhost" name="default_JMS_host"/>
               </jms-service>
               Command get-active-module-config executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-module-config(1), delete-module-config(1)

       asadmin(1M)

Java EE 7                        2013年2月6日      get-active-module-config(1)
