delete-cluster(1)       Unterbefehle des asadmin-Utili       delete-cluster(1)

NAME
       delete-cluster - löscht ein GlassFish Server-Cluster

ZUSAMMENFASSUNG
           delete-cluster [--help]
           [--autohadboverride={true|false}] [--node-agent=node-agent--name]
           cluster-name

BESCHREIBUNG
       Der Unterbefehl delete-cluster löscht ein GlassFish Server-Cluster. Ein
       Cluster kann nur dann gelöscht werden, wenn es keine GlassFish
       Server-Instanzen enthält. Wenn ein zu löschendes Cluster Instanzen
       enthält, stoppen und löschen Sie zuerst die Instanzen, bevor Sie das
       Cluster löschen.

       Wenn die benannte Konfiguration eines Clusters automatisch für das
       Cluster erstellt wurde und keine anderen Cluster oder Instanzen ohne
       Cluster auf die Konfiguration verweisen, wird beim Löschen des Clusters
       die Konfiguration gelöscht. Eine automatisch für ein Cluster erstellte
       Konfiguration erhält den Namen cluster-name-config, wobei cluster-name
       für den Namen des Clusters steht.

       Dieser Befehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --autohadboverride
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

       --nodeagent
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

OPERANDEN
       cluster-name
           Der Name des Clusters, das gelöscht werden soll.

BEISPIELE
       Beispiel 1, GlassFish Server-Cluster löschen
           In diesem Beispiel wird das GlassFish Server-Cluster adccluster
           gelöscht.

               asadmin> delete-cluster adccluster
               Command delete-cluster executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-cluster(1), delete-instance(1), delete-local-instance(1),
       list-clusters(1), start-cluster(1), stop-instance(1),
       stop-local-instance(1), stop-cluster(1)

       asadmin(1M)

Java EE 7                        7. Dez. 2010                delete-cluster(1)
