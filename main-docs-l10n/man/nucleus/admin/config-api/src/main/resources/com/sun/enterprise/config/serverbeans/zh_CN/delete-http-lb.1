delete-http-lb(1)           asadmin 实用程序子命令           delete-http-lb(1)

名称
       delete-http-lb - 删除负载平衡器

提要
           delete-http-lb [--help] load_balancer_name

说明
       使用 delete-http-lb 子命令删除物理负载平衡器。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       load_balancer_name
           要删除的负载平衡器的名称。

示例
       Example 1, 删除负载平衡器配置
           此示例删除名为 mylb 的负载平衡器配置。

               asadmin> delete-http-lb mylb

               Command delete-http-lb executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb(1), list-http-lbs(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日             delete-http-lb(1)
