create-http-lb-config(1)Subcomandos do utilitário asadcreate-http-lb-config(1)

NOME
       create-http-lb-config - cria uma configuração do balanceador de carga

SINOPSE
           create-http-lb-config [--help] [--responsetimeout 60]
            [httpsrouting=false] [--reloadinterval 60]
            [--monitor=false] [--property (name=value)[:name=value]*]
            --target target | config_name

DESCRIçãO
       Utilize o subcomando create-http-lb-config para criar uma configuração
       de balanceador de carga. Essa configuração se aplica ao balanceamento
       de carga no caminho de HTTP. Depois de usar o subcomando para criar o
       arquivo de configuração do balanceador de carga, crie o balanceador de
       carga executando create-http-lb.

       É preciso especificar um alvo, um nome de configuração ou ambos. Caso
       você não especifique um alvo, esta configuração é criada sem um alvo e
       um é depois adicionado usando create-http-lb-ref. Caso você não
       especifique um nome de configuração, um nome é criado com base no nome
       do alvo. Se ambos forem especificados, a configuração é criada com o
       nome especificado, referenciando o alvo especificado.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --responsetimeout
           O tempo em segundos dentro do qual uma instância de servidor deve
           retornar uma resposta. Se nenhuma resposta for recebida dentro do
           período de tempo, o servidor é considerado como estando em más
           condições. Se definido como um número positivo e a solicitação for
           imutável, a solicitação será repetida. Se a solicitação não for
           imutável, uma página de erro será retornada. Se definido como 0,
           nenhum time-out é utilizado. O default é 60.

       --httpsrouting
           Se definido como true, as solicitações HTTPS para o balanceador de
           carga resultam em solicitações HTTPS para a instância do servidor.
           Se definido como false, as solicitações HTTPS para o balanceador de
           carga resultam em solicitações HTTPS para a instância do servidor.
           O default é false.

       --reloadinterval
           O intervalo entre verificações de alterações ao arquivo de
           configuração do balanceador de carga loadbalancer.xml. Quando a
           verificação detectar alterações, o arquivo de configuração é
           recarregado. Um valor 0 desativa o recarregamento.

       --monitor
           Especifica se o monitoramento está ativado. O default é false.

       --routecookie
           Essa opção está obsoleta. O valor é sempre true.

       --property
           Os pares nome/valor do atributo são opcionais para configurar o
           balanceador de carga.

       --target
           Especifica o alvo para o qual a configuração do balanceador de
           carga se aplica. Se não for especificado um alvo, a configuração do
           balanceador de carga é criada sem um alvo. É possível especificar
           alvos posteriormente usando o subcomandocreate-http-lb-ref.

           Os valores válidos são:

           ·   cluster_name- Especifica que solicitações para esse cluster
               serão tratadas pelo balanceador de carga.

           ·   stand-alone_instance_name- Especifica se solicitações da
               instância standalone serão tratadas pelo balanceador de carga.

OPERANDOS
       config_name
           O nome da nova configuração do balanceador de carga. Esse nome não
           pode conflitar com quaisquer outros grupos de balanceador de carga,
           agentes, configurações, clusters ou instâncias de servidor no
           domínio. Se você não especificar um nome, o nome da configuração do
           balanceador de carga será baseado no nome do alvo
           target_name-http-lb-config.

EXEMPLOS
       Example 1, Criando uma Configuração do Balanceador de Carga
           Esse exemplo cria uma configuração de balanceador de carga em um
           alvo chamado mycluster e a configuração do balanceador de carga
           chamada mylbconfigname.

               asadmin> create-http-lb-config --target mycluster mylbconfigname

               Command create-http-lb-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-http-lb-config(1), list-http-lb-configs(1), create-http-lb(1)

       asadmin(1M)

Java EE 7                    25 de agosto de 2010     create-http-lb-config(1)
