delete-domain(1)        Subcomandos do utilitário asad        delete-domain(1)

NOME
       delete-domain - deleta um domínio

SINOPSE
           delete-domain [--help] [--domaindir domaindir] domain-name

DESCRIçãO
       O subcomando delete-domain deleta o domínio especificado. O domínio já
       existe e deve ser interrompido.

       Este subcomando é suportado só no modo local.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --domaindir
           O diretório em que o domínio a ser deletado está localizado. Se
           especificado, o caminho deverá estar acessível no sistema de
           arquivos. Se não especificado, o domínio no diretório-raiz do
           domínio, que assume como default as-install/domains, será deletado.

OPERANDOS
       domain-name
           O nome exclusivo do domínio que você deseja deletar.

EXEMPLOS
       Example 1, Deletando um Domínio
           Esse exemplo deleta um domínio com o nome mydomain4 do diretório de
           domínios default.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Example 2, deletando um Domínio de um Local Alternativo
           Este exemplo deleta um domínio com o nome sampleDomain do diretório
           /home/someuser/domains.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                         16 out 2009                 delete-domain(1)
