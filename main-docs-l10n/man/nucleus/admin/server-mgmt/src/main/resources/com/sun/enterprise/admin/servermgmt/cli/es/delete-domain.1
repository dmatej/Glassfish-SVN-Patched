delete-domain(1)        Subcomandos de la Utilidad asa        delete-domain(1)

NOMBRE DE REFERENCIA
       delete-domain - Suprime un dominio.

SINOPSIS
           delete-domain [--help] [--domaindir domaindir] domain-name

DESCRIPCIÓN
       El subcomando delete-domain suprime el dominio especificado. El dominio
       ya debe existir y debe estar parado.

       Este subcomando sólo está soportado en modo local.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --domaindir
           Directorio en el que está ubicado el dominio que se va a suprimir.
           Si se especifica, la ruta de acceso debe ser accesible en el
           sistema de archivos. Si no se especifica, se suprimirá el dominio
           ubicado en el directorio raíz de dominios, que tiene como valor por
           defecto as-install/domains.

OPERANDOS
       domain-name
           Nombre exclusivo del dominio que desea suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Dominio
           En este ejemplo se suprime un dominio denominado mydomain4 del
           directorio de dominios por defecto.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Ejemplo 2, Supresión de un Dominio de una Ubicación Alternativa
           En este ejemplo se suprime un dominio denominado sampleDomain del
           directorio /home/someuser/domains.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                    16 de octubre de 2009            delete-domain(1)
