delete-domain(1)          asadmin 유틸리티 하위 명령          delete-domain(1)

제목
       delete-domain - 도메인 삭제

시놉시스
           delete-domain [--help] [--domaindir domaindir] domain-name

설명
       delete-domain 하위 명령은 지정된 도메인을 삭제합니다. 도메인이 존재해야
       하며 정지되어야 합니다.

       이 하위 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --domaindir
           삭제할 도메인이 있는 디렉토리입니다. 지정된 경우 파일 시스템에서
           해당 경로에 액세스할 수 있어야 합니다. 지정되지 않은 경우 기본
           도메인 루트 디렉토리인 as-install/domains 아래의 도메인이
           삭제됩니다.

피연산자
       domain-name
           삭제할 도메인의 고유 이름입니다.

예제
       예 1, 도메인 삭제
           이 예제는 기본 도메인 디렉토리에서 mydomain4라는 도메인을
           삭제합니다.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       예 2, 대체 위치에서 도메인 삭제
           이 예제는 /home/someuser/domains 디렉토리에서 sampleDomain이라는
           도메인을 삭제합니다.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                      2009년 10월 16일               delete-domain(1)
