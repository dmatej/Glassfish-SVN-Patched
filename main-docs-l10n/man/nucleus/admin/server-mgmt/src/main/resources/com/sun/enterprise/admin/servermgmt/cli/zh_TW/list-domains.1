list-domains(1)             asadmin 公用程式子命令             list-domains(1)

參考名
       list-domains - 列出指定目錄中的網域

概要
           list-domains [--help]
           [--domaindir domaindir]
           [--long[={false|true}]]
           [--header[={false|true}]]

描述
       list-domains
       子命令會列出指定網域目錄中的網域。如果未指定網域目錄，則會列出預設目錄中的網域。如果有一個以上的網域目錄，則必須指定
       --domaindir 選項。其中會包含每個網域的狀態。

       只有本機模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --domaindir
           會列出網域的目錄。如果指定此項目，則該路徑必須可於檔案主體中存取。如果未指定，則會列出預設根目錄中的網域。網域根目錄的預設位置為
           as-install/domains。

       --long, -l
           顯示列出的網域中與管理伺服器有關的詳細資訊，包括主機名稱與連接埠號碼。預設值為
           false。

       --header, -h
           指定使用 --long 選項時是否顯示標頭。預設值為
           true。若不要顯示標頭，請將 --header 選項設為 false。

範例
       範例 1, 列出網域
           此範例會列出預設目錄中的網域。

               asadmin> list-domains
               Name: domain1 Status: Running
               Name: domain2 Status: Not running
               Name: domain4 Status: Running, restart required to apply configuration changes
               Command list-domains executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-domain(1), delete-domain(1), start-domain(1), stop-domain(1),

       asadmin(1M)

Java EE 7                     2013 年 2 月 12 日               list-domains(1)
