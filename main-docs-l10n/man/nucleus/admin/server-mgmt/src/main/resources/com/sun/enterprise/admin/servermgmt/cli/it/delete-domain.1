delete-domain(1)        Comandi secondari della utilit        delete-domain(1)

NOME
       delete-domain - elimina un dominio

SINTASSI
           delete-domain [--help] [--domaindir domaindir] domain-name

DESCRIZIONE
       Il comando secondario delete-domain consente di eliminare il dominio
       specificato. Il dominio deve già esistere e deve essere arrestato.

       Questo comando secondario è supportato solo in modalità locale.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --domaindir
           La directory che contiene il dominio da eliminare. Se viene
           specificata, il percorso deve essere accessibile nel file system.
           Se non viene specificata, viene eliminato il dominio nella
           directory radice, ovvero as-install/domains per impostazione
           predefinita.

OPERANDI
       domain-name
           Il nome univoco del dominio da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un dominio
           In questo esempio il dominio denominato mydomain4 viene eliminato
           dalla directory predefinita domains.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Esempio 2, Eliminazione di un dominio da una posizione alternativa
           In questo esempio il dominio denominato sampleDomain viene
           eliminato dalla directory /home/someuser/domains.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                       16 ottobre 2009               delete-domain(1)
