list-domains(1)         Subcomandos de la Utilidad asa         list-domains(1)

NOMBRE DE REFERENCIA
       list-domains - Muestra los dominios en el directorio especificado.

SINOPSIS
           list-domains [--help]
           [--domaindir domaindir]
           [--long[={false|true}]]
           [--header[={false|true}]]

DESCRIPCIÓN
       El subcomando list-domains enumera los dominios en el directorio de
       dominios especificado. Si no se especifica el directorio de dominios,
       se muestran los dominios del directorio por defecto. Si hay más de un
       directorio de dominios, se debe especificar la opción --domaindir. Se
       incluye el estado de cada dominio.

       Este subcomando sólo está soportado en modo local.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --domaindir
           El directorio del que se deben enumerar los dominios. Si se
           especifica, la ruta de acceso debe ser accesible en la raíz de
           archivos. Si no se especifica, se muestran los dominios del
           directorio raíz de dominios. La ubicación por defecto del
           directorio raíz de dominios es as-install/domains.

       --long, -l
           Muestra información detallada sobre los servidores de
           administración en los dominios mostrados, incluidos los nombres de
           host y números de puerto. El valor por defecto es false.

       --header, -h
           Especifica si se muestra una cabecera cuando se usa la opción
           --long . El valor por defecto es true. Para suprimir la cabecera,
           defina la opción --header en false.

EJEMPLOS
       Ejemplo 1, Lista de Dominios
           En este ejemplo se muestran los dominios del directorio por
           defecto.

               asadmin> list-domains
               Name: domain1 Status: Running
               Name: domain2 Status: Not running
               Name: domain4 Status: Running, restart required to apply configuration changes
               Command list-domains executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-domain(1), delete-domain(1), start-domain(1), stop-domain(1),

       asadmin(1M)

Java EE 7                    12 de febrero de 2013             list-domains(1)
