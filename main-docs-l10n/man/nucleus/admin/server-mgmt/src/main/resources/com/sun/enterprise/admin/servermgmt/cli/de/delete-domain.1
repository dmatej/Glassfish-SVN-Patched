delete-domain(1)        Unterbefehle des asadmin-Utili        delete-domain(1)

NAME
       delete-domain - löscht eine Domain

ZUSAMMENFASSUNG
           delete-domain [--help] [--domaindir domaindir] domain-name

BESCHREIBUNG
       Der Unterbefehl delete-domain löscht die angegebene Domain. Die Domain
       muss bereits vorhanden und gestoppt sein.

       Dieser Unterbefehl wird nur im lokalen Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --domaindir
           Das Verzeichnis, in dem sich die Domain befindet, die gelöscht
           werden soll. Wenn dies angegeben wird, muss der Pfad im Dateisystem
           zugänglich sein. Wenn nicht angegeben, wird die Domain im
           Standard-Root-Verzeichnis der Domain as-install/domains gelöscht.

OPERANDEN
       domain-name
           Der eindeutige Name der Domain, die Sie löschen möchten.

BEISPIELE
       Example 1, Domains löschen
           In diesem Beispiel wird eine Domain namens mydomain4 aus dem
           standardmäßigen Domainverzeichnis gelöscht.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Example 2, Domains aus einem alternativen Verzeichnis löschen
           In diesem Beispiel wird eine Domain namens sampleDomain aus dem
           Verzeichnis /home/someuser/domains gelöscht.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                        16. Okt. 2009                delete-domain(1)
