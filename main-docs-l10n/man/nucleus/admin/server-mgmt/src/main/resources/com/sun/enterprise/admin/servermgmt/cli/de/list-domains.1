list-domains(1)         Unterbefehle des asadmin-Utili         list-domains(1)

NAME
       list-domains - listet die Domains im angegebenen Verzeichnis auf

ZUSAMMENFASSUNG
           list-domains [--help]
           [--domaindir domaindir]
           [--long[={false|true}]]
           [--header[={false|true}]]

BESCHREIBUNG
       Der Unterbefehl list-domains listet die Domains im angegebenen
       Domainverzeichnis auf. Ist kein Domainverzeichnis angegeben, werden die
       Domains im Standardverzeichnis aufgelistet. Sind mehrere
       Domainverzeichnisse vorhanden, muss die Option --domaindir angegeben
       werden. Der Status jeder Domain wird ebenfalls angegeben.

       Dieser Unterbefehl wird nur im lokalen Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --domaindir
           Das Verzeichnis, in dem die Domains aufgelistet werden sollen. Wenn
           dies angegeben wird, muss der Pfad im Dateisystem zugänglich sein.
           Wenn dies nicht angegeben wird, werden die Domains im
           Domain-Root-Verzeichnis aufgelistet. Das Standard-Root-Verzeichnis
           der Domain ist as-install/domains.

       --long, -l
           Zeigt detaillierte Informationen zu den Administrationsservern in
           den aufgelisteten Domains an, einschließlich Hostnamen und
           Portnummern. Der Standardwert ist false.

       --header, -h
           Legt fest, ob bei Verwendung der Option --long ein Header angezeigt
           wird. Der Standardwert ist true. Um den Header zu unterdrücken,
           legen Sie für die Option --header den Wert false fest.

BEISPIELE
       Example 1, Domains auflisten
           In diesem Beispiel werden die Domains im Standardverzeichnis
           aufgelistet.

               asadmin> list-domains
               Name: domain1 Status: Running
               Name: domain2 Status: Not running
               Name: domain4 Status: Running, restart required to apply configuration changes
               Command list-domains executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-domain(1), delete-domain(1), start-domain(1), stop-domain(1),

       asadmin(1M)

Java EE 7                        12. Feb. 2013                 list-domains(1)
