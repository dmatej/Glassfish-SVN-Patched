delete-domain(1)            asadmin 实用程序子命令            delete-domain(1)

名称
       delete-domain - 删除域

提要
           delete-domain [--help] [--domaindir domaindir] domain-name

说明
       delete-domain 子命令删除指定的域。此域必须已存在而且已停止。

       该子命令仅在本地模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --domaindir
           要删除的域所在的目录。如果指定，该路径在文件系统中必须是可访问的。如果未指定，则将删除域根目录
           (默认为 as-install/domains) 下的域。

操作数
       domain-name
           要删除的域的唯一名称。

示例
       Example 1, 删除域
           此示例从默认的域目录中删除名为 mydomain4 的域。

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Example 2, 从备用位置删除域
           此示例从 /home/someuser/domains 目录中删除名为 sampleDomain 的域。

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 16 日             delete-domain(1)
