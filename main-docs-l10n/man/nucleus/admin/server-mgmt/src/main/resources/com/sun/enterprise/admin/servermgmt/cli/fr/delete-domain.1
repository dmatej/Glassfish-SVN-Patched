delete-domain(1)        Sous-commandes de l'utilitaire        delete-domain(1)

NOM
       delete-domain - supprime un domaine

SYNTAXE
           delete-domain [--help] [--domaindir domaindir] domain-name

DESCRIPTION
       La sous-commande delete-domain supprime le domaine indiqué. Le domaine
       doit déjà exister et être arrêté.

       Cette sous-commande est prise en charge en mode local uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --domaindir
           Répertoire dans lequel se situe le domaine à supprimer. S'il est
           spécifié, le chemin doit être accessible dans le système de
           fichiers. S'il n'est pas spécifié, le domaine sous le répertoire
           racine de domaines, dont la valeur par défaut est
           as-install/domains, est supprimé.

OPÉRANDES
       domain-name
           Nom unique du domaine à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un domaine
           Cet exemple montre comment supprimer un domaine nommé mydomain4 du
           répertoire des domaines par défaut.

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       Exemple 2, Suppression d'un domaine à partir d'un autre emplacement
           Cet exemple montre comment supprimer le domaine nommé sampleDomain
           du répertoire /home/someuser/domains.

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                         16 oct 2009                 delete-domain(1)
