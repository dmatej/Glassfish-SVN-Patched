list-domains(1)             asadmin 实用程序子命令             list-domains(1)

名称
       list-domains - 列出指定目录中的域

提要
           list-domains [--help]
           [--domaindir domaindir]
           [--long[={false|true}]]
           [--header[={false|true}]]

说明
       list-domains
       子命令列出指定域目录中的域。如果未指定域目录，将列出默认目录中的域。如果有多个域目录，则必须指定
       --domaindir 选项。将包括各个域的状态。

       该子命令仅在本地模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --domaindir
           要列出域的目录。如果指定，该路径在文件系统中必须是可访问的。如果未指定，则会列出域根目录中的域。域根目录的默认位置为
           as-install/domains。

       --long, -l
           显示有关所列域中的管理服务器的详细信息，包括主机名和端口号。默认值为假

       --header, -h
           指定在使用 --long
           选项时是否显示标头。默认值为真
           选项设置为假

示例
       Example 1, 列出域
           此示例列出默认目录中的域。

               asadmin> list-domains
               Name: domain1 Status: Running
               Name: domain2 Status: Not running
               Name: domain4 Status: Running, restart required to apply configuration changes
               Command list-domains executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-domain(1), delete-domain(1), start-domain(1), stop-domain(1),

       asadmin(1M)

Java EE 7                     2013 年 2 月 12 日               list-domains(1)
