verify-domain-xml(1)    Subcomandos do utilitário asad    verify-domain-xml(1)

NOME
       verify-domain-xml - verifica o conteúdo do arquivo domain.xml

SINOPSE
           verify-domain-xml [--help]
           [--domaindir domain-dir] [domain-name]

DESCRIçãO
       Verifica o conteúdo do arquivo domain.xml ao verificar o seguinte:

       ·   Se o parsing do arquivo domain.xml pode ser feito.

       ·   Se os nomes para elementos que os possuem são únicos.

       Este subcomando é suportado só no modo local.

OPçõES
       -h --help
           Exibe o texto de ajuda do subcomando.

       --domaindir
           Especifica o diretório-raiz do domínio, em que os domínios estão
           localizados. O caminho deve ser acessível no sistema de arquivo. O
           valor default é as-install/domains.

OPERANDOS
       domain_name
           Especifica o nome do domínio. O default é
            domain1.

EXEMPLOS
       Example 1, Utilizando verificar domínio xml

               asadmin> verify-domain-xml
               All Tests Passed.
               domain.xml is valid

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       asadmin(1M)

Java EE 7                   17 de novembro de 2010        verify-domain-xml(1)
