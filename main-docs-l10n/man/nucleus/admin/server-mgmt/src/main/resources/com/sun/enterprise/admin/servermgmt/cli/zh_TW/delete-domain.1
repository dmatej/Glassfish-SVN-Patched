delete-domain(1)            asadmin 公用程式子命令            delete-domain(1)

參考名
       delete-domain - 刪除網域

概要
           delete-domain [--help] [--domaindir domaindir] domain-name

描述
       delete-domain 子命令會刪除指定網域。該網域必須已經存在，且已停止使用。

       只有本機模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --domaindir
           要刪除的網域所在的目錄。如果指定，該路徑必須在檔案系統中可以存取。如果未指定，會刪除網域根目錄
           (預設為 as-install/domains) 底下的網域。

運算元
       domain-name
           您要刪除的網域的唯一名稱。

範例
       範例 1, 刪除網域
           此範例會從預設網域目錄刪除名為 mydomain4 的網域。

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       範例 2, 從替用位置刪除網域
           此範例會從 /home/someuser/domains 目錄刪除名為 sampleDomain
           的網域。

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 16 日             delete-domain(1)
