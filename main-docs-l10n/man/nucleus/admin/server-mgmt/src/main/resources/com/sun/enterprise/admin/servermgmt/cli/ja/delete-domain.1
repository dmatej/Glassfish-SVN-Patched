delete-domain(1)      asadminユーティリティのサブコマンド     delete-domain(1)

名前
       delete-domain - ドメインを削除します

概要
           delete-domain [--help] [--domaindir domaindir] domain-name

説明
       delete-domainサブコマンドは、指定したドメインを削除します。ドメインはすでに存在して、停止している必要があります。

       このサブコマンドは、ローカル・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --domaindir
           削除するドメインがあるディレクトリ。指定した場合、ファイル・システム内でこのパスにアクセスできる必要があります。指定しない場合は、as-install/domainsにデフォルト設定されているドメイン・ルート・ディレクトリの下にあるドメインが削除されます。

オペランド
       domain-name
           削除するドメインの一意の名前。

例
       例 1, ドメインの削除
           この例では、mydomain4という名前のドメインをデフォルト・ドメイン・ディレクトリから削除します。

               asadmin> delete-domain mydomain4
               Domain mydomain4 deleted.
               Command delete-domain executed successfully.

       例 2, 別の場所のドメインの削除
           この例では、/home/someuser/domainsディレクトリからsampleDomainという名前のドメインを削除します。

               asadmin> delete-domain --domaindir /home/someuser/domains sampleDomain
               Domain sampleDomain deleted
               Command delete-domain executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-domain(1), start-domain(1), stop-domain(1), list-domains(1)

       asadmin(1M)

Java EE 7                       2009年10月16日                delete-domain(1)
