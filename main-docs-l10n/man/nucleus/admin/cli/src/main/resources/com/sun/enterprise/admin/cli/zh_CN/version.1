version(1)                  asadmin 实用程序子命令                  version(1)

名称
       version - 显示 GlassFish Server 的版本信息

提要
           version [--help] [--verbose={false|true}]
           [--local={false|true}]

说明
       version 子命令显示 GlassFish Server
       的版本信息。默认情况下，当该子命令无法联系域管理服务器 (DAS)
       时，将在本地检索版本信息并显示警告消息。

       远程模式和本地模式下都支持此子命令。

选项
       --help, -?
           显示子命令的帮助文本。

       --verbose, -v
           如果将此选项设置为真
           (JRE) 的版本。默认值为假

       --local
           如果将此选项设置为真
           Server 安装本地获取版本。

           如果该选项设置为假
           以获取版本。如果尝试联系 DAS
           失败，该子命令将在本地检索版本并显示警告消息。

示例
       Example 1, 从正在运行的 DAS 获取版本信息

               asadmin> version
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Example 2, 无法访问 DAS 时获取版本信息

               asadmin> version
               Version string could not be obtained from Server [localhost:4848] for some reason.
               (Turn debugging on e.g. by setting AS_DEBUG=true in your environment, to see the
                details).
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Example 3, 本地获取版本信息

               asadmin> version --local
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-modules(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 20 日                   version(1)
