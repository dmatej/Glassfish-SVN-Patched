unset(1)                  asadmin 유틸리티 하위 명령                  unset(1)

제목
       unset - 다중 모드 환경에서 하나 이상의 변수 제거

시놉시스
           unset [--help] variable-list

설명
       unset 하위 명령은 다중 모드 환경에 대해 설정된 하나 이상의 환경 변수를
       제거합니다. 제거 후 변수 및 연관된 값은 더 이상 다중 모드 환경에
       적용되지 않습니다.

       설정된 환경 변수를 나열하려면 옵션 없이 export 하위 명령을 사용합니다.
       export 하위 명령이 환경 변수를 나열하지 않으면 환경 변수가 설정되지
       않습니다.

       이 하위 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       variable-list
           제거할 환경 변수의 공백으로 구분된 목록입니다.

예제
       예 1, 설정되어 있는 환경 변수 목록 표시
           이 예제는 export 하위 명령을 사용하여 설정된 환경 변수를
           나열합니다.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       예 2, 환경 변수 제거
           이 예제는 AS_ADMIN_PREFIX 환경 변수를 제거합니다.

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                      2010년 12월 20일                       unset(1)
