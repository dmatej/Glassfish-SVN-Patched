export(1)                   asadmin 实用程序子命令                   export(1)

名称
       export - 标记一个变量名，用于在多模式下自动导出到后续命令的环境中

提要
           export [--help] [variable-name=value [variable-name=value]*]

说明
       在多模式下，export
       子命令将环境变量标记为自动导出到后续命令的环境。所有后续命令都使用指定的变量名称值，除非您退出多模式，或者使用
       unset
       子命令取消变量设置。如果仅指定了变量名称，将显示变量名称的当前值。

       如果使用 export
       子命令而不带任何参数，将显示所有已导出的变量及其值的列表。调用 asadmin
       实用程序之前设置的已导出 shell 环境变量将自动导入，并在 asadmin
       中设置为导出的变量。无法通过 asadmin 实用程序读取未导出的环境变量。

       该子命令仅在本地模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       variable-name=value
           要自动导出到将由后续命令使用的环境的变量名称及其值。

示例
       Example 1, 列出设置的环境变量
           此示例列出已设置的环境变量。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Example 2, 设置环境变量
           此示例将 AS_ADMIN_HOST 环境变量设置为 bluestar。

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Example 3, 设置多个环境变量
           此示例设置了一些适用于多模式环境的环境变量。

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 5 日                     export(1)
