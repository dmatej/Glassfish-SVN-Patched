multimode(1)            Comandi secondari della utilit            multimode(1)

NOME
       multimode - consente di eseguire più comandi secondari conservando le
       impostazioni di ambiente e continuando a utilizzare la utility asadmin

SINTASSI
           multimode [--help] [--file filename]
           [--printprompt={true|false}] [--encoding encode]

DESCRIZIONE
       Il comando secondario multimode elabora i comandi secondari di asadmin
       in sequenza in un'unica sessione. L'interfaccia della riga di comando
       richiede un comando secondario, lo esegue, ne visualizza i risultati,
       quindi richiede il comando secondario successivo. Tutte le opzioni di
       asadmin impostate in modalità multipla si applicano ai comandi
       successivi finché non si esce dalla sessione in modalità multipla. Per
       uscire da multimode, digitare exit, quit o Ctrl-D.

       È possibile usare il comando secondario export per impostare
       l'ambiente, o il comando secondario unset per rimuovere le variabili di
       ambiente dall'ambiente modalità multipla. È possibile fornire i comandi
       secondari anche passandone una lista preparata in precedenza da un file
       o da un input standard (pipe).

       È possibile richiamare multimode da una sessione in modalità multipla.
       Quando si esce dal secondo ambiente in modalità multipla, si torna
       all'ambiente in modalità multipla originale.

       Quando si richiama il comando secondario multimode è possibile fornire
       tutte le opzioni della utility remota asadmin. Queste impostazioni
       saranno applicate come valori predefiniti per tutti i comandi secondari
       eseguiti nella sessione in modalità multipla. Per una lista delle
       opzioni della utility asadmin, vedere la pagina della Guida
       asadmin(1M).

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --file, -f
           Legge i comandi secondari nel file specificato.

       --printprompt
           Controlla la visualizzazione del prompt asadmin. Per impostazione
           predefinita, questa opzione è impostata sullo stesso valore
           dell'opzione --interactive della utility asadmin. In genere non è
           necessario specificare questa opzione. Il valore predefinito è
           true.

       --encoding
           Specifica il set di caratteri per il file da decodificare. Per
           impostazione predefinita, viene usato il set di caratteri del
           sistema.

ESEMPI
       Esempio 1, Avvio di una sessione in modalità multipla
           In questo esempio viene avviata una sessione in modalità multipla
           dove % è il prompt del sistema.

               % asadmin multimode
               asadmin>
           È possibile avviare una sessione in modalità multipla anche
           digitando asadmin senza opzioni né comandi secondari al prompt del
           sistema.

       Esempio 2, Esecuzione di più comandi da un file
           In questo esempio viene eseguita una sequenza di comandi secondari
           dal file commands_file.txt.

               % asadmin multimode --file commands_file.txt

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       export(1), unset(1)

       asadmin(1M)

Java EE 7                      20 dicembre 2010                   multimode(1)
