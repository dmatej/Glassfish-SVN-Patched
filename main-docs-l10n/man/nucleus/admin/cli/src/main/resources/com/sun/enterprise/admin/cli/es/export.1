export(1)               Subcomandos de la Utilidad asa               export(1)

NOMBRE DE REFERENCIA
       export - Marca un nombre de variable para exportarla automáticamente al
       entorno de comandos subsiguientes en modo múltiple.

SINOPSIS
           export [--help] [variable-name=value [variable-name=value]*]

DESCRIPCIÓN
       En modo múltiple, el subcomando export marca una variable de entorno
       para la exportación automática al entorno de los comandos
       subsiguientes. Todos los comandos subsiguientes utilizan el valor de la
       variable según se especifica, a menos que salga de modo múltiple o
       utilice el comando unset para desconfigurar la variable. Si sólo se
       especifica el nombre de la variable, se muestra el valor actual de ese
       nombre.

       Si se utiliza el subcomando export sin argumentos, se muestra una lista
       de todas las variables exportadas y sus valores. Las variables de
       entorno de shell exportadas antes de llamar a la utilidad asadmin se
       importan automáticamente y se definen como variables exportadas desde
       la propia asadmin. Las variables de entorno que no están exportadas no
       las puede leer la utilidad asadmin.

       Este subcomando sólo está soportado en modo local.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       variable-name=value
           Nombre y valor de la variable para su exportación automática al
           entorno para ser utilizado por los comandos subsiguientes.

EJEMPLOS
       Ejemplo 1, Listado de las Variables de Entorno Definidas
           En este ejemplo se muestra una lista de las variables de entorno
           que se han definido.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Ejemplo 2, Definición de una Variable de Entorno
           En este ejemplo se define el valor de la variable de entorno
           AS_ADMIN_HOST en bluestar.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Ejemplo 3, Definición de múltiples variables de entorno
           En este ejemplo se configura el valor de diversas variables de
           entorno para el entorno de modo múltiple.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                    5 de octubre de 2009                    export(1)
