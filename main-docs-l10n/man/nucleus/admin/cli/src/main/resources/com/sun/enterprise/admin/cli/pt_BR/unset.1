unset(1)                Subcomandos do utilitário asad                unset(1)

NOME
       unset - remove uma ou mais variáveis do ambiente multimodo

SINOPSE
           unset [--help] variable-list

DESCRIçãO
       O subcomando unset remove uma ou mais variáveis do ambiente multimodo
       que estão definidas para o ambiente multimodo. Após a remoção, as
       variáveis e seus valores associados não mais se aplicam ao ambiente de
       multimodo.

       Para listar variáveis que estão configuradas, use o subcomando export
       sem opções. Se o subcomando export não listar variáveis de ambiente,
       nenhuma variável de ambiente é definida.

       Este subcomando é suportado só no modo local.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       variable-list
           Uma lista separada por espaços da variáveis de ambiente a serem
           removidas.

EXEMPLOS
       Example 1, Listando as Variáveis Definidas do Ambiente
           Este exemplo usa o subcomando export para listar as variáveis de
           ambiente que foram definidas.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Example 2, Removendo uma Variável do Ambiente
           Este exemplo remove a variável de ambiente AS_ADMIN_PREFIX.

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                         20 dez 2010                         unset(1)
