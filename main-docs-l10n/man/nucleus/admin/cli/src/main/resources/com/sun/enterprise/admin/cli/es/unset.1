unset(1)                Subcomandos de la Utilidad asa                unset(1)

NOMBRE DE REFERENCIA
       unset - elimina una o más variables de entorno de modo múltiple.

SINOPSIS
           unset [--help] variable-list

DESCRIPCIÓN
       El subcomando unset elimina una o más variables de entorno que se hayan
       establecido para el entorno de modo múltiple. Una vez eliminadas, las
       variables y sus valores asociados ya no se aplicarán al entorno de modo
       múltiple.

       Para mostrar una lista de las variables de entorno definidas, utilice
       el subcomando export sin opciones. Si el subcomando export no muestra
       las variables de entorno, no se define ninguna variable de entorno.

       Este subcomando sólo está soportado en modo local.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       variable-list
           Una lista separada por espacios de las variables de entorno que se
           van a eliminar.

EJEMPLOS
       Ejemplo 1, Listado de las Variables de Entorno Definidas
           En este ejemplo se utiliza el subcomando export para mostrar un
           listado de las variables de entorno que se han definido.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Ejemplo 2, Eliminación de una Variable de Entorno
           En este ejemplo se elimina la variable de entorno AS_ADMIN_PREFIX.

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                   20 de diciembre de 2010                   unset(1)
