login(1)                    asadmin 实用程序子命令                    login(1)

名称
       login - 使您登录域

提要
           login [--help]

说明
       login
       子命令的目的在于通过登录特定域来简化域管理。如果在不同的计算机上创建了
       GlassFish Server 域 (本地)，则可以从其中任何一台计算机中运行 asadmin
       实用程序以及管理位于其他位置的域
       (远程)。当您选择某台特定计算机作为管理客户机来管理多个域和服务器时，这种方法尤为有用。

       login
       子命令会提示您输入管理员用户名和口令。成功登录后，将在主目录中创建
       .asadminpass 文件。(这与运行带有 --savelogin 选项的 create-domain
       子命令时修改的文件是同一文件。)将存储文字主机名，不会尝试解析
       DNS。如果从其他计算机管理域，运行一次 login
       子命令已足够。对该域运行其他远程子命令时，无需指定 asadmin 实用程序选项
       --user 和 --passwordfile。登录域后，除非选择 --host (localhost) 和
       --port (4848)
       选项的默认值，否则仍需为所有后续远程子命令提供主机和端口。

       后续使用具有相同参数的相同子命令，会造成给定管理主机和接口的
       .asadminpass 文件的内容被覆写。可以决定覆写文件或拒绝此类登录。

       登录信息将永久保存，并可以在多次域重新启动后使用。

       没有 logout 子命令。如果希望登录其他域，则运行 login 子命令并为 asadmin
       实用程序选项 --host 和 --port 指定新值。

选项
       --help, -?
           显示子命令的帮助文本。

示例
       Example 1, 登录到远程计算机上的域
           此示例登录到位于其他计算机上的域。在 login 子命令之前指定选项。

               asadmin --host foo --port 8282 login
               Please enter the admin user name>admin
               Please enter the admin password>

               Trying to authenticate for administration of server at host [foo]
               and port [8282] ...
               Login information relevant to admin user name [admin] for host [foo]
               and admin port [8282] stored at [/.asadminpass] successfully.
               Make sure that this file remains protected. Information stored in this
               file will be used by asadmin commands to manage associated domain.

       Example 2, 登录到 Localhost 的默认端口上的域
           此示例登录到 mylhost 的默认端口上的域。在 login
           子命令之前指定选项。

               asadmin --host myhost login
               Please enter the admin user name>admin
               Please enter the admin password>
               Trying to authenticate for administration of server
               at host [myhost] and port [4848] ...
               An entry for login exists for host [myhost] and port [4848], probably
               from an earlier login operation.
               Do you want to overwrite this entry (y/n)?y
               Login information relevant to admin user name [admin] for host [myhost]
               and admin port [4848] stored at [/home/joe/.asadminpass] successfully.
               Make sure that this file remains protected. Information stored in this
               file will be used by asadmin commands to manage associated domain.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-domain(1), delete-domain(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 9 日                      login(1)
