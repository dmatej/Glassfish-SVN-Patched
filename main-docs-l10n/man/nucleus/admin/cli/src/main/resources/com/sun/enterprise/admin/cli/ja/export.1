export(1)             asadminユーティリティのサブコマンド            export(1)

名前
       export -
       マルチモードでの後続のコマンドの環境に対する自動エクスポートの変数名にマークを付けます

概要
           export [--help] [variable-name=value [variable-name=value]*]

説明
       マルチモードでは、exportサブコマンドは、後続のコマンドの環境に対する自動エクスポートの環境変数にマークを付けます。マルチモードを終了するか、unsetサブコマンドを使用して変数を設定解除しないかぎり、すべての後続のコマンドは指定された変数名の値を使用します。変数名のみを指定した場合、その変数名の現在の値が表示されます。

       引数を指定しないでexportサブコマンドを実行すると、すべてのエクスポートされた変数とその値のリストが表示されます。asadminユーティリティを呼び出す前に設定された、エクスポート済のシェル環境変数は自動的にインポートされ、asadmin内のエクスポートされた変数として設定されます。エクスポートされていない環境変数は、asadminユーティリティで読み取ることはできません。

       このサブコマンドは、ローカル・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       variable-name=value
           後続のコマンドで使用される環境に対する自動エクスポートの変数名と値。

例
       例 1, 設定されている環境変数のリスト
           この例では、設定されている環境変数をリストします。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       例 2, 環境変数の設定
           この例では、AS_ADMIN_HOST環境変数をbluestarに設定します。

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       例 3, 複数の環境変数の設定
           この例では、マルチモード環境の複数の環境変数を設定します。

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                        2009年10月5日                       export(1)
