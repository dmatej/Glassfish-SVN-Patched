export(1)                 asadmin 유틸리티 하위 명령                 export(1)

제목
       export - 다중 모드에서 후속 명령의 환경으로 자동 익스포트에 필요한 변수
       이름 표시

시놉시스
           export [--help] [variable-name=value [variable-name=value]*]

설명
       다중 모드에서 export 하위 명령은 후속 명령의 환경으로 자동 익스포트에
       필요한 환경 변수를 표시합니다. 모든 후속 명령에서는 다중 모드를
       종료하지 않는 한 지정된 대로 변수 이름 값을 사용하거나 unset 하위
       명령을 사용하여 변수를 설정 해제합니다. 변수 이름만 지정할 경우 해당
       변수 이름의 현재 값이 표시됩니다.

       인수 없이 export 하위 명령을 사용할 경우 모든 익스포트된 변수와 해당
       값의 목록이 표시됩니다.  asadmin 유틸리티를 호출하기 전에 설정된
       익스포트된 셸 환경 변수를 자동으로 임포트하여 asadmin 내에서 익스포트된
       변수로 설정합니다. 익스포트되지 않은 환경 변수는 asadmin 유틸리티에서
       읽을 수 없습니다.

       이 하위 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       variable-name=value
           후속 명령에서 사용할 환경으로 자동 익스포트에 필요한 변수 이름 및
           값입니다.

예제
       예 1, 설정되어 있는 환경 변수 목록 표시
           이 예제는 설정된 환경 변수를 나열합니다.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       예 2, 환경 변수 설정
           이 예제는 AS_ADMIN_HOST 환경 변수를 bluestar로 설정합니다.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       예 3, 여러 환경 변수 설정
           이 예제는 다중 모드 환경에 대해 여러 환경 변수를 설정합니다.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                       2009년 10월 5일                      export(1)
