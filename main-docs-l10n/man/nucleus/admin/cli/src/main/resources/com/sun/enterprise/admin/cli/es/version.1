version(1)              Subcomandos de la Utilidad asa              version(1)

NOMBRE DE REFERENCIA
       version - muestra información sobre la versión de GlassFish Server

SINOPSIS
           version [--help] [--verbose={false|true}]
           [--local={false|true}]

DESCRIPCIÓN
       El subcomando version muestra información sobre la versión de GlassFish
       Server. Por defecto, si el subcomando no puede ponerse en contacto con
       el servidor de administración de dominio (DAS), el subcomando recupera
       la información de la versión localmente y muestra un mensaje de
       advertencia.

       Este subcomando está soportado en modo remoto y modo local.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --verbose, -v
           Si esta opción está definida en true, el subcomando proporciona la
           versión del entorno de tiempo de ejecución de Java (JRE) que el
           servidor está ejecutando. El valor por defecto es false.

       --local
           Si esta opción se configura como true, el subcomando obtiene la
           versión localmente de la instalación de GlassFish Server en el host
           en el que se ejecuta subcomando.

           Si esta opción se configura como false (valor por defecto), el
           subcomando intenta ponerse en contacto con el DAS para obtener la
           versión. Si el intento de contactar con el DAS falla, el subcomando
           recupera la versión localmente y muestra un mensaje de advertencia.

EJEMPLOS
       Ejemplo 1, Cómo Obtener Información sobre la Versión desde un DAS en
       Funcionamiento

               asadmin> version
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Ejemplo 2, Cómo Obtener Información de la Versión Cuando No es Posible
       Conectar con el DAS

               asadmin> version
               Version string could not be obtained from Server [localhost:4848] for some reason.
               (Turn debugging on e.g. by setting AS_DEBUG=true in your environment, to see the
                details).
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Ejemplo 3, Cómo Obtener Localmente Información sobre la Versión

               asadmin> version --local
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       list-modules(1)

       asadmin(1M)

Java EE 7                   20 de diciembre de 2010                 version(1)
