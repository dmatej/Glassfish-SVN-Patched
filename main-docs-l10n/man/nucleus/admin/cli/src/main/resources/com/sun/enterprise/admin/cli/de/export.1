export(1)               Unterbefehle des asadmin-Utili               export(1)

NAME
       export - markiert einen Variablennamen für den automatischen Export in
       die Umgebung von nachfolgenden Befehlen im Mehrfachmodus

ZUSAMMENFASSUNG
           export [--help] [variable-name=value [variable-name=value]*]

BESCHREIBUNG
       Im Mehrfachmodus markiert der Unterbefehl export eine Umgebungsvariable
       für den automatischen Export in die Umgebung von nachfolgenden
       Befehlen. Alle nachfolgenden Befehle verwenden den angegebenen Wert des
       Variablennamens, es sei denn, Sie verlassen den Mehrfachmodus oder
       verwenden den Unterbefehl unset, um die Festlegung der Variable
       aufzuheben. Wenn Sie nur den Variablennamen angeben, wird der aktuelle
       Wert dieses Variablennamens angezeigt.

       Wenn Sie den Unterbefehl export ohne Argumente verwenden, wird eine
       Liste aller exportierten Variablen und ihrer Werte angezeigt.
       Exportierte Shellumgebungsvariablen, die vor dem Aufruf des Utilitys
       asadmin festgelegt wurden, werden automatisch importiert und als
       exportierte Variablen innerhalb von asadmin festgelegt.
       Umgebungsvariablen, die nicht exportiert werden, können vom Utility
       asadmin nicht gelesen werden.

       Dieser Unterbefehl wird nur im lokalen Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       variable-name=value
           Variablenname und Wert für den automatischen Export in die
           Umgebung, die für nachfolgende Befehle verwendet werden soll.

BEISPIELE
       Example 1, Festgelegte Umgebungsvariablen auflisten
           In diesem Beispiel werden die festgelegten Umgebungsvariablen
           aufgelistet.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Example 2, Umgebungsvariable festlegen
           In diesem Beispiel wird die Umgebungsvariable AS_ADMIN_HOST auf
           bluestar gesetzt.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Example 3, Mehrere Umgebungsvariablen festlegen
           In diesem Beispiel werden mehrere Umgebungsvariablen für die
           Mehrfachmodusumgebung festgelegt.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                        05. Okt. 2009                       export(1)
