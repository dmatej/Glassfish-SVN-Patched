version(1)              Comandi secondari della utilit              version(1)

NOME
       version - visualizza le informazioni sulla versione per GlassFish
       Server

SINTASSI
           version [--help] [--verbose={false|true}]
           [--local={false|true}]

DESCRIZIONE
       Il comando secondario version consente di visualizzare le informazioni
       sulla versione di GlassFish Server. Per impostazione predefinita, se
       non può comunicare con il server di amministrazione del dominio (DAS),
       il comando secondario recupera le informazioni sulla versione
       localmente e visualizza un messaggio di avvertenza.

       Questo comando secondario è supportato in modalità remota e in modalità
       locale.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --verbose, -v
           Se questa opzione è impostata su true, il comando secondario
           fornisce la versione di Java Runtime Environment (JRE) in
           esecuzione nel server. Il valore predefinito è false.

       --local
           Se questa opzione è impostata su true, il comando secondario
           ottiene la versione localmente dall'installazione di GlassFish
           Server sull'host in cui viene eseguito il comando secondario.

           Se l'opzione è impostata su false (impostazione predefinita), il
           comando secondario tenta di comunicare con il server DAS per
           ottenere la versione. Se il tentativo di comunicare con il server
           DAS non riesce, il comando secondario recupera le informazioni
           sulla versione localmente e visualizza un messaggio di avvertenza.

ESEMPI
       Esempio 1, Ottenimento di informazioni sulla versione da un server DAS
       in esecuzione

               asadmin> version
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Esempio 2, Ottenimento di informazioni sulla versione quando non è
       possibile contattare il server DAS

               asadmin> version
               Version string could not be obtained from Server [localhost:4848] for some reason.
               (Turn debugging on e.g. by setting AS_DEBUG=true in your environment, to see the
                details).
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

       Esempio 3, Ottenimento di informazioni sulla versione localmente

               asadmin> version --local
               Using locally retrieved version string from version class.
               Version = Oracle GlassFish Server 3.1 (build 34)
               Command version executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       list-modules(1)

       asadmin(1M)

Java EE 7                      20 dicembre 2010                     version(1)
