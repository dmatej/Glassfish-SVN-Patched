unset(1)              asadminユーティリティのサブコマンド             unset(1)

名前
       unset - マルチモード環境から1つ以上の変数を除去します

概要
           unset [--help] variable-list

説明
       unsetサブコマンドは、マルチモード環境用に設定した1つ以上の環境変数を除去します。除去された変数およびそれに関連付けられていた値は、それ以降マルチモード環境には適用されません。

       設定されている環境変数をリストする場合は、オプションを指定しないでexportサブコマンドを実行します。exportサブコマンドで環境変数がリストされない場合、環境変数は設定されていません。

       このサブコマンドは、ローカル・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       variable-list
           除去する環境変数の空白区切りのリスト。

例
       例 1, 設定されている環境変数のリスト
           この例では、exportサブコマンドを使用して、設定済の環境変数をリストします。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       例 2, 環境変数の除去
           この例では、AS_ADMIN_PREFIX環境変数を除去します。

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                       2010年12月20日                        unset(1)
