unset(1)                    asadmin 实用程序子命令                    unset(1)

名称
       unset - 从多模式环境中删除一个或多个变量

提要
           unset [--help] variable-list

说明
       unset
       子命令删除为多模式环境设置的一个或多个环境变量。删除之后，这些变量及其关联值将不再应用到多模式环境。

       要列出设置的环境变量，请使用不带选项的 export 子命令。如果 export
       子命令未列出环境变量，则表示未设置环境变量。

       该子命令仅在本地模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       variable-list
           要删除的环境变量的空格分隔列表。

示例
       Example 1, 列出设置的环境变量
           该示例使用 export 子命令来列出已设置的环境变量。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Example 2, 删除环境变量
           该示例将删除 AS_ADMIN_PREFIX 环境变量。

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 20 日                     unset(1)
