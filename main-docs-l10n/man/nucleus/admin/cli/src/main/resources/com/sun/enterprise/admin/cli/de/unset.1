unset(1)                Unterbefehle des asadmin-Utili                unset(1)

NAME
       unset - entfernt mindestens eine Variable aus der Mehrfachmodusumgebung

ZUSAMMENFASSUNG
           unset [--help] variable-list

BESCHREIBUNG
       Der Unterbefehl unset entfernt eine oder mehrere Umgebungsvariablen,
       die für die Mehrfachmodusumgebung festgelegt sind. Nach der Entfernung
       gelten die Variablen und ihre zugehörigen Werte nicht mehr für die
       Mehrfachmodusumgebung.

       Um die festgelegten Umgebungsvariablen aufzulisten, verwenden Sie den
       Unterbefehl export ohne Optionen. Wenn der Unterbefehl export keine
       Umgebungsvariablen auflistet, sind keine Umgebungsvariablen festgelegt.

       Dieser Unterbefehl wird nur im lokalen Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       variable-list
           Eine durch Leerzeichen getrennte Liste der zu entfernenden
           Umgebungsvariablen.

BEISPIELE
       Beispiel 1, Festgelegte Umgebungsvariablen auflisten
           In diesem Beispiel wird der Unterbefehl export verwendet, um die
           festgelegten Umgebungsvariablen aufzulisten.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Beispiel 2, Umgebungsvariablen entfernen
           In diesem Beispiel wird die Umgebungsvariable AS_ADMIN_PREFIX
           entfernt.

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       export(1), multimode(1)

       asadmin(1M)

Java EE 7                        20. Dez. 2010                        unset(1)
