export(1)               Sous-commandes de l'utilitaire               export(1)

NOM
       export - identifie un nom de variable pour l'export automatique vers
       l'environnement des commandes ultérieures en multimode

SYNTAXE
           export [--help] [variable-name=value [variable-name=value]*]

DESCRIPTION
       En multimode, la sous-commande export identifie une variable
       d'environnement pour l'export automatique dans l'environnement des
       commandes ultérieures. Toutes les commandes ultérieures utilisent la
       valeur de nom de variable spécifiée, sauf si vous quittez le multimode
       ou que vous utilisez la sous-commande unset pour annuler la définition
       de la variable. Si seul le nom de la variable est spécifié, sa valeur
       en cours apparaît.

       Si la sous-commande export est utilisée sans aucun argument, la liste
       de toutes les variables exportées ainsi que les valeurs correspondantes
       est affichée. Les variables d'environnement de shell exportées et
       définies avant l'appel de l'utilitaire asadmin sont importées
       automatiquement et définies en tant que variables exportées dans
       asadmin. Les variables d'environnement qui ne sont pas exportées ne
       peuvent pas être lues par l'utilitaire asadmin.

       Cette sous-commande est prise en charge en mode local uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       variable-name=value
           Nom et valeur de la variable pour l'export automatique dans
           l'environnement à utiliser par les commandes ultérieures.

EXEMPLES
       Exemple 1, Liste des variables d'environnement définies
           Dans cet exemple, les variables d'environnement qui ont été
           définies sont répertoriées.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Exemple 2, Définition d'une variable d'environnement
           Dans cet exemple, la variable d'environnement AS_ADMIN_HOST est
           définie sur la valeur bluestar.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Exemple 3, Définition de plusieurs variables d'environnement
           Dans cet exemple, plusieurs variables d'environnement sont définies
           pour l'environnement multimode.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                         5 oct. 2009                        export(1)
