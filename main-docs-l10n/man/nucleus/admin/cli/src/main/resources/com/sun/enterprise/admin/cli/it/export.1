export(1)               Comandi secondari della utilit               export(1)

NOME
       export - contrassegna un nome variabile per l'esportazione automatica
       nell'ambiente dei comandi successivi in modalità multipla

SINTASSI
           export [--help] [variable-name=value [variable-name=value]*]

DESCRIZIONE
       In modalità multipla il comando secondario export contrassegna una
       variabile di ambiente per l'esportazione automatica nell'ambiente dei
       comandi successivi. Tutti i comandi successivi usano il valore del nome
       della variabile come specificato, a meno che non si esca dalla modalità
       multipla o non si usi il comando secondario unset per annullare
       l'impostazione della variabile. Se viene specificato solo il nome della
       variabile, viene visualizzato il valore corrente del nome.

       Se il comando secondario export viene usato senza argomenti, viene
       visualizzata la lista di tutte le variabili esportate e dei relativi
       valori. Le variabili di ambiente della shell esportate impostate prima
       di richiamare la utility asadmin vengono importate automaticamente e
       impostate come variabili esportate in asadmin. Le variabili di ambiente
       non esportate non possono essere lette dalla utility asadmin.

       Questo comando secondario è supportato solo in modalità locale.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       variable-name=value
           Il nome e il valore della variabile per l'esportazione automatica
           nell'ambiente che dovranno essere utilizzati dai comandi
           successivi.

ESEMPI
       Esempio 1, Visualizzazione della lista delle variabili di ambiente
       impostate
           In questo esempio sono elencate le variabili di ambiente impostate.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Esempio 2, Impostazione di una variabile di ambiente
           In questo esempio la variabile di ambiente AS_ADMIN_HOST viene
           impostata su bluestar.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Esempio 3, Impostazione di più variabili di ambiente
           In questo esempio vengono impostate numerose variabili di ambiente
           per l'ambiente con modalità multipla.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                       5 ottobre 2009                       export(1)
