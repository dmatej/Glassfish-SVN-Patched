export(1)               Subcomandos do utilitário asad               export(1)

NOME
       export - marca um nome de variável para exportação automática para o
       ambiente de comandos subsequentes em multimodo

SINOPSE
           export [--help] [variable-name=value [variable-name=value]*]

DESCRIçãO
       No multimodo, o subcomando export marca uma variável do ambiente para a
       exportação automática ao ambiente dos comandos subsequentes. Todos os
       comandos subsequentes utilizam o valor variável do nome como
       especificado, a não ser que você saia do multimodo ou utilize o
       subcomando unset para cancelar a definição da variável. Se apenas o
       nome da variável for especificado, o valor atual desse nome variável é
       exibido.

       Se o subcomando export for utilizado sem argumentos, uma lista das
       variáveis exportadas e de seus valores é exibida. As variáveis
       exportadas do ambiente de shell que foram definidas antes de chamar o
       utilitário asadmin são importadas automaticamente e definidas como
       variáveis exportadas em asadmin. As variáveis do ambiente que não são
       exportadas não podem ser lidas pelo utilitário asadmin.

       Este subcomando é suportado só no modo local.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       variable-name=value
           Nome e valor da variável para a exportação automática para o
           ambiente a ser utilizado pelos comandos subsequentes.

EXEMPLOS
       Example 1, Listando as Variáveis Definidas do Ambiente
           Este exemplo lista as variáveis do ambiente que foram definidas.

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       Example 2, Definindo uma Variável do Ambiente
           Este exemplo define a variável de ambiente AS_ADMIN_HOST como
           bluestar.

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       Example 3, Definindo Diversas Variáveis do Ambiente
           Este exemplo define um número de variáveis do ambiente para o
           ambiente multimodo.

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       unset(1), multimode(1)

       asadmin(1M)

Java EE 7                         5 out 2009                         export(1)
