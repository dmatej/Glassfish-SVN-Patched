disable-monitoring(1)   Sous-commandes de l'utilitaire   disable-monitoring(1)

NOM
       disable-monitoring - désactive la surveillance du serveur ou de modules
       spécifiques pouvant être surveillés

SYNTAXE
           disable-monitoring [--help] [--modules module-name][:module-name]*

DESCRIPTION
       La sous-commande disable-monitoring permet de désactiver la
       surveillance de GlassFish Server ou de modules spécifiques au moment de
       l'exécution. Les modifications sont dynamiques, ainsi, le redémarrage
       du serveur n'est pas obligatoire.

       L'exécution de la sous-commande disable-monitoring sans l'option
       --module désactive le service de surveillance en définissant l'attribut
       monitoring-enabled de l'élément monitoring-service sur false. Les
       différents modules conservent leurs niveaux de surveillance, mais
       aucune donnée de surveillance n'est générée, car le service de
       surveillance est entièrement désactivé.

       Cette sous-commande utilisée avec l'option --modules désactive la
       surveillance pour un module en définissant le niveau de surveillance
       sur OFF. Le statut du service de surveillance n'est pas modifié. Pour
       obtenir la liste des modules pouvant être surveillés, reportez-vous à
       l'option --modules dans cette page d'aide.

       L'autre méthode de désactivation de la surveillance consiste à utiliser
       la sous-commande set. Dans ce cas, le serveur doit être redémarré pour
       que les modifications prennent effet.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --modules
           Désactive les modules spécifiés en définissant le niveau de
           surveillance sur OFF. Les modules sont séparés par un signe
           deux-points (:). Modules pouvant être surveillés :
           connector-connection-pool, connector-service, ejb-container,
           http-service, jdbc-connection-pool, jersey, jpa, jms-service, jvm,
           security, thread-pool, transaction-service, web-container et
           web-services-container. D'autres modules peuvent être répertoriés à
           l'aide de la sous-commande get.

OPÉRANDES
EXEMPLES
       Exemple 1, Désactivation du service de surveillance de GlassFish Server
           Dans cet exemple, la surveillance de GlassFish Server en général
           est désactivée via la définition de l'indicateur enable-monitoring
           sur false (la valeur par défaut est true).

               asadmin> disable-monitoring
               Command disable-monitoring executed successfully

       Exemple 2, Désactivation de la surveillance des conteneurs Web et EJB
           Dans cet exemple, la surveillance de conteneurs spécifiques est
           désactivée. Leur niveau de surveillance est défini sur OFF.

               asadmin> disable-monitoring --modules web-container:ejb-container
               Command disable-monitoring executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       enable-monitoring(1), monitor(1), list(1), get(1), set(1)

       surveillance(5ASC)

       asadmin(1M)

       Administering the Monitoring Service dans le manuel Oracle GlassFish
       Server Administration Guide

Java EE 7                        21 oct. 2009            disable-monitoring(1)
