disable-monitoring(1)       asadmin 实用程序子命令       disable-monitoring(1)

名称
       disable-monitoring - 禁用对服务器或特定可监视模块的监视

提要
           disable-monitoring [--help] [--modules module-name][:module-name]*

说明
       disable-monitoring 子命令用于在运行期间对 GlassFish Server
       或特定的模块禁用监视功能。更改是动态的，即无需重新启动服务器。

       不使用 --module 选项运行 disable-monitoring 子命令将禁用监视服务
       (通过将 monitoring-service 元素的 monitoring-enabled
       属性设置为假

       如果将此子命令与 --modules
       选项一起使用，则通过将监视级别设置为“关闭”对模块禁用监视功能。监视服务的状态不受影响。有关可监视模块的列表，请参阅此帮助页中的
       --modules 选项。

       禁用监视功能的备用方法是使用 set
       子命令。在这种情况下，必须重新启动服务器才能使更改生效。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --modules
           通过将监视级别设置为“关闭”禁用一个或多个指定的模块。多个模块用 :
           (冒号) 分隔。可监视的模块包括
           connector-connection-pool、connector-service、ejb-container、http-service、jdbc-connection-pool、jersey、jpa、jms-service、jvm、security、thread-pool、transaction-service、web-container
           和 web-services-container。其他模块可通过使用 get 子命令列出。

操作数
示例
       Example 1, 对 GlassFish Server 禁用监视服务
           此示例对 GlassFish Server 禁用监视功能，一般来说，将
           enable-monitoring 标记设置为假

               asadmin> disable-monitoring
               Command disable-monitoring executed successfully

       Example 2, 对 Web 和 EJB 容器禁用监视功能
           此示例对特定容器禁用监视功能。其监视级别将设置为“关闭”。

               asadmin> disable-monitoring --modules web-container:ejb-container
               Command disable-monitoring executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-monitoring(1), monitor(1), list(1), get(1), set(1)

       monitoring(5ASC)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering the
       Monitoring Service

Java EE 7                     2009 年 10 月 21 日        disable-monitoring(1)
