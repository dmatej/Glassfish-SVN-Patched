list-batch-jobs(1)      Unterbefehle des asadmin-Utili      list-batch-jobs(1)

NAME
       list-batch-jobs - listet Batchjobs auf

ZUSAMMENFASSUNG
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

BESCHREIBUNG
       Der Unterbefehl list-batch-jobs listet Batchjobs und Jobdetails auf.
       Ein Job stellt den gesamten Batchprozess dar.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --job-name, -j
           Gibt den Namen eines Jobs an.

       --instance-id, -i
           Gibt die Instanz-ID eines Jobs an.

       --execution-id, -e
           Gibt die Ausführungs-ID eines Jobs an.

       --long, -l
           Zeigt detaillierte Informationen zu den Jobs an. Der Standardwert
           ist false.

       --output, -o
           Zeigt bestimmte Details zu jedem Job an. Mithilfe einer durch Komma
           getrennten Liste können Sie die anzuzeigenden Details und deren
           Reihenfolge festlegen. Die Werte sind von Groß- und Kleinschreibung
           unabhängig. Die Spaltenüberschriften job-name und instance-count
           werden standardmäßig angezeigt.

           Mögliche Werte:

           job-name
               Zeigt den Namen des Jobs an.

           instance-count
               Zeigt die Anzahl der derzeit ausgeführten Jobinstanzen an.

           execution-id
               Zeigt die Ausführungs-ID einer bestimmten Jobinstanz an.

           status
               Zeigt den Status des Jobs an.

           exit-status
               Zeigt den Beendigungsstatus des Jobs an.

           start-time
               Zeigt die Startzeit des Jobs an.

           end-time
               Zeigt die Beendigungszeit des Jobs an.

           job-parameters
               Zeigt die Jobparameter an.

       --header, -h
           Legt fest, ob bei Verwendung der Option --long Spaltenüberschriften
           angezeigt werden. Der Standardwert ist true. Um die Überschriften
           zu unterdrücken, legen Sie für die Option --header den Wert false
           fest.

BEISPIELE
       Example 1, Batchjobs auflisten
           Im folgenden Beispiel werden die Batchjobs aufgelistet.

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                        13. Feb. 2013              list-batch-jobs(1)
