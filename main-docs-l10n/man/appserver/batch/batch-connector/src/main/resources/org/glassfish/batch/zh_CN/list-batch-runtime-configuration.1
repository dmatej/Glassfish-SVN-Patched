list-batch-runtime-configuraasadmin 实用程序

名称
       list-batch-runtime-configuration - 显示批处理运行时的配置

提要
           list-batch-runtime-configuration [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

说明
       list-batch-runtime-configuration
       子命令可显示批处理运行时的配置。批处理运行时配置数据存储在 domain.xml
       的 config 元素中。

选项
       --help, -?
           显示子命令的帮助文本。

       --long, -l
           显示有关批处理运行时的详细信息。默认值为假

       --output, -o
           显示有关批处理运行时的特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。值不区分大小写。默认情况下显示
           min-thread-pool-size、max-thread-pool-size、max-queue-size 和
           data-source-name 列标题。

           可能的值如下：

           min-thread-pool-size
               最小线程池大小。

           max-thread-pool-size
               最大线程池大小。

           max-idle-thread-timeout-in-seconds
               销毁线程之前的最长空闲时间。

           max-queue-size
               用于保留作业的最大队列大小。

           data-source-name
               作业运行时保存作业相关数据的数据源的名称。

           max-data-retention-time-in-seconds
               已完成作业保留在作业资料档案库中的最长时间。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

示例
       Example 1, 列出批处理运行时配置
           以下示例列出批处理运行时的配置。

               asadmin> list-batch-runtime-configuration
               MIN-THREAD-POOL-SIZE  MAX-THREAD-POOL-SIZE  DATA-SOURCE-NAME  4                     16                    jdbc/__TimerPool
               Command list-batch-runtime-configuration executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-batch-jobs(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 list-batch-runtime-configuration(1)
