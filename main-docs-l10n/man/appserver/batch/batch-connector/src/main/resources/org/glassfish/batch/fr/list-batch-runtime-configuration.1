list-batch-runtime-confiSous-commandes de llist-batch-runtime-configuration(1)

NOM
       list-batch-runtime-configuration - affiche la configuration de
       l'exécution du batch

SYNTAXE
           list-batch-runtime-configuration [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

DESCRIPTION
       La sous-commande list-batch-runtime-configuration affiche la
       configuration de l'exécution du batch. Les données de configuration de
       l'exécution du batch sont stockées dans l'élément config du fichier
       domain.xml.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Affiche des informations détaillées sur l'exécution du batch. La
           valeur par défaut est false.

       --output, -o
           Affiche des détails spécifiques relatifs à l'exécution du batch.
           Utilisez une liste d'éléments séparés par des virgules pour
           indiquer les détails à afficher et leur ordre. Les valeurs ne
           respectent pas les majuscules/minuscules. Les en-têtes
           min-thread-pool-size, max-thread-pool-size, max-queue-size et
           data-source-name sont affichés par défaut.

           Les valeurs possibles sont les suivantes :

           min-thread-pool-size
               Taille minimale du pool de threads.

           max-thread-pool-size
               Taille maximale du pool de threads.

           max-idle-thread-timeout-in-seconds
               Durée maximale d'inactivité au-delà de laquelle le thread est
               détruit.

           max-queue-size
               Taille maximale de la file d'attente stockant les travaux.

           data-source-name
               Nom de la source de données dans laquelle les données relatives
               aux travaux sont rendues persistantes par l'exécution du
               travail.

           max-data-retention-time-in-seconds
               Durée maximale pendant laquelle les travaux terminés sont
               conservés dans le référentiel de travaux.

       --header, -h
           Indique si les en-têtes de colonne sont affichés lorsque l'option
           --long est utilisée. La valeur par défaut est true. Pour supprimer
           les en-têtes, définissez l'option --header sur false.

EXEMPLES
       Exemple 1, Liste de la configuration de l'exécution du batch
           Dans cet exemple, la configuration de l'exécution du batch est
           répertoriée.

               asadmin> list-batch-runtime-configuration
               MIN-THREAD-POOL-SIZE  MAX-THREAD-POOL-SIZE  DATA-SOURCE-NAME  4                     16                    jdbc/__TimerPool
               Command list-batch-runtime-configuration executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       list-batch-jobs(1)

       asadmin(1M)

Java EE 7                        13 fév. 20list-batch-runtime-configuration(1)
