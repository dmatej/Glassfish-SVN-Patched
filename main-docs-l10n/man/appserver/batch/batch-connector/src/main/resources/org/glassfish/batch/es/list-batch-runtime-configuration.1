list-batch-runtime-confiSubcomandos de la Ulist-batch-runtime-configuration(1)

NOMBRE DE REFERENCIA
       list-batch-runtime-configuration - Muestra la configuración del tiempo
       de ejecución por lotes.

SINOPSIS
           list-batch-runtime-configuration [--help]
           [--target target]
           [--output output]
           [--header={false|true}]

DESCRIPCIÓN
       El subcomando list-batch-runtime-configuration muestra la configuración
       del tiempo de ejecución por lotes. Los datos de configuración de tiempo
       de ejecución por lotes se almacenan en el elemento config de
       domain.xml.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va a mostrar la configuración de
           tiempo de ejecución por lotes. Los valores válidos son los
           siguientes:

           server
               Muestra la configuración de tiempo de ejecución por lotes de la
               instancia de servidor por defecto server y es el valor por
               defecto.

           cluster-name
               Muestra la configuración de tiempo de ejecución por lotes de
               cada instancia del servidor en el cluster.

           instance-name
               Muestra la configuración de tiempo de ejecución por lotes de
               una instancia de servidor determinada.

       --output, -o
           Muestra detalles concretos sobre la configuración de tiempo de
           ejecución por lotes. Utilice una lista separada por comas para
           especificar los detalles que desea mostrar y el orden de los
           mismos. Los valores son sensibles a mayúsculas/minúsculas. Las
           cabeceras de columna datasourcelookupname y
           executorservicelookupname se muestran por defecto.

           Los valores posibles son los siguientes:

           datasourcelookupname
               Nombre de búsqueda JNDI del origen de datos utilizado para
               almacenar la información de trabajo. Por defecto, el tiempo de
               ejecución por lotes utiliza el origen de datos por defecto
               jdbc/__TimerPool.

           executorservicelookupname
               Nombre de búsqueda JNDI del servicio de ejecutor gestionado
               utilizado para proporcionar threads a los trabajos. Por
               defecto, el tiempo de ejecución por lotes utiliza el servicio
               de ejecutor gestionado por defecto
               concurrent/__defaultManagedExecutorService.

       --header, -h
           Especifica si los encabezados de columna se muestran cuando se usa
           la opción --long. El valor por defecto es true. Para suprimir los
           encabezados, defina la opción --header en false.

EJEMPLOS
       Ejemplo 1, Visualización de la configuración de tiempo de ejecución por
       lotes
           En el siguiente ejemplo se muestra la configuración del tiempo de
           ejecución por lotes para la instancia de servidor por defecto.

               asadmin> list-batch-runtime-configuration
               DATASOURCELOOKUPNAME     EXECUTORSERVICELOOKUPNAME
               jdbc/_default            concurrent/__defaultManagedExecutorService
               Command list-batch-runtime-configuration executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       set-batch-runtime-configuration(1), list-batch-jobs(1),
       list-batch-job-executions(1), list-batch-job-steps(1)

       asadmin(1M)

Java EE 7                    13 de febrero list-batch-runtime-configuration(1)
