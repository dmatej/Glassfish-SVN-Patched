list-batch-runtime-confiSubcomandos do utillist-batch-runtime-configuration(1)

NOME
       list-batch-runtime-configuration - Exibe a configuração do runtime do
       batch

SINOPSE
           list-batch-runtime-configuration [--help]
           [--target target]
           [--output output]
           [--header={false|true}]

DESCRIçãO
       O subcomando list-batch-runtime-configuration exibe a configuração de
       runtime de batch. Os dados de configuração de runtime de batch são
       armazenados no elemento config no domain.xml.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo cuja configuração de tempo de execução em lote
           deve ser listada. Os valores válidos são os seguintes:

           server
               Lista a configuração de tempo de execução em batch para a
               instância do servidor default server e é o valor default.

           cluster-name
               Lista a configuração do tempo de execução em batch para cada
               instância do servidor no cluster.

           instance-name
               Lista a configuração do tempo de execução em batch para uma
               determinada instância do servidor.

       --output, -o
           Exibe detalhes específicos sobre a configuração do tempo de
           execução em batch. Use uma lista separada por vírgulas para
           especificar os detalhes que você deseja exibir e sua ordem. Os
           valores diferenciam maiúsculas de minúsculas. Os cabeçalhos de
           coluna datasourcelookupname e executorservicelookupname são
           exibidos por default.

           Os valores possíveis são os seguintes:

           datasourcelookupname
               O nome de pesquisa JNDI da origem de dados usado para armazenar
               informações sobre o job. Por default, o tempo de execução em
               batch utiliza a origem de dados default jdbc/__TimerPool.

           executorservicelookupname
               O nome da pesquisa JNDI do serviço executor gerenciado usado
               para fornecer threads aos jobs. Por default, o tempo de
               execução em batch usa o serviço executor gerenciado default
               concurrent/__defaultManagedExecutorService.

       --header, -h
           Especifica se os cabeçalhos da coluna são exibidos quando a opção
           --long for usada. O valor default é verdadeiro. Para suprimir os
           cabeçalhos, defina a opção --header como false.

EXEMPLOS
       Example 1, Listando a Configuração do Tempo de Execução em Batch
           O exemplo a seguir lista a configuração do tempo de execução em
           batch para a instância do servidor default.

               asadmin> list-batch-runtime-configuration
               DATASOURCELOOKUPNAME     EXECUTORSERVICELOOKUPNAME
               jdbc/_default            concurrent/__defaultManagedExecutorService
               Command list-batch-runtime-configuration executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       set-batch-runtime-configuration(1), list-batch-jobs(1),
       list-batch-job-executions(1), list-batch-job-steps(1)

       asadmin(1M)

Java EE 7                         13 fev 20list-batch-runtime-configuration(1)
