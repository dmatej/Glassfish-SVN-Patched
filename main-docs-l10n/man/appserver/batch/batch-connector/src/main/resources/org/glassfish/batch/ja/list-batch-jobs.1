list-batch-jobs(1)    asadminユーティリティのサブコマンド   list-batch-jobs(1)

名前
       list-batch-jobs - バッチ・ジョブを表示します

概要
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

説明
       list-batch-jobsサブコマンドは、バッチ・ジョブおよびジョブの詳細を表示します。ジョブは、バッチ・プロセス全体を表します。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --job-name, -j
           ジョブの名前を指定します。

       --instance-id, -i
           ジョブのインスタンスIDを指定します。

       --execution-id, -e
           ジョブの実行IDを指定します。

       --long, -l
           ジョブの詳細情報を表示します。デフォルト値はfalseです。

       --output, -o
           各ジョブの特定の詳細を表示します。表示する詳細とその順序を指定するには、カンマ区切りのリストを使用します。この値では大文字と小文字が区別されません。job-nameおよびinstance-countの列ヘッダーは、デフォルトで表示されます。

           使用可能な値は次のとおりです:

           job-name
               ジョブの名前を表示します。

           instance-count
               実行中のジョブ・インスタンスの数を表示します。

           execution-id
               特定のジョブ・インスタンスの実行IDを表示します。

           status
               ジョブのステータスを表示します。

           exit-status
               ジョブの終了ステータスを表示します。

           start-time
               ジョブの開始時間を表示します。

           end-time
               ジョブの終了時間を表示します。

           job-parameters
               ジョブ・パラメータを表示します。

       --header, -h
           --longオプションが使用されている場合に、列ヘッダーを表示するかどうかを指定します。デフォルト値はtrueです。ヘッダーを表示しないようにするには、--headerオプションをfalseに設定します。

例
       例 1, バッチ・ジョブの表示
           次の例では、バッチ・ジョブを表示します。

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                        2013年2月13日              list-batch-jobs(1)
