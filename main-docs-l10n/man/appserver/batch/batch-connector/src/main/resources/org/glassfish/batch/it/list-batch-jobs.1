list-batch-jobs(1)      Comandi secondari della utilit      list-batch-jobs(1)

NOME
       list-batch-jobs - elenca i processi batch

SINTASSI
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

DESCRIZIONE
       Il comando secondario list-batch-jobs elenca i processi batch e i
       dettagli dei processi. Un processo rappresenta l'intera elaborazione
       batch.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --job-name, -j
           Specifica il nome di un processo batch.

       --instance-id, -i
           Specifica l'ID istanza di un processo batch.

       --execution-id, -e
           Specifica l'ID esecuzione di un processo batch.

       --long, -l
           Visualizza informazioni dettagliate sui processi batch. Il valore
           predefinito è false.

       --output, -o
           Visualizza i dettagli specifici di ogni processo batch. Utilizzare
           una lista separata da virgole per specificare i dettagli da
           visualizzare e l'ordine di visualizzazione preferito. I valori non
           fanno distinzione tra maiuscole e minuscole. Le intestazioni di
           colonna job-name e instance-count vengono visualizzate per
           impostazione predefinita.

           Di seguito sono riportati i valori possibili.

           job-name
               Visualizza il nome del processo batch.

           instance-count
               Visualizza il numero delle istanze di processo in esecuzione.

           execution-id
               Visualizza l'ID esecuzione di un'istanza di processo
               particolare.

           status
               Visualizza lo stato del processo.

           exit-status
               Visualizza lo stato di uscita del processo.

           start-time
               Visualizza l'ora di inizio del processo.

           end-time
               Visualizza l'ora di fine del processo.

           job-parameters
               Visualizza i parametri del processo.

       --header, -h
           Specifica se le intestazioni di colonna vengono visualizzate quando
           si utilizza l'opzione --long. Il valore predefinito è true. Per
           eliminare le intestazioni, impostare l'opzione --header su false.

ESEMPI
       Esempio 1, Visualizzazione della lista dei processi batch
           Nell'esempio seguente vengono elencati i processi batch esistenti.

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                      13 febbraio 2013             list-batch-jobs(1)
