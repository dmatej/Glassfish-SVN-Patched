list-batch-runtime-confiComandinsecondari dlist-batch-runtime-configuration(1)

NOME
       list-batch-runtime-configuration - visualizza la configurazione del
       runtime batch

SINTASSI
           list-batch-runtime-configuration [--help]
           [--target target]
           [--output output]
           [--header={false|true}]

DESCRIZIONE
       Il comando secondario list-batch-runtime-configuration visualizza la
       configurazione del runtime batch. I dati di configurazione del runtime
       batch sono memorizzati nell'elemento config del file domain.xml.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione per la quale elencare la configurazione
           del runtime batch. Di seguito sono riportati i valori validi.

           server
               Elenca la configurazione del runtime batch per l'istanza server
               predefinita server ed è il valore predefinito.

           cluster-name
               Elenca la configurazione del runtime batch per ogni istanza
               server del cluster.

           instance-name
               Elenca la configurazione del runtime batch per un'istanza
               server particolare.

       --output, -o
           Visualizza dettagli specifici sulla configurazione del runtime
           batch. Utilizzare una lista separata da virgole per specificare i
           dettagli da visualizzare e l'ordine di visualizzazione. I valori
           non fanno distinzione tra maiuscole e minuscole. Le intestazioni di
           colonna datasourcelookupname e executorservicelookupname vengono
           visualizzate per impostazione predefinita.

           Di seguito sono riportati i valori possibili.

           datasourcelookupname
               Il nome della ricerca JNDI dell'origine dati utilizzata per
               memorizzare le informazioni sui processi. Per impostazione
               predefinita, il runtime batch utilizza l'origine dati
               predefinita jdbc/__TimerPool.

           executorservicelookupname
               Il nome della ricerca JNDI del servizio esecutore gestito
               utilizzato per fornire i thread ai processi. Per impostazione
               predefinita, il runtime batch utilizza il servizio esecutore
               gestito predefinito concurrent/__defaultManagedExecutorService.

       --header, -h
           Specifica se le intestazioni di colonna vengono visualizzate quando
           si utilizza l'opzione --long. Il valore predefinito è true. Per
           eliminare le intestazioni, impostare l'opzione --header su false.

ESEMPI
       Esempio 1, Visualizzazione della configurazione del runtime batch
           Nell'esempio riportato di seguito viene visualizzata la
           configurazione del runtime batch per l'istanza server predefinita.

               asadmin> list-batch-runtime-configuration
               DATASOURCELOOKUPNAME     EXECUTORSERVICELOOKUPNAME
               jdbc/_default            concurrent/__defaultManagedExecutorService
               Command list-batch-runtime-configuration executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       set-batch-runtime-configuration(1), list-batch-jobs(1),
       list-batch-job-executions(1), list-batch-job-steps(1)

       asadmin(1M)

Java EE 7                      13 febbraio list-batch-runtime-configuration(1)
