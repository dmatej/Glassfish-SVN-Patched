list-batch-jobs(1)          asadmin 实用程序子命令          list-batch-jobs(1)

名称
       list-batch-jobs - 列出批处理作业

提要
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

说明
       list-batch-jobs
       子命令可列出批处理作业和作业详细信息。一个作业表示整个批处理进程。

选项
       --help, -?
           显示子命令的帮助文本。

       --job-name, -j
           指定作业的名称。

       --instance-id, -i
           指定作业的实例 ID。

       --execution-id, -e
           指定作业的执行 ID。

       --long, -l
           显示有关作业的详细信息。默认值为假

       --output, -o
           显示有关每个作业的特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。值不区分大小写。默认情况下显示
           job-name 和 instance-count 列标题。

           可能的值如下：

           job-name
               显示作业的名称。

           instance-count
               显示正在运行的作业实例数。

           execution-id
               显示特定作业实例的执行 ID。

           status
               显示作业的状态。

           exit-status
               显示作业的退出状态。

           start-time
               显示作业的开始时间。

           end-time
               显示作业的完成时间。

           job-parameters
               显示作业参数。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

示例
       Example 1, 列出批处理作业
           以下示例列出批处理作业。

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日            list-batch-jobs(1)
