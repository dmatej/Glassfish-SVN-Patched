list-batch-jobs(1)          asadmin 实用程序子命令          list-batch-jobs(1)

名称
       list-batch-jobs - 列出批处理作业

提要
           list-batch-jobs [--help]
           [--target target]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [job_name]

说明
       list-batch-jobs 子命令可列出批处理作业和作业详细信息。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要列出其批处理作业和作业详细信息的目标。有效值如下：

           server
               列出默认服务器实例 server 的批处理作业，这是默认值。

           cluster-name
               列出集群中每个服务器实例的批处理作业。

           instance-name
               列出特定服务器实例的批处理作业。

       --long, -l
           显示有关批处理作业的详细信息。默认值为假

       --output, -o
           显示有关批处理作业的特定详细信息。使用逗号分隔的列表指定要显示的详细信息及其顺序。值不区分大小写。默认情况下显示
           jobname 和 instancecount 列标题。

           可能的值如下：

           jobname
               显示作业的名称。

           appname
               显示应用程序的名称。

           instancecount
               显示作业实例数。

           instanceid
               显示分配给作业实例的 ID。

           executionid
               显示分配给批处理作业的执行的
               ID。首次启动某个作业时将创建新执行，且每次都将重新启动现有执行。

           batchstatus
               将作业的状态显示为按批处理运行时列出的集。

           starttime
               显示作业的开始时间。

           endtime
               显示作业的完成时间。

           exitstatus
               将作业的状态显示为按作业的作业 XML
               或按批处理应用程序列出的集。默认情况下，退出状态和批处理状态相同，除非显式覆盖退出状态。

       --header, -h
           指定在使用 --long
           选项时是否显示列标题。默认值为真
           选项设置为假

操作数
       job_name
           要列出其详细信息的作业的名称。

示例
       Example 1, 列出批处理作业
           此示例列出默认服务器实例的批处理作业。

               asadmin> list-batch-jobs
               JOBNAME  INSTANCECOUNT
               payroll  9
               bonus    6
               Command list-batch-jobs executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       list-batch-job-executions(1), list-batch-job-steps(1),
       set-batch-runtime-configuration(1), list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日            list-batch-jobs(1)
