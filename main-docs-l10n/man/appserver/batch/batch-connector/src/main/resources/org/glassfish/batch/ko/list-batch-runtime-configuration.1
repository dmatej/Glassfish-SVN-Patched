list-batch-runtime-configuasadmin1유틸리티 list-batch-runtime-configuration(1)

제목
       list-batch-runtime-configuration - 일괄 런타임의 구성 표시

시놉시스
           list-batch-runtime-configuration [--help]
           [--target target]
           [--output output]
           [--header={false|true}]

설명
       list-batch-runtime-configuration 하위 명령은 일괄 런타임의 구성을
       표시합니다. 일괄 런타임 구성 데이터는 domain.xml의 config 요소에
       저장됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           일괄 처리 런타임 구성을 나열할 대상을 지정합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스인 server의 일괄 처리 런타임 구성을 나열하며
               기본값입니다.

           cluster-name
               클러스터에 있는 모든 서버 인스턴스의 일괄 처리 런타임 구성을
               나열합니다.

           instance-name
               특정 서버 인스턴스의 일괄 처리 런타임 구성을 나열합니다.

       --output, -o
           일괄 처리 런타임 구성에 대한 특정 세부 정보를 표시합니다. 쉼표로
           구분된 목록을 사용하여 표시할 세부 정보 및 해당 순서를 지정합니다.
           이러한 값은 대소문자를 구분하지 않습니다.  datasourcelookupname 및
           executorservicelookupname 열 머리글은 기본적으로 표시됩니다.

           가능한 값은 다음과 같습니다.

           datasourcelookupname
               작업 정보를 저장하는 데 사용되는 데이터 소스의 JNDI 조회
               이름입니다. 기본적으로 일괄 처리 런타임은 기본 데이터 소스인
               jdbc/__TimerPool을 사용합니다.

           executorservicelookupname
               작업에 스레드를 제공하는 데 사용되는 관리 대상 실행기 서비스의
               JNDI 조회 이름입니다. 기본적으로 일괄 처리 런타임은 기본 관리
               대상 실행기 서비스인
               concurrent/__defaultManagedExecutorService를 사용합니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

예제
       예 1, 일괄 런타임 구성 나열
           다음 예제에서는 기본 서버 인스턴스에 대한 일괄 처리 런타임 구성을
           나열합니다.

               asadmin> list-batch-runtime-configuration
               DATASOURCELOOKUPNAME     EXECUTORSERVICELOOKUPNAME
               jdbc/_default            concurrent/__defaultManagedExecutorService
               Command list-batch-runtime-configuration executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       set-batch-runtime-configuration(1), list-batch-jobs(1),
       list-batch-job-executions(1), list-batch-job-steps(1)

       asadmin(1M)

Java EE 7                       2013년 2월 list-batch-runtime-configuration(1)
