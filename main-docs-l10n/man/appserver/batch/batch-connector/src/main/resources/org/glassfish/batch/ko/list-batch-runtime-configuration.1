list-batch-runtime-configuasadmin1유틸리티 list-batch-runtime-configuration(1)

제목
       list-batch-runtime-configuration - 일괄 런타임의 구성 표시

시놉시스
           list-batch-runtime-configuration [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

설명
       list-batch-runtime-configuration 하위 명령은 일괄 런타임의 구성을
       표시합니다. 일괄 런타임 구성 데이터는 domain.xml의 config 요소에
       저장됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --long, -l
           일괄 런타임에 대한 세부 정보를 표시합니다. 기본값은 false입니다.

       --output, -o
           일괄 런타임에 대한 특정 세부 정보를 표시합니다. 표시할 세부 정보 및
           순서를 지정하려면 쉼표로 구분된 목록을 사용합니다. 이러한 값은
           대소문자를 구분하지 않습니다.  min-thread-pool-size,
           max-thread-pool-size, max-queue-size 및 data-source-name 열
           머리글이 기본적으로 표시됩니다.

           가능한 값은 다음과 같습니다.

           min-thread-pool-size
               최소 스레드 풀 크기입니다.

           max-thread-pool-size
               최대 스레드 풀 크기입니다.

           max-idle-thread-timeout-in-seconds
               스레드가 삭제되기 전의 최대 유휴 시간입니다.

           max-queue-size
               작업을 보유하기 위한 최대 대기열 크기입니다.

           data-source-name
               작업 런타임에서 작업에 대한 데이터를 지속하는 데이터 소스의
               이름입니다.

           max-data-retention-time-in-seconds
               완료된 작업의 최대 양이 작업 저장소에 보존됩니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

예제
       예 1, 일괄 런타임 구성 나열
           다음 예제는 일괄 런타임의 구성을 나열합니다.

               asadmin> list-batch-runtime-configuration
               MIN-THREAD-POOL-SIZE  MAX-THREAD-POOL-SIZE  DATA-SOURCE-NAME  4                     16                    jdbc/__TimerPool
               Command list-batch-runtime-configuration executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       list-batch-jobs(1)

       asadmin(1M)

Java EE 7                       2013년 2월 list-batch-runtime-configuration(1)
