list-batch-jobs(1)          asadmin 公用程式子命令          list-batch-jobs(1)

參考名
       list-batch-jobs - 列出批次工作

概要
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

描述
       list-batch-jobs
       子命令會列出批次工作與工作詳細資訊。一項工作代表整個批次處理作業。

選項
       --help, -?
           顯示子命令的說明文字。

       --job-name, -j
           指定工作名稱。

       --instance-id, -i
           指定工作的執行處理 ID。

       --execution-id, -e
           指定工作的執行 ID。

       --long, -l
           顯示與工作有關的詳細資訊。預設值為 false。

       --output, -o
           顯示與每項工作有關的特定詳細資訊。使用以逗號區隔的清單來指定您要顯示的詳細資訊和顯示順序。值有大小寫之分。預設會顯示
           job-name 與 instance-count 欄標題。

           可能值如下：

           job-name
               顯示工作的名稱。

           instance-count
               顯示正在執行之工作執行處理的數目。

           execution-id
               顯示特定工作執行處理的執行 ID。

           status
               顯示工作的狀態。

           exit-status
               顯示工作的結束狀態。

           start-time
               顯示工作的啟動時間。

           end-time
               顯示工作的完成時間。

           job-parameters
               顯示工作參數。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

範例
       範例 1, 列出批次工作
           以下範例會列出批次工作。

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日            list-batch-jobs(1)
