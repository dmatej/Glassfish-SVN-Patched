list-batch-jobs(1)          asadmin 公用程式子命令          list-batch-jobs(1)

參考名
       list-batch-jobs - 列出批次工作

概要
           list-batch-jobs [--help]
           [--target target]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [job_name]

描述
       list-batch-jobs 子命令會列出批次工作與工作詳細資訊。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定要列出批次工作和工作詳細資訊的目標。有效的值如下：

           server
               列出預設伺服器執行處理 server 的批次工作，且這是預設值。

           cluster-name
               列出叢集中每個伺服器執行處理的批次工作。

           instance-name
               列出特定伺服器執行處理的批次工作。

       --long, -l
           顯示與批次工作有關的詳細資訊。預設值為 false。

       --output, -o
           顯示與批次工作有關的特定詳細資訊。使用以逗號區隔的清單來指定要顯示的詳細資訊和其顯示順序。值有大小寫之分。預設會顯示
           jobname 和 instancecount 欄標題。

           可能值如下：

           jobname
               顯示工作的名稱。

           appname
               顯示應用程式的名稱。

           instancecount
               顯示工作執行處理數目。

           instanceid
               顯示指定給工作執行處理的 ID。

           executionid
               顯示指定給批次工作執行的
               ID。在工作第一次啟動時及每次重新啟動現有的執行時，都會建立新的執行。

           batchstatus
               顯示由批次執行階段設定的工作狀態。

           starttime
               顯示工作的啟動時間。

           endtime
               顯示工作的完成時間。

           exitstatus
               顯示由工作的工作 XML
               設定或由批次應用程式設定的工作狀態。根據預設，除非已明確置換結束狀態，否則結束狀態應與批次狀態相同。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

運算元
       job_name
           要列出詳細資訊的工作名稱。

範例
       範例 1, 列出批次工作
           此範例列出預設伺服器執行處理的批次工作。

               asadmin> list-batch-jobs
               JOBNAME  INSTANCECOUNT
               payroll  9
               bonus    6
               Command list-batch-jobs executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       list-batch-job-executions(1), list-batch-job-steps(1),
       set-batch-runtime-configuration(1), list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 13 日            list-batch-jobs(1)
