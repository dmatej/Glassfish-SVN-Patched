list-batch-jobs(1)        asadmin 유틸리티 하위 명령        list-batch-jobs(1)

제목
       list-batch-jobs - 일괄 처리 작업 나열

시놉시스
           list-batch-jobs [--help]
           [--target target]
           [--long={false|true}]
           [--output output]
           [--header={false|true}]
           [job_name]

설명
       list-batch-jobs 하위 명령은 일괄 작업 및 작업 세부 정보를 나열합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           일괄 처리 작업 및 작업 세부 정보를 나열할 대상을 지정합니다. 적합한
           값은 다음과 같습니다.

           server
               기본 서버 인스턴스인 server의 일괄 처리 작업을 나열하며
               기본값입니다.

           cluster-name
               클러스터에 있는 모든 서버 인스턴스의 일괄 처리 작업을
               나열합니다.

           instance-name
               특정 서버 인스턴스의 일괄 처리 작업을 나열합니다.

       --long, -l
           일괄 처리 작업에 대한 세부 정보를 표시합니다. 기본값은 false입니다.

       --output, -o
           일괄 처리 작업에 대한 특정 세부 정보를 표시합니다. 쉼표로 구분된
           목록을 사용하여 표시할 세부 정보 및 해당 순서를 지정합니다. 이러한
           값은 대소문자를 구분하지 않습니다.  jobname 및 instancecount 열
           머리글은 기본적으로 표시됩니다.

           가능한 값은 다음과 같습니다.

           jobname
               작업의 이름을 표시합니다.

           appname
               응용 프로그램의 이름을 표시합니다.

           instancecount
               작업 인스턴스 수를 표시합니다.

           instanceid
               작업 인스턴스에 지정된 ID를 표시합니다.

           executionid
               일괄 처리 작업 실행에 지정된 ID를 표시합니다. 새 실행은 작업이
               처음 시작될 때, 그리고 기존 실행이 재시작될 때마다 생성됩니다.

           batchstatus
               일괄 처리 런타임에 의해 설정된 작업 상태를 표시합니다.

           starttime
               작업의 시작 상태를 표시합니다.

           endtime
               작업의 완료 시간을 표시합니다.

           exitstatus
               해당 작업에 대한 작업 XML 또는 일괄 처리 응용 프로그램에 의해
               설정된 작업 상태를 표시합니다. 종료 상태가 명시적으로 대체되지
               않는 한 기본적으로 종료 상태와 일괄 처리 상태는 동일합니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

피연산자
       job_name
           세부 정보를 나열할 작업 이름입니다.

예제
       예 1, 일괄 처리 작업 나열
           이 예제에서는 기본 서버 인스턴스에 대한 일괄 처리 작업을
           나열합니다.

               asadmin> list-batch-jobs
               JOBNAME  INSTANCECOUNT
               payroll  9
               bonus    6
               Command list-batch-jobs executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       list-batch-job-executions(1), list-batch-job-steps(1),
       set-batch-runtime-configuration(1), list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                       2013년 2월 13일             list-batch-jobs(1)
