list-batch-jobs(1)        asadmin 유틸리티 하위 명령        list-batch-jobs(1)

제목
       list-batch-jobs - 일괄 작업 나열

시놉시스
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

설명
       list-batch-jobs 하위 명령은 일괄 작업 및 작업 세부 정보를 나열합니다.
       전체 일괄 프로세스를 나타내는 작업입니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --job-name, -j
           작업의 이름을 지정합니다.

       --instance-id, -i
           작업의 인스턴스 ID를 지정합니다.

       --execution-id, -e
           작업의 실행 ID를 지정합니다.

       --long, -l
           작업에 대한 자세한 정보를 표시합니다. 기본값은 false입니다.

       --output, -o
           각 작업에 대한 특정 세부 정보를 표시합니다. 표시할 세부 정보 및
           순서를 지정하려면 쉼표로 구분된 목록을 사용합니다. 이러한 값은
           대소문자를 구분하지 않습니다.  job-name 및 instance-count 열
           머리글은 기본적으로 표시됩니다.

           가능한 값은 다음과 같습니다.

           job-name
               작업의 이름을 표시합니다.

           instance-count
               실행 중인 작업 인스턴스의 수를 표시합니다.

           execution-id
               특정 작업 인스턴스의 실행 ID를 표시합니다.

           status
               작업의 상태를 표시합니다.

           exit-status
               작업의 종료 상태를 표시합니다.

           start-time
               작업의 시작 상태를 표시합니다.

           end-time
               작업의 완료 시간을 표시합니다.

           job-parameters
               작업 매개변수를 표시합니다.

       --header, -h
           --long 옵션이 사용될 때 열 머리글이 표시되는지 여부를 지정합니다.
           기본값은 true입니다. 머리글이 표시되지 않도록 하려면 --header
           옵션을 false로 설정합니다.

예제
       예 1, 일괄 작업 나열
           다음 예제는 일괄 작업을 나열합니다.

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                       2013년 2월 13일             list-batch-jobs(1)
