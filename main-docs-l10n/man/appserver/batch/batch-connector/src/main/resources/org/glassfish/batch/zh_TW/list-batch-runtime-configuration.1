list-batch-runtime-configuraasadmin 公用程式

參考名
       list-batch-runtime-configuration - 顯示批次執行階段的組態

概要
           list-batch-runtime-configuration [--help]
           [--target target]
           [--output output]
           [--header={false|true}]

描述
       list-batch-runtime-configuration
       子命令會顯示批次執行階段的組態。批次執行階段組態資料會儲存在 domain.xml
       內的 config 元素中。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定要列出批次執行階段組態的目標。有效的值如下：

           server
               列出預設伺服器執行處理 server
               的批次執行階段組態，且這是預設值。

           cluster-name
               列出叢集中每個伺服器執行處理的批次執行階段組態。

           instance-name
               列出特定伺服器執行處理的批次執行階段組態。

       --output, -o
           顯示與批次執行階段組態有關的特定詳細資訊。使用以逗號區隔的清單來指定要顯示的詳細資訊和其顯示順序。值有大小寫之分。預設會顯示
           datasourcelookupname 和 executorservicelookupname 欄標題。

           可能值如下：

           datasourcelookupname
               用來儲存工作資訊之資料來源的 JNDI
               查詢名稱。批次執行階段預設會使用預設的資料來源
               jdbc/__TimerPool。

           executorservicelookupname
               用來將繫線提供給工作之受管理執行程式服務的 JNDI
               查詢名稱。批次執行階段預設會使用預設的受管理執行程式服務
               concurrent/__defaultManagedExecutorService。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

範例
       範例 1, 列出批次執行階段組態
           以下範例會列出預設伺服器執行處理的批次執行階段組態。

               asadmin> list-batch-runtime-configuration
               DATASOURCELOOKUPNAME     EXECUTORSERVICELOOKUPNAME
               jdbc/_default            concurrent/__defaultManagedExecutorService
               Command list-batch-runtime-configuration executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       set-batch-runtime-configuration(1), list-batch-jobs(1),
       list-batch-job-executions(1), list-batch-job-steps(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 list-batch-runtime-configuration(1)
