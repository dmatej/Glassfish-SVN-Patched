list-batch-runtime-configuraasadmin 公用程式

參考名
       list-batch-runtime-configuration - 顯示批次執行階段的組態

概要
           list-batch-runtime-configuration [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

描述
       list-batch-runtime-configuration
       子命令會顯示批次執行階段的組態。批次執行階段組態資料會儲存在 domain.xml
       內的 config 元素中。

選項
       --help, -?
           顯示子命令的說明文字。

       --long, -l
           顯示與批次執行階段有關的詳細資訊。預設值為 false。

       --output, -o
           顯示與批次執行階段有關的特定詳細資訊。使用以逗號區隔的清單來指定您要顯示的詳細資訊和顯示順序。值有大小寫之分。預設會顯示
           min-thread-pool-size、max-thread-pool-size、max-queue-size 及
           data-source-name 欄標題。

           可能值如下：

           min-thread-pool-size
               繫線集區大小下限。

           max-thread-pool-size
               繫線集區大小上限。

           max-idle-thread-timeout-in-seconds
               繫線被銷毀之前的閒置時間上限。

           max-queue-size
               保留工作的佇列大小上限。

           data-source-name
               工作執行階段保留工作相關資料所在的資料來源名稱。

           max-data-retention-time-in-seconds
               已完成工作保留在工作儲存區域中的時間上限。

       --header, -h
           指定使用 --long 選項時是否顯示欄標題。預設值為
           true。若不要顯示標題，請將 --header 選項設為 false。

範例
       範例 1, 列出批次執行階段組態
           以下範例列出批次執行階段的組態。

               asadmin> list-batch-runtime-configuration
               MIN-THREAD-POOL-SIZE  MAX-THREAD-POOL-SIZE  DATA-SOURCE-NAME  4                     16                    jdbc/__TimerPool
               Command list-batch-runtime-configuration executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       list-batch-jobs(1)

       asadmin(1M)

Java EE 7                     2013 年 2 月 list-batch-runtime-configuration(1)
