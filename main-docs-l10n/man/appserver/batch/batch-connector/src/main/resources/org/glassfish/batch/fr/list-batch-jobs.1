list-batch-jobs(1)      Sous-commandes de l'utilitaire      list-batch-jobs(1)

NOM
       list-batch-jobs - répertorie les traitements batch

SYNTAXE
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

DESCRIPTION
       La sous-commande list-batch-jobs répertorie les traitements batch et
       les détails relatifs aux travaux associés. Un travail représente le
       processus de batch complet.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --job-name, -j
           Spécifie le nom d'un travail.

       --instance-id, -i
           Spécifie l'ID d'instance d'un travail.

       --execution-id, -e
           Spécifie l'ID d'exécution d'un travail.

       --long, -l
           Affiche des informations détaillées sur les travaux. La valeur par
           défaut est false.

       --output, -o
           Affiche des détails spécifiques relatifs à chaque travail. Utilisez
           une liste d'éléments séparés par des virgules pour indiquer les
           détails à afficher et leur ordre. Les valeurs ne respectent pas les
           majuscules/minuscules. Les en-têtes de colonne job-name et
           instance-count sont affichées par défaut.

           Les valeurs possibles sont les suivantes :

           job-name
               Affiche le nom du travail.

           instance-count
               Affiche le nombre d'instances de travail en cours d'exécution.

           execution-id
               Affiche l'ID d'exécution d'une instance de travail donnée.

           status
               Affiche le statut du travail.

           exit-status
               Affiche le statut de sortie du travail.

           start-time
               Affiche l'heure de début du travail.

           end-time
               Affiche l'heure de fin du travail.

           job-parameters
               Affiche les paramètres du travail.

       --header, -h
           Indique si les en-têtes de colonne sont affichés lorsque l'option
           --long est utilisée. La valeur par défaut est true. Pour supprimer
           les en-têtes, définissez l'option --header sur false.

EXEMPLES
       Exemple 1, Liste des traitements batch
           Dans cet exemple, les traitements batch sont répertoriés.

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                        13 fév. 2013               list-batch-jobs(1)
