list-batch-jobs(1)      Subcomandos do utilitário asad      list-batch-jobs(1)

NOME
       list-batch-jobs - Lista jobs em batch

SINOPSE
           list-batch-jobs [--help]
           [--job-name job-name]
           [--instance-id instance-id]
           [--execution-id execution-id]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

DESCRIçãO
       O subcomando list-batch-jobs lista jobs em batch e detalhes do job. Um
       job representa o processo em batch completo.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --job-name, -j
           Especifica o nome de um job.

       --instance-id, -i
           Especifica o ID da instância de um job.

       --execution-id, -e
           Especifica o ID de execução de um job.

       --long, -l
           Exibe informações detalhadas sobre os jobs. O valor default é
           false.

       --output, -o
           Exibe detalhes específicos sobre cada job. Use uma lista separada
           por vírgulas para especificar os detalhes que você deseja exibir e
           sua ordem. Os valores diferenciam maiúsculas de minúsculas. Os
           cabeçalhos da coluna job-name e instance-count são exibidos por
           default.

           Os valores possíveis são os seguintes:

           job-name
               Exibe o nome do job.

           instance-count
               Exibe o número de instâncias do job que estão em execução.

           execution-id
               Exibe o ID de execução de uma instância de job específica.

           status
               Exibe o status do job.

           exit-status
               Exibe o status de saída do job.

           start-time
               Exibe o horário inicial do job.

           end-time
               Exibe o horário de término do job.

           job-parameters
               Exibe os parâmetros do job.

       --header, -h
           Especifica se os cabeçalhos da coluna são exibidos quando a opção
           --long for usada. O valor default é verdadeiro. Para suprimir os
           cabeçalhos, defina a opção --header como false.

EXEMPLOS
       Example 1, Lista de Jobs em Batch
           O exemplo a seguir lista os jobs em batch.

               asadmin> list-batch-jobs

               Command list-batch-jobs executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       list-batch-runtime-configuration(1)

       asadmin(1M)

Java EE 7                         13 fev 2013               list-batch-jobs(1)
