list-batch-runtime-conasadminユ

名前
       list-batch-runtime-configuration - バッチ実行時の構成を表示します

概要
           list-batch-runtime-configuration [--help]
           [--long[={false|true}]]
           [--output output]
           [--header[={false|true}]]

説明
       list-batch-runtime-configurationサブコマンドは、バッチ実行時の構成を表示します。バッチ実行時の構成データは、domain.xmlのconfig要素に格納されます。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --long, -l
           バッチ実行時の詳細情報を表示します。デフォルト値はfalseです。

       --output, -o
           バッチ実行時の特定の詳細を表示します。表示する詳細とその順序を指定するには、カンマ区切りのリストを使用します。この値では大文字と小文字が区別されません。min-thread-pool-size、max-thread-pool-size、max-queue-sizeおよびdata-source-nameの列ヘッダーはデフォルトで表示されます。

           使用可能な値は次のとおりです:

           min-thread-pool-size
               スレッド・プールの最小サイズ。

           max-thread-pool-size
               スレッド・プールの最大サイズ。

           max-idle-thread-timeout-in-seconds
               スレッドが破棄されるまでの最大アイドル時間。

           max-queue-size
               ジョブを保持する最大キュー・サイズ。

           data-source-name
               ジョブ実行時により、ジョブに関するデータが保持されるデータソースの名前。

           max-data-retention-time-in-seconds
               完了したジョブがジョブ・リポジトリに保持される最大時間。

       --header, -h
           --longオプションが使用されている場合に、列ヘッダーを表示するかどうかを指定します。デフォルト値はtrueです。ヘッダーを表示しないようにするには、--headerオプションをfalseに設定します。

例
       例 1, バッチ実行時の構成の表示
           次の例では、バッチ実行時の構成を表示します。

               asadmin> list-batch-runtime-configuration
               MIN-THREAD-POOL-SIZE  MAX-THREAD-POOL-SIZE  DATA-SOURCE-NAME  4                     16                    jdbc/__TimerPool
               Command list-batch-runtime-configuration executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       list-batch-jobs(1)

       asadmin(1M)

Java EE 7                        2013年2月1list-batch-runtime-configuration(1)
