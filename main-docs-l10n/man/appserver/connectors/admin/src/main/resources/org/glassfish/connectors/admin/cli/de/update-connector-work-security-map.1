update-connector-work-seUnterbefehle)des update-connector-work-security-map(1)

NAME
       update-connector-work-security-map - ändert eine Work Security Map für
       den angegebenen Ressourcenadapter

ZUSAMMENFASSUNG
           update-connector-work-security-map [--help] --raname raname
           [--addprincipals eis-principal1=server-principal1[, eis-principal2=server-principal2]*]
           [--addgroups eis-group1=server-group1[, eis-group2=server-group2]*]
           [--removeprincipals eis-principal1[,eis-principal2]*]
           [--removegroups eis-group1[, eis-group2]*]
           mapname

BESCHREIBUNG
       Der Unterbefehl update-connector-work-security-map modifiziert eine
       Sicherheitszuordnung für den angegebenen Ressourcenadapter.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --addgroups
           Gibt eine durch Komma getrennte Liste der EIS-Gruppen an, die
           hinzugefügt werden sollen. Verwenden Sie entweder die Option
           --addprincipals oder die Option --addgroups, aber nicht beide.

       --addprincipals
           Gibt eine durch Komma getrennte Liste der EIS-spezifischen
           Principals an, die hinzugefügt werden sollen. Verwenden Sie
           entweder die Option --addprincipals oder die Option --addgroups,
           aber nicht beide.

       --removegroups
           Gibt eine durch Komma getrennte Liste der EIS-Gruppen an, die
           entfernt werden sollen.

       --removeprincipals
           Gibt eine durch Komma getrennte Liste der EIS-spezifischen
           Principals an, die entfernt werden sollen.

       --raname
           Gibt den Namen des Connector-Moduls an, zu dem die Work Security
           Map gehört.

OPERANDEN
       mapname
           Der Name der Work Security Map, die aktualisiert werden soll.

BEISPIELE
       Example 1, Work Security Maps eines Connectors aktualisieren
           In diesem Beispiel wird workSecurityMap2 aktualisiert, indem
           eis-group-2 entfernt wird.

               asadmin> update-connector-work-security-map
               --raname my-resource-adapter --removegroups eis-group-2 workSecurityMap2
               Command update-connector-work-security-map executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-work-security-map(1),
       delete-connector-work-security-map(1),
       list-connector-work-security-maps(1)

       asadmin(1M)

Java EE 7                        10. Feb.update-connector-work-security-map(1)
