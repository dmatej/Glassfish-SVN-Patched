create-connector-resourcSubcomandos do utilitário create-connector-resource(1)

NOME
       create-connector-resource - registra o recurso do conector com o nome
       da JNDI especificado

SINOPSE
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

DESCRIçãO
       O subcomando create-connector-resource registra o recurso do conector
       com o nome da JNDI especificado.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --poolname
           O nome do pool de conexões. Quando dois ou mais elementos do
           recurso apontam para o mesmo elemento do pool de conexões, eles
           utilizam as mesmas conexões do pool durante o runtime.

       --enabled
           Esta opção determina se o recurso é ativado durante o runtime. O
           valor default é verdadeiro.

       --objecttype
           Define o tipo do recurso do conector. O default é usuário. Os
           valores permitidos são:

           system-all
               Um recurso do sistema para todas as instâncias do servidor e
               para o servidor de administração de domínios (DAS).

           system-admin
               Um recurso do sistema apenas para o DAS.

           system-instance
               Um recurso do sistema para todas as instâncias do servidor.

           user
               Um recurso do usuário.

       --description
           Texto fornecendo detalhes sobre o recurso do conector.

       --property
           Pares opcionais do valor do nome do atributo para configurar o
           recurso.

       --target
           Esta opção especifica a localização final dos recursos do conector.
           Os alvos válidos são:

           server
               Cria o recurso do conector na instância do servidor default.
               Este é o valor default.

           domain
               Cria o recurso do conector no domínio.

           cluster_name
               Cria o recurso do conector em cada instância do servidor no
               cluster.

           instance_name
               Cria o recurso do conector na instância do servidor
               especificada.

               Nota
               ┌───────────────────────────────────────┐
               │Observação O recurso sempre é criado   │
               │para o domínio como um todo, mas       │
               │resource-ref do recurso só é criado    │
               │para o --target especificado. Isso     │
               │significa que, embora o recurso esteja │
               │definido no nível do domínio, ele      │
               │somente está disponível no alvo        │
               │especificado. Utilize o subcomando     │
               │create-resource-ref para fazer         │
               │referência ao recurso em vários alvos, │
               │se necessário.                         │
               └───────────────────────────────────────┘

OPERANDOS
       jndi_name
           O nome da JNDI desse recurso conector.

EXEMPLOS
       Example 1, Criando um Recurso do Conector
           Este exemplo cria um recurso do conector com o nome
           jms/qConnFactory.

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       Example 2, Utilizando o subcomando create-connector-resource
           Este exemplo mostra o uso desse subcomando.

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           Nos casos em que jms/qConnFactory é a amostra do recurso do
           conector criado.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                   10 de fevereiro de 201create-connector-resource(1)
