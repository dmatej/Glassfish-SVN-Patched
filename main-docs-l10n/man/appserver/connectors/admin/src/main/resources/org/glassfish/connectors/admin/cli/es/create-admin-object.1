create-admin-object(1)  Subcomandos de la Utilidad asa  create-admin-object(1)

NOMBRE DE REFERENCIA
       create-admin-object - Agrega el objeto administrado con el nombre de
       JNDI especificado para un adaptador de recursos.

SINOPSIS
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

DESCRIPCIÓN
       El subcomando create-admin-objet crea el objeto administrado con el
       nombre de JNDI especificado y la definición de interfaz de un adaptador
       de recursos.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino en el que se va a crear el objeto
           administrado. Los valores válidos son los siguientes:

           server
               Crea el objeto administrado para la instancia de servidor por
               defecto server y es el valor por defecto.

           configuration_name
               Crea el objeto administrado para la configuración con nombre.

           cluster_name
               Crea el objeto administrado para todas las instancias de
               servidor del cluster.

           instance_name
               Crea el objeto administrado para una determinada instancia de
               servidor.

               Nota
               ┌────────────────────────────────────────┐
               │Nota El recurso se crea siempre para el │
               │dominio como un conjunto, pero el       │
               │elemento resource-ref del recurso sólo  │
               │se crea para la opción --target         │
               │especificada. Esto significa que,       │
               │aunque el recurso se define en el nivel │
               │de dominio, sólo está disponible en el  │
               │nivel de destino especificado. Utilice  │
               │el subcomando create-resource-ref para  │
               │hacer referencia al recurso en varios   │
               │destinos si es necesario.               │
               └────────────────────────────────────────┘

       --restype
           Especifica la definición de la interfaz para el objeto
           administrado. El tipo de recurso debe ser una definición de
           interfaz especificada en el archivo ra.xml del adaptador de
           recursos.

       --classname
           Especifica el nombre de clase del objeto administrado. Es necesario
           si varios objetos administrados utilizan la misma definición de
           interfaz.

       --raname
           Especifica el nombre del adaptador de recursos asociado a este
           objeto administrado.

       --enabled
           Especifica si el objeto está activado. El valor por defecto es
           true.

       --description
           Cadena de texto que describe el objeto administrado.

       --property
           Descripción de los pares de nombre/valor para la configuración del
           recurso. Depende del adaptador de recursos. Para las propiedades de
           JMS, consulte create-jms-resource(1) para recursos de destino de
           JMS.

OPERANDOS
       jndi_name
           Nombre de JNDI del objeto administrado que se va a crear.

EJEMPLOS
       Ejemplo 1, Creación de un Objeto Administrado
           En este ejemplo, jmsra es un adaptador de recursos del sistema con
           las interfaces de objeto de administración javax.jms.Queue y
           javax.jms.Topic.

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011      create-admin-object(1)
