list-custom-resources(1)    asadmin 实用程序子命令;   list-custom-resources(1)

名称
       list-custom-resources - 获取所有定制资源

提要
           list-custom-resources [--help] [target]

说明
       list-custom-resources 子命令列出定制资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           此操作数指定定制资源的位置。有效目标包括：

           server
               列出默认服务器实例上的资源。这是默认值

           domain
               列出域中的资源。

           cluster_name
               列出集群中每个服务器实例的资源。

           instance_name
               列出特定服务器实例的资源。

示例
       Example 1, 列出定制资源
           此示例列出当前的定制资源。

               asadmin> list-custom-resources
               sample_custom_resource01
               sample_custom_resource02
               Command list-custom-resources executed successfully.

       Example 2, 对目标使用 list-custom-resources 命令
           以下示例显示此命令的用法。

               asadmin> list-custom-resources --user admin --passwordfile
               passwords.txt --host plum --port 4848 target6

               sample_custom_resource03
               sample_custom_resource04
               Command list-custom-resources executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-custom-resource(1),delete-custom-resource(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日     list-custom-resources(1)
