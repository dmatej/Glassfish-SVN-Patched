create-connector-security-maasadmin 公用程式子create-connector-security-map(1)

參考名
       create-connector-security-map - 建立指定連線器連線集區的安全性對應

概要
           create-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--principals principal-name1[,principal-name2]*]
           [--usergroups user-group1[,user-group2]*]
           [--mappedusername user-name]
           [--target target]
           mapname

描述
       create-connector-security-map
       子命令建立指定連線器連線集區的安全性對應。如果該安全對應不存在，則會建立新的安全對應。此外，在容器管理式認證案例中，您可以使用這個子命令將應用程式的呼叫者身份
       (主體或使用者群組) 對應到適當的企業資訊系統 (EIS) 主體。EIS
       是指控管組織資料的任何系統。它可以是主機、訊息傳送系統、資料庫系統或應用程式。一或多個指定的安全對應可能會與連線器連線集區相關聯。連線器安全性對應組態支援使用萬用字元星號
       (*) 來表示所有使用者或所有使用者群組。

       若要指定 EIS 密碼，您可以先將 AS_ADMIN_MAPPEDPASSWORD
       項目新增至密碼檔案中，接著再使用 --passwordfile asadmin
       公用程式選項來指定檔案。

       您必須先使用 create-connector-connection-pool
       子命令建立一個連線器連線集區，才能成功執行這個子命令。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --poolname
           指定安全性對應所屬的連線器連線集區名稱。

       --principals
           指定後端 EIS 主體的清單。可以使用逗號分隔清單來指定多個主體。使用
           --principals 或是 --usergroups 選項，但不可在同一個命令中同時使用。

       --usergroups
           指定後端 EIS
           使用者群組的清單。可以使用逗號分隔清單來指定多個使用者群組。 使用
           --principals 或是 --usergroups 選項，但不可在同一個命令中同時使用。

       --mappedusername
           指定 EIS 使用者名稱。

       --target
           請勿指定此選項。保留這個選項是為了兼顧與舊版本的相容性。如果您指定此選項，並不會發生語法錯誤。相反地，子命令會順利執行，並顯示已忽略該選項的警告訊息。

運算元
       mapname
           要建立的安全性對應名稱。

範例
       範例 1, 建立連線器安全對應
           此範例會為名稱為 connector-pool1 的現有連線集區建立 securityMap1。

               asadmin> create-connector-security-map --poolname connector-pool1
               --principals principal1,principal2 --mappedusername backend-username securityMap1
               Command create-connector-security-map executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-connector-security-map(1), list-connector-security-maps(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22create-connector-security-map(1)
