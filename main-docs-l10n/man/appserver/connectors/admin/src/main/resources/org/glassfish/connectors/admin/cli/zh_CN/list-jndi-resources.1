list-jndi-resources(1)      asadmin 实用程序子命令      list-jndi-resources(1)

名称
       list-jndi-resources - 列出所有现有 JNDI 资源

提要
           list-jndi-resources [--help] [target]

说明
       list-jndi-resources 子命令标识所有现有 JNDI 资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           要列出 JNDI 资源的目标。有效值如下：

           server
               列出默认服务器实例的 JNDI 资源。这是默认值。

           configuration-name
               列出指定配置的 JNDI 资源。

           cluster-name
               列出指定集群中所有服务器实例的 JNDI 资源。

           instance-name
               列出指定服务器实例的 JNDI 资源。

示例
       Example 1, 列出 JNDI 资源
           此示例列出默认服务器实例上的 JNDI 资源。

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 21 日        list-jndi-resources(1)
