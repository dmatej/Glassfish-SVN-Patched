update-connector-security-asadmin 유틸리티 하위

제목
       update-connector-security-map - 지정된 커넥터 접속 풀에 대한 보안 맵
       수정

시놉시스
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

설명
       update-connector-security-map 하위 명령은 지정된 커넥터 접속 풀에 대한
       보안 맵을 수정합니다.

       이 하위 명령을 성공하려면 먼저 create-connector-connection-pool 하위
       명령을 사용하여 커넥터 접속 풀을 생성해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --poolname
           업데이트할 보안 맵이 속한 커넥터 접속 풀의 이름을 지정합니다.

       --addprincipals
           추가할 쉼표로 구분된 EIS 특정 주체 목록을 지정합니다.
           --addprincipals 또는 --addusergroups 옵션을 사용합니다. 단, 동일한
           명령에서 함께 사용하지 마십시오.

       --addusergroups
           추가할 쉼표로 구분된 EIS 사용자 그룹 목록을 지정합니다.
           --addprincipals 또는 --addusergroups 옵션을 사용합니다. 단, 동일한
           명령에서 함께 사용하지 마십시오.

       --removeprincipals
           제거할 쉼표로 구분된 EIS 특정 주체 목록을 지정합니다.

       --removeusergroups
           제거할 쉼표로 구분된 EIS 사용자 그룹 목록을 지정합니다.

       --mappedusername
           EIS 사용자 이름을 지정합니다.

피연산자
       mapname
           업데이트할 보안 맵의 이름입니다.

예제
       예 1, 커넥터 보안 맵 업데이트
           이 예제는 securityMap1이라는 기존 보안 맵에 주체를 추가합니다.

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                       2010년 9월 15일
