delete-jndi-resource(1)     asadmin 实用程序子命令     delete-jndi-resource(1)

名称
       delete-jndi-resource - 删除 JNDI 资源

提要
           delete-jndi-resource [--help] [--target target] jndi_name

说明
       delete-jndi-resource 子命令删除指定的 JNDI
       资源。在运行此子命令之前，必须删除相应 JNDI 资源的所有关联项。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           有效目标如下所述。

               注意
               ┌─────────────────────────────────────────────────┐
               │注                                               │
               │始终为整个域创建资源，但资源仅在创建资源时使用   │
               │--target 选项为其创建 <resource-ref>             │
               │的目标上处于活动状态。这意味着删除资源仅删除指定 │
               │--target 中的 <resource-ref>                     │
               │元素，并不会从整个域中删除资源，除非将           │
               │domain 指定为要删除的 --target。                 │
               └─────────────────────────────────────────────────┘

           server
               从默认服务器实例删除资源。这是默认值

           domain
               从域删除资源

           cluster_name
               删除集群中每个服务器实例的资源

           instance_name
               从指定服务器实例删除资源

操作数
       jndi_name
           要删除的 JNDI 资源的名称。

示例
       Example 1, 删除 JNDI 资源
           此示例删除名为 sample_jndi_resource 的现有 JNDI 资源。

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-jndi-resource(1)
