create-jndi-resource(1)     asadmin 实用程序子命令     create-jndi-resource(1)

名称
       create-jndi-resource - 注册 JNDI 资源

提要
           create-jndi-resource [--help] [--target target]
           --restype restype --factoryclass factoryclass
           --jndilookupname jndilookupname [--enabled={true|false}]
           [--description description]
           [--property (name=value)[:name=value]*]
           jndi-name

说明
       create-jndi-resource 子命令注册 JNDI 资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           该选项指定要注册 JNDI 资源的目标。目标的有效值如下所述。

               注意
               ┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注                                                                                               │
               │始终作为一个整体为域创建资源，但仅为指定的                                                       │
               │--target 创建资源的                                                                              │
               │resource-ref。这意味着，虽然资源是在域级别定义的，但它仅在指定的目标级别中可用。如果需要，可使用 │
               │create-resource-ref                                                                              │
               │子命令在多个目标中引用资源。                                                                     │
               └─────────────────────────────────────────────────────────────────────────────────────────────────┘

           server
               为默认服务器实例创建资源。该值为默认值。

           domain
               为域创建资源

           cluster-name
               为集群中的每个服务器实例创建资源

           instance-name
               为特定服务器实例创建资源

       --restype
           JNDI 资源类型。有效值为 topic 或 queue。

       --factoryclass
           创建 JNDI 资源的类。

       --jndilookupname
           外部容器使用的查找名称。

       --enabled
           确定资源在运行时是否处于启用状态。默认值为“真”。

       --description
           提供 JNDI 资源详细信息的文本。

       --property
           用于配置资源的可选属性。将每个属性指定为一个名称/值对。

           可用属性特定于 --factoryclass
           选项指定的实现且由该实现使用。GlassFish Server 本身没有定义用于配置
           JNDI 资源的任何属性。

操作数
       jndi-name
           要创建的 JNDI 资源的唯一名称。

示例
       Example 1, 创建 JNDI 资源
           此示例为默认服务器实例创建 JNDI 资源 my-jndi-resource。

               asadmin> create-jndi-resource
               --restype com.example.jndi.MyResourceType
               --factoryclass com.example.jndi.MyInitialContextFactoryClass
               --jndilookupname remote-jndi-name
               --description "sample JNDI resource" my-jndi-resource
               JNDI resource my-jndi-resource created.
               Command create-jndi-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-jndi-resource(1), list-jndi-resources(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 21 日       create-jndi-resource(1)
