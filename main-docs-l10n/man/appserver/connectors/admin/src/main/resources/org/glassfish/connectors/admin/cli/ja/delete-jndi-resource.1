delete-jndi-resource(1asadminユーティリティのサブコマンdelete-jndi-resource(1)

名前
       delete-jndi-resource - JNDIリソースを除去します

概要
           delete-jndi-resource [--help] [--target target] jndi_name

説明
       delete-jndi-resourceサブコマンドは、指定したJNDIリソースを除去します。このサブコマンドを実行する前に、JNDIリソースへのアソシエーションをすべて除去する必要があります。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           有効なターゲットは次のとおりです。

               注記
               ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                                                                           │
               │リソースは常にドメイン全体に対して作成されますが、リソースがアクティブになるのは、リソースの作成時に--targetオプションを使用して<resource-ref>が作成されたターゲットに対してのみです。つまり、リソースの削除では、指定した--targetの<resource-ref>要素のみが削除され、削除対象の--targetとしてdomainを指定しないかぎり、ドメイン全体からリソースが削除されることはありません。 │
               └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

           server
               デフォルトのサーバー・インスタンスからリソースを削除します。この値がデフォルト値です。

           domain
               ドメインからリソースを削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのリソースを削除します。

           instance_name
               指定したサーバー・インスタンスからリソースを削除します。

オペランド
       jndi_name
           除去されるJNDIリソースの名前。

例
       例 1, JNDIリソースの削除
           この例では、sample_jndi_resourceという名前の既存のJNDIリソースを除去します。

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                        2011年2月11日         delete-jndi-resource(1)
