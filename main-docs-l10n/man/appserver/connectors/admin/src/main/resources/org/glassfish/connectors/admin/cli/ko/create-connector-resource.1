create-connector-resource(asadmin 유틸리티 하위 명create-connector-resource(1)

제목
       create-connector-resource - 지정된 JNDI 이름으로 커넥터 리소스 등록

시놉시스
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

설명
       create-connector-resource 하위 명령은 JNDI 이름이 지정된 커넥터
       리소스를 등록합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --poolname
           접속 풀의 이름입니다. 둘 이상의 리소스 요소가 동일한 접속 풀 요소를
           가리키는 경우 해당 요소들은 런타임 시 동일한 풀 접속을 사용합니다.

       --enabled
           이 옵션은 리소스가 런타임 시 사용으로 설정되어 있는지를 확인합니다.
           기본값은 true입니다.

       --objecttype
           커넥터 리소스의 유형을 정의합니다. 기본값은 user입니다. 허용되는
           값은 다음과 같습니다.

           system-all
               모든 서버 인스턴스 및 DAS(도메인 관리 서버)에 대한 시스템
               리소스입니다.

           system-admin
               DAS 전용 시스템 리소스입니다.

           system-instance
               모든 서버 인스턴스 전용 시스템 리소스입니다.

           user
               사용자 리소스입니다.

       --description
           커넥터 리소스에 대한 세부 정보를 제공하는 텍스트입니다.

       --property
           리소스를 구성하기 위한 선택적 속성 이름/값 쌍입니다.

       --target
           이 옵션은 커넥터 리소스의 종료 위치를 지정합니다. 적합한 대상은
           다음과 같습니다.

           server
               기본 서버 인스턴스에 커넥터 리소스를 생성합니다. 이 값은
               기본값입니다.

           domain
               도메인에 커넥터 리소스를 생성합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 커넥터 리소스를
               생성합니다.

           instance_name
               지정된 서버 인스턴스에 커넥터 리소스를 생성합니다.

               참고
               ┌───────────────────────────────────────┐
               │주 리소스는 항상 도메인에 대해 전체로  │
               │생성되지만 리소스의 resource-ref는     │
               │지정한 --target에 대해서만 생성됩니다. │
               │즉, 리소스가 도메인 레벨에서           │
               │정의되더라도 지정된 대상 레벨에서만    │
               │사용할 수 있습니다. 필요한 경우 여러   │
               │대상에서 리소스를 참조하려면           │
               │create-resource-ref 하위 명령을        │
               │사용합니다.                            │
               └───────────────────────────────────────┘

피연산자
       jndi_name
           이 커넥터 리소스의 JNDI 이름입니다.

예제
       예 1, 커넥터 리소스 생성
           이 예제는 jms/qConnFactory라는 커넥터 리소스를 생성합니다.

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       예 2, create-connector-resource 하위 명령 사용
           이 예제는 이 하위 명령의 사용법을 보여줍니다.

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           여기서 jms/qConnFactory는 생성된 샘플 커넥터 리소스입니다.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                       2011년 2월 10일   create-connector-resource(1)
