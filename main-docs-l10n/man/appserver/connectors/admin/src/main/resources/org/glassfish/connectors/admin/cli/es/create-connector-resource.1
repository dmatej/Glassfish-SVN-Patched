create-connector-resourcSubcomandos de la Utilidadcreate-connector-resource(1)

NOMBRE DE REFERENCIA
       create-connector-resource - Registra el recurso de conector con el
       nombre de JNDI especificado.

SINOPSIS
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

DESCRIPCIÓN
       El subcomando create-connector-resource registra el recurso de conector
       con el nombre de JNDI especificado.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --poolname
           El nombre del pool de conexiones. Si dos o más elementos de recurso
           apuntan al mismo elemento de pool de conexiones, utilizan las
           mismas conexiones del pool en tiempo de ejecución.

       --enabled
           Determina si el recurso está activado en tiempo de ejecución. El
           valor por defecto es true.

       --objecttype
           Define el tipo de recurso de conector. El valor por defecto es
           user. Los valores permitidos son:

           system-all
               Recurso del sistema para todas las instancias de servidor y
               servidor de administración de dominio (DAS).

           system-admin
               Recurso del sistema sólo para el DAS.

           system-instance
               Recurso del sistema sólo para todas las instancias del
               servidor.

           user
               Recurso de usuario.

       --description
           Texto que proporciona detalles sobre el recurso de conector.

       --property
           Pares de nombre y valor de atributo opcionales para configurar el
           recurso.

       --target
           Esta opción especifica la ubicación final de los recursos de
           conector. Los destinos válidos son:

           server
               Crea el recurso de conector en la instancia de servidor por
               defecto. Este es el valor por defecto.

           domain
               Crea el recurso de conector en el dominio.

           cluster_name
               Crea el recurso de conector en cada instancia de servidor del
               cluster.

           instance_name
               Crea el recurso de conector en la instancia de servidor
               especificada.

               Nota
               ┌────────────────────────────────────────┐
               │Nota El recurso se crea siempre para el │
               │dominio como un conjunto, pero el       │
               │elemento resource-ref del recurso sólo  │
               │se crea para la opción --target         │
               │especificada. Esto significa que,       │
               │aunque el recurso se define en el nivel │
               │de dominio, sólo está disponible en el  │
               │nivel de destino especificado. Utilice  │
               │el subcomando create-resource-ref para  │
               │hacer referencia al recurso en varios   │
               │destinos si es necesario.               │
               └────────────────────────────────────────┘

OPERANDOS
       jndi_name
           El nombre de JNDI de este recurso de conector.

EJEMPLOS
       Ejemplo 1, Creación de un Recurso de Conector
           En este ejemplo se crea un recurso de conector denominado
           jms/qConnFactory.

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       Ejemplo 2, Uso del Subcomando create-connector-resource
           Este ejemplo muestra el uso de este subcomando.

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           Donde jms/qConnFactory es el recurso de conector de ejemplo que se
           va a crear.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011create-connector-resource(1)
