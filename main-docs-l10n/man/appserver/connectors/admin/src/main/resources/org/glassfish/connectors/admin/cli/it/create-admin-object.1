create-admin-object(1)  Comandi secondari della utilit  create-admin-object(1)

NOME
       create-admin-object - aggiunge l'oggetto amministrato con il nome JNDI
       specificato per un Resource Adapter

SINTASSI
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

DESCRIZIONE
       Il comando secondario create-admin-object crea l'oggetto amministrato
       con il nome JNDI specificato e la definizione di interfaccia per un
       Resource Adapter.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione in cui viene creato l'oggetto
           amministrato. Di seguito sono riportati i valori validi.

           server
               Crea l'oggetto amministrato per l'istanza server predefinita
               server ed è il valore predefinito.

           configuration_name
               Crea l'oggetto amministrato per la configurazione specificata.

           cluster_name
               Crea l'oggetto amministrato per tutte le istanze server del
               cluster.

           instance_name
               Crea l'oggetto amministrato per un'istanza server particolare.

               Nota
               ┌────────────────────────────────────────┐
               │Nota La risorsa viene sempre creata per │
               │l'intero dominio, mentre l'elemento     │
               │resource-ref per la risorsa viene       │
               │creato solo per l'elemento --target     │
               │specificato. Ciò significa che, benché  │
               │sia definita a livello di dominio, la   │
               │risorsa è disponibile solo per il       │
               │livello destinazione specificato. Se    │
               │necessario, utilizzare il comando       │
               │secondario create-resource-ref per fare │
               │riferimento alla risorsa in più livelli │
               │target.                                 │
               └────────────────────────────────────────┘

       --restype
           Specifica la definizione di interfaccia per l'oggetto amministrato.
           Il tipo di risorsa deve essere una definizione di interfaccia
           specificata nel file
            ra.xml del Resource Adapter.

       --classname
           Specifica il nome della classe dell'oggetto amministrato.
           Obbligatoria se più oggetti amministrati utilizzano la stessa
           definizione di interfaccia.

       --raname
           Specifica il nome del Resource Adapter associato all'oggetto
           amministrato.

       --enabled
           Specifica se l'oggetto è abilitato. Il valore predefinito è true.

       --description
           La stringa di testo che descrive l'oggetto amministrato.

       --property
           La descrizione delle coppie nome-valore per la configurazione della
           risorsa. Dipende dal Resource Adapter. Per le proprietà JMS, vedere
           create-jms-resource(1) per le risorse di destinazione JMS.

OPERANDI
       jndi_name
           Il nome JNDI dell'oggetto amministrato da creare.

ESEMPI
       Esempio 1, Creazione di un oggetto amministrato
           In questo esempio jmsra è un Resource Adapter di sistema con le
           interfacce javax.jms.Queue e
            javax.jms.Topic dell'oggetto amministrato.

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                      10 febbraio 2011         create-admin-object(1)
