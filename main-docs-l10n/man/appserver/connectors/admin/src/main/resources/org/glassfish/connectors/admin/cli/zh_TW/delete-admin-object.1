delete-admin-object(1)      asadmin 公用程式子命令      delete-admin-object(1)

參考名
       delete-admin-object - 移除具有指定 JNDI 名稱的管理物件。

概要
           delete-admin-object [--help] [--target target] jndi_name

描述
       delete-admin-object 子命令會移除具有指定 JNDI 名稱的管理物件。

       只有遠端模式支援此子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此為要刪除管理物件的目標名稱。有效值包括

               注意
               ┌─────────────────────────────────────────────────────┐
               │備註                                                 │
               │資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
               │--target 選項建立 <resource-ref>                     │
               │的目標。這表示刪除資源只會刪除指定                   │
               │--target 的 <resource-ref>                           │
               │元素，而不會刪除完整網域的資源，除非已將             │
               │domain 指定為要刪除的 --target。                     │
               └─────────────────────────────────────────────────────┘

           server
               刪除預設伺服器執行處理 server 的管理物件，且此預設值。

           configuration_name
               刪除指定組態的管理物件。

           cluster_name
               刪除指定叢集的管理物件。

           instance_name
               刪除特定伺服器執行處理的管理物件。

運算元
       jndi_name
           要刪除的管理物件的 JNDI 名稱。

範例
       範例 1, 刪除管理物件
           此範例會刪除名稱為 jms/samplelqueue 的管理物件。

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 14 日        delete-admin-object(1)
