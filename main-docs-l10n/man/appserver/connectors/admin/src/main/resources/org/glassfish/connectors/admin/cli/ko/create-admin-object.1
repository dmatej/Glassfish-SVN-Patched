create-admin-object(1)    asadmin 유틸리티 하위 명령    create-admin-object(1)

제목
       create-admin-object - 리소스 어댑터에 대해 지정된 JNDI 이름으로
       관리되는 객체 추가

시놉시스
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

설명
       create-admin-object 하위 명령은 리소스 어댑터에 대해 지정된 JNDI 이름
       및 인터페이스 정의로 관리되는 객체를 생성합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           관리되는 객체를 생성할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스 server에 대한 관리되는 객체를 생성하고 이는
               기본값입니다.

           configuration_name
               이름이 지정된 구성에 대한 관리되는 객체를 생성합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 관리되는 객체를
               생성합니다.

           instance_name
               특정 서버 인스턴스에 대한 관리되는 객체를 생성합니다.

               참고
               ┌───────────────────────────────────────┐
               │주 리소스는 항상 도메인에 대해 전체로  │
               │생성되지만 리소스의 resource-ref는     │
               │지정한 --target에 대해서만 생성됩니다. │
               │즉, 리소스가 도메인 레벨에서           │
               │정의되더라도 지정된 대상 레벨에서만    │
               │사용할 수 있습니다. 필요한 경우 여러   │
               │대상에서 리소스를 참조하려면           │
               │create-resource-ref 하위 명령을        │
               │사용합니다.                            │
               └───────────────────────────────────────┘

       --restype
           관리되는 객체에 대한 인터페이스 정의를 지정합니다. 리소스 유형은
           리소스 어댑터의 ra.xml 파일에 지정되어 있는 인터페이스 정의여야
           합니다.

       --classname
           관리되는 객체의 클래스 이름을 지정합니다. 여러 관리되는 객체가
           동일한 인터페이스 정의를 사용하는 경우 필수입니다.

       --raname
           이 관리되는 객체와 연관된 리소스 어댑터의 이름을 지정합니다.

       --enabled
           이 옵션이 사용으로 설정되어 있는 경우 지정합니다. 기본값은
           true입니다.

       --description
           관리되는 객체를 설명하는 텍스트 문자열입니다.

       --property
           리소스를 구성하기 위한 이름/값 쌍의 설명입니다. 리소스 어댑터에
           종속됩니다. JMS 속성에 대해서는 JMS 정의 리소스에 대한
           create-jms-resource(1)를 참조하십시오.

피연산자
       jndi_name
           생성할 관리되는 객체의 JNDI 이름입니다.

예제
       예 1, 관리되는 객체 생성
           이 예제에서 jmsra는 admin 객체 인터페이스, javax.jms.Queue 및
           javax.jms.Topic이 있는 시스템 리소스 어댑터입니다.

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                       2011년 2월 10일         create-admin-object(1)
