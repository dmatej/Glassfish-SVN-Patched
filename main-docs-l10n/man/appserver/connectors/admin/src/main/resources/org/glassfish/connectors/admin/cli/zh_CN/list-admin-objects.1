list-admin-objects(1)       asadmin 实用程序子命令       list-admin-objects(1)

名称
       list-admin-objects - 获取所有受管理对象

提要
           list-admin-objects [--help] [target]

说明
       list-admin-objects 子命令列出所有受管理对象。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           要列出受管理对象的目标。有效值如下：

           server
               列出默认服务器实例上的受管理对象。这是默认值。

           configuration-name
               列出指定配置中的受管理对象。

           cluster-name
               列出指定集群中所有服务器实例上的受管理对象。

           instance-name
               列出指定服务器实例上的受管理对象。

示例
       Example 1, 列出受管理对象
           此示例列出所有受管理对象。

               asadmin> list-admin-objects
               jms/samplequeue
               jms/anotherqueue
               Command list-admin-objects executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-admin-object(1), delete-admin-object(1)

       asadmin(1M)

Java EE 7                     2011 年 6 月 21 日         list-admin-objects(1)
