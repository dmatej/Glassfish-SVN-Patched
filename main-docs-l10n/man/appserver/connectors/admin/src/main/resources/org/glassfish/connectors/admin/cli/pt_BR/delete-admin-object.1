delete-admin-object(1)  Subcomandos do utilitário asad  delete-admin-object(1)

NOME
       delete-admin-object - remove o objeto administrado com o nome da JNDI
       especificado

SINOPSE
           delete-admin-object [--help] [--target target] jndi_name

DESCRIçãO
       O subcomando delete-admin-object remove um objeto administrado com o
       nome da JNDI especificado.

       Este subcomando é suportado só no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Este é o nome dos alvo para qual o objeto administrado deve ser
           deletado. Os valores válidos são:

               Nota
               ┌────────────────────────────────────────┐
               │Observação Os recursos sempre são       │
               │criados para um domínio como um todo,   │
               │mas só estarão ativos para alvos para   │
               │os quais <resource-ref> tiver sido      │
               │criado usando a opção --target durante  │
               │a criação do recurso. Isso significa    │
               │que a deleção de um recurso só deleta o │
               │elemento <resource-ref> para o --target │
               │especificado e não deleta o recurso do  │
               │domínio como um todo, a não ser que     │
               │domain seja especificado como o         │
               │--target para a deleção.                │
               └────────────────────────────────────────┘

           server
               Deleta o objeto administrado para a instância do servidor
               default server e seu valor default.

           configuration_name
               Deleta o objeto administrado para a configuração especificada.

           cluster_name
               Deleta o objeto administrado para o cluster especificado.

           instance_name
               Deleta o objeto administrado para uma instância do servidor
               específica.

OPERANDOS
       jndi_name
           Nome da JNDI do objeto administrado a ser deletado.

EXEMPLOS
       Example 1, Deletando um Objeto Administrado
           Este exemplo deleta o objeto administrado com o nome
           jms/samplelqueue.

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                         14 set 2010           delete-admin-object(1)
