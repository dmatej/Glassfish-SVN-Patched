create-custom-resource(1Sous-commandes de l'utilitaircreate-custom-resource(1)

NOM
       create-custom-resource - crée une ressource personnalisée

SYNTAXE
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

DESCRIPTION
       La sous-commande create-custom-resource crée une ressource
       personnalisée. Une ressource personnalisée indique une fabrique
       personnalisée d'objets de ressource au niveau du serveur qui implémente
       l'interface javax.naming.spi.ObjectFactory.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cible sur laquelle la ressource personnalisée que vous créez sera
           disponible. Les valeurs valides sont les suivantes :

           server
               La ressource sera disponible sur l'instance de serveur par
               défaut et tous les domaines hébergés par l'instance. Il s'agit
               de la valeur par défaut.

           domain
               La ressource sera disponible sur le domaine indiqué uniquement.

           cluster_name
               La ressource sera disponible sur chaque instance de serveur
               dans le cluster.

           instance_name
               La ressource sera disponible sur l'instance de serveur indiquée
               uniquement.

               Note
               ┌────────────────────────────────────────┐
               │Remarque La ressource est toujours      │
               │créée pour le domaine dans son          │
               │ensemble, mais l'élément resource-ref   │
               │de la ressource est créé uniquement     │
               │pour la valeur --target indiquée. De ce │
               │fait, bien que la ressource soit        │
               │définie au niveau du domaine, elle      │
               │n'est disponible que sur la cible       │
               │indiquée. Utilisez la sous-commande     │
               │create-resource-ref pour faire          │
               │référence à la ressource dans plusieurs │
               │cibles si nécessaire.                   │
               └────────────────────────────────────────┘

       --restype
           Type de ressource personnalisée à créer. Indiquez une définition de
           type qualifié complet (par exemple javax.naming.spi.ObjectFactory).
           La définition de type de ressource est au format xxx.xxx.

       --factoryclass
           Nom de classe de fabrique de la ressource personnalisée. Cette
           classe implémente l'interface javax.naming.spi.ObjectFactory.

       --enabled
           Cette option détermine si la ressource personnalisée est activée
           durant l'exécution. La valeur par défaut est True.

       --description
           Texte décrivant la ressource personnalisée. Cette description doit
           se présenter sous la forme d'une chaîne pouvant inclure un maximum
           de 250 caractères.

       --property
           Paires nom/valeur d'attribut facultatives pour la configuration de
           la ressource.

OPÉRANDES
       jndi-name
           Nom JNDI de cette ressource.

EXEMPLES
       Exemple 1, Création d'une ressource personnalisée
           Cet exemple montre comment créer une ressource personnalisée.

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                         11 fév 2011        create-custom-resource(1)
