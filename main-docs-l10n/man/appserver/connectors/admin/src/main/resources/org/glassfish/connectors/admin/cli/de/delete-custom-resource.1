delete-custom-resource(1Unterbefehle des asadmin-Utildelete-custom-resource(1)

NAME
       delete-custom-resource - entfernt eine benutzerdefinierte Ressource

ZUSAMMENFASSUNG
           delete-custom-resource [--help] [--target target] jndi-name

BESCHREIBUNG
       Der Unterbefehl delete-custom-resource entfernt eine benutzerdefinierte
       Ressource.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option hilft bei der Angabe des Verzeichnisses der
           benutzerdefinierten Ressourcen, die von Ihnen gelöscht werden.
           Gültige Ziele sind "server", "domain", "cluster" und "instance".
           Der Standardwert ist "server".

               Note
               ┌────────────────────────────────────────┐
               │Hinweis Ressourcen werden stets für     │
               │eine Domain als Ganzes erstellt, sind   │
               │aber nur für Ziele aktiv, für die bei   │
               │der Erstellung der Ressource eine       │
               │<resource-ref> mit der Option --target  │
               │erstellt wurde. Das bedeutet, dass beim │
               │Löschen einer Ressource nur das         │
               │<resource-ref>-Element für das          │
               │angegebene Ziel (--target) und nicht    │
               │die Ressource aus der Domain als Ganzes │
               │gelöscht wird, es sei denn, domain ist  │
               │als --target für den Löschvorgang       │
               │angegeben.                              │
               └────────────────────────────────────────┘

           server
               Löscht die Ressource für die standardmäßige Serverinstanz. Dies
               ist der Standardwert.

           domain
               Löscht die Ressource für die Domain.

           cluster_name
               Löscht die Ressource für jede Serverinstanz im Cluster.

           instance_name
               Löscht die Ressource für eine bestimmte Serverinstanz.

OPERANDEN
       jndi-name
           Der JNDI-Name dieser Ressource.

BEISPIELE
       Example 1, Benutzerdefinierte Ressourcen löschen
           In diesem Beispiel wird eine benutzerdefinierte Ressource namens
           mycustomresource gelöscht.

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010       delete-custom-resource(1)
