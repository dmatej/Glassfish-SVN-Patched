update-connector-security-maasadmin 实用程序子update-connector-security-map(1)

名称
       update-connector-security-map - 修改指定的连接器连接池的安全映射

提要
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

说明
       update-connector-security-map 子命令为指定的连接器连接池修改安全映射。

       要成功执行此子命令，必须首先使用 create-connector-connection-pool
       子命令创建一个连接器连接池。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --poolname
           指定要更新的安全映射所属的连接器连接池的名称。

       --addprincipals
           指定要添加的以逗号分隔的特定于 EIS 的主用户列表。使用
           --addprincipals 或 --addusergroups
           选项，但不能在同一个命令中同时使用。

       --addusergroups
           指定要添加的以逗号分隔的 EIS 用户组列表。使用 --addprincipals 或
           --addusergroups 选项，但不能在同一个命令中同时使用。

       --removeprincipals
           指定要删除的以逗号分隔的特定于 EIS 的主用户列表。

       --removeusergroups
           指定要删除的以逗号分隔的 EIS 用户组列表。

       --mappedusername
           指定 EIS 用户名。

操作数
       mapname
           要更新的安全映射的名称。

示例
       Example 1, 更新连接器安全映射
           此示例将主用户添加到名为 securityMap1 的现有安全映射中。

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 15 update-connector-security-map(1)
