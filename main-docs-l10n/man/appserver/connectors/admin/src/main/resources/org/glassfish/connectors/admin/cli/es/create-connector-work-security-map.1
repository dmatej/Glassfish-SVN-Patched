create-connector-work-seSubcomandos1de lacreate-connector-work-security-map(1)

NOMBRE DE REFERENCIA
       create-connector-work-security-map - Crea un mapa de seguridad de
       trabajo para el adaptador de recursos especificado.

SINOPSIS
           create-connector-work-security-map [--help] --raname raname
           [--principalsmap eis-principal1=principal_name1[, eis-principal2=principal_name2]*
           |--groupsmap eis-group1=server-group1[, eis-group2=server-group2]*}
           [--description description]
           mapname

DESCRIPCIÓN
       El subcomando create-connector-work-security-map asigna la identidad
       del emisor de llamada del trabajo ejecutado por el principal o un grupo
       de usuarios del EIS del adaptador de recursos a un principal o grupo de
       usuarios adecuado en el dominio de seguridad de GlassFish Server. Se
       puede asociar uno o más mapas de seguridad de trabajo a un adaptador de
       recursos. La configuración del mapa de seguridad de trabajo de conector
       soporta el uso del carácter asterisco como comodín (*) para hacer
       referencia a todos los usuarios o todos los grupos de usuarios.

       El sistema de información empresarial (EIS) es cualquier sistema que
       almacene los datos de una organización. Puede ser un mainframe, un
       sistema de mensajería, un sistema de base de datos o una aplicación.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --description
           Texto descriptivo que proporciona información detallada sobre el
           mapa de seguridad de trabajo del conector.

       --groupsmap
           Especifica un mapa del grupo de usuarios de EIS de backend para el
           grupo de usuarios de GlassFish Server. Utilice una lista separada
           por comas para especificar más de una asignación. Utilice la opción
           --principalsmap o --groupsmap, pero no ambas.

       --principalsmap
           Especifica un mapa del principal de EIS de backend para el
           principal de GlassFish Server. Utilice una lista separada por comas
           para especificar más de una asignación. Utilice la opción
           --principalsmap o --groupsmap, pero no ambas.

       --raname
           Indica el nombre del módulo de conector, que es el nombre del
           adaptador de recursos.

OPERANDOS
       mapname
           Nombre del mapa de seguridad de trabajo que se va a crear.

EJEMPLOS
       Ejemplo 1, Creación de un Mapa de Seguridad de Trabajo de Conector
       (Principal)
           En este ejemplo se crea el mapa de seguridad de trabajo de conector
           workSecurityMap1 que asigna el principal del EIS de backend al
           principal de GlassFish Server.

               asadmin create-connector-work-security-map --raname my-resource-adapter
               --principalsmap eis-principal-1=server-principal-1,eis-principal-2
               =server-principal-2,eis-principal-3=server-principal-1
               workSecurityMap1
               Command create-connector-work-security-map executed successfully.

       Ejemplo 2, Creación de un mapa de seguridad de trabajo de conector
       (grupo)
           En este ejemplo se crea el mapa de seguridad de trabajo de conector
           workSecurityMap2 que asigna el grupo de usuarios del EIS de backend
           al grupo de usuarios de GlassFish Server.

               asadmin create-connector-work-security-map --raname my-resource-adapter
               --groupsmap eis-group-1=server-group-1,eis-group-2=server-group-2,
               eis-group-3=server-group-1 workSecurityMap2
               Command create-connector-work-security-map executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-connector-work-security-map(1),
       list-connector-work-security-maps(1),
       update-connector-work-security-map(1)

       asadmin(1M)

Java EE 7                    11 de febrercreate-connector-work-security-map(1)
