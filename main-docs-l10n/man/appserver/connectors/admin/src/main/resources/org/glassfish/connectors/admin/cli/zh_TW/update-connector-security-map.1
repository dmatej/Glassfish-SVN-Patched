update-connector-security-maasadmin 公用程式子update-connector-security-map(1)

參考名
       update-connector-security-map - 可修改指定之連線器連線集區的安全性對應

概要
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

描述
       update-connector-security-map
       子命令可修改指定連線器連線集區的安全性對應。

       您必須先使用 create-connector-connection-pool
       子命令建立一個連線器連線集區，才能成功執行這個子命令。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --poolname
           指定要更新之安全性對應所屬的連線器連線集區名稱。

       --addprincipals
           指定要增加的 EIS 特定主體逗號分隔清單。請使用 --addprincipals 或
           --addusergroups 選項，但不要在同一命令中同時使用。

       --addusergroups
           指定要增加的 EIS 使用者群組逗號分隔清單。請使用 --addprincipals 或
           --addusergroups 選項，但不要在同一命令中同時使用。

       --removeprincipals
           指定要移除之 EIS 特定主體的逗號分隔清單。

       --removeusergroups
           指定要移除之 EIS 使用者群組的逗號分隔清單。

       --mappedusername
           指定 EIS 使用者名稱。

運算元
       mapname
           要更新的安全性對應名稱。

範例
       範例 1, 更新連線器安全性對應
           此範例會將主體增加到名稱為 securityMap1 的現有安全性對應。

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 15 update-connector-security-map(1)
