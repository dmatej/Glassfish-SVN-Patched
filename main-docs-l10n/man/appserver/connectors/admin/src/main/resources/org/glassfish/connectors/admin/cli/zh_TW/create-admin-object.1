create-admin-object(1)      asadmin 公用程式子命令      create-admin-object(1)

參考名
       create-admin-object - 以指定 JNDI 名稱，為資源轉接器新增管理物件

概要
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

描述
       create-admin-object 子命令為資源轉接器建立具有特定 JNDI
       名稱和介面定義的受管理物件。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要用來建立受管理物件的目標。有效的值如下：

           server
               為預設的伺服器執行處理 server 建立受管理物件，且為預設值。

           configuration_name
               為已命名的組態建立受管理物件。

           cluster_name
               為叢集中每個伺服器執行處理建立受管理物件。

           instance_name
               為特定的伺服器執行處理建立受管理物件。

               注意
               ┌───────────────────────────────────────────────────────────────────────────────────────┐
               │備註                                                                                   │
               │資源皆是為整個網域所建立，但只會為指定的                                               │
               │--target 建立資源的                                                                    │
               │resource-ref。這表示雖然資源定義於網域層級，但只能用於所指定的目標層級。請視需要，使用 │
               │create-resource-ref                                                                    │
               │子命令來參照多個目標中的資源。                                                         │
               └───────────────────────────────────────────────────────────────────────────────────────┘

       --restype
           為受管理物件指定介面定義。資源類型必須是資源轉接器的 ra.xml
           檔案所指定的一個介面定義。

       --classname
           指定受管理物件的類別名稱。如果多個受管理物件都使用相同的介面定義，則此項目為必要。

       --raname
           指定與此受管理物件相關聯的資源配接器名稱。

       --enabled
           指定此物件是否已啟用。預設值為 true。

       --description
           描述受管理物件的文字字串。

       --property
           用來組態資源的名/值配對描述。依賴資源轉接器。如需 JMS
           特性的相關資訊，請參閱 JMS 目的地資源的 create-jms-resource(1)。

運算元
       jndi_name
           要建立之受管理物件的 JNDI 名稱。

範例
       範例 1, 建立受管理物件
           在此範例中，jmsra 是一個具有管理物件介面 javax.jms.Queue、
           javax.jms.Topic 的系統資源轉接器。

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 10 日        create-admin-object(1)
