create-connector-connectComandilsecondari dcreate-connector-connection-pool(1)

NOME
       create-connector-connection-pool - aggiunge il connection pool con il
       nome specificato

SINTASSI
           create-connector-connection-pool [--help] [--target=target]
           --raname raname
           --connectiondefinition connectiondefinitionname
           [--steadypoolsize steadypoolsize]
           [--maxpoolsize maxpoolsize]
           [--maxwait maxwait]
           [--poolresize poolresize]
           [--idletimeout idletimeout]
           [--isconnectvalidatereq={false|true}]
           [--failconnection={false|true}]
           [--leaktimeout=timeout]
           [--leakreclaim={false|true}]
           [--creationretryattempts=attempts]
           [--creationretryinterval=interval]
           [--lazyconnectionenlistment={false|true}]
           [--lazyconnectionassociation={false|true}]
           [--associatewiththread={false|true}]
           [--matchconnections={true|false}]
           [--maxconnectionusagecount=count]
           [--validateatmostonceperiod=interval]
           [--transactionsupport transactionsupport]
           [--descrip[tion description]
           [--ping {false|true}]
           [--pooling {true|false}]
           [--property (name=value)[:name=value]*]
           poolname

DESCRIZIONE
       Il comando secondario create-connector-connection-pool definisce un
       connection pool in un sistema EIS (Enterprise Information System). Il
       pool denominato può essere utilizzato come riferimento da più risorse
       connettore. All'avvio del server viene creata un'istanza di ogni pool
       definito, che viene popolato al momento del primo accesso. Se due o più
       risorse connettore puntano allo stesso connection pool del connettore,
       utilizzano lo stesso connection pool in fase di esecuzione. In un
       singolo Resource Adapter possono esistere più pool per una definizione
       di connessione.

       Per creare un connection pool del connettore con autenticazione, è
       possibile utilizzare l'opzione --property per specificare l'utente, la
       password o altre informazioni di connessione oppure specificare le
       informazioni di connessione in file descrittore XML.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --associatewiththread
           Specifica se al thread è associata una connessione per consentire
           al thread di riutilizzarla. Se al thread non è associata una
           connessione, il thread deve ottenerne una dal pool ogni volta che è
           necessario. Di seguito sono riportati i valori possibili.

           false
               Al thread non è associata una connessione (impostazione
               predefinita).

           true
               Al thread è associata una connessione.

       --connectiondefinition
           Il nome della definizione di connessione.

       --creationretryattempts
           Specifica il numero massimo di nuovi tentativi di creazione di una
           connessione che il server può effettuare se il tentativo iniziale
           non riesce.

           Il valore predefinito è 0, che specifica che il server non ripete
           il tentativo di creare la connessione.

       --creationretryinterval
           Specifica l'intervallo, in secondi, tra i tentativi di creazione
           della connessione successivi.

           Se il valore di --creationretryattempts è 0, l'opzione
           --creationretryinterval verrà ignorata. Il valore predefinito è 10.

       --description
           Il testo descrittivo del connection pool del connettore.

       --failconnection
           Se l'impostazione è true, tutte le connessioni del pool vengono
           chiuse in caso di esito negativo di un singolo controllo di
           convalida. Questo parametro è obbligatorio se l'opzione
           --isconnectvalidatereq è impostata su true. Il valore predefinito è
           false.

       --idletimeout
           Il periodo di tempo massimo di inattività di una connessione nel
           pool. Trascorso questo intervallo di tempo, il pool può chiudere la
           connessione. Il valore predefinito è 300.

       --isconnectvalidatereq
           Se il valore è impostato su true, le connessioni vengono
           controllate per verificare se possono essere utilizzate, prima di
           essere fornite all'applicazione. Il valore predefinito è false.

       --lazyconnectionenlistment
           Specifica se una risorsa per una transazione viene integrata solo
           quando un metodo la utilizza effettivamente. Il valore predefinito
           è false.

       --lazyconnectionassociation
           Specifica se una connessione fisica deve essere associata alla
           connessione logica solo quando è utilizzata, e disassociata al
           completamento della transazione. L'associazione e la
           disassociazione permettono il riutilizzo delle connessioni fisiche.
           Di seguito sono riportati i valori possibili.

           false
               Una connessione fisica viene associata alla connessione logica
               anche prima di essere utilizzata, e non viene disassociata al
               completamento della transazione (valore predefinito).

           true
               Una connessione fisica viene associata alla connessione logica
               solo quando è utilizzata, e disassociata al completamento della
               transazione. Anche l'opzione --lazyconnectionenlistment deve
               essere impostata su true.

       --leakreclaim
           Specifica se le connessioni perse vengono ripristinate nel
           connection pool quando il trace della connessione persa è completo.
           Di seguito sono riportati i valori possibili.

           false
               Le connessioni perse non vengono ripristinate nel connection
               pool (impostazione predefinita).

           true
               Le connessioni perse vengono ripristinate nel connection pool.

       --leaktimeout
           Specifica la quantità di tempo, in secondi, destinata al trace
           delle perdite di connessione in un connection pool.

           Se il trace delle perdite di connessione è abilitato, è possibile
           usare Console di amministrazione per attivare il monitoraggio del
           connection pool JDBC allo scopo di ottenere statistiche sul numero
           di perdite di connessione. Il valore predefinito è 0, che
           disabilita il trace delle perdite di connessione.

       --matchconnections
           Specifica se una connessione selezionata nel pool deve essere
           associata al Resource Adapter. Se tutte le connessioni del pool
           sono identiche, non è necessaria l'associazione tra le connessioni
           e i Resource Adapter. Di seguito sono riportati i valori possibili.

           true
               Una connessione deve essere associata al Resource Adapter
               (impostazione predefinita).

           false
               Una connessione non deve essere associata al Resource Adapter.

       --maxconnectionusagecount
           Specifica il numero massimo di volte in cui è possibile
           riutilizzare una connessione.

           Raggiunto questo limite, la connessione viene chiusa. Il valore
           predefinito è 0 e indica che non è previsto alcun limite per il
           numero di volte in cui una connessione può essere riutilizzata.

       --maxpoolsize
           Il numero massimo di connessioni che possono essere create per
           soddisfare le richieste dei client. Il valore predefinito è 32.

       --maxwait
           L'intervallo di tempo, in millisecondi, di attesa per un chiamante
           prima che venga creata una connessione, qualora non ve ne sia una
           disponibile. Se si imposta 0, il chiamante viene bloccato a tempo
           indeterminato finché non sarà disponibile una risorsa o finché non
           si verificherà un errore. Il valore predefinito è 60000.

       --ping
           Un pool con questo attributo impostato su true viene contattato
           durante la creazione (o la riconfigurazione) per identificare
           eventuali valori non validi per gli attributi e generare la
           relativa avvertenza. Il valore predefinito è false.

       --pooling
           Se si imposta false, questo attributo disabilita il connection
           pooling. Il valore predefinito è true.

       --poolresize
           Quantità in base alla quale verrà eseguito lo scale up o lo scale
           down del numero di connessioni nel pool. Scale up: quando nel pool
           non sono presenti connessioni libere, il pool viene aumentato di
           questa quantità. Scale down: tutte le connessioni non valide o
           inattive vengono rimosse, a volte in numero superiore a quello
           indicato dal valore. Verrà garantito il numero di connessioni
           specificato da --steadypoolsize. I valori possibili sono compresi
           tra 0 e MAX_INTEGER. Il valore predefinito è 2.

       --property
           Coppie nome-valore di attributo facoltative per la configurazione
           del pool.

           LazyConnectionEnlistment
               Non più valida. Usare l'opzione equivalente. Il valore
               predefinito è false.

           LazyConnectionAssociation
               Non più valida. Usare l'opzione equivalente. Il valore
               predefinito è false.

           AssociateWithThread
               Non più valida. Usare l'opzione equivalente. Il valore
               predefinito è false.

           MatchConnections
               Non più valida. Usare l'opzione equivalente. Il valore
               predefinito è false.

       --raname
           Il nome del Resource Adapter.

       --steadypoolsize
           Il numero minimo e iniziale di connessioni gestite nel pool. Il
           valore predefinito è 8.

       --target
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

       --transactionsupport
           Indica il livello di supporto delle transazioni nel pool. I valori
           possibili sono XATransaction, LocalTransaction e NoTransaction.
           Questo attributo può avere un valore minore o uguale all'attributo
           transactionsupport del Resource Adapter, ma non maggiore.
           L'attributo transactionsupport del Resource Adapter prevede un
           ordine di valori, dove XATransaction è il valore più alto e
           NoTransaction è il valore più basso.

       --validateatmostonceperiod
           Specifica l'intervallo di tempo espresso in secondi tra richieste
           successive di convalida di una connessione (al massimo una volta).
           Impostando un valore appropriato per questo attributo si riduce al
           minimo il numero di richieste di convalida di una connessione. Il
           valore predefinito è 0 e indica che l'attributo non è abilitato.

OPERANDI
       poolname
           Il nome del connection pool da creare.

ESEMPI
       Esempio 1, Creazione di un connection pool del connettore
           In questo esempio viene creato il nuovo connection pool del
           connettore denominato jms/qConnPool.

               asadmin> create-connector-connection-pool --raname jmsra
               --connectiondefinition javax.jms.QueueConnectionFactory --steadypoolsize 20
               --maxpoolsize 100 --poolresize 2 --maxwait 60000 jms/qConnPool
               Command create-connector-connection-pool executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-connector-connection-pool(1),
       list-connector-connection-pools(1), ping-connection-pool(1)

       asadmin(1M)

Java EE 7                      9 settembre create-connector-connection-pool(1)
