create-connector-resouasadminユーティリティのサブコ

名前
       create-connector-resource -
       指定したJNDI名でコネクタ・リソースを登録します

概要
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

説明
       create-connector-resourceサブコマンドは、指定したJNDI名でコネクタ・リソースを登録します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --poolname
           接続プールの名前。複数のリソース要素が同じ接続プール要素に関連付けられている場合、それらの要素は実行時に同じプール接続を使用します。

       --enabled
           このオプションは、リソースが実行時に有効かどうかを判断します。デフォルト値はtrueです。

       --objecttype
           コネクタ・リソースの型を定義します。デフォルトはuserです。指定できる値は次のとおりです。

           system-all
               すべてのサーバー・インスタンスとドメイン管理サーバー(DAS)のシステム・リソース。

           system-admin
               DAS限定のシステム・リソース。

           system-instance
               すべてのサーバー・インスタンス限定のシステム・リソース。

           user
               ユーザー・リソース。

       --description
           コネクタ・リソースの詳細を説明するテキスト。

       --property
           リソースを構成するための属性名と値のペア(オプション)。

       --target
           このオプションは、コネクタ・リソースの終了位置を指定します。有効なターゲットは次のとおりです。

           server
               デフォルトのサーバー・インスタンスにコネクタ・リソースを作成します。この値がデフォルト値です。

           domain
               ドメインにコネクタ・リソースを作成します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスにコネクタ・リソースを作成します。

           instance_name
               指定したサーバー・インスタンスにコネクタ・リソースを作成します。

               注記
               ┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                              │
               │リソースは常にドメイン全体に対して作成されますが、リソースのresource-refは指定された--targetに対してのみ作成されます。つまり、リソースはドメイン・レベルで定義されますが、指定されたターゲット・レベルでのみ使用可能です。必要に応じて、複数のターゲット内のリソースを参照するには、create-resource-refサブコマンドを使用します。 │
               └──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

オペランド
       jndi_name
           このコネクタ・リソースのJNDI名。

例
       例 1, コネクタ・リソースの作成
           この例では、jms/qConnFactoryという名前のコネクタ・リソースを作成します。

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       例 2, create-connector-resourceサブコマンドの使用
           この例では、このサブコマンドの使用方法を示します。

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           jms/qConnFactoryは、作成されたサンプルのコネクタ・リソースです。

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                        2011年2月10日    create-connector-resource(1)
