list-connector-security-mapsasadmin 公用程式子命

參考名
       list-connector-security-maps -
       子命令會列出屬於指定之連線器連線集區的安全性對應。

概要
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

描述
       list-connector-security-maps
       子命令會列出屬於指定之連線器連線集區的安全性對應。

       您必須先使用 create-connector-connection-pool
       子命令建立一個連線器連線集區，才能成功執行這個子命令。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --securitymap
           可指定包含於連線器連線集區之安全性對應的名稱，其中應列出身份及主體。使用此選項時，便無須使用
           --verbose。

       --verbose
           如果設定為 true，則會傳回包含身份、主體與安全性名稱的清單。預設值為
           false。

       --target
           請勿指定此選項。保留這個選項是為了兼顧與舊版本的相容性。如果您指定此選項，並不會發生語法錯誤。相反地，子命令會順利執行，並顯示已忽略該選項的警告訊息。

運算元
       pool-name
           您想要列出安全性對應的連線器連線集區名稱。

範例
       範例 1, 列出連線器安全性對應
           此範例會列出名稱為 connector-Pool1
           之連線集區現有的連線器安全性對應。

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 list-connector-security-maps(1)
