delete-custom-resource(1)   asadmin 公用程式子命令   delete-custom-resource(1)

參考名
       delete-custom-resource - 移除自訂資源

概要
           delete-custom-resource [--help] [--target target] jndi-name

描述
       delete-custom-resource 子命令會移除自訂資源。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項可協助指定您要刪除自訂資源的位置。有效目標為伺服器、網域、叢集及執行處理。預設值為伺服器。

               注意
               ┌─────────────────────────────────────────────────────┐
               │備註                                                 │
               │資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
               │--target 選項建立 <resource-ref>                     │
               │的目標。這表示刪除資源只會刪除指定                   │
               │--target 的 <resource-ref>                           │
               │元素，而不會刪除完整網域的資源，除非已將             │
               │domain 指定為要刪除的 --target。                     │
               └─────────────────────────────────────────────────────┘

           server
               刪除預設伺服器執行處理的資源。此為預設值。

           domain
               刪除網域的資源。

           cluster_name
               刪除叢集中每個伺服器執行處理的資源。

           instance_name
               刪除特定伺服器執行處理的資源。

運算元
       jndi-name
           此資源的 JNDI 名稱。

範例
       範例 1, 刪除自訂資源
           此範例會刪除名為 mycustomresource 的自訂資源。

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日    delete-custom-resource(1)
