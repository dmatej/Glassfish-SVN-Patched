delete-admin-object(1)asadminユーティリティのサブコマンド

名前
       delete-admin-object -
       指定したJNDI名の管理対象オブジェクトを除去します。

概要
           delete-admin-object [--help] [--target target] jndi_name

説明
       delete-admin-objectサブコマンドは、指定したJNDI名の管理対象オブジェクトを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           管理対象オブジェクトを削除するターゲットの名前です。有効な値は次のとおりです:

               注記
               ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                                                                           │
               │リソースは常にドメイン全体に対して作成されますが、リソースがアクティブになるのは、リソースの作成時に--targetオプションを使用して<resource-ref>が作成されたターゲットに対してのみです。つまり、リソースの削除では、指定した--targetの<resource-ref>要素のみが削除され、削除対象の--targetとしてdomainを指定しないかぎり、ドメイン全体からリソースが削除されることはありません。 │
               └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

           server
               デフォルトのサーバー・インスタンスserverの管理対象オブジェクトを削除します。これはデフォルト値です。

           configuration_name
               指定された構成の管理対象オブジェクトを削除します。

           cluster_name
               指定されたクラスタの管理対象オブジェクトを削除します。

           instance_name
               特定のサーバー・インスタンスの管理対象オブジェクトを削除します。

オペランド
       jndi_name
           削除する管理対象オブジェクトのJNDI名。

例
       例 1, 管理対象オブジェクトの削除
           この例では、jms/samplelqueueという名前の管理対象オブジェクトを削除します。

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                        2010年9月14日          delete-admin-object(1)
