create-custom-resource(1Subcomandos de la Utilidad ascreate-custom-resource(1)

NOMBRE DE REFERENCIA
       create-custom-resource - Crea un recurso personalizado.

SINOPSIS
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

DESCRIPCIÓN
       El subcomando create-custom-resource crea un recurso personalizado. Un
       recurso personalizado especifica una fabrica personalizada de objetos
       de recursos de servidor que implanta la interfaz
       javax.naming.spi.ObjectFactory.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Destino en el que estará disponible el recurso personalizado que
           está creando. Los valores válidos son:

           server
               El recurso estará disponible en la instancia por defecto del
               servidor y en todos los dominios alojados en la instancia. Este
               es el valor por defecto.

           domain
               El recurso sólo estará disponible en el dominio especificado.

           cluster_name
               El recurso estará disponible en todas las instancias de
               servidor del cluster.

           instance_name
               El recurso sólo estará disponible en la instancia de servidor
               especificada.

               Nota
               ┌────────────────────────────────────────┐
               │Nota El recurso se crea siempre para el │
               │dominio como un conjunto, pero el       │
               │elemento resource-ref del recurso sólo  │
               │se crea para la opción --target         │
               │especificada. Esto significa que,       │
               │aunque el recurso se define en el nivel │
               │de dominio, sólo está disponible en el  │
               │nivel de destino especificado. Utilice  │
               │el subcomando create-resource-ref para  │
               │hacer referencia al recurso en varios   │
               │destinos si es necesario.               │
               └────────────────────────────────────────┘

       --restype
           Tipo de recurso personalizado que se va a crear. Especifique una
           definición de tipo totalmente cualificada, por ejemplo
           javax.naming.spi.ObjectFactory. La definición de tipo de recurso
           sigue el formato xxx.xxx.

       --factoryclass
           Nombre de clase de fábrica para el recurso personalizado. Esta
           clase implanta la interfaz javax.naming.spi.ObjectFactory.

       --enabled
           Determina si el recurso personalizado está activado en el tiempo de
           ejecución. El valor por defecto es true.

       --description
           Texto que proporciona detalles sobre el recurso personalizado. Esta
           descripción es un valor de cadena y puede incluir un máximo de 250
           caracteres.

       --property
           Pares de nombre/valor de atributo opcional para configurar el
           recurso.

OPERANDOS
       jndi-name
           Nombre de JNDI de este recurso.

EJEMPLOS
       Ejemplo 1, Creación de un recurso personalizado
           En este ejemplo se crea un recurso personalizado.

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011   create-custom-resource(1)
