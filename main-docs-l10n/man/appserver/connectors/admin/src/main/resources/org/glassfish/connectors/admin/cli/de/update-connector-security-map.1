update-connector-securitUnterbefehle des asadmupdate-connector-security-map(1)

NAME
       update-connector-security-map - ändert eine Sicherheitszuordnung für
       den angegebenen Connector Connection Pool

ZUSAMMENFASSUNG
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

BESCHREIBUNG
       Der Unterbefehl update-connector-security-map ändert eine
       Sicherheitszuordnung für den angegebenen Connector Connection Pool.

       Damit dieser Unterbefehl erfolgreich ausgeführt wird, müssen Sie
       zunächst mit dem Unterbefehl create-connector-connection-pool einen
       Connector Connection Pool erstellen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --poolname
           Gibt den Namen des Connector Connection Pools an, zu dem die
           Sicherheitszuordnung gehört, die aktualisiert werden soll.

       --addprincipals
           Gibt eine durch Komma getrennte Liste der EIS-spezifischen
           Principals an, die hinzugefügt werden sollen. Verwenden Sie
           entweder die Option --addprincipals oder die Option
           --addusergroups, aber nicht beide Optionen in demselben Befehl.

       --addusergroups
           Gibt eine durch Komma getrennte Liste der EIS-Benutzergruppen an,
           die hinzugefügt werden sollen. Verwenden Sie entweder die Option
           --addprincipals oder die Option --addusergroups, aber nicht beide
           Optionen in demselben Befehl.

       --removeprincipals
           Gibt eine durch Komma getrennte Liste der EIS-spezifischen
           Principals an, die entfernt werden sollen.

       --removeusergroups
           Gibt eine durch Komma getrennte Liste der EIS-Benutzergruppen an,
           die entfernt werden sollen.

       --mappedusername
           Gibt den EIS-Benutzernamen an.

OPERANDEN
       mapname
           Der Name der Sicherheitszuordnung, die aktualisiert werden soll.

BEISPIELE
       Beispiel 1, Connector-Sicherheitszuordnung aktualisieren
           In diesem Beispiel werden Principals zu einer vorhandenen
           Sicherheitszuordnung namens securityMap1 hinzugefügt.

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                        15. Sep. 2010update-connector-security-map(1)
