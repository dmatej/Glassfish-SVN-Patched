create-jndi-resource(1) Subcomandos de la Utilidad asa create-jndi-resource(1)

NOMBRE DE REFERENCIA
       create-jndi-resource - Registra un recurso JNDI.

SINOPSIS
           create-jndi-resource [--help] [--target target]
           --restype restype --factoryclass factoryclass
           --jndilookupname jndilookupname [--enabled={true|false}]
           [--description description]
           [--property (name=value)[:name=value]*]
           jndi-name

DESCRIPCIÓN
       El subcomando create-jndi-resource registra un recurso JNDI.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Esta opción especifica el destino para el que se va a registrar un
           recurso JNDI. Los valores válidos del destino se describen a
           continuación.

               Nota
               ┌────────────────────────────────────────┐
               │Nota El recurso se crea siempre para el │
               │dominio como un conjunto, pero el       │
               │elemento resource-ref del recurso sólo  │
               │se crea para la opción --target         │
               │especificada. Esto significa que,       │
               │aunque el recurso se define en el nivel │
               │de dominio, sólo está disponible en el  │
               │nivel de destino especificado. Utilice  │
               │el subcomando create-resource-ref para  │
               │hacer referencia al recurso en varios   │
               │destinos si es necesario.               │
               └────────────────────────────────────────┘

           server
               Crea el recurso para la instancia de servidor por defecto. Éste
               es el valor por defecto.

           domain
               Crea el recurso para el dominio.

           cluster-name
               Crea el recurso para todas las instancias de servidor del
               cluster.

           instance-name
               Crea el recurso para una instancia de servidor determinada.

       --restype
           El tipo del recurso JNDI. Los valores válidos son topic o queue.

       --factoryclass
           La clase que crea el recurso JNDI.

       --jndilookupname
           El nombre de búsqueda que utiliza el contenedor externo.

       --enabled
           Determina si el recurso está activado en tiempo de ejecución. El
           valor por defecto es true.

       --description
           Texto que proporciona detalles sobre el recurso JNDI.

       --property
           Propiedades opcionales para configurar el recurso. Cada propiedad
           se especifica como un par nombre-valor.

           Las propiedades disponibles son específicas de la implantación que
           especifica la opción --factoryclass y se utilizan en dicha
           implantación. GlassFish Server no define ninguna propiedad para
           configurar un recurso JNDI.

OPERANDOS
       jndi-name
           El nombre exclusivo del recurso JNDI que se va a crear.

EJEMPLOS
       Ejemplo 1, Creación de un Recurso JNDI
           En este ejemplo se crea el recurso JNDI my-jndi-resource para la
           instancia de servidor por defecto.

               asadmin> create-jndi-resource
               --restype com.example.jndi.MyResourceType
               --factoryclass com.example.jndi.MyInitialContextFactoryClass
               --jndilookupname remote-jndi-name
               --description "sample JNDI resource" my-jndi-resource
               JNDI resource my-jndi-resource created.
               Command create-jndi-resource executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-jndi-resource(1), list-jndi-resources(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                     21 de junio de 2011      create-jndi-resource(1)
