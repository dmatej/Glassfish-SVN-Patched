list-connector-security-Comandi secondari dellalist-connector-security-maps(1)

NOME
       list-connector-security-maps - elenca le mappe di sicurezza che
       appartengono al connection pool di connettore specificato

SINTASSI
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

DESCRIZIONE
       Il comando secondario list-connector-security-maps elenca le mappe di
       sicurezza che appartengono al connection pool di connettore
       specificato.

       Affinché l'esecuzione di questo comando secondario riesca è necessario
       creare preliminarmente un connection pool di connettore mediante il
       comando secondario create-connector-connection-pool.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --securitymap
           Specifica il nome della mappa di sicurezza contenuta nel connection
           pool connettore da cui devono essere elencati l'ID e i principal.
           Con questa opzione --verbose è ridondante.

       --verbose
           Se si imposta true, viene restituita una lista contenente l'ID, i
           principal e il nome di sicurezza. Il valore predefinito è false.

       --target
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

OPERANDI
       pool-name
           Il nome del connection pool connettore per cui si desidera elencare
           le mappe di sicurezza.

ESEMPI
       Esempio 1, Visualizzazione della lista delle mappe di sicurezza del
       connettore
           In questo esempio vengono elencate le mappe di sicurezza del
           connettore esistenti per il pool denominato connector-Pool1.

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010list-connector-security-maps(1)
