create-admin-object(1)asadminユーティリティのサブコマンド

名前
       create-admin-object -
       リソース・アダプタの指定されたJNDI名の管理対象オブジェクトを追加します

概要
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

説明
       create-admin-objectサブコマンドは、リソース・アダプタの指定されたJNDI名の管理対象オブジェクトとインタフェース定義を作成します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           管理対象オブジェクトを作成するターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverの管理対象オブジェクトを作成します。これはデフォルト値です。

           configuration_name
               名前付き構成の管理対象オブジェクトを作成します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスに管理対象オブジェクトを作成します。

           instance_name
               特定のサーバー・インスタンスに管理対象オブジェクトを作成します。

               注記
               ┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                                              │
               │リソースは常にドメイン全体に対して作成されますが、リソースのresource-refは指定された--targetに対してのみ作成されます。これは、リソースはドメイン・レベルで定義されますが、指定されたターゲット・レベルでのみ使用可能であることを示します。必要に応じて、複数のターゲット内のリソースを参照するには、create-resource-refサブコマンドを使用します。 │
               └──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       --restype
           管理対象オブジェクトのインタフェース定義を指定します。リソース・タイプは、リソース・アダプタのra.xmlファイルで指定されたインタフェース定義にしてください。

       --classname
           管理対象オブジェクトのクラス名を指定します。複数の管理対象オブジェクトが同じインタフェース定義を使用する場合は必須です。

       --raname
           この管理対象オブジェクトに関連付けられているリソース・アダプタの名前を指定します。

       --enabled
           このオブジェクトを有効にするかを指定します。デフォルトはtrueです。

       --description
           管理対象オブジェクトを表すテキスト文字列。

       --property
           リソースを構成する名前と値のペアの説明。リソース・アダプタに依存します。JMSプロパティについては、create-jms-resource(1)でJMS宛先リソースを参照してください。

オペランド
       jndi_name
           作成する管理対象オブジェクトのJNDI名。

例
       例 1, 管理対象オブジェクトの作成
           この例では、jmsraは、管理オブジェクト・インタフェースjavax.jms.Queueおよびjavax.jms.Topicを使用するシステム・リソース・アダプタです。

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                        2011年2月10日          create-admin-object(1)
