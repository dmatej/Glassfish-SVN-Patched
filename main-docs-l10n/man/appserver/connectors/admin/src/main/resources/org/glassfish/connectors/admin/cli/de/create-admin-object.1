create-admin-object(1)  Unterbefehle des asadmin-Utili  create-admin-object(1)

NAME
       create-admin-object - fügt das verwaltete Objekt mit dem angegebenen
       JNDI-Namen für einen Ressourcenadapter hinzu

ZUSAMMENFASSUNG
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

BESCHREIBUNG
       Der Unterbefehl create-admin-object erstellt das verwaltete Objekt mit
       dem angegebenen JNDI-Namen und der Schnittstellendefinition für einen
       Ressourcenadapter.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, auf dem Sie das verwaltete Objekt erstellen.
           Folgende Werte sind gültig:

           server
               Erstellt das verwaltete Objekt für die standardmäßige
               Serverinstanz server und ist der Standardwert.

           configuration_name
               Erstellt das verwaltete Objekt für die benannte Konfiguration.

           cluster_name
               Erstellt das verwaltete Objekt für jede Serverinstanz im
               Cluster.

           instance_name
               Erstellt das verwaltete Objekt für eine bestimmte
               Serverinstanz.

               Note
               ┌────────────────────────────────────────┐
               │Hinweis Die Ressource wird immer für    │
               │die gesamte Domain erstellt, doch       │
               │resource-ref für die Ressource wird nur │
               │für das angegebene Ziel (--target)      │
               │erstellt. Das bedeutet, dass die        │
               │Ressource, obwohl sie auf Domainebene   │
               │definiert wird, nur auf der angegebenen │
               │Zielebene verfügbar ist. Verwenden Sie  │
               │den Unterbefehl create-resource-ref, um │
               │die Ressource bei Bedarf in mehreren    │
               │Zielen zu referenzieren.                │
               └────────────────────────────────────────┘

       --restype
           Gibt die Schnittstellendefinition für das verwaltete Objekt an. Der
           Ressourcentyp muss eine Schnittstellendefinition sein, die in der
           Datei ra.xml des Ressourcenadapters angegeben ist.

       --classname
           Gibt den Klassennamen für das verwaltete Objekt an. Erforderlich,
           wenn mehrere verwaltete Objekte dieselbe Schnittstellendefinition
           verwenden.

       --raname
           Gibt den Namen des Ressourcenadapters an, der mit diesem
           verwalteten Objekt verknüpft ist.

       --enabled
           Gibt an, ob dieses Objekt aktiviert ist. Der Standardwert ist
           "true".

       --description
           Textzeichenfolge, die das verwaltete Objekt beschreibt.

       --property
           Beschreibung der Name/Wert-Paare zum Konfigurieren der Ressource.
           Abhängig vom Ressourcenadapter. Weitere Informationen zu
           JMS-Eigenschaften finden Sie unter create-jms-resource(1) für
           JMS-Zielressourcen.

OPERANDEN
       jndi_name
           JNDI-Name des verwalteten Objekts, das erstellt werden soll.

BEISPIELE
       Example 1, Verwaltete Objekte erstellen
           In diesem Beispiel ist jmsra ein Systemressourcenadapter mit den
           Admin-Objektschnittstellen javax.jms.Queue und javax.jms.Topic.

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                        10. Feb. 2011          create-admin-object(1)
