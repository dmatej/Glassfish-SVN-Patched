update-connector-securitSubcomandos de la Utilupdate-connector-security-map(1)

NOMBRE DE REFERENCIA
       update-connector-security-map - crea o modifica un mapa de seguridad
       para el pool de conexiones de conector especificado.

SINOPSIS
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

DESCRIPCIÓN
       El subcomando update-connector-security-map modifica un mapa de
       seguridad para el pool de conexiones de conector especificado.

       Para que este subcomando se ejecute correctamente, se debe haber creado
       antes un pool de conexiones de conector mediante el subcomando
       create-connector-connection-pool.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --poolname
           Especifica el nombre del pool de conexiones de conector al que
           pertenece el mapa de seguridad que se va a actualizar.

       --addprincipals
           Especifica una lista separada por comas de principales específicos
           de EIS que se deben agregar. Utilice la opción --addprincipals o la
           opción --addusergroups, pero no ambas en el mismo comando.

       --addusergroups
           Especifica una lista separada por comas de grupos de usuarios EIS
           que se deben agregar. Utilice la opción --addprincipals o la opción
           --addusergroups, pero no ambas en el mismo comando.

       --removeprincipals
           Especifica una lista separada por comas de principales específicos
           de EIS que se deben eliminar.

       --removeusergroups
           Especifica una lista separada por comas de grupos de usuarios EIS
           que se deben eliminar.

       --mappedusername
           Especifica el nombre de usuario de EIS.

OPERANDOS
       mapname
           El nombre de la asignación de seguridad que se va a actualizar.

EJEMPLOS
       Ejemplo 1, Actualización de una asignación de seguridad de conector
           En este ejemplo se agregan principales a la asignación de seguridad
           existente denominada securityMap1 .

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                  15 de septiembre deupdate-connector-security-map(1)
