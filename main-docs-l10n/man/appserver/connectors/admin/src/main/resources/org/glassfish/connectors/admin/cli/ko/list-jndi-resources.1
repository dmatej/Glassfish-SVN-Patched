list-jndi-resources(1)    asadmin 유틸리티 하위 명령    list-jndi-resources(1)

제목
       list-jndi-resources - 모든 기존 JNDI 리소스 나열

시놉시스
           list-jndi-resources [--help] [target]

설명
       list-jndi-resources 하위 명령은 기존 JNDI 리소스를 모두 식별합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           JNDI 리소스를 나열할 대상입니다. 적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스의 JNDI 리소스를 나열합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성의 JNDI 리소스를 나열합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스의 JNDI 리소스를
               나열합니다.

           instance-name
               지정된 서버 인스턴스의 JNDI 리소스를 나열합니다.

예제
       예 1, JNDI 리소스 나열
           이 예제는 기본 서버 인스턴스의 JNDI 리소스를 나열합니다.

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

Java EE 7                       2011년 6월 21일         list-jndi-resources(1)
