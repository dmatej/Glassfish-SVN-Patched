delete-jndi-resource(1) Subcomandos do utilitário asad delete-jndi-resource(1)

NOME
       delete-jndi-resource - remove um recurso JNDI

SINOPSE
           delete-jndi-resource [--help] [--target target] jndi_name

DESCRIçãO
       O subcomando delete-jndi-resource remove o recurso JNDI especificado. É
       necessário remover todas as associações ao recurso JNDI antes de
       executar o subcomando.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Os alvos válidos estão descritos abaixo.

               Nota
               ┌────────────────────────────────────────┐
               │Observação Os recursos sempre são       │
               │criados para um domínio como um todo,   │
               │mas só estarão ativos para alvos para   │
               │os quais <resource-ref> tiver sido      │
               │criado usando a opção --target durante  │
               │a criação do recurso. Isso significa    │
               │que a deleção de um recurso só deleta o │
               │elemento <resource-ref> para o --target │
               │especificado e não deleta o recurso do  │
               │domínio como um todo, a não ser que     │
               │domain seja especificado como o         │
               │--target para a deleção.                │
               └────────────────────────────────────────┘

           server
               Deleta o recurso a partir da instância de servidor default.
               Este é o valor default

           domain
               Deleta o recurso do domínio

           cluster_name
               Deleta o recurso de cada instância de servidor do cluster

           instance_name
               Deleta o recurso da instância de servidor especificada

OPERANDOS
       jndi_name
           O nome do recurso JNDI a ser removido.

EXEMPLOS
       Example 1, Deletando um Recurso JNDI.
           Este exemplo remove um recurso JNDI existente com o nome
           sample_jndi_resource.

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                   11 de fevereiro de 2011    delete-jndi-resource(1)
