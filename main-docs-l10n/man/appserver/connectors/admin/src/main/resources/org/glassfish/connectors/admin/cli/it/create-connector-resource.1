create-connector-resourcComandi secondari della utcreate-connector-resource(1)

NOME
       create-connector-resource - registra la risorsa connettore con il nome
       JNDI specificato

SINTASSI
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

DESCRIZIONE
       Il comando secondario create-connector-resource registra la risorsa
       connettore con il nome JNDI specificato.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --poolname
           Il nome del connection pool. Quando due o più elementi risorsa
           puntano allo stesso elemento del connection pool, utilizzano le
           stesse connessioni del pool in fase di esecuzione.

       --enabled
           Questa opzione determina se la risorsa viene abilitata in fase di
           esecuzione. Il valore predefinito è true.

       --objecttype
           Definisce il tipo della risorsa connettore. Il valore predefinito è
           user. Di seguito sono riportati i valori consentiti.

           system-all
               La risorsa di sistema per tutte le istanze server e per il
               server di amministrazione del dominio (DAS).

           system-admin
               La risorsa di sistema solo per il server DAS.

           system-instance
               La risorsa di sistema solo per tutte le istanze server.

           user
               Una risorsa utente.

       --description
           Il testo descrittivo della risorsa connettore.

       --property
           Coppie nome-valore di attributo facoltative per la configurazione
           della risorsa.

       --target
           Questa opzione specifica la posizione finale delle risorse
           connettore. Le destinazioni valide sono:

           server
               Crea la risorsa connettore nell'istanza server predefinita.
               Questo è il valore predefinito.

           domain
               Crea la risorsa connettore nel dominio.

           cluster_name
               Crea la risorsa connettore in tutte le istanze server del
               cluster.

           instance_name
               Crea la risorsa connettore nell'istanza server specificata.

               Nota
               ┌────────────────────────────────────────┐
               │Nota La risorsa viene sempre creata per │
               │l'intero dominio, mentre l'elemento     │
               │resource-ref per la risorsa viene       │
               │creato solo per l'elemento --target     │
               │specificato. Ciò significa che, benché  │
               │sia definita a livello di dominio, la   │
               │risorsa è disponibile solo per il       │
               │livello destinazione specificato. Se    │
               │necessario, utilizzare il comando       │
               │secondario create-resource-ref per fare │
               │riferimento alla risorsa in più livelli │
               │target.                                 │
               └────────────────────────────────────────┘

OPERANDI
       jndi_name
           Il nome JNDI della risorsa connettore.

ESEMPI
       Esempio 1, Creazione di una risorsa connettore
           In questo esempio viene creata la risorsa connettore denominata
           jms/qConnFactory.

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       Esempio 2, Uso del comando secondario create-connector-resource
           L'esempio illustra l'uso di questo comando secondario.

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           Dove jms/qConnFactory è la risorsa connettore di esempio da creare.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                      10 febbraio 2011   create-connector-resource(1)
