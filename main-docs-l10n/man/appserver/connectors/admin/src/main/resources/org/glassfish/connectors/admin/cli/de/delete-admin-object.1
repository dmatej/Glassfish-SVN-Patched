delete-admin-object(1)  Unterbefehle des asadmin-Utili  delete-admin-object(1)

NAME
       delete-admin-object - entfernt das verwaltete Objekt mit dem
       angegebenen JNDI-Namen.

ZUSAMMENFASSUNG
           delete-admin-object [--help] [--target target] jndi_name

BESCHREIBUNG
       Der Unterbefehl delete-admin-object entfernt ein verwaltetes Objekt mit
       dem angegebenen JNDI-Namen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Dies ist der Name der Ziele, für die das verwaltete Objekt gelöscht
           werden soll. Gültige Werte sind:

               Note
               ┌────────────────────────────────────────┐
               │Hinweis Ressourcen werden stets für     │
               │eine Domain als Ganzes erstellt, sind   │
               │aber nur für Ziele aktiv, für die bei   │
               │der Erstellung der Ressource eine       │
               │<resource-ref> mit der Option --target  │
               │erstellt wurde. Das bedeutet, dass beim │
               │Löschen einer Ressource nur das         │
               │<resource-ref>-Element für das          │
               │angegebene Ziel (--target) und nicht    │
               │die Ressource aus der Domain als Ganzes │
               │gelöscht wird, es sei denn, domain ist  │
               │als --target für den Löschvorgang       │
               │angegeben.                              │
               └────────────────────────────────────────┘

           server
               Löscht das verwaltete Objekt für die standardmäßige
               Serverinstanz server und ist der Standardwert.

           configuration_name
               Löscht das verwaltete Objekt für die angegebene Konfiguration.

           cluster_name
               Löscht das verwaltete Objekt für das angegebene Cluster.

           instance_name
               Löscht das verwaltete Objekt für eine bestimmte Serverinstanz.

OPERANDEN
       jndi_name
           JNDI-Name des verwalteten Objekts, das gelöscht werden soll.

BEISPIELE
       Example 1, Verwaltete Objekte löschen
           In diesem Beispiel wird das verwaltete Objekt namens
           jms/samplelqueue gelöscht.

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                        14. Sep. 2010          delete-admin-object(1)
