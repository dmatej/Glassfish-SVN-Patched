list-jndi-resources(1)asadminユーティリティのサブコマンド

名前
       list-jndi-resources - 既存のすべてのJNDIリソースをリストします

概要
           list-jndi-resources [--help] [target]

説明
       list-jndi-resourcesサブコマンドは既存のすべてのJNDIリソースを識別します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           JNDIリソースをリストするターゲット。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのJNDIリソースをリストします。この値がデフォルト値です。

           configuration-name
               指定した構成のJNDIリソースをリストします。

           cluster-name
               指定したクラスタにあるすべてのサーバー・インスタンスのJNDIリソースをリストします。

           instance-name
               指定したサーバー・インスタンスのJNDIリソースをリストします。

例
       例 1, JNDIリソースのリスト
           この例では、デフォルトのサーバー・インスタンスのJNDIリソースをリストします。

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

Java EE 7                        2011年6月21日          list-jndi-resources(1)
