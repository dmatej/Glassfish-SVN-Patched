delete-connector-resourcUnterbefehle des asadmin-Udelete-connector-resource(1)

NAME
       delete-connector-resource - entfernt die Connector-Ressource mit dem
       angegebenen JNDI-Namen

ZUSAMMENFASSUNG
           delete-connector-resource [--help] [--target target] jndi_name

BESCHREIBUNG
       Der Unterbefehl delete-connector-resource entfernt die
       Connector-Ressource mit dem angegebenen JNDI-Namen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, aus dem Sie die Connector-Ressource
           entfernen möchten. Gültige Ziele sind:

               Anmerkung
               ┌────────────────────────────────────────┐
               │Hinweis Ressourcen werden stets für     │
               │eine Domain als Ganzes erstellt, sind   │
               │aber nur für Ziele aktiv, für die bei   │
               │der Erstellung der Ressource eine       │
               │<resource-ref> mit der Option --target  │
               │erstellt wurde. Das bedeutet, dass beim │
               │Löschen einer Ressource nur das         │
               │<resource-ref>-Element für das          │
               │angegebene Ziel (--target) und nicht    │
               │die Ressource aus der Domain als Ganzes │
               │gelöscht wird, es sei denn, domain ist  │
               │als --target für den Löschvorgang       │
               │angegeben.                              │
               └────────────────────────────────────────┘

           server
               Löscht die Connector-Ressource aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           domain
               Löscht die Connector-Ressource aus der Domain.

           cluster_name
               Löscht die Connector-Ressource aus jeder Serverinstanz im
               Cluster.

           instance_name
               Löscht die Connector-Ressource aus einer angegebenen
               Serverinstanz.

OPERANDEN
       jndi_name
           Der JNDI-Name dieser Connector-Ressource.

BEISPIELE
       Beispiel 1, Connector-Ressourcen löschen
           In diesem Beispiel wird eine Connector-Ressource namens
           jms/qConnFactory gelöscht.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       Beispiel 2, Unterbefehl "delete-connector-resource" verwenden
           In diesem Beispiel wird die Verwendung dieses Unterbefehls gezeigt.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           Dabei ist jms/qConnFactory die Connector-Ressource, die entfernt
           wird.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011    delete-connector-resource(1)
