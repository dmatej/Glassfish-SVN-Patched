create-custom-resource(1Comandi secondari della utilicreate-custom-resource(1)

NOME
       create-custom-resource - crea una risorsa personalizzata

SINTASSI
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

DESCRIZIONE
       Il comando secondario create-custom-resource consente di creare una
       risorsa personalizzata. Una risorsa personalizzata specifica un object
       factory personalizzato di risorse a livello di server che implementa
       l'interfaccia javax.naming.spi.ObjectFactory.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           La destinazione in cui sarà disponibile la risorsa personalizzata
           che si sta creando. Di seguito sono riportati i valori validi.

           server
               La risorsa sarà disponibile nell'istanza server predefinita e
               in tutti i domini ospitati nell'istanza. Questo è il valore
               predefinito.

           domain
               La risorsa sarà disponibile solo nel dominio specificato.

           cluster_name
               La risorsa sarà disponibile in tutte le istanze server nel
               cluster.

           instance_name
               La risorsa sarà disponibile solo nell'istanza server
               specificata.

               Nota
               ┌────────────────────────────────────────┐
               │Nota La risorsa viene sempre creata per │
               │l'intero dominio, mentre l'elemento     │
               │resource-ref per la risorsa viene       │
               │creato solo per l'elemento --target     │
               │specificato. Ciò significa che, benché  │
               │sia definita a livello di dominio, la   │
               │risorsa è disponibile solo per il       │
               │livello destinazione specificato. Se    │
               │necessario, utilizzare il comando       │
               │secondario create-resource-ref per fare │
               │riferimento alla risorsa in più livelli │
               │target.                                 │
               └────────────────────────────────────────┘

       --restype
           Il tipo di risorsa personalizzata da creare. Specificare una
           definizione tipo completamente qualificata, ad esempio
           javax.naming.spi.ObjectFactory. Per la definizione del tipo di
           risorsa si applica il formato xxx.xxx.

       --factoryclass
           Il nome della classe di factory per la risorsa personalizzata.
           Questa classe implementa l'interfaccia
           javax.naming.spi.ObjectFactory.

       --enabled
           Determina se la risorsa personalizzata è abilitata in fase di
           esecuzione. Il valore predefinito è true.

       --description
           Il testo descrittivo della risorsa personalizzata. La descrizione è
           un valore stringa che può contenere 250 caratteri al massimo.

       --property
           Coppie nome-valore di attributo facoltative per la configurazione
           della risorsa.

OPERANDI
       jndi-name
           Il nome JNDI della risorsa.

ESEMPI
       Esempio 1, Creazione di una risorsa personalizzata
           In questo esempio viene creata una risorsa personalizzata.

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011      create-custom-resource(1)
