create-custom-resource(1) asadmin 유틸리티 하위 명령 create-custom-resource(1)

제목
       create-custom-resource - 사용자 정의 리소스 생성

시놉시스
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

설명
       create-custom-resource 하위 명령은 사용자 정의 리소스를 생성합니다.
       사용자 정의 리소스는 javax.naming.spi.ObjectFactory 인터페이스를
       구현하는 사용자 정의 서버 범위 리소스 객체 팩토리를 지정합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           생성할 사용자 정의 리소스를 사용할 수 있는 대상입니다. 적합한 값은
           다음과 같습니다.

           server
               리소스를 기본 서버 인스턴스 및 인스턴스에 호스팅되는 모든
               도메인에서 사용할 수 있습니다. 이 값은 기본값입니다.

           domain
               리소스를 지정된 도메인에서만 사용할 수 있습니다.

           cluster_name
               리소스를 클러스터에 있는 모든 서버 인스턴스에서 사용할 수
               있습니다.

           instance_name
               리소스를 지정된 서버 인스턴스에서만 사용할 수 있습니다.

               참고
               ┌───────────────────────────────────────┐
               │주 리소스는 항상 도메인에 대해 전체로  │
               │생성되지만 리소스의 resource-ref는     │
               │지정한 --target에 대해서만 생성됩니다. │
               │즉, 리소스가 도메인 레벨에서           │
               │정의되더라도 지정된 대상 레벨에서만    │
               │사용할 수 있습니다. 필요한 경우 여러   │
               │대상에서 리소스를 참조하려면           │
               │create-resource-ref 하위 명령을        │
               │사용합니다.                            │
               └───────────────────────────────────────┘

       --restype
           생성할 사용자 정의 리소스의 유형입니다. 정규화된 유형 정의(예:
           javax.naming.spi.ObjectFactory)를 지정합니다. 리소스 유형 정의는
           xxx.xxx 형식을 따릅니다.

       --factoryclass
           사용자 정의 리소스의 팩토리 클래스 이름입니다. 이 클래스는
           javax.naming.spi.ObjectFactory 인터페이스를 구현합니다.

       --enabled
           사용자 정의 리소스가 런타임 시 사용으로 설정되어 있는지를
           확인합니다. 기본값은 true입니다.

       --description
           사용자 정의 리소스에 대한 세부 정보를 제공하는 텍스트입니다. 이
           설명은 최대 250자가 포함될 수 있는 문자열 값입니다.

       --property
           리소스를 구성하기 위한 선택적 속성 이름/값 쌍입니다.

피연산자
       jndi-name
           이 리소스의 JNDI 이름입니다.

예제
       예 1, 사용자 정의 리소스 생성
           이 예제는 사용자 정의 리소스를 생성합니다.

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일      create-custom-resource(1)
