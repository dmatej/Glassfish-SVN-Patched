create-connector-resource(1)asadmin 实用程序子命令create-connector-resource(1)

名称
       create-connector-resource - 使用指定的 JNDI 名称注册连接器资源

提要
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

说明
       create-connector-resource 子命令使用指定的 JNDI 名称注册连接器资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --poolname
           连接池的名称。如果两个或更多个资源元素指向同一个连接池元素，则这些资源元素在运行时会使用同一个池连接。

       --enabled
           此选项可确定是否在运行时启用资源。默认值为“真”。

       --objecttype
           定义连接器资源的类型。默认值为 user。允许的值包括：

           system-all
               所有服务器实例和域管理服务器 (DAS) 的系统资源。

           system-admin
               仅限于 DAS 的系统资源。

           system-instance
               仅限于所有服务器实例的系统资源。

           user
               用户资源。

       --description
           提供连接器资源详细信息的文本。

       --property
           用于配置资源的可选属性名称/值对。

       --target
           此选项指定连接器资源的结束位置。有效目标包括：

           server
               在默认服务器实例中创建连接器资源。这是默认值。

           domain
               在域中创建连接器资源。

           cluster_name
               在集群的每个服务器实例中创建连接器资源。

           instance_name
               在指定服务器实例中创建连接器资源。

               注意
               ┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注                                                                                               │
               │始终作为一个整体为域创建资源，但仅为指定的                                                       │
               │--target 创建资源的                                                                              │
               │resource-ref。这意味着，虽然资源是在域级别定义的，但它仅在指定的目标级别中可用。如果需要，可使用 │
               │create-resource-ref                                                                              │
               │子命令在多个目标中引用资源。                                                                     │
               └─────────────────────────────────────────────────────────────────────────────────────────────────┘

操作数
       jndi_name
           此连接器资源的 JNDI 名称。

示例
       Example 1, 创建连接器资源
           此示例创建名为 jms/qConnFactory 的连接器资源。

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       Example 2, 使用 create-connector-resource 子命令
           此示例显示了此子命令的用法。

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           其中 jms/qConnFactory 是所创建的连接器资源示例。

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 10 日  create-connector-resource(1)
