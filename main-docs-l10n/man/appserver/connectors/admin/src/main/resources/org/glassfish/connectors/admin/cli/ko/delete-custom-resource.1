delete-custom-resource(1) asadmin 유틸리티 하위 명령 delete-custom-resource(1)

제목
       delete-custom-resource - 사용자 정의 리소스 제거

시놉시스
           delete-custom-resource [--help] [--target target] jndi-name

설명
       delete-custom-resource 하위 명령은 사용자 정의 리소스를 제거합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 삭제할 사용자 정의 리소스의 위치를 지정하는 데 도움을
           줍니다. 적합한 대상은 서버, 도메인, 클러스터 또는 인스턴스입니다.
           기본값은 서버입니다.

               참고
               ┌────────────────────────────────────────┐
               │주 리소스는 항상 도메인에 대해 전체로서 │
               │생성되지만 리소스를 생성할 때 --target  │
               │옵션을 사용하여 <resource-ref>를 생성한 │
               │대상에 대해서만 활성화됩니다. 즉,       │
               │리소스를 삭제하면 지정된 --target에     │
               │대한 <resource-ref> 요소만 삭제되고     │
               │domain이 삭제에 대한 --target으로       │
               │지정되지 않는 한 도메인에서 리소스가    │
               │전체로서 삭제되지 않습니다.             │
               └────────────────────────────────────────┘

           server
               기본 서버 인스턴스에 대한 리소스를 삭제합니다. 이 값은
               기본값입니다.

           domain
               도메인에 대한 리소스를 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 리소스를 삭제합니다.

           instance_name
               특정 서버 인스턴스에 대한 리소스를 삭제합니다.

피연산자
       jndi-name
           이 리소스의 JNDI 이름입니다.

예제
       예 1, 사용자 정의 리소스 삭제
           이 예제는 mycustomresource라는 사용자 정의 리소스를 삭제합니다.

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일      delete-custom-resource(1)
