list-connector-security-Subcomandos do utilitárlist-connector-security-maps(1)

NOME
       list-connector-security-maps - lista os mapas de segurança que
       pertencem ao pool de conexões do conector especificado

SINOPSE
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

DESCRIçãO
       O subcomando list-connector-security-maps lista os mapas de segurança
       que pertencem ao pool de conexões do conector especificado.

       Para que esse subcomando seja bem sucedido, é necessário ter criado,
       primeiro, um pool de conexões do conector com o uso do subcomando
       create-connector-connection-pool.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --securitymap
           Especifica o nome do mapa de segurança contido no pool de conexões
           do conector a partir do qual a identidade e princípios devem ser
           listados. Com essa opção, --verbose é redundante.

       --verbose
           Se definido como true, retorna uma lista incluindo a identidade,
           princípios e nome de segurança. O default é false.

       --target
           Não especificar essa opção. Esta opção é retida para
           compatibilidade com versões anteriores. se você especificar esta
           opção, um erro de sintaxe não ocorrerá. Em vez disso, o subcomando
           é executado com sucesso e exibe uma mensagem de advertência
           informando que a opção será ignorada.

OPERANDOS
       pool-name
           Nome do pool de conexões do conector para o qual os mapas de
           segurança serão listados.

EXEMPLOS
       Example 1, Listando os Mapas de Segurança do Conector
           Este exemplo lista os mapas de segurança do conector existentes
           para o pool com o nome connector-Pool1.

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                   22 de dezembro de 2list-connector-security-maps(1)
