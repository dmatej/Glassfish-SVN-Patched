delete-custom-resourceasadminユーティリティのサブコマdelete-custom-resource(1)

名前
       delete-custom-resource - カスタム・リソースを除去します

概要
           delete-custom-resource [--help] [--target target] jndi-name

説明
       delete-custom-resourceサブコマンドは、カスタム・リソースを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションは、削除するカスタム・リソースの場所を指定するのに役立ちます。有効なターゲットは、サーバー、ドメイン、クラスタ、およびインスタンスです。デフォルトはサーバーです。

               注記
               ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                                                                           │
               │リソースは常にドメイン全体に対して作成されますが、リソースがアクティブになるのは、リソースの作成時に--targetオプションを使用して<resource-ref>が作成されたターゲットに対してのみです。つまり、リソースの削除では、指定した--targetの<resource-ref>要素のみが削除され、削除対象の--targetとしてdomainを指定しないかぎり、ドメイン全体からリソースが削除されることはありません。 │
               └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

           server
               デフォルトのサーバー・インスタンスのリソースを削除します。この値がデフォルト値です。

           domain
               ドメインのリソースを削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのリソースを削除します。

           instance_name
               特定のサーバー・インスタンスのリソースを削除します。

オペランド
       jndi-name
           リソースのJNDI名。

例
       例 1, カスタム・リソースの削除
           この例では、mycustomresourceという名前のカスタム・リソースを削除します。

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                       2010年12月22日       delete-custom-resource(1)
