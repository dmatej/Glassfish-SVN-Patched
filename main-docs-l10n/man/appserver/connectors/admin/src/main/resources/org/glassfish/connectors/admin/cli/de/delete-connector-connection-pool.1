delete-connector-connectUnterbefehle des asdelete-connector-connection-pool(1)

NAME
       delete-connector-connection-pool - entfernt den angegebenen Connector
       Connection Pool

ZUSAMMENFASSUNG
           delete-connector-connection-pool [--help] [--target target]
           [--cascade={false|true}] poolname

BESCHREIBUNG
       Der Unterbefehl delete-connector-connection-pool entfernt den
       angegebenen Connector Connection Pool.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

       --cascade
           Wenn die Option auf "true" (wahr) gesetzt ist, werden alle mit dem
           Pool verknüpften Connector-Ressourcen und der Pool selbst gelöscht.
           Wenn die Option auf "false" (falsch) gesetzt ist, kann der Pool
           nicht gelöscht werden, wenn mit dem Pool Ressourcen verknüpft sind.
           Die Ressource muss explizit gelöscht werden, oder die Option muss
           auf "true" (wahr) gesetzt werden. Der Standardwert ist "false".

OPERANDEN
       poolname
           Der Name des Connection Pools, der entfernt werden soll.

BEISPIELE
       Beispiel 1, Connector Connection Pools löschen
           In diesem Beispiel wird der Connector Connection Pool namens
           jms/qConnPool gelöscht.

               asadmin> delete-connector-connection-pool
               --cascade=false jms/qConnPool
               Command delete-connector-connection-pool executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-connection-pool(1),
       list-connector-connection-pools(1), ping-connection-pool(1)

       asadmin(1M)

Java EE 7                        14. Sep. 2delete-connector-connection-pool(1)
