delete-custom-resource(1Comandi secondari della utilidelete-custom-resource(1)

NOME
       delete-custom-resource - rimuove una risorsa personalizzata

SINTASSI
           delete-custom-resource [--help] [--target target] jndi-name

DESCRIZIONE
       Il comando secondario delete-custom-resource rimuove una risorsa
       personalizzata.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione consente di specificare la posizione delle risorse
           personalizzate che si desidera eliminare. Le destinazioni valide
           sono server, dominio, cluster e istanza. Il valore predefinito è
           server.

               Nota
               ┌────────────────────────────────────────┐
               │Nota Le risorse vengono sempre create   │
               │per l'intero dominio, ma sono attive    │
               │solo per le destinazioni per le quali è │
               │stato creato un elemento <resource-ref> │
               │utilizzando l'opzione --target al       │
               │momento della creazione della risorsa.  │
               │Ciò significa che l'eliminazione di una │
               │risorsa elimina solo l'elemento         │
               │<resource-ref> per l'opzione --target   │
               │specificata, ma non la risorsa dal      │
               │dominio completo a meno che domain      │
               │venga specificato come --target per     │
               │l'eliminazione.                         │
               └────────────────────────────────────────┘

           server
               Elimina la risorsa dell'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Elimina la risorsa del dominio.

           cluster_name
               Elimina la risorsa per tutte le istanze server del cluster.

           instance_name
               Elimina la risorsa di un'istanza server specifica.

OPERANDI
       jndi-name
           Il nome JNDI della risorsa.

ESEMPI
       Esempio 1, Eliminazione di una risorsa personalizzata
           In questo esempio viene eliminata la risorsa personalizzata
           denominata mycustomresource.

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010      delete-custom-resource(1)
