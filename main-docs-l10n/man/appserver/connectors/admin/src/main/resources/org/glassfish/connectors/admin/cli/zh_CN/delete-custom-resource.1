delete-custom-resource(1)   asadmin 实用程序子命令   delete-custom-resource(1)

名称
       delete-custom-resource - 删除定制资源

提要
           delete-custom-resource [--help] [--target target] jndi-name

说明
       delete-custom-resource 子命令删除定制资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项帮助指定要删除的定制资源的位置。有效的目标是服务器、域、集群和实例。默认目标为服务器。

               注意
               ┌─────────────────────────────────────────────────┐
               │注                                               │
               │始终为整个域创建资源，但资源仅在创建资源时使用   │
               │--target 选项为其创建 <resource-ref>             │
               │的目标上处于活动状态。这意味着删除资源仅删除指定 │
               │--target 中的 <resource-ref>                     │
               │元素，并不会从整个域中删除资源，除非将           │
               │domain 指定为要删除的 --target。                 │
               └─────────────────────────────────────────────────┘

           server
               删除默认服务器实例的资源。这是默认值。

           domain
               删除域的资源。

           cluster_name
               删除集群中每个服务器实例的资源。

           instance_name
               删除特定服务器实例的资源。

操作数
       jndi-name
           此资源的 JNDI 名称。

示例
       Example 1, 删除定制资源
           此示例删除名为 mycustomresource 的定制资源。

               asadmin> delete-custom-resource mycustomresource
               Command delete-custom-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-custom-resource(1), list-custom-resources(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日    delete-custom-resource(1)
