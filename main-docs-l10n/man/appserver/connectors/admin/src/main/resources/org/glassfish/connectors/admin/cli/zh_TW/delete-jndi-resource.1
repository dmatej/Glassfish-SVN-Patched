delete-jndi-resource(1)     asadmin 公用程式子命令     delete-jndi-resource(1)

參考名
       delete-jndi-resource - 移除 JNDI 資源

概要
           delete-jndi-resource [--help] [--target target] jndi_name

描述
       delete-jndi-resource 子命令會移除指定的 JNDI
       資源。在執行此子命令之前，您必須移除 JNDI 資源的所有關聯。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           有效目標說明如下。

               注意
               ┌─────────────────────────────────────────────────────┐
               │備註                                                 │
               │資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
               │--target 選項建立 <resource-ref>                     │
               │的目標。這表示刪除資源只會刪除指定                   │
               │--target 的 <resource-ref>                           │
               │元素，而不會刪除完整網域的資源，除非已將             │
               │domain 指定為要刪除的 --target。                     │
               └─────────────────────────────────────────────────────┘

           server
               從預設伺服器執行處理刪除資源。此為預設值

           domain
               從網域刪除資源

           cluster_name
               刪除叢集中每個伺服器執行處理的資源

           instance_name
               從指定的伺服器執行處理刪除資源

運算元
       jndi_name
           要移除的 JNDI 資源名稱。

範例
       範例 1, 刪除 JNDI 資源
           此範例會移除名為 sample_jndi_resource 的現有 JNDI 資源。

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-jndi-resource(1)
