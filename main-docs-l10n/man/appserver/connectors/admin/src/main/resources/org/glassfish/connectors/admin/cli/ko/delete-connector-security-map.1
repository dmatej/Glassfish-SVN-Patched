delete-connector-security-asadmin 유틸리티 하위

제목
       delete-connector-security-map - 지정된 커넥터 접속 풀에 대한 보안 맵
       삭제

시놉시스
           delete-connector-security-map [--help]
           --poolname connector_connection_pool_name [--target target] mapname

설명
       delete-connector-security-map 하위 명령은 지정된 커넥터 접속 풀에 대한
       보안 맵을 삭제합니다.

       이 하위 명령을 성공하려면 먼저 create-connector-connection-pool 하위
       명령을 사용하여 커넥터 접속 풀을 생성해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --poolname
           삭제할 보안 맵이 속한 커넥터 접속 풀의 이름을 지정합니다.

       --target
           이 옵션은 지정하지 마십시오. 이 옵션은 이전 릴리스와의 호환성을
           위해 보존됩니다. 이 옵션을 지정하면 구문 오류가 발생하지 않습니다.
           대신, 하위 명령이 성공적으로 실행되고 옵션이 무시되었다는 경고
           메시지가 표시됩니다.

피연산자
       mapname
           삭제할 보안 맵의 이름입니다.

예제
       예 1, 커넥터 보안 맵 삭제
           이 예제는 connector-pool1이라는 기존 접속 풀에 대한 securityMap1을
           삭제합니다.

               asadmin> delete-connector-security-map
               --poolname connector-pool1 securityMap1
               Command delete-connector-security-map executed successfully

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-connector-security-map(1), list-connector-security-maps(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                       2010년 9월 14일
