list-custom-resources(1)  asadmin 유틸리티 하위 명령; list-custom-resources(1)

제목
       list-custom-resources - 모든 사용자 정의 리소스 가져오기

시놉시스
           list-custom-resources [--help] [target]

설명
       list-custom-resources 하위 명령은 모든 사용자 정의 리소스를 나열합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           이 피연산자는 사용자 정의 리소스의 위치를 지정합니다. 적합한 대상은
           다음과 같습니다.

           server
               기본 서버 인스턴스의 리소스를 나열합니다. 이 값은 기본값입니다.

           domain
               도메인의 리소스를 나열합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스의 리소스를 나열합니다.

           instance_name
               특정 서버 인스턴스의 리소스를 나열합니다.

예제
       예 1, 사용자 정의 리소스 나열
           이 예제는 현재 사용자 정의 리소스를 나열합니다.

               asadmin> list-custom-resources
               sample_custom_resource01
               sample_custom_resource02
               Command list-custom-resources executed successfully.

       예 2, 대상에서 list-custom-resources 명령 사용
           다음 예제는 이 명령의 사용법을 표시합니다.

               asadmin> list-custom-resources --user admin --passwordfile
               passwords.txt --host plum --port 4848 target6

               sample_custom_resource03
               sample_custom_resource04
               Command list-custom-resources executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-custom-resource(1),delete-custom-resource(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일       list-custom-resources(1)
