update-connector-work-seSubcomandos1de laupdate-connector-work-security-map(1)

NOMBRE DE REFERENCIA
       update-connector-work-security-map - modifica una asignación de
       seguridad de trabajo para el adaptador de recursos especificado.

SINOPSIS
           update-connector-work-security-map [--help] --raname raname
           [--addprincipals eis-principal1=server-principal1[, eis-principal2=server-principal2]*]
           [--addgroups eis-group1=server-group1[, eis-group2=server-group2]*]
           [--removeprincipals eis-principal1[,eis-principal2]*]
           [--removegroups eis-group1[, eis-group2]*]
           mapname

DESCRIPCIÓN
       El subcomando update-connector-work-security-map modifica una
       asignación de seguridad de trabajo para el adaptador de recursos
       especificado.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --addgroups
           Especifica una lista separada por comas de grupos EIS que se deben
           agregar. Usa la opción --addprincipals o la opción --addgroups,
           pero no ambas.

       --addprincipals
           Especifica una lista separada por comas de principales específicos
           de EIS que se deben agregar. Usa la opción --addprincipals o la
           opción --addgroups, pero no ambas.

       --removegroups
           Especifica una lista separada por comas de grupos EIS que se deben
           eliminar.

       --removeprincipals
           Especifica una lista separada por comas de principales específicos
           de EIS que se deben eliminar.

       --raname
           Indica el nombre del módulo de conector al que está asociado el
           mapa de seguridad de trabajo.

OPERANDOS
       mapname
           El nombre de la asignación de seguridad de trabajo que se debe
           actualizar.

EJEMPLOS
       Ejemplo 1, Actualización de un asignación de seguridad de trabajo de
       conector
           En este ejemplo se actualiza workSecurityMap2 mediante la
           eliminación de eis-group-2.

               asadmin> update-connector-work-security-map
               --raname my-resource-adapter --removegroups eis-group-2 workSecurityMap2
               Command update-connector-work-security-map executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-connector-work-security-map(1),
       delete-connector-work-security-map(1),
       list-connector-work-security-maps(1)

       asadmin(1M)

Java EE 7                    11 de febrerupdate-connector-work-security-map(1)
