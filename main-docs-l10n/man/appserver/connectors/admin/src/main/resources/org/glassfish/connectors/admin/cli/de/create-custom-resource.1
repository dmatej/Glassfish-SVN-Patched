create-custom-resource(1Unterbefehle des asadmin-Utilcreate-custom-resource(1)

NAME
       create-custom-resource - erstellt eine benutzerdefinierte Ressource

ZUSAMMENFASSUNG
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

BESCHREIBUNG
       Der Unterbefehl create-custom-resource erstellt eine benutzerdefiniert
       Ressource. Eine benutzerdefinierte Ressource gibt eine
       benutzerdefinierte serverweite Ressourcenobjekt-Factory an, die die
       Schnittstelle javax.naming.spi.ObjectFactory implementiert.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Das Ziel, auf dem die benutzerdefinierte Ressource, die Sie
           erstellen, verfügbar ist. Gültige Werte sind:

           server
               Die Ressource steht auf der standardmäßigen Serverinstanz und
               allen auf der Instanz gehosteten Domains zur Verfügung. Dies
               ist der Standardwert.

           domain
               Die Ressource steht nur auf der angegebenen Domain zur
               Verfügung.

           cluster_name
               Die Ressource steht auf jeder Serverinstanz im Cluster zur
               Verfügung.

           instance_name
               Die Ressource steht nur auf der angegebenen Serverinstanz zur
               Verfügung.

               Anmerkung
               ┌────────────────────────────────────────┐
               │Hinweis Die Ressource wird immer für    │
               │die gesamte Domain erstellt, doch       │
               │resource-ref für die Ressource wird nur │
               │für das angegebene Ziel (--target)      │
               │erstellt. Das bedeutet, dass die        │
               │Ressource, obwohl sie auf Domainebene   │
               │definiert wird, nur auf der angegebenen │
               │Zielebene verfügbar ist. Verwenden Sie  │
               │den Unterbefehl create-resource-ref, um │
               │die Ressource bei Bedarf in mehreren    │
               │Zielen zu referenzieren.                │
               └────────────────────────────────────────┘

       --restype
           Der Typ der benutzerdefinierten Ressource, die erstellt werden
           soll. Geben Sie eine vollqualifizierte Typdefinition an (Beispiel:
           javax.naming.spi.ObjectFactory). Für die Ressourcentypdefinition
           gilt das Format xxx.xxx.

       --factoryclass
           Factory-Klassenname für die benutzerdefinierte Ressource. Diese
           Klasse implementiert die Schnittstelle
           javax.naming.spi.ObjectFactory.

       --enabled
           Legt fest, ob die benutzerdefinierte Ressource zur Laufzeit
           aktiviert ist. Der Standardwert ist "true".

       --description
           Text mit Einzelheiten zur benutzerdefinierten Ressource. Bei dieser
           Beschreibung handelt es sich um einen Zeichenfolgenwert, der
           maximal 250 Zeichen umfassen darf.

       --property
           Optionale Name/Wert-Paare für Attribute zum Konfigurieren der
           Ressource.

OPERANDEN
       jndi-name
           Der JNDI-Name dieser Ressource.

BEISPIELE
       Beispiel 1, Benutzerdefinierte Ressourcen erstellen
           In diesem Beispiel wird eine benutzerdefinierte Ressource erstellt.

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011       create-custom-resource(1)
