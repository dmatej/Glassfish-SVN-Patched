list-connector-security-maasadmin 유틸리티 하위list-connector-security-maps(1)

제목
       list-connector-security-maps - 지정된 커넥터 접속 풀에 속한 보안 맵
       나열

시놉시스
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

설명
       list-connector-security-maps 하위 명령은 지정된 커넥터 접속 풀에 속한
       보안 맵을 나열합니다.

       이 하위 명령을 성공하려면 먼저 create-connector-connection-pool 하위
       명령을 사용하여 커넥터 접속 풀을 생성해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --securitymap
           ID 및 주체를 나열할 커넥터 접속 풀 내에 포함된 보안 맵의 이름을
           지정합니다. 이 옵션을 사용하면 --verbose가 중복됩니다.

       --verbose
           true로 설정하면 ID, 주체 및 보안 이름을 포함하는 목록을 반환합니다.
           기본값은 false입니다.

       --target
           이 옵션은 지정하지 마십시오. 이 옵션은 이전 릴리스와의 호환성을
           위해 보존됩니다. 이 옵션을 지정하면 구문 오류가 발생하지 않습니다.
           대신, 하위 명령이 성공적으로 실행되고 옵션이 무시되었다는 경고
           메시지가 표시됩니다.

피연산자
       pool-name
           보안 맵을 나열할 커넥터 접속 풀의 이름입니다.

예제
       예 1, 커넥터 보안 맵 나열
           이 예제는 connector-Pool1이라는 풀에 대한 기존 커넥터 보안 맵을
           나열합니다.

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                      2010년 12월 22일list-connector-security-maps(1)
