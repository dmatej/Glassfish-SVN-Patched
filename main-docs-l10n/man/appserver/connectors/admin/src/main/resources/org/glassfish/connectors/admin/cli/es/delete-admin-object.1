delete-admin-object(1)  Subcomandos de la Utilidad asa  delete-admin-object(1)

NOMBRE DE REFERENCIA
       delete-admin-object - Elimina el objeto administrado con el nombre de
       JNDI especificado.

SINOPSIS
           delete-admin-object [--help] [--target target] jndi_name

DESCRIPCIÓN
       El subcomando
        delete-admin-object elimina un objeto administrado con el nombre de
       JNDI especificado.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Éste es el nombre de los destinos para los que se va a suprimir el
           objeto administrado. Los valores válidos son:

               Nota
               ┌────────────────────────────────────────┐
               │Nota Los recursos siempre se crean para │
               │un dominio completo, pero sólo están    │
               │activos para los destinos para los que  │
               │se ha creado una <resource-ref>         │
               │mediante la opción --target al crear el │
               │recurso. Esto significa que al suprimir │
               │un recurso sólo se suprime el elemento  │
               │<resource-ref> para el --target         │
               │especificado y no se suprime el recurso │
               │del dominio completo a menos que se     │
               │especifique domain como --target de la  │
               │supresión.                              │
               └────────────────────────────────────────┘

           server
               Suprime el objeto administrado para la instancia de servidor
               por defecto server y es el valor por defecto.

           configuration_name
               Suprime el objeto administrado para la configuración
               especificada.

           cluster_name
               Suprime el objeto administrado para el cluster especificado.

           instance_name
               Suprime el objeto administrado para una instancia de servidor
               determinada.

OPERANDOS
       jndi_name
           Nombre de JNDI del objeto administrado que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Objeto Administrado
           Este ejemplo suprime el objeto administrado denominado
           jms/samplelqueue.

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                  14 de septiembre de 2010     delete-admin-object(1)
