create-connector-securitComandi)secondari dellcreate-connector-security-map(1)

NOME
       create-connector-security-map - crea una mappa di sicurezza per il
       connection pool del connettore specificato

SINTASSI
           create-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--principals principal-name1[,principal-name2]*]
           [--usergroups user-group1[,user-group2]*]
           [--mappedusername user-name]
           [--target target]
           mapname

DESCRIZIONE
       Il comando secondario create-connector-security-map crea una mappa di
       sicurezza per il connection pool del connettore specificato. Se non è
       presente una mappa di sicurezza, ne viene creata una nuova. Questo
       comando secondario consente inoltre di mappare l'identità del chiamante
       dell'applicazione (principal o gruppo di utenti) al principal di un
       sistema EIS (Enterprise Information System) adeguato negli scenari di
       autenticazione gestiti da contenitore. Per sistema EIS (Enterprise
       Information System) si intende qualsiasi sistema contenente i dati di
       un'organizzazione. Può essere un mainframe, un sistema di
       messaggistica, un sistema di database o un'applicazione. È possibile
       associare una o più mappe di sicurezza denominate a un connection pool
       di connettore. La configurazione della mappa di sicurezza connettore
       supporta l'uso del carattere jolly asterisco (*) per indicare tutti gli
       utenti o tutti i gruppi.

       Per specificare la password EIS, è possibile aggiungere la voce
       AS_ADMIN_MAPPEDPASSWORD al password file, quindi specificare il file
       utilizzando l'opzione --passwordfile della utility asadmin.

       Affinché l'esecuzione di questo comando secondario riesca è necessario
       creare preliminarmente un connection pool di connettore mediante il
       comando secondario create-connector-connection-pool.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --poolname
           Specifica il nome del connection pool del connettore a cui
           appartiene la mappa di sicurezza.

       --principals
           Specifica una lista di principal EIS backend. È possibile
           specificare più principal utilizzando una lista separata da
           virgole. Usare l'opzione --principals o --usergroups, ma non
           entrambe le opzioni, nello stesso comando.

       --usergroups
           Specifica una lista di gruppi di utenti EIS backend. È possibile
           specificare più gruppi di utenti utilizzando una lista separata da
           virgole. Usare l'opzione --principals o --usergroups, ma non
           entrambe le opzioni, nello stesso comando.

       --mappedusername
           Specifica il nome utente EIS.

       --target
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

OPERANDI
       mapname
           Il nome della mappa di sicurezza da creare.

ESEMPI
       Esempio 1, Creazione di una mappa di sicurezza di un connettore
           In questo esempio viene creata la mappa securityMap1 per il
           connection pool esistente denominato connector-pool1.

               asadmin> create-connector-security-map --poolname connector-pool1
               --principals principal1,principal2 --mappedusername backend-username securityMap1
               Command create-connector-security-map executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-connector-security-map(1), list-connector-security-maps(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                      22 dicembre 201create-connector-security-map(1)
