delete-jndi-resource(1) Unterbefehle des asadmin-Utili delete-jndi-resource(1)

NAME
       delete-jndi-resource - entfernt eine JNDI-Ressource

ZUSAMMENFASSUNG
           delete-jndi-resource [--help] [--target target] jndi_name

BESCHREIBUNG
       Der Unterbefehl delete-jndi-resource entfernt die angegebene
       JNDI-Ressource. Sie müssen alle Verknüpfungen mit der JNDI-Ressource
       entfernen, bevor Sie diesen Unterbefehl ausführen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gültige Ziele sind nachstehend beschrieben.

               Anmerkung
               ┌────────────────────────────────────────┐
               │Hinweis Ressourcen werden stets für     │
               │eine Domain als Ganzes erstellt, sind   │
               │aber nur für Ziele aktiv, für die bei   │
               │der Erstellung der Ressource eine       │
               │<resource-ref> mit der Option --target  │
               │erstellt wurde. Das bedeutet, dass beim │
               │Löschen einer Ressource nur das         │
               │<resource-ref>-Element für das          │
               │angegebene Ziel (--target) und nicht    │
               │die Ressource aus der Domain als Ganzes │
               │gelöscht wird, es sei denn, domain ist  │
               │als --target für den Löschvorgang       │
               │angegeben.                              │
               └────────────────────────────────────────┘

           server
               Löscht die Ressource aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert

           domain
               Löscht die Ressource aus der Domain

           cluster_name
               Löscht die Ressource für jede Serverinstanz im Cluster

           instance_name
               Löscht die Ressource aus der angegebenen Serverinstanz

OPERANDEN
       jndi_name
           Der Name der JNDI-Ressource, die entfernt werden soll.

BEISPIELE
       Beispiel 1, JNDI-Ressourcen löschen
           In diesem Beispiel wird eine vorhandene JNDI-Ressource namens
           sample_jndi_resource entfernt.

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011         delete-jndi-resource(1)
