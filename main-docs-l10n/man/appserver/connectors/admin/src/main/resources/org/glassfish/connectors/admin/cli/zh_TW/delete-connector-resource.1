delete-connector-resource(1)asadmin 公用程式子命令delete-connector-resource(1)

參考名
       delete-connector-resource - 移除具有指定 JNDI 名稱的連線器資源

概要
           delete-connector-resource [--help] [--target target] jndi_name

描述
       delete-connector-resource 子命令會移除具有指定 JNDI 名稱的連線器資源。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項指定要從中移除連線器資源的目標。有效目標包括：

               注意
               ┌─────────────────────────────────────────────────────┐
               │備註                                                 │
               │資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
               │--target 選項建立 <resource-ref>                     │
               │的目標。這表示刪除資源只會刪除指定                   │
               │--target 的 <resource-ref>                           │
               │元素，而不會刪除完整網域的資源，除非已將             │
               │domain 指定為要刪除的 --target。                     │
               └─────────────────────────────────────────────────────┘

           server
               從預設伺服器執行處理刪除連線器資源。此為預設值。

           domain
               從網域刪除連線器資源。

           cluster_name
               從叢集中的每個伺服器執行處理刪除連線器資源。

           instance_name
               從指定的伺服器執行處理刪除連線器資源。

運算元
       jndi_name
           此連線器資源的 JNDI 名稱。

範例
       範例 1, 刪除連線器資源
           此範例會刪除名為 jms/qConnFactory 的連線器資源。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       範例 2, 使用 delete-connector-resource 子命令
           此範例顯示此子命令的使用方法。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           其中 jms/qConnFactory 是移除的連線器資源。

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日  delete-connector-resource(1)
