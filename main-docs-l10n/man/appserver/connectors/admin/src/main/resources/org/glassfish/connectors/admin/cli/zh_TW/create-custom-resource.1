create-custom-resource(1)   asadmin 公用程式子命令   create-custom-resource(1)

參考名
       create-custom-resource - 建立自訂資源

概要
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

描述
       create-custom-resource 子命令會建立自訂資源。自訂資源會指定實行
       javax.naming.spi.ObjectFactory 介面的自訂伺服器範圍資源物件工廠。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           您在其上建立的自訂資源將可供使用的目標。有效值包括

           server
               預設伺服器執行處理及執行處理上託管的所有網域中均可取得資源。
               此為預設值。

           domain
               僅指定的網域可取得資源。

           cluster_name
               叢集內所有伺服器執行處理中均可取得資源。

           instance_name
               僅指定的伺服器執行處理可取得資源。

               注意
               ┌───────────────────────────────────────────────────────────────────────────────────────┐
               │備註                                                                                   │
               │資源皆是為整個網域所建立，但只會為指定的                                               │
               │--target 建立資源的                                                                    │
               │resource-ref。這表示雖然資源定義於網域層級，但只能用於所指定的目標層級。請視需要，使用 │
               │create-resource-ref                                                                    │
               │子命令來參照多個目標中的資源。                                                         │
               └───────────────────────────────────────────────────────────────────────────────────────┘

       --restype
           要建立之自訂資源的類型。指定一個完全合格的類型定義，例如
           javax.naming.spi.ObjectFactory。資源類型定義的格式為 xxx.xxx。

       --factoryclass
           自訂資源的工廠類別名稱。該類別實行 javax.naming.spi.ObjectFactory
           介面。

       --enabled
           決定是否在執行階段中啟用自訂資源。預設值為 true。

       --description
           提供有關自訂資源詳細資料的文字。該描述是一個字串值，最多可以包含
           250 個字元。

       --property
           用於組態資源的選擇性屬性名/值配對。

運算元
       jndi-name
           此資源的 JNDI 名稱。

範例
       範例 1, 建立自訂資源
           此範例會建立自訂資源。

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日     create-custom-resource(1)
