create-connector-resourcUnterbefehle des asadmin-Ucreate-connector-resource(1)

NAME
       create-connector-resource - registriert die Connector-Ressource mit dem
       angegebenen JNDI-Namen

ZUSAMMENFASSUNG
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

BESCHREIBUNG
       Der Unterbefehl create-connector-resource registriert die
       Connector-Ressource mit dem angegebenen JNDI-Namen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --poolname
           Der Name des Connection Pools. Wenn zwei oder mehr
           Ressourcenelemente auf dasselbe Connection Pool-Element verweisen,
           verwenden sie während der Laufzeit dieselben Poolverbindungen.

       --enabled
           Diese Option legt fest, ob die Ressource während der Laufzeit
           aktiviert wird. Der Standardwert ist "true".

       --objecttype
           Definiert den Typ der Connector-Ressource. Die Standardeinstellung
           lautet "user" (Benutzer). Zulässige Werte sind:

           system-all
               Eine Systemressource für alle Serverinstanzen und den DAS
               (Domain Administration Server).

           system-admin
               Eine Systemressource, die nur für den DAS bestimmt ist.

           system-instance
               Eine Systemressource, die nur für alle Serverinstanzen bestimmt
               ist.

           user
               Eine Benutzerressource.

       --description
           Text mit Einzelheiten zur Connector-Ressource.

       --property
           Optionale Name/Wert-Paare für Attribute zum Konfigurieren der
           Ressource.

       --target
           Diese Option gibt das Endverzeichnis der Connector-Ressourcen an.
           Gültige Ziele sind:

           server
               Erstellt die Connector-Ressource in der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           domain
               Erstellt die Connector-Ressource in der Domain.

           cluster_name
               Erstellt die Connector-Ressource in jeder Serverinstanz im
               Cluster.

           instance_name
               Erstellt die Connector-Ressource in der angegebenen
               Serverinstanz.

               Note
               ┌────────────────────────────────────────┐
               │Hinweis Die Ressource wird immer für    │
               │die gesamte Domain erstellt, doch       │
               │resource-ref für die Ressource wird nur │
               │für das angegebene Ziel (--target)      │
               │erstellt. Das bedeutet, dass die        │
               │Ressource, obwohl sie auf Domainebene   │
               │definiert wird, nur auf der angegebenen │
               │Zielebene verfügbar ist. Verwenden Sie  │
               │den Unterbefehl create-resource-ref, um │
               │die Ressource bei Bedarf in mehreren    │
               │Zielen zu referenzieren.                │
               └────────────────────────────────────────┘

OPERANDEN
       jndi_name
           Der JNDI-Name dieser Connector-Ressource.

BEISPIELE
       Example 1, Connector-Ressourcen erstellen
           In diesem Beispiel wird eine Connector-Ressource namens
           jms/qConnFactory erstellt.

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       Example 2, Unterbefehl "create-connector-resource" verwenden
           In diesem Beispiel wird die Verwendung dieses Unterbefehls gezeigt.

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           jms/qConnFactory ist die Beispiel-Connector-Ressource, die erstellt
           wird.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                        10. Feb. 2011    create-connector-resource(1)
