create-admin-object(1)  Subcomandos do utilitário asad  create-admin-object(1)

NOME
       create-admin-object - adiciona o objeto administrado com o nome da JNDI
       especificado a um adaptador de recurso

SINOPSE
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

DESCRIçãO
       O subcomando create-admin-object cria um objeto administrado com o nome
       da JNDI especificado e a definição de interface para um adaptador de
       recurso.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo onde o objeto administrado está sendo criado. Os
           valores válidos são os seguintes:

           server
               Cria um objeto administrado para a instância de servidor
               default server e é o valor default.

           configuration_name
               Cria o objeto administrado para a configuração nomeada.

           cluster_name
               Cria o objeto administrado para cada instância de servidor no
               cluster.

           instance_name
               Cria o objeto administrado para uma instância de servidor
               específica.

               Nota
               ┌───────────────────────────────────────┐
               │Observação O recurso sempre é criado   │
               │para o domínio como um todo, mas       │
               │resource-ref do recurso só é criado    │
               │para o --target especificado. Isso     │
               │significa que, embora o recurso esteja │
               │definido no nível do domínio, ele      │
               │somente está disponível no alvo        │
               │especificado. Utilize o subcomando     │
               │create-resource-ref para fazer         │
               │referência ao recurso em vários alvos, │
               │se necessário.                         │
               └───────────────────────────────────────┘

       --restype
           Especifica a definição de interface para o objeto administrado. O
           tipo de recurso deve ser uma definição de interface que é
           especificada no arquivo ra.xml do adaptador de recurso.

       --classname
           Especifica o nome da classe do objeto administrado. É solicitado se
           vários objetos administrados utilizarem a mesma definição de
           interface.

       --raname
           Especifica o nome do adaptador de recurso associado com esse objeto
           administrado.

       --enabled
           Especifica se o objeto está ativado. O default é verdadeiro.

       --description
           String de texto que descreve o objeto administrado.

       --property
           Descrição dos pares de nomes/valores para configurar o recurso.
           Dependente do adaptador de recurso. Para propriedades do JMS,
           consulte create-jms-resource(1) para ver recursos do destino JMS.

OPERANDOS
       jndi_name
           Nome da JNDI do objeto administrado a ser criado.

EXEMPLOS
       Example 1, Criando um Objeto Administrado
           Neste exemplo, jmsra é um adaptador de recurso do sistema com as
           interfaces de objeto admin, javax.jms.Queue e javax.jms.Topic.

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                   10 de fevereiro de 2011     create-admin-object(1)
