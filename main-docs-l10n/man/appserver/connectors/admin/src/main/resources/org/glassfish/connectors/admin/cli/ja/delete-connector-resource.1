delete-connector-resouasadminユーティリティのサブコ

名前
       delete-connector-resource -
       指定したJNDI名のコネクタ・リソースを除去します

概要
           delete-connector-resource [--help] [--target target] jndi_name

説明
       delete-connector-resourceサブコマンドは、指定したJNDI名のコネクタ・リソースを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションでは、コネクタ・リソースを除去するターゲットを指定します。有効なターゲットは次のとおりです:

               注記
               ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注意                                                                                                                                                                                                                                                                                                                                                                           │
               │リソースは常にドメイン全体に対して作成されますが、リソースがアクティブになるのは、リソースの作成時に--targetオプションを使用して<resource-ref>が作成されたターゲットに対してのみです。つまり、リソースの削除では、指定した--targetの<resource-ref>要素のみが削除され、削除対象の--targetとしてdomainを指定しないかぎり、ドメイン全体からリソースが削除されることはありません。 │
               └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

           server
               デフォルトのサーバー・インスタンスからコネクタ・リソースを削除します。これがデフォルト値です。

           domain
               ドメインからコネクタ・リソースを削除します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスからコネクタ・リソースを削除します。

           instance_name
               指定したサーバー・インスタンスからコネクタ・リソースを削除します。

オペランド
       jndi_name
           このコネクタ・リソースのJNDI名。

例
       例 1, コネクタ・リソースの削除
           この例では、jms/qConnFactoryという名前のコネクタ・リソース削除します。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       例 2, delete-connector-resourceサブコマンドの使用
           この例では、このサブコマンドの使用方法を示します。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           jms/qConnFactoryは、除去されるコネクタ・リソースです。

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                        2011年2月11日    delete-connector-resource(1)
