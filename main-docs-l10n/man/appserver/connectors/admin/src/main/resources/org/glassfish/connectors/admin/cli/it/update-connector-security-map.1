update-connector-securitComandi)secondari dellupdate-connector-security-map(1)

NOME
       update-connector-security-map - modifica una mappa di sicurezza per il
       connection pool connettore specificato

SINTASSI
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

DESCRIZIONE
       Il comando secondario update-connector-security-map modifica una mappa
       di sicurezza per il connection pool connettore specificato.

       Affinché l'esecuzione di questo comando secondario riesca è necessario
       creare preliminarmente un connection pool di connettore mediante il
       comando secondario create-connector-connection-pool.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --poolname
           Specifica il nome del connection pool di connettore a cui
           appartiene la mappa di sicurezza da aggiornare.

       --addprincipals
           Specifica una lista separata da virgole di principal specifici di
           EIS da aggiungere. Usare l'opzione --addprincipals o
           --addusergroups, ma non entrambe nello stesso comando.

       --addusergroups
           Specifica una lista separata da virgole dei gruppi di utenti EIS da
           aggiungere. Usare l'opzione --addprincipals o --addusergroups, ma
           non entrambe nello stesso comando.

       --removeprincipals
           Specifica una lista separata da virgole di principal specifici di
           EIS da rimuovere.

       --removeusergroups
           Specifica una lista separata da virgole dei gruppi di utenti EIS da
           rimuovere.

       --mappedusername
           Specifica il nome utente EIS.

OPERANDI
       mapname
           Il nome della mappa di sicurezza da aggiornare.

ESEMPI
       Esempio 1, Aggiornamento di una mappa di sicurezza di un connettore
           In questo esempio vengono aggiunti principal alla mappa di
           sicurezza esistente denominata securityMap1.

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                      15 settembre 20update-connector-security-map(1)
