create-connector-resource(1)asadmin 公用程式子命令create-connector-resource(1)

參考名
       create-connector-resource - 以指定的 JNDI 名稱註冊連線器資源

概要
           create-connector-resource [--help]
           --poolname connectorConnectionPoolName
           [--enabled={true|false}]
           [--description description]
           [--objecttype ovjecttype]
           [--property (name=value)[:name=value]*]
           [--target target]
           jndi_name

描述
       create-connector-resource 子命令會註冊具有指定 JNDI 名稱的連線器資源。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --poolname
           連線集區的名稱。如果兩個或多個資源元素指向同一個連線集區元素，這些元素將會在執行階段中使用同一個集區連線。

       --enabled
           此選項會決定是否在執行階段中啟用資源。預設值為 true。

       --objecttype
           定義連線器資源類型。預設值為使用者。允許的值包括：

           system-all
               適用於所有伺服器執行處理及網域管理伺服器 (DAS) 的系統資源。

           system-admin
               僅適用於 DAS 的系統資源。

           system-instance
               僅適用於所有伺服器執行處理的系統資源。

           user
               使用者資源。

       --description
           提供有關連線器資源詳細資料的文字。

       --property
           用於組態資源的選擇性屬性名/值配對。

       --target
           此選項會指定連線器資源的最終位置。有效目標包括：

           server
               在預設伺服器執行處理中建立連線器資源。此為預設值。

           domain
               在網域中建立連線器資源。

           cluster_name
               為叢集中每個伺服器執行處理建立連線器資源。

           instance_name
               在指定的伺服器執行處理中建立連線器資源。

               注意
               ┌───────────────────────────────────────────────────────────────────────────────────────┐
               │備註                                                                                   │
               │資源皆是為整個網域所建立，但只會為指定的                                               │
               │--target 建立資源的                                                                    │
               │resource-ref。這表示雖然資源定義於網域層級，但只能用於所指定的目標層級。請視需要，使用 │
               │create-resource-ref                                                                    │
               │子命令來參照多個目標中的資源。                                                         │
               └───────────────────────────────────────────────────────────────────────────────────────┘

運算元
       jndi_name
           此連線器資源的 JNDI 名稱。

範例
       範例 1, 建立連線器資源
           此範例會建立名稱為 jms/qConnFactory 的連線器資源。

               asadmin> create-connector-resource --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully

       範例 2, 使用 create-connector-resource 子命令
           此範例顯示此子命令的使用方法。

               asadmin> create-connector-resource --target server --poolname jms/qConnPool
               --description "sample connector resource" jms/qConnFactory
               Command create-connector-resource executed successfully
           jms/qConnFactory 是建立範例連線器資源的位置。

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-connector-resource(1), list-connector-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 10 日  create-connector-resource(1)
