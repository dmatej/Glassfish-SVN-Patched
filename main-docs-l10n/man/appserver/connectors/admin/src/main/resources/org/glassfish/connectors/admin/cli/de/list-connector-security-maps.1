list-connector-security-Unterbefehle des asadmilist-connector-security-maps(1)

NAME
       list-connector-security-maps - listet die Sicherheitszuordnungen auf,
       die zum angegebenen Connector Connection Pool gehören

ZUSAMMENFASSUNG
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

BESCHREIBUNG
       Der Unterbefehl list-connector-security-maps listet die
       Sicherheitszuordnungen auf, die zum angegebenen Connector Connection
       Pool gehören.

       Damit dieser Unterbefehl erfolgreich ausgeführt wird, müssen Sie
       zunächst mit dem Unterbefehl create-connector-connection-pool einen
       Connector Connection Pool erstellen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --securitymap
           Gibt den Namen der Sicherheitszuordnung an, die in dem Connector
           Connection Pool enthalten ist, aus dem die Identität und die
           Principals aufgelistet werden sollen. Bei der Verwendung dieser
           Option ist --verbose redundant.

       --verbose
           Ist der Wert auf true gesetzt, wird eine Liste mit Identität,
           Principals und Sicherheitsname zurückgegeben. Der Standardwert ist
           false.

       --target
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

OPERANDEN
       pool-name
           Name des Connector Connection Pools, für den Sie
           Sicherheitszuordnungen auflisten möchten.

BEISPIELE
       Beispiel 1, Connector-Sicherheitszuordnungen auflisten
           In diesem Beispiel werden die vorhandenen
           Connector-Sicherheitszuordnungen für den Pool mit dem Namen
           connector-Pool1 aufgelistet.

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010 list-connector-security-maps(1)
