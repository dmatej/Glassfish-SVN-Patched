create-connector-conneasadminユ

名前
       create-connector-connection-pool -
       指定した接続プール名で接続プールを追加します

概要
           create-connector-connection-pool [--help] [--target=target]
           --raname raname
           --connectiondefinition connectiondefinitionname
           [--steadypoolsize steadypoolsize]
           [--maxpoolsize maxpoolsize]
           [--maxwait maxwait]
           [--poolresize poolresize]
           [--idletimeout idletimeout]
           [--isconnectvalidatereq={false|true}]
           [--failconnection={false|true}]
           [--leaktimeout=timeout]
           [--leakreclaim={false|true}]
           [--creationretryattempts=attempts]
           [--creationretryinterval=interval]
           [--lazyconnectionenlistment={false|true}]
           [--lazyconnectionassociation={false|true}]
           [--associatewiththread={false|true}]
           [--matchconnections={true|false}]
           [--maxconnectionusagecount=count]
           [--validateatmostonceperiod=interval]
           [--transactionsupport transactionsupport]
           [--descrip[tion description]
           [--ping {false|true}]
           [--pooling {true|false}]
           [--property (name=value)[:name=value]*]
           poolname

説明
       create-connector-connection-poolサブコマンドは、エンタープライズ情報システム(Enterprise
       Information
       System、EIS)への接続のプールを定義します。名前付きのプールは、複数のコネクタ・リソースで参照できます。定義された各プールは、サーバー起動時にインスタンス化され、最初のアクセス時に設定されます。2つ以上のコネクタ・リソースが同じコネクタ接続プールを指している場合、それらのリソースは実行時に同じ接続プールを使用します。単一のリソース・アダプタの1つの接続定義に対し、複数のプールを適用できます。

       認証されたコネクタ接続プールは、--propertyオプションを使用してユーザー、パスワード、またはその他の接続情報を指定する方法か、XML記述子ファイルで接続情報を指定する方法のいずれかで作成できます。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --associatewiththread
           接続をスレッドに関連付けて、スレッドが接続を再利用できるようにするかを指定します。接続がスレッドに関連付けられていない場合、スレッドは接続が必要になるたびにプールから接続を取得する必要があります。使用可能な値は次のとおりです:

           false
               接続はスレッドに関連付けられてい

           true
               接続はスレッドに関連付けられています。

       --connectiondefinition
           接続定義の名前。

       --creationretryattempts
           最初の試行が失敗した場合に、サーバーが接続の作成を再試行する最大回数を指定します。

           デフォルト値は0で、サーバーは接続の作成を再試行しない設定です。

       --creationretryinterval
           接続作成の試行の間隔を秒単位で指定します。

           --creationretryattemptsが0の場合、--creationretryintervalオプションは無視されます。デフォルト値は10です。

       --description
           コネクタ接続プールの詳細を説明するテキスト。

       --failconnection
           trueに設定した場合、1つの検証チェックに失敗するとプール内のすべての接続がクローズします。--isconnectvalidatereqオプションがtrueに設定されている場合、このパラメータは必須です。デフォルト値はfalseです。

       --idletimeout
           プールで接続がアイドル状態のままでいられる最長時間。この時間が経過すると、プールはこの接続をクローズできます。デフォルト値は300です。

       --isconnectvalidatereq
           値がtrueに設定されている場合、接続がアプリケーションに渡される前に、使用可能な接続であるかどうかが確認されます。デフォルト値はfalseです。

       --lazyconnectionenlistment
           メソッドが実際にリソースを使用するときのみリソースをトランザクションに登録するかを指定します。デフォルト値はfalseです。

       --lazyconnectionassociation
           物理接続が使用されているときのみ物理接続を論理接続に関連付け、トランザクションが完了したときに関連付けを解除するかを指定します。このようなアソシエーションとアソシエーション解除により、物理接続の再利用が可能になります。使用可能な値は次のとおりです:

           false
               物理接続は、物理接続が使用される前から論理接続に関連付けられており、トランザクションが完了しても関連付けは解除さ

           true
               物理接続は、物理接続が使用されているときのみ論理接続に関連付けられ、トランザクションが完了すると関連付けは解除されます。--lazyconnectionenlistmentオプションもtrueに設定します。

       --leakreclaim
           リーク接続の追跡完了後に、リークした接続を接続プールに復元するかを指定します。使用可能な値は次のとおりです:

           false
               リークした接続は接続プールに復元さ

           true
               リークした接続は接続プールに復元されます。

       --leaktimeout
           接続プール内の接続リークを追跡する時間を秒単位で指定します。

           接続リーク追跡を有効にすると、管理コンソールを使用してJDBC接続プールのモニターを有効にし、接続リーク数の統計を取得できます。デフォルト値は0で、接続リーク追跡は無効です。

       --matchconnections
           プールから選択された接続とリソース・アダプタが一致する必要があるかどうかを指定します。プール内のすべての接続が同一である場合、接続とリソース・アダプタ間の一致は必要ありません。使用可能な値は次のとおりです:

           true
               接続はリソース・アダプタと一致する必要があります(デフォルト)。

           false
               接続はリソース・アダプタと一致する必要はあ

       --maxconnectionusagecount
           接続を再利用できる最大回数を指定します。

           この制限に達すると接続がクローズします。デフォルト値は0で、接続を再利用できる回数は無制限であることを示します。

       --maxpoolsize
           クライアント・リクエストに応じて作成できる接続数の上限。デフォルト値は32です。

       --maxwait
           接続が使用できない場合に、接続が作成されるまでコール側が待機する時間(ミリ秒)。0に設定された場合、コール側は、リソースが使用可能になるまで、またはエラーが発生するまでブロックされたままになります。デフォルト値は60000です。

       --ping
           作成中(または再構成中)にこの属性がtrueに設定されたプールにアクセスし、属性に対する誤った値があればそれを識別して警告します。デフォルト値はfalseです。

       --pooling
           falseに設定すると、この属性は接続プールを無効にします。デフォルト値はtrueです。

       --poolresize
           プールが接続数をスケール・アップまたはスケール・ダウンする単位量。スケール・アップ:
           プールの接続に空きがない場合は、この量のプールがスケール・アップします。スケール・ダウン:
           無効な接続とアイドル接続がすべて除去されます。この値より多くの量の接続が除去されることもあります。--steadypoolsizeによって指定された接続数は確保されます。使用可能な値は0からMAX_INTEGERです。デフォルト値は2です。

       --property
           プールを構成するための属性名と値のペア(オプション)。

           LazyConnectionEnlistment
               非推奨。同等のオプションを使用します。デフォルト値はfalseです。

           LazyConnectionAssociation
               非推奨。同等のオプションを使用します。デフォルト値はfalseです。

           AssociateWithThread
               非推奨。同等のオプションを使用します。デフォルト値はfalseです。

           MatchConnections
               非推奨。同等のオプションを使用します。デフォルト値はfalseです。

       --raname
           リソース・アダプタの名前。

       --steadypoolsize
           プールで維持する接続数の下限および初期値です。デフォルト値は8です。

       --target
           このオプションは指定しないでください。このオプションは、旧リリースとの互換性のために保持されています。このオプションを指定した場合、構文エラーは発生しません。かわりに、サブコマンドが正常に実行され、オプションが無視されたことを示す警告メッセージが表示されます。

       --transactionsupport
           このプールが持つトランザクション・サポートのレベルを示します。使用可能な値は、XATransaction、LocalTransaction、およびNoTransactionです。この属性には、リソース・アダプタのトランザクション・サポート属性値以下の値を指定できます。リソース・アダプタのトランザクション・サポート属性には値の順序があり、XATransactionが最も高く、NoTransactionが最も低くなります。

       --validateatmostonceperiod
           接続を検証するための連続したリクエストの時間間隔を秒単位で一度のみ指定します。この属性を適切な値に設定すると、接続ごとの確認リクエスト回数を最小にできます。デフォルト値は0です。この属性が無効であることを意味します。

オペランド
       poolname
           作成される接続プールの名前。

例
       例 1, コネクタ接続プールの作成
           この例では、jms/qConnPoolという名前の新しいコネクタ接続プールを作成します。

               asadmin> create-connector-connection-pool --raname jmsra
               --connectiondefinition javax.jms.QueueConnectionFactory --steadypoolsize 20
               --maxpoolsize 100 --poolresize 2 --maxwait 60000 jms/qConnPool
               Command create-connector-connection-pool executed successfully

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-connector-connection-pool(1),
       list-connector-connection-pools(1), ping-connection-pool(1)

       asadmin(1M)

Java EE 7                        2010年9月9create-connector-connection-pool(1)
