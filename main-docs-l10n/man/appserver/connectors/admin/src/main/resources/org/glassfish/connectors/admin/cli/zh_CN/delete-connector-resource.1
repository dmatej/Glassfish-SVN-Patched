delete-connector-resource(1)asadmin 实用程序子命令delete-connector-resource(1)

名称
       delete-connector-resource - 删除具有指定 JNDI 名称的连接器资源

提要
           delete-connector-resource [--help] [--target target] jndi_name

说明
       delete-connector-resource 子命令删除具有指定 JNDI 名称的连接器资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要从其中删除连接器资源的目标。有效目标包括：

               注意
               ┌─────────────────────────────────────────────────┐
               │注                                               │
               │始终为整个域创建资源，但资源仅在创建资源时使用   │
               │--target 选项为其创建 <resource-ref>             │
               │的目标上处于活动状态。这意味着删除资源仅删除指定 │
               │--target 中的 <resource-ref>                     │
               │元素，并不会从整个域中删除资源，除非将           │
               │domain 指定为要删除的 --target。                 │
               └─────────────────────────────────────────────────┘

           server
               从默认服务器实例删除连接器资源。这是默认值。

           domain
               从域删除连接器资源。

           cluster_name
               从集群中的每个服务器实例删除连接器资源。

           instance_name
               从指定的服务器实例删除连接器资源。

操作数
       jndi_name
           此连接器资源的 JNDI 名称。

示例
       Example 1, 删除连接器资源
           此示例删除名为 jms/qConnFactory 的连接器资源。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       Example 2, 使用 delete-connector-resource 子命令
           此示例显示了此子命令的用法。

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           其中，jms/qConnFactory 是删除的连接器资源。

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日  delete-connector-resource(1)
