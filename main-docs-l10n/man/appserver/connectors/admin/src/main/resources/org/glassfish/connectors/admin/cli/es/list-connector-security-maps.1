list-connector-security-Subcomandos de la Utililist-connector-security-maps(1)

NOMBRE DE REFERENCIA
       list-connector-security-maps - Muestra los mapas de seguridad que
       pertenecen al pool especificado de conexiones de conector.

SINOPSIS
           list-connector-security-maps [--help] [--securitymap securitymap]
           [--verbose={false|true}] [--target target]
           pool-name

DESCRIPCIÓN
       El subcomando list-connector-security-maps muestra los mapas de
       seguridad que pertenecen al pool especificado de conexiones de
       conector.

       Para que este subcomando se ejecute correctamente, se debe haber creado
       antes un pool de conexiones de conector mediante el subcomando
       create-connector-connection-pool.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --securitymap
           Especifica el nombre del mapa de seguridad contenido dentro del
           pool de conexiones de conector del que se deben mostrar la
           identidad y los principales. Con esta opción, la opción --verbose
           resulta redundante.

       --verbose
           Si se define en true, devuelve una lista que incluye la identidad,
           los principales y el nombre de seguridad. El valor por defecto es
           false.

       --target
           No especifica esta opción. Esta opción se mantienen para su
           compatibilidad con versiones anteriores. Si especifica esta opción,
           no se produce un error de sintaxis. En su lugar, el subcomando se
           ejecuta correctamente y muestra una advertencia que indica que se
           ha ignorado la opción.

OPERANDOS
       nombre_pool
           Nombre del pool de conexiones de conector para el que desea mostrar
           los mapas de seguridad.

EJEMPLOS
       Ejemplo 1, Listado de los mapas de seguridad de conector
           En este ejemplo se muestran los mapas de seguridad de conector
           existentes para el pool denominado connector-Pool1.

               asadmin> list-connector-security-maps connector-Pool1
               securityMap1
               Command list-connector-security-maps executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-connector-security-map(1), delete-connector-security-map(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                   23 de diciembre de list-connector-security-maps(1)
