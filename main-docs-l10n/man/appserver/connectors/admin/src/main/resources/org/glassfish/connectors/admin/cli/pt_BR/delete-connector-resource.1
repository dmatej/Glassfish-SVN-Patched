delete-connector-resourcSubcomandos do utilitário delete-connector-resource(1)

NOME
       delete-connector-resource - remove o recurso do conector com o nome da
       JNDI especificado

SINOPSE
           delete-connector-resource [--help] [--target target] jndi_name

DESCRIçãO
       O subcomando delete-connector-resource remove o recurso conector com o
       nome da JNDI especificado.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Essa opção especifica o alvo a partir do qual você deseja remover o
           recurso conector. Os alvos válidos são:

               Nota
               ┌────────────────────────────────────────┐
               │Observação Os recursos sempre são       │
               │criados para um domínio como um todo,   │
               │mas só estarão ativos para alvos para   │
               │os quais <resource-ref> tiver sido      │
               │criado usando a opção --target durante  │
               │a criação do recurso. Isso significa    │
               │que a deleção de um recurso só deleta o │
               │elemento <resource-ref> para o --target │
               │especificado e não deleta o recurso do  │
               │domínio como um todo, a não ser que     │
               │domain seja especificado como o         │
               │--target para a deleção.                │
               └────────────────────────────────────────┘

           server
               Deleta o recurso do conector a partir da instância de servidor
               default. Este é o valor default.

           domain
               Deleta o recurso do conector a partir do domínio.

           cluster_name
               Deleta o recurso do conector a partir de cada instância do
               servidor no cluster.

           instance_name
               Deleta o recurso do conector a partir da instância de servidor
               especificada.

OPERANDOS
       jndi_name
           O nome da JNDI desse recurso conector.

EXEMPLOS
       Example 1, Deletando um Recurso do Conector
           Este exemplo deleta um recurso do conector com o nome
           jms/qConnFactory.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       Example 2, Utilizando o subcomando delete-connector-resource
           Este exemplo mostra o uso desse subcomando.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           Onde jms/qConnFactory é o recurso do conector que será removido.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                   11 de fevereiro de 201delete-connector-resource(1)
