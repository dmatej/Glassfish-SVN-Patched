create-custom-resource(1)   asadmin 实用程序子命令   create-custom-resource(1)

名称
       create-custom-resource - 创建定制资源

提要
           create-custom-resource [--help] --restype type --factoryclass classname
           [--enabled={true|false}] [--description text]
           [--property (name=value)[:name=value]*] jndi-name
           [--target target]

说明
       create-custom-resource 子命令创建定制资源。定制资源指定实现
       javax.naming.spi.ObjectFactory 接口的定制服务器范围资源对象工厂。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           您创建的定制资源将可用的目标。有效值为：

           server
               资源将在默认服务器实例和该实例上托管的所有域上可用。这是默认值。

           domain
               资源将仅在指定的域上可用。

           cluster_name
               资源将在集群中的每个服务器实例上可用。

           instance_name
               资源将仅在指定的服务器实例上可用。‏

               注意
               ┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注                                                                                               │
               │始终作为一个整体为域创建资源，但仅为指定的                                                       │
               │--target 创建资源的                                                                              │
               │resource-ref。这意味着，虽然资源是在域级别定义的，但它仅在指定的目标级别中可用。如果需要，可使用 │
               │create-resource-ref                                                                              │
               │子命令在多个目标中引用资源。                                                                     │
               └─────────────────────────────────────────────────────────────────────────────────────────────────┘

       --restype
           要创建的定制资源类型。指定完全限定类型定义，例如
           javax.naming.spi.ObjectFactory。资源类型定义的格式为 xxx.xxx。

       --factoryclass
           定制资源的工厂类名。此类实现 javax.naming.spi.ObjectFactory 接口。

       --enabled
           确定是否在运行时启用定制资源。默认值为“真”。

       --description
           提供定制资源详细信息的文本。此说明是字符串值，最多可以包含 250
           个字符。

       --property
           用于配置资源的可选属性名称/值对。

操作数
       jndi-name
           此资源的 JNDI 名称。

示例
       Example 1, 创建定制资源
           此示例创建一个定制资源。

               asadmin> create-custom-resource --restype topic
               --factoryclass com.imq.topic mycustomresource
               Command create-custom-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-custom-resource(1), list-custom-resources(1),
       create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日     create-custom-resource(1)
