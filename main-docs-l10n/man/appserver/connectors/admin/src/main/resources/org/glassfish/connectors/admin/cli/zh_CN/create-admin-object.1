create-admin-object(1)      asadmin 实用程序子命令      create-admin-object(1)

名称
       create-admin-object - 为资源适配器添加具有指定 JNDI 名称的受管理对象

提要
           create-admin-object [--help] [--target target]
           --restype restype
           [--classname classname]
           --raname raname
           [--enabled={true|false}]
           [--description description]
           [--property name=value[:name=value]*]
           jndi_name

说明
       create-admin-object 子命令为资源适配器创建具有指定 JNDI
       名称和接口定义的受管理对象。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要创建受管理对象的目标。有效值如下：

           server
               为默认服务器实例 server 创建受管理对象，这是默认值。

           configuration_name
               为命名配置创建受管理对象。

           cluster_name
               为集群中的每个服务器实例创建受管理对象。

           instance_name
               为特定服务器实例创建受管理对象。

               注意
               ┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
               │注                                                                                               │
               │始终作为一个整体为域创建资源，但仅为指定的                                                       │
               │--target 创建资源的                                                                              │
               │resource-ref。这意味着，虽然资源是在域级别定义的，但它仅在指定的目标级别中可用。如果需要，可使用 │
               │create-resource-ref                                                                              │
               │子命令在多个目标中引用资源。                                                                     │
               └─────────────────────────────────────────────────────────────────────────────────────────────────┘

       --restype
           指定受管理对象的接口定义。资源类型必须是资源适配器的 ra.xml
           文件中指定的接口定义。

       --classname
           指定受管理对象的类名。如果多个受管理对象使用同一个接口定义，则是必需的。

       --raname
           指定与该受管理对象关联的资源适配器的名称。

       --enabled
           指定该对象是否已启用。默认值为“真”。

       --description
           描述受管理对象的文本字符串。

       --property
           用于配置资源的名称/值对的说明。具体取决于资源适配器。有关 JMS
           属性，请参阅 JMS 目标资源的 create-jms-resource(1)。

操作数
       jndi_name
           要创建的受管理对象的 JNDI 名称。

示例
       Example 1, 创建受管理对象
           在此示例中，jmsra 是具有管理对象接口 javax.jms.Queue 和
           javax.jms.Topic 的系统资源适配器。

               asadmin> create-admin-object --restype javax.jms.Queue
               --raname jmsra --description "sample administered object"
               --property Name=sample_jmsqueue jms/samplequeue
               Command create-admin-object executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-admin-object(1), list-admin-objects(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 10 日        create-admin-object(1)
