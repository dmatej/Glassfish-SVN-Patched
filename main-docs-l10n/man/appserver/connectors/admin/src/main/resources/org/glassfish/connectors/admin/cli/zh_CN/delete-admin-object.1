delete-admin-object(1)      asadmin 实用程序子命令      delete-admin-object(1)

名称
       delete-admin-object - 删除具有指定 JNDI 名称的受管理对象。

提要
           delete-admin-object [--help] [--target target] jndi_name

说明
       delete-admin-object 子命令删除具有指定 JNDI 名称的受管理对象。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           这是要删除受管理对象的目标的名称。有效值为：

               注意
               ┌─────────────────────────────────────────────────┐
               │注                                               │
               │始终为整个域创建资源，但资源仅在创建资源时使用   │
               │--target 选项为其创建 <resource-ref>             │
               │的目标上处于活动状态。这意味着删除资源仅删除指定 │
               │--target 中的 <resource-ref>                     │
               │元素，并不会从整个域中删除资源，除非将           │
               │domain 指定为要删除的 --target。                 │
               └─────────────────────────────────────────────────┘

           server
               为默认服务器实例 server 删除受管理对象，这是默认值。

           configuration_name
               为指定配置删除受管理对象。

           cluster_name
               为指定集群删除受管理对象。

           instance_name
               为特定服务器实例删除受管理对象。

操作数
       jndi_name
           要删除的受管理对象的 JNDI 名称。

示例
       Example 1, 删除受管理对象
           此示例删除名为 jms/samplelqueue 的受管理对象。

               asadmin> delete-admin-object jms/samplequeue
               Command delete-admin-object executed successfully

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-admin-object(1), list-admin-objects(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 14 日        delete-admin-object(1)
