list-jndi-resources(1)  Unterbefehle des asadmin-Utili  list-jndi-resources(1)

NAME
       list-jndi-resources - listet alle vorhandenen JNDI-Ressourcen auf

ZUSAMMENFASSUNG
           list-jndi-resources [--help] [target]

BESCHREIBUNG
       Der Unterbefehl list-jndi-resources identifiziert alle vorhandenen
       JNDI-Ressourcen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Das Ziel, für das die JNDI-Ressourcen aufgelistet werden sollen.
           Folgende Werte sind gültig:

           server
               Listet die JNDI-Ressourcen der standardmäßigen Serverinstanz
               auf. Dies ist der Standardwert.

           configuration-name
               Listet die JNDI-Ressourcen für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die JNDI-Ressourcen aller Serverinstanzen im angegebenen
               Cluster auf.

           instance-name
               Listet die JNDI-Ressourcen einer angegebenen Serverinstanz auf.

BEISPIELE
       Beispiel 1, JNDI-Ressourcen auflisten
           In diesem Beispiel werden die JNDI-Ressourcen in der
           standardmäßigen Serverinstanz aufgelistet.

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

Java EE 7                        21. Jun. 2011          list-jndi-resources(1)
