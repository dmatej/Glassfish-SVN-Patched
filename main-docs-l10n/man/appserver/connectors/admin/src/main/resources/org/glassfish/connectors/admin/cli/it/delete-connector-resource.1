delete-connector-resourcComandi secondari della utdelete-connector-resource(1)

NOME
       delete-connector-resource - rimuove la risorsa connettore con il nome
       JNDI specificato

SINTASSI
           delete-connector-resource [--help] [--target target] jndi_name

DESCRIZIONE
       Il comando secondario delete-connector-resource rimuove la risorsa
       connettore con il nome JNDI specificato.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica la destinazione da cui si desidera
           rimuovere la risorsa connettore. Le destinazioni valide sono:

               Nota
               ┌────────────────────────────────────────┐
               │Nota Le risorse vengono sempre create   │
               │per l'intero dominio, ma sono attive    │
               │solo per le destinazioni per le quali è │
               │stato creato un elemento <resource-ref> │
               │utilizzando l'opzione --target al       │
               │momento della creazione della risorsa.  │
               │Ciò significa che l'eliminazione di una │
               │risorsa elimina solo l'elemento         │
               │<resource-ref> per l'opzione --target   │
               │specificata, ma non la risorsa dal      │
               │dominio completo a meno che domain      │
               │venga specificato come --target per     │
               │l'eliminazione.                         │
               └────────────────────────────────────────┘

           server
               Elimina la risorsa connettore dall'istanza server predefinita.
               Questo è il valore predefinito.

           domain
               Elimina la risorsa connettore dal dominio.

           cluster_name
               Elimina la risorsa connettore da tutte le istanze server del
               cluster.

           instance_name
               Elimina la risorsa connettore dall'istanza server specificata.

OPERANDI
       jndi_name
           Il nome JNDI della risorsa connettore.

ESEMPI
       Esempio 1, Eliminazione di una risorsa connettore
           In questo esempio viene eliminata la risorsa connettore denominata
           jms/qConnFactory.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       Esempio 2, Uso del comando secondario delete-connector-resource
           L'esempio illustra l'uso di questo comando secondario.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           Dove jms/qConnFactory è la risorsa connettore rimossa.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011   delete-connector-resource(1)
