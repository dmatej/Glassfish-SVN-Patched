delete-connector-securitUnterbefehle des asadmdelete-connector-security-map(1)

NAME
       delete-connector-security-map - löscht eine Sicherheitszuordnung für
       den angegebenen Connector Connection Pool

ZUSAMMENFASSUNG
           delete-connector-security-map [--help]
           --poolname connector_connection_pool_name [--target target] mapname

BESCHREIBUNG
       Der Unterbefehl delete-connector-security-map löscht eine
       Sicherheitszuordnung für den angegebenen Connector Connection Pool.

       Damit dieser Unterbefehl erfolgreich ausgeführt wird, müssen Sie
       zunächst mit dem Unterbefehl create-connector-connection-pool einen
       Connector Connection Pool erstellen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --poolname
           Gibt den Namen des Connector Connection Pools an, zu dem die
           Sicherheitszuordnung gehört, die gelöscht werden soll.

       --target
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

OPERANDEN
       mapname
           Name der Sicherheitszuordnung, die gelöscht werden soll.

BEISPIELE
       Example 1, Connector-Sicherheitszuordnungen löschen
           In diesem Beispiel wird securityMap1 für den vorhandenen Connection
           Pool namens connector-pool1 gelöscht.

               asadmin> delete-connector-security-map
               --poolname connector-pool1 securityMap1
               Command delete-connector-security-map executed successfully

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-connector-security-map(1), list-connector-security-maps(1),
       update-connector-security-map(1)

       asadmin(1M)

Java EE 7                        14. Sep. 2010delete-connector-security-map(1)
