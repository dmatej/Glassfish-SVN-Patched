delete-jndi-resource(1) Comandi secondari della utilit delete-jndi-resource(1)

NOME
       delete-jndi-resource - rimuove una risorsa JNDI

SINTASSI
           delete-jndi-resource [--help] [--target target] jndi_name

DESCRIZIONE
       Il comando secondario delete-jndi-resource rimuove la risorsa JNDI
       specificata. Prima di eseguire questo comando secondario, è necessario
       rimuovere tutte le associazioni alla risorsa JNDI.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Di seguito sono descritte le destinazioni valide.

               Nota
               ┌────────────────────────────────────────┐
               │Nota Le risorse vengono sempre create   │
               │per l'intero dominio, ma sono attive    │
               │solo per le destinazioni per le quali è │
               │stato creato un elemento <resource-ref> │
               │utilizzando l'opzione --target al       │
               │momento della creazione della risorsa.  │
               │Ciò significa che l'eliminazione di una │
               │risorsa elimina solo l'elemento         │
               │<resource-ref> per l'opzione --target   │
               │specificata, ma non la risorsa dal      │
               │dominio completo a meno che domain      │
               │venga specificato come --target per     │
               │l'eliminazione.                         │
               └────────────────────────────────────────┘

           server
               Elimina la risorsa dall'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Elimina la risorsa dal dominio.

           cluster_name
               Elimina la risorsa per tutte le istanze server del cluster.

           instance_name
               Elimina la risorsa JMS dall'istanza server specificata.

OPERANDI
       jndi_name
           Il nome della risorsa JNDI da rimuovere.

ESEMPI
       Esempio 1, Eliminazione di una risorsa JNDI
           In questo esempio viene rimossa la risorsa JNDI esistente
           denominata sample_jndi_resource.

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011        delete-jndi-resource(1)
