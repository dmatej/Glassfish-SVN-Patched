update-connector-securitSous-commandes de l'utupdate-connector-security-map(1)

NOM
       update-connector-security-map - modifie une mappe de sécurité pour le
       pool de connexions de connecteur spécifié

SYNTAXE
           update-connector-security-map [--help]
           --poolname connector_connection_pool_name
           [--addprincipals principal_name1[,principal_name2]*]
           [--addusergroups user_group1[,user_group2]*]
           [--removeprincipals principal_name1[,principal_name2]*]
           [--removeusergroups user_group1[,user_group2]*]
           [--mappedusername username]
           mapname

DESCRIPTION
       La sous-commande update-connector-security-map modifie une mappe de
       sécurité pour le pool de connexions de connecteur spécifié.

       Pour que cette sous-commande aboutisse, vous devez préalablement créer
       le pool de connexions du connecteur à l'aide de la sous-commande
       create-connector-connection-pool.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --poolname
           Spécifie le nom du pool de connexions de connecteur auquel la mappe
           de sécurité à mettre à jour appartient.

       --addprincipals
           Spécifie la liste des principaux propres aux EIS à ajouter, séparés
           par des virgules. Utilisez soit l'option --addprincipals, soit
           l'option --addusergroups, mais pas les deux dans la même commande.

       --addusergroups
           Spécifie la liste des groupes d'utilisateurs EIS à ajouter, séparés
           par des virgules. Utilisez soit l'option --addprincipals, soit
           l'option --addusergroups, mais pas les deux dans la même commande.

       --removeprincipals
           Spécifie la liste des principaux propres aux EIS à enlever, séparés
           par des virgules.

       --removeusergroups
           Spécifie la liste des groupes d'utilisateurs EIS à enlever, séparés
           par des virgules.

       --mappedusername
           Cette option indique le nom utilisateur EIS.

OPÉRANDES
       mapname
           Nom de la mappe de sécurité à mettre à jour.

EXEMPLES
       Exemple 1, Mise à jour d'une mappe de sécurité de connecteur
           Dans cet exemple, des principaux sont ajoutés à la mappe de
           sécurité existante nommée securityMap1.

               asadmin> update-connector-security-map --poolname connector-pool1
               --addprincipals principal1,principal2 securityMap1
               Command update-connector-security-map executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-connector-security-map(1), delete-connector-security-map(1),
       list-connector-security-maps(1)

       asadmin(1M)

Java EE 7                        15 sept. 2010update-connector-security-map(1)
