delete-jndi-resource(1) Subcomandos de la Utilidad asa delete-jndi-resource(1)

NOMBRE DE REFERENCIA
       delete-jndi-resource - Elimina un recurso JNDI.

SINOPSIS
           delete-jndi-resource [--help] [--target target] jndi_name

DESCRIPCIÓN
       El subcomando delete-jndi-resource elimina el recurso JNDI
       especificado. Deberá eliminar todas las asociaciones al recurso JNDI
       antes de ejecutar este subcomando.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Los destinos válidos se describen a continuación.

               Nota
               ┌────────────────────────────────────────┐
               │Nota Los recursos siempre se crean para │
               │un dominio completo, pero sólo están    │
               │activos para los destinos para los que  │
               │se ha creado una <resource-ref>         │
               │mediante la opción --target al crear el │
               │recurso. Esto significa que al suprimir │
               │un recurso sólo se suprime el elemento  │
               │<resource-ref> para el --target         │
               │especificado y no se suprime el recurso │
               │del dominio completo a menos que se     │
               │especifique domain como --target de la  │
               │supresión.                              │
               └────────────────────────────────────────┘

           server
               Suprime el recurso de la instancia de servidor por defecto.
               Éste es el valor por defecto.

           domain
               Suprime el recurso del dominio.

           cluster_name
               Suprime el recurso de todas las instancias de servidor del
               cluster.

           instance_name
               Suprime el recurso de la instancia de servidor especificada.

OPERANDOS
       jndi_name
           Nombre del recurso JNDI que se va a eliminar.

EJEMPLOS
       Ejemplo 1, Supresión de un Recurso JNDI
           En este ejemplo se elimina un recurso JNDI existente denominado
           sample_jndi_resource.

               asadmin> delete-jndi-resource sample_jndi_resource
               Command delete-jndi-resource executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jndi-resource(1), list-jndi-resources(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011     delete-jndi-resource(1)
