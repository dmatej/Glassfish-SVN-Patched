list-jndi-resources(1)  Subcomandos de la Utilidad asa  list-jndi-resources(1)

NOMBRE DE REFERENCIA
       list-jndi-resources - Muestra todos los recursos JNDI existentes.

SINOPSIS
           list-jndi-resources [--help] [target]

DESCRIPCIÓN
       El subcomando list-jndi-resources identifica todos los recursos JNDI
       existentes.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           El destino para el que se van a enumerar los recursos JNDI. Los
           valores válidos son los siguientes:

           server
               Muestra los recursos JNDI en la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration-name
               Muestra los recursos JNDI para la configuración especificada.

           cluster-name
               Muestra los recursos JNDI en todas las instancias de servidor
               del cluster especificado.

           instance-name
               Muestra los recursos JNDI en la instancia de servidor
               especificada.

EJEMPLOS
       Ejemplo 1, Lista de Recursos JNDI
           En este ejemplo se muestran los recursos JNDI en la instancia de
           servidor por defecto.

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

Java EE 7                     21 de junio de 2011       list-jndi-resources(1)
