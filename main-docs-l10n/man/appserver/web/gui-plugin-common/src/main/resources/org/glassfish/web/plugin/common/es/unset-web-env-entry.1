unset-web-env-entry(1)  Subcomandos de la Utilidad asa  unset-web-env-entry(1)

NOMBRE DE REFERENCIA
       unset-web-env-entry - anula la definición de una entrada de entorno
       para una aplicación web o un módulo desplegado.

SINOPSIS
           unset-web-env-entry [--help] --name=env-entry-name application-name[/module]

DESCRIPCIÓN
       El subcomando unset-web-env-entry anula la definición de una entrada de
       entorno para uno de los siguientes elementos:

       ·   Una aplicación web desplegada

       ·   Un módulo web en una aplicación de Java Platform, Enterprise
           Edition (Java EE) desplegada

       Cuando se anula la definición de una entrada, su valor vuelve al valor,
       si lo hay, que se define en el descriptor de despliegue de la
       aplicación.

       La aplicación debe estar ya desplegada. De lo contrario, se produce un
       error.

       La entrada se debe haber definido previamente mediante el subcomando
       set-web-env-entry(1). De lo contrario, se produce un error.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           No utilice el subcomando   │
           │           unset-web-env-entry para   │
           │           cambiar el valor de una    │
           │           entrada de entorno que se  │
           │           define en el descriptor de │
           │           despliegue de una          │
           │           aplicación. En su lugar,   │
           │           utilice para ello el       │
           │           subcomando                 │
           │           set-web-env-entry.         │
           └──────────────────────────────────────┘

       Este subcomando permite cambiar la configuración de una aplicación
       desplegada sin tener que modificar los descriptores de despliegue de la
       aplicación y volver a empaquetar y desplegar la aplicación.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           El nombre de la entrada de entorno cuya definición se va a anular.
           Se trata de un nombre JNDI relativo al contexto java:comp/env. El
           nombre debe ser único dentro de un componente de despliegue. Esta
           entrada se debe haber definido previamente mediante el subcomando
           set-web-env-entry. De lo contrario, se produce un error.

OPERANDOS
       application-name
           Nombre de la aplicación. Este nombre puede obtenerse de la Consola
           de Administración o mediante el subcomando list-applications(1).

           La aplicación debe estar ya desplegada. De lo contrario, se produce
           un error.

       module
           Ruta de acceso relativa al módulo dentro del archivo de
           almacenamiento de empresa (EAR) de la aplicación. La ruta de acceso
           al módulo se especifica en el elemento module del archivo
           application.xml de la aplicación.

           module sólo es necesario si la entrada de entorno se refiere a un
           módulo web de una aplicación Java EE. Si se especifica, module se
           debe ubicar a continuación de application-name, separados por una
           barra (/).

           Por ejemplo, el archivo application.xml de la aplicación myApp
           puede especificar el siguiente módulo web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           El módulo se especificaría como operando de este comando como
           myApp/myWebModule.war.

EJEMPLOS
       Ejemplo 1, Anulación de la Definición de una Entrada de Entorno para
       una Aplicación Web
           En este ejemplo se anula la definición de la entrada de entorno
           Hello User de la aplicación web hello. La entrada vuelve al valor,
           si lo hay, que se define en el descriptor de despliegue de la
           aplicación.

               asadmin> unset-web-env-entry --name="Hello User" hello

               Command unset-web-env-entry executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       list-applications(1), list-web-env-entry(1), set-web-env-entry(1)

       asadmin(1M)

Java EE 7                    30 de octubre de 2009      unset-web-env-entry(1)
