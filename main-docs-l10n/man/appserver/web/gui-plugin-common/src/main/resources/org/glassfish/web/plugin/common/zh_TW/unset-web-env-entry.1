unset-web-env-entry(1)      asadmin 公用程式子命令      unset-web-env-entry(1)

參考名
       unset-web-env-entry - 取消設定建置的 Web 應用程式或模組的環境項目

概要
           unset-web-env-entry [--help] --name=env-entry-name application-name[/module]

描述
       unset-web-env-entry 子命令可取消設定下列其中一個項目的環境項目：

       ·   建置的 Web 應用程式

       ·   已建置之 Java 平台、Enterprise Edition (Java EE) 應用程式中的 Web
           模組

       取消設定項目時，其值會復原為在應用程式的建置描述元中設定的值 (如果有)。

       應用程式必須已完成建置。否則，將發生錯誤。

       該項目必須先使用 set-web-env-entry(1) 子命令設定。否則，將發生錯誤。

           注意
           ┌────────────────────────────────────────────────────────────────────────┐
           │備註                                                                    │
           │                                                                        │
           │           請不要使用                                                   │
           │           unset-web-env-entry                                          │
           │           子命令來變更在應用程式的建置描述元中設定的環境項目值。請改用 │
           │           set-web-env-entry                                            │
           │           子命令來達成此目的。                                         │
           └────────────────────────────────────────────────────────────────────────┘

       此子命令可讓您變更已建置的應用程式組態，不需要修改應用程式的建置描述元，也不需要重新封裝或重新建置應用程式。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --name
           要取消設定的環境項目名稱。此名稱為相對於 java:comp/env 環境的 JNDI
           名稱。名稱在建置元件中必須是唯一的。此項目必須先使用
           set-web-env-entry 子命令設定。否則，將發生錯誤。

運算元
       application-name
           應用程式的名稱。此名稱可從 管理主控台 取得，或可使用
           list-applications(1) 子命令取得。

           應用程式必須已完成建置。否則，將發生錯誤。

       module
           應用程式之企業存檔 (EAR) 檔案中模組的相對路徑。會在應用程式之
           application.xml 檔案的 module 元素中指定模組的路徑。

           只有在環境項目套用至 Java EE 應用程式的 Web 模組時，才需要使用
           module。如果指定，module 必須接在 application-name 之後，並以斜線
           (/) 區隔。

           例如，myApp 應用程式的 application.xml 檔案可能會指定下列 Web
           模組：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           模組可能會被指定為此命令的運算元，其形式如下：myApp/myWebModule.war。

範例
       範例 1, 取消設定 Web 應用程式的環境項目
           此範例會取消設定 Web 應用程式 hello 的環境項目 Hello
           User。項目會復原為值 (如果有)，且是在應用程式的建置描述元中定義。

               asadmin> unset-web-env-entry --name="Hello User" hello

               Command unset-web-env-entry executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       list-applications(1), list-web-env-entry(1), set-web-env-entry(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日       unset-web-env-entry(1)
