list-web-context-paramasadminユーティリティのサブコマlist-web-context-param(1)

名前
       list-web-context-param -
       デプロイされたWebアプリケーションまたはモジュールのサーブレット・コンテキスト初期化パラメータをリストします

概要
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

説明
       list-web-context-paramサブコマンドは、次の項目のいずれかについて、サーブレット・コンテキスト初期化パラメータをリストします。

       ·   デプロイされたWebアプリケーション

       ·   デプロイされたJavaプラットフォーム、Enterprise Edition (Java
           EE)アプリケーションのWebモジュール

       アプリケーションはデプロイ済である必要があります。それ以外の場合はエラーが発生します。

       list-web-context-paramサブコマンドは、set-web-context-param(1)サブコマンドを使用して事前に設定されたパラメータのみをリストします。アプリケーションのデプロイメント記述子のみに設定されたパラメータは、このサブコマンドではリストさ

       各パラメータについて、次の情報が表示されます。

       ·   パラメータの名前

       ·   パラメータに設定される値

       ·   パラメータの設定時に指定されたset-web-context-paramサブコマンドの--ignoreDescriptorItemオプションの値

       ·   パラメータの説明、またはパラメータの設定時に説明が指定されていない場合はnull

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --name
           リストするサーブレット・コンテキスト初期化パラメータの名前。このオプションを省略した場合、アプリケーションについて事前に設定されたすべてのパラメータがリストされます。

オペランド
       application-name
           アプリケーションの名前。この名前は、管理コンソールから、またはlist-applications(1)サブコマンドを使用して取得できます。

           アプリケーションはデプロイ済である必要があります。それ以外の場合はエラーが発生します。

       module
           アプリケーションのエンタープライズ・アーカイブ(EAR)ファイル内のモジュールへの相対パス。モジュールへのパスは、アプリケーションのapplication.xmlファイルのmodule要素に指定されます。

           moduleは、サーブレット・コンテキスト初期化パラメータがJava
           EEアプリケーションのWebモジュールに適用される場合にのみ必要となります。指定する場合、moduleはapplication-nameの後にスラッシュ(/)で区切って指定する必要があります。

           たとえば、myAppアプリケーションのapplication.xmlファイルには、次のWebモジュールが指定されます。

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           モジュールは、このコマンドのオペランドとして指定され、myApp/myWebModule.warとなります。

例
       例 1,
       Webアプリケーションのサーブレット・コンテキスト初期化パラメータのリスト
           この例では、set-web-context-paramサブコマンドを使用して設定されたWebアプリケーションbasic-ezcompのすべてのサーブレット・コンテキスト初期化パラメータをリストします。javax.faces.PROJECT_STAGEパラメータの設定時に説明が指定されていないため、このパラメータの説明のかわりにnullが表示されます。

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                       2009年10月30日       list-web-context-param(1)
