list-web-context-param(1Unterbefehle des asadmin-Utillist-web-context-param(1)

NAME
       list-web-context-param - listet
       Servlet-Kontextinitialisierungsparameter einer bereitgestellten
       Anwendung bzw. eines Moduls auf

ZUSAMMENFASSUNG
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

BESCHREIBUNG
       Der Unterbefehl list-web-context-param listet die
       Servlet-Kontextinitialisierungsparameter eines der folgenden Elemente
       auf:

       ·   Eine bereitgestellte Webanwendung

       ·   Webmodul in einer bereitgestellten Java Platform, Enterprise
           Edition-(Java EE-)Anwendung

       Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt ein
       Fehler auf.

       Der Unterbefehl list-web-context-param listet nur Parameter auf, die
       zuvor mithilfe des Unterbefehls set-web-context-param(1) festgelegt
       wurden. Der Unterbefehl listet keine Parameter auf, die nur im
       Deployment-Deskriptor der Anwendung festgelegt wurden.

       Für jeden Parameter werden folgende Informationen angezeigt:

       ·   der Name des Parameters

       ·   der Wert, auf den der Parameter gesetzt ist

       ·   der Wert der Option --ignoreDescriptorItem des Unterbefehls
           set-web-context-param, der angegeben wurde, als der Parameter
           gesetzt wurde

       ·   die Beschreibung des Parameters oder null, wenn keine Beschreibung
           angegeben wurde, als der Parameter gesetzt wurde

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Der Name des Servlet-Kontextinitialisierungsparameters, der
           aufgelistet werden soll. Wird diese Option nicht angegeben, werden
           alle zuvor gesetzten Parameter der Anwendung aufgelistet.

OPERANDEN
       application-name
           Der Name der Anwendung. Dieser Name kann aus der
           Administrationskonsole oder mit dem Unterbefehl
           list-applications(1) abgerufen werden.

           Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt
           ein Fehler auf.

       module
           Der relative Pfad zum Modul innerhalb der Enterprise
           Archive-(EAR-)Datei der Anwendung. Der Pfad zum Modul wird im
           module-Element der application.xml-Datei der Anwendung angegeben.

           module ist nur dann erforderlich, wenn der
           Servlet-Kontextinitialisierungsparameter auf ein Webmodul einer
           Java EE-Anwendung bezogen ist. Falls angegeben, muss module auf
           application-name folgen und durch einen Schrägstrich (/) getrennt
           sein.

           Beispiel: Die Datei application.xml für die Anwendung myApp kann
           das folgende Webmodul angeben:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Das Modul würde als Operand dieses Befehls als
           myApp/myWebModule.war angegeben werden.

BEISPIELE
       Example 1, Servlet-Kontextinitialisierungsparameter für eine
       Webanwendung auflisten
           In diesem Beispiel werden alle
           Servlet-Kontextinitialisierungsparameter der Webanwendung
           basic-ezcomp aufgelistet, die mithilfe des Unterbefehls
           set-web-context-param festgelegt wurden. Da beim Setzen des
           Parameters javax.faces.PROJECT_STAGE keine Beschreibung angegeben
           wurde, wird null anstelle einer Beschreibung für diesen Parameter
           angezeigt.

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                        30. Okt. 2009       list-web-context-param(1)
