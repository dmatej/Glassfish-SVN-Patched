list-web-env-entry(1)   Unterbefehle des asadmin-Utili   list-web-env-entry(1)

NAME
       list-web-env-entry - listet Umgebungseinträge für eine bereitgestellte
       Webanwendung oder ein bereitgestelltes Modul auf

ZUSAMMENFASSUNG
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

BESCHREIBUNG
       Der Unterbefehl list-web-env-entry listet die Umgebungseinträge für
       eines der folgenden Elemente auf:

       ·   Eine bereitgestellte Webanwendung

       ·   Webmodul in einer bereitgestellten Java Platform, Enterprise
           Edition-(Java EE-)Anwendung

       Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt ein
       Fehler auf.

       Der Unterbefehl list-web-env-entry listet nur Einträge auf, die zuvor
       mit dem Unterbefehl set-web-env-entry(1) festgelegt wurden. Der
       Unterbefehl listet keine Umgebungseinträge auf, die nur im
       Deployment-Deskriptor der Anwendung festgelegt wurden.

       Für jeden Eintrag werden folgende Informationen angezeigt:

       ·   der Name des Eintrags

       ·   der Java-Typ des Eintrags

       ·   der Wert, auf den der Eintrag gesetzt ist

       ·   der Wert der Option --ignoreDescriptorItem des Unterbefehls
           set-web-env-entry, der angegeben wurde, als der Eintrag festgelegt
           wurde

       ·   die Beschreibung des Eintrags oder null, wenn keine Beschreibung
           angegeben wurde, als der Eintrag festgelegt wurde

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Der Name des Umgebungseintrags, der aufgelistet werden soll. Der
           Name ist ein JNDI-Name, der auf den java:comp/env-Kontext bezogen
           ist. Der Name muss innerhalb einer Deployment-Komponente eindeutig
           sein. Wird diese Option nicht angegeben, werden alle
           Umgebungseinträge aufgelistet, die zuvor für die Anwendung
           festgelegt wurden.

OPERANDEN
       application-name
           Der Name der Anwendung. Dieser Name kann aus der
           Administrationskonsole oder mit dem Unterbefehl
           list-applications(1) abgerufen werden.

           Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt
           ein Fehler auf.

       module
           Der relative Pfad zum Modul innerhalb der Enterprise
           Archive-(EAR-)Datei der Anwendung. Der Pfad zum Modul wird im
           module-Element der application.xml-Datei der Anwendung angegeben.

           module ist nur dann erforderlich, wenn der Umgebungseintrag auf ein
           Webmodul einer Java EE-Anwendung bezogen ist. Falls angegeben, muss
           module auf application-name folgen und durch einen Schrägstrich (/)
           getrennt sein.

           Beispiel: Die Datei application.xml für die Anwendung myApp kann
           das folgende Webmodul angeben:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Das Modul würde als Operand dieses Befehls als
           myApp/myWebModule.war angegeben werden.

BEISPIELE
       Example 1, Umgebungseinträge für eine Webanwendung auflisten
           In diesem Beispiel werden alle Umgebungseinträge aufgelistet, die
           für die Webanwendung hello mit dem Unterbefehl set-web-env-entry
           festgelegt wurden. Da beim Festlegen des Umgebungseintrags Hello
           Port keine Beschreibung angegeben wurde, wird null anstelle einer
           Beschreibung für diesen Eintrag angezeigt.

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                        30. Okt. 2009           list-web-env-entry(1)
