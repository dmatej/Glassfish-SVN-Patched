unset-web-context-param(1)  asadmin 实用程序子命令  unset-web-context-param(1)

名称
       unset-web-context-param - 取消设置已部署 Web 应用程序或模块的 servlet
       上下文初始化参数

提要
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

说明
       unset-web-context-param 子命令可取消设置以下某一项的 Servlet
       上下文初始化参数：

       ·   已部署的 Web 应用程序

       ·   已部署的 Java Platform Enterprise Edition (Java EE) 应用程序中的
           Web 模块

       当参数被取消设置时，其值将恢复为在应用程序的部署描述符中设置的值
       (如果有)。

       该应用程序必须已部署。否则，会出现错误。

       该参数必须在之前已通过 set-web-context-param
       子命令进行了设置。否则，会出现错误。

           注意
           ┌──────────────────────────────────────────────────────┐
           │注                                                    │
           │                                                      │
           │           不要使用                                   │
           │           unset-web-context-param                    │
           │           子命令来更改在应用程序的部署描述符中设置的 │
           │           Servlet                                    │
           │           上下文参数的值。而是使用                   │
           │           set-web-context-param(1)                   │
           │           子命令来实现此目的。                       │
           └──────────────────────────────────────────────────────┘

       通过此子命令，可以更改部署的应用程序的配置，而无需修改应用程序的部署描述符、重新打包和重新部署应用程序。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --name
           要取消设置的 Servlet 上下文初始化参数的名称。该参数必须在之前已通过
           set-web-context-param 子命令进行了设置。否则，会出现错误。

操作数
       application-name
           应用程序名称。可以从管理控制台或通过使用 list-applications(1)
           子命令获取此名称。

           该应用程序必须已部署。否则，会出现错误。

       module
           应用程序的企业档案 (EAR) 文件内的模块的相对路径。在应用程序的
           application.xml 文件的 module 元素中指定模块的路径。

           仅当 Servlet 上下文初始化参数应用于 Java EE 应用程序的 Web
           模块时，才需要 module。如果已指定，则 module 必须位于
           application-name 之后，用斜杠 (/) 分隔。

           例如，myApp 应用程序的 application.xml 文件可能会指定以下 Web
           模块：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           会将模块指定为此命令的操作数，如 myApp/myWebModule.war。

示例
       Example 1, 取消设置 Web 应用程序的 Servlet 上下文初始化参数。
           该示例将取消设置 Web 应用程序 basic-ezcomp 的 servlet
           上下文初始化参数
           javax.faces.STATE_SAVING_METHOD。该参数将恢复为在应用程序的部署描述符中定义的值
           (如果有)。

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日   unset-web-context-param(1)
