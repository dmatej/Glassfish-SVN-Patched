list-web-context-param(1Subcomandos do utilitário asalist-web-context-param(1)

NOME
       list-web-context-param - lista parâmetros de inicialização de contexto
       de servlet de uma aplicação ou módulo web implantado(a)

SINOPSE
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

DESCRIçãO
       O subcomando list-web-context-param lista parâmetros no contexto de
       inicialização servlet de um dos seguintes itens:

       ·   Uma aplicação Web implantada

       ·   Um módulo web em uma aplicação da Plataforma Java, Enterprise
           Edition (Java EE) implantada

       A aplicação já deve ter sido implantada. Caso contrário, ocorrerá um
       erro.

       O subcomando list-web-context-param lista apenas parâmetros que foram
       previamente definidos utilizando o subcomando set-web-context-param(1).
       O subcomando não lista parâmetros que são configurados apenas no
       descritor implantado da aplicação.

       Para cada parâmetro, as seguintes informações são exibidas:

       ·   O nome do parâmetro

       ·   O valor para o qual o parâmetro é definido

       ·   O valor da opção --ignoreDescriptorItem do subcomando
           set-web-context-param que foi especificado quando o parâmetro foi
           definido

       ·   A descrição do parâmetro ou null se nenhuma descrição foi
           especificada quando o parâmetro foi configurado

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --name
           O nome do parâmetro no contexto de inicialização servlet a ser
           listado. Se essa opção for omitida, todos os parâmetros da
           aplicação que foram previamente configurados são listados.

OPERANDOS
       application-name
           O nome da aplicação. Esse nome pode ser obtido na Console de
           Administração ou usando o subcomando list-applications(1).

           A aplicação já deve ter sido implantada. Caso contrário, ocorrerá
           um erro.

       module
           O caminho relativo para o módulo do arquivo compactado EAR
           (Enterprise archive) da aplicação. O caminho para o módulo é
           especificado no elemento module do arquivo application.xml da
           aplicação.

           O module será necessário apenas se o parâmetro no contexto de
           inicialização de servlet se referir a um módulo Web ou a uma
           aplicação Java EE. Se especificado, module deve seguir
           application-name, separado por uma barra (/).

           Por exemplo o arquivo application.xml para a aplicação myApp pode
           especificar o seguinte módulo Web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           O módulo seria especificado como o operando deste comando como
           myApp/myWebModule.war.

EXEMPLOS
       Example 1, Listando Parâmetros do Contexto de Inicialização do Servlet
       para uma Aplicação Web
           Esse exemplo lista todos os parâmetros do contexto de inicialização
           da aplicação Web basic-ezcomp que foram configurados utilizando o
           subcomando set-web-context-param. Pelo fato de nenhuma descrição
           ter sido especificada quando o parâmetro javax.faces.PROJECT_STAGE
           foi definido, null é exibido em vez de uma descrição para esse
           parâmetro.

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                         30 out 2009        list-web-context-param(1)
