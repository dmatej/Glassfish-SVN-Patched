unset-web-context-paraasadminユーティリティのサブコマ

名前
       unset-web-context-param -
       デプロイされたWebアプリケーションまたはモジュールのサーブレット・コンテキスト初期化パラメータの設定を解除します

概要
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

説明
       unset-web-context-paramサブコマンドは、次に示すいずれかの項目のサーブレット・コンテキスト初期化パラメータの設定を解除します。

       ·   デプロイされたWebアプリケーション

       ·   デプロイされたJavaプラットフォーム、Enterprise Edition (Java
           EE)アプリケーションのWebモジュール

       パラメータの設定が解除されると、その値はアプリケーションのデプロイメント記述子に設定された値(存在する場合)に戻されます。

       アプリケーションはデプロイ済である必要があります。それ以外の場合はエラーが発生します。

       このパラメータは、set-web-context-paramサブコマンドを使用して事前に設定されている必要があります。それ以外の場合はエラーが発生します。

           注記
           ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注意                                                                                                                                                                                                                                                                       │
           │                                                                                                                                                                                                                                                                           │
           │           アプリケーションのデプロイメント記述子に設定されているサーブレット・コンテキスト初期化パラメータの値を変更する場合は、unset-web-context-paramサブコマンドは使用しないでください。かわりに、この目的ではset-web-context-param(1)サブコマンドを使用してください。 │
           └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       このサブコマンドではデプロイ済アプリケーションの構成を変更できますが、その際、そのアプリケーションのデプロイメント記述子を変更してアプリケーションの再パッケージ化と再デプロイを行う必要はありません。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --name
           設定を解除するサーブレット・コンテキスト初期化パラメータの名前。このパラメータは、set-web-context-paramサブコマンドを使用して事前に設定されている必要があります。それ以外の場合はエラーが発生します。

オペランド
       application-name
           アプリケーションの名前。この名前は、管理コンソールから、またはlist-applications(1)サブコマンドを使用して取得できます。

           アプリケーションはデプロイ済である必要があります。それ以外の場合はエラーが発生します。

       module
           アプリケーションのエンタープライズ・アーカイブ(EAR)ファイル内のモジュールへの相対パス。モジュールへのパスは、アプリケーションのapplication.xmlファイルのmodule要素に指定されます。

           moduleは、サーブレット・コンテキスト初期化パラメータがJava
           EEアプリケーションのWebモジュールに適用される場合にのみ必要となります。指定する場合、moduleはapplication-nameの後にスラッシュ(/)で区切って指定する必要があります。

           たとえば、myAppアプリケーションのapplication.xmlファイルには、次のWebモジュールが指定されます。

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           モジュールは、このコマンドのオペランドとして指定され、myApp/myWebModule.warとなります。

例
       例 1,
       Webアプリケーションのサーブレット・コンテキスト初期化パラメータの設定解除
           この例では、Webアプリケーションbasic-ezcompのサーブレット・コンテキスト初期化パラメータjavax.faces.STATE_SAVING_METHODの設定を解除します。このパラメータの値は、アプリケーションのデプロイメント記述子で定義されている値(存在する場合)に戻ります。

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                       2009年10月30日      unset-web-context-param(1)
