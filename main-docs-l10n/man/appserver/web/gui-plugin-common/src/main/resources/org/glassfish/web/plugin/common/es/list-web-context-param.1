list-web-context-param(1Subcomandos de la Utilidad aslist-web-context-param(1)

NOMBRE DE REFERENCIA
       list-web-context-param - muestra los parámetros de inicialización de
       contexto de servlet de un módulo o una aplicación web desplegada.

SINOPSIS
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

DESCRIPCIÓN
       El subcomando list-web-context-param muestra los parámetros de
       inicialización de contexto de servlet de uno de los elementos
       siguientes:

       ·   Una aplicación web desplegada

       ·   Un módulo web en una aplicación de Java Platform, Enterprise
           Edition (Java EE) desplegada

       La aplicación debe estar ya desplegada. De lo contrario, se produce un
       error.

       El subcomando list-web-context-param muestra sólo los parámetros que se
       han configurado previamente mediante el subcomando
       set-web-context-param(1). El subcomando no muestra los parámetros
       configurados sólo en el descriptor de despliegue de la aplicación.

       Para cada parámetro se indica la siguiente información:

       ·   El nombre del parámetro

       ·   El valor en el que se define

       ·   El valor de la opción --ignoreDescriptorItem del subcomando
           set-web-context-param especificado cuando se definió el parámetro

       ·   La descripción del parámetro, o null, si no se ha especificado
           ninguna descripción cuando se definió el parámetro

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           El nombre del parámetro de inicialización de contexto de servlet
           que debe aparecer en la lista. Si se omite esta opción, se enumeran
           todos los parámetros de la aplicación previamente configurados.

OPERANDOS
       application-name
           Nombre de la aplicación. Este nombre puede obtenerse de la Consola
           de Administración o mediante el subcomando list-applications(1).

           La aplicación debe estar ya desplegada. De lo contrario, se produce
           un error.

       module
           Ruta de acceso relativa al módulo dentro del archivo de
           almacenamiento de empresa (EAR) de la aplicación. La ruta de acceso
           al módulo se especifica en el elemento module del archivo
           application.xml de la aplicación.

           module sólo es necesaria si el parámetro de inicialización de
           contexto de servlet se aplica a un módulo web de una aplicación
           Java EE. Si se especifica, module se debe ubicar a continuación de
           application-name, separados por una barra (/).

           Por ejemplo, el archivo application.xml de la aplicación myApp
           puede especificar el siguiente módulo web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           El módulo se especificaría como operando de este comando como
           myApp/myWebModule.war.

EJEMPLOS
       Ejemplo 1, Listado de parámetros de inicialización de contexto de
       servlet para una aplicación web
           En este ejemplo se muestran todos los parámetros de inicialización
           de contexto de servlet de la aplicación web basic-ezcomp que se han
           configurados mediante el subcomando set-web-context-param. Dado que
           no se definió ninguna descripción al configurar el parámetro
           javax.faces.PROJECT_STAGE, se muestra null en lugar de una
           descripción para este parámetro.

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                    30 de octubre de 2009   list-web-context-param(1)
