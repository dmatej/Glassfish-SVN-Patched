unset-web-context-param(Comandi secondari della utilunset-web-context-param(1)

NOME
       unset-web-context-param - annulla l'impostazione di un parametro di
       inizializzazione del contesto servlet di un'applicazione Web o di un
       modulo distribuito

SINTASSI
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

DESCRIZIONE
       Il comando secondario unset-web-context-param annulla l'impostazione di
       un parametro di inizializzazione del contesto servlet di uno degli
       elementi seguenti:

       ·   un'applicazione Web distribuita;

       ·   un modulo Web in un'applicazione Java Platform, Enterprise Edition
           (Java EE) distribuita.

       Quando si annulla l'impostazione di un parametro, viene ripristinato
       l'eventuale valore impostato nel descrittore di distribuzione
       dell'applicazione.

       L'applicazione deve essere stata già distribuita. In caso contrario, si
       verifica un errore.

       Il parametro deve essere stato impostato in precedenza mediante il
       comando secondario set-web-context-param. In caso contrario, si
       verifica un errore.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Non utilizzare il comando   │
           │           secondario                  │
           │           unset-web-context-param per │
           │           modificare il valore di un  │
           │           parametro di                │
           │           inizializzazione del        │
           │           contesto servlet impostato  │
           │           nel descrittore di          │
           │           distribuzione di            │
           │           un'applicazione. A tale     │
           │           scopo, utilizzare invece il │
           │           comando secondario          │
           │           set-web-context-param(1).   │
           └───────────────────────────────────────┘

       Questo comando secondario consente di modificare la configurazione di
       un'applicazione distribuita senza dover modificare i descrittori di
       distribuzione dell'applicazione né creare un nuovo package e
       ridistribuire l'applicazione.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --name
           Il nome del parametro di inizializzazione del contesto servlet di
           cui annullare l'impostazione. Questo parametro deve essere stato
           impostato in precedenza mediante il comando secondario
           set-web-context-param. In caso contrario, si verifica un errore.

OPERANDI
       application-name
           Il nome dell'applicazione. Il nome può essere ottenuto da Console
           di amministrazione o utilizzando il comando secondario
           list-applications(1).

           L'applicazione deve essere stata già distribuita. In caso
           contrario, si verifica un errore.

       module
           Il percorso relativo del modulo all'interno del file EAR
           dell'applicazione. Il percorso del modulo è specificato
           nell'elemento module del file application.xml dell'applicazione.

           module è necessario solo se il parametro di inizializzazione del
           contesto del servlet si applica a un modulo Web di un'applicazione
           Java EE. Se specificata, module deve seguire application-name,
           separato da una barra (/).

           Ad esempio, il file application.xml per l'applicazione myApp
           potrebbe specificare il modulo Web seguente:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Il modulo sarebbe specificato come operando di questo comando:
           myApp/myWebModule.war.

ESEMPI
       Esempio 1, Annullamento dell'impostazione di un parametro di
       inizializzazione del contesto servlet per un'applicazione Web
           In questo esempio viene annullata l'impostazione del parametro di
           inizializzazione del contesto servlet
           javax.faces.STATE_SAVING_METHOD dell'applicazione Web basic-ezcomp.
           Per il parametro viene ripristinato l'eventuale valore definito nel
           descrittore di distribuzione dell'applicazione.

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                       30 ottobre 2009     unset-web-context-param(1)
