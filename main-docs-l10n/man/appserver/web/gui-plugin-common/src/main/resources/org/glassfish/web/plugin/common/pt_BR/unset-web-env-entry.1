unset-web-env-entry(1)  Subcomandos do utilitário asad  unset-web-env-entry(1)

NOME
       unset-web-env-entry - cancela a definição de uma entrada de ambiente de
       uma aplicação ou módulo web implantado(a)

SINOPSE
           unset-web-env-entry [--help] --name=env-entry-name application-name[/module]

DESCRIçãO
       O subcomando unset-web-env-entry cancela a definição de uma entrada de
       ambiente para um dos itens a seguir:

       ·   Uma aplicação Web implantada

       ·   Um módulo web em uma aplicação da Plataforma Java, Enterprise
           Edition (Java EE) implantada

       Quando a definição de uma entrada é cancelada, seu valor reverte para o
       valor definido no descritor de implantação da aplicação.

       A aplicação já deve ter sido implantada. Caso contrário, ocorrerá um
       erro.

       A entrada deverá ter sido definida previamente utilizando o subcomando
       set-web-env-entry(1). Caso contrário, ocorrerá um erro.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Não utilize o subcomando     │
           │           unset-web-env-entry para     │
           │           alterar o valor de uma       │
           │           entrada de ambiente definido │
           │           em um descritor de           │
           │           implantação da aplicação.    │
           │           Utilize o subcomando         │
           │           set-web-env-entry para isso. │
           └────────────────────────────────────────┘

       Este subcomando permite alterar a configuração de uma aplicação
       implantada sem a necessidade de modificar os descritores de implantação
       da aplicação e reencapsular e reimplantar a aplicação.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --name
           O nome da entrada de ambiente que deve ter a definição cancelada. O
           nome é um nome da JNDI relativo ao contexto java:comp/env. O nome
           deve ser exclusivo em um componente de implantação. Essa entrada
           deve ter sido estabelecida previamente utilizando o subcomando
           set-web-env-entry. Caso contrário, ocorrerá um erro.

OPERANDOS
       application-name
           O nome da aplicação. Esse nome pode ser obtido na Console de
           Administração ou usando o subcomando list-applications(1).

           A aplicação já deve ter sido implantada. Caso contrário, ocorrerá
           um erro.

       module
           O caminho relativo para o módulo do arquivo compactado EAR
           (Enterprise archive) da aplicação. O caminho para o módulo é
           especificado no elemento module do arquivo application.xml da
           aplicação.

           module será necessário apenas se a entrada de ambiente se referir a
           um módulo Web de uma aplicação Java EE. Se especificado, module
           deve seguir application-name, separado por uma barra (/).

           Por exemplo o arquivo application.xml para a aplicação myApp pode
           especificar o seguinte módulo Web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           O módulo seria especificado como o operando deste comando como
           myApp/myWebModule.war.

EXEMPLOS
       Example 1, Cancelando a Definição de uma Entrada de Ambiente para uma
       Aplicação Web
           Este exemplo cancela a definição de uma entrada de ambiente Hello
           User da aplicação Web hello. A entrada reverte ao valor definido no
           descritor de implantação da aplicação.

               asadmin> unset-web-env-entry --name="Hello User" hello

               Command unset-web-env-entry executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       list-applications(1), list-web-env-entry(1), set-web-env-entry(1)

       asadmin(1M)

Java EE 7                         30 out 2009           unset-web-env-entry(1)
