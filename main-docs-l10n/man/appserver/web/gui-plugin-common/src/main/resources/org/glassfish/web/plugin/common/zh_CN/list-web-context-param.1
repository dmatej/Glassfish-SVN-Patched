list-web-context-param(1)   asadmin 实用程序子命令   list-web-context-param(1)

名称
       list-web-context-param - 列出已部署 Web 应用程序或模块的 servlet
       上下文初始化参数

提要
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

说明
       list-web-context-param 子命令列出以下项之一的 Servlet
       上下文初始化参数：

       ·   已部署的 Web 应用程序

       ·   已部署的 Java Platform Enterprise Edition (Java EE) 应用程序中的
           Web 模块

       该应用程序必须已部署。否则，会出现错误。

       list-web-context-param 子命令仅列出以前使用 set-web-context-param(1)
       子命令设置的参数。该子命令不

       对于每个参数，都将显示以下信息：

       ·   参数的名称

       ·   为参数设置的值

       ·   设置参数时指定的 set-web-context-param 子命令的
           --ignoreDescriptorItem 选项的值

       ·   参数的说明或空

选项
       --help, -?
           显示子命令的帮助文本。

       --name
           要列出的 Servlet
           上下文初始化参数的名称。如果忽略此选项，则会列出应用程序的以前已设置的所有参数。

操作数
       application-name
           应用程序名称。可以从管理控制台或通过使用 list-applications(1)
           子命令获取此名称。

           该应用程序必须已部署。否则，会出现错误。

       module
           应用程序的企业档案 (EAR) 文件内的模块的相对路径。在应用程序的
           application.xml 文件的 module 元素中指定模块的路径。

           仅当 Servlet 上下文初始化参数应用于 Java EE 应用程序的 Web
           模块时，才需要 module。如果已指定，则 module 必须位于
           application-name 之后，用斜杠 (/) 分隔。

           例如，myApp 应用程序的 application.xml 文件可能会指定以下 Web
           模块：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           会将模块指定为此命令的操作数，如 myApp/myWebModule.war。

示例
       Example 1, 列出 Web 应用程序的 Servlet 上下文初始化参数
           此示例列出通过使用 set-web-context-param 子命令设置的 Web 应用程序
           basic-ezcomp 的所有 Servlet 上下文初始化参数。由于设置
           javax.faces.PROJECT_STAGE
           参数时未指定说明，因此将显示空

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日    list-web-context-param(1)
