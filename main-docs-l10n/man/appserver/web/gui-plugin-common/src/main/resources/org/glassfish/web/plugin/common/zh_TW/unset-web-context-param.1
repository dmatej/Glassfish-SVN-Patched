unset-web-context-param(1)  asadmin 公用程式子命令  unset-web-context-param(1)

參考名
       unset-web-context-param - 取消設定建置的 Web 應用程式或模組的 Servlet
       環境初始化參數

概要
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

描述
       unset-web-context-param 子命令可取消設定下列其中一個項目之 Servlet
       環境初始化參數：

       ·   建置的 Web 應用程式

       ·   已建置之 Java 平台、Enterprise Edition (Java EE) 應用程式中的 Web
           模組

       取消設定參數時，其值會復原為值
       (如果有)，且是在應用程式的建置描述元中設定。

       應用程式必須已完成建置。否則，將發生錯誤。

       該參數必須先使用 set-web-context-param 子命令設定。否則，將發生錯誤。

           注意
           ┌──────────────────────────────────────────────────────┐
           │備註                                                  │
           │                                                      │
           │           請不要使用                                 │
           │           unset-web-context-param                    │
           │           子命令來變更在應用程式的建置描述元中設定的 │
           │           Servlet                                    │
           │           環境初始化參數值。請改用                   │
           │           set-web-context-param(1)                   │
           │           子命令來達成此目的。                       │
           └──────────────────────────────────────────────────────┘

       此子命令可讓您變更已建置的應用程式組態，不需要修改應用程式的建置描述元，也不需要重新封裝或重新建置應用程式。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --name
           要取消設定的 Servlet 環境初始化參數名稱。此參數必須先使用
           set-web-context-param 子命令設定。否則，將發生錯誤。

運算元
       application-name
           應用程式的名稱。此名稱可從 管理主控台 取得，或可使用
           list-applications(1) 子命令取得。

           應用程式必須已完成建置。否則，將發生錯誤。

       module
           應用程式之企業存檔 (EAR) 檔案中模組的相對路徑。會在應用程式之
           application.xml 檔案的 module 元素中指定模組的路徑。

           只有在 Servlet 環境初始化參數套用至 Java EE 應用程式的 Web
           模組時，才需要使用 module。如果指定，module 必須接在
           application-name 之後，並以斜線 (/) 區隔。

           例如，myApp 應用程式的 application.xml 檔案可能會指定下列 Web
           模組：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           模組可能會被指定為此命令的運算元，其形式如下：myApp/myWebModule.war。

範例
       範例 1, 取消設定 Web 應用程式的 Servlet 環境初始化參數
           此範例會取消設定 Web 應用程式 basic-ezcomp 的 Servlet
           環境初始化參數 javax.faces.STATE_SAVING_METHOD。參數會復原為值
           (如果有)，且是在應用程式的建置描述元中定義。

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日   unset-web-context-param(1)
