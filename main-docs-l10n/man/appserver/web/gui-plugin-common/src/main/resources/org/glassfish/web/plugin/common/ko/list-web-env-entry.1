list-web-env-entry(1)     asadmin 유틸리티 하위 명령     list-web-env-entry(1)

제목
       list-web-env-entry - 배치된 웹 응용 프로그램 또는 모듈에 대한 환경 항목
       나열

시놉시스
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

설명
       list-web-env-entry 하위 명령은 다음 항목 중 하나의 환경 항목을
       나열합니다.

       ·   배치된 웹 응용 프로그램

       ·   배치된 Java 플랫폼, Enterprise Edition(Java EE) 응용 프로그램의 웹
           모듈

       해당 응용 프로그램은 미리 배치되어 있어야 합니다. 그렇지 않으면 오류가
       발생합니다.

       list-web-env-entry 하위 명령은 이전에 set-web-env-entry(1) 하위 명령을
       사용하여 설정된 항목만 나열합니다. 이 하위 명령은 응용 프로그램의 배치
       기술자에서만 설정된 환경 항목을 나열하지 않

       각 항목에 대해 표시되는 정보는 다음과 같습니다.

       ·   항목 이름

       ·   항목의 Java 유형

       ·   항목에 설정할 값

       ·   항목 설정 시 지정된 set-web-env-entry 하위 명령의
           --ignoreDescriptorItem 옵션 값

       ·   항목 설명 또는 null(항목 설정 시 설명을 지정하지 않은 경우)

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --name
           나열할 환경 항목의 이름입니다. 이 이름은 java:comp/env 컨텍스트에
           대한 상대적인 JNDI 이름입니다. 이름은 배치 구성 요소 내에서
           고유해야 합니다. 이 옵션을 생략하면 이전에 응용 프로그램에 대해
           설정된 모든 환경 항목이 나열됩니다.

피연산자
       application-name
           응용 프로그램의 이름입니다. 이 이름은 관리 콘솔 또는
           list-applications(1) 하위 명령을 통해 얻을 수 있습니다.

           해당 응용 프로그램은 미리 배치되어 있어야 합니다. 그렇지 않으면
           오류가 발생합니다.

       module
           응용 프로그램의 엔터프라이즈 아카이브(EAR) 파일 내 모듈의 상대
           경로입니다. 모듈의 경로는 응용 프로그램의 application.xml 파일에
           있는 module 요소에서 지정합니다.

           module은 환경 항목이 Java EE 응용 프로그램의 웹 모듈에 적용되는
           경우에만 필요합니다. 지정된 경우 module은 application-name 뒤에
           오고 슬래시(/)로 구분되어야 합니다.

           예를 들어, myApp 응용 프로그램의 application.xml 파일은 다음과 같은
           웹 모듈을 지정할 수 있습니다.

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           모듈은 이 명령의 피연산자 myApp/myWebModule.war로 지정됩니다.

예제
       예 1, 웹 응용 프로그램의 환경 항목 나열
           이 예제는 set-web-env-entry 하위 명령을 사용하여 웹 응용 프로그램
           hello에 대해 설정된 모든 환경 항목을 나열합니다.  Hello Port 환경
           항목 설정 시 설명을 지정하지 않았으므로 이 항목에 대한 설명 대신
           null이 표시됩니다.

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                      2009년 10월 30일          list-web-env-entry(1)
