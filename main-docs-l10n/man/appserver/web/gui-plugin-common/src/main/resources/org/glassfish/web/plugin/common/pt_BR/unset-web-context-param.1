unset-web-context-param(Subcomandos do utilitário asunset-web-context-param(1)

NOME
       unset-web-context-param - cancela a definição de um parâmetro de
       inicialização de contexto de servlet de uma aplicação ou módulo web

SINOPSE
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

DESCRIçãO
       O subcomando unset-web-context-param não define um parâmetro de
       inicialização do contexto servlet de um dos itens a seguir:

       ·   Uma aplicação Web implantada

       ·   Um módulo web em uma aplicação da Plataforma Java, Enterprise
           Edition (Java EE) implantada

       Quando o parâmetro é indefinido, seu valor reverte-se ao valor, se
       houver, que está definido no descritor de implantação da aplicação.

       A aplicação já deve ter sido implantada. Caso contrário, ocorrerá um
       erro.

       Esse parâmetro deve ter sido previamente definido com a utilização do
       subcomando set-web-context-param. Caso contrário, ocorrerá um erro.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Não utilize o subcomando     │
           │           unset-web-context-param para │
           │           alterar o valor de um        │
           │           parâmetro de inicialização   │
           │           do contexto servlet que é    │
           │           definido em um descritor de  │
           │           implantação da aplicação. Em │
           │           vez disso, use o subcomando  │
           │           set-web-context-param(1)     │
           │           para esta finalidade.        │
           └────────────────────────────────────────┘

       Este subcomando permite alterar a configuração de uma aplicação
       implantada sem a necessidade de modificar os descritores de implantação
       da aplicação e reencapsular e reimplantar a aplicação.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --name
           O nome do parâmetro de inicialização de contexto servlet que é para
           ser indefinido. Esse parâmetro deve ter sido previamente definido
           com a utilização do subcomando set-web-context-param. Caso
           contrário, ocorrerá um erro.

OPERANDOS
       application-name
           O nome da aplicação. Esse nome pode ser obtido na Console de
           Administração ou usando o subcomando list-applications(1).

           A aplicação já deve ter sido implantada. Caso contrário, ocorrerá
           um erro.

       module
           O caminho relativo para o módulo do arquivo compactado EAR
           (Enterprise archive) da aplicação. O caminho para o módulo é
           especificado no elemento module do arquivo application.xml da
           aplicação.

           O module será necessário apenas se o parâmetro no contexto de
           inicialização de servlet se referir a um módulo Web ou a uma
           aplicação Java EE. Se especificado, module deve seguir
           application-name, separado por uma barra (/).

           Por exemplo o arquivo application.xml para a aplicação myApp pode
           especificar o seguinte módulo Web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           O módulo seria especificado como o operando deste comando como
           myApp/myWebModule.war.

EXEMPLOS
       Example 1, Cancelando a Definição de um Parâmetro de Inicialização de
       Contexto do Servlet para uma Aplicação Web
           Este exemplo cancela a definição do parâmetro de inicialização de
           contexto servlet javax.faces.STATE_SAVING_METHOD da aplicação Web
           basic-ezcomp. O parâmetro reverte para o valor, se houver, que está
           definido no descritor de implantação da aplicação.

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                         30 out 2009       unset-web-context-param(1)
