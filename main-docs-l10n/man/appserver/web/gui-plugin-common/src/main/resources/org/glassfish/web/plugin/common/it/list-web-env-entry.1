list-web-env-entry(1)   Comandi secondari della utilit   list-web-env-entry(1)

NOME
       list-web-env-entry - elenca le voci di ambiente per un'applicazione Web
       o un modulo distribuito

SINTASSI
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

DESCRIZIONE
       Il comando secondario list-web-env-entry elenca le voci di ambiente per
       uno degli elementi seguenti:

       ·   un'applicazione Web distribuita;

       ·   un modulo Web in un'applicazione Java Platform, Enterprise Edition
           (Java EE) distribuita.

       L'applicazione deve essere stata già distribuita. In caso contrario, si
       verifica un errore.

       Il comando secondario list-web-env-entry elenca solo le voci
       precedentemente impostate utilizzando il comando secondario
       set-web-env-entry(1).  Non elenca le voci di ambiente impostate
       esclusivamente nel descrittore di distribuzione dell'applicazione.

       Per ogni voce, vengono visualizzate le informazioni seguenti:

       ·   il nome della voce;

       ·   il tipo Java della voce;

       ·   il valore su cui è impostata la voce;

       ·   il valore dell'opzione --ignoreDescriptorItem del comando
           secondario set-web-env-entry specificato durante l'impostazione
           della voce;

       ·   la descrizione della voce oppure null se non è stata specificata
           alcuna descrizione durante l'impostazione della voce.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --name
           Il nome della voce di ambiente da elencare. Il nome è un nome JNDI
           relativo al contesto java:comp/env. Il nome deve essere univoco
           all'interno di un componente di distribuzione. Se questa opzione
           viene omessa, vengono elencate tutte le voci di ambiente impostate
           in precedenza per l'applicazione.

OPERANDI
       application-name
           Il nome dell'applicazione. Il nome può essere ottenuto da Console
           di amministrazione o utilizzando il comando secondario
           list-applications(1).

           L'applicazione deve essere stata già distribuita. In caso
           contrario, si verifica un errore.

       module
           Il percorso relativo del modulo all'interno del file EAR
           dell'applicazione. Il percorso del modulo è specificato
           nell'elemento module del file application.xml dell'applicazione.

           module è necessario solo se la voce di ambiente si applica a un
           modulo Web di un'applicazione Java EE. Se specificata, module deve
           seguire application-name, separato da una barra (/).

           Ad esempio, il file application.xml per l'applicazione myApp
           potrebbe specificare il modulo Web seguente:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Il modulo sarebbe specificato come operando di questo comando:
           myApp/myWebModule.war.

ESEMPI
       Esempio 1, Visualizzazione della lista delle voci di ambiente per
       un'applicazione Web
           In questo esempio vengono elencate tutte le voci di ambiente
           impostate per l'applicazione Web hello mediante il comando
           secondario set-web-env-entry. Poiché durante l'impostazione della
           voce di ambiente Hello Port non è stata specificata alcuna
           descrizione, viene visualizzato il valore null anziché una
           descrizione della voce.

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                       30 ottobre 2009          list-web-env-entry(1)
