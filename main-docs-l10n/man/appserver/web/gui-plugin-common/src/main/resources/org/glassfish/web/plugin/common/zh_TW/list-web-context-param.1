list-web-context-param(1)   asadmin 公用程式子命令   list-web-context-param(1)

參考名
       list-web-context-param - 列出已建置之 Web 應用程式或模組的 Servlet
       環境初始化參數

概要
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

描述
       list-web-context-param 子命令會列出下列其中一個項目的 Servlet
       環境初始化參數：

       ·   建置的 Web 應用程式

       ·   已建置之 Java 平台、Enterprise Edition (Java EE) 應用程式中的 Web
           模組

       應用程式必須已完成建置。否則，將發生錯誤。

       list-web-context-param 子命令只會列出先前已使用
       set-web-context-param(1)
       子命令設定的參數。此子命令不

       對於每個參數，將會顯示以下資訊：

       ·   參數名稱

       ·   參數的設定值

       ·   set-web-context-param 子命令之 --ignoreDescriptorItem
           選項的值會在設定參數時指定

       ·   參數說明或 null (如果設定參數時沒有指定說明)

選項
       --help, -?
           顯示子命令的說明文字。

       --name
           要列出的 Servlet
           環境初始化參數名稱。如果省略此選項，則會列出先前已設定之應用程式的所有參數。

運算元
       application-name
           應用程式的名稱。此名稱可從 管理主控台 取得，或可使用
           list-applications(1) 子命令取得。

           應用程式必須已完成建置。否則，將發生錯誤。

       module
           應用程式之企業存檔 (EAR) 檔案中模組的相對路徑。會在應用程式之
           application.xml 檔案的 module 元素中指定模組的路徑。

           只有在 Servlet 環境初始化參數套用至 Java EE 應用程式的 Web
           模組時，才需要使用 module。如果指定，module 必須接在
           application-name 之後，並以斜線 (/) 區隔。

           例如，myApp 應用程式的 application.xml 檔案可能會指定下列 Web
           模組：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           模組可能會被指定為此命令的運算元，其形式如下：myApp/myWebModule.war。

範例
       範例 1, 列出 Web 應用程式的 Servlet 環境初始化參數
           此範例會列出 Web 應用程式 basic-ezcomp 的所有 Servlet
           環境初始化參數，這些參數是使用 set-web-context-param
           子命令所設定。由於在設定 javax.faces.PROJECT_STAGE
           參數時未指定任何描述，因此會顯示 null 而非此參數的描述。

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日    list-web-context-param(1)
