unset-web-env-entry(1)  Comandi secondari della utilit  unset-web-env-entry(1)

NOME
       unset-web-env-entry - annulla l'impostazione di una voce di ambiente
       per un'applicazione Web o un modulo distribuito

SINTASSI
           unset-web-env-entry [--help] --name=env-entry-name application-name[/module]

DESCRIZIONE
       Il comando secondario unset-web-env-entry annulla l'impostazione di una
       voce di ambiente per uno degli elementi seguenti:

       ·   un'applicazione Web distribuita;

       ·   un modulo Web in un'applicazione Java Platform, Enterprise Edition
           (Java EE) distribuita.

       Quando si annulla l'impostazione di una voce, viene ripristinato
       l'eventuale valore impostato nel descrittore di distribuzione
       dell'applicazione.

       L'applicazione deve essere stata già distribuita. In caso contrario, si
       verifica un errore.

       La voce deve essere stata imposta in precedenza mediante il comando
       secondario set-web-env-entry(1). In caso contrario, si verifica un
       errore.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Non utilizzare il comando   │
           │           secondario                  │
           │           unset-web-env-entry per     │
           │           modificare il valore di una │
           │           voce di ambiente impostata  │
           │           nel descrittore di          │
           │           distribuzione di            │
           │           un'applicazione. A tale     │
           │           scopo, utilizzare invece il │
           │           comando secondario          │
           │           set-web-env-entry.          │
           └───────────────────────────────────────┘

       Questo comando secondario consente di modificare la configurazione di
       un'applicazione distribuita senza dover modificare i descrittori di
       distribuzione dell'applicazione né creare un nuovo package e
       ridistribuire l'applicazione.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --name
           Il nome della voce di ambiente di cui annullare l'impostazione. Il
           nome è un nome JNDI relativo al contesto java:comp/env. Il nome
           deve essere univoco all'interno di un componente di distribuzione.
           La voce deve essere stata imposta in precedenza mediante
           set-web-env-entry. In caso contrario, si verifica un errore.

OPERANDI
       application-name
           Il nome dell'applicazione. Il nome può essere ottenuto da Console
           di amministrazione o utilizzando il comando secondario
           list-applications(1).

           L'applicazione deve essere stata già distribuita. In caso
           contrario, si verifica un errore.

       module
           Il percorso relativo del modulo all'interno del file EAR
           dell'applicazione. Il percorso del modulo è specificato
           nell'elemento module del file application.xml dell'applicazione.

           module è necessario solo se la voce di ambiente si applica a un
           modulo Web di un'applicazione Java EE. Se specificata, module deve
           seguire application-name, separato da una barra (/).

           Ad esempio, il file application.xml per l'applicazione myApp
           potrebbe specificare il modulo Web seguente:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Il modulo sarebbe specificato come operando di questo comando:
           myApp/myWebModule.war.

ESEMPI
       Esempio 1, Annullamento dell'impostazione di una voce di ambiente per
       un'applicazione Web
           In questo esempio viene annullata l'impostazione della voce di
           ambiente Hello User dell'applicazione Web hello. Per la voce viene
           ripristinato l'eventuale valore definito nel descrittore di
           distribuzione dell'applicazione.

               asadmin> unset-web-env-entry --name="Hello User" hello

               Command unset-web-env-entry executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       list-applications(1), list-web-env-entry(1), set-web-env-entry(1)

       asadmin(1M)

Java EE 7                       30 ottobre 2009         unset-web-env-entry(1)
