unset-web-env-entry(1)  Unterbefehle des asadmin-Utili  unset-web-env-entry(1)

NAME
       unset-web-env-entry - hebt die Einstellung eines Umgebungseintrags für
       eine bereitgestellte Webanwendung oder ein bereitgestelltes Modul auf

ZUSAMMENFASSUNG
           unset-web-env-entry [--help] --name=env-entry-name application-name[/module]

BESCHREIBUNG
       Der Unterbefehl unset-web-env-entry hebt die Einstellung eines
       Umgebungseintrags für eines der folgenden Elemente auf:

       ·   Eine bereitgestellte Webanwendung

       ·   Webmodul in einer bereitgestellten Java Platform, Enterprise
           Edition-(Java EE-)Anwendung

       Wenn Sie die Einstellung eines Eintrags aufheben, wird der Wert auf den
       Wert zurückgesetzt, der im Deployment-Deskriptor der Anwendung
       festgelegt wurde (sofern ein solcher Wert vorhanden ist).

       Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt ein
       Fehler auf.

       Der Eintrag muss zuvor mit dem Unterbefehl set-web-env-entry(1)
       festgelegt worden sein. Andernfalls tritt ein Fehler auf.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Verwenden Sie den            │
           │           Unterbefehl                  │
           │           unset-web-env-entry nicht,   │
           │           um den Wert eines            │
           │           Umgebungseintrags zu ändern, │
           │           der im Deployment-Deskriptor │
           │           einer Anwendung festgelegt   │
           │           wurde. Verwenden Sie zu      │
           │           diesem Zweck stattdessen den │
           │           Unterbefehl                  │
           │           set-web-env-entry.           │
           └────────────────────────────────────────┘

       Mit diesem Unterbefehl können Sie die Konfiguration einer
       bereitgestellten Anwendung ändern, ohne die Deployment-Deskriptoren der
       Anwendung zu modifizieren und die Anwendung erneut in ein Package
       einzubinden und bereitzustellen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Der Name des Umgebungseintrags, dessen Einstellung aufgehoben
           werden soll. Der Name ist ein JNDI-Name, der auf den
           java:comp/env-Kontext bezogen ist. Der Name muss innerhalb einer
           Deployment-Komponente eindeutig sein. Dieser Eintrag muss zuvor mit
           dem Unterbefehl set-web-env-entry festgelegt worden sein.
           Andernfalls tritt ein Fehler auf.

OPERANDEN
       application-name
           Der Name der Anwendung. Dieser Name kann aus der
           Administrationskonsole oder mit dem Unterbefehl
           list-applications(1) abgerufen werden.

           Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt
           ein Fehler auf.

       module
           Der relative Pfad zum Modul innerhalb der Enterprise
           Archive-(EAR-)Datei der Anwendung. Der Pfad zum Modul wird im
           module-Element der application.xml-Datei der Anwendung angegeben.

           module ist nur dann erforderlich, wenn der Umgebungseintrag auf ein
           Webmodul einer Java EE-Anwendung bezogen ist. Falls angegeben, muss
           module auf application-name folgen und durch einen Schrägstrich (/)
           getrennt sein.

           Beispiel: Die Datei application.xml für die Anwendung myApp kann
           das folgende Webmodul angeben:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Das Modul würde als Operand dieses Befehls als
           myApp/myWebModule.war angegeben werden.

BEISPIELE
       Example 1, Einstellung eines Umgebungseintrags für eine Webanwendung
       aufheben
           In diesem Beispiel wird die Einstellung des Umgebungseintrags Hello
           User der Webanwendung hello aufgehoben. Der Eintrag wird auf den
           Wert zurückgesetzt, der im Deployment-Deskriptor der Anwendung
           definiert wurde (sofern ein solcher Wert vorhanden ist).

               asadmin> unset-web-env-entry --name="Hello User" hello

               Command unset-web-env-entry executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       list-applications(1), list-web-env-entry(1), set-web-env-entry(1)

       asadmin(1M)

Java EE 7                        30. Okt. 2009          unset-web-env-entry(1)
