list-web-env-entry(1)       asadmin 公用程式子命令       list-web-env-entry(1)

參考名
       list-web-env-entry - 列出已建置之 Web 應用程式或模組的環境項目

概要
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

描述
       list-web-env-entry 子命令會列出下列其中一個項目的環境項目：

       ·   建置的 Web 應用程式

       ·   已建置之 Java 平台、Enterprise Edition (Java EE) 應用程式中的 Web
           模組

       應用程式必須已完成建置。否則，將發生錯誤。

       list-web-env-entry 子命令只會列出先前已使用 set-web-env-entry(1)
       子命令設定的項目。此子命令不

       對於每個項目，將會顯示以下資訊：

       ·   項目名稱

       ·   項目的 Java 類型

       ·   項目的設定值

       ·   set-web-env-entry 子命令之 --ignoreDescriptorItem
           選項的值會在設定項目時指定

       ·   項目說明或 null (如果設定項目時沒有指定說明)

選項
       --help, -?
           顯示子命令的說明文字。

       --name
           要列出之環境項目的名稱。此名稱為相對於 java:comp/env 環境的 JNDI
           名稱。名稱在建置元件中必須是唯一的。如果省略此選項，則會列出先前已針對應用程式設定的所有環境項目。

運算元
       application-name
           應用程式的名稱。此名稱可從 管理主控台 取得，或可使用
           list-applications(1) 子命令取得。

           應用程式必須已完成建置。否則，將發生錯誤。

       module
           應用程式之企業存檔 (EAR) 檔案中模組的相對路徑。會在應用程式之
           application.xml 檔案的 module 元素中指定模組的路徑。

           只有在環境項目套用至 Java EE 應用程式的 Web 模組時，才需要使用
           module。如果指定，module 必須接在 application-name 之後，並以斜線
           (/) 區隔。

           例如，myApp 應用程式的 application.xml 檔案可能會指定下列 Web
           模組：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           模組可能會被指定為此命令的運算元，其形式如下：myApp/myWebModule.war。

範例
       範例 1, 列出 Web 應用程式的環境項目
           此範例會列出使用 set-web-env-entry 子命令針對 Web 應用程式 hello
           設定的所有環境項目。由於在設定 Hello Port
           環境項目時未指定任何說明，因此會顯示 null 而非此項目的說明。

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日        list-web-env-entry(1)
