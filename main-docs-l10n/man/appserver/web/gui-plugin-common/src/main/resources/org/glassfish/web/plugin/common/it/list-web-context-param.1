list-web-context-param(1Comandi secondari della utililist-web-context-param(1)

NOME
       list-web-context-param - elenca i parametri di inizializzazione del
       contesto del servlet di un'applicazione Web o di un modulo distribuito

SINTASSI
           list-web-context-param [--help]
           [--name=context-param-name] application-name[/module]

DESCRIZIONE
       Il comando secondario list-web-context-param elenca i parametri di
       inizializzazione del contesto servlet di uno degli elementi seguenti:

       ·   un'applicazione Web distribuita;

       ·   un modulo Web in un'applicazione Java Platform, Enterprise Edition
           (Java EE) distribuita.

       L'applicazione deve essere stata già distribuita. In caso contrario, si
       verifica un errore.

       Il comando secondario list-web-context-param elenca solo i parametri
       precedentemente impostati utilizzando il comando secondario
       set-web-context-param(1).  Non elenca i parametri impostati
       esclusivamente nel descrittore di distribuzione dell'applicazione.

       Per ogni parametro, vengono visualizzate le informazioni seguenti:

       ·   il nome del parametro;

       ·   il valore su cui è impostato il parametro;

       ·   il valore dell'opzione --ignoreDescriptorItem del comando
           secondario set-web-context-param specificato durante l'impostazione
           del parametro;

       ·   la descrizione del parametro oppure null se non è stata specificata
           alcuna descrizione durante l'impostazione del parametro.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --name
           Il nome del parametro di inizializzazione del contesto servlet da
           elencare. Se questa opzione viene omessa, vengono elencati tutti i
           parametri dell'applicazione impostati in precedenza.

OPERANDI
       application-name
           Il nome dell'applicazione. Il nome può essere ottenuto da Console
           di amministrazione o utilizzando il comando secondario
           list-applications(1).

           L'applicazione deve essere stata già distribuita. In caso
           contrario, si verifica un errore.

       module
           Il percorso relativo del modulo all'interno del file EAR
           dell'applicazione. Il percorso del modulo è specificato
           nell'elemento module del file application.xml dell'applicazione.

           module è necessario solo se il parametro di inizializzazione del
           contesto del servlet si applica a un modulo Web di un'applicazione
           Java EE. Se specificata, module deve seguire application-name,
           separato da una barra (/).

           Ad esempio, il file application.xml per l'applicazione myApp
           potrebbe specificare il modulo Web seguente:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Il modulo sarebbe specificato come operando di questo comando:
           myApp/myWebModule.war.

ESEMPI
       Esempio 1, Visualizzazione della lista dei parametri di
       inizializzazione del contesto servlet per un'applicazione Web
           In questo esempio vengono elencati tutti i parametri di
           inizializzazione del contesto servlet dell'applicazione Web
           basic-ezcomp che sono stati impostati mediante il comando
           secondario set-web-context-param. Poiché durante l'impostazione del
           parametro javax.faces.PROJECT_STAGE non è stata specificata alcuna
           descrizione, viene visualizzato il valore null anziché una
           descrizione del parametro.

               asadmin> list-web-context-param basic-ezcomp
               javax.faces.STATE_SAVING_METHOD = client ignoreDescriptorItem=false
               //The location where the application's state is preserved
               javax.faces.PROJECT_STAGE = null ignoreDescriptorItem=true //null

               Command list-web-context-param executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       list-applications(1), set-web-context-param(1),
       unset-web-context-param(1)

       asadmin(1M)

Java EE 7                       30 ottobre 2009      list-web-context-param(1)
