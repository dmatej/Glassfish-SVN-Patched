list-web-env-entry(1)   Subcomandos do utilitário asad   list-web-env-entry(1)

NOME
       list-web-env-entry - lista entradas de ambiente de uma aplicação ou
       módulo web implantado(a)

SINOPSE
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

DESCRIçãO
       O subcomando list-web-env-entry lista as entradas de ambiente para um
       dos seguintes itens:

       ·   Uma aplicação Web implantada

       ·   Um módulo web em uma aplicação da Plataforma Java, Enterprise
           Edition (Java EE) implantada

       A aplicação já deve ter sido implantada. Caso contrário, ocorrerá um
       erro.

       O subcomando list-web-env-entry lista somente entradas que foram
       previamente definidas utilizando o subcomando set-web-env-entry(1). O
       subcomando não lista entradas de ambiente que estejam definidas somente
       no descritor de implantação da aplicação.

       Para cada entrada, as seguintes informações são exibidas:

       ·   O nome da entrada

       ·   O tipo Java da entrada

       ·   O valor definido como a entrada

       ·   O valor da opção --ignoreDescriptorItem do subcomando
           set-web-env-entry que foi especificado quando a entrada foi
           definida

       ·   A descrição da entrada ou null se nenhuma descrição foi
           especificada quando a entrada foi definida

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --name
           O nome da entrada de ambiente a ser listada. O nome é um nome da
           JNDI relativo ao contexto java:comp/env. O nome deve ser exclusivo
           em um componente de implantação. Se essa opção for omitida, todas
           as entradas de ambiente que tiverem sido definidas previamente para
           a aplicação serão listadas.

OPERANDOS
       application-name
           O nome da aplicação. Esse nome pode ser obtido na Console de
           Administração ou usando o subcomando list-applications(1).

           A aplicação já deve ter sido implantada. Caso contrário, ocorrerá
           um erro.

       module
           O caminho relativo para o módulo do arquivo compactado EAR
           (Enterprise archive) da aplicação. O caminho para o módulo é
           especificado no elemento module do arquivo application.xml da
           aplicação.

           module será necessário apenas se a entrada de ambiente se referir a
           um módulo Web de uma aplicação Java EE. Se especificado, module
           deve seguir application-name, separado por uma barra (/).

           Por exemplo o arquivo application.xml para a aplicação myApp pode
           especificar o seguinte módulo Web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           O módulo seria especificado como o operando deste comando como
           myApp/myWebModule.war.

EXEMPLOS
       Example 1, Listando Entradas de Ambiente para uma Aplicação Web
           Este exemplo lista todas as entradas de ambiente definidas para a
           aplicação Web hello com o subcomando set-web-env-entry. Como
           nenhuma descrição foi especificada quando a entrada de ambiente
           Hello Port foi definida, null é exibido em vez de uma descrição
           para esta entrada.

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                         30 out 2009            list-web-env-entry(1)
