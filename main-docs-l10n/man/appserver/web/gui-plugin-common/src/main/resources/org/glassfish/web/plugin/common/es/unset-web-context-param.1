unset-web-context-param(Subcomandos de la Utilidad aunset-web-context-param(1)

NOMBRE DE REFERENCIA
       unset-web-context-param - anula la definición de un parámetro de
       inicialización de contexto de servlet de una aplicación web o un módulo
       desplegado.

SINOPSIS
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

DESCRIPCIÓN
       El subcomando unset-web-context-param anula la definición de un
       parámetro de inicialización de contexto de servlet de uno de los
       elementos siguientes:

       ·   Una aplicación web desplegada

       ·   Un módulo web en una aplicación de Java Platform, Enterprise
           Edition (Java EE) desplegada

       Cuando se anula la definición de un parámetro, su valor vuelve al
       valor, si lo hay, que se define en el descriptor de despliegue de la
       aplicación.

       La aplicación debe estar ya desplegada. De lo contrario, se produce un
       error.

       El parámetro se debe haber definido previamente mediante el subcomando
       set-web-context-param. De lo contrario, se produce un error.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           No utilice el subcomando     │
           │           unset-web-context-param para │
           │           cambiar el valor de un       │
           │           parámetro de inicialización  │
           │           de contexto de servlet que   │
           │           se define en el descriptor   │
           │           de despliegue de una         │
           │           aplicación. En su lugar,     │
           │           utilice para ello el         │
           │           subcomando                   │
           │           set-web-context-param(1).    │
           └────────────────────────────────────────┘

       Este subcomando permite cambiar la configuración de una aplicación
       desplegada sin tener que modificar los descriptores de despliegue de la
       aplicación y volver a empaquetar y desplegar la aplicación.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           El nombre del parámetro de inicialización de contexto de servlet
           cuya definición se va a anular. El parámetro se debe haber definido
           previamente mediante el subcomando set-web-context-param. De lo
           contrario, se produce un error.

OPERANDOS
       application-name
           Nombre de la aplicación. Este nombre puede obtenerse de la Consola
           de Administración o mediante el subcomando list-applications(1).

           La aplicación debe estar ya desplegada. De lo contrario, se produce
           un error.

       module
           Ruta de acceso relativa al módulo dentro del archivo de
           almacenamiento de empresa (EAR) de la aplicación. La ruta de acceso
           al módulo se especifica en el elemento module del archivo
           application.xml de la aplicación.

           module sólo es necesaria si el parámetro de inicialización de
           contexto de servlet se aplica a un módulo web de una aplicación
           Java EE. Si se especifica, module se debe ubicar a continuación de
           application-name, separados por una barra (/).

           Por ejemplo, el archivo application.xml de la aplicación myApp
           puede especificar el siguiente módulo web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           El módulo se especificaría como operando de este comando como
           myApp/myWebModule.war.

EJEMPLOS
       Ejemplo 1, Anulación de la definición de un parámetro de inicialización
       de contexto de servlet para una aplicación web
           En este ejemplo se anula la definición del parámetro de
           inicialización de contexto de servlet
           javax.faces.STATE_SAVING_METHOD de la aplicación web basic-ezcomp.
           El parámetro vuelve al valor, si lo hay, que se define en el
           descriptor de despliegue de la aplicación.

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                    30 de octubre de 2009  unset-web-context-param(1)
