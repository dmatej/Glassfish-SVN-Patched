list-web-env-entry(1)       asadmin 实用程序子命令       list-web-env-entry(1)

名称
       list-web-env-entry - 列出已部署 Web 应用程序或模块的环境条目

提要
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

说明
       list-web-env-entry 子命令列出以下项之一的环境条目：

       ·   已部署的 Web 应用程序

       ·   已部署的 Java Platform Enterprise Edition (Java EE) 应用程序中的
           Web 模块

       该应用程序必须已部署。否则，会出现错误。

       list-web-env-entry 子命令仅列出以前使用 set-web-env-entry(1)
       子命令设置的条目。该子命令不

       对于每个条目，都将显示以下信息：

       ·   条目的名称

       ·   条目的 Java 类型

       ·   将条目设置为的值

       ·   设置条目时指定的 set-web-env-entry 子命令的 --ignoreDescriptorItem
           选项的值

       ·   条目的说明或空

选项
       --help, -?
           显示子命令的帮助文本。

       --name
           要列出的环境条目的名称。该名称是相对于 java:comp/env 上下文的 JNDI
           名称。该名称在部署组件中必须唯一。如果忽略此选项，则会列出以前为应用程序设置的所有环境条目。

操作数
       application-name
           应用程序名称。可以从管理控制台或通过使用 list-applications(1)
           子命令获取此名称。

           该应用程序必须已部署。否则，会出现错误。

       module
           应用程序的企业档案 (EAR) 文件内的模块的相对路径。在应用程序的
           application.xml 文件的 module 元素中指定模块的路径。

           仅当环境条目适用于 Java EE 应用程序的 Web 模块时需要
           module。如果已指定，则 module 必须位于 application-name
           之后，用斜杠 (/) 分隔。

           例如，myApp 应用程序的 application.xml 文件可能会指定以下 Web
           模块：

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           会将模块指定为此命令的操作数，如 myApp/myWebModule.war。

示例
       Example 1, 列出 Web 应用程序的环境条目
           此示例通过使用 set-web-env-entry 子命令列出已为 Web 应用程序 hello
           设置的所有环境条目。由于设置 Hello Port
           环境条目时未指定说明，因此将显示空

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                     2009 年 10 月 30 日        list-web-env-entry(1)
