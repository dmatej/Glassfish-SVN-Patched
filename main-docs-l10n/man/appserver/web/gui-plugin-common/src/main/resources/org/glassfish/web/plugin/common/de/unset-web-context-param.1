unset-web-context-param(Unterbefehle des asadmin-Utiunset-web-context-param(1)

NAME
       unset-web-context-param - hebt die Festlegung eines
       Servlet-Kontextinitialisierungsparameters einer bereitgestellten
       Webanwendung bzw. eines Moduls auf

ZUSAMMENFASSUNG
           unset-web-context-param [--help]
           --name=context-param-name application-name[/module]

BESCHREIBUNG
       Der Unterbefehl unset-web-context-param hebt die Festlegung eines
       Servlet-Kontextinitialisierungsparameters eines der folgenden Elemente
       auf:

       ·   Eine bereitgestellte Webanwendung

       ·   Webmodul in einer bereitgestellten Java Platform, Enterprise
           Edition-(Java EE-)Anwendung

       Wenn die Festlegung eines Parameters aufgehoben wird, wird sein Wert
       auf den Wert zurückgesetzt, der im Deployment-Deskriptor der Anwendung
       festgelegt wurde (sofern ein solcher Wert vorhanden ist).

       Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt ein
       Fehler auf.

       Der Parameter muss zuvor mithilfe des Unterbefehls
       set-web-context-param festgelegt worden sein. Andernfalls tritt ein
       Fehler auf.

           Note
           ┌─────────────────────────────────────────────────────┐
           │Hinweis                                              │
           │                                                     │
           │           Verwenden Sie den                         │
           │           Unterbefehl                               │
           │           unset-web-context-param                   │
           │           nicht, um den Wert eines                  │
           │           Servlet-Kontextinitialisierungsparameters │
           │           zu ändern, der in einem                   │
           │           Deployment-Deskriptor einer               │
           │           Anwendung festgelegt wurde.               │
           │           Verwenden Sie stattdessen                 │
           │           den Unterbefehl                           │
           │           set-web-context-param(1).                 │
           └─────────────────────────────────────────────────────┘

       Mit diesem Unterbefehl können Sie die Konfiguration einer
       bereitgestellten Anwendung ändern, ohne die Deployment-Deskriptoren der
       Anwendung zu modifizieren und die Anwendung erneut in ein Package
       einzubinden und bereitzustellen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Der Name des Servlet-Kontextinitialisierungsparameters, dessen
           Festlegung aufgehoben werden soll. Dieser Parameter muss zuvor
           mithilfe des Unterbefehls set-web-context-param festgelegt worden
           sein. Andernfalls tritt ein Fehler auf.

OPERANDEN
       application-name
           Der Name der Anwendung. Dieser Name kann aus der
           Administrationskonsole oder mit dem Unterbefehl
           list-applications(1) abgerufen werden.

           Die Anwendung muss bereits bereitgestellt sein. Andernfalls tritt
           ein Fehler auf.

       module
           Der relative Pfad zum Modul innerhalb der Enterprise
           Archive-(EAR-)Datei der Anwendung. Der Pfad zum Modul wird im
           module-Element der application.xml-Datei der Anwendung angegeben.

           module ist nur dann erforderlich, wenn der
           Servlet-Kontextinitialisierungsparameter auf ein Webmodul einer
           Java EE-Anwendung bezogen ist. Falls angegeben, muss module auf
           application-name folgen und durch einen Schrägstrich (/) getrennt
           sein.

           Beispiel: Die Datei application.xml für die Anwendung myApp kann
           das folgende Webmodul angeben:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           Das Modul würde als Operand dieses Befehls als
           myApp/myWebModule.war angegeben werden.

BEISPIELE
       Example 1, Festlegung eines Servlet-Kontextinitialisierungsparameters
       für eine Webanwendung aufheben
           In diesem Beispiel wird die Festlegung des
           Servlet-Kontextinitialisierungsparameters
           javax.faces.STATE_SAVING_METHOD der Webanwendung basic-ezcomp
           aufgehoben. Der Parameter wird auf den Wert zurückgesetzt, der im
           Deployment-Deskriptor der Anwendung definiert wurde (sofern ein
           solcher Wert vorhanden ist).

               asadmin> unset-web-context-param
               --name=javax.faces.STATE_SAVING_METHOD basic-ezcomp

               Command unset-web-context-param executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       list-applications(1), list-web-context-param(1),
       set-web-context-param(1)

       asadmin(1M)

Java EE 7                        30. Okt. 2009      unset-web-context-param(1)
