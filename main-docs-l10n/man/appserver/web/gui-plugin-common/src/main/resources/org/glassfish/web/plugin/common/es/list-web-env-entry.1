list-web-env-entry(1)   Subcomandos de la Utilidad asa   list-web-env-entry(1)

NOMBRE DE REFERENCIA
       list-web-env-entry - muestra entradas de entorno para un módulo o una
       aplicación web desplegada.

SINOPSIS
           list-web-env-entry [--help] [--name=env-entry-name] application-name[/module]

DESCRIPCIÓN
       El subcomando list-web-env-entry muestra las entradas de entorno para
       uno de los siguientes elementos:

       ·   Una aplicación web desplegada

       ·   Un módulo web en una aplicación de Java Platform, Enterprise
           Edition (Java EE) desplegada

       La aplicación debe estar ya desplegada. De lo contrario, se produce un
       error.

       El subcomando list-web-env-entry muestra sólo las entradas que se han
       configurado previamente mediante el subcomando set-web-env-entry(1). El
       subcomando no muestra las entradas de entorno definidas únicamente en
       el descriptor de despliegue de la aplicación.

       Para cada entrada se indica la siguiente información:

       ·   El nombre de la entrada

       ·   El tipo Java de la entrada

       ·   El valor definido para la entrada

       ·   El valor de la opción --ignoreDescriptorItem del subcomando
           set-web-env-entry especificado cuando se definió la entrada

       ·   La descripción de la entrada o null en caso de que no se hubiese
           incluido descripción al definir la entrada

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           El nombre de la entrada de entorno que se va a enumerar. Se trata
           de un nombre JNDI relativo al contexto java:comp/env. El nombre
           debe ser único dentro de un componente de despliegue. Si se omite
           esta opción, se enumeran todas las entradas de entorno que se han
           configurado previamente para la aplicación.

OPERANDOS
       application-name
           Nombre de la aplicación. Este nombre puede obtenerse de la Consola
           de Administración o mediante el subcomando list-applications(1).

           La aplicación debe estar ya desplegada. De lo contrario, se produce
           un error.

       module
           Ruta de acceso relativa al módulo dentro del archivo de
           almacenamiento de empresa (EAR) de la aplicación. La ruta de acceso
           al módulo se especifica en el elemento module del archivo
           application.xml de la aplicación.

           module sólo es necesario si la entrada de entorno se refiere a un
           módulo web de una aplicación Java EE. Si se especifica, module se
           debe ubicar a continuación de application-name, separados por una
           barra (/).

           Por ejemplo, el archivo application.xml de la aplicación myApp
           puede especificar el siguiente módulo web:

               <module>
                   <web>
                       <web-uri>myWebModule.war</web-uri>
                   </web>
               </module>
           El módulo se especificaría como operando de este comando como
           myApp/myWebModule.war.

EJEMPLOS
       Ejemplo 1, Listado de las entradas de entorno para una aplicación web
           En este ejemplo se muestran todas las entradas de entorno
           establecidas para la aplicación web hello mediante el subcomando
           set-web-env-entry. Debido a que no se definió ninguna descripción
           cuando se estableció la entrada de entorno Hello Port, se muestra
           null en lugar de una descripción para esta entrada.

               asadmin> list-web-env-entry hello
               Hello User (java.lang.String) = techscribe ignoreDescriptorItem=false
                //User authentication for Hello appplication
               Hello Port (java.lang.Integer) = null ignoreDescriptorItem=true //null

               Command list-web-env-entry executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       list-applications(1), set-web-env-entry(1), unset-web-env-entry(1)

       asadmin(1M)

Java EE 7                    30 de octubre de 2009       list-web-env-entry(1)
