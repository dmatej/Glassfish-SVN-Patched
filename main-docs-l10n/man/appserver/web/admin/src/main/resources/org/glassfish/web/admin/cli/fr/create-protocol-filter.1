create-protocol-filter(1Sous-commandes de l'utilitaircreate-protocol-filter(1)

NOM
       create-protocol-filter - ajoute un nouveau filtre de protocole

SYNTAXE
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

DESCRIPTION
       La sous-commande create-protocol-filter crée un filtre de protocole.
       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --protocol
           Nom du protocole associé.

       --classname
           Nom qualifié complet de la classe Java qui implémente le filtre de
           protocole.

       --target
           Cette option crée le filtre de protocole uniquement sur la cible
           indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur crée le filtre de protocole sur l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Cette valeur crée le filtre de protocole dans la configuration
               indiquée.

           cluster-name
               Cette valeur crée le filtre de protocole sur toutes les
               instances de serveur du cluster indiqué.

           standalone-instance-name
               Cette valeur crée le filtre de protocole sur l'instance de
               serveur autonome indiquée.

OPÉRANDES
       protocol-filter-name
           Nom du filtre de protocole.

EXEMPLES
       Exemple 1, Création d'un filtre de protocole
           La commande suivante crée un filtre de protocole nommé http1-filter
           :

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                         11 oct 2010        create-protocol-filter(1)
