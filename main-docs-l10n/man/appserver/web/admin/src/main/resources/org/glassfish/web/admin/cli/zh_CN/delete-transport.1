delete-transport(1)         asadmin 实用程序子命令         delete-transport(1)

名称
       delete-transport - 删除传输

提要
           delete-transport [--help]
           [--target target]
           transport-name

说明
       delete-transport 子命令删除指定的传输。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除传输。有效值如下：

           server
               从默认服务器实例中删除传输。这是默认值。

           configuration-name
               从指定配置中删除传输。

           cluster-name
               从指定集群的所有服务器实例中删除传输。

           standalone-instance-name
               从指定的独立服务器实例中删除传输。

操作数
       transport-name
           要删除的传输的名称。

示例
       Example 1, 删除传输
           以下命令删除名为 http1-trans 的传输：

               asadmin> delete-transport http1-trans
               Command delete-transport executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-transport(1), list-transports(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日           delete-transport(1)
