list-protocols(1)           asadmin 实用程序子命令           list-protocols(1)

名称
       list-protocols - 列出现有协议

提要
           list-protocols [--help]
           [target]

说明
       list-protocols 子命令用于列出现有协议。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           将列出的内容限制为指定目标的协议。有效值如下：

           server
               列出默认服务器实例的协议。这是默认值。

           configuration-name
               列出用于指定配置的协议。

           cluster-name
               列出指定集群中所有服务器实例的协议。

           instance-name
               列出指定服务器实例的协议。

示例
       Example 1, 列出协议
           以下命令可列出用于服务器实例的所有协议：

               asadmin> list-protocols
               admin-listener
               http-1
               http-listener-1
               http-listener-2
               Command list-protocols executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-protocol(1), delete-protocol(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日             list-protocols(1)
