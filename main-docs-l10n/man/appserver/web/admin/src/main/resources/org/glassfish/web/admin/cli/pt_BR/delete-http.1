delete-http(1)          Subcomandos do utilitário asad          delete-http(1)

NOME
       delete-http - remove parâmetros HTTP de um protocolo

SINOPSE
           delete-http [--help]
           [--target target]
           protocol-name

DESCRIçãO
       O subcomando delete-http remove o conjunto de parâmetros HTTP
       especificado a partir de um protocolo. Esse subcomando só é suportado
       no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Deleta o conjunto de parâmetros HTTP somente do alvo especificado.
           Os valores válidos são os seguintes:

           server
               Deleta o conjunto de parâmetros HTTP da instância do servidor
               default. Este é o valor default.

           configuration-name
               Deleta o conjunto de parâmetros HTTP da configuração
               especificada.

           cluster-name
               Deleta o conjunto de parâmetros HTTP de todas as instâncias do
               servidor do cluster especificado.

           standalone-instance-name
               Deleta o conjunto de parâmetros HTTP da instância do servidor
               standalone especificada.

OPERANDOS
       protocol-name
           O nome do protocolo a partir do qual deletar o conjunto de
           parâmetros HTTP.

EXEMPLOS
       Example 1, Deletando um Conjunto de Parâmetros HTTP
           O comando a seguir deleta o conjunto de parâmetros HTTP de um
           protocolo com o nome http-1.

               asadmin> delete-http http-1
               Command delete-http executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-http(1)

       asadmin(1M)

Java EE 7                         08 out 2010                   delete-http(1)
