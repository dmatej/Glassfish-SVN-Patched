delete-protocol-filterasadminユーティリティのサブコマdelete-protocol-filter(1)

名前
       delete-protocol-filter - プロトコル・フィルタを除去します

概要
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

説明
       delete-protocol-filterサブコマンドは、指定したプロトコル・フィルタを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --protocol-name
           関連付けられたプロトコルの名前。

       --target
           指定したターゲットからのみプロトコル・フィルタを削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからプロトコル・フィルタを削除します。これがデフォルト値です。

           configuration-name
               指定した構成からプロトコル・フィルタを削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからプロトコル・フィルタを削除します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスからプロトコル・フィルタを削除します。

オペランド
       protocol-filter-name
           削除するプロトコル・フィルタの名前。

例
       例 1, プロトコル・フィルタの削除
           次のコマンドは、http1-filterという名前のプロトコル・フィルタを削除します。

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                       2010年10月11日       delete-protocol-filter(1)
