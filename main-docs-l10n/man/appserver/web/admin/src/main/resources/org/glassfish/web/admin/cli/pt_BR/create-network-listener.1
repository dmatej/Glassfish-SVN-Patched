create-network-listener(Subcomandos do utilitário ascreate-network-listener(1)

NOME
       create-network-listener - adiciona um novo soquete do listener de rede

SINOPSE
           create-network-listener [--help]
           [--address address]
           --listenerport listener-port
           [--threadpool thread-pool]
           --protocol protocol
           [--transport transport]
           [--enabled={true|false}]
           [--jkenabled={false|true}]
           [--target target]
           listener-name

DESCRIçãO
       O subcomando create-network-listener cria um listener de rede. Esse
       subcomando só é suportado no modo remoto.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Se você editar o listener de │
           │           rede especial com o nome     │
           │           admin-listener , é           │
           │           necessário reiniciar o       │
           │           servidor para que as         │
           │           alterações tenham efeito. A  │
           │           Console de Administração não │
           │           informa se uma               │
           │           reinicialização é necessária │
           │           nesse caso.                  │
           └────────────────────────────────────────┘

           Nota
           ┌───────────────────────────────────────┐
           │Observação                             │
           │                                       │
           │           É possível utilizar o       │
           │           subcomando                  │
           │           create-http-listener para   │
           │           criar um listener de rede   │
           │           que utiliza o protocolo de  │
           │           HTTP sem ter que criar      │
           │           primeiro um protocolo,      │
           │           transporte ou configuração  │
           │           HTTP. Esse subcomando é um  │
           │           atalho conveniente, mas     │
           │           fornece acesso apenas a um  │
           │           número de opções limitadas. │
           └───────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --address
           O endereço IP ou o nome do host (resolúvel por DNS).

       --listenerport
           O número de porta para criar o soquete de listening. Os valores
           legais são 1- 65535. No UNIX, a criação de soquetes com listener
           nas portas 1-1024 requer privilégios de superusuário. A
           configuração de um soquete de interpretação SSL para interpretar a
           porta 443 é padrão.

       --threadpool
           O nome do pool de threads para esse listener. A especificação de um
           pool de threads é opcional. O default é http-thread-pool.

       --protocol
           O nome do protocolo para esse listener.

       --transport
           O nome do transporte para esse listener. A especificação de um
           transporte é opcional. O default é tcp.

       --enabled
           Se o default estiver definido como true, o listener será ativado
           durante o runtime.

       --jkenabled
           Se for definido como true, mod_jk estará ativado para esse
           listener. O default é falso.

       --target
           Cria o listener de rede só no alvo especificado. Os valores válidos
           são os seguintes:

           server
               Cria o listener de rede na instância do servidor default. Este
               é o valor default.

           configuration-name
               Cria o listener de rede na configuração especificada.

           cluster-name
               Cria o listener de rede em todas as instâncias de servidor no
               cluster especificado.

           standalone-instance-name
               Cria o listener de rede na instância do servidor standalone
               especificado.

OPERANDOS
       listener-name
           O nome do listener de rede.

EXEMPLOS
       Example 1, Criando um Listener de Rede
           O comando a seguir cria um listener de rede com o nome
           sampleListener que não é ativado durante o runtime:

               asadmin> create-network-listener --listenerport 7272 protocol http-1
               --enabled=false sampleListener
               Command create-network-listener executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       delete-network-listener(1), list-network-listeners(1),
       create-transport(1), create-protocol(1), create-threadpool(1),
       create-http-listener(1)

       asadmin(1M)

Java EE 7                         08 out 2010       create-network-listener(1)
