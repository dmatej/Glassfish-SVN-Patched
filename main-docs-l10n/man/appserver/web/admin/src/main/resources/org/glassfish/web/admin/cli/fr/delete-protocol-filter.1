delete-protocol-filter(1Sous-commandes de l'utilitairdelete-protocol-filter(1)

NOM
       delete-protocol-filter - enlève un filtre de protocole

SYNTAXE
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

DESCRIPTION
       La sous-commande delete-protocol-filter enlève le filtre de protocole
       spécifié. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --protocol-name
           Nom du protocole associé.

       --target
           Supprime le filtre de protocole de la cible spécifiée uniquement.
           Les valeurs valides sont les suivantes :

           server
               Supprime le filtre de protocole de l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Supprime le filtre de protocole de la configuration spécifiée.

           cluster-name
               Supprime le filtre de protocole de toutes les instances de
               serveur dans le cluster spécifié.

           standalone-instance-name
               Supprime le filtre de protocole de l'instance de serveur
               autonome spécifiée.

OPÉRANDES
       protocol-filter-name
           Nom du filtre de protocole à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un filtre de protocole
           La commande suivante supprime le filtre de protocole nommé
           http1-filter :

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                         11 oct 2010        delete-protocol-filter(1)
