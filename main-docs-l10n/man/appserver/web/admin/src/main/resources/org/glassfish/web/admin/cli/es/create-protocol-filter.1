create-protocol-filter(1Subcomandos de la Utilidad ascreate-protocol-filter(1)

NOMBRE DE REFERENCIA
       create-protocol-filter - Agrega un nuevo filtro de protocolo.

SINOPSIS
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

DESCRIPCIÓN
       El subcomando create-protocol-filter crea un filtro de protocolo para
       un protocolo. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --protocol
           El nombre del protocolo asociado.

       --classname
           Nombre completamente cualificado de la clase Java que implanta el
           filtro de protocolo.

       --target
           Crea el filtro de protocolo sólo en el destino especificado. Los
           valores válidos son los siguientes:

           server
               Crea el filtro de protocolo en la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration-name
               Crea el filtro de protocolo en la configuración especificada.

           cluster-name
               Crea el filtro de protocolo en todas las instancias de servidor
               del cluster especificado.

           standalone-instance-name
               Crea el filtro de protocolo en la instancia de servidor
               independiente especificada.

OPERANDOS
       protocol-filter-name
           Nombre del filtro de protocolo.

EJEMPLOS
       Ejemplo 1, Creación de un filtro de protocolo
           El siguiente comando crea un filtro de protocolo denominado
           http1-filter:

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                    11 de octubre de 2010   create-protocol-filter(1)
