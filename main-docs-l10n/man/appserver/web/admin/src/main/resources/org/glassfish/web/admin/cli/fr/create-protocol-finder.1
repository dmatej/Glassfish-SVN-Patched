create-protocol-finder(1Sous-commandes de l'utilitaircreate-protocol-finder(1)

NOM
       create-protocol-finder - ajoute un nouveau sélecteur de protocole

SYNTAXE
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

DESCRIPTION
       La sous-commande create-protocol-finder crée un sélecteur de protocole.
       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --protocol
           Nom du protocole associé.

       --targetprotocol
           Nom du protocole cible.

       --classname
           Nom qualifié complet de la classe Java qui implémente le sélecteur
           de protocole.

       --target
           Cette option crée le sélecteur de protocole uniquement sur la cible
           indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur crée le sélecteur de protocole sur l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Cette valeur crée le sélecteur de protocole dans la
               configuration indiquée.

           cluster-name
               Cette valeur crée le sélecteur de protocole sur toutes les
               instances de serveur du cluster indiqué.

           standalone-instance-name
               Cette valeur crée le sélecteur de protocole sur l'instance de
               serveur autonome indiquée.

OPÉRANDES
       protocol-finder-name
           Nom du sélecteur de protocole.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                         24 nov 2010        create-protocol-finder(1)
