create-protocol-filter(1)   asadmin 实用程序子命令   create-protocol-filter(1)

名称
       create-protocol-filter - 添加新的协议过滤器

提要
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

说明
       create-protocol-filter
       子命令为协议创建协议过滤器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --protocol
           关联协议的名称。

       --classname
           实现协议过滤器的 Java 类的全限定名称。

       --target
           仅在指定目标上创建协议过滤器。有效值如下：

           server
               在默认服务器实例上创建协议过滤器。这是默认值。

           configuration-name
               在指定配置中创建协议过滤器。

           cluster-name
               在指定集群中的所有服务器实例上创建协议过滤器。

           standalone-instance-name
               在指定的独立服务器实例上创建协议过滤器。

操作数
       protocol-filter-name
           协议过滤器的名称。

示例
       Example 1, 创建协议过滤器
           以下命令创建名为 http1-filter 的协议过滤器：

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日    create-protocol-filter(1)
