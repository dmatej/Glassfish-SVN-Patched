list-protocol-finders(1)Unterbefehle des asadmin-Utililist-protocol-finders(1)

NAME
       list-protocol-finders - listet die vorhandenen Protokoll-Finder auf

ZUSAMMENFASSUNG
           list-protocol-finders [--help]
           [--target server]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl list-protocol-finders listet die vorhandenen
       Protokoll-Finder auf. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       target
           Beschränkt die Auflistung auf Protokoll-Finder für ein angegebenes
           Ziel. Folgende Werte sind gültig:

           server
               Listet die Protokoll-Finder für die standardmäßige
               Serverinstanz auf. Dies ist der Standardwert.

           configuration-name
               Listet die Protokoll-Finder für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die Protokoll-Finder für alle Serverinstanzen im
               angegebenen Cluster auf.

           instance-name
               Listet die Protokoll-Finder für die angegebene Serverinstanz
               auf.

OPERANDEN
       protocol-name
           Der Name des Protokolls, für das Protokoll-Finder aufgelistet
           werden sollen.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                        11. Okt. 2010        list-protocol-finders(1)
