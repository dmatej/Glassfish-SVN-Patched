delete-protocol-finder(1Unterbefehle des asadmin-Utildelete-protocol-finder(1)

NAME
       delete-protocol-finder - entfernt einen Protokoll-Finder

ZUSAMMENFASSUNG
           delete-protocol-finder [--help]
           --protocol protocol-name
           [--target server]
           protocol-finder-name

BESCHREIBUNG
       Der Unterbefehl delete-protocol-finder entfernt den angegebenen
       Protokoll-Finder. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --protocol-name
           Der Name des verknüpften Protokolls.

       --target
           Löscht den Protokoll-Finder nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht den Protokoll-Finder aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den Protokoll-Finder aus der angegebenen Konfiguration.

           cluster-name
               Löscht den Protokoll-Finder aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den Protokoll-Finder aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-finder-name
           Der Name des Protokoll-Finders, der gelöscht werden soll.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                        11. Okt. 2010       delete-protocol-finder(1)
