create-protocol(1)          asadmin 实用程序子命令          create-protocol(1)

名称
       create-protocol - 添加新协议

提要
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

说明
       create-protocol 子命令创建协议。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --securityenabled
           如果设置为真
           SSL3
           并设置密码。通过使证书可用于服务器实例，此安全设置可全局启用或禁用
           SSL。默认值为假

       --target
           仅在指定目标上创建协议。有效值如下：

           server
               在默认服务器实例上创建协议。这是默认值。

           configuration-name
               在指定配置中创建协议。

           cluster-name
               在指定集群中的所有服务器实例上创建协议。

           standalone-instance-name
               在指定的独立服务器实例上创建协议。

操作数
       protocol-name
           协议名称。

示例
       Example 1, 创建协议
           以下命令创建一个名为 http-1 且启用了安全性的协议：

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日            create-protocol(1)
