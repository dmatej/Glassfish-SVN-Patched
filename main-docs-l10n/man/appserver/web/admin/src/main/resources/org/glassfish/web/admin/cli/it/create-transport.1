create-transport(1)     Comandi secondari della utilit     create-transport(1)

NOME
       create-transport - aggiunge un nuovo trasporto

SINTASSI
           create-transport [--help]
           [--acceptorthreads acceptor-threads]
           [--buffersizebytes buffer-size]
           [--bytebuffertype byte-buffer-type]
           [--classname class-name]
           [--displayconfiguration={false|true}]
           [--enablesnoop={false|true}]
           [--idlekeytimeoutseconds idle-key-timeout]
           [--maxconnectionscount max-connections]
           [--readtimeoutmillis read-timeout]
           [--writetimeoutmillis write-timeout]
           [--selectionkeyhandler selection-key-handler]
           [--selectorpolltimeoutmillis selector-poll-timeout]
           [--tcpnodelay={false|true}]
           [--target target]
           transport-name

DESCRIZIONE
       Il comando secondario create-transport crea un trasporto per un
       listener di rete. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --acceptorthreads
           Il numero di thread di processo di accettazione per il trasporto.
           Il valore consigliato è il numero di processori del computer. Il
           valore predefinito è 1.

       --buffersizebytes
           La dimensione, in byte, del buffer da fornire per i flussi di input
           creati dal listener di rete che fa riferimento al trasporto. Il
           valore predefinito è 8192.

       --bytebuffertype
           Il tipo di buffer da fornire per i flussi di input creati da un
           listener di rete. I valori consentiti sono HEAP e DIRECT. Il valore
           predefinito è HEAP.

       --classname
           Il nome completamente qualificato della classe Java che implementa
           il trasporto. Il valore predefinito è
           org.glassfish.grizzly.TCPSelectorHandler.

       --displayconfiguration
           Se si imposta true, la configurazione di rete interna viene
           scaricata nel log del server. Utile per il debug, ma riduce le
           prestazioni. Il valore predefinito è false.

       --enablesnoop
           Se l'impostazione è true, le informazioni relative alle
           richieste-risposte vengono scritte nel log del server. Utile per il
           debug, ma riduce le prestazioni. Il valore predefinito è false.

       --idlekeytimeoutseconds
           Il timeout di chiave inattiva. Il valore predefinito è 30 secondi.

       --maxconnectionscount
           Il numero massimo di connessioni per il listener di rete che fa
           riferimento al trasporto. Il valore -1 indica che non è previsto
           alcun limite. Il valore predefinito è 4096.

       --readtimeoutmillis
           L'intervallo di tempo di attesa del server durante la fase di
           analisi delle intestazioni e del corpo. Il valore predefinito è
           30000 millisecondi, ovvero 30 secondi.

       --writetimeoutmillis
           L'intervallo di tempo di attesa del server prima di considerare
           disconnesso il client remoto durante la scrittura della risposta.
           Il valore predefinito è 30000 millisecondi, ovvero 30 secondi.

       --selectionkeyhandler
           Il nome dell'handler delle chiavi di selezione associato al
           trasporto. Non è previsto alcun valore predefinito.

       --selectorpolltimeoutmillis
           Il numero di millisecondi di blocco di un selettore NIO in attesa
           di eventi (richieste di utenti). Il valore predefinito è 1000
           millisecondi.

       --tcpnodelay
           Se il valore è true, ovvero l'impostazione predefinita, viene
           abilitato TCP_NODELAY (noto anche come algoritmo di Nagle). Il
           valore predefinito è false.

       --target
           Crea il trasporto solo nella destinazione specificata. Di seguito
           sono riportati i valori validi.

           server
               Crea il trasporto nell'istanza server predefinita. Questo è il
               valore predefinito.

           configuration-name
               Crea il trasporto nella configurazione specificata.

           cluster-name
               Crea il trasporto su tutte le istanze server nel cluster
               specificato.

           standalone-instance-name
               Crea il trasporto nell'istanza server standalone predefinita.

OPERANDI
       transport-name
           Il nome del trasporto.

ESEMPI
       Esempio 1, Creazione di un trasporto
           Il seguente comando crea un trasporto denominato http1-trans che
           utilizza un numero non predefinito di thread di processo di
           accettazione:

               asadmin> create-transport --acceptorthreads 100 http1-trans
               Command create-transport executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       delete-transport(1), list-transports(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011            create-transport(1)
