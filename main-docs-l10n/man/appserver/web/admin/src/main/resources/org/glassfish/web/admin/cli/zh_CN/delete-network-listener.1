delete-network-listener(1)  asadmin 实用程序子命令  delete-network-listener(1)

名称
       delete-network-listener - 删除网络监听程序

提要
           delete-network-listener [--help]
           [--target target]
           listener-name

说明
       delete-network-listener
       子命令删除指定的网络监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除网络监听程序。有效值如下：

           server
               从默认服务器实例中删除网络监听程序。这是默认值。

           configuration-name
               从指定配置中删除网络监听程序。

           cluster-name
               从指定集群的所有服务器实例中删除网络监听程序。

           standalone-instance-name
               从指定的独立服务器实例中删除网络监听程序。

操作数
       listener-name
           要删除的网络监听程序的名称。

示例
       Example 1, 删除网络监听程序
           以下命令删除名为 sampleListener 的网络监听程序：

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日    delete-network-listener(1)
