create-protocol-filter(1Unterbefehle des asadmin-Utilcreate-protocol-filter(1)

NAME
       create-protocol-filter - fügt einen neuen Protokollfilter hinzu

ZUSAMMENFASSUNG
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

BESCHREIBUNG
       Der Unterbefehl create-protocol-filter erstellt einen Protokollfilter
       für ein Protokoll. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --protocol
           Der Name des verknüpften Protokolls.

       --classname
           Der vollqualifizierte Name der Java-Klasse, die den Protokollfilter
           implementiert.

       --target
           Erstellt den Protokollfilter nur auf dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Erstellt den Protokollfilter auf der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Erstellt den Protokollfilter in der angegebenen Konfiguration.

           cluster-name
               Erstellt den Protokollfilter auf allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Erstellt den Protokollfilter auf der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-filter-name
           Der Name des Protokollfilters.

BEISPIELE
       Beispiel 1, Protokollfilter erstellen
           Der folgende Befehl erstellt einen Protokollfilter mit dem Namen
           http1-filter:

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                        11. Okt. 2010       create-protocol-filter(1)
