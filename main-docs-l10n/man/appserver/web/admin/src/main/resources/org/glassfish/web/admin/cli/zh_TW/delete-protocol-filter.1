delete-protocol-filter(1)   asadmin 公用程式子命令   delete-protocol-filter(1)

參考名
       delete-protocol-filter - 移除協定篩選器

概要
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

描述
       delete-protocol-filter
       子命令會移除指定的協定篩選器。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --protocol-name
           關聯協定的名稱。

       --target
           僅從指定目標中刪除協定篩選器。有效的值如下：

           server
               從預設伺服器執行處理中刪除協定篩選器。此為預設值。

           configuration-name
               從指定組態中刪除協定篩選器。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除協定篩選器。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除協定篩選器。

運算元
       protocol-filter-name
           要刪除的協定篩選器名稱。

範例
       範例 1, 刪除協定篩選器
           以下命令會刪除名為 http1-filter 的協定篩選器：

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日    delete-protocol-filter(1)
