create-protocol(1)        asadmin 유틸리티 하위 명령        create-protocol(1)

제목
       create-protocol - 새 프로토콜 추가

시놉시스
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

설명
       create-custom-resource 하위 명령은 프로토콜을 생성합니다. 이 하위
       명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --securityenabled
           true로 설정된 경우 프로토콜이 SSL을 실행합니다. SSL2 또는 SSL3을 ON
           또는 OFF로 설정하고 ssl 요소를 사용하여 비밀번호를 설정할 수
           있습니다. 보안 설정은 서버 인스턴스에 인증서를 사용할 수 있도록
           하여 전역적으로 SSL을 사용 또는 사용 안함으로 설정합니다. 기본값은
           false입니다.

       --target
           지정된 대상에만 프로토콜을 생성합니다. 적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스에 프로토콜을 생성합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에 프로토콜을 생성합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에 프로토콜을
               생성합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에 프로토콜을 생성합니다.

피연산자
       protocol-name
           프로토콜의 이름입니다.

예제
       예 1, 프로토콜 생성
           다음 명령은 보안이 사용으로 설정된 상태에서 http-1이라는 프로토콜을
           생성합니다.

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일             create-protocol(1)
