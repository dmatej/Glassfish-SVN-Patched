delete-transport(1)     Sous-commandes de l'utilitaire     delete-transport(1)

NOM
       delete-transport - enlève un transport

SYNTAXE
           delete-transport [--help]
           [--target target]
           transport-name

DESCRIPTION
       La sous-commande delete-transport enlève le transport spécifié. Cette
       sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Supprime le transport de la cible spécifiée uniquement. Les valeurs
           valides sont les suivantes :

           server
               Supprime le transport de l'instance de serveur par défaut. Il
               s'agit de la valeur par défaut.

           configuration-name
               Supprime le transport de la configuration spécifiée.

           cluster-name
               Supprime le transport de toutes les instances de serveur dans
               le cluster spécifié.

           standalone-instance-name
               Supprime le transport de l'instance de serveur autonome
               spécifiée.

OPÉRANDES
       transport-name
           Nom du transport à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un transport
           La commande suivante supprime le transport nommé http1-trans :

               asadmin> delete-transport http1-trans
               Command delete-transport executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-transport(1), list-transports(1)

       asadmin(1M)

Java EE 7                         8 oct 2010               delete-transport(1)
