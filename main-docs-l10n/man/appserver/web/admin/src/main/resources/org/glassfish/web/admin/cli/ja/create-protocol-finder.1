create-protocol-finderasadminユーティリティのサブコマcreate-protocol-finder(1)

名前
       create-protocol-finder - 新しいプロトコル検索を追加します

概要
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

説明
       create-protocol-finderサブコマンドは、プロトコルのプロトコル検索を作成します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --protocol
           関連付けられたプロトコルの名前。

       --targetprotocol
           ターゲット・プロトコルの名前。

       --classname
           プロトコル検索を実装するJavaクラスの完全修飾名。

       --target
           指定したターゲットにのみプロトコル検索を作成します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスにプロトコル検索を作成します。この値がデフォルト値です。

           configuration-name
               指定した構成のプロトコル検索を作成します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスにプロトコル検索を作成します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスにプロトコル検索を作成します。

オペランド
       protocol-finder-name
           プロトコル検索の名前。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                       2010年11月24日       create-protocol-finder(1)
