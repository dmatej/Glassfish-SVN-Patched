list-http-listeners(1)    asadmin 유틸리티 하위 명령    list-http-listeners(1)

제목
       list-http-listeners - 기존 네트워크 리스너 나열

시놉시스
           list-http-listeners [--help]
           [target]

설명
       list-http-listeners 하위 명령은 기존 네트워크 리스너를 나열합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           지정된 대상에 대한 네트워크 리스너만 나열되도록 제한합니다. 적합한
           값은 다음과 같습니다.

           server
               기본 서버 인스턴스의 네트워크 리스너를 나열합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성의 네트워크 리스너를 나열합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스의 네트워크 리스너를
               나열합니다.

           instance-name
               지정된 서버 인스턴스의 네트워크 리스너를 나열합니다.

예제
       예 1, 네트워크 리스너 나열
           다음 명령은 서버 인스턴스의 모든 네트워크 리스너를 나열합니다.

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일         list-http-listeners(1)
