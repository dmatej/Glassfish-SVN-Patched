list-protocol-filters(1)Subcomandos de la Utilidad asalist-protocol-filters(1)

NOMBRE DE REFERENCIA
       list-protocol-filters - muestra los filtros de protocolo existentes.

SINOPSIS
           list-protocol-filters [--help]
           [--target server]
           protocol-name

DESCRIPCIÓN
       El subcomandolist-protocol-filters muestra los filtros de protocolo
       existentes. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       target
           Restringe el listado a filtros de protocolo de un destino
           especificado. Los valores válidos son los siguientes:

           server
               Muestra una lista de filtros de protocolo para la instancia de
               servidor por defecto. Este es el valor por defecto.

           configuration-name
               Muestra una lista de filtros de protocolo para la configuración
               especificada.

           cluster-name
               Muestra una lista de filtros de protocolo para todas las
               instancias del servidor en el cluster especificado.

           instance-name
               Muestra una lista de filtros de protocolo para la instancia de
               servidor especificada.

OPERANDOS
       protocol-name
           El nombre del protocolo para el que se muestra una lista de filtros
           de protocolo.

EJEMPLOS
       Ejemplo 1, Listado de filtros de protocolo
           El siguiente comando muestra una lista de todos los filtros de
           protocolo para la instancia de servidor:

               asadmin> list-protocol-filters http1
               http1-filter
               https1-filter
               Command list-protocol-filters executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-protocol-filter(1), delete-protocol-filter(1)

       asadmin(1M)

Java EE 7                    11 de octubre de 2010    list-protocol-filters(1)
