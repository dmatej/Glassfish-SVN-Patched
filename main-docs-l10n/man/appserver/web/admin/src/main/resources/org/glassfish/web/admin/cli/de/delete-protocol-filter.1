delete-protocol-filter(1Unterbefehle des asadmin-Utildelete-protocol-filter(1)

NAME
       delete-protocol-filter - entfernt einen Protokollfilter

ZUSAMMENFASSUNG
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

BESCHREIBUNG
       Der Unterbefehl delete-protocol-filter entfernt den angegebenen
       Protokollfilter. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --protocol-name
           Der Name des verknüpften Protokolls.

       --target
           Löscht den Protokollfilter nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht den Protokollfilter aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den Protokollfilter aus der angegebenen Konfiguration.

           cluster-name
               Löscht den Protokollfilter aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den Protokollfilter aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-filter-name
           Der Name des Protokollfilters, der gelöscht werden soll.

BEISPIELE
       Beispiel 1, Protokollfilter löschen
           Der folgende Befehl löscht den Protokollfilter mit dem Namen
           http1-filter:

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                        11. Okt. 2010       delete-protocol-filter(1)
