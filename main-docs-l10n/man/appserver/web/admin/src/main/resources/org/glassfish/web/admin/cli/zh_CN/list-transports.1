list-transports(1)          asadmin 实用程序子命令          list-transports(1)

名称
       list-transports - 列出现有传输

提要
           list-transports [--help]
           [target]

说明
       list-transports 子命令列出现有的传输。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           将列出的内容限制为指定目标的传输。有效值如下：

           server
               列出默认服务器实例的传输。这是默认值。

           configuration-name
               列出指定配置的传输。

           cluster-name
               列出指定集群中所有服务器实例的传输。

           instance-name
               列出指定服务器实例的传输。

示例
       Example 1, 列出传输
           以下命令列出服务器实例的所有传输：

               asadmin> list-transports
               http1-trans
               tcp
               Command list-transports executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-transport(1), delete-transport(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日            list-transports(1)
