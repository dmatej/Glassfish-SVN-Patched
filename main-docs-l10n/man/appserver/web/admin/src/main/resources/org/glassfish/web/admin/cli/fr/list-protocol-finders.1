list-protocol-finders(1)Sous-commandes de l'utilitairelist-protocol-finders(1)

NOM
       list-protocol-finders - répertorie les sélecteurs de protocole
       existants

SYNTAXE
           list-protocol-finders [--help]
           [--target server]
           protocol-name

DESCRIPTION
       La sous-commande list-protocol-finders répertorie les sélecteurs de
       protocole existants. Cette sous-commande est prise en charge en mode
       distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       target
           Restreint la liste aux sélecteurs de protocole pour une cible
           spécifiée. Les valeurs valides sont les suivantes :

           server
               Répertorie les sélecteurs de protocole pour l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Répertorie les sélecteurs de protocole pour la configuration
               spécifiée.

           cluster-name
               Répertorie les sélecteurs de protocole pour toutes les
               instances de serveur dans le cluster spécifié.

           instance-name
               Répertorie les sélecteurs de protocole pour l'instance de
               serveur spécifiée.

OPÉRANDES
       protocol-name
           Nom du protocole pour lequel les sélecteurs de protocole doivent
           être répertoriés.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                         11 oct 2010         list-protocol-finders(1)
