create-http-listener(1asadminユーティリティのサブコマンcreate-http-listener(1)

名前
       create-http-listener -
       新しいHTTPネットワーク・リスナー・ソケットを追加します

概要
           create-http-listener [--help] --listeneraddress address
           --listenerport listener-port
           {--default-virtual-server | --defaultvs} virtual-server
           [--servername server-name]
           [--acceptorthreads acceptor-threads]
           [--xpowered={true|false}]
           [--redirectport redirect-port]
           [--securityenabled={false|true}]
           [--enabled={true|false}]
           [--target target]
           listener-id

説明
       create-http-listenerサブコマンドでは、HTTPネットワーク・リスナーを作成します。このサブコマンドは、リモート・モードでのみサポートされています。

           注記
           ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注意                                                                                                                                                                                                                │
           │                                                                                                                                                                                                                    │
           │           admin-listenerという名前の特別なHTTPネットワーク・リスナーを編集する場合は、変更を有効にするためにサーバーを再起動する必要があります。管理コンソールは、この場合に再起動が必要であることを指示しません。 │
           └────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

           注記
           ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注意                                                                                                                                                                                                                                                                                                                    │
           │                                                                                                                                                                                                                                                                                                                        │
           │           このサブコマンドは、下位互換性を維持するために、HTTPプロトコルを使用するネットワーク・リスナーを作成するためのショートカットとして用意されています。バックグラウンドでは、このサブコマンドによって、ネットワーク・リスナーと、それに関連付けられたプロトコル、トランスポート、およびHTTP構成が作成されます。 │
           └────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --listeneraddress
           IPアドレスまたはホスト名(DNSにより解決可能)。

       --listenerport
           リスニング・ソケットを作成するポートの番号。適切な値は1から65535です。UNIXでは、ポート1から1024でリスニングするソケットを作成するには、スーパーユーザー権限が必要です。SSLリスニング・ソケットは、ポート443に構成することをお薦めします。

       --default-virtual-server, --defaultvs
           このリスナーのデフォルト仮想サーバーのID属性。--defaultvsオプションは推奨されません。

       --servername
           サーバーがクライアントに送信するURLのホスト名セクションに入れる値をサーバーに指示します。この値は、サーバーが自動的に生成するURLには影響しますが、サーバーに格納されているディレクトリやファイルのURLには影響しません。サーバーがエイリアスを使用する場合は、この名前もエイリアスにする必要があります。コロンとポート番号を追加すると、サーバーがクライアントに送信するURL内でそのポートが使用されます。

       --acceptorthreads
           リスナー・ソケットのアクセプタ・スレッドの数。推奨値は、マシン内のプロセッサの数です。デフォルト値は1です。

       --xpowered
           trueに設定すると、X-Powered-By: Servlet/3.0ヘッダーとX-Powered-By:
           JSP/2.0ヘッダーが適切なレスポンスに追加されます。Servlet
           3.0仕様では、サーブレットが生成するレスポンスにコンテナがX-Powered-By:
           Servlet/3.0ヘッダーを追加できるように定義されています。同様に、JSP
           2.0仕様では、JSPテクノロジを使用するレスポンスにコンテナがX-Powered-By:
           JSP/2.0ヘッダーを追加できるように定義されています。これらのヘッダーの目的は、サーブレットとJSPテクノロジの使用に関する統計データを収集することです。デフォルト値はtrueです。

       --redirectport
           このオプションは指定しないでください。このオプションは、旧リリースとの互換性のために保持されています。このオプションを指定した場合、構文エラーは発生しません。かわりに、サブコマンドが正常に実行され、オプションが無視されたことを示す警告メッセージが表示されます。

       --securityenabled
           trueに設定すると、HTTPリスナーはSSLを実行します。SSL2またはSSL3のON/OFFを切り替え、SSL要素を使用して暗号化方式を設定することができます。サーバー・インスタンスで証明書を使用できるようにすることにより、セキュリティ設定でSSLをグローバルに有効または無効にします。デフォルト値はfalseです。

       --enabled
           trueに設定すると、実行時にリスナーが有効になります。デフォルト値はtrueです。

       --target
           指定したターゲットにのみHTTPリスナーを作成します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスにHTTPリスナーを作成します。これがデフォルト値です。

           configuration-name
               指定した構成にHTTPリスナーを作成します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスにHTTPリスナーを作成します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスにHTTPリスナーを作成します。

オペランド
       listener-id
           HTTPネットワーク・リスナーのリスナーID。

例
       例 1, HTTPネットワーク・リスナーの作成
           次のコマンドでは、sampleListenerという名前のHTTPネットワーク・リスナーを作成します。このHTTPネットワーク・リスナーは、アクセプタ・スレッドの数としてデフォルト以外の値を使用し、実行時に無効になります。

               asadmin> create-http-listener --listeneraddress 0.0.0.0 --listenerport 7272
               --defaultvs server --servername host1.sun.com --acceptorthreads 100
               --securityenabled=false --enabled=false sampleListener
               Command create-http-listener executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       delete-http-listener(1), list-http-listeners(1),
       create-virtual-server(1), create-ssl(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                        2010年10月8日         create-http-listener(1)
