create-protocol-finder(1Subcomandos de la Utilidad ascreate-protocol-finder(1)

NOMBRE DE REFERENCIA
       create-protocol-finder - Agrega un nuevo buscador de protocolo.

SINOPSIS
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

DESCRIPCIÓN
       El subcomando create-protocolo-finder crea un buscador de protocolo
       para un protocolo. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --protocol
           El nombre del protocolo asociado.

       --targetprotocol
           Nombre del protocolo de destino.

       --classname
           Nombre totalmente cualificado de la clase Java que implanta el
           buscador de protocolo.

       --target
           Crea el buscador de protocolo sólo en el destino especificado. Los
           valores válidos son los siguientes:

           server
               Crea el buscador de protocolo en la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration-name
               Crea el buscador de protocolo en la configuración especificada.

           cluster-name
               Crea el buscador de protocolo en todas las instancias de
               servidor del cluster especificado.

           standalone-instance-name
               Crea el buscador de protocolo en la instancia de servidor
               independiente especificada.

OPERANDOS
       protocol-finder-name
           El nombre del buscador de protocolo.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                   24 de noviembre de 2010  create-protocol-finder(1)
