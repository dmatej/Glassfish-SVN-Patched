delete-http(1)            asadmin 유틸리티 하위 명령            delete-http(1)

제목
       delete-http - 프로토콜에서 HTTP 매개변수 제거

시놉시스
           delete-http [--help]
           [--target target]
           protocol-name

설명
       delete-http 하위 명령은 프로토콜에서 지정된 HTTP 매개변수 세트를
       제거합니다. 이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에서만 HTTP 매개변수 세트를 삭제합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스에서 HTTP 매개변수 세트를 삭제합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에서 HTTP 매개변수 세트를 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 HTTP 매개변수
               세트를 삭제합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에서 HTTP 매개변수 세트를
               삭제합니다.

피연산자
       protocol-name
           HTTP 매개변수 세트를 삭제할 프로토콜의 이름입니다.

예제
       예 1, HTTP 매개변수 세트 삭제
           다음 명령은 http-1이라는 프로토콜에서 HTTP 매개변수 세트를
           삭제합니다.

               asadmin> delete-http http-1
               Command delete-http executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-http(1)

       asadmin(1M)

Java EE 7                       2010년 10월 8일                 delete-http(1)
