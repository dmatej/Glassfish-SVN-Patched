list-http-listeners(1)  Subcomandos do utilitário asad  list-http-listeners(1)

NOME
       list-http-listeners - lista os listeners de rede existentes

SINOPSE
           list-http-listeners [--help]
           [target]

DESCRIçãO
       O subcomando list-http-listeners lista os listeners de rede existentes.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Restringe a listagem dos listeners de rede para um alvo
           especificado. Os valores válidos são os seguintes:

           server
               Lista os listeners de rede da instância de servidor default.
               Este é o valor default.

           configuration-name
               Lista os listeners de rede para a configuração especificada.

           cluster-name
               Lista os listeners de rede para todas as instâncias de servidor
               no cluster especificado.

           instance-name
               Lista os listeners de rede para a instância do servidor
               especificada.

EXEMPLOS
       Example 1, Listando Listeners de Rede
           O comando a seguir lista todos os listeners de rede da instância do
           servidor:

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

Java EE 7                   11 de fevereiro de 2011     list-http-listeners(1)
