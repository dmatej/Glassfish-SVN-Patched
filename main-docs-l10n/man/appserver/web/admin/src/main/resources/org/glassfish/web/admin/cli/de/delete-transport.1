delete-transport(1)     Unterbefehle des asadmin-Utili     delete-transport(1)

NAME
       delete-transport - entfernt einen Transport

ZUSAMMENFASSUNG
           delete-transport [--help]
           [--target target]
           transport-name

BESCHREIBUNG
       Der Unterbefehl delete-transport entfernt den angegebenen Transport.
       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den Transport nur aus dem angegebenen Ziel. Folgende Werte
           sind gültig:

           server
               Löscht den Transport aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           configuration-name
               Löscht den Transport aus der angegebenen Konfiguration.

           cluster-name
               Löscht den Transport aus allen Serverinstanzen im angegebenen
               Cluster.

           standalone-instance-name
               Löscht den Transport aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       transport-name
           Der Name des Transports, der gelöscht werden soll.

BEISPIELE
       Beispiel 1, Transport löschen
           Der folgende Befehl löscht den Transport namens http1-trans:

               asadmin> delete-transport http1-trans
               Command delete-transport executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-transport(1), list-transports(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010             delete-transport(1)
