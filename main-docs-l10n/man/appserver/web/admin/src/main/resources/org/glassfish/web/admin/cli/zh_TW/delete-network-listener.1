delete-network-listener(1)  asadmin 公用程式子命令  delete-network-listener(1)

參考名
       delete-network-listener - 移除網路監聽程式

概要
           delete-network-listener [--help]
           [--target target]
           listener-name

描述
       delete-network-listener
       子命令會移除指定的網路監聽程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除網路監聽程式。有效的值如下：

           server
               從預設伺服器執行處理中刪除網路監聽程式。此為預設值。

           configuration-name
               從指定組態中刪除網路監聽程式。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除網路監聽程式。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除網路監聽程式。

運算元
       listener-name
           要刪除的網路監聽程式名稱。

範例
       範例 1, 刪除網路監聽程式
           下列命令會刪除名為 sampleListener 的網路監聽程式。

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日    delete-network-listener(1)
