create-network-listener(Subcomandos de la Utilidad acreate-network-listener(1)

NOMBRE DE REFERENCIA
       create-network-listener - Agrega un nuevo socket de listener de red.

SINOPSIS
           create-network-listener [--help]
           [--address address]
           --listenerport listener-port
           [--threadpool thread-pool]
           --protocol protocol
           [--transport transport]
           [--enabled={true|false}]
           [--jkenabled={false|true}]
           [--target target]
           listener-name

DESCRIPCIÓN
       El subcomando create-network-listener crea un listener de red. Este
       subcomando sólo está soportado en modo remoto.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Si edita el listener de red │
           │           especial denominado         │
           │           admin-listener, deberá      │
           │           reiniciar el servidor para  │
           │           que los cambios surtan      │
           │           efecto. Consola de          │
           │           Administración no indica    │
           │           que, en este caso, se debe  │
           │           reiniciar el servidor.      │
           └───────────────────────────────────────┘

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Puede utilizar el subcomando │
           │           create-http-listener para    │
           │           crear un listener de red que │
           │           utiliza el protocolo HTTP    │
           │           sin necesidad de volver a    │
           │           crear antes un protocolo,    │
           │           transporte o configuración   │
           │           de HTTP. Este subcomando es  │
           │           un acceso directo práctico,  │
           │           pero sólo da acceso a un     │
           │           número limitado de opciones. │
           └────────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --address
           Dirección IP o nombre de host (resoluble por DNS).

       --listenerport
           Número de puerto en el que se debe crear el socket de recepción.
           Los valores válidos se encuentran entre 1 y 65535. En sistemas
           UNIX, la creación de sockets de recepción en los puertos 1-1024
           requiere privilegios de superusuario. Lo normal es configurar un
           socket de recepción SSL para recibir a través del puerto 443.

       --threadpool
           Nombre del pool de threads para este listener. La especificación de
           un pool de threads es opcional. El valor por defecto es
           http-thread-pool.

       --protocol
           Nombre del protocolo para este listener.

       --transport
           Nombre del transporte para este listener. La especificación de un
           transporte es opcional. El valor por defecto es tcp.

       --enabled
           Si se define en true, que es el valor por defecto, el listener se
           activa en tiempo de ejecución.

       --jkenabled
           Si se define en true, mod_jk se activa para este listener. El valor
           por defecto es false.

       --target
           Crea el listener de red sólo en el destino especificado. Los
           valores válidos son los siguientes:

           server
               Crea el listener de red en la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration-name
               Crea el listener de red en la configuración especificada.

           cluster-name
               Crea el listener de red en todas las instancias de servidor del
               cluster especificado.

           standalone-instance-name
               Crea el listener de red en la instancia de servidor
               independiente especificada.

OPERANDOS
       listener-name
           Nombre del listener de red.

EJEMPLOS
       Ejemplo 1, Creación de un listener de red
           El siguiente comando crea un listener de red denominado
           sampleListener que no se activa en tiempo de ejecución:

               asadmin> create-network-listener --listenerport 7272 protocol http-1
               --enabled=false sampleListener
               Command create-network-listener executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-network-listener(1), list-network-listeners(1),
       create-transport(1), create-protocol(1), create-threadpool(1),
       create-http-listener(1)

       asadmin(1M)

Java EE 7                    08 de octubre de 2010  create-network-listener(1)
