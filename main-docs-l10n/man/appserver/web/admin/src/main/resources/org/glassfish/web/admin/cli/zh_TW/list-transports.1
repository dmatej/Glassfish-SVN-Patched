list-transports(1)          asadmin 公用程式子命令          list-transports(1)

參考名
       list-transports - 列出現有的傳輸

概要
           list-transports [--help]
           [target]

描述
       list-transports 子命令會列出現有的傳輸。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           將清單限制為指定目標的傳輸。有效的值如下：

           server
               列出預設伺服器執行處理的傳輸。此為預設值。

           configuration-name
               列出指定組態的傳輸。

           cluster-name
               列出指定叢集內所有伺服器執行處理的傳輸。

           instance-name
               列出指定伺服器執行處理的傳輸。

範例
       範例 1, 列出傳輸
           下列命令會列出伺服器執行處理的所有傳輸：

               asadmin> list-transports
               http1-trans
               tcp
               Command list-transports executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-transport(1), delete-transport(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日            list-transports(1)
