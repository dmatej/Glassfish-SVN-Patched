create-protocol(1)          asadmin 公用程式子命令          create-protocol(1)

參考名
       create-protocol - 新增協定

概要
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

描述
       create-protocol 子命令會建立協定。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --securityenabled
           如果設為 true，則協定會執行 SSL。您可以開啟或關閉 SSL2 或
           SSL3，並使用 ssl
           元素設定密碼。安全性設定藉由使憑證可供伺服器執行處理使用來全域啟用或停用
           SSL。預設值為 false。

       --target
           僅在指定目標上建立協定。有效的值如下：

           server
               在預設伺服器執行處理上建立協定。此為預設值。

           configuration-name
               在指定組態中建立協定。

           cluster-name
               在指定叢集內的所有伺服器執行處理上建立協定。

           standalone-instance-name
               在指定的獨立伺服器執行處理上建立協定。

運算元
       protocol-name
           協定的名稱。

範例
       範例 1, 建立協定
           下列命令會建立已啟用安全且名為 http-1 的協定：

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日            create-protocol(1)
