create-http-redirect(1asadminユーティリティのサブコマンcreate-http-redirect(1)

名前
       create-http-redirect - 新しいHTTPリダイレクトを追加します

概要
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

説明
       create-http-redirectサブコマンドは、HTTPリダイレクトを作成します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --redirect-port
           リダイレクト用のポート番号。HTTPリスナーが非SSLリクエストをサポートしており、着信したリクエストに適用されるセキュリティ制約がSSLトランスポートを必要とする場合、GlassFish
           Serverは、リクエストをこのポート番号に自動的にリダイレクトします。

       --secure-redirect
           trueに設定すると、HTTPリダイレクトはSSLを実行します。デフォルト値はfalseです。

       --target
           指定したターゲットにのみHTTPリダイレクトを作成します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスにHTTPリダイレクトを作成します。この値がデフォルト値です。

           configuration-name
               指定した構成にHTTPリダイレクトを作成します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスにHTTPリダイレクトを作成します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスにHTTPリダイレクトを作成します。

オペランド
       protocol-name
           リダイレクトを適用するプロトコルの名前。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       delete-http-redirect(1)

       asadmin(1M)

Java EE 7                        2010年10月8日         create-http-redirect(1)
