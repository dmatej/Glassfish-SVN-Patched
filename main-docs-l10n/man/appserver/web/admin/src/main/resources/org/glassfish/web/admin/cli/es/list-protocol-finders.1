list-protocol-finders(1)Subcomandos de la Utilidad asalist-protocol-finders(1)

NOMBRE DE REFERENCIA
       list-protocol-finders - muestra los buscadores de protocolo existentes.

SINOPSIS
           list-protocol-finders [--help]
           [--target server]
           protocol-name

DESCRIPCIÓN
       El subcomando list-protocol-finders muestra los buscadores de protocolo
       existentes. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       target
           Restringe el listado a los buscadores de protocolo de un destino
           especificado. Los valores válidos son los siguientes:

           server
               Muestra una lista de buscadores de protocolo para la instancia
               de servidor por defecto. Este es el valor por defecto.

           configuration-name
               Muestra una lista de buscadores de protocolo para la
               configuración especificada.

           cluster-name
               Muestra una lista de buscadores de protocolo para todas las
               instancias del servidor en el cluster especificado.

           instance-name
               Muestra una lista de buscadores de protocolo para la instancia
               de servidor especificada.

OPERANDOS
       protocol-name
           El nombre del protocolo para el que se va a mostrar una lista de
           buscadores de protocolo.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                    11 de octubre de 2010    list-protocol-finders(1)
