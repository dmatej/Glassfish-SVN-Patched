list-virtual-servers(1) Unterbefehle des asadmin-Utili list-virtual-servers(1)

NAME
       list-virtual-servers - listet die vorhandenen virtuellen Server auf

ZUSAMMENFASSUNG
           list-virtual-servers [--help]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-virtual-servers listet die vorhandenen virtuellen
       Server auf. Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Beschränkt die Auflistung auf virtuelle Server für ein angegebenes
           Ziel. Folgende Werte sind gültig:

           server
               Listet die virtuellen Server für die standardmäßige
               Serverinstanz auf. Dies ist der Standardwert.

           configuration-name
               Listet die virtuellen Server für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die virtuellen Server für alle Serverinstanzen im
               angegebenen Cluster auf.

           instance-name
               Listet die virtuellen Server für die angegebene Serverinstanz
               auf.

BEISPIELE
       Beispiel 1, Virtuelle Server auflisten
           Der folgende Befehl listet alle virtuellen Server für die
           Serverinstanz auf:

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010         list-virtual-servers(1)
