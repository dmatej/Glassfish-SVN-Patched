create-protocol-filter(1Subcomandos do utilitário asacreate-protocol-filter(1)

NOME
       create-protocol-filter - adiciona um novo filtro de protocolo

SINOPSE
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

DESCRIçãO
       O subcomando create-protocol-filter cria um filtro de protocolo para o
       mesmo. Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --protocol
           O nome do protocolo associado.

       --classname
           O nome totalmente qualificado da classe Java que implementa o
           filtro do protocolo.

       --target
           Cria o filtro do protocolo só no alvo especificado. Os valores
           válidos são os seguintes:

           server
               Cria o filtro do protocolo na instância do servidor default.
               Este é o valor default.

           configuration-name
               Cria o filtro do protocolo na configuração especificada.

           cluster-name
               Cria o filtro do protocolo em todas as instâncias de servidor
               no cluster especificado.

           standalone-instance-name
               Cria o filtro do protocolo na instância do servidor standalone
               especificada.

OPERANDOS
       protocol-filter-name
           O nome do filtro do protocolo.

EXEMPLOS
       Example 1, Criando um Filtro de Protocolo
           O comando a seguir cria um filtro de protocolo com o nome
           http1-filter:

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                         11 out 2010        create-protocol-filter(1)
