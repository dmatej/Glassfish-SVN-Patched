delete-network-listener(Unterbefehle des asadmin-Utidelete-network-listener(1)

NAME
       delete-network-listener - entfernt einen Netzwerk-Listener

ZUSAMMENFASSUNG
           delete-network-listener [--help]
           [--target target]
           listener-name

BESCHREIBUNG
       Der Unterbefehl delete-network-listener entfernt den angegebenen
       Netzwerk-Listener. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den Netzwerk-Listener nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht den Netzwerk-Listener aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den Netzwerk-Listener aus der angegebenen Konfiguration.

           cluster-name
               Löscht den Netzwerk-Listener aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den Netzwerk-Listener aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       listener-name
           Der Name des Netzwerk-Listeners, der gelöscht werden soll.

BEISPIELE
       Beispiel 1, Netzwerk-Listener löschen
           Der folgende Befehl löscht den Netzwerk-Listener sampleListener:

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010      delete-network-listener(1)
