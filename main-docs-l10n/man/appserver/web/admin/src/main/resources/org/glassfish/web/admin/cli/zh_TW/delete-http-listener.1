delete-http-listener(1)     asadmin 公用程式子命令     delete-http-listener(1)

參考名
       delete-http-listener - 移除網路監聽程式

概要
           delete-http-listener [--help]
           [--target target]
           listener-id

描述
       delete-http-listener 子命令會移除指定的網路監聽程式。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除網路監聽程式。有效的值如下：

           server
               從預設伺服器執行處理中刪除網路監聽程式。此為預設值。

           configuration-name
               從指定組態中刪除網路監聽程式。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除網路監聽程式。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除網路監聽程式。

運算元
       listener-id
           要刪除的網路監聽程式的唯一識別碼。

範例
       範例 1, 使用 delete-http-listener 子命令
           下列命令會刪除名為 sampleListener 的網路監聽程式。

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-http-listener(1)
