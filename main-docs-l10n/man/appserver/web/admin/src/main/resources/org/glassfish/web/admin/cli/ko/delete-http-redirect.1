delete-http-redirect(1)   asadmin 유틸리티 하위 명령   delete-http-redirect(1)

제목
       delete-http-redirect - HTTP 재지정 제거

시놉시스
           delete-http-redirect [--help]
           [--target target]
           protocol-name

설명
       delete-http-redirect 하위 명령은 지정된 HTTP 재지정을 제거합니다. 이
       하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에서만 HTTP 재지정을 삭제합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에서 HTTP 재지정을 삭제합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에서 HTTP 재지정을 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 HTTP 재지정을
               삭제합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에서 HTTP 재지정을 삭제합니다.

피연산자
       protocol-name
           연관된 프로토콜의 이름입니다.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                       2010년 10월 8일        delete-http-redirect(1)
