list-http-listeners(1)  Comandi secondari della utilit  list-http-listeners(1)

NOME
       list-http-listeners - elenca i listener di rete esistenti

SINTASSI
           list-http-listeners [--help]
           [target]

DESCRIZIONE
       Il comando secondario list-http-listeners elenca i listener di rete
       esistenti.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Limita la lista ai listener di rete per la destinazione
           specificata. Di seguito sono riportati i valori validi.

           server
               Elenca i listener di rete per l'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Elenca i listener di rete per la configurazione specificata.

           cluster-name
               Elenca i listener di rete per tutte le istanze server del
               cluster specificato.

           instance-name
               Elenca i listener di rete per l'istanza server specificata.

ESEMPI
       Esempio 1, Visualizzazione della lista dei listener di rete
           Il comando seguente elenca tutti i listener di rete per l'istanza
           server:

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011         list-http-listeners(1)
