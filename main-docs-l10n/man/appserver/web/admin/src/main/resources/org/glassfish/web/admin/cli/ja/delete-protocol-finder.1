delete-protocol-finderasadminユーティリティのサブコマdelete-protocol-finder(1)

名前
       delete-protocol-finder - プロトコル検索を除去します

概要
           delete-protocol-finder [--help]
           --protocol protocol-name
           [--target server]
           protocol-finder-name

説明
       delete-protocol-finderサブコマンドは、指定したプロトコル検索を除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --protocol-name
           関連付けられたプロトコルの名前。

       --target
           指定したターゲットからのみプロトコル検索を削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからプロトコル検索を削除します。これがデフォルト値です。

           configuration-name
               指定した構成からプロトコル検索を削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからプロトコル検索を削除します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスからプロトコル検索を削除します。

オペランド
       protocol-finder-name
           削除するプロトコル検索の名前。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                       2010年10月11日       delete-protocol-finder(1)
