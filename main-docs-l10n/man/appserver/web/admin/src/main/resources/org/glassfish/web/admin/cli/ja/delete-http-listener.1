delete-http-listener(1asadminユーティリティのサブコマンdelete-http-listener(1)

名前
       delete-http-listener - ネットワーク・リスナーを除去します

概要
           delete-http-listener [--help]
           [--target target]
           listener-id

説明
       delete-http-listenerサブコマンドは、指定したネットワーク・リスナーを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           指定したターゲットからのみネットワーク・リスナーを削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからネットワーク・リスナーを削除します。この値がデフォルト値です。

           configuration-name
               指定した構成からネットワーク・リスナーを削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからネットワーク・リスナーを削除します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスからネットワーク・リスナーを削除します。

オペランド
       listener-id
           削除されるネットワーク・リスナーの一意の識別子。

例
       例 1, delete-http-listenerサブコマンドの使用
           次のコマンドは、sampleListenerという名前のネットワーク・リスナーを削除します。

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                        2011年2月11日         delete-http-listener(1)
