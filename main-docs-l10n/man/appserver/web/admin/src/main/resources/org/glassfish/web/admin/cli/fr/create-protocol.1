create-protocol(1)      Sous-commandes de l'utilitaire      create-protocol(1)

NOM
       create-protocol - ajoute un nouveau protocole

SYNTAXE
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

DESCRIPTION
       La sous-commande create-protocol crée un protocole. Cette sous-commande
       est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --securityenabled
           Si la valeur est true, le protocole exécute SSL. Vous pouvez
           activer ou désactiver SSL2 et SSL3, et définir des cryptages à
           l'aide d'un élément ssl. Le paramètre de sécurité active ou
           désactive globalement SSL en rendant les certificats disponibles
           pour l'instance de serveur. La valeur par défaut est false.

       --target
           Cette option crée le protocole uniquement sur la cible indiquée.
           Les valeurs valides sont les suivantes :

           server
               Cette valeur crée le protocole sur l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Cette valeur crée le protocole dans la configuration indiquée.

           cluster-name
               Cette valeur crée le protocole sur toutes les instances de
               serveur du cluster indiqué.

           standalone-instance-name
               Cette valeur crée le protocole sur l'instance de serveur
               autonome indiquée.

OPÉRANDES
       protocol-name
           Nom du protocole.

EXEMPLES
       Exemple 1, Création d'un protocole
           La commande suivante crée un protocole nommé http-1 avec la
           sécurité activée :

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                         11 fév 2011               create-protocol(1)
