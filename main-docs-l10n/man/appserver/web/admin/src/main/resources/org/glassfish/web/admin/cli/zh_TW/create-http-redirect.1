create-http-redirect(1)     asadmin 公用程式子命令     create-http-redirect(1)

參考名
       create-http-redirect - 新增 HTTP 重新導向

概要
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

描述
       create-http-redirect 子命令會建立 HTTP
       重新導向。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --redirect-port
           重新導向的連接埠號碼。如果 HTTP 監聽程式支援非 SSL
           請求，並且接收到其相符的安全性限制需要 SSL 傳輸的請求，則
           &ProductName 會自動將請求重新導向到此連接埠號碼。

       --secure-redirect
           如果設為 true，則 HTTP 重新導向會執行 SSL。預設值為 false。

       --target
           僅在指定目標上建立 HTTP 重新導向。有效的值如下：

           server
               在預設伺服器執行處理上建立 HTTP 重新導向。此為預設值。

           configuration-name
               在指定組態中建立 HTTP 重新導向。

           cluster-name
               在指定叢集內的所有伺服器執行處理上建立 HTTP 重新導向。

           standalone-instance-name
               在指定的獨立伺服器執行處理上建立 HTTP 重新導向。

運算元
       protocol-name
           要套用重新導向的協定名稱。

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-http-redirect(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       create-http-redirect(1)
