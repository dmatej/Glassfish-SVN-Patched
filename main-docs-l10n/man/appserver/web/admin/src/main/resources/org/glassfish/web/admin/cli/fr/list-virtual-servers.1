list-virtual-servers(1) Sous-commandes de l'utilitaire list-virtual-servers(1)

NOM
       list-virtual-servers - répertorie les serveurs virtuels existants

SYNTAXE
           list-virtual-servers [--help]
           [target]

DESCRIPTION
       La sous-commande list-virtual-servers répertorie les serveurs virtuels
       existants. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Restreint la liste aux serveurs virtuels pour une cible spécifiée.
           Les valeurs valides sont les suivantes :

           server
               Répertorie les serveurs virtuels pour l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Répertorie les serveurs virtuels pour la configuration
               spécifiée.

           cluster-name
               Répertorie les serveurs virtuels pour toutes les instances de
               serveur dans le cluster spécifié.

           instance-name
               Répertorie les serveurs virtuels pour l'instance de serveur
               spécifiée.

EXEMPLES
       Exemple 1, Liste des serveurs virtuels
           La commande suivante répertorie tous les serveurs virtuels pour
           l'instance de serveur :

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                         8 oct 2010           list-virtual-servers(1)
