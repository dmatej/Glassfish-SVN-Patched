delete-http(1)              asadmin 实用程序子命令              delete-http(1)

名称
       delete-http - 从协议中删除 HTTP 参数

提要
           delete-http [--help]
           [--target target]
           protocol-name

说明
       delete-http 子命令从协议中删除指定的 HTTP
       参数。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除 HTTP 参数集。有效值如下：

           server
               从默认服务器实例中删除 HTTP 参数集。这是默认值。

           configuration-name
               从指定配置中删除 HTTP 参数集。

           cluster-name
               从指定集群中的所有服务器实例中删除 HTTP 参数集。

           standalone-instance-name
               从指定的独立服务器实例中删除 HTTP 参数集。

操作数
       protocol-name
           要从中删除 HTTP 参数集的协议的名称。

示例
       Example 1, 删除 HTTP 参数集
           以下命令从名为 http-1 的协议中删除 HTTP 参数集：

               asadmin> delete-http http-1
               Command delete-http executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-http(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日                delete-http(1)
