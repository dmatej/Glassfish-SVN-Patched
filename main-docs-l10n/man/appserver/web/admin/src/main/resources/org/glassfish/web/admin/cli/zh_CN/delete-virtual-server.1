delete-virtual-server(1)    asadmin 实用程序子命令;   delete-virtual-server(1)

名称
       delete-virtual-server - 删除虚拟服务器

提要
           delete-virtual-server [--help]
           [--target target] virtual-server-id

说明
       delete-virtual-server 子命令删除具有指定虚拟服务器 ID
       的虚拟服务器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除虚拟服务器。有效值如下：

           server
               从默认服务器实例中删除虚拟服务器。这是默认值。

           configuration-name
               从指定配置中删除虚拟服务器。

           cluster-name
               从指定集群的所有服务器实例中删除虚拟服务器。

           standalone-instance-name
               从指定的独立服务器实例中删除虚拟服务器。

操作数
       virtual-server-id
           要删除的虚拟服务器的唯一标识符。

示例
       Example 1, 删除虚拟服务器
           以下命令删除名为 sample_vs1 的虚拟服务器：

               asadmin> delete-virtual-server sample_vs1
               Command delete-virtual-server executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-virtual-server(1), list-virtual-servers(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日      delete-virtual-server(1)
