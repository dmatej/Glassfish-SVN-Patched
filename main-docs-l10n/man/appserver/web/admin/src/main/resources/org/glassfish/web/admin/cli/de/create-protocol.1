create-protocol(1)      Unterbefehle des asadmin-Utili      create-protocol(1)

NAME
       create-protocol - fügt ein neues Protokoll hinzu

ZUSAMMENFASSUNG
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl create-protocol erstellt ein Protokoll. Dieser
       Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --securityenabled
           Ist der Wert auf true gesetzt, führt das Protokoll SSL aus. Sie
           können SSL2 oder SSL3 aktivieren oder deaktivieren und Cipher mit
           einem ssl-Element festlegen. Die Sicherheitseinstellung aktiviert
           oder deaktiviert SSL global, indem der Serverinstanz Zertifikate
           verfügbar gemacht werden. Der Standardwert ist false.

       --target
           Erstellt das Protokoll nur auf dem angegebenen Ziel. Folgende Werte
           sind gültig:

           server
               Erstellt das Protokoll auf der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           configuration-name
               Erstellt das Protokoll in der angegebenen Konfiguration.

           cluster-name
               Erstellt das Protokoll auf allen Serverinstanzen im angegebenen
               Cluster.

           standalone-instance-name
               Erstellt das Protokoll auf der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-name
           Der Name des Protokolls.

BEISPIELE
       Example 1, Protokolle erstellen
           Der folgende Unterbefehl erstellt ein Protokoll namens http-1 mit
           aktivierter Sicherheit:

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011              create-protocol(1)
