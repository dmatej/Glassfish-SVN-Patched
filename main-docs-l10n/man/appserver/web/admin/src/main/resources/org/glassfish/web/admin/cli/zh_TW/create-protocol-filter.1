create-protocol-filter(1)   asadmin 公用程式子命令   create-protocol-filter(1)

參考名
       create-protocol-filter - 新增協定篩選器

概要
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

描述
       create-protocol-filter
       子命令會為協定建立協定篩選器。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --protocol
           關聯協定的名稱。

       --classname
           實行此協定篩選器的 Java 類別之完全合格名稱。

       --target
           僅在指定目標上建立協定篩選器。有效的值如下：

           server
               在預設伺服器執行處理上建立協定篩選器。此為預設值。

           configuration-name
               在指定組態中建立協定篩選器。

           cluster-name
               在指定叢集內的所有伺服器執行處理上建立協定篩選器。

           standalone-instance-name
               在指定的獨立伺服器執行處理上建立協定篩選器。

運算元
       protocol-filter-name
           協定篩選器的名稱。

範例
       範例 1, 建立協定篩選器
           以下命令會建立名為 http1-filter 的協定篩選器：

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日    create-protocol-filter(1)
