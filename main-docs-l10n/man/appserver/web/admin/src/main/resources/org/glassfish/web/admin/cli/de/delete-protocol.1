delete-protocol(1)      Unterbefehle des asadmin-Utili      delete-protocol(1)

NAME
       delete-protocol - entfernt ein Protokoll

ZUSAMMENFASSUNG
           delete-protocol [--help]
           [--target target]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl delete-protocol entfernt das angegebene Protokoll.
       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht das Protokoll nur aus dem angegebenen Ziel. Folgende Werte
           sind gültig:

           server
               Löscht das Protokoll aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           configuration-name
               Löscht das Protokoll aus der angegebenen Konfiguration.

           cluster-name
               Löscht das Protokoll aus allen Serverinstanzen im angegebenen
               Cluster.

           standalone-instance-name
               Löscht das Protokoll aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-name
           Der Name des Protokolls, das gelöscht werden soll.

BEISPIELE
       Beispiel 1, Protokolle löschen
           Der folgende Befehl löscht das Protokoll mit dem Namen http-1:

               asadmin> delete-protocol http-1
               Command delete-protocol executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol(1), list-protocols(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010              delete-protocol(1)
