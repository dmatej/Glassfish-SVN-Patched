delete-transport(1)         asadmin 公用程式子命令         delete-transport(1)

參考名
       delete-transport - 移除傳輸

概要
           delete-transport [--help]
           [--target target]
           transport-name

描述
       delete-transport 子命令會移除指定的傳輸。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除傳輸。有效的值如下：

           server
               從預設伺服器執行處理中刪除傳輸。此為預設值。

           configuration-name
               從指定組態中刪除傳輸。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除傳輸。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除傳輸。

運算元
       transport-name
           要刪除的傳輸名稱。

範例
       範例 1, 刪除傳輸
           下列命令會刪除名為 http1-trans 的傳輸：

               asadmin> delete-transport http1-trans
               Command delete-transport executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-transport(1), list-transports(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日           delete-transport(1)
