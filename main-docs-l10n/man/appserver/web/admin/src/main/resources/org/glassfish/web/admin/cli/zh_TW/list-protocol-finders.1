list-protocol-finders(1)    asadmin 公用程式子命令    list-protocol-finders(1)

參考名
       list-protocol-finders - 列出現有的協定搜尋器

概要
           list-protocol-finders [--help]
           [--target server]
           protocol-name

描述
       list-protocol-finders
       子命令會列出現有的協定尋檔程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       target
           將清單限制為指定目標的協定尋檔程式。有效的值如下：

           server
               列出預設伺服器執行處理的協定尋檔程式。此為預設值。

           configuration-name
               列出指定組態的協定尋檔程式。

           cluster-name
               列出指定叢集內所有伺服器執行處理的協定尋檔程式。

           instance-name
               列出指定伺服器執行處理的協定尋檔程式。

運算元
       protocol-name
           要列出協定尋檔程式的協定名稱。

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日     list-protocol-finders(1)
