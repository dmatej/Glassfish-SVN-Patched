list-protocol-filters(1)Unterbefehle des asadmin-Utililist-protocol-filters(1)

NAME
       list-protocol-filters - listet die vorhandenen Protokollfilter auf

ZUSAMMENFASSUNG
           list-protocol-filters [--help]
           [--target server]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl list-protocol-filters listet die vorhandenen
       Protokollfilter auf. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       target
           Beschränkt die Auflistung auf Protokollfilter für ein angegebenes
           Ziel. Folgende Werte sind gültig:

           server
               Listet die Protokollfilter für die standardmäßige Serverinstanz
               auf. Dies ist der Standardwert.

           configuration-name
               Listet die Protokollfilter für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die Protokollfilter für alle Serverinstanzen im
               angegebenen Cluster auf.

           instance-name
               Listet die Protokollfilter für die angegebene Serverinstanz
               auf.

OPERANDEN
       protocol-name
           Der Name des Protokolls, für das Protokollfilter aufgelistet werden
           sollen.

BEISPIELE
       Beispiel 1, Protokollfilter auflisten
           Der folgende Befehl listet alle Protokollfilter für die
           Serverinstanz auf:

               asadmin> list-protocol-filters http1
               http1-filter
               https1-filter
               Command list-protocol-filters executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol-filter(1), delete-protocol-filter(1)

       asadmin(1M)

Java EE 7                        11. Okt. 2010        list-protocol-filters(1)
