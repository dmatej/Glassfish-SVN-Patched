delete-http(1)              asadmin 公用程式子命令              delete-http(1)

參考名
       delete-http - 移除協定中的 HTTP 參數

概要
           delete-http [--help]
           [--target target]
           protocol-name

描述
       delete-http 子命令會從協定移除指定的 HTTP
       參數集。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除 HTTP 參數集。有效的值如下：

           server
               從預設伺服器執行處理中刪除 HTTP 參數集。此為預設值。

           configuration-name
               從指定組態中刪除 HTTP 參數集。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除 HTTP 參數集。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除 HTTP 參數集。

運算元
       protocol-name
           要從中刪除 HTTP 參數集的協定名稱。

範例
       範例 1, 刪除 HTTP 參數集
           下列命令會從名為 http-1 的協定刪除 HTTP 參數集：

               asadmin> delete-http http-1
               Command delete-http executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-http(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日                delete-http(1)
