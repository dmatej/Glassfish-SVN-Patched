create-protocol-finder(1)   asadmin 公用程式子命令   create-protocol-finder(1)

參考名
       create-protocol-finder - 新增協定尋檔程式

概要
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

描述
       create-protocol-finder
       子命令會為協定建立協定尋檔程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --protocol
           關聯協定的名稱。

       --targetprotocol
           目標協定的名稱。

       --classname
           實行此協定尋檔程式的 Java 類別之完全合格名稱。

       --target
           僅在指定目標上建立協定尋檔程式。有效的值如下：

           server
               在預設伺服器執行處理上建立協定尋檔程式。此為預設值。

           configuration-name
               在指定組態中建立協定尋檔程式。

           cluster-name
               在指定叢集內的所有伺服器執行處理上建立協定尋檔程式。

           standalone-instance-name
               在指定的獨立伺服器執行處理上建立協定尋檔程式。

運算元
       protocol-finder-name
           協定尋檔程式的名稱。

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                     2010 年 11 月 24 日    create-protocol-finder(1)
