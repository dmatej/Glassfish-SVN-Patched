list-protocol-finders(asadminユーティリティのサブコマン

名前
       list-protocol-finders - 既存のプロトコル検索をリストします

概要
           list-protocol-finders [--help]
           [--target server]
           protocol-name

説明
       list-protocol-findersサブコマンドは、既存のプロトコル検索をリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       target
           表示する内容を、指定したターゲットのプロトコル検索に制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのプロトコル検索をリストします。これがデフォルト値です。

           configuration-name
               指定した構成のプロトコル検索をリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのプロトコル検索をリストします。

           instance-name
               指定したサーバー・インスタンスのプロトコル検索をリストします。

オペランド
       protocol-name
           プロトコル検索をリストするプロトコルの名前。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                       2010年10月11日        list-protocol-finders(1)
