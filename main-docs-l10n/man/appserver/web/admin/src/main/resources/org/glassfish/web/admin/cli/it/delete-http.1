delete-http(1)          Comandi secondari della utilit          delete-http(1)

NOME
       delete-http - rimuove i parametri HTTP da un protocollo

SINTASSI
           delete-http [--help]
           [--target target]
           protocol-name

DESCRIZIONE
       Il comando secondario delete-http rimuove da un protocollo il set di
       parametri HTTP specificato. Questo comando secondario è supportato solo
       in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina il set di parametri HTTP solo dalla destinazione
           specificata. Di seguito sono riportati i valori validi.

           server
               Elimina il set di parametri HTTP dall'istanza server
               predefinita. Questo è il valore predefinito.

           configuration-name
               Elimina il set di parametri HTTP dalla configurazione
               specificata.

           cluster-name
               Elimina il set di parametri HTTP da tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Elimina il set di parametri HTTP dall'istanza server standalone
               specificata.

OPERANDI
       protocol-name
           Il nome del protocollo da cui eliminare il set di parametri HTTP.

ESEMPI
       Esempio 1, Eliminazione di un set di parametri HTTP
           Il comando seguente elimina il set di parametri HTTP dal protocollo
           denominato http-1:

               asadmin> delete-http http-1
               Command delete-http executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-http(1)

       asadmin(1M)

Java EE 7                       8 ottobre 2010                  delete-http(1)
