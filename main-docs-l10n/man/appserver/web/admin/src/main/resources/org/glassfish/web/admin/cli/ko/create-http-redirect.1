create-http-redirect(1)   asadmin 유틸리티 하위 명령   create-http-redirect(1)

제목
       create-http-redirect - 새 HTTP 재지정 추가

시놉시스
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

설명
       create-http-redirect 하위 명령은 HTTP 재지정을 생성합니다. 이 하위
       명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --redirect-port
           재지정을 위한 포트 번호입니다. HTTP 리스너가 비 SSL 요청을 지원하고
           일치하는 보안 제약 조건이 SSL 전송을 필요로 하는 요청이 수신된 경우
           GlassFish Server에서는 자동으로 해당 요청을 이 포트 번호로
           재지정합니다.

       --secure-redirect
           true로 설정된 경우 HTTP 재지정이 SSL을 실행합니다. 기본값은
           false입니다.

       --target
           지정된 대상에만 HTTP 재지정을 생성합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에 HTTP 재지정을 생성합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에 HTTP 재지정을 생성합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에 HTTP 재지정을
               생성합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에 HTTP 재지정을 생성합니다.

피연산자
       protocol-name
           재지정을 적용할 프로토콜의 이름입니다.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       delete-http-redirect(1)

       asadmin(1M)

Java EE 7                       2010년 10월 8일        create-http-redirect(1)
