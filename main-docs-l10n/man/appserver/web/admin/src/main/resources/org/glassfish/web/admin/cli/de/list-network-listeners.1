list-network-listeners(1Unterbefehle des asadmin-Utillist-network-listeners(1)

NAME
       list-network-listeners - listet die vorhandenen Netzwerk-Listener auf

ZUSAMMENFASSUNG
           list-network-listeners [--help]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-network-listeners listet die vorhandenen
       Netzwerk-Listener auf. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Beschränkt die Auflistung auf Netzwerk-Listener für ein angegebenes
           Ziel. Folgende Werte sind gültig:

           server
               Listet die Netzwerk-Listener für die standardmäßige
               Serverinstanz auf. Dies ist der Standardwert.

           configuration-name
               Listet die Netzwerk-Listener für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die Netzwerk-Listener für alle Serverinstanzen im
               angegebenen Cluster auf.

           instance-name
               Listet die Netzwerk-Listener für die angegebene Serverinstanz
               auf.

BEISPIELE
       Example 1, Netzwerk-Listener auflisten
           Der folgende Befehl listet alle Netzwerk-Listener für die
           Serverinstanz auf:

               asadmin> list-network-listeners
               admin-listener
               http-listener-1
               https-listener-2
               Command list-network-listeners executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-network-listener(1), delete-network-listener(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010       list-network-listeners(1)
