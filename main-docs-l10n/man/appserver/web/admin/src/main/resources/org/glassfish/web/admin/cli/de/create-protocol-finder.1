create-protocol-finder(1Unterbefehle des asadmin-Utilcreate-protocol-finder(1)

NAME
       create-protocol-finder - fügt einen neuen Protokoll-Finder hinzu

ZUSAMMENFASSUNG
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

BESCHREIBUNG
       Der Unterbefehl create-protocol-finder erstellt einen Protokoll-Finder
       für ein Protokoll. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --protocol
           Der Name des verknüpften Protokolls.

       --targetprotocol
           Der Name des Zielprotokolls.

       --classname
           Der vollqualifizierte Name der Java-Klasse, die den
           Protokoll-Finder implementiert.

       --target
           Erstellt den Protokoll-Finder nur auf dem angegebenen Ziel.
           Folgende Werte sind gültig:

           server
               Erstellt den Protokoll-Finder auf der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Erstellt den Protokoll-Finder in der angegebenen Konfiguration.

           cluster-name
               Erstellt den Protokoll-Finder auf allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Erstellt den Protokoll-Finder auf der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-finder-name
           Der Name des Protokoll-Finders.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                        24. Nov. 2010       create-protocol-finder(1)
