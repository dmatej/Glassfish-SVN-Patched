delete-virtual-server(1)  asadmin 유틸리티 하위 명령; delete-virtual-server(1)

제목
       delete-virtual-server - 가상 서버 제거

시놉시스
           delete-virtual-server [--help]
           [--target target] virtual-server-id

설명
       delete-virtual-server 하위 명령은 지정된 가상 서버 ID를 가진 가상
       서버를 제거합니다. 이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에서만 가상 서버를 삭제합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에서 가상 서버를 삭제합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에서 가상 서버를 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 가상 서버를
               삭제합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에서 가상 서버를 삭제합니다.

피연산자
       virtual-server-id
           삭제할 가상 서버의 고유 식별자입니다.

예제
       예 1, 가상 서버 삭제
           다음 명령은 sample_vs1이라는 가상 서버를 삭제합니다.

               asadmin> delete-virtual-server sample_vs1
               Command delete-virtual-server executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-virtual-server(1), list-virtual-servers(1)

       asadmin(1M)

Java EE 7                       2010년 10월 8일       delete-virtual-server(1)
