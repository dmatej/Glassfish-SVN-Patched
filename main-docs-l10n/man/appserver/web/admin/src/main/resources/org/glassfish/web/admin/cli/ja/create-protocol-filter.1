create-protocol-filterasadminユーティリティのサブコマcreate-protocol-filter(1)

名前
       create-protocol-filter - 新しいプロトコル・フィルタを追加します

概要
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

説明
       create-protocol-filterサブコマンドは、プロトコルのプロトコル・フィルタを作成します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --protocol
           関連付けられたプロトコルの名前。

       --classname
           プロトコル・フィルタを実装するJavaクラスの完全修飾名。

       --target
           指定したターゲットにのみプロトコル・フィルタを作成します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスにプロトコル・フィルタを作成します。この値がデフォルト値です。

           configuration-name
               指定した構成のプロトコル・フィルタを作成します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスにプロトコル・フィルタを作成します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスにプロトコル・フィルタを作成します。

オペランド
       protocol-filter-name
           プロトコル・フィルタの名前。

例
       例 1, プロトコル・フィルタの作成
           次のコマンドは、http1-filterという名前のプロトコル・フィルタを作成します。

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

Java EE 7                       2010年10月11日       create-protocol-filter(1)
