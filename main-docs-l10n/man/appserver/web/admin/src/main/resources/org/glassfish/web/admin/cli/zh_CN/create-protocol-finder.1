create-protocol-finder(1)   asadmin 实用程序子命令   create-protocol-finder(1)

名称
       create-protocol-finder - 添加新的协议查找器

提要
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

说明
       create-protocol-finder
       子命令为协议创建协议查找器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --protocol
           关联协议的名称。

       --targetprotocol
           目标协议的名称。

       --classname
           实现协议查找器的 Java 类的全限定名称。

       --target
           仅在指定目标上创建协议查找器。有效值如下：

           server
               在默认服务器实例上创建协议查找器。这是默认值。

           configuration-name
               在指定配置中创建协议查找器。

           cluster-name
               在指定集群中的所有服务器实例上创建协议查找器。

           standalone-instance-name
               在指定的独立服务器实例上创建协议查找器。

操作数
       protocol-finder-name
           协议查找器的名称。

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                     2010 年 11 月 24 日    create-protocol-finder(1)
