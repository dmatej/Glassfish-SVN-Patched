list-http-listeners(1)  Unterbefehle des asadmin-Utili  list-http-listeners(1)

NAME
       list-http-listeners - listet die vorhandenen Netzwerk-Listener auf

ZUSAMMENFASSUNG
           list-http-listeners [--help]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-http-listeners listet die vorhandenen
       Netzwerk-Listener auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Beschränkt die Auflistung auf Netzwerk-Listener für ein angegebenes
           Ziel. Folgende Werte sind gültig:

           server
               Listet die Netzwerk-Listener für die standardmäßige
               Serverinstanz auf. Dies ist der Standardwert.

           configuration-name
               Listet die Netzwerk-Listener für die angegebene Konfiguration
               auf.

           cluster-name
               Listet die Netzwerk-Listener für alle Serverinstanzen im
               angegebenen Cluster auf.

           instance-name
               Listet die Netzwerk-Listener für die angegebene Serverinstanz
               auf.

BEISPIELE
       Example 1, Netzwerk-Listener auflisten
           Der folgende Befehl listet alle Netzwerk-Listener für die
           Serverinstanz auf:

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011          list-http-listeners(1)
