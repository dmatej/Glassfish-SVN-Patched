list-transports(1)    asadminユーティリティのサブコマンド   list-transports(1)

名前
       list-transports - 既存のトランスポートをリストします

概要
           list-transports [--help]
           [target]

説明
       list-transportsサブコマンドは、既存のトランスポートをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           リストする内容を、指定したターゲットのトランスポートに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのトランスポートをリストします。これがデフォルト値です。

           configuration-name
               指定した構成のトランスポートをリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのトランスポートをリストします。

           instance-name
               指定したサーバー・インスタンスのトランスポートをリストします。

例
       例 1, トランスポートのリスト
           次のコマンドは、サーバー・インスタンスのすべてのトランスポートをリストします。

               asadmin> list-transports
               http1-trans
               tcp
               Command list-transports executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-transport(1), delete-transport(1)

       asadmin(1M)

Java EE 7                        2010年10月8日              list-transports(1)
