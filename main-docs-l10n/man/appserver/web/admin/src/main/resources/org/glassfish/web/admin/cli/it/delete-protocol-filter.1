delete-protocol-filter(1Comandi secondari della utilidelete-protocol-filter(1)

NOME
       delete-protocol-filter - rimuove un filtro di protocollo

SINTASSI
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

DESCRIZIONE
       Il comando secondario delete-protocol-filter rimuove il filtro di
       protocollo specificato. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --protocol-name
           Il nome del protocollo associato.

       --target
           Elimina il filtro di protocollo solo dalla destinazione
           specificata. Di seguito sono riportati i valori validi.

           server
               Elimina il filtro di protocollo dall'istanza server
               predefinita. Questo è il valore predefinito.

           configuration-name
               Elimina il filtro di protocollo dalla configurazione
               specificata.

           cluster-name
               Elimina il filtro di protocollo da tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Elimina il filtro di protocollo dall'istanza server standalone
               specificata.

OPERANDI
       protocol-filter-name
           Il nome del filtro di protocollo da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un filtro di protocollo
           Il comando seguente elimina il filtro di protocollo denominato
           http1-filter:

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                       11 ottobre 2010      delete-protocol-filter(1)
