create-protocol(1)      Subcomandos de la Utilidad asa      create-protocol(1)

NOMBRE DE REFERENCIA
       create-protocol - Agrega un nuevo protocolo.

SINOPSIS
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

DESCRIPCIÓN
       El subcomando create-protocol crea un protocolo. Este subcomando sólo
       está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --securityenabled
           Si se define en true, el protocolo ejecuta SSL. Puede activar o
           desactivar SSL2 o SSL3 y definir cifrados mediante un elemento ssl.
           La configuración de seguridad activa o desactiva SSL globalmente
           poniendo certificados a disposición de la instancia de servidor. El
           valor por defecto es false.

       --target
           Crea el protocolo únicamente en el destino especificado. Los
           valores válidos son los siguientes:

           server
               Crea el protocolo para la instancia de servidor por defecto.
               Este es el valor por defecto.

           configuration-name
               Crea el protocolo en la configuración especificada.

           cluster-name
               Crea el protocolo en todas las instancias de servidor del
               cluster especificado.

           standalone-instance-name
               Crea el protocolo en la instancia de servidor independiente
               especificada.

OPERANDOS
       protocol-name
           Nombre del protocolo.

EJEMPLOS
       Ejemplo 1, Creación de un Protocolo
           El siguiente comando crea un protocolo denominado http-1 con la
           seguridad activada:

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011          create-protocol(1)
