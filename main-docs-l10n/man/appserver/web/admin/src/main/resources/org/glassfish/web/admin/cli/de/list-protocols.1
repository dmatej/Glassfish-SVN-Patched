list-protocols(1)       Unterbefehle des asadmin-Utili       list-protocols(1)

NAME
       list-protocols - listet die vorhandenen Protokolle auf

ZUSAMMENFASSUNG
           list-protocols [--help]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-protocols listet die vorhandenen Protokolle auf.
       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Beschränkt die Auflistung auf Protokolle für ein angegebenes Ziel.
           Folgende Werte sind gültig:

           server
               Listet die Protokolle für die standardmäßige Serverinstanz auf.
               Dies ist der Standardwert.

           configuration-name
               Listet die Protokolle für die angegebene Konfiguration auf.

           cluster-name
               Listet die Protokolle für alle Serverinstanzen im angegebenen
               Cluster auf.

           instance-name
               Listet die Protokolle für die angegebene Serverinstanz auf.

BEISPIELE
       Example 1, Protokolle auflisten
           Der folgende Befehl listet alle Protokolle für die Serverinstanz
           auf:

               asadmin> list-protocols
               admin-listener
               http-1
               http-listener-1
               http-listener-2
               Command list-protocols executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-protocol(1), delete-protocol(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010               list-protocols(1)
