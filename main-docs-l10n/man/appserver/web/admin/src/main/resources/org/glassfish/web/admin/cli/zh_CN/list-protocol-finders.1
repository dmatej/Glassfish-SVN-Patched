list-protocol-finders(1)    asadmin 实用程序子命令    list-protocol-finders(1)

名称
       list-protocol-finders - 列出现有的协议查找器

提要
           list-protocol-finders [--help]
           [--target server]
           protocol-name

说明
       list-protocol-finders
       子命令列出现有的协议查找器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       target
           将列出的内容限制为指定目标的协议查找器。有效值如下：

           server
               列出默认服务器实例的协议查找器。这是默认值。

           configuration-name
               列出指定配置的协议查找器。

           cluster-name
               列出指定集群中所有服务器实例的协议查找器。

           instance-name
               列出指定服务器实例的协议查找器。

操作数
       protocol-name
           要列出协议查找器的协议的名称。

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-protocol-finder(1), delete-protocol-finder(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日     list-protocol-finders(1)
