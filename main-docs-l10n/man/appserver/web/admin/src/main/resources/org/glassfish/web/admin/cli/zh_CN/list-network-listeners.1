list-network-listeners(1)   asadmin 实用程序子命令   list-network-listeners(1)

名称
       list-network-listeners - 列出现有网络监听程序

提要
           list-network-listeners [--help]
           [target]

说明
       list-network-listeners
       子命令列出现有的网络监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           将列出的内容限制为指定目标的网络监听程序。有效值如下：

           server
               列出默认服务器实例的网络监听程序。这是默认值。

           configuration-name
               列出指定配置的网络监听程序。

           cluster-name
               列出指定集群中所有服务器实例的网络监听程序。

           instance-name
               列出指定服务器实例的网络监听程序。

示例
       Example 1, 列出网络监听程序
           以下命令可列出服务器实例的所有网络监听程序：

               asadmin> list-network-listeners
               admin-listener
               http-listener-1
               https-listener-2
               Command list-network-listeners executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-network-listener(1), delete-network-listener(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日     list-network-listeners(1)
