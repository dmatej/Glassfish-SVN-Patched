list-virtual-servers(1asadminユーティリティのサブコマンlist-virtual-servers(1)

名前
       list-virtual-servers - 既存の仮想サーバーをリストします

概要
           list-virtual-servers [--help]
           [target]

説明
       list-virtual-serversサブコマンドは、既存の仮想サーバーをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           リストする内容を、指定したターゲットの仮想サーバーに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスの仮想サーバーをリストします。これがデフォルト値です。

           configuration-name
               指定した構成の仮想サーバーをリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスの仮想サーバーをリストします。

           instance-name
               指定したサーバー・インスタンスの仮想サーバーをリストします。

例
       例 1, 仮想サーバーのリスト
           次のコマンドは、サーバー・インスタンスのすべての仮想サーバーをリストします。

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                        2010年10月8日         list-virtual-servers(1)
