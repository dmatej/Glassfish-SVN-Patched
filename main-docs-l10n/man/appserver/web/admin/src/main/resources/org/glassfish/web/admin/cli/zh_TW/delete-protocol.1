delete-protocol(1)          asadmin 公用程式子命令          delete-protocol(1)

參考名
       delete-protocol - 移除協定

概要
           delete-protocol [--help]
           [--target target]
           protocol-name

描述
       delete-protocol 子命令會移除指定的協定。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除協定。有效的值如下：

           server
               從預設伺服器執行處理中刪除協定。此為預設值。

           configuration-name
               從指定組態中刪除協定。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除協定。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除協定。

運算元
       protocol-name
           要刪除的協定名稱。

範例
       範例 1, 刪除協定
           下列命令會刪除名為 http-1 的協定：

               asadmin> delete-protocol http-1
               Command delete-protocol executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-protocol(1), list-protocols(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日            delete-protocol(1)
