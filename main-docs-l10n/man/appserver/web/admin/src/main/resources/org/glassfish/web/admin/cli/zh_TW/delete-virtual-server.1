delete-virtual-server(1)    asadmin 公用程式子命令;   delete-virtual-server(1)

參考名
       delete-virtual-server - 移除虛擬伺服器

概要
           delete-virtual-server [--help]
           [--target target] virtual-server-id

描述
       delete-virtual-server 子命令會移除具有指定虛擬伺服器 ID
       的虛擬伺服器。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除虛擬伺服器。有效的值如下：

           server
               從預設伺服器執行處理中刪除虛擬伺服器。此為預設值。

           configuration-name
               從指定組態中刪除虛擬伺服器。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除虛擬伺服器。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除虛擬伺服器。

運算元
       virtual-server-id
           要刪除的虛擬伺服器的唯一識別碼。

範例
       範例 1, 刪除虛擬伺服器
           下列命令會刪除名為 sample_vs1 的虛擬伺服器：

               asadmin> delete-virtual-server sample_vs1
               Command delete-virtual-server executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-virtual-server(1), list-virtual-servers(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日      delete-virtual-server(1)
