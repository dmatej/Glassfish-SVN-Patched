delete-http-listener(1) Unterbefehle des asadmin-Utili delete-http-listener(1)

NAME
       delete-http-listener - entfernt einen Netzwerk-Listener

ZUSAMMENFASSUNG
           delete-http-listener [--help]
           [--target target]
           listener-id

BESCHREIBUNG
       Der Unterbefehl delete-http-listener entfernt den angegebenen
       Netzwerk-Listener.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den Netzwerk-Listener nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht den Netzwerk-Listener aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den Netzwerk-Listener aus der angegebenen Konfiguration.

           cluster-name
               Löscht den Netzwerk-Listener aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den Netzwerk-Listener aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       listener-id
           Die eindeutige ID für den Netzwerk-Listener, der gelöscht werden
           soll.

BEISPIELE
       Example 1, Unterbefehl "delete-http-listener" verwenden
           Der folgende Befehl löscht den Netzwerk-Listener sampleListener:

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011         delete-http-listener(1)
