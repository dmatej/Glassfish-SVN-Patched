create-http-redirect(1)     asadmin 实用程序子命令     create-http-redirect(1)

名称
       create-http-redirect - 添加新的 HTTP 重定向

提要
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

说明
       create-http-redirect 子命令创建 HTTP
       重定向。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --redirect-port
           用于重定向的端口号。如果 HTTP 监听程序支持非 SSL
           请求，并且接收请求时匹配的安全约束条件需要 SSL 传输，则 GlassFish
           Server 会自动将此请求重定向到此端口号。

       --secure-redirect
           如果设置为“真”，则 HTTP 重定向会运行 SSL。默认值为假

       --target
           仅在指定的目标上创建 HTTP 重定向。有效值如下：

           server
               在默认服务器实例上创建 HTTP 重定向。这是默认值。

           configuration-name
               在指定的配置中创建 HTTP 重定向。

           cluster-name
               在指定集群中的所有服务器实例上创建 HTTP 重定向。

           standalone-instance-name
               在指定的独立服务器实例上创建 HTTP 重定向。

操作数
       protocol-name
           要应用重定向的协议的名称。

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-http-redirect(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       create-http-redirect(1)
