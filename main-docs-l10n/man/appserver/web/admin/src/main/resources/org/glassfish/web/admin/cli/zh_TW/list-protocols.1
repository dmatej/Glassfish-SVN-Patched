list-protocols(1)           asadmin 公用程式子命令           list-protocols(1)

參考名
       list-protocols - 列出現有的協定

概要
           list-protocols [--help]
           [target]

描述
       list-protocols 子命令會列出現有的協定。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           將清單限制為指定目標的協定。有效的值如下：

           server
               列出預設伺服器執行處理的協定。此為預設值。

           configuration-name
               列出指定組態的協定。

           cluster-name
               列出指定叢集內所有伺服器執行處理的協定。

           instance-name
               列出指定伺服器執行處理的協定。

範例
       範例 1, 列出協定
           下列命令會列出伺服器執行處理的所有協定：

               asadmin> list-protocols
               admin-listener
               http-1
               http-listener-1
               http-listener-2
               Command list-protocols executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-protocol(1), delete-protocol(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日             list-protocols(1)
