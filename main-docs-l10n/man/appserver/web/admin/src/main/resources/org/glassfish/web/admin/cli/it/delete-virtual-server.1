delete-virtual-server(1)Comandi secondari della utilitdelete-virtual-server(1)

NOME
       delete-virtual-server - rimuove un server virtuale

SINTASSI
           delete-virtual-server [--help]
           [--target target] virtual-server-id

DESCRIZIONE
       Il comando secondario delete-virtual-server rimuove il server virtuale
       con l'ID specificato. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina il server virtuale solo dalla destinazione specificata. Di
           seguito sono riportati i valori validi.

           server
               Elimina il server virtuale dall'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Elimina il server virtuale dalla configurazione specificata.

           cluster-name
               Elimina il server virtuale da tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Elimina il server virtuale dall'istanza server standalone
               specificata.

OPERANDI
       virtual-server-id
           L'identificativo univoco del server virtuale da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un server virtuale
           Il comando seguente elimina il server virtuale denominato
           sample_vs1:

               asadmin> delete-virtual-server sample_vs1
               Command delete-virtual-server executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-virtual-server(1), list-virtual-servers(1)

       asadmin(1M)

Java EE 7                       8 ottobre 2010        delete-virtual-server(1)
