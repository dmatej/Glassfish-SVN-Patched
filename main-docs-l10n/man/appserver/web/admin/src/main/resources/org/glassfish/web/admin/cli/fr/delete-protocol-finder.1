delete-protocol-finder(1Sous-commandes de l'utilitairdelete-protocol-finder(1)

NOM
       delete-protocol-finder - enlève un sélecteur de protocole

SYNTAXE
           delete-protocol-finder [--help]
           --protocol protocol-name
           [--target server]
           protocol-finder-name

DESCRIPTION
       La sous-commande delete-protocol-finder enlève le sélecteur de
       protocole spécifié. Cette sous-commande est prise en charge en mode
       distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --protocol-name
           Nom du protocole associé.

       --target
           Supprime le sélecteur de protocole de la cible spécifiée
           uniquement. Les valeurs valides sont les suivantes :

           server
               Supprime le sélecteur de protocole de l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Supprime le sélecteur de protocole de la configuration
               spécifiée.

           cluster-name
               Supprime le sélecteur de protocole de toutes les instances de
               serveur dans le cluster spécifié.

           standalone-instance-name
               Supprime le sélecteur de protocole de l'instance de serveur
               autonome spécifiée.

OPÉRANDES
       protocol-finder-name
           Nom du sélecteur de protocole à supprimer.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                         11 oct 2010        delete-protocol-finder(1)
