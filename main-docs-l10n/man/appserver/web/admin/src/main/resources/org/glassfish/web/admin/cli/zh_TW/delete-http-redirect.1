delete-http-redirect(1)     asadmin 公用程式子命令     delete-http-redirect(1)

參考名
       delete-http-redirect - 移除 HTTP 重新導向

概要
           delete-http-redirect [--help]
           [--target target]
           protocol-name

描述
       delete-http-redirect 子命令會移除指定的 HTTP
       重新導向。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除 HTTP 重新導向。有效的值如下：

           server
               從預設伺服器執行處理中刪除 HTTP 重新導向。此為預設值。

           configuration-name
               從指定組態中刪除 HTTP 重新導向。

           cluster-name
               從指定叢集內的所有伺服器執行處理中刪除 HTTP 重新導向。

           standalone-instance-name
               從指定的獨立伺服器執行處理中刪除 HTTP 重新導向。

運算元
       protocol-name
           關聯協定的名稱。

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       delete-http-redirect(1)
