create-protocol-finder(1) asadmin 유틸리티 하위 명령 create-protocol-finder(1)

제목
       create-protocol-finder - 새 프로토콜 검사기 추가

시놉시스
           create-protocol-finder [--help]
           --protocol protocol-name
           --targetprotocol target-protocol-name
           --classname class-name
           [--target server]
           protocol-finder-name

설명
       create-protocol-finder 하위 명령은 프로토콜에 대한 프로토콜 검사기를
       생성합니다. 이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --protocol
           연관된 프로토콜의 이름입니다.

       --targetprotocol
           대상 프로토콜의 이름입니다.

       --classname
           프로토콜 검사기를 구현하는 Java 클래스의 정규화된 이름입니다.

       --target
           지정된 대상에만 프로토콜 검사기를 생성합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에 프로토콜 검사기를 생성합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에 프로토콜 검사기를 생성합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에 프로토콜 검사기를
               생성합니다.

           standalone-instance-name
               지정된 독립형 서버 인스턴스에 프로토콜 검사기를 생성합니다.

피연산자
       protocol-finder-name
           프로토콜 검사기의 이름입니다.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       delete-protocol-finder(1), list-protocol-finders(1)

       asadmin(1M)

Java EE 7                      2010년 11월 24일      create-protocol-finder(1)
