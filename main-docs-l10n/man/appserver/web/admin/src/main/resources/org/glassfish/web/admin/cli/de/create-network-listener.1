create-network-listener(Unterbefehle des asadmin-Uticreate-network-listener(1)

NAME
       create-network-listener - fügt ein neues Netzwerk-Listener Socket hinzu

ZUSAMMENFASSUNG
           create-network-listener [--help]
           [--address address]
           --listenerport listener-port
           [--threadpool thread-pool]
           --protocol protocol
           [--transport transport]
           [--enabled={true|false}]
           [--jkenabled={false|true}]
           [--target target]
           listener-name

BESCHREIBUNG
       Der Unterbefehl create-network-listener erstellt einen
       Netzwerk-Listener. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

           Anmerkung
           ┌───────────────────────────────────────┐
           │Hinweis                                │
           │                                       │
           │           Wenn Sie den speziellen     │
           │           Netzwerk-Listener namens    │
           │           admin-listener bearbeiten,  │
           │           müssen Sie den Server neu   │
           │           starten, damit die          │
           │           Änderungen in Kraft treten. │
           │           Die Administrationskonsole  │
           │           informiert Sie nicht, dass  │
           │           in diesem Fall ein Neustart │
           │           erforderlich ist.           │
           └───────────────────────────────────────┘

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Sie können den Unterbefehl   │
           │           create-http-listener         │
           │           verwenden, um einen          │
           │           Netzwerk-Listener zu         │
           │           erstellen, der das           │
           │           HTTP-Protokoll verwendet,    │
           │           ohne zunächst ein Protokoll, │
           │           einen Transport oder eine    │
           │           HTTP-Konfiguration erstellen │
           │           zu müssen. Dieser            │
           │           Unterbefehl ist praktisch,   │
           │           ermöglicht aber nur den      │
           │           Zugriff auf eine begrenzte   │
           │           Anzahl an Optionen.          │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --address
           Die IP-Adresse oder der Hostname (durch DNS auflösbar).

       --listenerport
           Die Portnummer zum Erstellen des Listener Sockets. Gültige Werte
           sind 1 - 65535. Bei UNIX-Systemen erfordert das Erstellen von
           Sockets, die auf den Ports 1-1024 horchen,
           Superuser-Berechtigungen. Standardmäßig wird ein SSL-Listener
           Socket zum Horchen auf Port 443 konfiguriert.

       --threadpool
           Der Name des Threadpools für diesen Listener. Die Angabe eines
           Threadpools ist optional. Der Standardwert ist http-thread-pool.

       --protocol
           Der Name des Protokolls für diesen Listener.

       --transport
           Der Name des Transports für diesen Listener. Die Angabe eines
           Transports ist optional. Der Standardwert ist tcp.

       --enabled
           Wenn der Wert auf true gesetzt ist, wird der Listener zur Laufzeit
           aktiviert.

       --jkenabled
           Wenn der Wert auf true gesetzt ist, wird mod_jk für diesen Listener
           aktiviert. Der Standardwert ist "false".

       --target
           Erstellt den Netzwerk-Listener nur auf dem angegebenen Ziel.
           Folgende Werte sind gültig:

           server
               Erstellt den Netzwerk-Listener auf der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Erstellt den Netzwerk-Listener in der angegebenen
               Konfiguration.

           cluster-name
               Erstellt den Netzwerk-Listener auf allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Erstellt den Netzwerk-Listener auf der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       listener-name
           Der Name des Netzwerk-Listeners.

BEISPIELE
       Beispiel 1, Netzwerk-Listener erstellen
           Der folgende Unterbefehl erstellt einen Netzwerk-Listener namens
           sampleListener, der nicht zur Laufzeit aktiviert ist:

               asadmin> create-network-listener --listenerport 7272 protocol http-1
               --enabled=false sampleListener
               Command create-network-listener executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       delete-network-listener(1), list-network-listeners(1),
       create-transport(1), create-protocol(1), create-threadpool(1),
       create-http-listener(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010      create-network-listener(1)
