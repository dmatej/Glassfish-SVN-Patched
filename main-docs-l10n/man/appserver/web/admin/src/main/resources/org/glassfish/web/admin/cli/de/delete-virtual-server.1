delete-virtual-server(1)Unterbefehle des asadmin-Utilidelete-virtual-server(1)

NAME
       delete-virtual-server - entfernt einen virtuellen Server

ZUSAMMENFASSUNG
           delete-virtual-server [--help]
           [--target target] virtual-server-id

BESCHREIBUNG
       Der Unterbefehl delete-virtual-server entfernt den virtuellen Server
       mit der angegebenen ID. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den virtuellen Server nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht den virtuellen Server aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den virtuellen Server aus der angegebenen Konfiguration.

           cluster-name
               Löscht den virtuellen Server aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den virtuellen Server aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       virtual-server-id
           Die eindeutige ID für den virtuellen Server, der gelöscht werden
           soll.

BEISPIELE
       Example 1, Virtuelle Server löschen
           Der folgende Unterbefehl löscht den virtuellen Server namens
           sample_vs1:

               asadmin> delete-virtual-server sample_vs1
               Command delete-virtual-server executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-virtual-server(1), list-virtual-servers(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010        delete-virtual-server(1)
