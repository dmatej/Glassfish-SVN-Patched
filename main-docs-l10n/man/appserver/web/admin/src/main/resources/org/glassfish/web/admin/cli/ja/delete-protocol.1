delete-protocol(1)    asadminユーティリティのサブコマンド   delete-protocol(1)

名前
       delete-protocol - プロトコルを除去します

概要
           delete-protocol [--help]
           [--target target]
           protocol-name

説明
       delete-protocolサブコマンドは、指定したプロトコルを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           指定したターゲットからのみプロトコルを削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからプロトコルを削除します。これがデフォルト値です。

           configuration-name
               指定した構成からプロトコルを削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからプロトコルを削除します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスからプロトコルを削除します。

オペランド
       protocol-name
           削除するプロトコルの名前。

例
       例 1, プロトコルの削除
           次のコマンドは、http-1という名前のプロトコルを削除します。

               asadmin> delete-protocol http-1
               Command delete-protocol executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol(1), list-protocols(1)

       asadmin(1M)

Java EE 7                        2010年10月8日              delete-protocol(1)
