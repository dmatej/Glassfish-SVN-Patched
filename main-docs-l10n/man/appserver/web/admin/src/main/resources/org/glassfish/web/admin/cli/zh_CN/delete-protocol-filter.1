delete-protocol-filter(1)   asadmin 实用程序子命令   delete-protocol-filter(1)

名称
       delete-protocol-filter - 删除协议过滤器

提要
           delete-protocol-filter [--help]
           --protocol protocol-name
           [--target server]
           protocol-filter-name

说明
       delete-protocol-filter
       子命令删除指定的协议过滤器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --protocol-name
           关联协议的名称。

       --target
           仅从指定目标中删除协议过滤器。有效值如下：

           server
               从默认服务器实例中删除协议过滤器。这是默认值。

           configuration-name
               从指定配置中删除协议过滤器。

           cluster-name
               从指定集群的所有服务器实例中删除协议过滤器。

           standalone-instance-name
               从指定的独立服务器实例中删除协议过滤器。

操作数
       protocol-filter-name
           要删除的协议过滤器的名称。

示例
       Example 1, 删除协议过滤器
           以下命令删除名为 http1-filter 的协议过滤器：

               asadmin> delete-protocol-filter --protocol http1 http1-filter
               Command delete-protocol-filter executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-protocol-filter(1), list-protocol-filters(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日    delete-protocol-filter(1)
