list-network-listeners(1Subcomandos de la Utilidad aslist-network-listeners(1)

NOMBRE DE REFERENCIA
       list-network-listeners - Muestra los listeners de red existentes.

SINOPSIS
           list-network-listeners [--help]
           [target]

DESCRIPCIÓN
       El subcomando list-network-listeners muestra una lista de los listeners
       de red existentes. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Restringe la lista de listeners de red para un destino
           especificado. Los valores válidos son los siguientes:

           server
               Muestra una lista de listeners de red para la instancia de
               servidor por defecto. Este es el valor por defecto.

           configuration-name
               Muestra una lista de listeners de red para la configuración
               especificada.

           cluster-name
               Muestra una lista de listeners de red para todas las instancias
               del servidor en el cluster especificado.

           instance-name
               Muestra una lista de listeners de red para la instancia de
               servidor especificada.

EJEMPLOS
       Ejemplo 1, Lista de Listeners de Red
           El siguiente comando muestra todos los listeners de red para la
           instancia de servidor:

               asadmin> list-network-listeners
               admin-listener
               http-listener-1
               https-listener-2
               Command list-network-listeners executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-network-listener(1), delete-network-listener(1)

       asadmin(1M)

Java EE 7                    08 de octubre de 2010   list-network-listeners(1)
