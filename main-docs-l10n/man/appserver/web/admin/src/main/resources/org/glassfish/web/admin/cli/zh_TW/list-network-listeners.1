list-network-listeners(1)   asadmin 公用程式子命令   list-network-listeners(1)

參考名
       list-network-listeners - 列出現有的網路監聽程式

概要
           list-network-listeners [--help]
           [target]

描述
       list-network-listeners
       子命令會列出現有的網路監聽程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           將清單限制為指定目標的網路監聽程式。有效的值如下：

           server
               列出預設伺服器執行處理的網路監聽程式。此為預設值。

           configuration-name
               列出指定組態的網路監聽程式。

           cluster-name
               列出指定叢集內所有伺服器執行處理的網路監聽程式。

           instance-name
               列出指定伺服器執行處理的網路監聽程式。

範例
       範例 1, 列出網路監聽程式
           下列命令會列出伺服器執行處理的所有網路監聽程式：

               asadmin> list-network-listeners
               admin-listener
               http-listener-1
               https-listener-2
               Command list-network-listeners executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-network-listener(1), delete-network-listener(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日     list-network-listeners(1)
