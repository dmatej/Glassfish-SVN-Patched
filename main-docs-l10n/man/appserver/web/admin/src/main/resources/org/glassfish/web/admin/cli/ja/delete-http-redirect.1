delete-http-redirect(1asadminユーティリティのサブコマンdelete-http-redirect(1)

名前
       delete-http-redirect - HTTPリダイレクトを除去します

概要
           delete-http-redirect [--help]
           [--target target]
           protocol-name

説明
       delete-http-redirectサブコマンドは、指定されたHTTPリダイレクトを除去します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           指定したターゲットからのみHTTPリダイレクトを削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからHTTPリダイレクトを削除します。これがデフォルト値です。

           configuration-name
               指定した構成からHTTPリダイレクトを削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからHTTPリダイレクトを削除します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスからHTTPリダイレクトを削除します。

オペランド
       protocol-name
           関連付けられたプロトコルの名前。

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                        2010年10月8日         delete-http-redirect(1)
