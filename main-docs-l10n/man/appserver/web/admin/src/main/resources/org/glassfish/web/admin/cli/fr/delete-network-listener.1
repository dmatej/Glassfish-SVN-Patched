delete-network-listener(Sous-commandes de l'utilitaidelete-network-listener(1)

NOM
       delete-network-listener - enlève un processus d'écoute réseau

SYNTAXE
           delete-network-listener [--help]
           [--target target]
           listener-name

DESCRIPTION
       La sous-commande delete-network-listener enlève le processus d'écoute
       réseau spécifié. Cette sous-commande est prise en charge en mode
       distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option supprime le processus d'écoute réseau uniquement sur
           la cible indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime le processus d'écoute réseau sur
               l'instance de serveur par défaut. Il s'agit de la valeur par
               défaut.

           configuration-name
               Cette valeur supprime le processus d'écoute réseau dans la
               configuration indiquée.

           cluster-name
               Cette valeur supprime le processus d'écoute réseau sur toutes
               les instances de serveur dans le cluster indiqué.

           standalone-instance-name
               Cette valeur supprime le processus d'écoute réseau sur
               l'instance de serveur autonome indiquée.

OPÉRANDES
       listener-name
           Nom du processus d'écoute réseau à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un processus d'écoute réseau
           La commande suivante supprime le processus d'écoute réseau nommé
           sampleListener :

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                         8 oct 2010        delete-network-listener(1)
