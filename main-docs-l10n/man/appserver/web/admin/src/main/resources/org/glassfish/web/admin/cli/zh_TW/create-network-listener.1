create-network-listener(1)  asadmin 公用程式子命令  create-network-listener(1)

參考名
       create-network-listener - 新增網路監聽程式通訊端

概要
           create-network-listener [--help]
           [--address address]
           --listenerport listener-port
           [--threadpool thread-pool]
           --protocol protocol
           [--transport transport]
           [--enabled={true|false}]
           [--jkenabled={false|true}]
           [--target target]
           listener-name

描述
       create-network-listener
       子命令會建立網路監聽程式。只有遠端模式支援這個子命令。

           注意
           ┌────────────────────────────────────────────────────────────────────────────┐
           │備註                                                                        │
           │                                                                            │
           │           如果您編輯名為                                                   │
           │           admin-listener                                                   │
           │           的特定網路監聽程式，必須重新啟動伺服器才能使變更生效。管理主控台 │
           │           不會告知在此情形下必須重新啟動。                                 │
           └────────────────────────────────────────────────────────────────────────────┘

           注意
           ┌────────────────────────────────────────────────────────────────────────────┐
           │備註                                                                        │
           │                                                                            │
           │           您可以使用                                                       │
           │           create-http-listener                                             │
           │           子命令來建立使用 HTTP                                            │
           │           協定的網路監聽程式，而無需先建立協定、傳輸或                     │
           │           HTTP                                                             │
           │           組態。這個子命令是一個便利的捷徑，但是它只能存取有限數目的選項。 │
           └────────────────────────────────────────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --address
           IP 位址或主機名稱 (可用 DNS 解析)。

       --listenerport
           要在其上建立監聽程式通訊端的連接埠號碼。有效值為 1 - 65535。在 UNIX
           上，建立監聽連接埠 1-1024 的通訊端需要超級使用者權限。組態 SSL
           監聽通訊端可在連接埠 443 上監聽為標準作法。

       --threadpool
           此監聽程式之繫線集區的名稱。指定繫線集區是選擇性的。預設值為
           http-thread-pool。

       --protocol
           此監聽程式之協定的名稱。

       --transport
           此監聽程式之傳輸的名稱。指定傳輸是選擇性的。預設值為 tcp。

       --enabled
           如果設為預設 true，會在執行階段啟用監聽程式。

       --jkenabled
           如果設為 true，則會對此監聽程式啟用 mod_jk。預設值為 false。

       --target
           僅在指定目標上建立網路監聽程式。有效的值如下：

           server
               在預設伺服器執行處理上建立網路監聽程式。此為預設值。

           configuration-name
               在指定組態中建立網路監聽程式。

           cluster-name
               在指定叢集內的所有伺服器執行處理上建立網路監聽程式。

           standalone-instance-name
               在指定的獨立伺服器執行處理上建立網路監聽程式。

運算元
       listener-name
           網路監聽程式的名稱。

範例
       範例 1, 建立網路監聽程式
           下列命令會建立不會在執行時期啟用、名為 sampleListener
           的網路監聽程式：

               asadmin> create-network-listener --listenerport 7272 protocol http-1
               --enabled=false sampleListener
               Command create-network-listener executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-network-listener(1), list-network-listeners(1),
       create-transport(1), create-protocol(1), create-threadpool(1),
       create-http-listener(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日    create-network-listener(1)
