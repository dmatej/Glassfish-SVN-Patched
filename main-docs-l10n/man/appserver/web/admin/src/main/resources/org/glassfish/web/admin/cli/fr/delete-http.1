delete-http(1)          Sous-commandes de l'utilitaire          delete-http(1)

NOM
       delete-http - enlève les paramètres HTTP d'un protocole

SYNTAXE
           delete-http [--help]
           [--target target]
           protocol-name

DESCRIPTION
       La sous-commande delete-http enlève l'ensemble de paramètres HTTP
       indiqué d'un protocole. Cette sous-commande est prise en charge en mode
       distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option supprime l'ensemble de paramètres HTTP uniquement sur
           la cible indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime l'ensemble de paramètres HTTP sur
               l'instance de serveur par défaut. Il s'agit de la valeur par
               défaut.

           configuration-name
               Cette valeur supprime l'ensemble de paramètres HTTP dans la
               configuration indiquée.

           cluster-name
               Cette valeur supprime l'ensemble de paramètres HTTP sur toutes
               les instances de serveur dans le cluster indiqué.

           standalone-instance-name
               Cette valeur supprime l'ensemble de paramètres HTTP sur
               l'instance de serveur autonome indiquée.

OPÉRANDES
       protocol-name
           Nom du protocole pour lequel supprimer l'ensemble de paramètres
           HTTP.

EXEMPLES
       Exemple 1, Suppression d'un ensemble de paramètres HTTP
           La commande suivante supprime l'ensemble de paramètres HTTP d'un
           protocole nommé http-1 :

               asadmin> delete-http http-1
               Command delete-http executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-http(1)

       asadmin(1M)

Java EE 7                         8 oct 2010                    delete-http(1)
