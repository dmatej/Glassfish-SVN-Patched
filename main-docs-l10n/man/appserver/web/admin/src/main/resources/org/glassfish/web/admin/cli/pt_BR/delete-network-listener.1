delete-network-listener(Subcomandos do utilitário asdelete-network-listener(1)

NOME
       delete-network-listener - remove o listener de uma rede

SINOPSE
           delete-network-listener [--help]
           [--target target]
           listener-name

DESCRIçãO
       O subcomando delete-network-listener remove o listener de rede
       especificado. Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Deleta o listener de rede só do alvo especificado. Os valores
           válidos são os seguintes:

           server
               Deleta o listener de rede da instância do servidor default.
               Este é o valor default.

           configuration-name
               Deleta o listener de rede da configuração especificada.

           cluster-name
               Deleta o listener de rede de todas as instâncias do servidor do
               cluster especificado.

           standalone-instance-name
               Deleta o listener de rede da instância do servidor standalone
               especificado.

OPERANDOS
       listener-name
           O nome do listener de rede a ser deletado.

EXEMPLOS
       Example 1, Deletando um Listener de Rede
           O comando a seguir deleta o listener de rede com o nome
           sampleListener :

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                         08 out 2010       delete-network-listener(1)
