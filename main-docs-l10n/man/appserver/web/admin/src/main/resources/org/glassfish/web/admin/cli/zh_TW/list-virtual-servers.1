list-virtual-servers(1)     asadmin 公用程式子命令     list-virtual-servers(1)

參考名
       list-virtual-servers - 列出現有的虛擬伺服

概要
           list-virtual-servers [--help]
           [target]

描述
       list-virtual-servers
       子命令會列出現有的虛擬伺服器。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           將清單限制為指定目標的虛擬伺服器。有效的值如下：

           server
               列出預設伺服器執行處理的虛擬伺服器。此為預設值。

           configuration-name
               列出指定組態的虛擬伺服器。

           cluster-name
               列出指定叢集內所有伺服器執行處理的虛擬伺服器。

           instance-name
               列出指定伺服器執行處理的虛擬伺服器。

範例
       範例 1, 列出虛擬伺服器
           下列命令會列出伺服器執行處理的所有虛擬伺服器：

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       list-virtual-servers(1)
