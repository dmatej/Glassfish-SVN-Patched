delete-http-listener(1) Sous-commandes de l'utilitaire delete-http-listener(1)

NOM
       delete-http-listener - enlève un processus d'écoute réseau

SYNTAXE
           delete-http-listener [--help]
           [--target target]
           listener-id

DESCRIPTION
       La sous-commande delete-http-listener enlève le processus d'écoute
       réseau indiqué.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option supprime le processus d'écoute réseau uniquement sur
           la cible indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime le processus d'écoute réseau sur
               l'instance de serveur par défaut. Il s'agit de la valeur par
               défaut.

           configuration-name
               Cette valeur supprime le processus d'écoute réseau dans la
               configuration indiquée.

           cluster-name
               Cette valeur supprime le processus d'écoute réseau sur toutes
               les instances de serveur dans le cluster indiqué.

           standalone-instance-name
               Cette valeur supprime le processus d'écoute réseau sur
               l'instance de serveur autonome indiquée.

OPÉRANDES
       listener-id
           Identificateur unique du processus d'écoute réseau à supprimer.

EXEMPLES
       Exemple 1, Utilisation de la sous-commande delete-http-listener
           La commande suivante supprime le processus d'écoute réseau nommé
           sampleListener :

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                         11 fév 2011          delete-http-listener(1)
