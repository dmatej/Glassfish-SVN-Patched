list-protocol-filters(asadminユーティリティのサブコマン

名前
       list-protocol-filters - 既存のプロトコル・フィルタをリストします

概要
           list-protocol-filters [--help]
           [--target server]
           protocol-name

説明
       list-protocol-filtersサブコマンドは、既存のプロトコル・フィルタをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       target
           リストする内容を、指定したターゲットのプロトコル・フィルタに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのプロトコル・フィルタをリストします。この値がデフォルト値です。

           configuration-name
               指定した構成のプロトコル・フィルタをリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのプロトコル・フィルタをリストします。

           instance-name
               指定したサーバー・インスタンスのプロトコル・フィルタをリストします。

オペランド
       protocol-name
           プロトコル・フィルタをリストするプロトコルの名前。

例
       例 1, プロトコル・フィルタのリスト
           次のコマンドは、サーバー・インスタンスのすべてのプロトコル・フィルタをリストします。

               asadmin> list-protocol-filters http1
               http1-filter
               https1-filter
               Command list-protocol-filters executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol-filter(1), delete-protocol-filter(1)

       asadmin(1M)

Java EE 7                       2010年10月11日        list-protocol-filters(1)
