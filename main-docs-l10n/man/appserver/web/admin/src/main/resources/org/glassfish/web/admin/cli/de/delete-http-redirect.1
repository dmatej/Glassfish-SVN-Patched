delete-http-redirect(1) Unterbefehle des asadmin-Utili delete-http-redirect(1)

NAME
       delete-http-redirect - entfernt eine HTTP-Umleitung

ZUSAMMENFASSUNG
           delete-http-redirect [--help]
           [--target target]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl delete-http-redirect entfernt die angegebene
       HTTP-Umleitung. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht die HTTP-Umleitung nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

           server
               Löscht die HTTP-Umleitung aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht die HTTP-Umleitung aus der angegebenen Konfiguration.

           cluster-name
               Löscht die HTTP-Umleitung aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht die HTTP-Umleitung aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-name
           Der Name des verknüpften Protokolls.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010         delete-http-redirect(1)
