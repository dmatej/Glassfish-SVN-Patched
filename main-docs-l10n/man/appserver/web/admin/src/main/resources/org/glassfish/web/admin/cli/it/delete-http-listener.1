delete-http-listener(1) Comandi secondari della utilit delete-http-listener(1)

NOME
       delete-http-listener - rimuove un listener di rete

SINTASSI
           delete-http-listener [--help]
           [--target target]
           listener-id

DESCRIZIONE
       Il comando secondario delete-http-listener rimuove il listener di rete
       specificato.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina il listener di rete solo dalla destinazione specificata. Di
           seguito sono riportati i valori validi.

           server
               Elimina il listener di rete dall'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Elimina il listener di rete dalla configurazione specificata.

           cluster-name
               Elimina il listener di rete da tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Elimina il listener di rete dall'istanza server standalone
               specificata.

OPERANDI
       listener-id
           L'identificativo univoco del listener di rete da eliminare.

ESEMPI
       Esempio 1, Uso del comando secondario delete-http-listener
           Il comando seguente elimina il listener di rete denominato
           sampleListener:

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011        delete-http-listener(1)
