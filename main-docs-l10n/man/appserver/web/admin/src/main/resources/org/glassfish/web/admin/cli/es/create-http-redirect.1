create-http-redirect(1) Subcomandos de la Utilidad asa create-http-redirect(1)

NOMBRE DE REFERENCIA
       create-http-redirect - Agrega un nuevo redireccionamiento HTTP.

SINOPSIS
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

DESCRIPCIÓN
       El subcomando create-http-redirect crea un redireccionamiento HTTP.
       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --redirect-port
           Número de puerto para redireccionamientos. Si el listener HTTP
           soporta solicitudes que no son SSL, y se recibe una solicitud para
           la que una restricción de seguridad coincidente exige transporte
           SSL, GlassFish Server redireccionará automáticamente la solicitud a
           este número de puerto.

       --secure-redirect
           Si se define en true, el redireccionamiento HTTP ejecuta SSL. El
           valor por defecto es false.

       --target
           Crea el redireccionamiento HTTP sólo en el destino especificado.
           Los valores válidos son los siguientes:

           server
               Crea el redireccionamiento HTTP en la instancia de servidor por
               defecto. Este es el valor por defecto.

           configuration-name
               Crea el redireccionamiento HTTP en la configuración
               especificada.

           cluster-name
               Crea el redireccionamiento HTTP en todas las instancias de
               servidor del cluster especificado.

           standalone-instance-name
               Crea el redireccionamiento HTTP en la instancia de servidor
               independiente especificada.

OPERANDOS
       protocol-name
           Nombre del protocolo al que desea aplicar el redireccionamiento.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-http-redirect(1)

       asadmin(1M)

Java EE 7                    08 de octubre de 2010     create-http-redirect(1)
