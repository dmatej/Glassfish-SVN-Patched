list-virtual-servers(1)   asadmin 유틸리티 하위 명령   list-virtual-servers(1)

제목
       list-virtual-servers - 기존 가상 서버 나열

시놉시스
           list-virtual-servers [--help]
           [target]

설명
       list-virtual-servers 하위 명령은 기존 가상 서버를 나열합니다. 이 하위
       명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           지정된 대상의 가상 서버만 나열되도록 제한합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스의 가상 서버를 나열합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성의 가상 서버를 나열합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스의 가상 서버를
               나열합니다.

           instance-name
               지정된 서버 인스턴스에 대한 가상 서버를 나열합니다.

예제
       예 1, 가상 서버 나열
           다음 명령은 서버 인스턴스의 모든 가상 서버를 나열합니다.

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                       2010년 10월 8일        list-virtual-servers(1)
