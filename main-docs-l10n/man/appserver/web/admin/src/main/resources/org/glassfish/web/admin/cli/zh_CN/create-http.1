create-http(1)              asadmin 实用程序子命令              create-http(1)

名称
       create-http - 设置协议的 HTTP 参数

提要
           create-http [--help]
           --default-virtual-server virtual-server
           [--request-timeout-seconds timeout]
           [--timeout-seconds timeout]
           [--max-connection max-keepalive]
           [--dns-lookup-enabled={false|true}]
           [--servername server-name]
           [--target target]

           protocol-name

说明
       create-http 子命令为协议创建一组 HTTP
       参数，这组参数又可用于配置一个或多个网络监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --default-virtual-server
           关联网络监听程序的默认虚拟服务器的 ID 属性。

       --request-timeout-seconds
           请求超时时间 (秒)。如果不设置该选项，请求将在 30 秒后超时。

       --timeout-seconds
           保持活动连接处于打开状态的最长时间 (秒)。如果值为 0 或小于
           0，则表示保持活动连接将无限期地保持打开状态。默认值为 30。

       --max-connection
           在服务器关闭连接之前可以连续处理的最大 HTTP 请求数。将此属性设置为
           1 可禁用 HTTP/1.0 的保持活动功能以及 HTTP/1.1
           的保持活动和管道功能。默认值为 256。

       --dns-lookup-enabled
           如果设置为真

       --servername
           通知服务器应该在它发送给客户机的任何 URL
           的主机名部分中使用什么名称。这会影响服务器自动生成的
           URL，但不会影响存储在服务器中的目录和文件的
           URL。如果服务器使用别名，则此名称应为别名。如果附加冒号和端口号，则服务器发送给客户机的
           URL 中将使用此端口。

       --target
           仅在指定的目标上创建 HTTP 参数集。有效值如下：

           server
               在默认服务器实例上创建 HTTP 参数集。这是默认值。

           configuration-name
               在指定的配置中创建 HTTP 参数集。

           cluster-name
               在指定集群中的所有服务器实例上创建 HTTP 参数集。

           standalone-instance-name
               在指定的独立服务器实例上创建 HTTP 参数集。

操作数
       protocol-name
           应用此 HTTP 参数集的协议的名称。

示例
       Example 1, 使用 create-http 子命令
           以下命令为名为 http-1 的协议创建一个 HTTP 参数集：

               asadmin> create-http --timeout-seconds 60 --default-virtual-server server http-1
               Command create-http executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-http(1), create-network-listener(1), create-protocol(1),
       create-virtual-server(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日                create-http(1)
