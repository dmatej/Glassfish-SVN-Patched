list-virtual-servers(1)     asadmin 实用程序子命令     list-virtual-servers(1)

名称
       list-virtual-servers - 列出现有虚拟服务器

提要
           list-virtual-servers [--help]
           [target]

说明
       list-virtual-servers
       子命令列出现有的虚拟服务器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           将列出的内容限制为指定目标的虚拟服务器。有效值如下：

           server
               列出默认服务器实例的虚拟服务器。这是默认值。

           configuration-name
               列出指定配置的虚拟服务器。

           cluster-name
               列出指定集群中所有服务器实例的虚拟服务器。

           instance-name
               列出指定服务器实例的虚拟服务器。

示例
       Example 1, 列出虚拟服务器
           以下命令列出服务器实例的所有虚拟服务器：

               asadmin> list-virtual-servers
               server
               __asadmin
               Command list-virtual-servers executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-virtual-server(1), delete-virtual-server(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       list-virtual-servers(1)
