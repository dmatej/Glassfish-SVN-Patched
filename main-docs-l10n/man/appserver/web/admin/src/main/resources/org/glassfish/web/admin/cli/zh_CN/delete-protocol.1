delete-protocol(1)          asadmin 实用程序子命令          delete-protocol(1)

名称
       delete-protocol - 删除协议

提要
           delete-protocol [--help]
           [--target target]
           protocol-name

说明
       delete-protocol 子命令删除指定的协议。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除协议。有效值如下：

           server
               从默认服务器实例中删除协议。这是默认值。

           configuration-name
               从指定配置中删除协议。

           cluster-name
               从指定集群的所有服务器实例中删除协议。

           standalone-instance-name
               从指定的独立服务器实例中删除协议。

操作数
       protocol-name
           要删除的协议的名称。

示例
       Example 1, 删除协议
           以下命令删除名为 http-1 的协议：

               asadmin> delete-protocol http-1
               Command delete-protocol executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-protocol(1), list-protocols(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日            delete-protocol(1)
