delete-http-redirect(1) Sous-commandes de l'utilitaire delete-http-redirect(1)

NOM
       delete-http-redirect - enlève une redirection HTTP

SYNTAXE
           delete-http-redirect [--help]
           [--target target]
           protocol-name

DESCRIPTION
       La sous-commande delete-http-redirect enlève la redirection HTTP
       indiquée. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option supprime la redirection HTTP uniquement sur la cible
           indiquée. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime la redirection HTTP sur l'instance de
               serveur par défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Cette valeur supprime la redirection HTTP dans la configuration
               indiquée.

           cluster-name
               Cette valeur supprime la redirection HTTP sur toutes les
               instances de serveur dans le cluster indiqué.

           standalone-instance-name
               Cette valeur supprime la redirection HTTP sur l'instance de
               serveur autonome indiquée.

OPÉRANDES
       protocol-name
           Nom du protocole associé.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                         8 oct 2010           delete-http-redirect(1)
