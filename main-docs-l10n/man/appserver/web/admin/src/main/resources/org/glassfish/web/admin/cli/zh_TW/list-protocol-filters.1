list-protocol-filters(1)    asadmin 公用程式子命令    list-protocol-filters(1)

參考名
       list-protocol-filters - 列出現有的協定篩選

概要
           list-protocol-filters [--help]
           [--target server]
           protocol-name

描述
       list-protocol-filters
       子命令會列出現有的協定篩選器。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       target
           將清單限制為指定目標的協定篩選器。有效的值如下：

           server
               列出預設伺服器執行處理的協定篩選器。此為預設值。

           configuration-name
               列出指定組態的協定篩選器。

           cluster-name
               列出指定叢集內所有伺服器執行處理的協定篩選器。

           instance-name
               列出指定伺服器執行處理的協定篩選器。

運算元
       protocol-name
           要列出協定篩選器的協定名稱。

範例
       範例 1, 列出協定篩選器
           下列命令會列出伺服器執行處理的所有協定篩選器：

               asadmin> list-protocol-filters http1
               http1-filter
               https1-filter
               Command list-protocol-filters executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-protocol-filter(1), delete-protocol-filter(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 11 日     list-protocol-filters(1)
