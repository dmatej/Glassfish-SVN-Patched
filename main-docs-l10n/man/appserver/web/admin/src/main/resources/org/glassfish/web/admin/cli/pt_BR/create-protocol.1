create-protocol(1)      Subcomandos do utilitário asad      create-protocol(1)

NOME
       create-protocol - adiciona um novo protocolo

SINOPSE
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

DESCRIçãO
       O subcomando create-protocol cria um protocolo. Esse subcomando só é
       suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --securityenabled
           Se definido como true, o protocolo executa SSL. É possível ligar ou
           desligar SSL2 ou SSL3 e definir cifras usando um elemento ssl. A
           definição de segurança global ativa ou desativa o SSL tornando os
           certificados disponíveis para a instância de servidor. O valor
           default é false.

       --target
           Cria o protocolo só no alvo especificado. Os valores válidos são os
           seguintes:

           server
               Cria o protocolo na instância do servidor default. Este é o
               valor default.

           configuration-name
               Cria o protocolo na configuração especificada.

           cluster-name
               Cria o protocolo em todas as instâncias de servidor no cluster
               especificado.

           standalone-instance-name
               Cria o protocolo na instância do servidor standalone
               especificada.

OPERANDOS
       protocol-name
           O nome do protocolo.

EXEMPLOS
       Example 1, Criando um Protocolo
           O comando a seguir cria um protocolo com o nome http-1 com
           segurança ativada:

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                   11 de fevereiro de 2011         create-protocol(1)
