create-protocol(1)    asadminユーティリティのサブコマンド   create-protocol(1)

名前
       create-protocol - 新しいプロトコルを追加します

概要
           create-protocol [--help]
           [--securityenabled={false|true}]
           [--target target]
           protocol-name

説明
       create-protocolサブコマンドは、プロトコルを作成します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --securityenabled
           trueに設定した場合、プロトコルはSSLを実行します。SSL2またはSSL3のON/OFFを切り替え、ssl要素を使用して暗号化方式を設定することができます。サーバー・インスタンスで証明書を使用できるようにすることにより、セキュリティ設定でSSLをグローバルに有効または無効にします。デフォルト値はfalseです。

       --target
           指定したターゲットにのみプロトコルを作成します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスにプロトコルを作成します。これがデフォルト値です。

           configuration-name
               指定した構成のプロトコルを作成します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスにプロトコルを作成します。

           standalone-instance-name
               指定したスタンドアロン・サーバー・インスタンスにプロトコルを作成します。

オペランド
       protocol-name
           プロトコルの名前。

例
       例 1, プロトコルの作成
           次のコマンドは、セキュリティ保護を有効にしたhttp-1という名前のプロトコルを作成します。

               asadmin> create-protocol --securityenabled=true http-1
               Command create-protocol executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       delete-protocol(1), list-protocols(1), create-network-listener(1)

       asadmin(1M)

Java EE 7                        2011年2月11日              create-protocol(1)
