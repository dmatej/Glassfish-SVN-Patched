delete-network-listener(Comandi secondari della utildelete-network-listener(1)

NOME
       delete-network-listener - rimuove un listener di rete

SINTASSI
           delete-network-listener [--help]
           [--target target]
           listener-name

DESCRIZIONE
       Il comando secondario delete-network-listener rimuove il listener di
       rete specificato. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina il listener di rete solo dalla destinazione specificata. Di
           seguito sono riportati i valori validi.

           server
               Elimina il listener di rete dall'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Elimina il listener di rete dalla configurazione specificata.

           cluster-name
               Elimina il listener di rete da tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Elimina il listener di rete dall'istanza server standalone
               specificata.

OPERANDI
       listener-name
           Il nome del listener di rete da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un listener di rete
           Il comando seguente elimina il listener di rete denominato
           sampleListener:

               asadmin> delete-network-listener sampleListener
               Command delete-network-listener executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-network-listener(1), list-network-listeners(1)

       asadmin(1M)

Java EE 7                       8 ottobre 2010      delete-network-listener(1)
