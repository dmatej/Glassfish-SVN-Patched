delete-http-redirect(1)     asadmin 实用程序子命令     delete-http-redirect(1)

名称
       delete-http-redirect - 删除 HTTP 重定向

提要
           delete-http-redirect [--help]
           [--target target]
           protocol-name

说明
       delete-http-redirect 子命令删除指定的 HTTP
       重定向。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除 HTTP 重定向。有效值如下：

           server
               从默认服务器实例中删除 HTTP 重定向。这是默认值。

           configuration-name
               从指定配置中删除 HTTP 重定向。

           cluster-name
               从指定集群的所有服务器实例中删除 HTTP 重定向。

           standalone-instance-name
               从指定的独立服务器实例中删除 HTTP 重定向。

操作数
       protocol-name
           关联协议的名称。

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-http-redirect(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 8 日       delete-http-redirect(1)
