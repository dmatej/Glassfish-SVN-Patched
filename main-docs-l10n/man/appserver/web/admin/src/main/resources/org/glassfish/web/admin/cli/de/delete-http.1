delete-http(1)          Unterbefehle des asadmin-Utili          delete-http(1)

NAME
       delete-http - entfernt HTTP-Parameter aus einem Protokoll

ZUSAMMENFASSUNG
           delete-http [--help]
           [--target target]
           protocol-name

BESCHREIBUNG
       Der Unterbefehl delete-http entfernt den angegebenen HTTP-Parametersatz
       aus einem Protokoll. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den HTTP-Parametersatz nur aus dem angegebenen Ziel.
           Folgende Werte sind gültig:

           server
               Löscht den HTTP-Parametersatz aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht den HTTP-Parametersatz aus der angegebenen
               Konfiguration.

           cluster-name
               Löscht den HTTP-Parametersatz aus allen Serverinstanzen im
               angegebenen Cluster.

           standalone-instance-name
               Löscht den HTTP-Parametersatz aus der angegebenen
               Standalone-Serverinstanz.

OPERANDEN
       protocol-name
           Der Name des Protokolls, aus dem der HTTP-Parametersatz gelöscht
           werden soll.

BEISPIELE
       Beispiel 1, HTTP-Parametersatz löschen
           Der folgende Befehl löscht den HTTP-Parametersatz aus einem
           Protokoll mit dem Namen http-1:

               asadmin> delete-http http-1
               Command delete-http executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-http(1)

       asadmin(1M)

Java EE 7                        08. Okt. 2010                  delete-http(1)
