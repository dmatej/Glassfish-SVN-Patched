delete-http-listener(1)     asadmin 实用程序子命令     delete-http-listener(1)

名称
       delete-http-listener - 删除网络监听程序

提要
           delete-http-listener [--help]
           [--target target]
           listener-id

说明
       delete-http-listener 子命令删除指定的网络监听程序。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除网络监听程序。有效值如下：

           server
               从默认服务器实例中删除网络监听程序。这是默认值。

           configuration-name
               从指定配置中删除网络监听程序。

           cluster-name
               从指定集群的所有服务器实例中删除网络监听程序。

           standalone-instance-name
               从指定的独立服务器实例中删除网络监听程序。

操作数
       listener-id
           要删除的网络监听程序的唯一标识符。

示例
       Example 1, 使用 delete-http-listener 子命令
           以下命令删除名为 sampleListener 的网络监听程序：

               asadmin> delete-http-listener sampleListener
               Command delete-http-listener executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-http-listener(1), list-http-listeners(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-http-listener(1)
