list-protocols(1)     asadminユーティリティのサブコマンド    list-protocols(1)

名前
       list-protocols - 既存のプロトコルをリストします

概要
           list-protocols [--help]
           [target]

説明
       list-protocolsサブコマンドは、既存のプロトコルをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           リストする内容を、指定したターゲットのプロトコルに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのプロトコルをリストします。この値がデフォルト値です。

           configuration-name
               指定した構成のプロトコルをリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのプロトコルをリストします。

           instance-name
               指定したサーバー・インスタンスのプロトコルをリストします。

例
       例 1, プロトコルのリスト
           次のコマンドは、サーバー・インスタンスのすべてのプロトコルをリストします。

               asadmin> list-protocols
               admin-listener
               http-1
               http-listener-1
               http-listener-2
               Command list-protocols executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-protocol(1), delete-protocol(1)

       asadmin(1M)

Java EE 7                        2010年10月8日               list-protocols(1)
