rollback-transaction(1asadminユーティリティのサブコマンrollback-transaction(1)

名前
       rollback-transaction - 特定のトランザクションをロール・バックします

概要
           rollback-transaction [--help] [--target target]
            transaction_id

説明
       rollback-transactionサブコマンドは、特定のトランザクションをロール・バックします。

       トランザクションをロール・バックする前に、次の操作を行う必要があります。

        1. setサブコマンドを使用してモニターを有効にします。例:

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

           2. freeze-transaction-serviceサブコマンドを使用して、処理中のトランザクションを停止します。

           3. getサブコマンドに--monitorオプションを指定して、アクティブなトランザクションを探します。例:

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションには、トランザクションをロール・バックするターゲットを指定します。有効な値は、server、その他のクラスタ化されたインスタンス、またはその他のスタンドアロン・インスタンスです。

オペランド
       transaction_id
           ロール・バックされるトランザクションの識別子。

例
       例 1, rollback-transactionコマンドの使用

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server管

       Java EE 6チ

Java EE 7                       2010年11月17日         rollback-transaction(1)
