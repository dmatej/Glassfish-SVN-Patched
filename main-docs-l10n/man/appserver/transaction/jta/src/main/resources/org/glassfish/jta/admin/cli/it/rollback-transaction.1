rollback-transaction(1) Comandi secondari della utilit rollback-transaction(1)

NOME
       rollback-transaction - esegue il rollback della transazione specificata

SINTASSI
           rollback-transaction [--help] [--target target]
            transaction_id

DESCRIZIONE
       Il comando secondario rollback-transaction esegue il rollback della
       transazione specificata.

       Prima di eseguire il rollback di una transazione, è necessario
       effettuare le operazioni riportate di seguito.

        1. Abilitare il monitoraggio mediante il comando secondario set. Ad
           esempio:

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

        2. Utilizzare il comando secondario freeze-transaction-service per
           interrompere le transazioni in corso di elaborazione.

        3. Ricercare le transazioni attive utilizzando il comando secondario
           get con l'opzione --monitor. Ad esempio:

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica la destinazione in cui eseguire il
           rollback delle transazioni. I valori validi sono server o qualsiasi
           altra istanza standalone o in cluster.

OPERANDI
       transaction_id
           L'identificativo della transazione di cui eseguire il rollback.

ESEMPI
       Esempio 1, Uso del comando rollback-transaction

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       "Administering Transactions" in Oracle GlassFish Server Administration
       Guide

       "Transactions" in The Java EE 6 Tutorial

Java EE 7                      17 novembre 2010        rollback-transaction(1)
