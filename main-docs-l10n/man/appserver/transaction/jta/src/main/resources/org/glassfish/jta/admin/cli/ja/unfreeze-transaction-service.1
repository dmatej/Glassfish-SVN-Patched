unfreeze-transaction-sasadminユ

名前
       unfreeze-transaction-service -
       一時停止中のすべてのトランザクションを再開します

概要
           unfreeze-transaction-service [--help] [--target target]

説明
       unfreeze-transaction-serviceサブコマンドは、トランザクション・サブシステムを再起動し、処理中のすべてのトランザクションを再開します。このサブコマンドは、すでに凍結しているトランザクション・サブシステムに対して呼び出します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションには、トランザクション・サービスの凍結を解除するターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverに対するトランザクション・サービスの凍結を解除します(デフォルト値)。

           configuration_name
               名前付き構成を使用するすべてのサーバー・インスタンスに対するトランザクション・サービスの凍結を解除します。

           cluster_name
               クラスタ内の各サーバー・インスタンスに対するトランザクション・サービスの凍結を解除します。

           instance_name
               特定のサーバー・インスタンスに対するトランザクション・サービスの凍結を解除します。

例
       例 1, unfreeze-transaction-serviceの使用

               % asadmin unfreeze-transaction-service
               Command unfreeze-transaction-service executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       freeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server管

       Java EE 6チ

Java EE 7                       2010年11月17日 unfreeze-transaction-service(1)
