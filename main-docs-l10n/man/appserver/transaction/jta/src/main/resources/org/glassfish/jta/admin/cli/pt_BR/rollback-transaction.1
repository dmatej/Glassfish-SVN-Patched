rollback-transaction(1) Subcomandos do utilitário asad rollback-transaction(1)

NOME
       rollback-transaction - reverte a transação nomeada

SINOPSE
           rollback-transaction [--help] [--target target]
            transaction_id

DESCRIçãO
       O subcomando rollback-transaction reverte a transação nomeada.

       Antes de reverter a transação, você deve fazer o seguinte:

        1. Ativar o monitoramento utilizando o subcomando set. Por exemplo:

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

        2. Use o subcomando freeze-transaction-service para interromper as
           transações em processo.

        3. Procurar as transações ativas utilizando o subcomando get com a
           opção --monitor. Por exemplo:

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Essa opção especifica o alvo no qual você está revertendo as
           transações. Os valores válidos são server ou qualquer outra
           instância standalone ou clusterizada.

OPERANDOS
       transaction_id
           Identificador para a transação a ser revertida.

EXEMPLOS
       Example 1, Utilizando o comando reverter transação

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       "Administering Transactions" no Oracle GlassFish Server Administration
       Guide

       "Transactions" no The Java EE 6 Tutorial

Java EE 7                   17 de novembro de 2010     rollback-transaction(1)
