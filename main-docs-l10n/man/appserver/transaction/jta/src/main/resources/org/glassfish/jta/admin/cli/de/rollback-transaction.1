rollback-transaction(1) Unterbefehle des asadmin-Utili rollback-transaction(1)

NAME
       rollback-transaction - führt ein Rollback der benannten Transaktion aus

ZUSAMMENFASSUNG
           rollback-transaction [--help] [--target target]
            transaction_id

BESCHREIBUNG
       Der Unterbefehl rollback-transaction führt ein Rollback der benannten
       Transaktion durch.

       Bevor Sie ein Rollback einer Transaktion durchführen können, müssen Sie
       Folgendes tun:

        1. Aktivieren Sie mithilfe des Unterbefehls set die Überwachung.
           Beispiel:

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

        2. Verwenden Sie den Unterbefehl freeze-transaction-service, um
           laufende Transaktionen anzuhalten.

        3. Suchen Sie nach aktiven Transaktionen, indem Sie den Unterbefehl
           get mit der Option --monitor verwenden. Beispiel:

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Diese Option gibt das Ziel an, auf dem Sie ein Rollback der
           Transaktionen durchführen. Gültige Werte sind server oder eine
           beliebige andere Cluster- oder Standalone-Instanz.

OPERANDEN
       transaction_id
           ID für die Transaktion, für die ein Rollback durchgeführt werden
           soll.

BEISPIELE
       Example 1, Befehl "rollback-transaction" verwenden

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       "Administering Transactions" im Oracle GlassFish Server Administration
       Guide

       "Transactions" im The Java EE 6 Tutorial

Java EE 7                        17. Nov. 2010         rollback-transaction(1)
