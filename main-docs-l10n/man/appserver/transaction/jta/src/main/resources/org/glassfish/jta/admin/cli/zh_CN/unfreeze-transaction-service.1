unfreeze-transaction-serviceasadmin 实用程序子命

名称
       unfreeze-transaction-service - 恢复所有挂起的事务处理

提要
           unfreeze-transaction-service [--help] [--target target]

说明
       unfreeze-transaction-service
       子命令重新启动事务处理子系统，并恢复所有进行中事务处理。在已冻结的事务处理子系统上调用该子命令。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定取消冻结事务处理服务的目标。有效值为：

           server
               为默认服务器实例 server 取消冻结事务处理服务，且为默认值。

           configuration_name
               取消冻结使用命名配置的所有服务器实例的事务处理服务。

           cluster_name
               为集群中的每个服务器实例取消冻结事务处理服务。

           instance_name
               为特定的服务器实例取消冻结事务处理服务。

示例
       Example 1, 使用 unfreeze-transaction-service

               % asadmin unfreeze-transaction-service
               Command unfreeze-transaction-service executed successfully

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       freeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering
       Transactions

       The Java EE 6 Tutorial 中的 Transactions

Java EE 7                     2010 年 11 月 17 unfreeze-transaction-service(1)
