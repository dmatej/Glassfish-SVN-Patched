freeze-transaction-service(1asadmin 实用程序子命令

名称
       freeze-transaction-service - 冻结事务处理子系统

提要
           freeze-transaction-service [--help] [--target target]

说明
       freeze-transaction-service
       子命令冻结事务处理子系统，以防止事务处理管理器启动、完成所有进行中事务处理或者更改所有进行中事务处理的状态。请在回退任何进行中事务处理之前调用此命令。对已经冻结的事务处理子系统调用此子命令不会有任何作用。重新启动服务器会取消冻结事务处理子系统。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项指定要冻结事务处理服务的目标。有效值为：

           server
               冻结默认服务器实例 server 的事务处理服务，这是默认值。

           configuration_name
               冻结使用命名配置的所有服务器实例的事务处理服务。

           cluster_name
               冻结集群中每个服务器实例的事务处理服务。

           instance_name
               冻结特定服务器实例的事务处理服务。

示例
       Example 1, 使用 freeze-transaction-service

               % asadmin freeze-transaction-service
               Command freeze-transaction-service executed successfully

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       unfreeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering
       Transactions

       The Java EE 6 Tutorial 中的 Transactions

Java EE 7                     2010 年 11 月 17 日freeze-transaction-service(1)
