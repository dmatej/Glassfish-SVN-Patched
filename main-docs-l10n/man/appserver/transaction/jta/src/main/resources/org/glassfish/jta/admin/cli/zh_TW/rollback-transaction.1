rollback-transaction(1)     asadmin 公用程式子命令     rollback-transaction(1)

參考名
       rollback-transaction - 倒回已命名的作業事件

概要
           rollback-transaction [--help] [--target target]
            transaction_id

描述
       rollback-transaction 子命令會倒回已命名的作業事件。

       您必須先執行下列作業，才能倒回作業事件：

        1. 使用 set 子命令啟用監視功能。例如︰

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

        2. 使用 freeze-transaction-service 子命令以停止處理中的作業事件。

        3. 使用 get 子命令和 --monitor 選項查找使用中的作業事件。例如︰

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項可指定要倒回作業事件的目標。有效的值為 server
           或任何其他叢集執行處理或獨立執行處理。

運算元
       transaction_id
           要倒回之作業事件的識別碼。

範例
       範例 1, 使用 rollback-transaction 命令

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide 中的 Administering
       Transactions

       Java EE 6 教

Java EE 7                     2010 年 11 月 17 日      rollback-transaction(1)
