freeze-transaction-serviSubcomandos do utilitáriofreeze-transaction-service(1)

NOME
       freeze-transaction-service - freezes the transaction subsystem

SINOPSE
           freeze-transaction-service [--help] [--target target]

DESCRIçãO
       O subcomando freeze-transaction-service congela o subsistema de
       transação, prevenindo que o gerenciador de transação seja iniciado, ou
       altere o estado de todas as transações instantâneas. Chame esse comando
       antes de reverter alguma transação em curso. Chamar esse subcomando em
       um subsistema de transações já congelado não possui efeito. Reiniciar o
       servidor descongela os subsistema de transação. Esse subcomando só é
       suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Esta opção especifica o alvo no qual o serviço de transações será
           congelado. Os valores válidos são:

           server
               Congela o serviço de transações para a instância do servidor
               default server e é o valor default.

           configuration_name
               Congela o serviço de transação para todas as instâncias do
               servidor que usam a configuração nomeada.

           cluster_name
               Congela o serviço de transações para cada instância do servidor
               em um cluster.

           instance_name
               Congela o serviço de transações para uma instância do servidor
               específica.

EXEMPLOS
       Example 1, Utilizando o subcomando freeze-transaction-service

               % asadmin freeze-transaction-service
               Command freeze-transaction-service executed successfully

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       unfreeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       "Administering Transactions" no Oracle GlassFish Server Administration
       Guide

       "Transactions" no The Java EE 6 Tutorial

Java EE 7                   17 de novembro de 201freeze-transaction-service(1)
