freeze-transaction-serviSous-commandes de l'utilifreeze-transaction-service(1)

NOM
       freeze-transaction-service - fige le sous-système de transactions

SYNTAXE
           freeze-transaction-service [--help] [--target target]

DESCRIPTION
       La sous-commande freeze-transaction-service fige le sous-système de
       transactions, ce qui empêche le gestionnaire de transactions de
       démarrer, de terminer ou de modifier l'état de toutes les transactions
       en cours d'exécution. Appelez cette commande avant d'annuler (rollback)
       une transaction en cours d'exécution. L'appel de cette sous-commande
       sur un sous-système de transactions déjà figé n'a pas d'effet. Le
       redémarrage du serveur libère le sous-système de transactions. Cette
       sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible sur laquelle figer le service de
           transactions. Les valeurs valides sont les suivantes :

           server
               Fige le service de transactions pour l'instance de serveur par
               défaut (server). Il s'agit de la valeur par défaut.

           configuration_name
               Fige le service de transactions pour toutes les instances de
               serveur qui utilisent la configuration nommée.

           cluster_name
               Fige le service de transactions pour chaque instance de serveur
               du cluster.

           instance_name
               Fige le service de transactions pour une instance de serveur
               donnée.

EXEMPLES
       Exemple 1, Utilisation de la sous-commande freeze-transaction-service

               % asadmin freeze-transaction-service
               Command freeze-transaction-service executed successfully

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       unfreeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Administering Transactions dans le manuel Oracle GlassFish Server
       Administration Guide

       Transactions dans le manuel The Java EE 6 Tutorial

Java EE 7                         17 nov 2010    freeze-transaction-service(1)
