unfreeze-transaction-serviasadmin 유틸리티 하위unfreeze-transaction-service(1)

제목
       unfreeze-transaction-service - 일시 중지된 모든 트랜잭션 재개

시놉시스
           unfreeze-transaction-service [--help] [--target target]

설명
       unfreeze-transaction-service 하위 명령은 트랜잭션 하위 시스템을
       재시작하고 실행 중인 모든 트랜잭션을 재개합니다. 이미 중지된 트랜잭션
       하위 시스템에서 이 하위 명령을 호출합니다. 이 하위 명령은 원격
       모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 트랜잭션 서비스의 중지를 해제할 대상을 지정합니다. 적합한
           값은 다음과 같습니다.

           server
               기본 서버 인스턴스 server에 대한 트랜잭션 서비스의 중지를
               해제하며 기본값입니다.

           configuration_name
               명명된 구성을 사용하는 모든 서버 인스턴스에 대한 트랜잭션
               서비스의 중지를 해제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에 대한 트랜잭션 서비스의
               중지를 해제합니다.

           instance_name
               특정 서버 인스턴스에 대한 트랜잭션 서비스의 중지를 해제합니다.

예제
       예 1, unfreeze-transaction-service 사용

               % asadmin unfreeze-transaction-service
               Command unfreeze-transaction-service executed successfully

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       freeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide의 "Administering
       Transactions"

       The Java EE 6 Tutorial의 "Transactions"

Java EE 7                      2010년 11월 17일unfreeze-transaction-service(1)
