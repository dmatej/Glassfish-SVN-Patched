rollback-transaction(1)   asadmin 유틸리티 하위 명령   rollback-transaction(1)

제목
       rollback-transaction - 명명된 트랜잭션 롤백

시놉시스
           rollback-transaction [--help] [--target target]
            transaction_id

설명
       rollback-transaction 하위 명령은 명명된 트랜잭션을 롤백합니다.

       트랜잭션을 롤백하려면 다음 작업을 수행해야 합니다.

        1. set 하위 명령으로 모니터링을 사용으로 설정합니다. 예를 들면 다음과
           같습니다.

               asadmin> set clstr1-config.monitoring-service.module-monitoring-levels.transaction-service=HIGH

        2. freeze-transaction-service 하위 명령으로 진행 중인 트랜잭션을
           중지합니다.

        3. --monitor 옵션이 지정된 get 하위 명령을 사용하여 활성 트랜잭션을
           조회합니다. 예를 들면 다음과 같습니다.

               asadmin> get --monitor inst1.server.transaction-service.activeids-current

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 옵션은 트랜잭션을 롤백하는 대상을 지정합니다. 적합한 값은 server
           또는 기타 클러스터된 인스턴스나 독립형 인스턴스입니다.

피연산자
       transaction_id
           롤백할 트랜잭션의 식별자입니다.

예제
       예 1, rollback-transaction 명령 사용

               % asadmin rollback-transaction 0000000000000001_00
               Command rollback-transaction executed succeessfully

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       freeze-transaction-service(1), unfreeze-transaction-service(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server Administration Guide의 "Administering
       Transactions"

       The Java EE 6 Tutorial의 "Transactions"

Java EE 7                      2010년 11월 17일        rollback-transaction(1)
