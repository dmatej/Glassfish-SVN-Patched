freeze-transaction-serasadminユーティリティのサブfreeze-transaction-service(1)

名前
       freeze-transaction-service - トランザクション・サブシステムを凍結します

概要
           freeze-transaction-service [--help] [--target target]

説明
       freeze-transaction-serviceサブコマンドはトランザクション・サブシステムを凍結して、トランザクション・マネージャが処理中のすべてのトランザクションの開始、完了、または状態変更をできないようにします。このコマンドは、処理中のトランザクションをロール・バックする前に呼び出します。すでに凍結しているトランザクション・サブシステムに対してこのサブコマンドを呼び出しても、効果はありません。サーバーを再起動すると、トランザクション・サブシステムの凍結が解除されます。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           このオプションでは、トランザクション・サービスを凍結するターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverのトランザクション・サービスを凍結します。これはデフォルト値です。

           configuration_name
               名前付き構成を使用するすべてのサーバー・インスタンスに対するトランザクション・サービスを凍結します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスのトランザクション・サービスを凍結します。

           instance_name
               特定のサーバー・インスタンスのトランザクション・サービスを凍結します。

例
       例 1, freeze-transaction-serviceの使用

               % asadmin freeze-transaction-service
               Command freeze-transaction-service executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       unfreeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Oracle GlassFish Server管

       Java EE 6チ

Java EE 7                       2010年11月17日   freeze-transaction-service(1)
