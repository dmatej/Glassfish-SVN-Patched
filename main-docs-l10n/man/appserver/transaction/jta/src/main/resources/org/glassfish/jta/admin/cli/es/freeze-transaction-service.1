freeze-transaction-serviSubcomandos de la Utilidafreeze-transaction-service(1)

NOMBRE DE REFERENCIA
       freeze-transaction-service - Congela el subsistema de transacciones.

SINOPSIS
           freeze-transaction-service [--help] [--target target]

DESCRIPCIÓN
       El subcomando freeze-transaction-service congela el subsistema de
       transacciones, lo que impide que el gestor de transacciones inicie,
       termine o cambie el estado de ninguna transacción en ejecución. Llame a
       este comando antes de realizar rollback de cualquier transacción en
       marcha. La llamada a este subcomando en un subsistema de transacciones
       ya congelado no tiene efecto alguno. Al reiniciar el servidor se libera
       el subsistema de transacciones. Este subcomando sólo está soportado en
       modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Esta opción especifica el destino en el que se va a detener el
           servicio de transacción. Los valores válidos son:

           server
               Se congelará el servicio de transacciones para la instancia de
               servidor por defecto server y es el valor por defecto.

           configuration_name
               Congela el servicio de transacciones para todas las instancias
               del servidor que utilizan la configuración con nombre.

           cluster_name
               Se congelará el servicio de transacciones para todas las
               instancias de servidor del cluster.

           instance_name
               Se congelará el servicio de transacciones para una determinada
               instancia de servidor.

EJEMPLOS
       Ejemplo 1, Uso de freeze-transaction-service

               % asadmin freeze-transaction-service
               Command freeze-transaction-service executed successfully

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       unfreeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Sección sobre administración de transacciones en Oracle GlassFish
       Server Administration Guide

       Sección sobre transacciones en el tutorial de Java EE 6

Java EE 7                   17 de noviembre de 20freeze-transaction-service(1)
