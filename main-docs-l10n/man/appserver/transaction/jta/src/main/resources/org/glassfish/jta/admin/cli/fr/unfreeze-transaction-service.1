unfreeze-transaction-serSous-commandes de l'utiunfreeze-transaction-service(1)

NOM
       unfreeze-transaction-service - reprend toutes les transactions
       suspendues

SYNTAXE
           unfreeze-transaction-service [--help] [--target target]

DESCRIPTION
       La sous-commande unfreeze-transaction-service redémarre le sous-système
       de transactions et reprend toutes les transactions en cours
       d'exécution. Appelez cette sous-commande sur un sous-système de
       transactions déjà figé. Cette sous-commande est prise en charge en mode
       distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible sur laquelle libérer le service de
           transactions. Les valeurs valides sont les suivantes :

           server
               Libère le service de transactions pour l'instance de serveur
               par défaut (server). Il s'agit de la valeur par défaut.

           configuration_name
               Libère le service de transactions pour toutes les instances de
               serveur qui utilisent la configuration nommée.

           cluster_name
               Libère le service de transactions pour chaque instance de
               serveur du cluster.

           instance_name
               Libère le service de transactions pour une instance de serveur
               donnée.

EXEMPLES
       Exemple 1, Utilisation de la sous-commande unfreeze-transaction-service

               % asadmin unfreeze-transaction-service
               Command unfreeze-transaction-service executed successfully

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       freeze-transaction-service(1), rollback-transaction(1),
       recover-transactions(1)

       asadmin(1M)

       Administering Transactions dans le manuel Oracle GlassFish Server
       Administration Guide

       Transactions dans le manuel The Java EE 6 Tutorial

Java EE 7                         17 nov 2010  unfreeze-transaction-service(1)
