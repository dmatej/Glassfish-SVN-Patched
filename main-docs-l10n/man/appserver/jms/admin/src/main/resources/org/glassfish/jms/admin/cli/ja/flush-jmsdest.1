flush-jmsdest(1)      asadminユーティリティのサブコマンド     flush-jmsdest(1)

名前
       flush-jmsdest - JMS宛先内のメッセージを削除します。

概要
           flush-jmsdest [--help]
           --desttype {topic|queue}
           [--target target]
           destname

説明
       flush-jmsdestサブコマンドは、サーバーのJava Message
       Service(JMS)構成の物理宛先からメッセージを削除します。

       このサブコマンドは、リモート・モードでのみサポートされています。リモートのasadminサブコマンドを実行するには、ドメイン管理サーバー(DAS)が稼働している必要があります。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --desttype
           このオプションでは、メッセージを削除する物理宛先のタイプを指定します。サポートされている宛先のタイプは、topicとqueueです。

       --target
           指定されたターゲットのみに対するメッセージを物理宛先から削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスに対するメッセージを物理宛先から削除します。これがデフォルト値です。

           configuration-name
               メッセージを指定された構成の物理宛先から削除します。

           cluster-name
               指定されたクラスタ内のすべてのサーバー・インスタンスに対するメッセージを物理宛先から削除します。

           instance-name
               指定されたサーバー・インスタンスに対するメッセージを物理宛先から削除します。

オペランド
       dest_name
           削除されるJMS宛先の一意の識別子。

例
       例 1, 物理宛先からのメッセージの削除
           次のサブコマンドは、PhysicalQueueという名前のキューからメッセージを削除します。

               asadmin> flush-jmsdest --desttype queue PhysicalQueue
               Command flush-jmsdest executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jmsdest(1), list-jmsdest(1), create-jmsdest(1)

       asadmin(1M)

Java EE 7                        2011年2月11日                flush-jmsdest(1)
