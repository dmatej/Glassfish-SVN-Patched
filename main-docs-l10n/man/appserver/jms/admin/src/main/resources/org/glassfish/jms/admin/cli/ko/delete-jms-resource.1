delete-jms-resource(1)    asadmin 유틸리티 하위 명령    delete-jms-resource(1)

제목
       delete-jms-resource - JMS 리소스 제거

시놉시스
           delete-jms-resource [--help]
           [--target target]
           jndi_name

설명
       delete-jms-resource 하위 명령은 지정된 JMS(Java Message Service)
       리소스를 제거합니다. 이 하위 명령을 실행하기 전에 이 리소스에 대한 모든
       참조를 제거해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에서만 JMS 리소스를 삭제합니다. 적합한 값은 다음과
           같습니다.

               참고
               ┌────────────────────────────────────────┐
               │주 리소스는 항상 도메인에 대해 전체로서 │
               │생성되지만 리소스를 생성할 때 --target  │
               │옵션을 사용하여 <resource-ref>를 생성한 │
               │대상에 대해서만 활성화됩니다. 즉,       │
               │리소스를 삭제하면 지정된 --target에     │
               │대한 <resource-ref> 요소만 삭제되고     │
               │domain이 삭제에 대한 --target으로       │
               │지정되지 않는 한 도메인에서 리소스가    │
               │전체로서 삭제되지 않습니다.             │
               └────────────────────────────────────────┘

           server
               기본 서버 인스턴스에서 JMS 리소스를 삭제합니다. 이 값은
               기본값입니다.

           domain
               도메인에서 JMS 리소스를 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 JMS 리소스를
               삭제합니다.

           instance-name
               지정된 서버 인스턴스에서 JMS 리소스를 삭제합니다.

피연산자
       jndi_name
           삭제할 JMS 리소스의 JNDI 이름입니다.

예제
       예 1, JMS 대상 리소스 삭제
           다음 하위 명령은 jms/MyQueue라는 JMS 대상 리소스를 삭제합니다.

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일         delete-jms-resource(1)
