list-jms-hosts(1)       Subcomandos do utilitário asad       list-jms-hosts(1)

NOME
       list-jms-hosts - lista os hosts JMS existentes

SINOPSE
           list-jms-hosts [--help]
           [--target target]

DESCRIçãO
       O subcomando list-jms-hosts lista os hosts Java Message Service (JMS)
       existentes para o serviço JMS.

       Esse subcomando só é suportado no modo remoto. Os subcomandos asadmin
       remotos requerem que um servidor de administração de domínios (DAS)
       esteja em execução.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Restringe a listagem aos host JMS para um alvo especificado. Os
           valores válidos são os seguintes:

           server
               Lista os hosts JMS para a instância do servidor default. Este é
               o valor default.

           configuration-name
               Lista os hosts JMS para a configuração especificada.

           cluster-name
               Lista aos host JMS para todas as instâncias de servidor no
               cluster especificado.

           instance-name
               Lista os hosts JMS para a instância do servidor especificada.

EXEMPLOS
       Example 1, Listando todos os hosts JMS
           O subcomando a seguir lista os hosts para o serviço JMS.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                         22 out 2010                list-jms-hosts(1)
