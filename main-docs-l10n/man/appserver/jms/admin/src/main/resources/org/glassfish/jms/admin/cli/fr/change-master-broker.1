change-master-broker(1) Sous-commandes de l'utilitaire change-master-broker(1)

NOM
       change-master-broker - modifie le broker maître dans un cluster Message
       Queue fournissant des services JMS pour un cluster GlassFish Server

SYNTAXE
           change-master-broker [--help]
           clustered-instance-name

DESCRIPTION
       La sous-commande change-master-broker modifie le broker maître dans un
       cluster Message Queue qui remplit le rôle de fournisseur JMS pour un
       cluster GlassFish Server. Par défaut, le broker maître est celui
       associé à la première instance configurée dans le cluster GlassFish
       Server.

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       clustered-instance-name
           Nom de l'instance de serveur dont le broker Message Queue doit
           devenir le broker maître du cluster Message Queue. Cette instance
           de serveur doit être une instance dans un cluster GlassFish Server.

EXEMPLES
       Exemple 1, Modification du broker maître
           La sous-commande suivante remplace le broker maître Message Queue
           par celui de l'instance incluse dans un cluster clustinst3.

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       asadmin(1M)

Java EE 7                         11 fév 2011          change-master-broker(1)
