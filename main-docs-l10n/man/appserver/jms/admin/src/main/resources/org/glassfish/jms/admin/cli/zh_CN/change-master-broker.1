change-master-broker(1)     asadmin 实用程序子命令     change-master-broker(1)

名称
       change-master-broker - 更改消息队列集群 (此集群为 GlassFish Server
       集群提供 JMS 服务) 中的主中介。

提要
           change-master-broker [--help]
           clustered-instance-name

说明
       change-master-broker 子命令可更改消息队列集群 (此集群是 GlassFish
       Server 集群的 JMS 提供方) 中的主中介。默认情况下，主中介与 GlassFish
       Server 集群中配置的第一个实例相关联。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       clustered-instance-name
           服务器实例的名称，该服务器实例的消息队列中介将成为消息队列集群的主中介。此服务器实例必须是
           GlassFish Server 集群中的实例。

示例
       Example 1, 更改主中介
           以下子命令将消息队列主中介更改为 clustinst3 集群实例的主中介。

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       change-master-broker(1)
