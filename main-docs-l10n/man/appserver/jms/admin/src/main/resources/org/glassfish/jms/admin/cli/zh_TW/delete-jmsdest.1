delete-jmsdest(1)           asadmin 公用程式子命令           delete-jmsdest(1)

參考名
       delete-jmsdest - 移除 JMS 實體目的地

概要
           delete-jmsdest [--help]
           --desttype type
           [--target target]
           dest_name

描述
       delete-jmsdest 子命令會移除指定的 Java 訊息服務 (JMS) 實體目的地。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --desttype
           JMS 目的地的類型。有效值為 topic 和 queue。

       --target
           僅從指定目標中刪除實體目的地。雖然 delete-jmsdest
           子命令與資源相關，但仍會使用 JMS 服務 (JMS 代理程式)
           刪除實體目的地，此為組態的一部分。JMS 代理程式組態於 domain.xml
           的組態區段。有效的值如下：

           server
               從預設伺服器執行處理刪除實體目的地。此為預設值。

           configuration-name
               從指定組態中刪除實體目的地。

           cluster-name
               從指定叢集的每個伺服器執行處理中刪除實體目的地。

           instance-name
               從指定的伺服器執行處理中建立實體目的地。

運算元
       dest_name
           要刪除的 JMS 目的地的唯一識別碼。

範例
       範例 1, 刪除實體目的地
           下列子命令會刪除名為 PhysicalQueue 的佇列。

               asadmin> delete-jmsdest --desttype queue PhysicalQueue
               Command delete-jmsdest executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jmsdest(1), list-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日             delete-jmsdest(1)
