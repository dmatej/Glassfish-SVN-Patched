delete-jms-resource(1)      asadmin 实用程序子命令      delete-jms-resource(1)

名称
       delete-jms-resource - 删除 JMS 资源

提要
           delete-jms-resource [--help]
           [--target target]
           jndi_name

说明
       delete-jms-resource 子命令删除指定的 Java 消息服务 (JMS)
       资源。在执行此子命令之前，确保删除对此资源的所有引用。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除 JMS 资源。有效值如下：

               注意
               ┌─────────────────────────────────────────────────┐
               │注                                               │
               │始终为整个域创建资源，但资源仅在创建资源时使用   │
               │--target 选项为其创建 <resource-ref>             │
               │的目标上处于活动状态。这意味着删除资源仅删除指定 │
               │--target 中的 <resource-ref>                     │
               │元素，并不会从整个域中删除资源，除非将           │
               │domain 指定为要删除的 --target。                 │
               └─────────────────────────────────────────────────┘

           server
               从默认服务器实例中删除 JMS 资源。这是默认值。

           domain
               从域删除 JMS 资源。

           cluster-name
               从指定集群中的每个服务器实例中删除 JMS 资源。

           instance-name
               从指定服务器实例中删除 JMS 资源。

操作数
       jndi_name
           要删除的 JMS 资源的 JNDI 名称。

示例
       Example 1, 删除 JMS 目标资源
           以下子命令删除名为 jms/MyQueue 的 JMS 目标资源。

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日        delete-jms-resource(1)
