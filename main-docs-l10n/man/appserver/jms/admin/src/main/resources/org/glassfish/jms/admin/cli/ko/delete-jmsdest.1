delete-jmsdest(1)         asadmin 유틸리티 하위 명령         delete-jmsdest(1)

제목
       delete-jmsdest - JMS 물리적 대상 제거

시놉시스
           delete-jmsdest [--help]
           --desttype type
           [--target target]
           dest_name

설명
       delete-jmsdest 하위 명령은 지정된 JMS(Java Message Service) 물리적
       대상을 제거합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --desttype
           JMS 대상의 유형입니다. 적합한 값은 topic 및 queue입니다.

       --target
           지정된 대상에서만 물리적 대상을 삭제합니다.  delete-jmsdest 하위
           명령이 리소스와 연관되어 있기는 하지만 물리적 대상은 구성의 일부인
           JMS Service(JMS Broker)를 사용하여 삭제합니다. JMS Broker는
           domain.xml의 config 섹션에 구성되어 있습니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에서 물리적 대상을 삭제합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에서 물리적 대상을 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 물리적 대상을
               삭제합니다.

           instance-name
               지정된 서버 인스턴스에서 물리적 대상을 생성합니다.

피연산자
       dest_name
           삭제할 JMS 대상의 고유 식별자입니다.

예제
       예 1, 물리적 대상 삭제
           다음 하위 명령은 PhysicalQueue라는 대기열을 삭제합니다.

               asadmin> delete-jmsdest --desttype queue PhysicalQueue
               Command delete-jmsdest executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jmsdest(1), list-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일              delete-jmsdest(1)
