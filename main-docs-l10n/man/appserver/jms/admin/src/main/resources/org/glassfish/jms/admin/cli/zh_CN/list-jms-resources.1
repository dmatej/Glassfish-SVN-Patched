list-jms-resources(1)       asadmin 实用程序子命令       list-jms-resources(1)

名称
       list-jms-resources - 列出 JMS 资源

提要
           list-jms-resources [--help]
           [--restype type]
           [target]

说明
       list-jms-resources 子命令列出现有 Java 消息服务 (JMS) 资源
       (目标资源和连接工厂资源)。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --restype
           JMS 资源类型可以是
           javax.jms.Topic、javax.jms.Queue、javax.jms.ConnectionFactory、javax.jms.TopicConnectionFactory
           或 javax.jms.QueueConnectionFactory。

操作数
       target
           将列出的内容限制为指定目标的资源。有效值如下：

           server
               列出默认服务器实例的资源。这是默认值。

           domain
               列出域的资源。

           cluster-name
               列出指定集群中每个服务器实例的资源。

           instance-name
               列出指定服务器实例的资源。

示例
       Example 1, 列出所有 JMS 资源
           以下子命令可列出所有 JMS 资源。

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       Example 2, 列出指定类型的 JMS 资源
           以下子命令可列出所有 javax.jms.ConnectionFactory 资源。

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日         list-jms-resources(1)
