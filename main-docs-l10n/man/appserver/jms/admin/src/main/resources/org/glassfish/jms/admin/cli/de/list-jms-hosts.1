list-jms-hosts(1)       Unterbefehle des asadmin-Utili       list-jms-hosts(1)

NAME
       list-jms-hosts - listet die vorhandenen JMS-Hosts auf

ZUSAMMENFASSUNG
           list-jms-hosts [--help]
           [--target target]

BESCHREIBUNG
       Der Unterbefehl list-jms-hosts listet die vorhandenen Java Message
       Service-(JMS-)Hosts für den JMS-Service auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Beschränkt die Auflistung auf JMS-Hosts für ein angegebenes Ziel.
           Folgende Werte sind gültig:

           server
               Listet die JMS-Hosts für die standardmäßige Serverinstanz auf.
               Dies ist der Standardwert.

           configuration-name
               Listet die JMS-Hosts für die angegebene Konfiguration auf.

           cluster-name
               Listet die JMS-Hosts für alle Serverinstanzen im angegebenen
               Cluster auf.

           instance-name
               Listet die JMS-Hosts für die angegebene Serverinstanz auf.

BEISPIELE
       Beispiel 1, Alle JMS-Hosts auflisten
           Der folgende Unterbefehl listet die JMS-Hosts für den JMS-Service
           auf.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                        22. Okt. 2010               list-jms-hosts(1)
