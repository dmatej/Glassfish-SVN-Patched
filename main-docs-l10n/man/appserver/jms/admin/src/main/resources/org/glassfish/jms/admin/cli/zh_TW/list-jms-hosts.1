list-jms-hosts(1)           asadmin 公用程式子命令           list-jms-hosts(1)

參考名
       list-jms-hosts - 列出現有的 JMS 主機

概要
           list-jms-hosts [--help]
           [--target target]

描述
       list-jms-hosts 子命令會列出 JMS 服務現有的 Java Message Service (JMS)
       主機。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           將清單限制為指定目標的 JMS 主機。有效的值如下：

           server
               列出預設伺服器執行處理的 JMS 主機。此為預設值。

           configuration-name
               列出指定組態的 JMS 主機。

           cluster-name
               列出指定叢集中所有伺服器執行處理的 JMS 主機。

           instance-name
               列出指定伺服器執行處理的 JMS 主機。

範例
       範例 1, 列出所有 JMS 主機
           下列子命令會列出 JMS 服務的 JMS 主機。

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 22 日            list-jms-hosts(1)
