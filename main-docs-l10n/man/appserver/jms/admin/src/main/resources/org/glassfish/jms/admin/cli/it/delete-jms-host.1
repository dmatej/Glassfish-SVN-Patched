delete-jms-host(1)      Comandi secondari della utilit      delete-jms-host(1)

NOME
       delete-jms-host - rimuove un host JMS

SINTASSI
           delete-jms-host [--help]
           [--target target]
           jms_host_name

DESCRIZIONE
       Il comando secondario delete-jms-host rimuove l'host JMS (Java Message
       Service) specificato.

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

       Non è consigliabile eliminare l'host JMS predefinito, denominato
       default_JMS_host.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina l'host JMS solo dalla destinazione specificata. Di seguito
           sono riportati i valori validi.

           server
               Elimina l'host JMS dall'istanza server predefinita. Questo è il
               valore predefinito.

           configuration-name
               Elimina l'host JMS dalla configurazione specificata.

           cluster-name
               Elimina l'host JMS da tutte le istanze server del cluster
               specificato.

           instance-name
               Elimina l'host JMS dall'istanza server specificata.

OPERANDI
       jms_host_name
           Il nome dell'host da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un host JMS
           Il comando secondario seguente elimina l'host JMS denominato
           MyNewHost.

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                       22 ottobre 2010             delete-jms-host(1)
