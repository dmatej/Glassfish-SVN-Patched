list-jms-hosts(1)       Sous-commandes de l'utilitaire       list-jms-hosts(1)

NOM
       list-jms-hosts - répertorie les hôtes JMS existants

SYNTAXE
           list-jms-hosts [--help]
           [--target target]

DESCRIPTION
       La sous-commande list-jms-hosts répertorie les hôtes JMS (Java Message
       Service) existants pour le service JMS.

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Restreint la liste aux hôtes JMS pour une cible spécifiée. Les
           valeurs valides sont les suivantes :

           server
               Répertorie les hôtes JMS pour l'instance de serveur par défaut.
               Il s'agit de la valeur par défaut.

           configuration-name
               Répertorie les hôtes JMS pour la configuration spécifiée.

           cluster-name
               Répertorie les hôtes JMS pour toutes les instances de serveur
               dans le cluster spécifié.

           instance-name
               Répertorie les hôtes JMS pour l'instance de serveur spécifiée.

EXEMPLES
       Exemple 1, Liste de tous les hôtes JMS
           La sous-commande suivante répertorie les hôtes JMS pour le service
           JMS.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                        22 oct. 2010                list-jms-hosts(1)
