delete-jmsdest(1)           asadmin 实用程序子命令           delete-jmsdest(1)

名称
       delete-jmsdest - 删除 JMS 物理目标

提要
           delete-jmsdest [--help]
           --desttype type
           [--target target]
           dest_name

说明
       delete-jmsdest 子命令删除指定的 Java 消息服务 (JMS) 物理目标。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --desttype
           JMS 目标类型。有效值有 topic 和 queue。

       --target
           仅从指定目标中删除物理目标。虽然 delete-jmsdest
           子命令与资源相关，但可使用配置中的 JMS 服务 (JMS 中介)
           删除物理目标。JMS 中介是在 domain.xml 的 config
           部分中配置的。有效值如下：

           server
               从默认服务器实例删除物理目标。这是默认值。

           configuration-name
               从指定配置中删除物理目标。

           cluster-name
               从指定集群中的每个服务器实例上删除物理目标。

           instance-name
               从指定服务器实例中删除物理目标。

操作数
       dest_name
           要删除的 JMS 目标的唯一标识符。

示例
       Example 1, 删除物理目标
           以下子命令删除名为 PhysicalQueue 的队列。

               asadmin> delete-jmsdest --desttype queue PhysicalQueue
               Command delete-jmsdest executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jmsdest(1), list-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日             delete-jmsdest(1)
