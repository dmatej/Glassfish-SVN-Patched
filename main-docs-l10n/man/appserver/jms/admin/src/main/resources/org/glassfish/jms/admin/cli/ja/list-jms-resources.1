list-jms-resources(1) asadminユーティリティのサブコマンドlist-jms-resources(1)

名前
       list-jms-resources - JMSリソースをリストします

概要
           list-jms-resources [--help]
           [--restype type]
           [target]

説明
       list-jms-resourcesサブコマンドは、既存のJava Message Service
       (JMS)リソース(宛先リソースおよび接続ファクトリ・リソース)をリストします。

       このサブコマンドは、リモート・モードでのみサポートされています。リモートのasadminサブコマンドを実行するには、ドメイン管理サーバー(DAS)が稼働している必要があります。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --restype
           JMSリソース・タイプは、javax.jms.Topic、javax.jms.Queue、javax.jms.ConnectionFactory、javax.jms.TopicConnectionFactory、またはjavax.jms.QueueConnectionFactoryです。

オペランド
       target
           リストする内容を、指定したターゲットのリソースに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのリソースをリストします。この値がデフォルト値です。

           domain
               ドメインのリソースをリストします。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのリソースをリストします。

           instance-name
               指定したサーバー・インスタンスのリソースをリストします。

例
       例 1, すべてのJMSリソースのリスト
           次のサブコマンドは、すべてのJMSリソースをリストします。

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       例 2, 指定したタイプのJMSリソースのリスト
           次のサブコマンドは、すべてのjavax.jms.ConnectionFactoryリソースをリストします。

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                        2011年2月11日           list-jms-resources(1)
