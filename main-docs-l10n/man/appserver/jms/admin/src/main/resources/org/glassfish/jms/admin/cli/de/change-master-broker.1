change-master-broker(1) Unterbefehle des asadmin-Utili change-master-broker(1)

NAME
       change-master-broker - Ändert den Master-Broker in einem Message
       Queue-Cluster mit JMS-Services für ein GlassFish Server-Cluster.

ZUSAMMENFASSUNG
           change-master-broker [--help]
           clustered-instance-name

BESCHREIBUNG
       Der Unterbefehl change-master-broker ändert den Master-Broker in einem
       Message Queue-Cluster, das der JMS-Provider für ein GlassFish
       Server-Cluster ist. Der Master-Broker ist derjenige, der standardmäßig
       mit der ersten im GlassFish Server-Cluster konfigurierten Instanz
       verknüpft ist.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       clustered-instance-name
           Der Name der Serverinstanz, deren Message Queue-Broker zum
           Master-Broker des Message Queue-Clusters wird. Diese Serverinstanz
           muss eine Instanz in einem GlassFish Server-Cluster sein.

BEISPIELE
       Example 1, Master-Broker ändern
           Der folgende Unterbefehl ändert den Message Queue Master-Broker in
           den für die geclusterte clustinst3-Instanz.

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       asadmin(1M)

Java EE 7                        11. Feb. 2011         change-master-broker(1)
