list-jms-resources(1)   Comandi secondari della utilit   list-jms-resources(1)

NOME
       list-jms-resources - elenca le risorse JMS

SINTASSI
           list-jms-resources [--help]
           [--restype type]
           [target]

DESCRIZIONE
       Il comando secondario list-jms-resources elenca le risorse JMS (Java
       Message Service) esistenti (risorse di destinazione e di connection
       factory).

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --restype
           Il tipo di risorsa JMS può essere javax.jms.Topic, javax.jms.Queue,
           javax.jms.ConnectionFactory, javax.jms.TopicConnectionFactory o
           javax.jms.QueueConnectionFactory.

OPERANDI
       target
           Limita la lista alle risorse per la destinazione specificata. Di
           seguito sono riportati i valori validi.

           server
               Elenca le risorse per l'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Elenca le risorse per il dominio.

           cluster-name
               Elenca le risorse di tutte le istanze server del cluster
               specificato.

           instance-name
               Elenca le risorse per l'istanza server specificata.

ESEMPI
       Esempio 1, Visualizzazione della lista di tutte le risorse JMS
           Il comando secondario seguente elenca tutte le risorse JMS.

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       Esempio 2, Visualizzazione della lista delle risorse JMS del tipo
       specificato
           Il comando secondario seguente elenca tutte le risorse
           javax.jms.ConnectionFactory.

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011          list-jms-resources(1)
