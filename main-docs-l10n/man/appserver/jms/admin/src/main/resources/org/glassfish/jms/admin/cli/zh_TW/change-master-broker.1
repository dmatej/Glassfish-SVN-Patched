change-master-broker(1)     asadmin 公用程式子命令     change-master-broker(1)

參考名
       change-master-broker - 變更提供 JMS 服務給 GlassFish Server
       叢集之訊息佇列叢集中的主代理程式。

概要
           change-master-broker [--help]
           clustered-instance-name

描述
       change-master-broker 子命令會變更提供 JMS 服務給 GlassFish Server
       叢集之訊息佇列叢集中的主代理程式。根據預設，主代理程式會與 GlassFish
       Server 叢集中設定的第一個執行處理相關聯。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       clustered-instance-name
           其 Message Queue 代理程式將成為 Message Queue
           叢集主代理程式的伺服器執行處理名稱。此伺服器執行處理必須是
           GlassFish Server 叢集中的執行處理。

範例
       範例 1, 變更主代理程式
           以下子命令會將 Message Queue 主代理程式變更為 clustinst3
           叢集執行處理的主代理程式。

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       change-master-broker(1)
