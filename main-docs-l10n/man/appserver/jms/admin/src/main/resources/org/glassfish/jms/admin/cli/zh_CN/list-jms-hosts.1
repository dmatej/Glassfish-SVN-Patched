list-jms-hosts(1)           asadmin 实用程序子命令           list-jms-hosts(1)

名称
       list-jms-hosts - 列出现有的 JMS 主机

提要
           list-jms-hosts [--help]
           [--target target]

说明
       list-jms-hosts 子命令列出 JMS 服务的现有 Java 消息服务 (JMS) 主机。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           将列出的内容限制为指定目标的 JMS 主机。有效值如下：

           server
               列出默认服务器实例的 JMS 主机。这是默认值。

           configuration-name
               列出指定配置的 JMS 主机。

           cluster-name
               列出指定集群中所有服务器实例的 JMS 主机。

           instance-name
               列出指定服务器实例的 JMS 主机。

示例
       Example 1, 列出所有 JMS 主机
           以下子命令可列出 JMS 服务的 JMS 主机。

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 22 日            list-jms-hosts(1)
