configure-jms-cluster(1)Subcomandos do utilitário asadconfigure-jms-cluster(1)

NOME
       configure-jms-cluster - configura o cluster do Message Queue,
       fornecendo serviços JMS a um cluster do GlassFish Server

SINOPSE
           configure-jms-cluster [--help]
           [--clustertype={conventional|enhanced}]
           [--configstoretype={masterbroker|shareddb}]
           [--messagestoretype={file|jdbc}]
           [--dbvendor database-vendor]
           [--dbuser database-user]
           [--dburl database-url]
           [--property (name=value)[:name=value]*]
           cluster-name

DESCRIçãO
       configure-jms-cluster configura o cluster do Message Queue, fornecendo
       serviços JMS a um cluster do GlassFish Server.

       Este subcomando deveria ser usado antes que o cluster do GlassFish
       Server seja iniciado pela primeira vez. Caso contrário, siga as
       instruções descritas em "Administrando o JMS (Java Message Service)" no
       Oracle GlassFish Server Administration Guide.

       Esse subcomando só é suportado no modo remoto. Os subcomandos asadmin
       remotos requerem que um servidor de administração de domínios (DAS)
       esteja em execução.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --clustertype
           O tipo de cluster do Message Queue a ser configurado. O valor
            conventional especifica um cluster convencional e o valor enhanced
           especifica um cluster aprimorado e de alta disponibilidade. Para
           obter informações sobre esses tipos de cluster de clusters do
           Message Queue, consulte "Broker Clusters" no Oracle GlassFish
           Server Message Queue Technical Overview.

           O valor default é conventional.

           Se enhanced for especificado, as opções configstorestype e
           messagestoretype serão ignoradas.

       --configstoretype
           O tipo de armazenamento de dados para os dados de configuração em
           um cluster convencional. O valor masterbroker especifica o uso de
           um broker mestre para armazenar e gerenciar os dados da
           configuração. O valor shareddb especifica o uso de um banco de
           dados compartilhado para armazenar os dados de configuração.

           O valor default é masterbroker.

           Esta opção é ignorada se clustertype for definido como enhanced.

       --messagestoretype
           O tipo de armazenamento de dados para os dados de mensagens em um
           cluster convencional. O valor file especifica um armazenamento de
           arquivos. O valor jdbc especifica um armazenamento de JDBC.

           O valor default é file.

           Esta opção é ignorada se clustertype for definido como enhanced.

       --dbvendor, --dbuser, --dburl
           O fornecedor do banco de dados, usuário e URL de acesso do banco de
           dados JDBC a ser usado em qualquer uma dessas situações:

           ·   Quando clustertype for definido como enhanced

           ·   Quando configstoretype for definido como shareddb

           ·   Quando messagestoretype for definido como jdbc

           Para obter informações sobre fornecedores e os formatos dos urls de
           acesso suportados para cada fornecedor, consulte JDBC-Based
           Persistence no Oracle GlassFish Server Message Queue Administration
           Guide.

               Nota
               ┌───────────────────────────────────────┐
               │Observação Para especificar a senha do │
               │usuário do banco de dados JDBC, use a  │
               │opção --passwordfile do utilitário do  │
               │comando asadmin(1M) após adicionar a   │
               │entrada AAS_ADMIN_JMSDBPASSWORD ao     │
               │arquivo de senha.                      │
               └───────────────────────────────────────┘

       --property
           Uma lista de propriedades específicas do fornecedor de banco de
           banco de dados para configurar o banco de dados JDBC para uso pelo
           cluster do Message Queue. Especifique propriedades como uma lista
           separada por dois-pontos (:) de nomes e valores de propriedades no
           formato:

               prop1name=prop1value:prop2name=prop2value

OPERANDOS
       cluster-name
           O nome do cluster do GlassFish Server para o qual o cluster do
           Message Queue cluster fornecerá serviços do JMS.

           Com o GlassFish Server usa o nome do cluster para identificar
           exclusivamente tabelas banco de dados pode exigir o cluster de fila
           de mensagem, o tamanho do nome está restrito nas seguintes
           situações:

           ·   Se clustertype for definido como enhanced, o nome não pode ser
               mais longo que n–21 caracteres, em que n é o tamanho máximo do
               nome da tabela do banco de dados permitido pelo banco de dados.

           ·   Se configstoretype for definido como shareddb, o nome não pode
               ser mais longo do que n–21 caracteres, em que n é o tamanho
               máximo do nome da tabela do banco de dados permitido pelo banco
               de dados.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       asadmin(1M)

Java EE 7                   11 de fevereiro de 2011   configure-jms-cluster(1)
