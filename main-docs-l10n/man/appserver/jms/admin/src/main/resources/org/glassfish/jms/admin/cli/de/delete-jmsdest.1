delete-jmsdest(1)       Unterbefehle des asadmin-Utili       delete-jmsdest(1)

NAME
       delete-jmsdest - entfernt ein physisches JMS-Ziel

ZUSAMMENFASSUNG
           delete-jmsdest [--help]
           --desttype type
           [--target target]
           dest_name

BESCHREIBUNG
       Der Unterbefehl delete-jmsdest entfernt ein physisches JMS-(Java
       Message Service-)Ziel.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --desttype
           Der Typ des JMS-Ziels. Gültige Werte sind topic und queue.

       --target
           Löscht das physische Ziel nur aus dem angegebenen Ziel. Obwohl der
           Unterbefehl delete-jmsdest auf Ressourcen bezogen ist, wird
           mithilfe des JMS-Service (JMS-Broker), der Teil der Konfiguration
           ist, ein physisches Ziel gelöscht. Ein JMS-Broker wird im
           Konfigurationsabschnitt von
            domain.xml konfiguriert. Folgende Werte sind gültig:

           server
               Löscht das physische Ziel aus der standardmäßigen
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht das physische Ziel aus der angegebenen Konfiguration.

           cluster-name
               Löscht das physische Ziel aus jeder Serverinstanz im
               angegebenen Cluster.

           instance-name
               Erstellt das physische Ziel aus der angegebenen Serverinstanz.

OPERANDEN
       dest_name
           Die eindeutige ID für das JMS-Ziel, das gelöscht werden soll.

BEISPIELE
       Beispiel 1, Physische Ziele löschen
           Der folgende Unterbefehl löscht die Queue namens PhysicalQueue.

               asadmin> delete-jmsdest --desttype queue PhysicalQueue
               Command delete-jmsdest executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jmsdest(1), list-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011               delete-jmsdest(1)
