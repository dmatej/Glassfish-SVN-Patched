flush-jmsdest(1)            asadmin 公用程式子命令            flush-jmsdest(1)

參考名
       flush-jmsdest - 清除 JMS 目的地中的訊息。

概要
           flush-jmsdest [--help]
           --desttype {topic|queue}
           [--target target]
           destname

描述
       flush-jmsdest 子命令會清除伺服器的 Java Message Service (JMS)
       組態之實體目的地中的訊息。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --desttype
           此選項指出您要清除訊息之實體目的地的類型。支援的目的地類型為 topic
           及 queue。

       --target
           只清除指定目標之實體目的地中的訊息。有效的值如下：

           server
               清除預設伺服器執行處理之實體目的地中的訊息。此為預設值。

           configuration-name
               清除指定組態之實體目的地中的訊息。

           cluster-name
               清除指定叢集內每個伺服器執行處理之實體目的地中的訊息。

           instance-name
               清除指定伺服器執行處理之實體目的地中的訊息。

運算元
       dest_name
           要清除之 JMS 目的地的唯一識別碼。

範例
       範例 1, 從實體目的地清除訊息
           下列子命令會從一個名為 PhysicalQueue 的佇列清除訊息。

               asadmin> flush-jmsdest --desttype queue PhysicalQueue
               Command flush-jmsdest executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jmsdest(1), list-jmsdest(1), create-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日              flush-jmsdest(1)
