create-jms-host(1)      Unterbefehle des asadmin-Utili      create-jms-host(1)

NAME
       create-jms-host - erstellt einen JMS-Host

ZUSAMMENFASSUNG
           create-jms-host [--help]
           --mqhost mq-host --mqport mq-port
           --mquser mq-user --mqpassword mq-password
           [--target target]
           [--force={false|true}]
           jms_host_name

BESCHREIBUNG
       Erstellt einen Java Message Service-(JMS-)Host innerhalb des
       JMS-Service.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --mqhost
           Der Hostname für den JMS-Service.

       --mqport
           Die vom JMS-Service verwendete Portnummer.

       --mquser
           Der Benutzername für den JMS-Service.

       --mqpassword
           Das Kennwort für den JMS-Service.

       --target
           Erstellt den JMS-Host nur für das angegebene Ziel. Folgende Werte
           sind gültig:

           server
               Erstellt den JMS-Host für die standardmäßige Serverinstanz.
               Dies ist der Standardwert.

           configuration-name
               Erstellt den JMS-Host in der angegebenen Konfiguration.

           cluster-name
               Erstellt den JMS-Host für jede Serverinstanz im angegebenen
               Cluster.

           instance-name
               Erstellt den JMS-Host für die angegebene Serverinstanz.

       --force
           Legt fest, ob der Unterbefehl den vorhandenen gleichnamigen
           JMS-Host überschreibt. Der Standardwert ist false.

OPERANDEN
       jms_host_name
           Eine eindeutige ID für den JMS-Host, der erstellt werden soll.

BEISPIELE
       Beispiel 1, JMS-Hosts mit einem nicht standardmäßigen Port erstellen
           Der folgende Befehl erstellt einen JMS-Host namens MyNewHost auf
           dem System pigeon.

               asadmin> create-jms-host --mqhost pigeon.example.com --mqport 7677
               --mquser admin --mqpassword admin MyNewHost
               Jms Host MyNewHost created.
               Command create-jms-host executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       list-jms-hosts(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                        12. Feb. 2013              create-jms-host(1)
