create-jms-host(1)        asadmin 유틸리티 하위 명령        create-jms-host(1)

제목
       create-jms-host - JMS 호스트 생성

시놉시스
           create-jms-host [--help]
           --mqhost mq-host --mqport mq-port
           --mquser mq-user --mqpassword mq-password
           [--target target]
           [--force={false|true}]
           jms_host_name

설명
       JMS 서비스 내에서 JMS(Java Message Service) 호스트를 생성합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --mqhost
           JMS 서비스에 대한 호스트 이름입니다.

       --mqport
           JMS 서비스에서 사용하는 포트 번호입니다.

       --mquser
           JMS 서비스에 대한 사용자 이름입니다.

       --mqpassword
           JMS 서비스에 대한 비밀번호입니다.

       --target
           지정된 대상에 대해서만 JMS 호스트를 생성합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에 대한 JMS 호스트를 생성합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에 JMS 호스트를 생성합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에 대한 JMS 호스트를
               생성합니다.

           instance-name
               지정된 서버 인스턴스에 대한 JMS 호스트를 생성합니다.

       --force
           하위 명령이 동일한 이름의 기존 JMS 호스트를 겹쳐쓰는지 여부를
           지정합니다. 기본값은 false입니다.

피연산자
       jms_host_name
           생성할 JMS 호스트의 고유 식별자입니다.

예제
       예 1, 비기본 포트를 사용하여 JMS 호스트 생성
           다음 명령은 시스템 pigeon에 MyNewHost라는 JMS 호스트를 생성합니다.

               asadmin> create-jms-host --mqhost pigeon.example.com --mqport 7677
               --mquser admin --mqpassword admin MyNewHost
               Jms Host MyNewHost created.
               Command create-jms-host executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       list-jms-hosts(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                       2013년 2월 12일             create-jms-host(1)
