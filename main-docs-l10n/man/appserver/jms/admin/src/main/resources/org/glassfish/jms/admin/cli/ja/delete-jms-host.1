delete-jms-host(1)    asadminユーティリティのサブコマンド   delete-jms-host(1)

名前
       delete-jms-host - JMSホストを除去します

概要
           delete-jms-host [--help]
           [--target target]
           jms_host_name

説明
       delete-jms-hostサブコマンドは、指定したJava Message
       Service(JMS)ホストを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。リモートのasadminサブコマンドを実行するには、ドメイン管理サーバー(DAS)が稼働している必要があります。

       default_JMS_hostという名前のデフォルトJMSホストを削除するのはお薦めできません。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           指定したターゲットからのみJMSホストを削除します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスからJMSホストを削除します。これがデフォルト値です。

           configuration-name
               指定した構成からJMSホストを削除します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスからJMSホストを削除します。

           instance-name
               指定したサーバー・インスタンスからJMSホストを削除します。

オペランド
       jms_host_name
           削除するホストの名前。

例
       例 1, JMSホストの削除
           次のサブコマンドは、MyNewHostという名前のJMSホストを削除します。

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                       2010年10月22日              delete-jms-host(1)
