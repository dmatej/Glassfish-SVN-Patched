change-master-broker(1)   asadmin 유틸리티 하위 명령   change-master-broker(1)

제목
       change-master-broker - GlassFish Server 클러스터에 JMS 서비스를
       제공하는 메시지 대기열 클러스터의 마스터 브로커 변경

시놉시스
           change-master-broker [--help]
           clustered-instance-name

설명
       change-master-broker 하위 명령은 GlassFish Server 클러스터에 대한 JMS
       제공자인 메시지 대기열 클러스터의 마스터 브로커를 변경합니다.
       기본적으로 마스터 브로커는 GlassFish Server 클러스터에서 구성된 첫번째
       인스턴스와 연관되어 있습니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       clustered-instance-name
           메시지 대기열 브로커가 메시지 대기열 클러스터의 마스터 브로커가 될
           서버 인스턴스의 이름입니다. 이 서버 인스턴스는 GlassFish Server
           클러스터에 있는 인스턴스여야 합니다.

예제
       예 1, 마스터 브로커 변경
           다음 하위 명령은 메시지 대기열 마스터 브로커를 clustinst3
           클러스터된 인스턴스에 대한 메시지 대기열 마스터 브로커로
           변경합니다.

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       asadmin(1M)

Java EE 7                       2011년 2월 11일        change-master-broker(1)
