list-jms-resources(1)   Sous-commandes de l'utilitaire   list-jms-resources(1)

NOM
       list-jms-resources - répertorie les ressources JMS

SYNTAXE
           list-jms-resources [--help]
           [--restype type]
           [target]

DESCRIPTION
       La sous-commande list-jms-resources répertorie les ressources JMS (Java
       Message Service) (ressources de destination et de fabrique de
       connexions).

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --restype
           Le type de ressource JMS peut être javax.jms.Topic,
           javax.jms.Queue, javax.jms.ConnectionFactory,
           javax.jms.TopicConnectionFactory ou
           javax.jms.QueueConnectionFactory.

OPÉRANDES
       target
           Restreint la liste aux ressources pour une cible spécifiée. Les
           valeurs valides sont les suivantes :

           server
               Répertorie les ressources pour l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           domain
               Répertorie les ressources pour le domaine.

           cluster-name
               Répertorie les ressources pour chaque instance de serveur dans
               le cluster spécifié.

           instance-name
               Répertorie les ressources pour l'instance de serveur spécifiée.

EXEMPLES
       Exemple 1, Liste de toutes les ressources JMS
           La sous-commande suivante répertorie toutes les ressources JMS.

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       Exemple 2, Liste des ressources JMS d'un type spécifié
           La sous-commande suivante répertorie toutes les ressources
           javax.jms.ConnectionFactory.

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                         11 fév 2011            list-jms-resources(1)
