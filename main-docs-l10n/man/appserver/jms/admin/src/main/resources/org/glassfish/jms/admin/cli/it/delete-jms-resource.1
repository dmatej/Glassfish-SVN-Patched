delete-jms-resource(1)  Comandi secondari della utilit  delete-jms-resource(1)

NOME
       delete-jms-resource - rimuove una risorsa JMS

SINTASSI
           delete-jms-resource [--help]
           [--target target]
           jndi_name

DESCRIZIONE
       Il comando secondario delete-jms-resource rimuove la risorsa JMS (Java
       Message Service) specificata. Prima di eseguire questo comando
       secondario, verificare di aver rimosso tutti i riferimenti alla
       risorsa.

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina la risorsa JMS solo dalla destinazione specificata. Di
           seguito sono riportati i valori validi.

               Nota
               ┌────────────────────────────────────────┐
               │Nota Le risorse vengono sempre create   │
               │per l'intero dominio, ma sono attive    │
               │solo per le destinazioni per le quali è │
               │stato creato un elemento <resource-ref> │
               │utilizzando l'opzione --target al       │
               │momento della creazione della risorsa.  │
               │Ciò significa che l'eliminazione di una │
               │risorsa elimina solo l'elemento         │
               │<resource-ref> per l'opzione --target   │
               │specificata, ma non la risorsa dal      │
               │dominio completo a meno che domain      │
               │venga specificato come --target per     │
               │l'eliminazione.                         │
               └────────────────────────────────────────┘

           server
               Elimina la risorsa JMS dall'istanza server predefinita. Questo
               è il valore predefinito.

           domain
               Elimina la risorsa JMS dal dominio.

           cluster-name
               Elimina la risorsa JMS da tutte le istanze server del cluster
               specificato.

           instance-name
               Elimina la risorsa JMS dall'istanza server specificata.

OPERANDI
       jndi_name
           Il nome JNDI della risorsa JMS da eliminare.

ESEMPI
       Esempio 1, Eliminazione di una risorsa di destinazione JMS
           Il comando secondario seguente elimina la risorsa di destinazione
           JMS denominata jms/MyQueue.

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011         delete-jms-resource(1)
