list-jmsdest(1)             asadmin 实用程序子命令             list-jmsdest(1)

名称
       list-jmsdest - 列出现有的 JMS 物理目标

提要
           list-jmsdest [--help]
           [--desttype type]
           [target]

说明
       list-jmsdest 子命令列出 Java 消息服务 (JMS) 物理目标。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --desttype
           要列出的 JMS 目标类型。有效值有 topic 和 queue。

操作数
       target
           将列出的内容限制为指定目标的物理目标。有效值如下：

           server
               列出默认服务器实例的物理目标。这是默认值。

           configuration-name
               列出指定配置中的物理目标。

           cluster-name
               列出指定集群中每个服务器实例的物理目标。

           instance-name
               列出指定服务器实例的物理目标。

示例
       Example 1, 列出所有物理目标
           以下子命令可列出所有物理目标。

               asadmin> list-jmsdest
               PhysicalQueue
               PhysicalTopic
               Command list-jmsdest executed successfully.

       Example 2, 列出指定类型的所有物理目标
           以下子命令可列出所有物理主题。

               asadmin> list-jmsdest --desttype topic
               PhysicalTopic
               Command list-jmsdest executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jmsdest(1), delete-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日               list-jmsdest(1)
