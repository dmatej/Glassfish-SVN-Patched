flush-jmsdest(1)        Unterbefehle des asadmin-Utili        flush-jmsdest(1)

NAME
       flush-jmsdest - löscht Meldungen in einem JMS-Ziel.

ZUSAMMENFASSUNG
           flush-jmsdest [--help]
           --desttype {topic|queue}
           [--target target]
           destname

BESCHREIBUNG
       Der Unterbefehl flush-jmsdest löscht Meldungen aus einem physischen
       Ziel in der Java Message Service-(JMS-)Konfiguration des Servers.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --desttype
           Diese Option gibt den Typ des physischen Ziels an, aus dem Sie
           Meldungen löschen möchten. Unterstützte Zieltypen sind topic und
           queue.

       --target
           Löscht Meldungen aus dem physischen Ziel nur für das angegebene
           Ziel. Folgende Werte sind gültig:

           server
               Löscht Meldungen aus dem physischen Ziel für die standardmäßige
               Serverinstanz. Dies ist der Standardwert.

           configuration-name
               Löscht Meldungen aus dem physischen Ziel in der angegebenen
               Konfiguration.

           cluster-name
               Löscht Meldungen aus dem physischen Ziel für jede Serverinstanz
               im angegebenen Cluster.

           instance-name
               Löscht Meldungen aus dem physischen Ziel für die angegebene
               Serverinstanz.

OPERANDEN
       dest_name
           Die eindeutige ID für das JMS-Ziel, das bereinigt werden soll.

BEISPIELE
       Example 1, Meldungen aus einem physischen Ziel löschen
           Der folgende Unterbefehl löscht Meldungen aus der Queue namens
           PhysicalQueue.

               asadmin> flush-jmsdest --desttype queue PhysicalQueue
               Command flush-jmsdest executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jmsdest(1), list-jmsdest(1), create-jmsdest(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011                flush-jmsdest(1)
