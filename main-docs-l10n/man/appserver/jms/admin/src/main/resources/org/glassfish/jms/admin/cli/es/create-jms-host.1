create-jms-host(1)      Subcomandos de la Utilidad asa      create-jms-host(1)

NOMBRE DE REFERENCIA
       create-jms-host - Crea un host JMS.

SINOPSIS
           create-jms-host [--help]
           --mqhost mq-host --mqport mq-port
           --mquser mq-user --mqpassword mq-password
           [--target target]
           [--force[={false|true}]]
           jms_host_name

DESCRIPCIÓN
       Crea un host de Java Message Service (JMS) dentro del servicio JMS.

       Este subcomando sólo está soportado en modo remoto. Los subcomandos
       asadmin remotos necesitan un servidor de administración de dominio
       (DAS) en ejecución.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --mqhost
           El nombre de host para el servicio JMS.

       --mqport
           El número de puerto que utiliza el servicio JMS.

       --mquser
           El nombre de usuario del servicio JMS.

       --mqpassword
           La contraseña para el servicio JMS.

       --target
           Crea el host JMS sólo para el destino especificado. Los valores
           válidos son los siguientes:

           server
               Crea el host JMS para la instancia de servidor por defecto.
               Este es el valor por defecto.

           configuration-name
               Crea el host JMS en la configuración especificada.

           cluster-name
               Crea el host JMS para todas las instancias de servidor del
               cluster especificado.

           instance-name
               Crea el host JMS para la instancia de servidor especificada.

       --force
           Especifica si el subcomando sobrescribe el host JMS existente con
           el mismo nombre. El valor por defecto es false.

OPERANDOS
       jms_host_name
           Un identificador único para el host JMS que se va a crear.

EJEMPLOS
       Ejemplo 1, Creación de un Host JMS con un Puerto Distinto del Puerto
       por Defecto
           El siguiente comando crea un host JMS denominado MyNewHost en el
           sistema pigeon.

               asadmin> create-jms-host --mqhost pigeon.example.com --mqport 7677
               --mquser admin --mqpassword admin MyNewHost
               Jms Host MyNewHost created.
               Command create-jms-host executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       list-jms-hosts(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                    12 de febrero de 2013          create-jms-host(1)
