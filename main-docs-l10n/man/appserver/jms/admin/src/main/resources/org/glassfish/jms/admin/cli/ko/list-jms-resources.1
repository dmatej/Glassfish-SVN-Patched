list-jms-resources(1)     asadmin 유틸리티 하위 명령     list-jms-resources(1)

제목
       list-jms-resources - JMS 리소스 나열

시놉시스
           list-jms-resources [--help]
           [--restype type]
           [target]

설명
       list-jms-resources 하위 명령은 기존 JMS(Java Message Service)
       리소스(대상 및 접속 팩토리 리소스)를 나열합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --restype
           JMS 리소스 유형은 javax.jms.Topic, javax.jms.Queue,
           javax.jms.ConnectionFactory, javax.jms.TopicConnectionFactory 또는
           javax.jms.QueueConnectionFactory일 수 있습니다.

피연산자
       target
           지정된 대상에 대한 리소스만 나열되도록 제한합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스의 리소스를 나열합니다. 이 값은 기본값입니다.

           domain
               도메인의 리소스를 나열합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스의 리소스를
               나열합니다.

           instance-name
               지정된 서버 인스턴스의 리소스를 나열합니다.

예제
       예 1, 모든 JMS 리소스 나열
           다음 하위 명령은 모든 JMS 리소스를 나열합니다.

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       예 2, 특정 유형의 JMS 리소스 나열
           다음 하위 명령은 모든 javax.jms.ConnectionFactory 리소스를
           나열합니다.

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일          list-jms-resources(1)
