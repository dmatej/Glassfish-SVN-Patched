list-jms-resources(1)       asadmin 公用程式子命令       list-jms-resources(1)

參考名
       list-jms-resources - 列出 JMS 資源

概要
           list-jms-resources [--help]
           [--restype type]
           [target]

描述
       list-jms-resources 子命令會列出現有的 Java Message Service (JMS) 資源
       (目的地與連線工廠資源)。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --restype
           JMS 資源類型可為
           javax.jms.Topic、javax.jms.Queue、javax.jms.ConnectionFactory、javax.jms.TopicConnectionFactory
           或 javax.jms.QueueConnectionFactory。

運算元
       target
           將清單限制為指定目標的資源。有效的值如下：

           server
               列出預設伺服器執行處理的資源。此為預設值。

           domain
               列出網域的資源。

           cluster-name
               列出指定叢集中每個伺服器執行處理的資源。

           instance-name
               列出指定伺服器執行處理的資源。

範例
       範例 1, 列出所有 JMS 資源
           下列子命令會列出所有 JMS 資源。

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       範例 2, 列出指定類型的 JMS 資源
           下列子命令會列出所有 javax.jms.ConnectionFactory 資源。

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日         list-jms-resources(1)
