delete-jms-resource(1)  Sous-commandes de l'utilitaire  delete-jms-resource(1)

NOM
       delete-jms-resource - enlève une ressource JMS

SYNTAXE
           delete-jms-resource [--help]
           [--target target]
           jndi_name

DESCRIPTION
       La sous-commande delete-jms-resource enlève la ressource JMS (Java
       Message Service) spécifiée. Veillez à enlever toutes les références à
       cette ressource avant d'exécuter cette sous-commande.

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Supprime la ressource JMS de la cible spécifiée uniquement. Les
           valeurs valides sont les suivantes :

               Note
               ┌───────────────────────────────────────┐
               │Remarque Les ressources sont toujours  │
               │créées pour un domaine dans sa         │
               │totalité, mais elles sont actives      │
               │uniquement pour les cibles pour        │
               │lesquelles un élément <resource-ref> a │
               │été créé à l'aide de l'option --target │
               │lors de la création de la ressource.   │
               │Cela signifie que la suppression d'une │
               │ressource entraîne seulement celle de  │
               │l'élément <resource-ref> pour l'option │
               │--target spécifiée, mais pas celle de  │
               │la ressource du domaine dans sa        │
               │totalité, à moins que la valeur domain │
               │ne soit indiquée pour --target lors de │
               │la suppression.                        │
               └───────────────────────────────────────┘

           server
               Supprime la ressource JMS de l'instance de serveur par défaut.
               Il s'agit de la valeur par défaut.

           domain
               Supprime la ressource JMS du domaine.

           cluster-name
               Supprime la ressource JMS de chaque instance de serveur du
               cluster spécifié.

           instance-name
               Supprime la ressource JMS de l'instance de serveur spécifiée.

OPÉRANDES
       jndi_name
           Nom JNDI de la ressource JMS à supprimer.

EXEMPLES
       Exemple 1, Suppression d'une ressource de destination JMS
           La sous-commande suivante supprime la ressource de destination JMS
           nommée ms/MyQueue.

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                         11 fév 2011           delete-jms-resource(1)
