list-jms-hosts(1)       Comandi secondari della utilit       list-jms-hosts(1)

NOME
       list-jms-hosts - elenca gli host JMS esistenti

SINTASSI
           list-jms-hosts [--help]
           [--target target]

DESCRIZIONE
       Il comando secondario list-jms-hosts elenca gli host JMS (Java Message
       Service) esistenti per il servizio JMS.

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Limita la lista agli host JMS per la destinazione specificata. Di
           seguito sono riportati i valori validi.

           server
               Elenca gli host JMS per l'istanza server predefinita. Questo è
               il valore predefinito.

           configuration-name
               Elenca gli host JMS per la configurazione specificata.

           cluster-name
               Elenca gli host JMS per tutte le istanze server nel cluster
               specificato.

           instance-name
               Elenca gli host JMS per l'istanza server specificata.

ESEMPI
       Esempio 1, Visualizzazione della lista di tutti gli host JMS
           Il comando secondario seguente elenca gli host JMS per il servizio
           JMS.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                       22 ottobre 2010              list-jms-hosts(1)
