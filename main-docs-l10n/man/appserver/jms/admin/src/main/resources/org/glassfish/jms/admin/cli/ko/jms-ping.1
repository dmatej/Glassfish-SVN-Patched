jms-ping(1)               asadmin 유틸리티 하위 명령               jms-ping(1)

제목
       jms-ping - JMS 서비스가 작동 및 실행 중인지 여부 확인

시놉시스
           jms-ping [--help]
           [-- target target]

설명
       jms-ping 하위 명령은 JMS(Java Message Service) 서비스(JMS 제공자라고도
       함)가 작동 및 실행 중인지 확인합니다. GlassFish Server을(를) 시작하면
       JMS 서비스가 기본적으로 시작됩니다.

       jms-ping 하위 명령은 JMS 서비스 내에서 기본 JMS 호스트만 핑합니다.
       내장된 JMS 서비스를 핑할 수 없는 경우 오류 메시지가 표시됩니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           작업을 수행할 대상을 지정합니다. 적합한 값은 다음과 같습니다.

           server
               기본 서버 인스턴스에 대해 JMS 서비스를 핑합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성을 사용하는 모든 클러스터에 대해 JMS 서비스를
               핑합니다.

           cluster-name
               지정된 클러스터에 대해 JMS 서비스를 핑합니다.

           instance-name
               지정된 서버 인스턴스에 대해 JMS 서비스를 핑합니다.

예제
       예 1, JMS 서비스가 실행 중인지 확인
           다음 하위 명령은 JMS 서비스가 기본 서버에서 실행 중인지 여부를
           확인합니다.

               asadmin> jms-ping
               JMS-ping command executed successfully
               Connector resoure test_jms_adapter created.
               Command jms-ping executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jms-host(1), list-jms-hosts(1), delete-jms-host(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일                    jms-ping(1)
