flush-jmsdest(1)            asadmin 实用程序子命令            flush-jmsdest(1)

名称
       flush-jmsdest - 清除 JMS 目标中的消息。

提要
           flush-jmsdest [--help]
           --desttype {topic|queue}
           [--target target]
           destname

说明
       flush-jmsdest 子命令将清除服务器的 Java 消息服务 (JMS)
       配置中物理目标的消息。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --desttype
           此选项指示要从其中清除消息的物理目标的类型。支持的目标类型为 topic
           和 queue。

       --target
           仅清除指定目标的物理目标中的消息。有效值如下：

           server
               清除默认服务器实例的物理目标中的消息。这是默认值。

           configuration-name
               清除指定配置内物理目标中的消息。

           cluster-name
               清除指定集群中每个服务器实例的物理目标中的消息。

           instance-name
               清除指定服务器实例的物理目标中的消息。

操作数
       dest_name
           要清除的 JMS 目标的唯一标识符。

示例
       Example 1, 从物理目标中清除消息
           以下子命令将从名为 PhysicalQueue 的队列中清除消息。

               asadmin> flush-jmsdest --desttype queue PhysicalQueue
               Command flush-jmsdest executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jmsdest(1), list-jmsdest(1), create-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日              flush-jmsdest(1)
