delete-jms-resource(1)  Unterbefehle des asadmin-Utili  delete-jms-resource(1)

NAME
       delete-jms-resource - entfernt eine JMS-Ressource

ZUSAMMENFASSUNG
           delete-jms-resource [--help]
           [--target target]
           jndi_name

BESCHREIBUNG
       Der Unterbefehl delete-jms-resource entfernt die angegebene Java
       Message Service-(JMS-)Ressource. Stellen Sie sicher, dass Sie alle
       Referenzen zu dieser Ressource entfernt haben, bevor Sie diesen
       Unterbefehl ausführen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht die JMS-Ressource nur aus dem angegebenen Ziel. Folgende
           Werte sind gültig:

               Anmerkung
               ┌────────────────────────────────────────┐
               │Hinweis Ressourcen werden stets für     │
               │eine Domain als Ganzes erstellt, sind   │
               │aber nur für Ziele aktiv, für die bei   │
               │der Erstellung der Ressource eine       │
               │<resource-ref> mit der Option --target  │
               │erstellt wurde. Das bedeutet, dass beim │
               │Löschen einer Ressource nur das         │
               │<resource-ref>-Element für das          │
               │angegebene Ziel (--target) und nicht    │
               │die Ressource aus der Domain als Ganzes │
               │gelöscht wird, es sei denn, domain ist  │
               │als --target für den Löschvorgang       │
               │angegeben.                              │
               └────────────────────────────────────────┘

           server
               Löscht die JMS-Ressource aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           domain
               Löscht die JMS-Ressource aus der Domain.

           cluster-name
               Löscht die JMS-Ressource aus jeder Serverinstanz im angegebenen
               Cluster.

           instance-name
               Löscht die JMS-Ressource aus der angegebenen Serverinstanz.

OPERANDEN
       jndi_name
           Der JNDI-Name der JMS-Ressource, die gelöscht werden soll.

BEISPIELE
       Beispiel 1, JMS-Zielressourcen löschen
           Der folgende Unterbefehl löscht die JMS-Zielressource namens
           jms/MyQueue.

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011          delete-jms-resource(1)
