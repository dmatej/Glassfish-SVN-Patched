list-jms-resources(1)   Unterbefehle des asadmin-Utili   list-jms-resources(1)

NAME
       list-jms-resources - listet die JMS-Ressourcen auf

ZUSAMMENFASSUNG
           list-jms-resources [--help]
           [--restype type]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-jms-resources listet die vorhandenen JMS-(Java
       Message Service-)Ressourcen (Ziel- und Connection Factory-Ressourcen)
       auf.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --restype
           Der JMS-Ressourcentyp kann javax.jms.Topic, javax.jms.Queue,
           javax.jms.ConnectionFactory, javax.jms.TopicConnectionFactory oder
           javax.jms.QueueConnectionFactory sein.

OPERANDEN
       target
           Beschränkt die Auflistung auf Ressourcen für ein angegebenes Ziel.
           Folgende Werte sind gültig:

           server
               Listet die Ressourcen für die standardmäßige Serverinstanz auf.
               Dies ist der Standardwert.

           domain
               Listet die Ressourcen für die Domain auf.

           cluster-name
               Listet die Ressourcen für jede Serverinstanz im angegebenen
               Cluster auf.

           instance-name
               Listet die Ressourcen für die angegebene Serverinstanz auf.

BEISPIELE
       Example 1, Alle JMS-Ressourcen auflisten
           Der folgende Unterbefehl listet alle JMS-Ressourcen auf.

               asadmin> list-jms-resources
               jms/Queue
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               jms/Topic
               Command list-jms-resources executed successfully.

       Example 2, JMS-Ressourcen eines bestimmten Typs auflisten
           Der folgende Unterbefehl listet alle
           javax.jms.ConnectionFactory-Ressourcen auf.

               asadmin> list-jms-resources --restype javax.jms.ConnectionFactory
               jms/ConnectionFactory
               jms/DurableConnectionFactory
               Command list-jms-resources executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jms-resource(1), delete-jms-resource(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011           list-jms-resources(1)
