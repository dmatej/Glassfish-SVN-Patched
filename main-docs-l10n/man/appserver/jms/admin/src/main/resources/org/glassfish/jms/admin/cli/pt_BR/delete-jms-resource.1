delete-jms-resource(1)  Subcomandos do utilitário asad  delete-jms-resource(1)

NOME
       delete-jms-resource - remove um recurso JMS

SINOPSE
           delete-jms-resource [--help]
           [--target target]
           jndi_name

DESCRIçãO
       O subcomando delete-jms-resource remove o recurso JMS (Java Message
       Service). Assegure-se de remover todas as referências a esse recurso
       antes de executar o subcomando.

       Esse subcomando só é suportado no modo remoto. Os subcomandos asadmin
       remotos requerem que um servidor de administração de domínios (DAS)
       esteja em execução.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Deleta o recurso JMS somente do alvo especificado. Os valores
           válidos são os seguintes:

               Nota
               ┌────────────────────────────────────────┐
               │Observação Os recursos sempre são       │
               │criados para um domínio como um todo,   │
               │mas só estarão ativos para alvos para   │
               │os quais <resource-ref> tiver sido      │
               │criado usando a opção --target durante  │
               │a criação do recurso. Isso significa    │
               │que a deleção de um recurso só deleta o │
               │elemento <resource-ref> para o --target │
               │especificado e não deleta o recurso do  │
               │domínio como um todo, a não ser que     │
               │domain seja especificado como o         │
               │--target para a deleção.                │
               └────────────────────────────────────────┘

           server
               Deleta o recurso JMS da instância do servidor default. Este é o
               valor default.

           domain
               Deleta o recurso JMS a partir do domínio.

           cluster-name
               Deleta o recurso JMS de cada instância de servidor no cluster
               especificado.

           instance-name
               Deleta o recurso JMS da instância do servidor especificada.

OPERANDOS
       jndi_name
           O nome JDNI do recurso JMS a ser deletado.

EXEMPLOS
       Example 1, Deletando um recurso do destino JMS
           O subcomando a seguir deleta o recurso do destino JMS com o nome
           jms/MyQueue.

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                   11 de fevereiro de 2011     delete-jms-resource(1)
