delete-jms-host(1)      Sous-commandes de l'utilitaire      delete-jms-host(1)

NOM
       delete-jms-host - enlève un hôte JMS

SYNTAXE
           delete-jms-host [--help]
           [--target target]
           jms_host_name

DESCRIPTION
       La sous-commande delete-jms-host enlève l'hôte JMS (Java Message
       Service) spécifié.

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

       La suppression de l'hôte JMS par défaut, nommé default_JMS_host, n'est
       pas recommandée.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Supprime l'hôte JMS de la cible spécifiée uniquement. Les valeurs
           valides sont les suivantes :

           server
               Supprime l'hôte JMS de l'instance de serveur par défaut. Il
               s'agit de la valeur par défaut.

           configuration-name
               Supprime l'hôte JMS de la configuration spécifiée.

           cluster-name
               Supprime l'hôte JMS de chaque instance de serveur du cluster
               spécifié.

           instance-name
               Supprime l'hôte JMS de l'instance de serveur spécifiée.

OPÉRANDES
       jms_host_name
           Nom de l'hôte à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un hôte JMS
           La sous-commande suivante supprime l'hôte JMS nommé MyNewHost.

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                        22 oct. 2010               delete-jms-host(1)
