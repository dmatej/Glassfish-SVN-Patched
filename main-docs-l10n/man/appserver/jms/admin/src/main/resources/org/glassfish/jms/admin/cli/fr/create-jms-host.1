create-jms-host(1)      Sous-commandes de l'utilitaire      create-jms-host(1)

NOM
       create-jms-host - crée un hôte JMS

SYNTAXE
           create-jms-host [--help]
           --mqhost mq-host --mqport mq-port
           --mquser mq-user --mqpassword mq-password
           [--target target]
           [--force[={false|true}]]
           jms_host_name

DESCRIPTION
       Crée un hôte JMS (Java Message Service) dans le service JMS.

       Cette sous-commande est prise en charge en mode distant uniquement. Les
       sous-commandes asadmin distantes requièrent l'exécution d'un serveur
       d'administration de domaine (DAS).

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --mqhost
           Nom d'hôte pour le service JMS.

       --mqport
           Numéro de port utilisé par le service JMS.

       --mquser
           Nom utilisateur pour le service JMS.

       --mqpassword
           Mot de passe pour le service JMS.

       --target
           Cette option crée l'hôte JMS uniquement pour la cible indiquée. Les
           valeurs valides sont les suivantes :

           server
               Cette valeur crée l'hôte JMS pour l'instance de serveur par
               défaut. Il s'agit de la valeur par défaut.

           configuration-name
               Cette valeur crée l'hôte JMS dans la configuration indiquée.

           cluster-name
               Cette valeur crée l'hôte JMS pour chaque instance de serveur du
               cluster indiqué.

           instance-name
               Cette valeur crée l'hôte JMS pour l'instance de serveur
               indiquée.

       --force
           Cette option indique si la sous-commande écrase l'hôte JMS existant
           qui porte le même nom. La valeur par défaut est false.

OPÉRANDES
       jms_host_name
           Identificateur unique de l'hôte JMS à créer.

EXEMPLES
       Exemple 1, Création d'un hôte JMS à l'aide d'un port différent de celui
       par défaut
           La commande suivante crée un hôte JMS nommé MyNewHost sur le
           système pigeon.

               asadmin> create-jms-host --mqhost pigeon.example.com --mqport 7677
               --mquser admin --mqpassword admin MyNewHost
               Jms Host MyNewHost created.
               Command create-jms-host executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       list-jms-hosts(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                         12 fév 2013               create-jms-host(1)
