delete-jms-host(1)      Unterbefehle des asadmin-Utili      delete-jms-host(1)

NAME
       delete-jms-host - entfernt einen JMS-Host

ZUSAMMENFASSUNG
           delete-jms-host [--help]
           [--target target]
           jms_host_name

BESCHREIBUNG
       Der Unterbefehl delete-jms-host entfernt den angegebenen Java Message
       Service-(JMS-)Host.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt. Für
       Remote-asadmin-Unterbefehle muss ein Domainadministrationsserver (DAS)
       ausgeführt werden.

       Es wird nicht empfohlen, den standardmäßigen JMS-Host namens
       default_JMS_host zu löschen.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Löscht den JMS-Host nur aus dem angegebenen Ziel. Folgende Werte
           sind gültig:

           server
               Löscht den JMS-Host aus der standardmäßigen Serverinstanz. Dies
               ist der Standardwert.

           configuration-name
               Löscht den JMS-Host aus der angegebenen Konfiguration.

           cluster-name
               Löscht den JMS-Host aus jeder Serverinstanz im angegebenen
               Cluster.

           instance-name
               Löscht den JMS-Host aus der angegebenen Serverinstanz.

OPERANDEN
       jms_host_name
           Der Name des Hosts, der gelöscht werden soll.

BEISPIELE
       Example 1, JMS-Hosts löschen
           Der folgende Befehl löscht den JMS-Host namens MyNewHost.

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                        22. Okt. 2010              delete-jms-host(1)
