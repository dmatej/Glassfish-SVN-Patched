list-jms-hosts(1)       Subcomandos de la Utilidad asa       list-jms-hosts(1)

NOMBRE DE REFERENCIA
       list-jms-hosts - Muestra los hosts de JMS existentes.

SINOPSIS
           list-jms-hosts [--help]
           [--target target]

DESCRIPCIÓN
       El subcomando list-jms-hosts muestra los hosts de Java Message Service
       (JMS) existentes para el servicio JMS.

       Este subcomando sólo está soportado en modo remoto. Los subcomandos
       asadmin remotos necesitan un servidor de administración de dominio
       (DAS) en ejecución.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Restringe el listado a hosts JMS de un destino especificado. Los
           valores válidos son los siguientes:

           server
               Muestra una lista de hosts JMS para la instancia de servidor
               por defecto. Este es el valor por defecto.

           configuration-name
               Muestra los hosts JMS para la configuración especificada.

           cluster-name
               Muestra una lista de hosts JMS para todas las instancias del
               servidor en el cluster especificado.

           instance-name
               Muestra una lista de hosts JMS para la instancia de servidor
               especificada.

EJEMPLOS
       Ejemplo 1, Lista de Todos los Hosts de JMS
           El siguiente subcomando muestra los hosts JMS para el servicio JMS.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                    22 de octubre de 2010           list-jms-hosts(1)
