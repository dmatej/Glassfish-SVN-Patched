delete-jms-host(1)        asadmin 유틸리티 하위 명령        delete-jms-host(1)

제목
       delete-jms-host - JMS 호스트 제거

시놉시스
           delete-jms-host [--help]
           [--target target]
           jms_host_name

설명
       delete-jms-host 하위 명령은 지정된 JMS(Java Message Service) 호스트를
       제거합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

       기본 JMS 호스트 default_JMS_host는 삭제하지 않는 것이 좋습니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에서만 JMS 호스트를 삭제합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스에서 JMS 호스트를 삭제합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성에서 JMS 호스트를 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 JMS 호스트를
               삭제합니다.

           instance-name
               지정된 서버 인스턴스에서 JMS 호스트를 삭제합니다.

피연산자
       jms_host_name
           삭제할 호스트의 이름입니다.

예제
       예 1, JMS 호스트 삭제
           다음 하위 명령은 MyNewHost라는 JMS 호스트를 삭제합니다.

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                      2010년 10월 22일             delete-jms-host(1)
