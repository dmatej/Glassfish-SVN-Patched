list-jmsdest(1)             asadmin 公用程式子命令             list-jmsdest(1)

參考名
       list-jmsdest - 列出現有的 JMS 實體目的地

概要
           list-jmsdest [--help]
           [--desttype type]
           [target]

描述
       list-jmsdest 子命令會列出 Java Message Service (JMS) 實體目的地。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --desttype
           要列出的 JMS 目的地類型。有效值為 topic 和 queue。

運算元
       target
           將清單限制為指定目標的實體目的地。有效的值如下：

           server
               列出預設伺服器執行處理的實體目的地。此為預設值。

           configuration-name
               列出指定組態中的實體目的地。

           cluster-name
               列出指定叢集中每個伺服器執行處理的實體目的地。

           instance-name
               列出指定伺服器執行處理的實體目的地。

範例
       範例 1, 列出所有實體目的地
           下列子命令會列出所有實體目的地。

               asadmin> list-jmsdest
               PhysicalQueue
               PhysicalTopic
               Command list-jmsdest executed successfully.

       範例 2, 列出指定類型的所有實體目的地
           下列子命令會列出所有實體主題。

               asadmin> list-jmsdest --desttype topic
               PhysicalTopic
               Command list-jmsdest executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jmsdest(1), delete-jmsdest(1), flush-jmsdest(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日               list-jmsdest(1)
