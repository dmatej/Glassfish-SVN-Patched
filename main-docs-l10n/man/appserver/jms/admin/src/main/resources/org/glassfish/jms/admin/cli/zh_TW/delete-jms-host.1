delete-jms-host(1)          asadmin 公用程式子命令          delete-jms-host(1)

參考名
       delete-jms-host - 移除 JMS 主機

概要
           delete-jms-host [--help]
           [--target target]
           jms_host_name

描述
       delete-jms-host 子命令會移除指定的 Java 訊息服務 (JMS) 主機。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

       建議不要刪除名為 default_JMS_host 的預設 JMS 主機。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除 JMS 主機。有效的值如下：

           server
               從預設伺服器執行處理中刪除 JMS 主機。此為預設值。

           configuration-name
               從指定組態中刪除 JMS 主機。

           cluster-name
               從指定叢集的每個伺服器執行處理中刪除 JMS 主機。

           instance-name
               從指定伺服器執行處理中刪除 JMS 主機。

運算元
       jms_host_name
           要刪除的主機名稱。

範例
       範例 1, 刪除 JMS 主機
           下列子命令會刪除名為 MyNewHost 的 JMS 主機。

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 22 日           delete-jms-host(1)
