delete-jms-host(1)          asadmin 实用程序子命令          delete-jms-host(1)

名称
       delete-jms-host - 删除 JMS 主机

提要
           delete-jms-host [--help]
           [--target target]
           jms_host_name

说明
       delete-jms-host 子命令删除指定的 Java 消息服务 (JMS) 主机。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

       建议不要删除名为 default_JMS_host 的默认 JMS 主机。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           仅从指定目标中删除 JMS 主机。有效值如下：

           server
               从默认服务器实例中删除 JMS 主机。这是默认值。

           configuration-name
               从指定配置中删除 JMS 主机。

           cluster-name
               从指定集群中的每个服务器实例中删除 JMS 主机。

           instance-name
               从指定服务器实例中删除 JMS 主机。

操作数
       jms_host_name
           要删除的主机名称。

示例
       Example 1, 删除 JMS 主机
           以下子命令删除名为 MyNewHost 的 JMS 主机。

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                     2010 年 10 月 22 日           delete-jms-host(1)
