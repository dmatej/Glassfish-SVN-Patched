delete-jms-resource(1)      asadmin 公用程式子命令      delete-jms-resource(1)

參考名
       delete-jms-resource - 移除 JMS 資源

概要
           delete-jms-resource [--help]
           [--target target]
           jndi_name

描述
       delete-jms-resource 子命令會移除指定的 Java 訊息服務 (JMS)
       資源。在執行此子命令之前，確保您已移除此資源的所有參照。

       只有遠端模式支援這個子命令。遠端 asadmin
       子命令需要使用執行中的網域管理伺服器 (DAS)。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           僅從指定目標中刪除 JMS 資源。有效的值如下：

               注意
               ┌─────────────────────────────────────────────────────┐
               │備註                                                 │
               │資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
               │--target 選項建立 <resource-ref>                     │
               │的目標。這表示刪除資源只會刪除指定                   │
               │--target 的 <resource-ref>                           │
               │元素，而不會刪除完整網域的資源，除非已將             │
               │domain 指定為要刪除的 --target。                     │
               └─────────────────────────────────────────────────────┘

           server
               從預設伺服器執行處理中刪除 JMS 資源。此為預設值。

           domain
               從網域刪除 JMS 資源。

           cluster-name
               從指定叢集的每個伺服器執行處理中刪除 JMS 資源。

           instance-name
               從指定伺服器執行處理中刪除 JMS 資源。

運算元
       jndi_name
           要刪除的 JMS 資源的 JNDI 名稱。

範例
       範例 1, 刪除 JMS 目的地資源
           下列子命令會刪除名為 jms/MyQueue 的 JMS 目的地資源。

               asadmin> delete-jms-resource jms/MyQueue
               Administered object jms/MyQueue deleted.
               Command delete-jms-resource executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jms-resource(1), list-jms-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日        delete-jms-resource(1)
