jms-ping(1)                 asadmin 实用程序子命令                 jms-ping(1)

名称
       jms-ping - 检查 JMS 服务是否已启动且正在运行

提要
           jms-ping [--help]
           [-- target target]

说明
       jms-ping 子命令检查 Java 消息服务 (JMS) 服务 (也称为 JMS 提供方)
       是否已启动且正在运行。启动 GlassFish Server 时，默认情况下会启动 JMS
       服务。

       jms-ping 子命令仅对 JMS 服务中的默认 JMS
       主机执行试通操作。当它无法对内置 JMS
       服务执行试通操作时，将显示错误消息。

       该子命令仅在远程模式下受支持。远程 asadmin
       子命令需要一个正在运行的域管理服务器 (DAS)。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要执行操作的目标。有效值如下：

           server
               对默认服务器实例的 JMS 服务执行试通操作。这是默认值

           configuration-name
               对使用指定配置的所有集群的 JMS 服务执行试通操作。

           cluster-name
               对指定集群的 JMS 服务执行试通操作。

           instance-name
               对指定的服务器实例的 JMS 服务执行试通操作。

示例
       Example 1, 验证 JMS 服务是否正在运行
           以下子命令检查 JMS 服务是否正在默认服务器上运行。

               asadmin> jms-ping
               JMS-ping command executed successfully
               Connector resoure test_jms_adapter created.
               Command jms-ping executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jms-host(1), list-jms-hosts(1), delete-jms-host(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日                   jms-ping(1)
