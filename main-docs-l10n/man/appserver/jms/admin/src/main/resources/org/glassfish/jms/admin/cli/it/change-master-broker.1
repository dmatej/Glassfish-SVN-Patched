change-master-broker(1) Comandi secondari della utilit change-master-broker(1)

NOME
       change-master-broker - modifica il broker principale in un cluster
       Message Queue che fornisce i servizi JMS per un cluster di GlassFish
       Server.

SINTASSI
           change-master-broker [--help]
           clustered-instance-name

DESCRIZIONE
       Il comando secondario change-master-broker modifica il broker
       principale in un cluster Message Queue che costituisce il provider di
       servizi JMS per un cluster di GlassFish Server. Per impostazione
       predefinita, il broker principale è quello associato alla prima istanza
       configurata nel cluster di GlassFish Server.

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       clustered-instance-name
           Il nome dell'istanza server il cui broker Message Queue è destinato
           a diventare il broker principale del cluster Message Queue. Questa
           istanza server deve essere un'istanza in un cluster di GlassFish
           Server.

ESEMPI
       Esempio 1, Modifica del broker principale
           Il comando secondario seguente modifica il broker principale
           Message Queue in uno idoneo all'istanza in clusterclustinst3.

               asadmin> change-master-broker clustinst3
               Command change-master-broker executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       asadmin(1M)

Java EE 7                      11 febbraio 2011        change-master-broker(1)
