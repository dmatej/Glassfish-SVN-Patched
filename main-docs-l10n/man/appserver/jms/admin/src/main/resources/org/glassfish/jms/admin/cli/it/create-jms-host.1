create-jms-host(1)      Comandi secondari della utilit      create-jms-host(1)

NOME
       create-jms-host - crea un host JMS

SINTASSI
           create-jms-host [--help]
           --mqhost mq-host --mqport mq-port
           --mquser mq-user --mqpassword mq-password
           [--target target]
           [--force={false|true}]
           jms_host_name

DESCRIZIONE
       Crea un host JMS (Java Message Service) all'interno del servizio JMS.

       Questo comando secondario è supportato solo in modalità remota. Per i
       comandi secondari asadmin è necessario un server di amministrazione del
       dominio (DAS) in esecuzione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --mqhost
           Il nome dell'host per il servizio JMS.

       --mqport
           Il numero di porta utilizzato dal servizio JMS.

       --mquser
           Il nome utente per il servizio JMS.

       --mqpassword
           La password per il servizio JMS.

       --target
           Crea l'host JMS solo per la destinazione specificata. Di seguito
           sono riportati i valori validi.

           server
               Crea l'host JMS per l'istanza server predefinita. Questo è il
               valore predefinito.

           configuration-name
               Crea l'host JMS nella configurazione specificata.

           cluster-name
               Crea l'host JMS per tutte le istanze server nel cluster
               specificato.

           instance-name
               Crea l'host JMS per l'istanza server specificata.

       --force
           Specifica se il comando secondario sovrascrive l'host JMS esistente
           che ha lo stesso nome. Il valore predefinito è false.

OPERANDI
       jms_host_name
           L'identificativo univoco dell'host JMS da creare.

ESEMPI
       Esempio 1, Creazione di un host JMS con una porta non predefinita
           Il comando seguente crea l'host JMS denominato MyNewHost nel
           sistema pigeon.

               asadmin> create-jms-host --mqhost pigeon.example.com --mqport 7677
               --mquser admin --mqpassword admin MyNewHost
               Jms Host MyNewHost created.
               Command create-jms-host executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       list-jms-hosts(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                      12 febbraio 2013             create-jms-host(1)
