list-jms-hosts(1)         asadmin 유틸리티 하위 명령         list-jms-hosts(1)

제목
       list-jms-hosts - 기존 JMS 호스트 나열

시놉시스
           list-jms-hosts [--help]
           [--target target]

설명
       list-jms-hosts 하위 명령은 JMS(Java Message Service) 서비스에 대한 기존
       JMS 호스트를 나열합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           지정된 대상에 대한 JMS 호스트만 나열되도록 제한합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스에 대한 JMS 호스트를 나열합니다. 이 값은
               기본값입니다.

           configuration-name
               지정된 구성의 JMS 호스트를 나열합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스의 JMS 호스트를
               나열합니다.

           instance-name
               지정된 서버 인스턴스에 대한 JMS 호스트를 나열합니다.

예제
       예 1, 모든 JMS 호스트 나열
           다음 하위 명령은 JMS 서비스에 대한 JMS 호스트를 나열합니다.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

Java EE 7                      2010년 10월 22일              list-jms-hosts(1)
