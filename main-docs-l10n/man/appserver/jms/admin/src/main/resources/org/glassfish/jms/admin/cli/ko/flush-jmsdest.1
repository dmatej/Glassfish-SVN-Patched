flush-jmsdest(1)          asadmin 유틸리티 하위 명령          flush-jmsdest(1)

제목
       flush-jmsdest - JMS 대상의 메시지 비우기

시놉시스
           flush-jmsdest [--help]
           --desttype {topic|queue}
           [--target target]
           destname

설명
       flush-jmsdest 하위 명령은 서버 JMS(Java Message Service) 구성의 물리적
       대상에서 메시지를 제거합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다. 원격 asadmin 하위 명령을
       사용하려면 DAS(도메인 관리 서버)가 실행 중이어야 합니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --desttype
           이 옵션은 메시지를 제거할 물리적 대상의 유형을 나타냅니다. 지원되는
           대상 유형은 topic 및 queue입니다.

       --target
           지정된 대상에 대해서만 물리적 대상에서 메시지를 삭제합니다. 적합한
           값은 다음과 같습니다.

           server
               기본 서버 인스턴스에 대한 물리적 대상에서 메시지를 삭제합니다.
               이 값은 기본값입니다.

           configuration-name
               지정된 구성의 물리적 대상에서 메시지를 삭제합니다.

           cluster-name
               지정된 클러스터에 있는 모든 서버 인스턴스에 대한 물리적
               대상에서 메시지를 삭제합니다.

           instance-name
               지정된 서버 인스턴스에 대한 물리적 대상에서 메시지를
               삭제합니다.

피연산자
       dest_name
           제거할 JMS 대상의 고유 식별자입니다.

예제
       예 1, 물리적 대상에서 메시지 제거
           다음은 PhysicalQueue라는 대기열에서 메시지를 제거하는 하위
           명령입니다.

               asadmin> flush-jmsdest --desttype queue PhysicalQueue
               Command flush-jmsdest executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jmsdest(1), list-jmsdest(1), create-jmsdest(1)

       asadmin(1M)

Java EE 7                       2011년 2월 11일               flush-jmsdest(1)
