delete-jms-host(1)      Subcomandos do utilitário asad      delete-jms-host(1)

NOME
       delete-jms-host - remove um host JMS

SINOPSE
           delete-jms-host [--help]
           [--target target]
           jms_host_name

DESCRIçãO
       O subcomando delete-jms-host remove o host Java Message Service (JMS)
       especificado.

       Esse subcomando só é suportado no modo remoto. Os subcomandos asadmin
       remotos requerem que um servidor de administração de domínios (DAS)
       esteja em execução.

       Não é recomendado deletar o host JMS default default_JMS_host.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Deleta o host JMS somente do alvo especificado. Os valores válidos
           são os seguintes:

           server
               Deleta o host JMS da instância do servidor default. Este é o
               valor default.

           configuration-name
               Deleta o host JMS da configuração especificada.

           cluster-name
               Deleta o host JMS de cada instância de servidor no cluster
               especificado.

           instance-name
               Deleta o host JMS da instância do servidor especificada.

OPERANDOS
       jms_host_name
           O nome do host a ser deletado.

EXEMPLOS
       Example 1, Deletando um host JMS
           O subcomando a seguir deleta o host JMS com o nome MyNewHost.

               asadmin> delete-jms-host MyNewHost
               Command delete-jms-host executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jms-host(1), list-jms-hosts(1), jms-ping(1)

       asadmin(1M)

Java EE 7                         22 out 2010               delete-jms-host(1)
