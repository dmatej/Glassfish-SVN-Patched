migrate-timers(1)         asadmin 유틸리티 하위 명령         migrate-timers(1)

제목
       migrate-timers - 클러스터된 인스턴스가 정지되거나 충돌할 경우 EJB
       타이머 이동

시놉시스
           migrate-timers [--help] [--target target_server_name]
            server_name

설명
       migrate-timers 하위 명령은 자동 타이머 마이그레이션이 클러스터 구성에서
       사용으로 설정되지 않은 경우 서버 인스턴스가 정지되거나 충돌하면 EJB
       타이머를 지정된 서버로 이동합니다. 이 하위 명령은 원격 모드에서만
       지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           이 항목은 대상 서버 인스턴스입니다. 이 옵션이 지정되지 않은 경우
           DAS가 단일 또는 다중 서버 인스턴스를 찾습니다. 선택된 서버
           인스턴스로 마이그레이션 통지가 전송됩니다.

       --destination
           이 옵션은 더 이상 사용되지 않습니다.  --target 옵션과 동일하게
           작동합니다.

피연산자
       server_name
           타이머가 현재 있는 서버 인스턴스입니다. 마이그레이션 프로세스
           중에는 이 서버 인스턴스가 실행 중이지 않아야 합니다.

예제
       예 1, 타이머 마이그레이션
           이 예제는 instance1이라는 서버에서 instance2라는 서버로 타이머를
           마이그레이션하는 방법을 보여줍니다.

               asadmin>migrate-timers --target instance2 instance1
               This command was successfully executed.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       list-timers(1)

       asadmin(1M)

Java EE 7                      2010년 11월 17일              migrate-timers(1)
