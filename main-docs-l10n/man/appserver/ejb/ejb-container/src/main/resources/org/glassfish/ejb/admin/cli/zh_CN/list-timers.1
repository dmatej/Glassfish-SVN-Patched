list-timers(1)              asadmin 实用程序子命令              list-timers(1)

名称
       list-timers - 列出服务器实例拥有的所有持久性计时器

提要
           list-timers [--help] [target]

说明
       list-timers
       子命令列出特定服务器实例或服务器实例集群所拥有的持久性计时器。该命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           目标为独立服务器实例或集群。如果目标为独立实例，则会列出该实例所拥有的计时器数量。如果目标为集群，则会列出集群中每个实例所拥有的计时器数量。默认目标为
           server，即默认的服务器实例。

示例
       Example 1, 列出服务器实例中的当前计时器
           此示例列出特定独立服务器实例中的持久性计时器。设置了一个当前活动的计时器。

               asadmin> list-timers server
               1

               The list-timers command was executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       migrate-timers(1)

       asadmin(1M)

       The Java EE 6 Tutorial 中的 Using the Timer Service

       Oracle GlassFish Server Application Development Guide 中的 EJB Timer
       Service

Java EE 7                     2010 年 10 月 12 日               list-timers(1)
