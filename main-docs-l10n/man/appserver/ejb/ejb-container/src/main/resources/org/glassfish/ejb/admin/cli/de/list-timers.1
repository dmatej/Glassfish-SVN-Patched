list-timers(1)          Unterbefehle des asadmin-Utili          list-timers(1)

NAME
       list-timers - listet alle persistenten Timer der Serverinstanz(en) auf

ZUSAMMENFASSUNG
           list-timers [--help] [target]

BESCHREIBUNG
       Der Unterbefehl list-timers listet die persistenten Timer auf, die
       Eigentum einer bestimmten Serverinstanz oder eines Clusters von
       Serverinstanzen sind. Dieser Befehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Das Ziel ist entweder eine Standalone-Serverinstanz oder ein
           Cluster. Handelt es sich beim Ziel um eine
           Standalone-Serverinstanz, wird die Anzahl der Timer aufgelistet,
           die Eigentum der Instanz sind. Handelt es sich beim Ziel um ein
           Cluster, wird die Anzahl der Timer aufgelistet, die Eigentum der
           einzelnen Instanzen im Cluster sind. Das Standardziel lautet server
           (die standardmäßige Serverinstanz).

BEISPIELE
       Example 1, Aktuelle Timer in einer Serverinstanz auflisten
           In diesem Beispiel werden die persistenten Timer in einer
           bestimmten Standalone-Serverinstanz aufgelistet. Derzeit ist ein
           aktiver Timer festgelegt.

               asadmin> list-timers server
               1

               The list-timers command was executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       migrate-timers(1)

       asadmin(1M)

       "Using the Timer Service" im The Java EE 6 Tutorial

       "EJB Timer Service" im Oracle GlassFish Server Application Development
       Guide

Java EE 7                        12. Okt. 2010                  list-timers(1)
