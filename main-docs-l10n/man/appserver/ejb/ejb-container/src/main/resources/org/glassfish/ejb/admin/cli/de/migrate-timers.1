migrate-timers(1)       Unterbefehle des asadmin-Utili       migrate-timers(1)

NAME
       migrate-timers - verschiebt EJB-Timer, wenn eine geclusterte Instanz
       gestoppt wurde oder abgestürzt ist

ZUSAMMENFASSUNG
           migrate-timers [--help] [--target target_server_name]
            server_name

BESCHREIBUNG
       Der Unterbefehl migrate-timers verschiebt EJB-Timer in einen
       angegebenen Server, wenn eine Serverinstanz gestoppt wurde oder
       abstürzt ist, sofern die automatische Timermigration nicht in der
       Clusterkonfiguration aktiviert ist. Dieser Unterbefehl wird nur im
       Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Dies ist die Zielserverinstanz. Ist diese Option nicht angegeben,
           sucht der DAS eine Serverinstanz oder mehrere Serverinstanzen. An
           die ausgewählten Serverinstanzen wird eine
           Migrationsbenachrichtigung gesendet.

       --destination
           Diese Option ist veraltet. Sie funktioniert genauso wie die Option
           --target.

OPERANDEN
       server_name
           Dies ist die Serverinstanz, auf der sich die Timer derzeit
           befinden. Diese Serverinstanz sollte während des
           Migrationsprozesses nicht ausgeführt werden.

BEISPIELE
       Beispiel 1, Timer migrieren
           Dieses Beispiel zeigt, wie Timer aus dem Server mit dem Namen
           instance1 in einen Server mit dem Namen instance2 migriert werden.

               asadmin>migrate-timers --target instance2 instance1
               This command was successfully executed.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       list-timers(1)

       asadmin(1M)

Java EE 7                        17. Nov. 2010               migrate-timers(1)
