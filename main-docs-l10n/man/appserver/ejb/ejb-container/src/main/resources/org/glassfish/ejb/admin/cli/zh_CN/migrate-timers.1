migrate-timers(1)           asadmin 实用程序子命令           migrate-timers(1)

名称
       migrate-timers - 在集群实例停止或崩溃时移动 EJB 计时器

提要
           migrate-timers [--help] [--target target_server_name]
            server_name

说明
       当某个服务器实例停止或崩溃时，如果未在集群配置中启用自动计时器移植，则可以使用
       migrate-timers 子命令将 EJB
       计时器移至指定的服务器。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           这是目标服务器实例。如果没有指定此选项，DAS
           将找到一个或多个服务器实例。会将移植通知发送到所选服务器实例。

       --destination
           此选项已过时。该选项的功能与 --target 选项完全相同。

操作数
       server_name
           这是计时器当前所在的服务器实例。在移植过程中，该服务器实例不应运行。

示例
       Example 1, 移植计时器
           该示例说明如何将计时器从名为 instance1 的服务器移植至名为 instance2
           的服务器。

               asadmin>migrate-timers --target instance2 instance1
               This command was successfully executed.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       list-timers(1)

       asadmin(1M)

Java EE 7                     2010 年 11 月 17 日            migrate-timers(1)
