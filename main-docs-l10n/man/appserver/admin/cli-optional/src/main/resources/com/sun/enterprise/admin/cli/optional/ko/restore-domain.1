restore-domain(1)         asadmin 유틸리티 하위 명령         restore-domain(1)

제목
       restore-domain - 백업에서 파일 복원

시놉시스
           restore-domain [--help]
           [--long[={false|true}]]
           [--filename backup-filename]
           [--domaindir domain-root-dir]
           [--backupdirbackup-directory]
           [--backupconfigbackup-config-name]
           [--force[={false|true}]]
           [domain-name]

설명
       이 명령은 백업 디렉토리에서 도메인의 파일을 복원합니다.

       restore-domain 명령은 로컬 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --long, -l
           복원 작업에 대한 세부 정보를 표시합니다.

           기본값은 false입니다.

       --filename
           소스로 사용할 백업 파일의 이름을 지정합니다.

       --domaindir
           복원할 도메인의 상위 디렉토리인 도메인 루트 디렉토리를 지정합니다.

           기본값은 as-install/domains입니다.

       --backupdir
           백업 파일을 저장할 디렉토리를 지정합니다.

           기본값은 as-install/domains/domain-dir/backups입니다. 도메인이 기본
           위치에 없는 경우 위치는 domain-dir/backups입니다.

       --backupconfig
           (Oracle GlassFish Server에서만 지원됩니다.) 백업 파일을 저장할 백업
           디렉토리의 도메인 백업 구성 이름입니다.

       --force
           복원할 도메인의 이름이 백업 파일에 저장되어 있는 도메인의 이름과
           일치하지 않는 경우에도 복원 작업이 계속되도록 합니다.

           기본값은 false입니다.

피연산자
       domain-name
           복원할 도메인의 이름을 지정합니다.

           GlassFish Server 설치에 도메인이 하나만 존재하는 경우 이 피연산자는
           선택 사항입니다.

           지정된 도메인 이름이 백업 파일에 저장되어 있는 도메인 이름과
           일치하지 않는 경우 --force 옵션이 지정되어 있지 않으면 오류가
           발생합니다.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       backup-domain(1), list-backups(1)

Java EE 7                      2010년 12월 22일              restore-domain(1)
