backup-domain(1)        Sous-commandes de l'utilitaire        backup-domain(1)

NOM
       backup-domain - effectue une sauvegarde sur le domaine

SYNTAXE
           backup-domain [--help]
           [--long[={false|true}]]
           [--description description-text]
           [--domaindir domain-root-dir]
           [--backupdir backup-directory]
           [--backupconfig backup-config-name]
           [domain_name]

DESCRIPTION
       La sous-commande backup-domain sauvegarde des fichiers sous le domaine
       nommé.

       Cette sous-commande est prise en charge en mode local uniquement dans
       GlassFish Server Open Source Edition. Elle est prise en charge en modes
       local et distant dans Oracle GlassFish Server.

       Dans GlassFish Server Open Source Edition, le domaine à sauvegarder
       doit être arrêté.

       Dans Oracle GlassFish Server, le domaine à sauvegarder doit être arrêté
       ou suspendu à l'aide de la sous-commande ().

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Cette option affiche des informations détaillées sur l'opération de
           sauvegarde.

           La valeur par défaut est false.

       --description
           Cette option indique une description à stocker dans le fichier de
           sauvegarde. La description s'affiche en tant qu'informations
           relatives au fichier de sauvegarde.

           La valeur par défaut prend la forme suivante :

               domain-name backup created on YYYY_MM_DD by user user-name

       --domaindir
           Cette option indique le répertoire racine de domaine, le répertoire
           parent du domaine à sauvegarder.

           La valeur par défaut est as-install/domains.

       --backupdir
           Cette option indique le répertoire dans lequel le fichier de
           sauvegarde doit être stocké.

           La valeur par défaut est as-install/domains/domain-dir/backups. Si
           le domaine ne se trouve pas à l'emplacement par défaut,
           l'emplacement est domain-dir/backups.

       --backupconfig
           (Pris en charge uniquement dans Oracle GlassFish Server.) Nom de la
           configuration de la sauvegarde du domaine dans le répertoire sous
           lequel le fichier de sauvegarde doit être stocké.

OPÉRANDES
       domain-name
           Cette option indique le nom du domaine à sauvegarder.

           Cet opérande est facultatif s'il n'existe qu'un seul domaine dans
           l'installation de GlassFish Server.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       restore-domain(1), list-backups(1), (), ()

Java EE 7                         22 déc 2010                 backup-domain(1)
