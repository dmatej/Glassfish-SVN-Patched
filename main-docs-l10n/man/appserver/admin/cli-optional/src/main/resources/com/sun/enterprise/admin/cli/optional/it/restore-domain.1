restore-domain(1)       Comandi secondari della utilit       restore-domain(1)

NOME
       restore-domain - ripristina i file dal backup

SINTASSI
           restore-domain [--help]
           [--long[={false|true}]]
           [--filename backup-filename]
           [--domaindir domain-root-dir]
           [--backupdirbackup-directory]
           [--backupconfigbackup-config-name]
           [--force[={false|true}]]
           [domain-name]

DESCRIZIONE
       Questo comando consente di ripristinare i file nel dominio da una
       directory di backup.

       Il comando restore-domain è supportato solo in modalità locale.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --long, -l
           Consente di visualizzare informazioni dettagliate sulle operazioni
           di ripristino.

           Il valore predefinito è false.

       --filename
           Specifica il nome del file di backup da utilizzare come origine.

       --domaindir
           Specifica la directory radice, ovvero la directory padre, del
           dominio da ripristinare.

           Il valore predefinito è as-install/domains.

       --backupdir
           Specifica la directory in cui è memorizzato il file di backup.

           Il valore predefinito è as-install/domains/domain-dir/backups. Se
           il dominio non si trova nella posizione predefinita, la posizione
           sarà domain-dir/backups.

       --backupconfig
           (Supportato solo in Oracle GlassFish Server). Il nome della
           configurazione di backup del dominio nella directory di backup in
           cui è memorizzato il file di backup.

       --force
           Fa sì che l'operazione di ripristino continui anche se il nome del
           dominio da ripristinare non corrisponde al nome del dominio
           memorizzato nel file di backup.

           Il valore predefinito è false.

OPERANDI
       domain-name
           Specifica il nome del dominio da ripristinare.

           Questo operando è facoltativo se esiste un solo dominio
           nell'installazione di GlassFish Server.

           Se il nome di dominio specificato non corrisponde al nome di
           dominio archiviato nel file di backup, si verifica un errore a meno
           che non venga specificata l'opzione --force.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       backup-domain(1), list-backups(1)

Java EE 7                      22 dicembre 2010              restore-domain(1)
