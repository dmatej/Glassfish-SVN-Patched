restore-domain(1)       Sous-commandes de l'utilitaire       restore-domain(1)

NOM
       restore-domain - restaure les fichiers à partir de la sauvegarde

SYNTAXE
           restore-domain [--help]
           [--long[={false|true}]]
           [--filename backup-filename]
           [--domaindir domain-root-dir]
           [--backupdirbackup-directory]
           [--backupconfigbackup-config-name]
           [--force[={false|true}]]
           [domain-name]

DESCRIPTION
       Cette commande restaure les fichiers sous le domaine à partir d'un
       répertoire de sauvegarde.

       La commande restore-domain est prise en charge en mode local
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --long, -l
           Affiche des informations détaillées sur l'opération de
           restauration.

           La valeur par défaut est false.

       --filename
           Spécifie le nom du fichier de sauvegarde à utiliser comme source.

       --domaindir
           Spécifie le répertoire racine de domaine, qui correspond au
           répertoire parent du domaine à restaurer.

           La valeur par défaut est as-install/domains.

       --backupdir
           Spécifie le répertoire dans lequel le fichier de sauvegarde est
           stocké.

           La valeur par défaut est as-install/domains/domain-dir/backups. Si
           le domaine ne se trouve pas à l'emplacement par défaut,
           l'emplacement est domain-dir/backups.

       --backupconfig
           (Pris en charge uniquement dans Oracle GlassFish Server.) Nom de la
           configuration de sauvegarde de domaine dans le répertoire de
           sauvegarde sous lequel le fichier de sauvegarde est stocké.

       --force
           L'opération de restauration continue, même lorsque le nom du
           domaine à restaurer ne correspond pas au nom de domaine stocké dans
           le fichier de sauvegarde.

           La valeur par défaut est false.

OPÉRANDES
       domain-name
           Spécifie le nom du domaine à restaurer.

           Cet opérande est facultatif s'il n'existe qu'un seul domaine dans
           l'installation de GlassFish Server.

           Si le nom de domaine indiqué ne correspond pas au nom de domaine
           stocké dans le fichier de sauvegarde, une erreur survient, sauf si
           l'option --force est spécifiée.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       backup-domain(1), list-backups(1)

Java EE 7                         22 déc 2010                restore-domain(1)
