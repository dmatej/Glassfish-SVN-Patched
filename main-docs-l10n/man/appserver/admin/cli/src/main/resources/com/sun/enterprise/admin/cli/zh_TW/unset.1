UNSET(1)                                                              UNSET(1)

參考名
       unset - 從多重模式環境移除一或多個變數

概要
           unset [--help] variable-list

說明
       unset
       子命令可移除針對多重模式環境設定的一或多個環境變數。移除變數之後，變數和其相關值就不會再套用至多重模式環境。

       若要列出設定的環境變數，請使用不含選項的 export 子命令。如果 export
       子命令沒有列出任何環境變數，則表示未設定環境變數。

       只有本機模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       variable-list
           要移除的環境變數的清單 (以空格分隔)。

範例
       範例 1, 列出已設定的環境變數
           此範例使用 export 子命令來列出已經設定的環境變數。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       範例 2, 移除環境變數
           此範例會移除 AS_ADMIN_PREFIX 環境變數。

               asadmin> unset AS_ADMIN_PREFIX
               Command unset executed successfully

結束狀態
       0
           子命令執行成功

       1
           執行子命令時發生錯誤

另請參閱
       export(1), multimode(1)

       asadmin(1M)

                              2010 年 12 月 20 日                     UNSET(1)
