EXPORT(1)                                                            EXPORT(1)

參考名
       export - 在多重模式中，標示變數名稱以自動匯出至後續命令的環境

概要
           export [--help] [variable-name=value [variable-name=value]*]

說明
       在多重模式下，export
       子命令會標示環境變數，以自動匯出至後續命令的環境。除非您結束多重模式，或是使用
       unset
       子命令取消設定變數，否則所有後續命令都會使用指定的變數名稱值。如果只指定變數名稱，則會顯示該變數名稱的目前值。

       如果 export
       子命令不搭配任何引數使用，則會顯示所有已匯出之變數及其值的清單。在呼叫
       asadmin 公用程式前設定之已匯出的 shell 環境變數，會自動匯入 asadmin
       並設為已匯出的變數。asadmin 公用程式無法讀取未匯出的環境變數。

       只有本機模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       variable-name=value
           變數名稱及值，用以自動匯出至後續命令要使用的環境。

範例
       範例 1, 列出已設定的環境變數
           此範例會列出已經設定的環境變數。

               asadmin> export
               AS_ADMIN_USER = admin
               AS_ADMIN_HOST = bluestar
               AS_ADMIN_PREFIX = server1.jms-service
               AS_ADMIN_PORT = 8000
               Command export executed successfully

       範例 2, 設定環境變數
           此範例會將 AS_ADMIN_HOST 環境變數設為 bluestar。

               asadmin> export AS_ADMIN_HOST=bluestar
               Command export executed successfully

       範例 3, 設定多重環境變數
           此範例會為多重模式環境設定多個環境變數。

               asadmin> export AS_ADMIN_HOST=bluestar AS_ADMIN_PORT=8000
               AS_ADMIN_USER=admin AS_ADMIN_PREFIX=server1.jms-service
               Command export executed successfully

結束狀態
       0
           子命令執行成功

       1
           執行子命令時發生錯誤

另請參閱
       unset(1), multimode(1)

       asadmin(1M)

                              2009 年 10 月 5 日                     EXPORT(1)
