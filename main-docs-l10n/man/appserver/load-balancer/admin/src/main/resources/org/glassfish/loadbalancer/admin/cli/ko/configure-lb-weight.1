configure-lb-weight(1)    asadmin 유틸리티 하위 명령    configure-lb-weight(1)

제목
       configure-lb-weight - 클러스터된 인스턴스에 대한 로드 균형 조정 가중치
       설정

시놉시스
           configure-lb-weight [--help] --cluster cluster_name
            instance-name=weight[:instance-name=weight]

설명
       configure-lb-weight 하위 명령은 클러스터의 서버 인스턴스에 가중치를
       지정합니다. 가중치는 HTTP, RMI/IIOP 및 JMS 로드 균형 조정에 사용할 수
       있습니다. HTTP 로드 밸런서의 경우 로드 밸런서의 정책이
       weighted-round-robin으로 설정된 경우에만 가중치가 사용됩니다. 로드
       밸런서 정책은 create-http-lb-ref 하위 명령 또는 set 하위 명령에서
       설정됩니다.

       클러스터의 서로 다른 인스턴스에 가해지는 로드를 다양하게 설정하려면
       가중치를 사용합니다. 예를 들어 용량이 보다 큰 시스템에 인스턴스가 있는
       경우 로드 밸런서가 해당 인스턴스에 보다 많은 요청을 보내도록 이
       인스턴스에 보다 높은 가중치를 둡니다. 기본 가중치는 100입니다. 모든
       인스턴스가 기본 가중치를 가지는 경우 로드 밸런서는 단순 라운드 로빈
       로드 균형 조정을 수행합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --cluster
           클러스터의 이름입니다.

피연산자
       instance-name=weight
           인스턴스의 이름과 해당 인스턴스에 지정할 가중치입니다. 가중치는
           정수여야 합니다. 인스턴스와 가중치 쌍은 콜론으로 구분합니다. 예를
           들어 instance1=1:instance2=4는 다섯 개 요청마다 요청 하나는
           instance1로 가고 요청 네 개는 instance2로 감을 의미합니다. 기본값은
           가중치 1입니다.

예제
       예 1, 클러스터 인스턴스에 로드 밸런서 가중치 지정
           다음 하위 명령은 가중치 1, 1 및 2를 cluster1 클러스터의 인스턴스
           i1, i2 및 i3에 지정합니다.

               asadmin> configure-lb-weight --cluster cluster1 i1=1:i2=1:i3=2
               Command configure-lb-weight executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb-ref(1)create-cluster(1)

       asadmin(1M)

Java EE 7                       2010년 8월 25일         configure-lb-weight(1)
