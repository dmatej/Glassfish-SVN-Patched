enable-http-lb-server(1)    asadmin 实用程序子命令    enable-http-lb-server(1)

名称
       enable-http-lb-server - 启用由负载平衡器管理的以前已禁用的服务器或集群

提要
           enable-http-lb-server [--help] target

说明
       enable-http-lb-server
       子命令为某个独立服务器实例或集群启用负载平衡。默认情况下，已经为实例和集群启用了负载平衡。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           此操作数指定要启用的服务器实例和集群。有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 为某个集群启用负载平衡
           此示例为名为 mycluster 的集群启用负载平衡。

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日      enable-http-lb-server(1)
