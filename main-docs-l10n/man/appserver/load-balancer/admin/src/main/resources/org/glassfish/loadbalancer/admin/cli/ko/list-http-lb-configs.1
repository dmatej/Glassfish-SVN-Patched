list-http-lb-configs(1)   asadmin 유틸리티 하위 명령   list-http-lb-configs(1)

제목
       list-http-lb-configs - 로드 밸런서 구성 나열

시놉시스
           list-http-lb-configs [--help] [target]

설명
       list-http-lb-configs 하위 명령은 로드 밸런서 구성을 나열합니다. 구성을
       모두 나열하거나 참조하는 클러스터 또는 서버 인스턴스별로 나열합니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           대상별로 로드 밸런서를 나열합니다. 적합한 값은 다음과 같습니다.

           ·   cluster_name- 대상 클러스터의 이름입니다.

           ·   instance_name- 대상 서버 인스턴스의 이름입니다.

예제
       예 1, 대상 없이 로드 밸런서 구성 나열
           이 예제는 모든 GlassFish Server 클러스터 및 인스턴스에 대해 정의된
           로드 밸런서 구성을 모두 나열합니다.

               asadmin> list-http-lb-configs

               mycluster-http-lb-config
               serverinstlb
               Command list-http-lb-configs executed successfully.

       예 2, 특정 대상에 대한 로드 밸런서 구성 나열
           이 예제는 mycluster라는 클러스터에 대해 정의된 로드 밸런서 구성을
           나열합니다.

               asadmin> list-http-lb-configs mycluster

               mycluster-http-lb-config
               Command list-http-lb-configs executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-http-lb-config(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                       2010년 8월 26일        list-http-lb-configs(1)
