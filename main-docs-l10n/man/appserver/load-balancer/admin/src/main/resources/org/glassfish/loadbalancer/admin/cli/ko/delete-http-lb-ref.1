delete-http-lb-ref(1)     asadmin 유틸리티 하위 명령     delete-http-lb-ref(1)

제목
       delete-http-lb-ref - 로드 밸런서에서 클러스터 또는 서버 인스턴스 삭제

시놉시스
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

설명
       로드 밸런서 구성 또는 로드 밸런서에서 클러스터 또는 독립형 서버
       인스턴스에 대한 참조를 제거하려면 delete-http-lb-ref 하위 명령을
       사용합니다. 사용자 요청을 중단하지 않도록 로드 밸런서 구성에서 독립형
       서버 인스턴스 또는 클러스터에 있는 모든 서버 인스턴스를 제거하기 전에
       이러한 인스턴스가 사용 안함으로 설정되어 있는지 확인합니다.  force
       옵션이 true로 설정된 경우 서버 인스턴스 또는 클러스터가 사용으로
       설정되어 있더라도 참조가 삭제됩니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --config
           클러스터 및 서버 인스턴스 참조를 삭제할 로드 밸런서 구성을
           지정합니다.

           로드 밸런서 구성 또는 로드 밸런서를 지정합니다. 둘 다 지정하면
           오류가 발생합니다.

       --lbname
           클러스터 및 서버 인스턴스 참조를 삭제할 로드 밸런서를 지정합니다.

           로드 밸런서 구성 또는 로드 밸런서를 지정합니다. 둘 다 지정하면
           오류가 발생합니다.

       --force
           force가 true로 설정된 경우 현재 사용으로 설정된 응용 프로그램 또는
           인스턴스가 있더라도 참조가 삭제됩니다. 기본값은 false입니다.

피연산자
       target
           로드 밸런서에서 제거할 클러스터 또는 인스턴스를 지정합니다. 적합한
           값은 다음과 같습니다.

           ·   cluster_name- 대상 클러스터의 이름입니다.

           ·   instance_name- 대상 서버 인스턴스의 이름입니다.

예제
       예 1, 로드 밸런서 구성에서 클러스터 참조 삭제
           이 예제는 mycluster-http-lb-config라는 로드 밸런서 구성에서
           cluster2라는 클러스터에 대한 참조를 삭제합니다.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                       2010년 8월 25일          delete-http-lb-ref(1)
