disable-http-lb-applicatSous-commandes de l'utildisable-http-lb-application(1)

NOM
       disable-http-lb-application - désactive une application gérée par un
       équilibreur de charge

SYNTAXE
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

DESCRIPTION
       La sous-commande de désactivation disable-http-lb-application désactive
       une application pour l'équilibrage de charge. L'application désactivée
       est mise hors ligne pour l'équilibrage de charge avec un impact minimal
       sur les utilisateurs. La désactivation d'une application permet un plus
       grand degré de finesse du contrôle que la désactivation d'une instance
       de serveur et est plus utile lorsqu'un cluster héberge plusieurs
       applications indépendantes.

       Une fois l'application désactivée et les modifications appliquées à
       l'équilibreur de charge, les nouvelles demandes pour l'application ne
       sont pas transmises à la cible. Les sessions existantes continuent à
       accéder à l'application jusqu'à l'expiration du délai d'attente. Ce
       processus est appelé mise au ralenti.

       Si une application est déployée sur plusieurs clusters, utilisez cette
       sous-commande pour la désactiver dans un cluster tout en la laissant
       activée sur les autres.

       Si une application est déployée sur une seule instance de serveur,
       utilisez cette sous-commande pour la désactiver dans cette instance
       tout en laissant cette dernière activée.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --timeout
           Délai d'attente (en minutes) au-delà duquel l'application spécifiée
           est désactivée. Il permet l'arrêt flexible (mise au ralenti) de
           l'application spécifiée. La valeur par défaut est 30 minutes. La
           valeur minimale est 1 minute.

       --name
           Nom de l'application à désactiver.

OPÉRANDES
       target
           Cet opérande spécifie l'instance de serveur ou le cluster sur
           lequel désactiver l'application. Les valeurs valides sont les
           suivantes :

           ·   cluster_name : nom d'un cluster cible.

           ·   instance_name : nom d'une instance de serveur cible.

EXEMPLES
       Exemple 1, Désactivation d'une application pour l'équilibrage de charge
           Dans cet exemple, une application est désactivée pour l'équilibrage
           de charge.

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                        26 août 2010   disable-http-lb-application(1)
