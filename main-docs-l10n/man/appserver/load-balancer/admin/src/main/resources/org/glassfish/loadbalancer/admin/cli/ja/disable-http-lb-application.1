disable-http-lb-applicasadminユーティリティのサブ

名前
       disable-http-lb-application -
       ロード・バランサで管理されるアプリケーションを無効にします

概要
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

説明
       disable-http-lb-applicationサブコマンドは、アプリケーションでロード・バランシングを無効にします。ロード・バランシングが無効なアプリケーションはオフラインになり、ユーザーへの影響が最小限に抑えられます。アプリケーションを無効にすると、サーバー・インスタンスを無効にする場合よりも詳細な制御が可能になります。これは、クラスタが複数の独立したアプリケーションをホストしている場合に非常に役立ちます。

       アプリケーションが無効化され、変更がロード・バランサに適用されると、アプリケーションに対する新しいリクエストはターゲットに転送されません。既存のセッションは、タイムアウトに達するまではアプリケーションにアクセスします。このプロセスは休止機能と呼ばれます。

       アプリケーションが複数のクラスタにデプロイされている場合、このサブコマンドを使用すると、他のクラスタ内のアプリケーションを有効にしたまま、特定のクラスタ内のアプリケーションを無効にすることができます。

       アプリケーションが単一のサーバー・インスタンスにデプロイされている場合、このサブコマンドを使用すると、インスタンス自体を有効にしたまま、そのインスタンス内のアプリケーションを無効にすることができます。

           注記
           ┌───────────────────────────────────────────────────────────────────┐
           │注意                                                               │
           │                                                                   │
           │           このサブコマンドは、Oracle                              │
           │           GlassFish                                               │
           │           Serverにのみ適用されます。このサブコマンドは、GlassFish │
           │           Server Open Source                                      │
           │           Editionには適用されません。                             │
           └───────────────────────────────────────────────────────────────────┘

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --timeout
           指定したアプリケーションを無効にするまでのタイムアウト(分)。この時間が経過すると、指定したアプリケーションは正常に停止します(休止機能)。デフォルト値は30分です。最小値は1分です。

       --name
           無効にするアプリケーションの名前。

オペランド
       target
           このオペランドでは、アプリケーションを無効にするサーバー・インスタンスまたはクラスタを指定します。有効な値は次のとおりです:

           ·   cluster_name- ターゲット・クラスタの名前。

           ·   instance_name- ターゲット・サーバー・インスタンスの名前。

例
       例 1, アプリケーションでのロード・バランシングの無効化
           この例では、アプリケーションでロード・バランシングを無効にします

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                        2010年8月26日  disable-http-lb-application(1)
