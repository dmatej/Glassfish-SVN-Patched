delete-http-lb-ref(1)   Subcomandos de la Utilidad asa   delete-http-lb-ref(1)

NOMBRE DE REFERENCIA
       delete-http-lb-ref - Suprime el cluster o la instancia del servidor de
       un equilibrador de carga.

SINOPSIS
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

DESCRIPCIÓN
       Utilice el subcomandodelete-http-lb-ref para eliminar una referencia a
       un cluster o instancia de servidor independiente de una configuración
       de equilibrador de carga o del equilibrador de carga. Para que no se
       interrumpan las solicitudes de usuario, asegúrese de que la instancia
       de servidor independiente o todas las instancias de servidor del
       cluster estén desactivadas antes de eliminarlas de la configuración de
       equilibrador de carga. Si la opción force se define en true, se
       suprimen las referencias aunque las instancias de servidor o los
       clusters estén activados.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --config
           Especifica de qué configuración de equilibrador de carga se deben
           suprimir las referencias de cluster e instancia de servidor.

           Especifique una configuración de equilibrador de carga o bien un
           equilibrador de carga. Si especifica ambos se produce un error.

       --lbname
           Especifica el equilibrador de carga del que se deben suprimir las
           referencias de cluster e instancia de servidor.

           Especifique una configuración de equilibrador de carga o bien un
           equilibrador de carga. Si especifica ambos se produce un error.

       --force
           Si force se define en true, se suprimen las referencias aunque haya
           aplicaciones o instancias activadas. El valor por defecto es false.

OPERANDOS
       target
           Especifica qué cluster o instancia se debe eliminar del
           equilibrador de carga. Los valores válidos son:

           ·   cluster_name: nombre de un cluster de destino.

           ·   instance_name: nombre de una instancia de servidor de destino.

EJEMPLOS
       Ejemplo 1, Supresión de una Referencia de Cluster de una Configuración
       de Equilibrador de Carga
           En este ejemplo se suprime la referencia al cluster denominado
           cluster2 de una configuración de equilibrador de carga denominada
           mycluster-http-lb-config.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                    25 de agosto de 2010        delete-http-lb-ref(1)
