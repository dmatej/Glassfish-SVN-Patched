delete-http-lb-ref(1)   Subcomandos do utilitário asad   delete-http-lb-ref(1)

NOME
       delete-http-lb-ref - deleta a instância do cluster ou do servidor de um
       balanceador de carga

SINOPSE
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

DESCRIçãO
       Utilize o subcomando delete-http-lb-ref para remover uma referência
       para um cluster ou instância de servidor standalone de uma configuração
       do balanceador de carga ou de um balanceador de carga. Para não
       interromper os solicitações do usuário, certifique-se de que a
       instância de servidor standalone ou todas as instâncias de servidor no
       cluster estejam desativadas antes de removê-las da configuração do
       balanceador de carga. Se a opção force estiver definida como
       verdadeira, as referências são deletadas mesmo se as instâncias de
       servidor ou clusters estiverem ativos.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --config
           Especifica qual configuração do balanceador de carga deletar das
           referências da instância de servidor e do cluster.

           Especifica tanto uma configuração do balanceador de carga quanto um
           balanceador de carga. Especificando os resultados em um erro.

       --lbname
           Especifica o balanceador de carga a deletar das referências da
           instância de servidor e do cluster.

           Especifica tanto uma configuração do balanceador de carga quanto um
           balanceador de carga. Especificando os resultados em um erro.

       --force
           Se force for definido como verdadeiro, as referências serão
           deletadas mesmo que haja aplicações ou instâncias ativadas. O
           default é falso.

OPERANDOS
       target
           Especifica qual cluster ou instância remover do balanceador de
           carga. Os valores válidos são:

           ·   cluster_name - O nome de um cluster do alvo.

           ·   instance_name - O nome de uma instância do servidor do alvo.

EXEMPLOS
       Example 1, Deletando uma Referência de Cluster de uma Configuração do
       Balanceador de Carga
           Este exemplo deleta a referência para um cluster com o nome
           cluster2 de uma configuração do balanceador de carga com o nome
           mycluster-http-lb-config.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                    25 de agosto de 2010        delete-http-lb-ref(1)
