delete-http-lb-ref(1)   Comandi secondari della utilit   delete-http-lb-ref(1)

NOME
       delete-http-lb-ref - elimina il cluster o un'istanza server da un load
       balancer

SINTASSI
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

DESCRIZIONE
       Usare il comando secondario delete-http-lb-ref per rimuovere un
       riferimento a un cluster o a un'istanza server standalone da una
       configurazione del load balancer o da un load balancer. Per non
       interrompere le richieste utente, accertarsi che l'istanza server
       standalone o che tutte le istanze server del cluster siano disabilitate
       prima di rimuoverle dalla configurazione del load balancer. Se si
       imposta l'opzione force su true, i riferimenti vengono eliminati anche
       se le istanze server o i cluster sono abilitati.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server Questo     │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --config
           Specifica la configurazione del load balancer da cui eliminare i
           riferimenti ai cluster e alle istanze server.

           Specificare una configurazione del load balancer o un load
           balancer. Se vengono specificati entrambi si verifica un errore.

       --lbname
           Specifica il load balancer da cui eliminare i riferimenti ai
           cluster e alle istanze server.

           Specificare una configurazione del load balancer o un load
           balancer. Se vengono specificati entrambi si verifica un errore.

       --force
           Se si imposta force su true, i riferimenti vengono eliminati anche
           se vi sono applicazioni o istanze abilitate al momento. Il valore
           predefinito è false.

OPERANDI
       target
           Specifica il cluster o l'istanza da rimuovere dal load balancer. Di
           seguito sono riportati i valori validi.

           ·   cluster_name: il nome di un cluster di destinazione.

           ·   instance_name: il nome di un'istanza server di destinazione.

ESEMPI
       Esempio 1, Eliminazione di un riferimento al cluster da una
       configurazione del load balancer
           In questo esempio viene eliminato da una configurazione del load
           balancer denominata mycluster-http-lb-config il riferimento a un
           cluster denominato cluster2.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                       25 agosto 2010           delete-http-lb-ref(1)
