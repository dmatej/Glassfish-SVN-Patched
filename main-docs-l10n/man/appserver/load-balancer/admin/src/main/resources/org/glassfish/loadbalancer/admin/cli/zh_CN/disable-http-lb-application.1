disable-http-lb-application(asadmin 实用程序子命disable-http-lb-application(1)

名称
       disable-http-lb-application - 禁用由负载平衡器管理的应用程序

提要
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

说明
       disable-http-lb-application
       子命令为某个应用程序禁用负载平衡。禁用的应用程序将脱机，在实现负载平衡的同时又能使对用户的影响减至最小。禁用应用程序比禁用服务器实例提供了更精确的控制，在集群承载多个独立应用程序时用处最大。

       禁用应用程序并对负载平衡器应用更改后，对应用程序的新请求不会转发到目标。现有会话会继续访问应用程序，直到超时为止。此过程称为停顿。

       如果跨多个集群部署某个应用程序，使用此子命令会在一个集群中禁用该应用程序而在其他集群中使该应用程序保持启用状态。

       如果将某个应用程序部署到单一服务器实例，使用此子命令会禁用该实例中的应用程序而使该实例本身保持启用状态。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --timeout
           禁用指定的应用程序前要等待的超时 (分钟)。这段时间允许正常关闭
           (停顿) 指定的应用程序。默认值为 30 分钟。最小值为 1 分钟。

       --name
           要禁用的应用程序的名称。

操作数
       target
           此操作数指定要禁用应用程序的服务器实例或集群。有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 为某个应用程序禁用负载平衡
           此示例将为某个应用程序禁用负载平衡

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日disable-http-lb-application(1)
