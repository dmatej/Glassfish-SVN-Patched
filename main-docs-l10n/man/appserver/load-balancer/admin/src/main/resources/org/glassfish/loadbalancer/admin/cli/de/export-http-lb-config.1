export-http-lb-config(1)Unterbefehle des asadmin-Utiliexport-http-lb-config(1)

NAME
       export-http-lb-config - exportiert die Load Balancer-Konfiguration oder
       den Load Balancer in eine Datei

ZUSAMMENFASSUNG
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

BESCHREIBUNG
       Der Unterbefehl export-http-lb-config exportiert eine Load
       Balancer-Konfiguration oder einen Load Balancer in eine Datei, die vom
       Load Balancer-Plug-in verwendet werden kann. Der standardmäßige
       Dateiname ist loadbalancer.xml. Sie können aber auch einen anderen
       Namen angeben. Nachdem die Datei exportiert wurde, können Sie sie
       manuell in das Verzeichnis des Load Balancer-Plug-ins kopieren, bevor
       die Konfigurationsänderungen angewendet werden. Mit der Option --target
       können Sie eine loadbalancer.xml-Datei für Cluster oder
       Standalone-Instanzen generieren, ohne dass lb-config- oder
       load-balancer-Elemente manuell in der Datei domain.xml des Ziels
       erstellt werden müssen.

       Um Änderungen auf den Load Balancer anzuwenden, ohne die
       Konfigurationsdatei manuell zu kopieren, konfigurieren Sie den Load
       Balancer so, dass Änderungen automatisch mit create-http-lb angewendet
       werden. Wenn Sie den Unterbefehl create-http-lb verwenden, muss
       export-http-lb-config nicht verwendet werden.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --config
           Gibt an, welche Load Balancer-Konfiguration exportiert werden soll.

           Geben Sie entweder eine Load Balancer-Konfiguration oder einen Load
           Balancer an. Wird beides angegeben, tritt ein Fehler auf.

       --lbname
           Gibt den Load Balancer an, der exportiert werden soll.

           Geben Sie entweder eine Load Balancer-Konfiguration oder einen Load
           Balancer an. Wird beides angegeben, tritt ein Fehler auf.

       --retrievefile
           Ist der Wert auf true gesetzt, wird die Datei loadbalancer.xml aus
           dem Remote-Rechner abgerufen. Der Standardwert ist false.

       --target
           Gibt das Ziel an, in das die Load Balancer-Konfiguration exportiert
           wird. Wenn kein Ziel angegeben ist, wird die Load
           Balancer-Konfiguration in das Verzeichnis exportiert, das mit
           file_name angegeben ist.

OPERANDEN
       file_name
           Gibt den Dateinamen und das Verzeichnis der exportierten
           Konfiguration an.

           ·   Wenn Sie ein (relatives oder absolutes) Verzeichnis, aber
               keinen Dateinamen angeben, wird eine Datei namens
               loadbalancer.xml.load_balancer_config_name im angegebenen
               Verzeichnis erstellt. Bei Microsoft Windows-Systemen muss der
               Pfad in Anführungszeichen gesetzt werden.

           ·   Wenn Sie einen Dateinamen in einem relativen oder absoluten
               Pfad angeben, wird die Datei mit dem angegebenen Namen im
               angegebenen Verzeichnis erstellt.

           ·   Wenn Sie einen Dateinamen, aber kein Verzeichnis angeben, wird
               die Datei mit diesem Namen im aktuellen Arbeitsverzeichnis
               erstellt.

           ·   Wenn Sie diesen Operanden nicht angeben, ist der Standardwert
               eine Datei mit dem Namen
               loadbalancer.xml.load_balancer_config_name, die im Verzeichnis
               domain-dir/generated erstellt wird.

       target
           Gibt das Ziel an, in das die Konfiguration exportiert wird.

           Gültige Werte sind:

           ·   cluster_name: Gibt ein Cluster und die zugehörigen
               Serverinstanzen an.

           ·   stand-alone_instance_name: Gibt eine bestimmte Serverinstanz
               an.

BEISPIELE
       Beispiel 1, Load Balancer-Konfiguration unter UNIX exportieren
           In diesem Beispiel wird eine Load Balancing-Konfiguration mit dem
           Namen mycluster-http-lb-config in eine Datei mit dem Namen
           loadbalancer.xml in das Verzeichnis /Sun/AppServer exportiert.

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       Beispiel 2, Load Balancer-Konfiguration unter Windows exportieren
           In diesem Beispiel wird eine Load Balancing-Konfiguration mit dem
           Namen mycluster-http-lb-config in eine Datei mit dem Namen
           loadbalancer.xml in das Verzeichnis C:\Sun\AppServer in einem
           Microsoft Windows-System exportiert.

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                        26. Aug. 2010        export-http-lb-config(1)
