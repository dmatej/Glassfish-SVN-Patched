apply-http-lb-changes(1)Comandi secondari della utilitapply-http-lb-changes(1)

NOME
       apply-http-lb-changes - applica le modifiche di configurazione al load
       balancer

SINTASSI
           apply-http-lb-changes [--help]
            lb-name

DESCRIZIONE
       Utilizzare il comando secondario applyhttp-lb-changes per applicare le
       modifiche apportate alla configurazione del load balancer al load
       balancer fisico. Il load balancer deve esistere già. Per creare un load
       balancer fisico, utilizzare il comando secondario create-http-lb.

       Questo comando secondario è applicabile solo a Oracle GlassFish Server
       Questo comando secondario non è applicabile a GlassFish Server Open
       Source Edition.

CONTESTO
       Il load balancer distribuisce il carico di lavoro tra più istanze
       Oracle GlassFish Server, aumentando il throughput complessivo del
       sistema. Il load balancer consente inoltre alle richieste di eseguire
       il failover da un'istanza server all'altra. Per rendere persistenti le
       informazioni di sessione HTTP, configurare la persistenza della
       sessione HTTP.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Il plugin load balancer è    │
           │           disponibile solo con Oracle  │
           │           GlassFish Server, ma non con │
           │           GlassFish Server Open Source │
           │           Edition. Per GlassFish       │
           │           Server Open Source Edition,  │
           │           è possibile utilizzare il    │
           │           modulo mod_jk per            │
           │           configurare il bilanciamento │
           │           del carico nel server HTTP   │
           │           Apache.                      │
           └────────────────────────────────────────┘

       Per ulteriori informazioni sulla configurazione del bilanciamento del
       carico con GlassFish Server, consultare la Guida in linea nella Console
       di amministrazione di GlassFish Server.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       lb-name
           Il nome del load balancer a cui vengono applicate le modifiche. Il
           load balancer deve esistere già. È possibile crearlo con il comando
           secondario create-http-lb.

ESEMPI
       Esempio 1, Utilizzo del comando secondario apply-http-lb-changes
           In questo esempio le modifiche di configurazione vengono applicate
           a un load balancer denominato mylb.

               asadmin> apply-http-lb-changes mylb
               Command apply-http-lb-changes executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-lb(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                      22 dicembre 2010       apply-http-lb-changes(1)
