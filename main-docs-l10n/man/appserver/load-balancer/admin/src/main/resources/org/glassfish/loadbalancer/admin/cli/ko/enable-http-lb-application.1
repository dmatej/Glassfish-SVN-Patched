enable-http-lb-applicationasadmin 유틸리티 하위 명

제목
       enable-http-lb-application - 로드 밸런서에서 관리되는, 이전에 사용
       안함으로 설정된 응용 프로그램을 사용으로 설정

시놉시스
           enable-http-lb-application [--help] --name application_name target

설명
       enable-http-lb-application 하위 명령은 독립형 인스턴스 또는 클러스터에
       배치된 응용 프로그램에 대해 로드 균형 조정을 사용으로 설정합니다.
       클러스터의 모든 인스턴스 또는 단일 독립형 서버 인스턴스에 있는 응용
       프로그램에 대해 로드 균형 조정을 사용으로 설정할 수 있습니다.
       기본적으로 로드 균형 조정은 응용 프로그램에 대해 사용으로 설정되어
       있습니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --name
           사용으로 설정할 응용 프로그램의 이름입니다.

피연산자
       target
           이 피연산자는 응용 프로그램을 사용으로 설정할 서버 인스턴스 또는
           클러스터를 지정합니다. 적합한 값은 다음과 같습니다.

           ·   cluster_name- 대상 클러스터의 이름입니다.

           ·   instance_name- 대상 서버 인스턴스의 이름입니다.

예제
       예 1, 응용 프로그램에 대해 로드 균형 조정 사용
           이 예제는 webapps-simple이라는 응용 프로그램을 사용으로 설정하여
           mycluster라는 클러스터에서 로드 균형 조정을 사용합니다.

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                       2010년 8월 26일  enable-http-lb-application(1)
