list-http-lb-configs(1) Subcomandos do utilitário asad list-http-lb-configs(1)

NOME
       list-http-lb-configs - lista configurações do balanceador de carga

SINOPSE
           list-http-lb-configs [--help] [target]

DESCRIçãO
       O subcomando list-http-lb-configs lista as configurações do balanceador
       de carga. Lista todas elas ou as listas por cluster ou instância do
       servidor que elas referenciam.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Lista os balanceadores de carga por alvo. Os valores válidos são:

           ·   cluster_name - O nome de um cluster do alvo.

           ·   instance_name - O nome de uma instância do servidor do alvo.

EXEMPLOS
       Example 1, Listando Configurações do Balanceador de Carga sem um Alvo
           Este exemplo lista todas as configurações do balanceador de carga
           definidas para todos os clusters e instâncias do GlassFish Server.

               asadmin> list-http-lb-configs

               mycluster-http-lb-config
               serverinstlb
               Command list-http-lb-configs executed successfully.

       Example 2, Listando as Configurações do Balanceador de Carga para um
       Alvo Específico
           Este exemplo lista a configuração do balanceador de carga definido
           como um cluster com o nome mycluster.

               asadmin> list-http-lb-configs mycluster

               mycluster-http-lb-config
               Command list-http-lb-configs executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-http-lb-config(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                         26 ago 2010          list-http-lb-configs(1)
