delete-http-health-checker(1asadmin 实用程序子命令

名称
       delete-http-health-checker - 删除指定负载平衡器配置的健康检查器

提要
           delete-http-health-checker [--help] [--config config_name]
            target

说明
       delete-http-health-checker
       子命令从负载平衡器配置中删除健康检查器。健康检查器对于目标和负载平衡器配置的组合是唯一的。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --config
           从中删除健康检查器的负载平衡器配置。

操作数
       target
           指定要从中删除健康检查器的目标。

           有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 从负载平衡器配置中删除健康检查器
           此示例从名为 mycluster 的集群上的名为 mycluster-http-lb-config
           的负载平衡器配置中删除健康检查器。

               asadmin> delete-http-health-checker --user admin
               --passwordfile password.txt --config mycluster-http-lb-config mycluster

               Command delete-http-health-checker executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-health-checker(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日 delete-http-health-checker(1)
