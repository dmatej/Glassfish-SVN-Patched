enable-http-lb-server(1)Subcomandos de la Utilidad asaenable-http-lb-server(1)

NOMBRE DE REFERENCIA
       enable-http-lb-server - Activa un servidor o cluster previamente
       desactivado gestionado por un equilibrador de carga.

SINOPSIS
           enable-http-lb-server [--help] target

DESCRIPCIÓN
       El subcomando enable-http-lb-server activa una instancia de servidor
       independiente o cluster para el equilibrio de carga. Por defecto, el
       equilibrio de carga está activado para las instancias y los clusters.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Este operando especifica las instancias de servidor y clusters que
           se deben activar. Los valores válidos son:

           ·   cluster_name: nombre de un cluster de destino.

           ·   instance_name: nombre de una instancia de servidor de destino.

EJEMPLOS
       Ejemplo 1, Activación de un Cluster para Equilibrio de Carga
           En este ejemplo se activa el equilibrio de carga en un cluster
           denominado mycluster.

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                    26 de agosto de 2010     enable-http-lb-server(1)
