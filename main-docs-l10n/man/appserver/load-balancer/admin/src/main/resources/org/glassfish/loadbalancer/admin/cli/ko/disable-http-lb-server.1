disable-http-lb-server(1) asadmin 유틸리티 하위 명령 disable-http-lb-server(1)

제목
       disable-http-lb-server - 로드 밸런서에서 관리되는 서버 또는 클러스터를
       사용 안함으로 설정

시놉시스
           disable-http-lb-server [--help] [--timeout 30]
            target

설명
       disable-http-lb-server 하위 명령은 로드 균형 조정을 위해 독립형 서버
       또는 서버 클러스터를 사용 안함으로 설정합니다. 사용 안함으로 설정된
       서버 인스턴스 또는 클러스터는 오프라인 상태로 로드 균형 조정을 수행하여
       사용자에게 미치는 영향을 최소화합니다.

       대상이 사용 안함으로 설정되고 변경 내용이 로드 밸런서에 적용되면 로드
       밸런서에서 대상에 새로운 요청을 지정하지 않습니다. 대상을 사용 안함으로
       설정하기 전에 생성된 세션이 있는 세션 요청은 시간 초과에 도달할 때까지
       해당 대상에 계속 지정할 수 있습니다. 이 프로세스를 중지라고 합니다.

       변경 사항이 로드 밸런서에 자동으로 적용됩니다.  export-http-lb-config를
       사용하여 구성을 수동으로 익스포트하고 로드 밸런서에 복사할 수도
       있습니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --timeout
           지정한 대상이 사용 안함으로 설정될 때까지 대기하는 시간
           초과(분)입니다. 이 시간은 지정한 대상의 정상 종료(중지)를
           허용합니다. 기본값은 30분입니다. 최소값은 1분입니다.

피연산자
       target
           이 피연산자는 사용 안함으로 설정할 서버 인스턴스 및 클러스터를
           지정합니다. 적합한 값은 다음과 같습니다.

           ·   cluster_name- 대상 클러스터의 이름입니다.

           ·   instance_name- 대상 서버 인스턴스의 이름입니다.

예제
       예 1, 로드 균형 조정을 위해 클러스터 사용 안함
           이 예제는 mycluster라는 클러스터에 대해 로드 균형 조정을 사용
           안함으로 설정합니다.

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

Java EE 7                       2010년 8월 26일      disable-http-lb-server(1)
