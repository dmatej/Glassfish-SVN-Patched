export-http-lb-config(1)    asadmin 实用程序子命令;   export-http-lb-config(1)

名称
       export-http-lb-config - 将负载平衡器配置或负载平衡器导出到文件

提要
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

说明
       export-http-lb-config
       子命令将负载平衡器配置或负载平衡器导出到负载平衡器插件可以使用的文件中。默认文件名为
       loadbalancer.xml，但您可以指定其他名称。导出后，可以手动将导出的文件复制到负载平衡器插件位置，然后应用配置更改。使用
       --target 选项可为集群或独立实例生成 loadbalancer.xml，而无需在目标的
       domain.xml 中手动创建 lb-config 或 load-balancer 元素。

       要将更改应用到负载平衡器而不必手动复制配置文件，请使用 create-http-lb
       将负载平衡器配置为自动应用更改。如果使用 create-http-lb
       子命令，则无需使用 export-http-lb-config。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --config
           指定要导出的负载平衡器配置。

           指定负载平衡器配置或负载平衡器。同时指定这两者会导致出现错误。

       --lbname
           指定要导出的负载平衡器。

           指定负载平衡器配置或负载平衡器。同时指定这两者会导致出现错误。

       --retrievefile
           如果设置为真
           文件。默认值为假

       --target
           指定负载平衡器配置将导出到的目标。如果未指定目标，则负载平衡器配置将导出到使用
           file_name 指定的位置。

操作数
       file_name
           指定所导出配置的文件名和位置。

           ·   如果指定目录
               (相对或绝对)，但不指定文件名，则将在指定的目录中创建名为
               loadbalancer.xml.load_balancer_config_name 的文件。在 Microsoft
               Windows 系统上，路径必须括在引号内。

           ·   如果以相对或绝对路径指定文件名，将在指定的目录中使用指定名称创建文件。

           ·   如果指定文件名，但不指定目录，将在当前的工作目录中使用该名称创建文件。

           ·   如果未指定此操作数，则默认值为在 domain-dir/generated
               目录中创建的名为 loadbalancer.xml.load_balancer_config_name
               的文件。

       target
           指定配置将导出到的目标。

           有效值为：

           ·   cluster_name - 指定集群及其服务器实例。

           ·   stand-alone_instance_name - 指定特定服务器实例。

示例
       Example 1, 在 UNIX 上导出负载平衡器配置
           以下示例将名为 mycluster-http-lb-config 的负载平衡配置导出到
           /Sun/AppServer 目录中名为 loadbalancer.xml 的文件。

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       Example 2, 在 Windows 上导出负载平衡器配置
           以下示例将名为 mycluster-http-lb-config 的负载平衡配置导出到
           Microsoft Windows 系统上 C:\Sun\AppServer 目录中名为
           loadbalancer.xml 的文件。

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日      export-http-lb-config(1)
