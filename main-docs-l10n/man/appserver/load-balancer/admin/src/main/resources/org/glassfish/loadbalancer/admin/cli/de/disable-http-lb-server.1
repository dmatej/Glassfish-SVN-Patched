disable-http-lb-server(1Unterbefehle des asadmin-Utildisable-http-lb-server(1)

NAME
       disable-http-lb-server - Deaktiviert einen Server oder ein Cluster, der
       bzw. das von einem Load Balancer verwaltet wird

ZUSAMMENFASSUNG
           disable-http-lb-server [--help] [--timeout 30]
            target

BESCHREIBUNG
       Der Unterbefehl disable-http-lb-server deaktiviert einen
       Standalone-Server oder ein Cluster aus Servern für Load Balancing. Die
       deaktivierte Serverinstanz oder das Cluster wechselt in den
       Offlinestatus, damit das Load Balancing minimale Auswirkungen auf die
       Benutzer hat.

       Sobald das Ziel deaktiviert wurde und die Änderungen auf den Load
       Balancer angewendet wurden, weist der Load Balancer dem Ziel keine
       weiteren Anforderungen zu. Sessionanforderungen mit Sessions, die vor
       der Deaktivierung des Ziels erstellt wurden, werden so lange diesem
       Ziel zugewiesen, bis das Timeout erreicht ist. Dieser Prozess wird auch
       als Stilllegung oder Deaktivierung bezeichnet.

       Die Änderungen werden automatisch auf den Load Balancer angewendet. Sie
       können die Konfiguration auch manuell mit export-http-lb-config
       exportieren und in den Load Balancer kopieren.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --timeout
           Das Timeout (in Minuten) bis zur Deaktivierung des angegebenen
           Ziels. Diese Zeitspanne ermöglicht ein ordnungsgemäßes
           Herunterfahren (Stilllegung) des angegebenen Ziels. Der
           Standardwert beträgt 30 Minuten. Der Mindestwert ist 1 Minute.

OPERANDEN
       target
           Dieser Operand gibt an, welche Serverinstanzen und Cluster
           deaktiviert werden sollen. Gültige Werte sind:

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

BEISPIELE
       Example 1, Cluster für Load Balancing deaktivieren
           In diesem Beispiel wird Load Balancing für ein Cluster namens
           mycluster deaktiviert.

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

Java EE 7                        26. Aug. 2010       disable-http-lb-server(1)
