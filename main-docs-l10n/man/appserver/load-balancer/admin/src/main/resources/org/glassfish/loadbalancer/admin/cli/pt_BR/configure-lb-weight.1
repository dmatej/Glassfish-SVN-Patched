configure-lb-weight(1)  Subcomandos do utilitário asad  configure-lb-weight(1)

NOME
       configure-lb-weight - define pesos do balanceamento de carga para
       instância clusterizadas

SINOPSE
           configure-lb-weight [--help] --cluster cluster_name
            instance-name=weight[:instance-name=weight]

DESCRIçãO
       O subcomando configure-lb-weight designa peso para instâncias do
       servidor em um cluster. Os pesos podem ser usados para balanceamento de
       carga HTTP, RMI/IIOP e JMS. Para o balanceador de carga HTTP, os pesos
       são utilizados apenas se a política do balanceador de cargas estiver
       definida como weighted-round-robin. A política do balanceador de cargas
       está definida no subcomando create-http-lb-ref ou no subcomando set .

       Utilize o peso para variar a carga que vai para diferentes instâncias
       no cluster. Por exemplo, se uma instância estiver em uma máquina com
       mais capacidade, dê à ela mais peso para que mais solicitações sejam
       enviadas para esta instância pelo balanceador de cargas. O peso default
       é 100. Se todas as instâncias possuírem o peso default, o balanceador
       de cargas executa o balanceamento de cargas com revezamento simples.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --cluster
           O nome do cluster.

OPERANDOS
       instance-name=weight
           O nome da instância e o peso que está sendo designado a ela. O
           valor do peso deve ser um número inteiro. Os pares de instâncias e
           pesos são separados por dois-pontos. Por exemplo,
           instance1=1:instance2=4 significa que, para cada cinco
           solicitações, uma vai para a instance1 e quatro vão para instance2.
           O valor 1 é o default para o peso.

EXEMPLOS
       Example 1, Designando Pesos do Balanceador de Carga às Instâncias do
       Cluster
           O subcomando a seguir designa pesos de 1,
            1 e 2 às instâncias i1,
            i2 e i3 no cluster cluster1.

               asadmin> configure-lb-weight --cluster cluster1 i1=1:i2=1:i3=2
               Command configure-lb-weight executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-http-lb-ref(1)create-cluster(1)

       asadmin(1M)

Java EE 7                    25 de agosto de 2010       configure-lb-weight(1)
