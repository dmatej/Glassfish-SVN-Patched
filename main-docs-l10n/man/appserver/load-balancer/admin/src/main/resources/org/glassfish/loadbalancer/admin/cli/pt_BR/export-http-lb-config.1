export-http-lb-config(1)Subcomandos do utilitário asadexport-http-lb-config(1)

NOME
       export-http-lb-config - exporta a configuração do balanceador de carga
       ou o próprio balanceador de carga para um arquivo

SINOPSE
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

DESCRIçãO
       O subcomando export-http-lb-config exporta uma configuração do
       balanceador de carga ou um balanceador de carga para um arquivo que o
       plug-in balanceador de carga possa usar. O nome de arquivo default é
       loadbalancer.xml, mas você pode especificar um nome diferente. Uma vez
       exportado, você copia manualmente o arquivo exportado para a
       localização do plug-in balanceador de carga antes que as alterações da
       configuração sejam aplicadas. A opção --target torna possível gerar um
       loadbalancer.xml para clusters ou instâncias standalone sem ter que
       criar manualmente os elementos lb-config ou load-balancer no domain.xml
       do alvo.

       Para aplicar as alterações no balanceador de carga sem copiar
       manualmente o arquivo de configuração, configure o balanceador de carga
       para aplicar automaticamente as alterações com create-http-lb. Se você
       usar o subcomando create-http-lb, não é necessário usar
       export-http-lb-config.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --config
           Especifica a configuração do balanceador de cargas a ser exportada.

           Especifica tanto uma configuração do balanceador de carga quanto um
           balanceador de carga. Especificando os resultados em um erro.

       --lbname
           Especifica o balanceador de cargas a ser exportado.

           Especifica tanto uma configuração do balanceador de carga quanto um
           balanceador de carga. Especificando os resultados em um erro.

       --retrievefile
           Se definido como true, recupera o arquivo loadbalancer.xml da
           máquina remota. O default é false.

       --target
           Especifica o alvo para o qual a configuração do balanceador de
           cargas será exportada. Se um alvo não for especificado, a
           configuração do balanceador de cargas será exportada para o local
           especificado com file_name.

OPERANDOS
       file_name
           Especifica o nome do arquivo e localização da configuração
           exportada.

           ·   Se você especificar um diretório (relativo ou absoluto) mas não
               um nome de arquivo, o arquivo com o nome
               loadbalancer.xml.load_balancer_config_name será criado no
               diretório especificado. Nos sistemas do Microsoft Windows, o
               caminho deve estar entre aspas.

           ·   Se especificar um nome de arquivo em um caminho relativo ou
               absoluto, o arquivo é criado com o nome específico do diretório
               escolhido.

           ·   Se especificar um nome de arquivo mas não um diretório, o
               arquivo é criado com aquele nome do diretório atual de
               trabalho.

           ·   Se você não especificar esse operando, o valor default será um
               arquivo com o nome loadbalancer.xml.load_balancer_config_name
               criado no diretório domain-dir/generated.

       target
           Especifica o alvo para o qual a configuração será exportada.

           Os valores válidos são:

           ·   cluster_name - Especifica um cluster e suas instâncias de
               servidor.

           ·   stand-alone_instance_name - Especifica uma instância de
               servidor específica.

EXEMPLOS
       Example 1, Exportando uma Configuração do Balanceador de Carga no UNIX
           O exemplo a seguir exporta uma configuração de balanceamento de
           carga com o nome mycluster-http-lb-config para um arquivo com o
           nome loadbalancer.xml no diretório /Sun/AppServer.

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       Example 2, Exportando uma Configuração do Balanceador de Carga no
       Windows
           O exemplo a seguir exporta uma configuração de balanceamento de
           carga com o nome
            mycluster-http-lb-config para um arquivo com o nome
           loadbalancer.xml no diretório C:\Sun\AppServer em um sistema
           Microsoft Windows.

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                         26 ago 2010         export-http-lb-config(1)
