enable-http-lb-applicatiComandi secondari della uenable-http-lb-application(1)

NOME
       enable-http-lb-application - abilita un'applicazione gestita da un load
       balancer disabilitata in precedenza

SINTASSI
           enable-http-lb-application [--help] --name application_name target

DESCRIZIONE
       Il comando secondario enable-http-lb-application abilita il
       bilanciamento del carico per le applicazioni distribuite in un'istanza
       standalone o in un cluster. È possibile abilitare il bilanciamento del
       carico per un'applicazione su tutte le istanze in un cluster o su una
       singola istanza server standalone. Per impostazione predefinita, il
       bilanciamento del carico è abilitato per le applicazioni.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --name
           Il nome dell'applicazione da abilitare.

OPERANDI
       target
           Questo operando specifica l'istanza server o il cluster in cui
           abilitare l'applicazione. Di seguito sono riportati i valori
           validi.

           ·   cluster_name: il nome di un cluster di destinazione.

           ·   instance_name: il nome di un'istanza server di destinazione.

ESEMPI
       Esempio 1, Abilitazione del bilanciamento del carico per
       un'applicazione
           In questo esempio l'applicazione denominata webapps-simple viene
           abilitata per l'utilizzo del bilanciamento del carico in un cluster
           denominato mycluster.

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                       26 agosto 2010   enable-http-lb-application(1)
