disable-http-lb-server(1)   asadmin 公用程式子命令   disable-http-lb-server(1)

參考名
       disable-http-lb-server - 停用負載平衡程式管理的伺服器或叢集

概要
           disable-http-lb-server [--help] [--timeout 30]
            target

描述
       disable-http-lb-server
       子命令會停用獨立伺服器或伺服器叢集的負載平衡。停用的伺服器執行處理或叢集會根據負載平衡離線，儘量降低對使用者的影響。

       在停用目標並已將變更套用至負載平衡程式時，負載平衡程式會停止將新請求指定給目標。在停用目標之前已建立階段作業的階段作業請求會繼續指定給該目標，直至達到逾時。此處理作業被稱為靜止。

       負載平衡程式會自動套用變更。您也可以使用 export-http-lb-config
       手動匯出組態，然後將其複製到負載平衡程式。

           注意
           ┌────────────────────────────────────────┐
           │備註                                    │
           │                                        │
           │           此子命令僅適用於 Oracle      │
           │           GlassFish                    │
           │           Server。此子命令不適用於     │
           │           GlassFish Server Open Source │
           │           Edition。                    │
           └────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --timeout
           在停用指定目標之前所需等待的逾時 (以分鐘為單位)。
           此時間允許正常關閉 (靜止) 指定的目標。預設值為 30 分鐘。最小值為 1
           分鐘。

運算元
       target
           此運算元指定要停用的伺服器執行處理和叢集。有效值包括

           ·   cluster_name - 目標叢集名稱。

           ·   instance_name - 目標伺服器執行處理名稱。

範例
       範例 1, 停用叢集的負載平衡
           此範例會停用名為 mycluster 的叢集的負載平衡。

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日     disable-http-lb-server(1)
