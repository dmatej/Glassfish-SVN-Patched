export-http-lb-config(1)Comandi secondari della utilitexport-http-lb-config(1)

NOME
       export-http-lb-config - esporta la configurazione del load balancer o
       il load balancer in un file

SINTASSI
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

DESCRIZIONE
       Il comando secondario export-http-lb-config esporta una configurazione
       del load balancer o un load balancer in un file utilizzabile dal plugin
       del load balancer. Il nome file predefinito è loadbalancer.xml, ma è
       possibile specificare un altro nome. Dopo l'esportazione, è possibile
       copiare manualmente il file esportato nella posizione del plugin del
       load balancer prima dell'applicazione delle modifiche di
       configurazione. L'opzione --target consente di generare un file
       loadbalancer.xml per i cluster o le istanze standalone senza dover
       creare manualmente elementi lb-config o load-balancer nel file
       domain.xml della destinazione.

       Per applicare le modifiche al load balancer senza copiare manualmente
       il file di configurazione, configurare il load balancer per applicare
       automaticamente le modifiche con create-http-lb. Se si usa il comando
       secondario create-http-lb, non è necessario utilizzare
       export-http-lb-config.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --config
           Specifica la configurazione di load balancer da esportare.

           Specificare una configurazione del load balancer o un load
           balancer. Se vengono specificati entrambi si verifica un errore.

       --lbname
           Specifica il load balancer da esportare.

           Specificare una configurazione del load balancer o un load
           balancer. Se vengono specificati entrambi si verifica un errore.

       --retrievefile
           Se si imposta true, recupera il file loadbalancer.xml dal computer
           remoto. Il valore predefinito è false.

       --target
           Specifica la destinazione in cui verrà esportata la configurazione
           del load balancer. Se non si specifica una destinazione, la
           configurazione del load balancer viene esportata nella posizione
           specificata con file_name.

OPERANDI
       file_name
           Specifica il nome file e la posizione della configurazione
           esportata.

           ·   Se si specifica una directory, relativa o assoluta, ma non un
               nome file, nella directory specificata viene creato il file
               denominato loadbalancer.xml.load_balancer_config_name. Nei
               sistemi Microsoft Windows il percorso deve essere tra racchiuso
               tra virgolette.

           ·   Se si specifica un nome file in un percorso relativo o
               assoluto, il file viene creato con il nome specificato nella
               directory specificata.

           ·   Se si specifica un nome file ma non una directory, il file
               viene creato con il nome specificato nella directory di lavoro
               corrente.

           ·   Se non si specifica questo operando, il valore predefinito è un
               file denominato loadbalancer.xml.load_balancer_config_name
               creato nella directory domain-dir/generated.

       target
           Specifica la destinazione in cui verrà esportata la configurazione.

           Di seguito sono riportati i valori validi.

           ·   cluster_name: specifica un cluster e le relative istanze
               server.

           ·   stand-alone_instance_name: specifica un'istanza server
               particolare.

ESEMPI
       Esempio 1, Esportazione di una configurazione del load balancer su UNIX
           Nell'esempio seguente viene esportata la configurazione di
           bilanciamento del carico denominata mycluster-http-lb-config in un
           file denominato loadbalancer.xml nella directory /Sun/AppServer.

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       Esempio 2, Esportazione di una configurazione del load balancer su
       Windows
           Nell'esempio seguente viene esportata la configurazione di
           bilanciamento del carico denominata mycluster-http-lb-config in un
           file denominato loadbalancer.xml nella directory C:\Sun\AppServer
           di un sistema Microsoft Windows.

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                       26 agosto 2010        export-http-lb-config(1)
