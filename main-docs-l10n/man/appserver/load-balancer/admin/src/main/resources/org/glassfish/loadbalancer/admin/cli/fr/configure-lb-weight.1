configure-lb-weight(1)  Sous-commandes de l'utilitaire  configure-lb-weight(1)

NOM
       configure-lb-weight - définit les pondérations d'équilibrage de charge
       des instances incluses dans un cluster

SYNTAXE
           configure-lb-weight [--help] --cluster cluster_name
            instance-name=weight[:instance-name=weight]

DESCRIPTION
       La sous-commande configure-lb-weight affecte une pondération aux
       instances de serveur dans un cluster. Les pondérations servent à
       équilibrer la charge HTTP, RMI/IIOP et JMS. Pour l'équilibreur de
       charge HTTP, ces pondérations sont utilisées uniquement si la règle de
       l'équilibreur de charge a la valeur weighted-round-robin. La règle de
       l'équilibreur de charge est définie dans les sous-commandes
       create-http-lb-ref ou set.

       Utilisez la pondération pour varier la charge allant à différentes
       instances du cluster. Par exemple, si une instance se trouve sur un
       ordinateur doté d'une capacité supérieure, attribuez-lui une
       pondération plus élevée afin que plusieurs demandes soient envoyées à
       cette instance par l'équilibreur de charge. La pondération par défaut
       est de 100. Si toutes les instances ont la pondération par défaut,
       l'équilibreur de charge effectue un simple équilibrage de charge
       circulaire.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --cluster
           Nom du cluster.

OPÉRANDES
       instance-name=weight
           Nom de l'instance et pondération que vous lui attribuez. La
           pondération doit être un nombre entier. Les paires d'instances et
           de pondérations sont séparées par le caractère deux-points. Par
           exemple, instance1=1:instance2=4 signifie que, toutes les cinq
           demandes, l'une passe à instance1 et quatre à instance2. La valeur
           par défaut de la pondération est 1.

EXEMPLES
       Exemple 1, Affectation de pondérations d'équilibreur de charge à des
       instances de cluster
           La sous-commande suivante affecte des pondérations de 1, 1 et 2 aux
           instances i1, i2 et i3 dans le cluster cluster1.

               asadmin> configure-lb-weight --cluster cluster1 i1=1:i2=1:i3=2
               Command configure-lb-weight executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-http-lb-ref(1)create-cluster(1)

       asadmin(1M)

Java EE 7                         25 aoû 2010           configure-lb-weight(1)
