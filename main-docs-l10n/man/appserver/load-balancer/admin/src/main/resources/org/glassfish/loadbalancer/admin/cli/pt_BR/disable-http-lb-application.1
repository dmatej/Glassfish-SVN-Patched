disable-http-lb-applicatSubcomandos do utilitáridisable-http-lb-application(1)

NOME
       disable-http-lb-application - desativa uma aplicação gerenciada por um
       balanceador de carga

SINOPSE
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

DESCRIçãO
       O subcomando disable-http-lb-application desativa uma aplicação para o
       balanceamento de carga. A aplicação desativada permanece off-line para
       o balanceamento de carga gerando um impacto mínimo para os usuários. A
       desativação de uma aplicação garante uma granularidade de controle
       melhor do que a desativação de uma instância do servidor e é mais útil
       quando um cluster está hospedando muitas aplicações independentes.

       Uma vez que a aplicação estiver desativada e as alterações forem
       aplicadas ao balanceador de carga, novas solicitações para a aplicação
       não serão encaminhadas para o alvo. As sessões existentes continuam a
       acessar a aplicação até que o time-out seja alcançado. Este processo é
       conhecido como desativação.

       Se uma aplicação for implantada por meio de vários clusters, use este
       subcomando para desativá-lo em um cluster enquanto o deixa ativado em
       outros clusters.

       Se uma aplicação for implantado através de uma única instância do
       servidor, use este subcomando para desativá-lo nessa instância enquanto
       deixa a própria instância ativada.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --timeout
           O time-out (em minutos) de espera antes de desativar a aplicação
           especificada. Este tempo permite um shutdown normal (suspensão) da
           aplicação especificada. O valor default é 30 minutos. O valor
           mínimo é 1 minuto.

       --name
           O nome da aplicação a ser desativada.

OPERANDOS
       target
           Este operando especifica a instância do servidor ou cluster no qual
           a aplicação será desativada. Os valores válidos são:

           ·   cluster_name - O nome de um cluster do alvo.

           ·   instance_name - O nome de uma instância do servidor do alvo.

EXEMPLOS
       Example 1, Desativando uma Aplicação para o Balanceamento de Carga
           Este exemplo desativa uma aplicação para o balanceamento de carga

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                         26 ago 2010   disable-http-lb-application(1)
