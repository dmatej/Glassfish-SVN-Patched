enable-http-lb-server(1)Subcomandos do utilitário asadenable-http-lb-server(1)

NOME
       enable-http-lb-server - ativa um servidor ou cluster desativado
       anteriormente gerenciado por um balanceador de carga

SINOPSE
           enable-http-lb-server [--help] target

DESCRIçãO
       O subcomando enable-http-lb-server ativa uma instância standalone do
       servidor ou cluster para o balanceamento de carga. Por default, o
       balanceamento de carga é ativado para instância e clusters.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Esse operando especifica quais instâncias de servidor e clusters
           devem ser ativados. Os valores válidos são:

           ·   cluster_name - O nome de um cluster do alvo.

           ·   instance_name - O nome de uma instância do servidor do alvo.

EXEMPLOS
       Example 1, Ativando um Cluster para Balanceamento de Carga
           Este exemplo permite o balanceamento de carga para um cluster com o
           nome mycluster.

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                         26 ago 2010         enable-http-lb-server(1)
