export-http-lb-config(1)    asadmin 公用程式子命令;   export-http-lb-config(1)

參考名
       export-http-lb-config - 會將負載平衡程式組態或負載平衡程式匯出至檔案

概要
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

描述
       export-http-lb-config
       子命令會將負載平衡程式組態或負載平衡程式匯出至負載平衡程式外掛程式能夠使用的檔案。預設檔案名稱為
       loadbalancer.xml，但是您可以指定不同的名稱。匯出之後，您可以在套用組態變更之前，手動將匯出的檔案複製到負載平衡程式外掛程式位置。--target
       選項使其可以為叢集或獨立執行處理產生 loadbalancer.xml，不需要在目標的
       domain.xml 中手動建立 lb-c nfig 或 load-balancer 元素。

       若要將變更套用至負載平衡程式而不手動複製組態檔案，請使用 create-http-lb
       將負載平衡程式組態成自動套用變更。如果使用 create-http-lb
       子命令，則不需要使用 export-http-lb-config。

           注意
           ┌────────────────────────────────────────┐
           │備註                                    │
           │                                        │
           │           此子命令僅適用於 Oracle      │
           │           GlassFish                    │
           │           Server。此子命令不適用於     │
           │           GlassFish Server Open Source │
           │           Edition。                    │
           └────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --config
           指定要匯出哪個負載平衡程式組態。

           指定負載平衡程式組態或負載平衡程式。同時指定兩者會導致錯誤。

       --lbname
           指定要匯出的負載平衡程式。

           指定負載平衡程式組態或負載平衡程式。同時指定兩者會導致錯誤。

       --retrievefile
           如果設為 true，則會從遠端機器擷取 loadbalancer.xml 檔案。預設值為
           false。

       --target
           指定負載平衡程式組態的匯出目標。如果未指定目標，負載平衡程式組態就會匯出至
           file_name 指定的位置。

運算元
       file_name
           指定匯出之組態的檔案名稱及位置。

           ·   如果您是指定目錄 (相對或絕對)
               而不是指定檔案名稱，就會在指定的目錄中建立名為
               loadbalancer.xml.load_balancer_config_name 的檔案。在 Microsoft
               Windows 系統上，路徑必須位於引號中。

           ·   如果以相對或絕對路徑指定檔案名稱，則會在您指定的目錄中，以您指定的名稱建立檔案。

           ·   如果您指定檔案名稱但不指定目錄，則會在目前的工作目錄中，以該名稱建立檔案。

           ·   如果您沒有指定此運算元，預設值是建立於 domain-dir/generated
               目錄中名為 loadbalancer.xml.load_balancer_config_name 的檔案。

       target
           指定組態的匯出目標。

           有效值包括

           ·   cluster_name - 指定叢集與其伺服器執行處理。

           ·   stand-alone_instance_name - 指定特定伺服器執行處理。

範例
       範例 1, 在 UNIX 上匯出負載平衡程式組態
           以下範例可將名為 mycluster-http-lb-config 的負載平衡組態匯出至
           /Sun/AppServer 目錄中名為 loadbalancer.xml 的檔案。

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       範例 2, 在 Windows 上匯出負載平衡程式組態
           下列範例可將名為 mycluster-http-lb-config 的負載平衡組態匯出至
           Microsoft Windows 系統上 C:\Sun\AppServer 目錄中名為
           loadbalancer.xml 的檔案。

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日      export-http-lb-config(1)
