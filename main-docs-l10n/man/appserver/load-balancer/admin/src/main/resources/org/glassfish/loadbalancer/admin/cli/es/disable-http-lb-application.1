disable-http-lb-applicatSubcomandos de la Utiliddisable-http-lb-application(1)

NOMBRE DE REFERENCIA
       disable-http-lb-application - Desactiva una aplicación gestionada por
       un equilibrador de carga.

SINOPSIS
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

DESCRIPCIÓN
       El comando disable-http-lb-application desactiva una aplicación para el
       equilibrio de carga. La aplicación desactivada pasa a estar fuera de
       línea para el equilibrio de carga con un impacto mínimo en los
       usuarios. La desactivación de una aplicación proporciona un control más
       detallado que la desactivación de una instancia de servidor y es más
       útil cuando un cluster aloja varias aplicaciones independientes.

       Una vez que la aplicación está desactivada y los cambios se han
       aplicado al equilibrador de carga, las solicitudes a la aplicación no
       se reenvían al destino. Las sesiones existentes siguen accediendo a la
       aplicación hasta que se alcanza el timeout. Este proceso se conoce como
       quiescencia.

       Si una aplicación se despliega en varios clusters, utilice este
       subcomando para desactivarla en un cluster y dejarla activada en los
       otros.

       Si una aplicación se despliega en una única instancia del servidor,
       utilice este subcomando para desactivarla en esa instancia y dejar la
       propia instancia activada.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --timeout
           Timeout (en minutos) antes de desactivar la aplicación
           especificada. Este tiempo permite el cierre normal (quiescencia) de
           la aplicación especificada. El valor por defecto es de 30 minutos.
           El valor mínimo es de 1 minuto.

       --name
           El nombre de la aplicación que se va a desactivar.

OPERANDOS
       target
           Este operando especifica la instancia del servidor o del cluster en
           la que se debe desactivar la aplicación. Los valores válidos son:

           ·   cluster_name: nombre de un cluster de destino.

           ·   instance_name: nombre de una instancia de servidor de destino.

EJEMPLOS
       Ejemplo 1, Desactivación de una Aplicación para el Equilibrado de Carga
           En este ejemplo, se desactiva una aplicación para el equilibrio de
           la carga.

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                    26 de agosto de 201disable-http-lb-application(1)
