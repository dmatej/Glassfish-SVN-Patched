delete-http-health-checkUnterbefehle des asadmin-delete-http-health-checker(1)

NAME
       delete-http-health-checker - löscht die Integritätsprüfung für eine
       angegebene Load Balancer-Konfiguration

ZUSAMMENFASSUNG
           delete-http-health-checker [--help] [--config config_name]
            target

BESCHREIBUNG
       Der Unterbefehl delete-http-health-checker löscht die
       Integritätsprüfung aus einer Load Balancer-Konfiguration. Eine
       Integritätsprüfung dient zur Überprüfung einer bestimmten Kombination
       aus Ziel- und Load Balancer-Konfiguration.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --config
           Die Load Balancer-Konfiguration, aus der Sie die Integritätsprüfung
           löschen.

OPERANDEN
       target
           Gibt das Ziel an, aus dem Sie die Integritätsprüfung löschen.

           Gültige Werte sind:

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

BEISPIELE
       Beispiel 1, Integritätsprüfung aus einer Load Balancer-Konfiguration
       löschen
           In diesem Beispiel wird die Integritätsprüfung für die Load
           Balancer-Konfiguration mit dem Namen mycluster-http-lb-config auf
           einem Cluster mit dem Namen mycluster gelöscht.

               asadmin> delete-http-health-checker --user admin
               --passwordfile password.txt --config mycluster-http-lb-config mycluster

               Command delete-http-health-checker executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-health-checker(1)

       asadmin(1M)

Java EE 7                        25. Aug. 2010   delete-http-health-checker(1)
