enable-http-lb-applicatiUnterbefehle des asadmin-enable-http-lb-application(1)

NAME
       enable-http-lb-application - aktiviert eine zuvor deaktivierte
       Anwendung, die von einem Load Balancer verwaltet wird

ZUSAMMENFASSUNG
           enable-http-lb-application [--help] --name application_name target

BESCHREIBUNG
       Der Unterbefehl enable-http-lb-application aktiviert Load Balancing für
       Anwendungen, die auf einer Standalone-Instanz oder einem Cluster
       bereitgestellt werden. Sie können Load Balancing für eine Anwendung auf
       allen Instanzen in einem Cluster oder auf einer einzelnen
       Standalone-Serverinstanz aktivieren. Standardmäßig ist Load Balancing
       für Anwendungen aktiviert.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --name
           Der Name der Anwendung, die aktiviert werden soll.

OPERANDEN
       target
           Dieser Operand gibt an, in welcher Serverinstanz oder in welchem
           Cluster die Anwendung aktiviert werden soll. Gültige Werte sind:

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

BEISPIELE
       Example 1, Load Balancing für eine Anwendung aktivieren
           In diesem Beispiel wird für eine Anwendung namens webapps-simple
           Load Balancing für ein Cluster namens mycluster aktiviert.

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                        26. Aug. 2010   enable-http-lb-application(1)
