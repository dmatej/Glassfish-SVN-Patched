list-http-lb-configs(1) Comandi secondari della utilit list-http-lb-configs(1)

NOME
       list-http-lb-configs - elenca le configurazioni del load balancer

SINTASSI
           list-http-lb-configs [--help] [target]

DESCRIZIONE
       Il comando secondario list-http-lb-configs elenca le configurazioni del
       load balancer. È possibile elencare tutte le configurazioni oppure
       ottenere una lista in base al cluster o all'istanza server a cui fanno
       riferimento.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Elenca i load balancer in base alla destinazione. Di seguito sono
           riportati i valori validi.

           ·   cluster_name: il nome di un cluster di destinazione.

           ·   instance_name: il nome di un'istanza server di destinazione.

ESEMPI
       Esempio 1, Visualizzazione della lista delle configurazioni del load
       balancer senza destinazione
           In questo esempio vengono elencate tutte le configurazioni del load
           balancer definite per tutti i cluster e le istanze GlassFish
           Server.

               asadmin> list-http-lb-configs

               mycluster-http-lb-config
               serverinstlb
               Command list-http-lb-configs executed successfully.

       Esempio 2, Visualizzazione della lista delle configurazioni del load
       balancer per una destinazione specifica
           In questo esempio viene elencata la configurazione del load
           balancer definita per un cluster denominato mycluster.

               asadmin> list-http-lb-configs mycluster

               mycluster-http-lb-config
               Command list-http-lb-configs executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-http-lb-config(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                       26 agosto 2010         list-http-lb-configs(1)
