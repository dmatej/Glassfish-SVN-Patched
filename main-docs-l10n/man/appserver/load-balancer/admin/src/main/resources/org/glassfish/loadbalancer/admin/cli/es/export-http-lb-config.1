export-http-lb-config(1)Subcomandos de la Utilidad asaexport-http-lb-config(1)

NOMBRE DE REFERENCIA
       export-http-lb-config - Exporta la configuración del equilibrador de
       carga o un equilibrador de carga a un archivo.

SINOPSIS
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

DESCRIPCIÓN
       El subcomando export-http-lb-config exporta un equilibrador de carga o
       una configuración de equlibrador de carga a un archivo que el
       complemento de equlibrador de carga puede usar. El nombre de archivo
       por defecto es loadbalancer.xml, pero puede especificar otro nombre.
       Una vez exportado, copie de forma manual el archivo exportado al
       complemento de equilibrador de carga en la ubicación antes de que se
       apliquen los cambios de configuración. La opción --target permite
       generar un loadbalancer.xml para instancias en cluster o independientes
       sin tener que crear manualmente los elementos lb-config o
        load-balancer en el archivo dominio.xml del destino.

       Para aplicar los cambios en el equilibrador de carga sin copiar
       manualmente el archivo de configuración, configure el equilibrador de
       carga mediante create-http-lb para aplicar cambios automáticamente . Si
       utiliza el subcomando
        create-http-lb, no es necesario que utilice
        export-http-lb-config.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --config
           Especifica qué configuración de equilibrador de carga se debe
           exportar.

           Especifique una configuración de equilibrador de carga o bien un
           equilibrador de carga. Si especifica ambos se produce un error.

       --lbname
           Especifica el equilibrador de carga que se debe exportar.

           Especifique una configuración de equilibrador de carga o bien un
           equilibrador de carga. Si especifica ambos se produce un error.

       --retrievefile
           Si se define en true, recupera el archivo loadbalancer.xml de la
           máquina remota. El valor por defecto es false.

       --target
           Especifica el destino al que se exportará la configuración de
           equilibrado de carga. Si no se especifica destino, la configuración
           de equilibrador de carga se exporta a la ubicación especificada en
           file_name.

OPERANDOS
       file_name
           Especifica el nombre de archivo de la configuración exportada y su
           ubicación.

           ·   Si especifica un directorio (relativo o absoluto) pero no un
               nombre de archivo, se creará un archivo llamado
               loadbalancer.xml.load_balancer_config_name en el directorio
               especificado. En los sistemas Microsoft Windows, la ruta debe
               estar entre comillas.

           ·   Si especifica un nombre de archivo en una ruta relativa o
               absoluta, se crea un archivo con ese nombre en el directorio
               especificado.

           ·   Si especifica un nombre de archivo pero no un directorio, se
               crea un archivo con ese nombre en el directorio de trabajo
               actual.

           ·   Si no especifica este operando, el valor por defecto es un
               archivo llamado loadbalancer.xml.oad_balancer_config_name que
               se crea en el directorio domain-dir/generated.

       target
           Especifica el destino al que se exportará la configuración.

           Los valores válidos son:

           ·   cluster_name: especifica un cluster y sus instancias de
               servidor.

           ·   stand-alone_instance_name: especifica una instancia de servidor
               específica.

EJEMPLOS
       Ejemplo 1, Exportación de una configuración de equilibrador de carga en
       UNIX
           En el siguiente ejemplo se exporta una configuración de equilibrio
           de carga llamada mycluster-http-lb-config a un archivo llamado
           loadbalancer.xml en el directorio /Sun/AppServer.

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       Ejemplo 2, Exportación de una configuración de equilibrador de carga en
       Windows
           En el siguiente ejemplo se exporta una configuración de
           equilibrador de carga llamada mycluster-http-lb-config a un archivo
           llamado loadbalancer.xml en el directorio C:\Sun\AppServer de un
           sistema Microsoft Windows.

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                    26 de agosto de 2010     export-http-lb-config(1)
