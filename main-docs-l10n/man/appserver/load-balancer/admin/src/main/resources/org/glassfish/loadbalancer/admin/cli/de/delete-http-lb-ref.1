delete-http-lb-ref(1)   Unterbefehle des asadmin-Utili   delete-http-lb-ref(1)

NAME
       delete-http-lb-ref - löscht das Cluster oder die Serverinstanz aus
       einem Load Balancer

ZUSAMMENFASSUNG
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

BESCHREIBUNG
       Verwenden Sie den Unterbefehl delete-http-lb-ref, um eine Referenz zu
       einem Cluster oder einer Standalone-Serverinstanz aus einer Load
       Balancer-Konfiguration oder einem Load Balancer zu löschen. Damit Sie
       keine Benutzeranforderungen unterbrechen, stellen Sie sicher, dass die
       Standalone-Serverinstanz oder alle Serverinstanzen im Cluster
       deaktiviert sind, bevor Sie sie aus der Load Balancer-Konfiguration
       entfernen. Wenn die Option force auf "true" gesetzt ist, werden die
       Referenzen auch dann gelöscht, wenn Serverinstanzen oder Cluster
       aktiviert sind.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --config
           Gibt an, aus welcher Load Balancer-Konfiguration Cluster- und
           Serverinstanzreferenzen gelöscht werden.

           Geben Sie entweder eine Load Balancer-Konfiguration oder einen Load
           Balancer an. Wird beides angegeben, tritt ein Fehler auf.

       --lbname
           Gibt die Load Balancer-Konfiguration an, aus der Cluster- und
           Serverinstanzreferenzen gelöscht werden.

           Geben Sie entweder eine Load Balancer-Konfiguration oder einen Load
           Balancer an. Wird beides angegeben, tritt ein Fehler auf.

       --force
           Wenn force auf "true" gesetzt ist, werden die Referenzen auch dann
           gelöscht, wenn derzeit aktivierte Anwendungen oder Instanzen
           vorhanden sind. Der Standardwert ist "false".

OPERANDEN
       target
           Gibt an, welches Cluster oder welche Instanz aus dem Load Balancer
           entfernt wird. Gültige Werte sind:

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

BEISPIELE
       Example 1, Clusterreferenzen aus einer Load Balancer-Konfiguration
       löschen
           In diesem Beispiel wird die Referenz zum Cluster namens cluster2
           aus einer Load Balancer-Konfiguration namens
           mycluster-http-lb-config gelöscht.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                        25. Aug. 2010           delete-http-lb-ref(1)
