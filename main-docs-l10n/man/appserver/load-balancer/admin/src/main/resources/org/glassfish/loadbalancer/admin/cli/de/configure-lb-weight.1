configure-lb-weight(1)  Unterbefehle des asadmin-Utili  configure-lb-weight(1)

NAME
       configure-lb-weight - legt Load Balancing-Gewichtungen für geclusterte
       Instanzen fest

ZUSAMMENFASSUNG
           configure-lb-weight [--help] --cluster cluster_name
            instance-name=weight[:instance-name=weight]

BESCHREIBUNG
       Der Unterbefehl configure-lb-weight dient dazu, den Serverinstanzen in
       einem Cluster eine Gewichtung zuzuweisen. Gewichtungen können für
       HTTP-, RMI/IIOP- und JMS-Load Balancing verwendet werden. Für den
       HTTP-Load Balancer werden die Gewichtungen nur dann verwendet, wenn die
       Policy für den Load Balancer auf weighted-round-robin gesetzt ist. Die
       Policy für den Load Balancer wird im Unterbefehl create-http-lb-ref
       oder set festgelegt.

       Verwenden Sie die Gewichtung, um die Lasten, die an verschiedene
       Instanzen im Cluster weitergeleitet werden, zu variieren. Wenn sich
       beispielsweise eine Instanz auf einem Rechner mit mehr Kapazität
       befindet, geben Sie dieser Instanz eine höhere Gewichtung, damit der
       Load Balancer mehr Anforderungen an diese Instanz sendet. Die
       Standardgewichtung ist 100. Wenn alle Instanzen die Standardgewichtung
       haben, führt der Load Balancer ein einfaches zyklisches Load Balancing
       (Round Robin Load Balancing) durch.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --cluster
           Der Name des Clusters.

OPERANDEN
       instance-name=weight
           Der Name der Instanz und die Gewichtung, die Sie der Instanz
           zuweisen. Die Gewichtung muss eine Ganzzahl sein. Die Paare der
           Instanzen und Gewichtungen werden durch Doppelpunkte getrennt.
           Beispiel: instance1=1:instance2=4 bedeutet, dass eine von fünf
           Anforderungen an instance1 geht und vier von fünf Anforderungen an
           instance2 gehen. Eine Gewichtung von 1 ist der Standardwert.

BEISPIELE
       Beispiel 1, Zuweisen von Load Balancer-Gewichtungen zu Clusterinstanzen
           Der folgende Unterbefehl dient dazu, Gewichtungen von 1, 1 und 2
           den Instanzen i1, i2 und i3 im Cluster cluster1 zuzuweisen.

               asadmin> configure-lb-weight --cluster cluster1 i1=1:i2=1:i3=2
               Command configure-lb-weight executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb-ref(1)create-cluster(1)

       asadmin(1M)

Java EE 7                        25. Aug. 2010          configure-lb-weight(1)
