disable-http-lb-server(1Sous-commandes de l'utilitairdisable-http-lb-server(1)

NOM
       disable-http-lb-server - désactive un serveur ou un cluster géré par un
       équilibreur de charge

SYNTAXE
           disable-http-lb-server [--help] [--timeout 30]
            target

DESCRIPTION
       La sous-commande disable-http-lb-server désactive un serveur autonome
       ou un cluster de serveurs pour l'équilibrage de charge. L'instance de
       serveur ou le cluster désactivé est mis hors ligne pour l'équilibrage
       de charge avec un impact minimal pour les utilisateurs.

       Une fois la cible désactivée et les modifications appliquées à
       l'équilibreur de charge, ce dernier arrête d'affecter de nouvelles
       demandes à la cible. Les demandes relatives à des sessions ayant été
       créées avant la désactivation de la cible continuent à être affectées à
       cette cible jusqu'à l'expiration du délai d'attente. Ce processus est
       appelé mise au ralenti.

       Les modifications sont automatiquement appliquées à l'équilibreur de
       charge. Vous pouvez également exporter manuellement la configuration à
       l'aide de la sous-commande export-http-lb-config et la copier dans
       l'équilibreur de charge.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --timeout
           Délai d'attente (en minutes) au-delà duquel la cible spécifiée est
           désactivée. Il permet l'arrêt flexible (mise au ralenti) de la
           cible spécifiée. La valeur par défaut est 30 minutes. La valeur
           minimale est 1 minute.

OPÉRANDES
       target
           Cet opérande spécifie les instances de serveur et les clusters à
           désactiver. Les valeurs valides sont les suivantes :

           ·   cluster_name : nom d'un cluster cible.

           ·   instance_name : nom d'une instance de serveur cible.

EXEMPLES
       Exemple 1, Désactivation d'un cluster pour l'équilibrage de charge
           Dans cet exemple, l'équilibrage de charge pour le cluster nommé
           mycluster est désactivé.

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

Java EE 7                        26 août 2010        disable-http-lb-server(1)
