disable-http-lb-server(1Subcomandos de la Utilidad asdisable-http-lb-server(1)

NOMBRE DE REFERENCIA
       disable-http-lb-server - Desactiva un servidor o un cluster gestionado
       por un equilibrador de carga.

SINOPSIS
           disable-http-lb-server [--help] [--timeout 30]
            target

DESCRIPCIÓN
       El subcomando disable-http-lb-server desactiva un servidor
       independiente o un cluster de servidores para el equilibrio de carga.
       La instancia de servidor o el cluster desactivado pasa a estar fuera de
       línea para equilibrar la carga con un impacto mínimo en los usuarios.

       Una vez el destino está desactivado y los cambios se han aplicado al
       equilibrador de carga, éste deja de asignar nuevas solicitudes al
       destino. Las solicitudes de sesión con sesiones creadas antes de
       desactivar el destino siguen asignadas a ese destino hasta que se haya
       agotado el timeout. Este proceso se conoce como quiescencia.

       Los cambios se aplican al equilibrador de carga de forma automática.
       También se puede exportar manualmente la configuración con
       export-http-lb-config y copiándola en el equilibrador de carga.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --timeout
           Timeout (en minutos) que se debe esperar antes de desactivar el
           destino especificado. Este tiempo permite el cierre normal
           (quiescencia) de la aplicación especificada. El valor por defecto
           es de 30 minutos. El valor mínimo es de 1 minuto.

OPERANDOS
       target
           Este operando especifica las instancias de servidor y clusters que
           se deben desactivar. Los valores válidos son:

           ·   cluster_name: nombre de un cluster de destino.

           ·   instance_name: nombre de una instancia de servidor de destino.

EJEMPLOS
       Ejemplo 1, Desactivación de un Cluster para Equilibrio de Carga
           En este ejemplo se desactiva el equilibrio de carga en un cluster
           denominado mycluster.

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

Java EE 7                    26 de agosto de 2010    disable-http-lb-server(1)
