delete-http-lb-ref(1)       asadmin 实用程序子命令       delete-http-lb-ref(1)

名称
       delete-http-lb-ref - 从负载平衡器中删除集群或服务器实例

提要
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

说明
       使用 delete-http-lb-ref
       子命令从负载平衡器配置或负载平衡器中删除对集群或独立服务器实例的引用。为了不中断用户请求，请确保在将集群中的独立服务器实例或所有服务器实例从负载平衡器配置中删除之前已将其禁用。如果
       force
       选项设置为“真”，即使服务器实例或集群处于启用状态，也会删除相应的引用。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --config
           指定从中删除集群和服务器实例引用的负载平衡器配置。

           指定负载平衡器配置或负载平衡器。同时指定这两者会导致出现错误。

       --lbname
           指定从中删除集群和服务器实例引用的负载平衡器。

           指定负载平衡器配置或负载平衡器。同时指定这两者会导致出现错误。

       --force
           如果 force
           设置为“真”，即使当前存在启用的应用程序或实例，也会删除相应的引用。默认值为“假”。

操作数
       target
           指定要从负载平衡器中删除的集群或实例。有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 从负载平衡器配置中删除集群引用
           此示例从名为 mycluster-http-lb-config 的负载平衡器配置中删除对名为
           cluster2 的集群的引用。

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日         delete-http-lb-ref(1)
