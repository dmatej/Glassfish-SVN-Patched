export-http-lb-config(1)  asadmin 유틸리티 하위 명령; export-http-lb-config(1)

제목
       export-http-lb-config - 로드 밸런서 구성 또는 로드 밸런서를 파일로
       익스포트

시놉시스
           export-http-lb-config [--help] --config config_name | --lbname load_balancer_name [--target target] [--retrievefile=false] [file_name]

설명
       export-http-lb-config 하위 명령은 로드 밸런서 플러그인이 사용할 수 있는
       파일로 로드 밸런서 구성 또는 로드 밸런서를 익스포트합니다. 기본 파일
       이름은 loadbalancer.xml이지만 다른 이름을 지정할 수 있습니다.
       익스포트하고 나면 구성 변경 사항을 적용하기 전에 익스포트된 파일을
       수동으로 로드 밸런서 플러그인 위치에 복사합니다.  --target 옵션을
       사용하면 수동으로 대상의 domain.xml에 lb-config 또는 load-balancer
       요소를 생성할 필요 없이 클러스터 또는 독립형 인스턴스에 대해
       loadbalancer.xml을 생성할 수 있습니다.

       구성 파일을 수동으로 복사하지 않고 로드 밸런서에 변경 사항을 적용하려면
       create-http-lb로 변경 사항을 자동으로 적용하도록 로드 밸런서를
       구성합니다.  create-http-lb 하위 명령을 사용하는 경우
       export-http-lb-config를 사용할 필요가 없습니다.

           참고
           ┌────────────────────────────────────────┐
           │주                                      │
           │                                        │
           │           이 하위 명령은 Oracle        │
           │           GlassFish Server에만 적용할  │
           │           수 있습니다. 이 하위 명령은  │
           │           GlassFish Server Open Source │
           │           Edition에 적용할 수          │
           │           없습니다.                    │
           └────────────────────────────────────────┘

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --config
           익스포트할 로드 밸런서 구성을 지정합니다.

           로드 밸런서 구성 또는 로드 밸런서를 지정합니다. 둘 다 지정하면
           오류가 발생합니다.

       --lbname
           익스포트할 로드 밸런서를 지정합니다.

           로드 밸런서 구성 또는 로드 밸런서를 지정합니다. 둘 다 지정하면
           오류가 발생합니다.

       --retrievefile
           true로 설정하면 원격 시스템에서 loadbalancer.xml 파일을 검색합니다.
           기본값은 false입니다.

       --target
           로드 밸런서 구성을 익스포트할 대상을 지정합니다. 대상을 지정하지
           않으면 file_name으로 지정된 위치로 로드 밸런서 구성을
           익스포트합니다.

피연산자
       file_name
           파일 이름 및 익스포트된 구성의 위치를 지정합니다.

           ·   파일 이름이 아닌 디렉토리(상대 또는 절대)를 지정하면 이름이
               loadbalancer.xml.load_balancer_config_name인 파일이 지정한
               디렉토리에 생성됩니다. Microsoft Windows 시스템의 경우 경로를
               따옴표로 묶어야 합니다.

           ·   상대 경로나 절대 경로로 파일 이름을 지정하면 지정한 디렉토리에
               지정한 이름으로 파일이 생성됩니다.

           ·   파일 이름을 지정하고 디렉토리는 지정하지 않으면 현재 작업
               디렉토리에 해당 이름으로 파일이 생성됩니다.

           ·   이 피연산자를 지정하지 않으면 기본값은 domain-dir/generated
               디렉토리에 생성된
               loadbalancer.xml.load_balancer_config_name이라는 파일입니다.

       target
           구성을 익스포트할 대상을 지정합니다.

           적합한 값은 다음과 같습니다.

           ·   cluster_name- 클러스터 및 해당 서버 인스턴스를 지정합니다.

           ·   stand-alone_instance_name- 특정 서버 인스턴스를 지정합니다.

예제
       예 1, UNIX에서 로드 밸런서 구성 익스포트
           다음 예제는 mycluster-http-lb-config라는 로드 균형 조정 구성을
           /Sun/AppServer 디렉토리의 loadbalancer.xml이라는 파일로
           익스포트합니다.

               asadmin> export-http-lb-config --config mycluster-http-lb-config /Sun/AppServer/loadbalancer.xml
               Command export-http-lb-config executed successfully.

       예 2, Windows에서 로드 밸런서 구성 익스포트
           다음 예제는 mycluster-http-lb-config라는 로드 균형 조정 구성을
           Microsoft Windows 시스템의 C:\Sun\AppServer 디렉토리에 있는
           loadbalancer.xml이라는 파일로 익스포트합니다.

               asadmin> export-http-lb-config --config mycluster-http-lb-config "C:\Sun\AppServer\loadbalancer.xml"
               Command export-http-lb-config executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-http-lb(1), create-http-lb-config(1), list-http-lb-configs(1)

       asadmin(1M)

Java EE 7                       2010년 8월 26일       export-http-lb-config(1)
