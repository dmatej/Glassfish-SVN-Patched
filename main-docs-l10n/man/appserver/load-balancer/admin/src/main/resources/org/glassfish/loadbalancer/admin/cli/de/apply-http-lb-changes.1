apply-http-lb-changes(1)Unterbefehle des asadmin-Utiliapply-http-lb-changes(1)

NAME
       apply-http-lb-changes - wendet Änderungen der Load
       Balancer-Konfiguration auf den Load Balancer an

ZUSAMMENFASSUNG
           apply-http-lb-changes [--help]
            lb-name

BESCHREIBUNG
       Verwenden Sie den Unterbefehl applyhttp-lb-changes, um die Änderungen
       in der Load Balancer-Konfiguration auf den physischen Load Balancer
       anzuwenden.  Der Load Balancer muss bereits vorhanden sein. Um einen
       physischen Load Balancer zu erstellen, verwenden Sie den Unterbefehl
       create-http-lb.

       Dieser Unterbefehl ist nur auf Oracle GlassFish Server anwendbar.
       Dieser Unterbefehl ist nicht auf GlassFish Server Open Source Edition
       anwendbar.

KONTEXT
       Der Load Balancer verteilt die Arbeitslast auf mehrere Oracle GlassFish
       Server-Instanzen, um den Gesamtdurchsatz des Systems zu verbessern. Der
       Load Balancer wird darüber hinaus für das Failover von Anforderungen
       von einer Serverinstanz auf eine andere Instanz verwendet. Damit
       HTTP-Sessioninformationen erhalten bleiben, konfigurieren Sie die
       HTTP-Sessionpersistenz.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Das Load Balancer-Plug-in    │
           │           ist nur für Oracle GlassFish │
           │           Server und nicht für         │
           │           GlassFish Server Open Source │
           │           Edition verfügbar. Bei       │
           │           GlassFish Server Open Source │
           │           Edition kann das Modul       │
           │           mod_jk zum Konfigurieren von │
           │           Load Balancing auf dem       │
           │           Apache HTTP-Server verwendet │
           │           werden.                      │
           └────────────────────────────────────────┘

       Weitere Informationen zum Konfigurieren von Load Balancing bei
       GlassFish Server finden Sie in der Onlinehilfe der GlassFish Server
       Administrationskonsole.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       lb-name
           Der Name des Load Balancers, auf den die Änderungen angewendet
           werden. Der Load Balancer muss bereits vorhanden sein. Die
           Erstellung kann mit dem Unterbefehl create-http-lb erfolgen.

BEISPIELE
       Example 1, Verwendung des Unterbefehls -http-lb-changes.
           Dieses Beispiel gilt für Konfigurationsänderungen an einem Load
           Balancer namens mylb.

               asadmin> apply-http-lb-changes mylb
               Command apply-http-lb-changes executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-http-lb(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                        22. Dez. 2010        apply-http-lb-changes(1)
