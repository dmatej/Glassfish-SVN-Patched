list-http-lb-configs(1)     asadmin 实用程序子命令     list-http-lb-configs(1)

名称
       list-http-lb-configs - 列出负载平衡器配置

提要
           list-http-lb-configs [--help] [target]

说明
       list-http-lb-configs
       子命令列出负载平衡器配置。列出所有负载平衡器配置或者按它们引用的集群或服务器实例列出。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           按目标列出负载平衡器。有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 列出负载平衡器配置 (不使用目标)
           此示例列出针对所有 GlassFish Server
           集群和实例定义的所有负载平衡器配置。

               asadmin> list-http-lb-configs

               mycluster-http-lb-config
               serverinstlb
               Command list-http-lb-configs executed successfully.

       Example 2, 列出特定目标的负载平衡器配置
           此示例列出针对名为 mycluster 的集群定义的负载平衡器配置。

               asadmin> list-http-lb-configs mycluster

               mycluster-http-lb-config
               Command list-http-lb-configs executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-http-lb-config(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日       list-http-lb-configs(1)
