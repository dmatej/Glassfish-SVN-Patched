configure-lb-weight(1)  Subcomandos de la Utilidad asa  configure-lb-weight(1)

NOMBRE DE REFERENCIA
       configure-lb-weight - Define medias ponderadas de equilibrio de carga
       para instancias en cluster.

SINOPSIS
           configure-lb-weight [--help] --cluster cluster_name
            instance-name=weight[:instance-name=weight]

DESCRIPCIÓN
       El subcomando configure-lb-weight asigna una media ponderada a las
       instancias de servidor en un cluster. Las medias ponderadas se pueden
       utilizar para el equilibrio de carga HTTP, RMI/IIOP y JMS. Para el
       equilibrador de carga HTTP, las medias ponderadas sólo se utilizan si
       la política del equilibrador se define en weighted-round-robin. La
       política del equilibrador de carga se define en el subcomando
       create-http-lb-ref o en el subcomando set.

       Utilice la media ponderada para variar la carga que se dirige a las
       distintas instancias del cluster. Por ejemplo, si una instancia está en
       una máquina con más capacidad, asígnele una mayor media ponderada para
       que el equilibrador de carga envíe más solicitudes a dicha instancia.
       La media ponderada por defecto es 100. Si todas las instancias tienen
       la media ponderada por defecto, el equilibrador realiza un simple
       equilibrio de carga por asignación en rueda.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --cluster
           Nombre del cluster.

OPERANDOS
       instance-name=weight
           Nombre de la instancia y media ponderada que le asigna. La media
           ponderada debe ser un número entero. Los pares de instancias y
           medias ponderadas están separados por dos puntos. Por ejemplo,
           instance1=1:instance2=4 significa que, de cada 5 solicitudes, una
           vaya a instance1 y cuatro vayan a instance2. 1 es el valor de media
           ponderada por defecto.

EJEMPLOS
       Ejemplo 1, Asignación de Medias Ponderadas del Equilibrador de Carga a
       las Instancias de Cluster
           El siguiente subcomando asigna las medias ponderadas de 1, 1 y 2 a
           las instancias i1, i2 e i3 en el cluster cluster1.

               asadmin> configure-lb-weight --cluster cluster1 i1=1:i2=1:i3=2
               Command configure-lb-weight executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-http-lb-ref(1)create-cluster(1)

       asadmin(1M)

Java EE 7                    25 de agosto de 2010       configure-lb-weight(1)
