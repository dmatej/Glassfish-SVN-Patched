disable-http-lb-application(asadmin 公用程式子命disable-http-lb-application(1)

參考名
       disable-http-lb-application - 停用負載平衡程式管理的應用程式

概要
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

描述
       disable-http-lb-application
       子命令會停用應用程式的負載平衡。停用的應用程式會離線以實現負載平衡，從而儘量降低對使用者的影響
       相對於停用伺服器執行處理，停用應用程式可以提供更好的控制詳細度，並且在叢集做為多個獨立應用程式的主機時最為有用。

       在停用應用程式並已將變更套用至負載平衡程式時，則不會將應用程式的新請求轉寄至目標。現有階段作業會繼續存取應用程式，直至達到逾時。此處理作業被稱為靜止。

       如果將應用程式建置在多個叢集上，使用此子命令可停用一個叢集中的應用程式，同時讓應用程式在其他叢集中保持啟用。

       如果將應用程式建置至單一伺服器執行處理，使用此子命令可停用該執行處理中的應用程式，同時讓該執行處理本身保持啟用。

           注意
           ┌────────────────────────────────────────┐
           │備註                                    │
           │                                        │
           │           此子命令僅適用於 Oracle      │
           │           GlassFish                    │
           │           Server。此子命令不適用於     │
           │           GlassFish Server Open Source │
           │           Edition。                    │
           └────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --timeout
           在停用指定應用程式之前所需等待的逾時
           (以分鐘為單位)。此時間允許正常關閉 (靜止) 指定的應用程式。預設值為
           30 分鐘。最小值為 1 分鐘。

       --name
           要停用的應用程式名稱。

運算元
       target
           此運算元指定要在其中停用應用程式的伺服器執行處理或叢集。有效值包括

           ·   cluster_name - 目標叢集名稱。

           ·   instance_name - 目標伺服器執行處理名稱。

範例
       範例 1, 停用應用程式的負載平衡
           此範例會根據負載平衡停用應用程式

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日disable-http-lb-application(1)
