enable-http-lb-application(1asadmin 实用程序子命令

名称
       enable-http-lb-application - 启用由负载平衡器管理的以前已禁用的应用程序

提要
           enable-http-lb-application [--help] --name application_name target

说明
       enable-http-lb-application
       子命令为独立实例或集群上部署的应用程序启用负载平衡。可为集群中所有实例上的应用程序启用负载平衡，也可为单个独立服务器实例上的应用程序启用负载平衡。默认情况下，已经为应用程序启用了负载平衡。

           注意
           ┌─────────────────────────────────────┐
           │注                                   │
           │                                     │
           │           该子命令仅适用于 Oracle   │
           │           GlassFish                 │
           │           Server。该子命令不适用于  │
           │           GlassFish Server 开源版。 │
           └─────────────────────────────────────┘

选项
       --help, -?
           显示子命令的帮助文本。

       --name
           要启用的应用程序的名称。

操作数
       target
           此操作数指定要对其启用应用程序的服务器实例或集群。有效值为：

           ·   cluster_name - 目标集群的名称。

           ·   instance_name - 目标服务器实例的名称。

示例
       Example 1, 为应用程序启用负载平衡
           此示例为名为 mycluster 的集群上名为 webapps-simple
           的应用程序启用负载平衡，使该应用程序能使用负载平衡。

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 26 日 enable-http-lb-application(1)
