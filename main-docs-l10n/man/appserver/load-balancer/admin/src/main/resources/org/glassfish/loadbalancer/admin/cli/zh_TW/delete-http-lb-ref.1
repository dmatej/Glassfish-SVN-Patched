delete-http-lb-ref(1)       asadmin 公用程式子命令       delete-http-lb-ref(1)

參考名
       delete-http-lb-ref - 刪除負載平衡程式中的叢集或伺服器執行處理

概要
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

描述
       使用 delete-http-lb-ref
       子命令可移除負載平衡程式組態或負載平衡程式中對叢集或獨立伺服器執行處理的參照。為了不中斷使用者請求，請確保該叢集中的獨立伺服器執行處理或所有伺服器執行處理，在您將它們從負載平衡程式組態移除之前已停用。如果
       force 選項設定為 true，即使伺服器執行處理或叢集已啟用，也會刪除參照。

           注意
           ┌────────────────────────────────────────┐
           │備註                                    │
           │                                        │
           │           此子命令僅適用於 Oracle      │
           │           GlassFish                    │
           │           Server。此子命令不適用於     │
           │           GlassFish Server Open Source │
           │           Edition。                    │
           └────────────────────────────────────────┘

選項
       --help, -?
           顯示子命令的說明文字。

       --config
           指定要從中刪除叢集和伺服器執行處理參照的負載平衡程式組態。

           指定負載平衡程式組態或負載平衡程式。同時指定兩者會導致錯誤。

       --lbname
           指定要從中刪除叢集和伺服器執行處理參照的負載平衡程式。

           指定負載平衡程式組態或負載平衡程式。同時指定兩者會導致錯誤。

       --force
           如果 force 設定為
           true，即使目前有啟用的應用程式或執行處理，也會刪除參照。預設值為
           false。

運算元
       target
           指定要從負載平衡程式移除的叢集或執行處理。有效值包括

           ·   cluster_name - 目標叢集名稱。

           ·   instance_name - 目標伺服器執行處理名稱。

範例
       範例 1, 刪除負載平衡程式組態中的叢集參照
           此範例會從名稱為 mycluster-http-lb-config
           的負載平衡程式組態中刪除對名稱為 cluster2 的叢集的參照。

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 25 日         delete-http-lb-ref(1)
