delete-http-lb-ref(1)   Sous-commandes de l'utilitaire   delete-http-lb-ref(1)

NOM
       delete-http-lb-ref - supprime le cluster ou l'instance de serveur d'un
       équilibreur de charge

SYNTAXE
           delete-http-lb-ref [--help] --config config_name | --lbname load_balancer_name
           [--force=false] target

DESCRIPTION
       La sous-commande delete-http-lb-ref permet d'enlever une référence à un
       cluster ou à une instance de serveur autonome dans une configuration
       d'équilibreur de charge ou un équilibreur de charge. Pour ne pas
       interrompre les demandes utilisateur, assurez-vous que l'instance de
       serveur autonome ou que toutes les instances de serveur du cluster sont
       désactivées avant de les enlever de la configuration d'équilibreur de
       charge. Si l'option force a la valeur True, les références sont
       supprimées même si des instances de serveur ou des clusters sont
       activés.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --config
           Cette option indique la configuration d'équilibreur de charge dans
           laquelle supprimer les références d'instance de serveur et de
           cluster.

           Spécifiez une configuration d'équilibreur de charge ou un
           équilibreur de charge. Le fait d'indiquer les deux entraîne une
           erreur.

       --lbname
           Cette option indique l'équilibreur de charge dans lequel supprimer
           les références d'instance de serveur et de cluster.

           Spécifiez une configuration d'équilibreur de charge ou un
           équilibreur de charge. Le fait d'indiquer les deux entraîne une
           erreur.

       --force
           Si l'option force a la valeur True, les références sont supprimées
           même si des applications ou des instances sont activées. La valeur
           par défaut est False.

OPÉRANDES
       target
           Cette option indique le cluster ou l'instance à supprimer de
           l'équilibreur de charge. Les valeurs valides sont les suivantes :

           ·   cluster_name : nom d'un cluster cible.

           ·   instance_name : nom d'une instance de serveur cible.

EXEMPLES
       Exemple 1, Suppression d'une référence à un cluster dans une
       configuration d'équilibreur de charge
           Cet exemple montre comment enlever la référence au cluster nommé
           cluster2 dans la configuration d'équilibreur
           mycluster-http-lb-config.

               asadmin> delete-http-lb-ref --config mycluster-http-lb-config cluster2

               Command delete-http-lb-ref executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                         25 aoû 2010            delete-http-lb-ref(1)
