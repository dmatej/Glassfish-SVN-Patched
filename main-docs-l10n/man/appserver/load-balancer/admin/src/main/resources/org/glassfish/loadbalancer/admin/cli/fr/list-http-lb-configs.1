list-http-lb-configs(1) Sous-commandes de l'utilitaire list-http-lb-configs(1)

NOM
       list-http-lb-configs - répertorie les configurations d'équilibreur de
       charge

SYNTAXE
           list-http-lb-configs [--help] [target]

DESCRIPTION
       La sous-commande list-http-lb-configs répertorie les configurations
       d'équilibreur de charge. Vous pouvez les répertorier toutes ou les
       répertorier en fonction du cluster ou de l'instance de serveur qu'elles
       référencent.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Répertorie les équilibreurs de charge par cible. Les valeurs
           valides sont les suivantes :

           ·   cluster_name : nom d'un cluster cible.

           ·   instance_name : nom d'une instance de serveur cible.

EXEMPLES
       Exemple 1, Liste des configurations d'équilibreur de charge sans cible
           Dans cet exemple, toutes les configurations d'équilibreur de charge
           définies pour l'ensemble des clusters et des instances GlassFish
           Server sont répertoriées.

               asadmin> list-http-lb-configs

               mycluster-http-lb-config
               serverinstlb
               Command list-http-lb-configs executed successfully.

       Exemple 2, Liste des configurations d'équilibreur de charge pour une
       cible spécifique
           Dans cet exemple, la configuration d'équilibreur de charge définie
           pour un cluster nommé mycluster est répertoriée.

               asadmin> list-http-lb-configs mycluster

               mycluster-http-lb-config
               Command list-http-lb-configs executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-http-lb-config(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                        26 août 2010          list-http-lb-configs(1)
