enable-http-lb-server(1)Sous-commandes de l'utilitaireenable-http-lb-server(1)

NOM
       enable-http-lb-server - active un serveur ou un cluster précédemment
       désactivé géré par un équilibreur de charge

SYNTAXE
           enable-http-lb-server [--help] target

DESCRIPTION
       La sous-commande enable-http-lb-server active une instance de serveur
       autonome ou un cluster pour l'équilibrage de charge. Par défaut,
       l'équilibrage de charge est activé pour les instances et les clusters.

           Note
           ┌───────────────────────────────────────┐
           │Remarque                               │
           │                                       │
           │           Cette sous-commande         │
           │           s'applique uniquement à     │
           │           Oracle GlassFish Server.    │
           │           Cette sous-commande n'est   │
           │           pas applicable à GlassFish  │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Cet opérande spécifie les instances de serveur et les clusters à
           activer. Les valeurs valides sont les suivantes :

           ·   cluster_name : nom d'un cluster cible.

           ·   instance_name : nom d'une instance de serveur cible.

EXEMPLES
       Exemple 1, Activation d'un cluster pour l'équilibrage de charge
           Dans cet exemple, l'équilibrage de charge est activé pour un
           cluster nommé mycluster.

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                        26 août 2010         enable-http-lb-server(1)
