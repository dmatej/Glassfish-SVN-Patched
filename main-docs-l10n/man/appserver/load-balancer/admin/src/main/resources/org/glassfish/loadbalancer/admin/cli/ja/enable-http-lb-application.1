enable-http-lb-applicaasadminユーティリティのサブenable-http-lb-application(1)

名前
       enable-http-lb-application -
       以前無効にしたロード・バランサで管理されるアプリケーションを有効にします

概要
           enable-http-lb-application [--help] --name application_name target

説明
       enable-http-lb-applicationサブコマンドは、スタンドアロン・インスタンスまたはクラスタ上にデプロイされたアプリケーションでロード・バランシングを有効にします。クラスタ内のすべてのインスタンス上、または単一のスタンドアロン・サーバー・インスタンス上のアプリケーションでロード・バランシングを有効にできます。デフォルトでは、ロード・バランシングはアプリケーションで有効になっています。

           注記
           ┌───────────────────────────────────────────────────────────────────┐
           │注意                                                               │
           │                                                                   │
           │           このサブコマンドは、Oracle                              │
           │           GlassFish                                               │
           │           Serverにのみ適用されます。このサブコマンドは、GlassFish │
           │           Server Open Source                                      │
           │           Editionには適用されません。                             │
           └───────────────────────────────────────────────────────────────────┘

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --name
           有効にするアプリケーションの名前。

オペランド
       target
           このオペランドでは、アプリケーションを有効にするサーバー・インスタンスまたはクラスタを指定します。有効な値は次のとおりです:

           ·   cluster_name- ターゲット・クラスタの名前。

           ·   instance_name- ターゲット・サーバー・インスタンスの名前。

例
       例 1, アプリケーションでのロード・バランシングの有効化
           この例では、webapps-simpleという名前のアプリケーションを有効にして、myclusterという名前のクラスタでロード・バランシングを使用します。

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                        2010年8月26日   enable-http-lb-application(1)
