delete-http-health-checkComandi secondari della udelete-http-health-checker(1)

NOME
       delete-http-health-checker - elimina lo strumento di controllo stato
       per la configurazione del load balancer specificata

SINTASSI
           delete-http-health-checker [--help] [--config config_name]
            target

DESCRIZIONE
       Il comando secondario delete-http-health-checker elimina lo strumento
       di controllo stato da una configurazione del load balancer. Uno
       strumento di controllo stato è univoco per la combinazione di
       destinazione e configurazione del load balancer.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --config
           La configurazione del load balancer da cui eliminare lo strumento
           di controllo stato.

OPERANDI
       target
           Specifica la destinazione da cui eliminare lo strumento di
           controllo stato.

           Di seguito sono riportati i valori validi.

           ·   cluster_name: il nome di un cluster di destinazione.

           ·   instance_name: il nome di un'istanza server di destinazione.

ESEMPI
       Esempio 1, Eliminazione di uno strumento di controllo stato da una
       configurazione del load balancer
           In questo esempio viene eliminato lo strumento di controllo stato
           per la configurazione del load balancer denominata
           mycluster-http-lb-config in un cluster denominato mycluster .

               asadmin> delete-http-health-checker --user admin
               --passwordfile password.txt --config mycluster-http-lb-config mycluster

               Command delete-http-health-checker executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-health-checker(1)

       asadmin(1M)

Java EE 7                       25 agosto 2010   delete-http-health-checker(1)
