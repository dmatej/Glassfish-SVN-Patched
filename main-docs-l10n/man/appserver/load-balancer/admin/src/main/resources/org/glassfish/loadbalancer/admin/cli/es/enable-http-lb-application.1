enable-http-lb-applicatiSubcomandos de la Utilidaenable-http-lb-application(1)

NOMBRE DE REFERENCIA
       enable-http-lb-application - Activa una aplicación desactivada
       previamente gestionada por un equilibrador de carga.

SINOPSIS
           enable-http-lb-application [--help] --name application_name target

DESCRIPCIÓN
       El subcomando enable-http-lb-application activa el equilibrio de carga
       para aplicaciones desplegadas en una instancia independiente o cluster.
       Puede activar el equilibrio de carga para una aplicación en todas las
       instancias de un cluster o en una única instancia de servidor
       independiente. Por defecto, el equilibrio de carga está activado para
       las aplicaciones.

           Nota
           ┌──────────────────────────────────────┐
           │Nota                                  │
           │                                      │
           │           Este subcomando sólo se    │
           │           puede aplicar a Oracle     │
           │           GlassFish Server. Este     │
           │           subcomando no se puede     │
           │           aplicar a GlassFish Server │
           │           Open Source Edition.       │
           └──────────────────────────────────────┘

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --name
           El nombre de la aplicación que se va a activar.

OPERANDOS
       target
           Este operando especifica en qué instancia de servidor o cluster se
           debe activar la aplicación. Los valores válidos son:

           ·   cluster_name: nombre de un cluster de destino.

           ·   instance_name: nombre de una instancia de servidor de destino.

EJEMPLOS
       Ejemplo 1, Activación del Equilibrio de Carga para una Aplicación
           En este ejemplo se activa una aplicación denominada webapps-simple
           para que utilice el equilibrio de carga en un cluster denominado
           mycluster.

               asadmin> enable-http-lb-application --name webapps-simple mycluster

               Command enable-http-lb-application executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       disable-http-lb-application(1)

       asadmin(1M)

Java EE 7                    26 de agosto de 2010enable-http-lb-application(1)
