apply-http-lb-changes(1)    asadmin 实用程序子命令;   apply-http-lb-changes(1)

名称
       apply-http-lb-changes - 将负载平衡器配置更改应用到负载平衡器

提要
           apply-http-lb-changes [--help]
            lb-name

说明
       使用 applyhttp-lb-changes
       子命令将对负载平衡器配置进行的更改应用到物理负载平衡器。负载平衡器必须已经存在。要创建物理负载平衡器，请使用
       create-http-lb 子命令。

       该子命令仅适用于 Oracle GlassFish Server。该子命令不适用于 GlassFish
       Server 开源版。

上下文
       负载平衡器可在多个 Oracle GlassFish Server
       实例之间分配工作量，从而提高了系统的总处理能力。负载平衡器也可使请求从一个服务器实例故障转移到另一个服务器实例。对于要保留的
       HTTP 会话信息，请配置 HTTP 会话持久性。

           注意
           ┌──────────────────────────────────────┐
           │注                                    │
           │                                      │
           │           负载平衡器插件仅可用于     │
           │           Oracle GlassFish           │
           │           Server，不可用于 GlassFish │
           │           Server 开源版。对于        │
           │           GlassFish Server           │
           │           开源版，可以使用 mod_jk    │
           │           模块在 Apache HTTP         │
           │           服务器上配置负载平衡。     │
           └──────────────────────────────────────┘

       有关在 GlassFish Server 中配置负载平衡的详细信息，请参阅 GlassFish
       Server 管理控制台中的联机帮助。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       lb-name
           要将所做的更改应用到的负载平衡器的名称。负载平衡器必须已经存在。您可使用
           create-http-lb 子命令创建它。

示例
       Example 1, 使用 apply-http-lb-changes 子命令
           此示例将对配置进行的更改应用到名为 mylb 的负载平衡器。

               asadmin> apply-http-lb-changes mylb
               Command apply-http-lb-changes executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-http-lb(1), create-http-lb-config(1)

       asadmin(1M)

Java EE 7                     2010 年 12 月 22 日     apply-http-lb-changes(1)
