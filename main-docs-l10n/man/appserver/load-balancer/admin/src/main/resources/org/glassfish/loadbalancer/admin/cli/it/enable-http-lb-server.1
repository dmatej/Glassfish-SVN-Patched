enable-http-lb-server(1)Comandi secondari della utilitenable-http-lb-server(1)

NOME
       enable-http-lb-server - abilita un server o un cluster gestito da un
       load balancer disabilitato in precedenza

SINTASSI
           enable-http-lb-server [--help] target

DESCRIZIONE
       Il comando secondario enable-http-lb-server abilita un'istanza server
       standalone o un cluster per il bilanciamento del carico. Per
       impostazione predefinita, il bilanciamento del carico è abilitato per
       le istanze e i cluster.

           Nota
           ┌───────────────────────────────────────┐
           │Nota                                   │
           │                                       │
           │           Questo comando secondario è │
           │           applicabile solo a Oracle   │
           │           GlassFish Server. Questo    │
           │           comando secondario non è    │
           │           applicabile a GlassFish     │
           │           Server Open Source Edition. │
           └───────────────────────────────────────┘

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Questo operando specifica le istanze server e i cluster da
           abilitare. Di seguito sono riportati i valori validi.

           ·   cluster_name: il nome di un cluster di destinazione.

           ·   instance_name: il nome di un'istanza server di destinazione.

ESEMPI
       Esempio 1, Abilitazione di un cluster per il bilanciamento del carico
           In questo esempio viene abilitato il bilanciamento del carico per
           il cluster denominato mycluster.

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

Java EE 7                       26 agosto 2010        enable-http-lb-server(1)
