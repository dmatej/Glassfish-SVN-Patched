disable-http-lb-applicatUnterbefehle des asadmindisable-http-lb-application(1)

NAME
       disable-http-lb-application - deaktiviert eine Anwendung, die von einem
       Load Balancer verwaltet wird

ZUSAMMENFASSUNG
           disable-http-lb-application [--help] [--timeout 30]
            --name application_name target

BESCHREIBUNG
       Der Unterbefehl disable-http-lb-application deaktiviert eine Anwendung
       für Load Balancing. Die deaktivierte Anwendung wechselt in den
       Offlinestatus, damit das Load Balancing minimale Auswirkung auf die
       Benutzer hat. Die Deaktivierung einer Anwendung erlaubt eine
       differenziertere Steuerung als die Deaktivierung einer Serverinstanz
       und ist äußerst hilfreich, wenn ein Cluster mehrere unabhängige
       Anwendungen enthält.

       Sobald die Anwendung deaktiviert ist und die Änderungen auf den Load
       Balancer angewendet wurden, werden neue anwendungsbezogene
       Anforderungen nicht an das Ziel weitergeleitet. Vorhandene Sessions
       greifen weiter auf die Anwendung zu, bis das Timeout erreicht ist.
       Dieser Prozess wird auch als Stilllegung oder Deaktivierung bezeichnet.

       Ist eine Anwendung in mehreren Clustern bereitgestellt, verwenden Sie
       diesen Unterbefehl, um die Anwendung in einem Cluster zu deaktivieren,
       während sie in anderen Clustern aktiviert bleibt.

       Ist eine Anwendung in einer einzelnen Serverinstanz bereitgestellt,
       verwenden Sie diesen Unterbefehl, um die Anwendung in der Instanz zu
       deaktivieren, während die Instanz selbst aktiviert bleibt.

           Anmerkung
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Dieser Unterbefehl ist nur   │
           │           auf Oracle GlassFish Server  │
           │           anwendbar. Dieser            │
           │           Unterbefehl ist nicht auf    │
           │           GlassFish Server Open Source │
           │           Edition anwendbar.           │
           └────────────────────────────────────────┘

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --timeout
           Das Timeout (in Minuten) bzw. die Dauer bis zur Deaktivierung der
           angegebenen Anwendung. Diese Dauer ermöglicht eine ordnungsgemäße
           Deaktivierung (Stilllegung) der angegebenen Anwendung. Der
           Standardwert beträgt 30 Minuten. Der Mindestwert ist 1 Minute.

       --name
           Der Name der Anwendung, die deaktiviert werden soll.

OPERANDEN
       target
           Dieser Operand gibt an, in welcher Serverinstanz oder in welchem
           Cluster die Anwendung deaktiviert werden soll. Gültige Werte sind:

           ·   cluster_name: Der Name eines Zielclusters.

           ·   instance_name: Der Name einer Zielserverinstanz.

BEISPIELE
       Beispiel 1, Anwendungen für Load Balancing deaktivieren
           In diesem Beispiel wird eine Anwendung für Load Balancing
           deaktiviert.

               asadmin> disable-http-lb-application --name webapps-simple mycluster

               Command disable-http-lb-application executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       enable-http-lb-application(1)

       asadmin(1M)

Java EE 7                        26. Aug. 2010  disable-http-lb-application(1)
