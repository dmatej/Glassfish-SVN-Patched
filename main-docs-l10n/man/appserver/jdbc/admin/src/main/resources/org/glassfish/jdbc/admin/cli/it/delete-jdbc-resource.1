delete-jdbc-resource(1) Comandi secondari della utilit delete-jdbc-resource(1)

NOME
       delete-jdbc-resource - rimuove la risorsa JDBC con il nome JNDI
       specificato

SINTASSI
           delete-jdbc-resource [--help] [--target target] jndi_name

DESCRIZIONE
       Il comando secondario delete-jdbc-resource rimuove una risorsa JDBC.
       Prima di eseguire questo comando secondario, verificare di aver rimosso
       tutte le associazioni con la risorsa JDBC.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione consente di specificare la destinazione da cui
           rimuovere la risorsa JDBC. Le destinazioni valide sono:

           server
               Rimuove la risorsa dall'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Rimuove la risorsa dal dominio.

           cluster_name
               Rimuove la risorsa da tutte le istanze server del cluster.

           instance_name
               Rimuove la risorsa da un'istanza server particolare.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Le risorse vengono sempre    │
           │           create per l'intero dominio, │
           │           ma sono attive solo per le   │
           │           destinazioni per le quali è  │
           │           stato creato un elemento     │
           │           <resource-ref> utilizzando   │
           │           l'opzione --target al        │
           │           momento della creazione      │
           │           della risorsa. Ciò significa │
           │           che l'eliminazione di una    │
           │           risorsa elimina solo         │
           │           l'elemento <resource-ref>    │
           │           per l'opzione --target       │
           │           specificata, ma non la       │
           │           risorsa dal dominio completo │
           │           a meno che domain venga      │
           │           specificato come --target    │
           │           per l'eliminazione.          │
           └────────────────────────────────────────┘

OPERANDI
       jndi_name
           Il nome JNDI della risorsa JDBC da rimuovere.

ESEMPI
       Esempio 1, Eliminazione di una risorsa JDBC
           In questo esempio viene eliminata la risorsa JDBC denominata
           jdbc/DerbyPool.

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                      11 febbraio 2011        delete-jdbc-resource(1)
