list-jdbc-resources(1)asadminユーティリティのサブコマンド

名前
       list-jdbc-resources - すべてのJDBCリソースをリストします

概要
           list-jdbc-resources [--help] [target target]

説明
       list-jdbc-resourcesサブコマンドは、既存のJDBCリソースのリストを表示します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       --target
           このオペランドは、リストするJDBCリソースを指定します。このオペランドの使用はオプションです。有効な値は次のとおりです:

           server
               現在のサーバー内のJDBCリソースをリストします(デフォルト)。

           domain
               現在のドメイン内のJDBCリソースをリストします。

           cluster_name
               クラスタ内のJDBCリソースをリストします。

           instance_name
               特定のインスタンス内のJDBCリソースをリストします。

例
       例 1, JDBCリソースのリスト
           この例では、現在のJDBCリソースをリストします。

               asadmin> list-jdbc-resources
               jdbc/DerbyPool
               Command list-jdbc-resources executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jdbc-resource(1), delete-jdbc-resource(1)

       asadmin(1M)

Java EE 7                        2010年9月17日          list-jdbc-resources(1)
