delete-jdbc-connection-pSubcomandos de la Utiliddelete-jdbc-connection-pool(1)

NOMBRE DE REFERENCIA
       delete-jdbc-connection-pool - Elimina el pool de conexiones JDBC
       especificado.

SINOPSIS
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

DESCRIPCIÓN
       El subcomando delete-jdbc-connection-pool suprime un pool de conexiones
       JDBC. Antes de ejecutar este comando se deben eliminar todas las
       asociaciones al pool de conexiones JDBC.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --cascade
           Si la opción se define en true, se suprimen todos los recursos JDBC
           asociados al pool, aparte del pool en sí. Cuando se define en
           false, la supresión del pool falla si hay algún recurso asociado al
           pool. Los recursos se deben suprimir explícitamente o la opción se
           debe definir en true. El valor por defecto es false.

       --target
           No especifica esta opción. Esta opción se mantienen para su
           compatibilidad con versiones anteriores. Si especifica esta opción,
           no se produce un error de sintaxis. En su lugar, el subcomando se
           ejecuta correctamente y muestra una advertencia que indica que se
           ha ignorado la opción.

OPERANDOS
       jdbc_connection_pool_id
           Nombre del recurso JDBC que se va a eliminar.

EJEMPLOS
       Ejemplo 1, Supresión de un Pool de Conexiones JDBC
           En este ejemplo se suprime el pool de conexiones JDBC
           sample_derby_pool.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                  17 de septiembre de 2delete-jdbc-connection-pool(1)
