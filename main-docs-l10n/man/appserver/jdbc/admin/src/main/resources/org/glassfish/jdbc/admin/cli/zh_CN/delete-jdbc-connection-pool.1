delete-jdbc-connection-pool(asadmin 实用程序子命delete-jdbc-connection-pool(1)

名称
       delete-jdbc-connection-pool - 删除指定的 JDBC 连接池

提要
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

说明
       delete-jdbc-connection-pool 子命令删除 JDBC
       连接池。在运行此子命令之前，必须删除相应 JDBC 连接池的所有关联项。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --cascade
           如果此选项设置为“真”，则除了池本身外，与池关联的所有 JDBC
           资源都将被删除。设置为“假”时，如果有任何资源与池关联，则删除池的操作会失败。此时必须明确删除资源，或者将此选项设置为“真”。默认值为“假”。

       --target
           不指定此选项。为了与早期发行版兼容而保留此选项。如果指定此选项，则不会出现语法错误。相反，子命令会成功运行，并显示忽略了此选项的警告消息。

操作数
       jdbc_connection_pool_id
           将要删除的 JDBC 资源的名称。

示例
       Example 1, 删除 JDBC 连接池
           此示例删除 sample_derby_pool JDBC 连接池。

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 17 日delete-jdbc-connection-pool(1)
