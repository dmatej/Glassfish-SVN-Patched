list-jdbc-resources(1)    asadmin 유틸리티 하위 명령    list-jdbc-resources(1)

제목
       list-jdbc-resources - 모든 JDBC 리소스 나열

시놉시스
           list-jdbc-resources [--help] [target target]

설명
       list-jdbc-resources 하위 명령은 기존 JDBC 리소스 목록을 표시합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       --target
           이 피연산자는 나열 가능한 JDBC 리소스를 지정합니다. 이 피연산자는
           선택적으로 사용합니다. 적합한 값은 다음과 같습니다.

           server
               현재 서버의 JDBC 리소스를 나열하며 기본값입니다.

           domain
               현재 도메인의 JDBC 리소스를 나열합니다.

           cluster_name
               클러스터의 JDBC 리소스를 나열합니다.

           instance_name
               특정 인스턴스의 JDBC 리소스를 나열합니다.

예제
       예 1, JDBC 리소스 나열
           이 예제는 현재 JDBC 리소스를 나열합니다.

               asadmin> list-jdbc-resources
               jdbc/DerbyPool
               Command list-jdbc-resources executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jdbc-resource(1), delete-jdbc-resource(1)

       asadmin(1M)

Java EE 7                       2010년 9월 17일         list-jdbc-resources(1)
