delete-jdbc-resource(1) Subcomandos de la Utilidad asa delete-jdbc-resource(1)

NOMBRE DE REFERENCIA
       delete-jdbc-resource - Elimina un recurso JDBC con el nombre de JNDI
       especificado.

SINOPSIS
           delete-jdbc-resource [--help] [--target target] jndi_name

DESCRIPCIÓN
       El subcomando delete-jdbc-resource elimina un recurso JDBC. Asegúrese
       de eliminar todas las asociaciones al recurso JDBC antes de ejecutar
       este subcomando.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Esta opción permite especificar el destino del que se va a eliminar
           el recurso JDBC. Los destinos válidos son:

           server
               Elimina el recurso de la instancia de servidor por defecto.
               Este es el valor por defecto.

           domain
               Elimina el recurso del dominio.

           cluster_name
               Elimina el recurso de todas las instancias de servidor del
               cluster.

           instance_name
               Elimina el recurso de una instancia de servidor específica.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Los recursos siempre se      │
           │           crean para un dominio        │
           │           completo, pero sólo están    │
           │           activos para los destinos    │
           │           para los que se ha creado    │
           │           una <resource-ref> mediante  │
           │           la opción --target al crear  │
           │           el recurso. Esto significa   │
           │           que al suprimir un recurso   │
           │           sólo se suprime el elemento  │
           │           <resource-ref> para el       │
           │           --target especificado y no   │
           │           se suprime el recurso del    │
           │           dominio completo a menos que │
           │           se especifique domain como   │
           │           --target de la supresión.    │
           └────────────────────────────────────────┘

OPERANDOS
       jndi_name
           Nombre de JNDI del recurso JDBC que se va a eliminar.

EJEMPLOS
       Ejemplo 1, Supresión de un Recurso JDBC
           El siguiente ejemplo elimina el recurso JDBC denominado
           jdbc/DerbyPool.

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                    11 de febrero de 2011     delete-jdbc-resource(1)
