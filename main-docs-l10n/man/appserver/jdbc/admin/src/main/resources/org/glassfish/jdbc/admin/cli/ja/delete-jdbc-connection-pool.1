delete-jdbc-connectionasadminユーティリティのサブ

名前
       delete-jdbc-connection-pool - 指定したJDBC接続プールを除去します

概要
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

説明
       delete-jdbc-connection-poolサブコマンドは、JDBC接続プールを削除します。このサブコマンドを実行する前に、JDBC接続プールのすべてのアソシエーションを除去する必要があります。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --cascade
           オプションがtrueに設定されている場合は、プール自体は残りますが、プールに関連付けられているすべてのJDBCリソースが削除されます。falseに設定したときは、リソースがプールに関連付けられている場合は、プールの削除に失敗します。リソースを明示的に削除するか、またはこのオプションをtrueに設定する必要があります。デフォルト値はfalseです。

       --target
           このオプションは指定しないでください。このオプションは、旧リリースとの互換性のために保持されています。このオプションを指定した場合、構文エラーは発生しません。かわりに、サブコマンドが正常に実行され、オプションが無視されたことを示す警告メッセージが表示されます。

オペランド
       jdbc_connection_pool_id
           除去されるJDBCリソースの名前です。

例
       例 1, JDBC接続プールの削除
           この例では、sample_derby_pool JDBC接続プールを削除します。

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                        2010年9月17日  delete-jdbc-connection-pool(1)
