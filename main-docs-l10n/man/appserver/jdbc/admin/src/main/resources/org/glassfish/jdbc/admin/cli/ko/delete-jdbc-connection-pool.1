delete-jdbc-connection-pooasadmin 유틸리티 하위 delete-jdbc-connection-pool(1)

제목
       delete-jdbc-connection-pool - 지정된 JDBC 접속 풀 제거

시놉시스
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

설명
       delete-jdbc-connection-pool 하위 명령은 JDBC 접속 풀을 삭제합니다. 이
       하위 명령을 실행하기 전에 JDBC 접속 풀에 대한 모든 연관을 제거해야
       합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --cascade
           이 옵션이 true로 설정된 경우 자체 풀과는 별개로 해당 풀에 연관된
           모든 JDBC 리소스가 삭제됩니다. false로 설정된 상태에서 풀과 연관된
           리소스가 있을 경우 풀이 삭제되지 않습니다. 리소스를 명시적으로
           삭제하거나 옵션을 true로 설정해야 합니다. 기본값은 false입니다.

       --target
           이 옵션은 지정하지 마십시오. 이 옵션은 이전 릴리스와의 호환성을
           위해 보존됩니다. 이 옵션을 지정하면 구문 오류가 발생하지 않습니다.
           대신, 하위 명령이 성공적으로 실행되고 옵션이 무시되었다는 경고
           메시지가 표시됩니다.

피연산자
       jdbc_connection_pool_id
           제거할 JDBC 리소스의 이름입니다.

예제
       예 1, JDBC 접속 풀 삭제
           이 예제는 sample_derby_pool JDBC 접속 풀을 삭제합니다.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                       2010년 9월 17일 delete-jdbc-connection-pool(1)
