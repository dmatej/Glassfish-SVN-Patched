delete-jdbc-resource(1) Unterbefehle des asadmin-Utili delete-jdbc-resource(1)

NAME
       delete-jdbc-resource - entfernt eine JDBC-Ressource mit dem angegebenen
       JNDI-Namen

ZUSAMMENFASSUNG
           delete-jdbc-resource [--help] [--target target] jndi_name

BESCHREIBUNG
       Der Unterbefehl delete-jdbc-resource entfernt eine JDBC-Ressource.
       Stellen Sie sicher, dass Sie alle Verknüpfungen zur JDBC-Ressource
       entfernt haben, bevor Sie diesen Unterbefehl ausführen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Mit dieser Option können Sie das Ziel angeben, aus dem Sie die
           JDBC-Ressource entfernen. Gültige Ziele sind:

           server
               Entfernt die Ressource aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           domain
               Entfernt die Ressource aus der Domain.

           cluster_name
               Entfernt die Ressource aus jeder Serverinstanz im Cluster.

           instance_name
               Entfernt die Ressource aus einer bestimmten Serverinstanz.

           Note
           ┌────────────────────────────────────────┐
           │Hinweis                                 │
           │                                        │
           │           Ressourcen werden stets für  │
           │           eine Domain als Ganzes       │
           │           erstellt, sind aber nur für  │
           │           Ziele aktiv, für die bei der │
           │           Erstellung der Ressource     │
           │           eine <resource-ref> mit der  │
           │           Option --target erstellt     │
           │           wurde. Das bedeutet, dass    │
           │           beim Löschen einer Ressource │
           │           nur das                      │
           │           <resource-ref>-Element für   │
           │           das angegebene Ziel          │
           │           (--target) und nicht die     │
           │           Ressource aus der Domain als │
           │           Ganzes gelöscht wird, es sei │
           │           denn, domain ist als         │
           │           --target für den             │
           │           Löschvorgang angegeben.      │
           └────────────────────────────────────────┘

OPERANDEN
       jndi_name
           Der JNDI-Name der zu entfernenden JDBC-Ressource.

BEISPIELE
       Example 1, JDBC-Ressourcen löschen
           Im folgenden Beispiel wird die JDBC-Ressource namens jdbc/DerbyPool
           gelöscht.

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                        11. Feb. 2011         delete-jdbc-resource(1)
