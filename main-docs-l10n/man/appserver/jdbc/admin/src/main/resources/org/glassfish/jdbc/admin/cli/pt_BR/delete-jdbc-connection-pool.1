delete-jdbc-connection-pSubcomandos do utilitáridelete-jdbc-connection-pool(1)

NOME
       delete-jdbc-connection-pool - remove o pool de conexões JDBC
       especificado

SINOPSE
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

DESCRIçãO
       O subcomando delete-jdbc-connection-pool deleta um pool de conexões
       JDBC. Antes de executar o subcomando, todas as associações ao pool de
       conexões JDBC devem ser removidas.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --cascade
           Se a opção for definida como verdadeira, todos os recursos JDBC
           associados ao pool, exceto o próprio pool, serão deletados. Quando
           definido como falso, a deleção do pool falhará, se qualquer recurso
           estiver associado ao pool. Os recursos devem ser deletados
           explicitamente ou a opção deve ser definida como verdadeira. O
           valor default é falso.

       --target
           Não especificar essa opção. Esta opção é retida para
           compatibilidade com versões anteriores. se você especificar esta
           opção, um erro de sintaxe não ocorrerá. Em vez disso, o subcomando
           é executado com sucesso e exibe uma mensagem de advertência
           informando que a opção será ignorada.

OPERANDOS
       jdbc_connection_pool_id
           Nome do recurso JDBC a ser removido.

EXEMPLOS
       Example 1, Deletando um pool de Conexões JDBC
           Este exemplo deleta o pool de conexões JDBC sample_derby_pool.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                         17 set 2010   delete-jdbc-connection-pool(1)
