delete-jdbc-resource(1)     asadmin 实用程序子命令     delete-jdbc-resource(1)

名称
       delete-jdbc-resource - 删除指定 JNDI 名称的 JDBC 资源

提要
           delete-jdbc-resource [--help] [--target target] jndi_name

说明
       delete-jdbc-resource 子命令删除 JDBC
       资源。在运行此子命令之前，确保删除相应 JDBC 资源的所有关联项。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           此选项可帮助指定要从其中删除 JDBC 资源的目标。有效目标包括：

           server
               从默认服务器实例删除资源。这是默认值。

           domain
               从域删除资源。

           cluster_name
               从集群中的每个服务器实例删除资源。

           instance_name
               从特定服务器实例删除资源。

           注意
           ┌────────────────────────────────────────────────────────────┐
           │注                                                          │
           │                                                            │
           │           始终为整个域创建资源，但资源仅在创建资源时使用   │
           │           --target 选项为其创建                            │
           │           <resource-ref>                                   │
           │           的目标上处于活动状态。这意味着删除资源仅删除指定 │
           │           --target 中的 <resource-ref>                     │
           │           元素，并不会从整个域中删除资源，除非将           │
           │           domain 指定为要删除的                            │
           │           --target。                                       │
           └────────────────────────────────────────────────────────────┘

操作数
       jndi_name
           将删除的此 JDBC 资源的 JNDI 名称。

示例
       Example 1, 删除 JDBC 资源
           以下示例删除名为 jdbc/DerbyPool 的 JDBC 资源。

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-jdbc-resource(1)
