delete-jdbc-connection-pSous-commandes de l'utildelete-jdbc-connection-pool(1)

NOM
       delete-jdbc-connection-pool - enlève le pool de connexions JDBC indiqué

SYNTAXE
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

DESCRIPTION
       La sous-commande delete-jdbc-connection-pool supprime un pool de
       connexions JDBC. Avant d'exécuter cette sous-commande, vous devez
       enlever toutes les associations au pool de connexions JDBC.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --cascade
           Si la valeur de l'option est True, toutes les ressources JDBC
           associées au pool sont supprimées, sauf le pool lui-même. Lorsque
           l'option a la valeur False, la suppression du pool échoue si des
           ressources sont associées au pool. Vous devez supprimer les
           ressources explicitement ou attribuer la valeur True à l'option. La
           valeur par défaut est False.

       --target
           Ne spécifiez pas cette option. Elle est conservée pour assurer la
           compatibilité avec les versions antérieures. Si vous spécifiez
           cette option, aucune erreur de syntaxe ne survient. Au lieu de
           cela, la sous-commande est exécutée et affiche un message
           d'avertissement indiquant que l'option est ignorée.

OPÉRANDES
       jdbc_connection_pool_id
           Nom de la ressource JDBC à enlever.

EXEMPLES
       Exemple 1, Suppression d'un pool de connexions JDBC
           Dans cet exemple, le pool de connexions JDBC sample_derby_pool est
           supprimé.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                        17 sept. 2010  delete-jdbc-connection-pool(1)
