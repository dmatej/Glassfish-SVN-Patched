delete-jdbc-resource(1) Sous-commandes de l'utilitaire delete-jdbc-resource(1)

NOM
       delete-jdbc-resource - enlève la ressource JDBC portant le nom JNDI
       indiqué

SYNTAXE
           delete-jdbc-resource [--help] [--target target] jndi_name

DESCRIPTION
       La sous-commande delete-jdbc-resource enlève une ressource JDBC.
       Assurez-vous que toutes les associations à la ressource JDBC sont
       enlevées avant d'exécuter cette sous-commande.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option spécifie la cible de laquelle enlever la ressource
           JDBC. Les cibles valides sont les suivantes :

           server
               Enlève la ressource de l'instance de serveur par défaut. Il
               s'agit de la valeur par défaut.

           domain
               Enlève la ressource du domaine.

           cluster_name
               Enlève la ressource de chaque instance de serveur du cluster.

           instance_name
               Enlève la ressource d'une instance de serveur donnée.

           Note
           ┌────────────────────────────────────────┐
           │Remarque                                │
           │                                        │
           │           Les ressources sont toujours │
           │           créées pour un domaine dans  │
           │           sa totalité, mais elles sont │
           │           actives uniquement pour les  │
           │           cibles pour lesquelles un    │
           │           élément <resource-ref> a été │
           │           créé à l'aide de l'option    │
           │           --target lors de la création │
           │           de la ressource. Cela        │
           │           signifie que la suppression  │
           │           d'une ressource entraîne     │
           │           seulement celle de l'élément │
           │           <resource-ref> pour l'option │
           │           --target spécifiée, mais pas │
           │           celle de la ressource du     │
           │           domaine dans sa totalité, à  │
           │           moins que la valeur domain   │
           │           ne soit indiquée pour        │
           │           --target lors de la          │
           │           suppression.                 │
           └────────────────────────────────────────┘

OPÉRANDES
       jndi_name
           Nom JNDI de la ressource JDBC à enlever.

EXEMPLES
       Exemple 1, Suppression d'une ressource JDBC
           Dans cet exemple, la ressource JDBC nommée jdbc/DerbyPool est
           supprimée.

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                         11 fév 2011          delete-jdbc-resource(1)
