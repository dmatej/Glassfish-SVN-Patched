create-jdbc-resource(1) Comandi secondari della utilit create-jdbc-resource(1)

NOME
       create-jdbc-resource - crea una risorsa JDBC con il nome JNDI
       specificato

SINTASSI
           create-jdbc-resource [--help]
           --connectionpoolid connectionpoolid
           [--enabled={false|true}]
           [--description description]
           [--property (property=value)[:name=value]*]
           [--target target]
           jndi_name

DESCRIZIONE
       Il comando secondario create-jdbc-resource crea una nuova risorsa JDBC.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --connectionpoolid
           Il nome del connection pool JDBC. Se due o più elementi di risorsa
           JDBC puntano allo stesso elemento connection pool, utilizzano la
           stessa connessione del pool in fase di esecuzione.

       --enabled
           Determina se la risorsa JDBC viene abilitata in fase di esecuzione.
           Il valore predefinito è true.

       --description
           Il testo della descrizione dettagliata della risorsa JDBC.

       --property
           Coppie nome-valore di attributo facoltative per la configurazione
           della risorsa.

       --target
           Questa opzione consente di specificare la destinazione della
           distribuzione. Di seguito sono riportati i valori validi.

           server
               Distribuisce il componente all'istanza server predefinita.
               Questo è il valore predefinito.

           domain
               Distribuisce il componente al dominio.

           cluster_name
               Distribuisce il componente a ogni istanza server del cluster.

           instance_name
               Distribuisce il componente in un'istanza server specifica.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           La risorsa viene sempre      │
           │           creata per l'intero dominio, │
           │           mentre l'elemento            │
           │           resource-ref per la risorsa  │
           │           viene creato solo per        │
           │           l'elemento --target          │
           │           specificato. Ciò significa   │
           │           che, benché sia definita a   │
           │           livello di dominio, la       │
           │           risorsa è disponibile solo   │
           │           per il livello destinazione  │
           │           specificato. Se necessario,  │
           │           utilizzare il comando        │
           │           secondario                   │
           │           create-resource-ref per fare │
           │           riferimento alla risorsa in  │
           │           più livelli target.          │
           └────────────────────────────────────────┘

OPERANDI
       jndi_name
           Il nome JNDI della risorsa JDBC.

ESEMPI
       Esempio 1, Creazione di una risorsa JDBC
           In questo esempio viene creata la risorsa JDBC denominata
           jdbc/DerbyPool.

               asadmin> create-jdbc-resource
               --connectionpoolid sample_derby_pool jdbc/DerbyPool
               Command create-jdbc-resource executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-jdbc-resource(1), list-jdbc-resources(1), create-resource-ref(1)

       asadmin(1M)

Java EE 7                      10 febbraio 2011        create-jdbc-resource(1)
