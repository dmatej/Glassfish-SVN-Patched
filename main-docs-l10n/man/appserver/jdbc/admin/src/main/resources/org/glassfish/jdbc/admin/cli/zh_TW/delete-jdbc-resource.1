delete-jdbc-resource(1)     asadmin 公用程式子命令     delete-jdbc-resource(1)

參考名
       delete-jdbc-resource - 移除指定 JNDI 名稱的 JDBC 資源

概要
           delete-jdbc-resource [--help] [--target target] jndi_name

描述
       delete-jdbc-resource 子命令會移除 JDBC
       資源。在執行此子命令之前，確保已移除 JDBC 資源的所有關聯。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           此選項可協助指定要從中移除 JDBC 資源的目標。有效目標包括：

           server
               從預設伺服器執行處理移除資源。此為預設值。

           domain
               從網域移除資源。

           cluster_name
               從叢集中的每個伺服器執行處理移除資源。

           instance_name
               從特定伺服器執行處理移除資源。

           注意
           ┌────────────────────────────────────────────────────────────────┐
           │備註                                                            │
           │                                                                │
           │           資源皆是為整個網域所建立，但僅適用於建立資源時已使用 │
           │           --target 選項建立                                    │
           │           <resource-ref>                                       │
           │           的目標。這表示刪除資源只會刪除指定                   │
           │           --target 的 <resource-ref>                           │
           │           元素，而不會刪除完整網域的資源，除非已將             │
           │           domain 指定為要刪除的                                │
           │           --target。                                           │
           └────────────────────────────────────────────────────────────────┘

運算元
       jndi_name
           將要移除的 JDBC 資源的 JNDI 名稱。

範例
       範例 1, 刪除 JDBC 資源
           下列範例會刪除名為 jdbc/DerbyPool 的 JDBC 資源。

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

Java EE 7                     2011 年 2 月 11 日       delete-jdbc-resource(1)
