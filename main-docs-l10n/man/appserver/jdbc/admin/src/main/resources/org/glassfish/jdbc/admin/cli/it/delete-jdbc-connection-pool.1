delete-jdbc-connection-pComandi secondari della delete-jdbc-connection-pool(1)

NOME
       delete-jdbc-connection-pool - rimuove il connection pool JDBC
       specificato

SINTASSI
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

DESCRIZIONE
       Il comando secondario delete-jdbc-connection-pool elimina un connection
       pool JDBC. Prima di eseguire questo comando secondario, è necessario
       rimuovere tutte le associazioni al connection pool JDBC.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --cascade
           Se l'opzione è impostata su true, oltre al pool vengono eliminate
           tutte le risorse JDBC associate al pool. Se l'opzione è impostata
           su false, l'eliminazione del pool non riesce se esistono risorse
           associate al pool. Le risorse devono essere eliminate in modo
           esplicito oppure l'opzione deve essere impostata su true. Il valore
           predefinito è false.

       --target
           Non specificare questa opzione. Questa opzione viene mantenuta per
           garantire la compatibilità con le release precedenti. Se la si
           specifica, non si verifica un errore di sintassi. L'esecuzione del
           comando secondario riesce e un messaggio di avvertenza indica che
           l'opzione è stata ignorata.

OPERANDI
       jdbc_connection_pool_id
           Il nome della risorsa JDBC da rimuovere.

ESEMPI
       Esempio 1, Eliminazione di un connection pool JDBC
           In questo esempio viene eliminato il connection pool JDBC
           denominato sample_derby_pool.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                      17 settembre 2010delete-jdbc-connection-pool(1)
