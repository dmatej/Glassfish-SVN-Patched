delete-jdbc-connection-pool(asadmin 公用程式子命delete-jdbc-connection-pool(1)

參考名
       delete-jdbc-connection-pool - 移除指定的 JDBC 連線集區

概要
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

描述
       delete-jdbc-connection-pool 子命令會刪除 JDBC
       連線集區。在執行此子命令之前，必須先移除 JDBC 連線集區的所有關聯。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --cascade
           如果選項設定為 true，除集區本身之外，將會刪除與集區相關聯的所有
           JDBC 資源。當設定為 false
           時，如果有任何資源與集區相關聯，則刪除集區會失敗。必須明確刪除資源，或者選項必須設定為
           true。預設值為 false。

       --target
           請勿指定此選項。保留這個選項是為了兼顧與舊版本的相容性。如果您指定此選項，並不會發生語法錯誤。相反地，子命令會順利執行，並顯示已忽略該選項的警告訊息。

運算元
       jdbc_connection_pool_id
           將要移除的 JDBC 資源名稱。

範例
       範例 1, 刪除 JDBC 連線集區
           此範例會刪除 sample_derby_pool JDBC 連線集區。

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                     2010 年 9 月 17 日delete-jdbc-connection-pool(1)
