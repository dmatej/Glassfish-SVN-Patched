delete-jdbc-connection-pUnterbefehle des asadmindelete-jdbc-connection-pool(1)

NAME
       delete-jdbc-connection-pool - entfernt den angegebenen JDBC-Connection
       Pool

ZUSAMMENFASSUNG
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

BESCHREIBUNG
       Der Unterbefehl delete-jdbc-connection-pool löscht einen
       JDBC-Connection Pool. Bevor Sie diesen Unterbefehl ausführen, müssen
       alle Verknüpfungen mit dem JDBC-Connection Pool entfernt werden.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --cascade
           Wenn die Option auf "true" (wahr) gesetzt ist, werden alle mit dem
           Pool verknüpften JDBC-Ressourcen, abgesehen vom Pool selbst,
           gelöscht. Wenn die Option auf "false" (falsch) gesetzt ist, kann
           der Pool nicht gelöscht werden, wenn mit dem Pool Ressourcen
           verknüpft sind. Die Ressourcen müssen explizit gelöscht werden,
           oder die Option muss auf "true" (wahr) gesetzt werden. Der
           Standardwert ist "false".

       --target
           Geben Sie diese Option nicht an. Diese Option wird für die
           Kompatibilität mit früheren Releases beibehalten. Durch Angabe
           dieser Option tritt jedoch kein Syntaxfehler auf. Stattdessen wird
           der Unterbefehl erfolgreich ausgeführt und eine Warnmeldung
           angezeigt, dass die Option ignoriert wurde.

OPERANDEN
       jdbc_connection_pool_id
           Der Name der zu entfernenden JDBC-Ressource.

BEISPIELE
       Beispiel 1, JDBC-Connection Pools löschen
           In diesem Beispiel wird der JDBC-Connection Pool sample_derby_pool
           gelöscht.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

Java EE 7                        17. Sep. 2010  delete-jdbc-connection-pool(1)
