delete-iiop-listener(1) Comandi secondari della utilit delete-iiop-listener(1)

NOME
       delete-iiop-listener - rimuove un listener IIOP

SINTASSI
           delete-iiop-listener [--help] [--target target] listener_id

DESCRIZIONE
       Il comando secondario delete-iiop-listener rimuove il listener IIOP
       specificato. Questo comando secondario è supportato solo in modalità
       remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione dalla quale si elimina il listener IIOP.
           Di seguito sono riportati i valori validi.

           server
               Elimina il listener dall'istanza server predefinita server ed è
               il valore predefinito.

           configuration_name
               Elimina il listener dalla configurazione specificata.

           cluster_name
               Elimina il listener da tutte le istanze server del cluster.

           instance_name
               Elimina il listener da un'istanza server particolare.

OPERANDI
       listener_id
           L'identificativo univoco del listener IIOP da eliminare.

ESEMPI
       Esempio 1, Eliminazione di un listener IIOP
           Il comando seguente elimina il listener IIOP denominato
           sample_iiop_listener:

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                       18 agosto 2010         delete-iiop-listener(1)
