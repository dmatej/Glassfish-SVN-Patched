list-iiop-listeners(1)      asadmin 实用程序子命令      list-iiop-listeners(1)

名称
       list-iiop-listeners - 列出现有 IIOP 监听程序

提要
           list-iiop-listeners [--help]
           [target]

说明
       list-iiop-listeners 子命令列出现有 IIOP
       监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

操作数
       target
           此操作数指定要列出 IIOP 监听程序的目标。有效值为：

           server
               列出默认服务器实例 server 中的监听程序，并且这是默认值。

           configuration_name
               列出指定配置中的监听程序。

           cluster_name
               列出指定集群中的监听程序。

           instance_name
               列出特定服务器实例中的监听程序。

示例
       Example 1, 使用 list-iiop-listeners 子命令
           以下命令可列出服务器实例的所有 IIOP 监听程序：

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日        list-iiop-listeners(1)
