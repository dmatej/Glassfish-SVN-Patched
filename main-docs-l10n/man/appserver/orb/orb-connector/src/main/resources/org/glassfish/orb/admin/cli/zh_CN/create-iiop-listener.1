create-iiop-listener(1)     asadmin 实用程序子命令     create-iiop-listener(1)

名称
       create-iiop-listener - 添加 IIOP 监听程序

提要
           create-iiop-listener [--help] --listeneraddress address
           [--iiopport iiop-port-number] [--securityenabled={false|true}] [--enabled={true|false}]
           [--property (name=value)[:name=value]*]
           [--target target] listener_id

说明
       create-iiop-listener 子命令创建 IIOP
       监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --listeneraddress
           IP 地址或主机名 (可由 DNS 解析)。

       --iiopport
           IIOP 端口号。默认值为 1072。

       --securityenabled
           如果设置为“真”，则 IIOP 监听程序会运行 SSL。可以使用 SSL
           元素启用或禁用 SSL2 或 SSL3
           并设置密码。通过使证书可用于服务器实例，此安全设置可全局启用或禁用
           SSL。默认值为假

       --enabled
           如果设置为“真”，则会在运行时启用 IIOP 监听程序。默认值为真

       --property
           用于配置 IIOP 监听程序的可选属性名称/值对。

       --target
           指定要创建 IIOP 监听程序的目标。有效值为

           server
               为默认服务器实例 server 创建监听程序，并且，这是默认值。

           configuration_name
               为已命名的配置创建监听程序。

           cluster_name
               为集群中的每个服务器实例创建监听程序。

           stand-alone_instance_name
               为特定的独立服务器实例创建监听程序。

操作数
       listener_id
           要创建的 IIOP 监听程序的唯一标识符。

示例
       Example 1, 创建 IIOP 监听程序
           以下命令可创建名为 sample_iiop_listener 的 IIOP 监听程序：

               asadmin> create-iiop-listener --listeneraddress 192.168.1.100
               --iiopport 1400 sample_iiop_listener
               Command create-iiop-listener executed successfully.

       Example 2, 使用目标集群创建 IIOP 监听程序
           以下命令可为 mycluster 集群创建名为 iiop_listener_2 的 IIOP
           监听程序。它使用目标选项。

               asadmin> create-iiop-listener --listeneraddress 0.0.0.0 --iiopport 1401
               --target mycluster iiop_listener_2
               Command create-iiop-listener executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       delete-iiop-listener(1), list-iiop-listeners(1), create-ssl(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日       create-iiop-listener(1)
