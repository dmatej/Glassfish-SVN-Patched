delete-iiop-listener(1)     asadmin 实用程序子命令     delete-iiop-listener(1)

名称
       delete-iiop-listener - 删除 IIOP 监听程序

提要
           delete-iiop-listener [--help] [--target target] listener_id

说明
       delete-iiop-listener 子命令删除指定的 IIOP
       监听程序。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --target
           指定要从其中删除 IIOP 监听程序的目标。有效值为

           server
               从默认服务器实例 server 删除监听程序，这是默认值。

           configuration_name
               从命名配置删除监听程序。

           cluster_name
               从集群中的每个服务器实例删除监听程序。

           instance_name
               从特定服务器实例删除监听程序。

操作数
       listener_id
           要删除的 IIOP 监听程序的唯一标识符。

示例
       Example 1, 删除 IIOP 监听程序
           以下命令删除名为 sample_iiop_listener 的 IIOP 监听程序：

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日       delete-iiop-listener(1)
