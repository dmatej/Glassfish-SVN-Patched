list-iiop-listeners(1)      asadmin 公用程式子命令      list-iiop-listeners(1)

參考名
       list-iiop-listeners - 列出現有的 IIOP 監聽程式

概要
           list-iiop-listeners [--help]
           [target]

描述
       list-iiop-listeners 子命令會列出現有的 IIOP
       監聽程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       target
           此運算元會指定列出之 IIOP 監聽程式的目標。有效值包括

           server
               列出預設伺服器執行處理 server 中的監聽程式，且為其預設值。

           configuration_name
               列出指定組態中的監聽程式。

           cluster_name
               列出指定叢集中的監聽程式。

           instance_name
               列出特定伺服器執行處理中的監聽程式。

範例
       範例 1, 使用 list-iiop-listeners 子命令
           下列命令會列出伺服器執行處理的所有 IIOP 網路監聽程式：

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日        list-iiop-listeners(1)
