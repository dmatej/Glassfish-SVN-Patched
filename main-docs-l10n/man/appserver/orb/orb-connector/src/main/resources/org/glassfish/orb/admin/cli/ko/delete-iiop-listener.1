delete-iiop-listener(1)   asadmin 유틸리티 하위 명령   delete-iiop-listener(1)

제목
       delete-iiop-listener - IIOP 리스너 제거

시놉시스
           delete-iiop-listener [--help] [--target target] listener_id

설명
       delete-iiop-listener 하위 명령은 지정된 IIOP 리스너를 제거합니다. 이
       하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           IIOP 리스너를 삭제할 대상을 지정합니다. 적합한 값은 다음과
           같습니다.

           server
               기본 서버 인스턴스 server에서 리스너를 삭제하고 이는
               기본값입니다.

           configuration_name
               이름이 지정된 구성에서 리스너를 삭제합니다.

           cluster_name
               클러스터에 있는 모든 서버 인스턴스에서 리스너를 삭제합니다.

           instance_name
               특정 서버 인스턴스에서 리스너를 삭제합니다.

피연산자
       listener_id
           삭제할 IIOP 리스너의 고유 식별자입니다.

예제
       예 1, IIOP 리스너 삭제
           다음 명령은 sample_iiop_listener라는 IIOP 리스너를 삭제합니다.

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                       2010년 8월 18일        delete-iiop-listener(1)
