delete-iiop-listener(1) Subcomandos do utilitário asad delete-iiop-listener(1)

NOME
       delete-iiop-listener - remove um listener IIOP

SINOPSE
           delete-iiop-listener [--help] [--target target] listener_id

DESCRIçãO
       O subcomando delete-iiop-listener remove o listener IIOP especificado.
       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Especifica o alvo do qual o listener IIOP está sendo deletado. Os
           valores válidos são

           server
               Deleta o listener da instância do servidor default server e é o
               valor default.

           configuration_name
               Deleta o listener da configuração nomeada.

           cluster_name
               Deleta o listener de cada instância do servidor no cluster.

           instance_name
               Deleta o listener de uma instância específica do servidor.

OPERANDOS
       listener_id
           O identificador único do listener IIOP a ser deletado.

EXEMPLOS
       Example 1, Deletando um Listener IIOP
           O comando a seguir deleta o listener IIOP com o nome
           sample_iiop_listener:

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                         18 ago 2010          delete-iiop-listener(1)
