list-iiop-listeners(1)  Unterbefehle des asadmin-Utili  list-iiop-listeners(1)

NAME
       list-iiop-listeners - listet die vorhandenen IIOP-Listener auf

ZUSAMMENFASSUNG
           list-iiop-listeners [--help]
           [target]

BESCHREIBUNG
       Der Unterbefehl list-iiop-listeners listet die vorhandenen
       IIOP-Listener auf. Dieser Unterbefehl wird nur im Remote-Modus
       unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

OPERANDEN
       target
           Dieser Operand gibt das Ziel an, für das die IIOP-Listener
           aufgelistet werden sollen. Gültige Werte sind:

           server
               Listet die Listener in der standardmäßigen Serverinstanz server
               auf und ist der Standardwert.

           configuration_name
               Listet die Listener in der angegebenen Konfiguration auf.

           cluster_name
               Listet die Listener im angegebenen Cluster auf.

           instance_name
               Listet die Listener in einer bestimmten Serverinstanz auf.

BEISPIELE
       Beispiel 1, Unterbefehl "list-iiop-listeners" verwenden
           Der folgende Befehl listet alle IIOP-Listener für die Serverinstanz
           auf:

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                        18. Aug. 2010          list-iiop-listeners(1)
