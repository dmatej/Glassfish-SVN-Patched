create-iiop-listener(1) Unterbefehle des asadmin-Utili create-iiop-listener(1)

NAME
       create-iiop-listener - fügt einen IIOP-Listener hinzu

ZUSAMMENFASSUNG
           create-iiop-listener [--help] --listeneraddress address
           [--iiopport iiop-port-number] [--securityenabled={false|true}] [--enabled={true|false}]
           [--property (name=value)[:name=value]*]
           [--target target] listener_id

BESCHREIBUNG
       Der Unterbefehl create-iiop-listener erstellt einen IIOP-Listener.
       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --listeneraddress
           Entweder die IP-Adresse oder der Hostname (mit DNS auflösbar).

       --iiopport
           Die IIOP-Portnummer. Der Standardwert ist 1072.

       --securityenabled
           Ist der Wert auf "true" gesetzt, führt der IIOP-Listener SSL aus.
           Sie können SSL2 oder SSL3 aktivieren oder deaktivieren und Cipher
           mithilfe eines SSL-Elements festlegen. Die Sicherheitseinstellung
           aktiviert oder deaktiviert SSL global, indem der Serverinstanz
           Zertifikate verfügbar gemacht werden. Der Standardwert ist false.

       --enabled
           Ist der Wert auf "true" gesetzt, ist der IIOP-Listener zur Laufzeit
           aktiviert. Der Standardwert ist true.

       --property
           Optionale Name/Wert-Paare für Attribute zum Konfigurieren des
           IIOP-Listeners.

       --target
           Gibt das Ziel an, für das Sie den IIOP-Listener erstellen. Gültige
           Werte sind

           server
               Erstellt den Listener für die Standardserverinstanz server und
               ist der Standardwert.

           configuration_name
               Erstellt den Listener für die benannte Konfiguration.

           cluster_name
               Erstellt den Listener für jede Serverinstanz im Cluster.

           stand-alone_instance_name
               Erstellt den Listener für eine bestimmte
               Standalone-Serverinstanz.

OPERANDEN
       listener_id
           Eine eindeutige ID für den IIOP-Listener, der erstellt werden soll.

BEISPIELE
       Example 1, IIOP-Listener erstellen
           Der folgende Befehl erstellt einen IIOP-Listener namens
           sample_iiop_listener:

               asadmin> create-iiop-listener --listeneraddress 192.168.1.100
               --iiopport 1400 sample_iiop_listener
               Command create-iiop-listener executed successfully.

       Example 2, IIOP-Listener mit einem Zielcluster erstellen
           Der folgende Befehl erstellt einen IIOP-Listener namens
           iiop_listener_2 für das Cluster mycluster. Er verwendet die
           Zieloption.

               asadmin> create-iiop-listener --listeneraddress 0.0.0.0 --iiopport 1401
               --target mycluster iiop_listener_2
               Command create-iiop-listener executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       delete-iiop-listener(1), list-iiop-listeners(1), create-ssl(1)

       asadmin(1M)

Java EE 7                        18. Aug. 2010         create-iiop-listener(1)
