delete-iiop-listener(1) Subcomandos de la Utilidad asa delete-iiop-listener(1)

NOMBRE DE REFERENCIA
       delete-iiop-listener - Elimina un listener IIOP.

SINOPSIS
           delete-iiop-listener [--help] [--target target] listener_id

DESCRIPCIÓN
       El subcomando delete-iiop-listener elimina el listener IIOP
       especificado. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --target
           Especifica el destino del que se va a suprimir el listener IIOP.
           Los valores válidos son:

           server
               Suprime el listener de la instancia de servidor por defecto
               server y es el valor por defecto.

           configuration_name
               Suprime el listener de la configuración con nombre.

           cluster_name
               Suprime el listener de todas las instancias de servidor del
               cluster.

           instance_name
               Suprime el listener de una instancia de servidor específica.

OPERANDOS
       listener_id
           Identificador único de listener IIOP que se va a suprimir.

EJEMPLOS
       Ejemplo 1, Supresión de un Listener IIOP
           El siguiente comando suprime el listener IIOP denominado
           sample_iiop_listener:

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                    18 de agosto de 2010      delete-iiop-listener(1)
