list-iiop-listeners(1)    asadmin 유틸리티 하위 명령    list-iiop-listeners(1)

제목
       list-iiop-listeners - 기존 IIOP 리스너 나열

시놉시스
           list-iiop-listeners [--help]
           [target]

설명
       list-iiop-listeners 하위 명령은 기존 IIOP 리스너를 나열합니다. 이 하위
       명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

피연산자
       target
           이 피연산자는 IIOP 리스너를 나열할 대상을 지정합니다. 적합한 값은
           다음과 같습니다.

           server
               기본 서버 인스턴스 server의 리스너를 나열하며 기본값입니다.

           configuration_name
               지정된 구성의 리스너를 나열합니다.

           cluster_name
               지정된 클러스터의 리스너를 나열합니다.

           instance_name
               특정 서버 인스턴스의 리스너를 나열합니다.

예제
       예 1, list-iiop-listeners 하위 명령 사용
           다음 명령은 서버 인스턴스의 모든 IIOP 리스너를 나열합니다.

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                       2010년 8월 18일         list-iiop-listeners(1)
