delete-iiop-listener(1) Unterbefehle des asadmin-Utili delete-iiop-listener(1)

NAME
       delete-iiop-listener - entfernt einen IIOP-Listener

ZUSAMMENFASSUNG
           delete-iiop-listener [--help] [--target target] listener_id

BESCHREIBUNG
       Der Unterbefehl delete-iiop-listener entfernt den angegebenen
       IIOP-Listener. Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie den IIOP-Listener löschen. Gültige
           Werte sind

           server
               Löscht den Listener aus der standardmäßigen Serverinstanz
               server und ist der Standardwert.

           configuration_name
               Löscht den Listener aus der benannten Konfiguration.

           cluster_name
               Löscht den Listener aus jeder Serverinstanz im Cluster.

           instance_name
               Löscht den Listener aus einer bestimmten Serverinstanz.

OPERANDEN
       listener_id
           Die eindeutige ID für den IIOP-Listener, der gelöscht werden soll.

BEISPIELE
       Example 1, IIOP-Listener löschen
           Der folgende Befehl löscht den IIOP-Listener namens
           sample_iiop_listener:

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                        18. Aug. 2010         delete-iiop-listener(1)
