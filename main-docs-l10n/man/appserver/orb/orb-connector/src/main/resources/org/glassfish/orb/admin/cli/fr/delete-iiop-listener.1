delete-iiop-listener(1) Sous-commandes de l'utilitaire delete-iiop-listener(1)

NOM
       delete-iiop-listener - enlève un processus d'écoute IIOP

SYNTAXE
           delete-iiop-listener [--help] [--target target] listener_id

DESCRIPTION
       La sous-commande delete-iiop-listener enlève le processus d'écoute IIOP
       indiqué. Cette sous-commande est prise en charge en mode distant
       uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cette option indique la cible sur laquelle vous supprimez le
           processus d'écoute IIOP. Les valeurs valides sont les suivantes :

           server
               Cette valeur supprime le processus d'écoute de l'instance de
               serveur par défaut (option server) et constitue la valeur par
               défaut.

           configuration_name
               Cette valeur supprime le processus d'écoute pour la
               configuration nommée.

           cluster_name
               Cette valeur supprime le processus d'écoute pour chaque
               instance de serveur du cluster.

           instance_name
               Cette valeur supprime le processus d'écoute pour une instance
               de serveur particulière.

OPÉRANDES
       listener_id
           Identificateur unique du processus d'écoute IIOP à supprimer.

EXEMPLES
       Exemple 1, Suppression d'un processus d'écoute IIOP
           La commande suivante supprime le processus d'écoute IIOP nommé
           sample_iiop_listener :

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                         18 aoû 2010          delete-iiop-listener(1)
