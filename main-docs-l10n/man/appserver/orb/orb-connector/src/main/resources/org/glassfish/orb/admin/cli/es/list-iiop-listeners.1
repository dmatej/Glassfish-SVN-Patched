list-iiop-listeners(1)  Subcomandos de la Utilidad asa  list-iiop-listeners(1)

NOMBRE DE REFERENCIA
       list-iiop-listeners - Muestra los listeners IIOP existentes.

SINOPSIS
           list-iiop-listeners [--help]
           [target]

DESCRIPCIÓN
       El subcomando list-iiop-listeners muestra los listeners IIOP
       existentes. Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Este operando especifica el destino del que se deben mostrar los
           listeners IIOP. Los valores válidos son:

           server
               Muestra los listeners en la instancia de servidor por defecto
               server y es el valor por defecto.

           configuration_name
               Muestra los listeners en la configuración especificada.

           cluster_name
               Muestra los listeners en el cluster especificado.

           instance_name
               Muestra los listeners en una instancia de servidor determinada.

EJEMPLOS
       Ejemplo 1, Uso del subcomando list-iiop-listeners
           El siguiente comando muestra todos los listeners IIOP para la
           instancia de servidor:

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                    18 de agosto de 2010       list-iiop-listeners(1)
