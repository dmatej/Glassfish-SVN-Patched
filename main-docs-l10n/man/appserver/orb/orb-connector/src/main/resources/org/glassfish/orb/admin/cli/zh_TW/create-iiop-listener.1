create-iiop-listener(1)     asadmin 公用程式子命令     create-iiop-listener(1)

參考名
       create-iiop-listener - 新增 IIOP 監聽程式

概要
           create-iiop-listener [--help] --listeneraddress address
           [--iiopport iiop-port-number] [--securityenabled={false|true}] [--enabled={true|false}]
           [--property (name=value)[:name=value]*]
           [--target target] listener_id

描述
       create-iiop-listener 子命令會建立 IIOP
       監聽程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --listeneraddress
           IP 位址或主機名稱 (可用 DNS 解析)。

       --iiopport
           IIOP 連接埠號碼。預設值為 1072。

       --securityenabled
           如果設定為 true，則 IIOP 監聽程式會執行 SSL。您可以開啟或關閉 SSL2
           或 SSL3，並使用 SSL
           元素設定密碼。安全性設定藉由使憑證可供伺服器執行處理使用來全域啟用或停用
           SSL。預設值為 false。

       --enabled
           如果設定為 true，則會在執行階段啟用 IIOP 監聽程式。預設值為 true。

       --property
           用於組態 IIOP 監聽程式的選擇性屬性名/值配對。

       --target
           指定您要用來建立 IIOP 監聽程式的目標。有效值包括

           server
               為預設伺服器執行處理 server 建立監聽程式，且為預設值。

           configuration_name
               為已命名的組態建立監聽程式。

           cluster_name
               為叢集中的每個伺服器執行處理建立監聽程式。

           stand-alone_instance_name
               為特定的獨立伺服器執行處理建立監聽程式。

運算元
       listener_id
           要建立的 IIOP 監聽程式之唯一識別碼。

範例
       範例 1, 建立 IIOP 監聽程式
           下列命令會建立名稱為 sample_iiop_listener 的 IIOP 監聽程式：

               asadmin> create-iiop-listener --listeneraddress 192.168.1.100
               --iiopport 1400 sample_iiop_listener
               Command create-iiop-listener executed successfully.

       範例 2, 使用「目標叢集」建立 IIOP 監聽程式
           下列命令會為叢集 mycluster 建立名稱為 iiop_listener_2 的 IIOP
           監聽程式。它使用目標選項。

               asadmin> create-iiop-listener --listeneraddress 0.0.0.0 --iiopport 1401
               --target mycluster iiop_listener_2
               Command create-iiop-listener executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       delete-iiop-listener(1), list-iiop-listeners(1), create-ssl(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日       create-iiop-listener(1)
