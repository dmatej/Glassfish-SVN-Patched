list-iiop-listeners(1)  Comandi secondari della utilit  list-iiop-listeners(1)

NOME
       list-iiop-listeners - elenca i listener IIOP esistenti

SINTASSI
           list-iiop-listeners [--help]
           [target]

DESCRIZIONE
       Il comando secondario list-iiop-listeners elenca i listener IIOP
       esistenti. Questo comando secondario è supportato solo in modalità
       remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Questo operando specifica la destinazione per cui devono essere
           elencati i listener IIOP. Di seguito sono riportati i valori
           validi.

           server
               Elenca i listener dell'istanza server predefinita server ed è
               il valore predefinito.

           configuration_name
               Elenca i listener nella configurazione specificata.

           cluster_name
               Elenca i listener nel cluster specificato.

           instance_name
               Elenca i listener per un'istanza server particolare.

ESEMPI
       Esempio 1, Uso del comando secondario list-iiop-listeners
           Il comando seguente elenca tutti i listener IIOP per l'istanza
           server:

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                       18 agosto 2010          list-iiop-listeners(1)
