list-iiop-listeners(1)  Subcomandos do utilitário asad  list-iiop-listeners(1)

NOME
       list-iiop-listeners - lista os listeners IIOP existentes

SINOPSE
           list-iiop-listeners [--help]
           [target]

DESCRIçãO
       O subcomando list-iiop-listeners lista os listeners IIOP existentes.
       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Esse operando especifica o alvo para o qual os listeners IIOP
           deverão ser listados. Os valores válidos são:

           server
               Lista os listeners na instância do servidor default server e é
               o valor default.

           configuration_name
               Lista os listeners na configuração especificada.

           cluster_name
               Lista os listeners no cluster especificado.

           instance_name
               Lista os listeners em uma instância do servidor específica.

EXEMPLOS
       Example 1, Utilizando o subcomando list-iiop-listeners
           O seguinte comando lista todos os listeners IIOP da instância do
           servidor:

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                         18 ago 2010           list-iiop-listeners(1)
