list-iiop-listeners(1)asadminユーティリティのサブコマンド

名前
       list-iiop-listeners - 既存のIIOPリスナーをリストします

概要
           list-iiop-listeners [--help]
           [target]

説明
       list-iiop-listenersサブコマンドは既存のIIOPリスナーをリストします。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           このオペランドは、IIOPリスナーをリストするターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverのリスナーをリストします(デフォルト値)。

           configuration_name
               指定した構成のリスナーをリストします。

           cluster_name
               指定したクラスタのリスナーをリストします。

           instance_name
               特定のサーバー・インスタンスのリスナーをリストします。

例
       例 1, list-iiop-listenersサブコマンドの使用
           次のコマンドは、サーバー・インスタンスのすべてのIIOPリスナーをリストします。

               asadmin> list-iiop-listeners
               orb-listener-1
               SSL
               SSL_MUTUALAUTH
               sample_iiop_listener
               Command list-iiop-listeners executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-iiop-listener(1), delete-iiop-listener(1)

       asadmin(1M)

Java EE 7                        2010年8月18日          list-iiop-listeners(1)
