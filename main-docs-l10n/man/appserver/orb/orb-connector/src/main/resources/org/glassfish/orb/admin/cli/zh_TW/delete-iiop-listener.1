delete-iiop-listener(1)     asadmin 公用程式子命令     delete-iiop-listener(1)

參考名
       delete-iiop-listener - 移除 IIOP 監聽程式

概要
           delete-iiop-listener [--help] [--target target] listener_id

描述
       delete-iiop-listener 子命令會移除指定的 IIOP
       監聽程式。只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要從中刪除 IIOP 監聽程式的目標。有效值包括

           server
               從預設伺服器執行處理 server 刪除監聽程式，且此為預設值。

           configuration_name
               從指定組態刪除監聽程式。

           cluster_name
               從叢集中的每個伺服器執行處理刪除監聽程式。

           instance_name
               從特定伺服器執行處理刪除監聽程式。

運算元
       listener_id
           要刪除的 IIOP 監聽程式的唯一識別碼。

範例
       範例 1, 刪除 IIOP 監聽程式
           下列命令會建立刪除名稱為 sample_iiop_listener 的 IIOP 監聽程式：

               asadmin> delete-iiop-listener sample_iiop_listener
               Command delete-iiop-listener executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-iiop-listener(1), list-iiop-listeners(1)

       asadmin(1M)

Java EE 7                     2010 年 8 月 18 日       delete-iiop-listener(1)
