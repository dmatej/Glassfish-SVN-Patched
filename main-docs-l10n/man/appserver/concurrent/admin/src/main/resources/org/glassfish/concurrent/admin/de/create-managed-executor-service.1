create-managed-executor-Unterbefehle des asacreate-managed-executor-service(1)

NAME
       create-managed-executor-service - erstellt eine verwaltete
       Executor-Serviceressource

ZUSAMMENFASSUNG
           create-managed-executor-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--longrunningtasks[={false|true}]]]
           [--hungafterseconds hungafterseconds]
           [--corepoolsize corepoolsize]
           [--maximumpoolsize maximumpoolsize]
           [--keepaliveseconds keepaliveseconds]
           [--threadlifetimeseconds threadlifetimeseconds]
           [--taskqueuecapacity taskqueuecapacity]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

BESCHREIBUNG
       Der Unterbefehl create-managed-executor-service erstellt eine
       verwaltete Executor-Serviceressource.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --enabled
           Gibt an, ob die verwaltete Executor-Serviceressource zur Laufzeit
           aktiviert ist.

           Der Standardwert ist true.

       --contextinfo
           Eine durch Komma getrennte Textzeichenfolge, die die zu
           propagierenden Kontexttypen angibt. Die Kontexttypen lauten
           Security, Classloader, JNDI und WorkArea. Wird diese Angabe
           ausgelassen, werden alle Kontexttypen propagiert.

       --threadpriority
           Legt die Priorität der Threads fest, die für die verwaltete
           Executor-Serviceinstanz erstellt werden sollen.

           Der Standardwert ist 5.

       --longrunningtasks
           Legt fest, wie ein Container Threads verarbeiten soll. Ist der Wert
           auf true gesetzt, werden hängende Threads nicht gemeldet, und das
           Herunterfahren des verwalteten Executor-Service wird nicht durch
           Warten auf Tasks mit langer Ausführungszeit blockiert. Der
           Standardwert ist false.

       --hungafterseconds
           Gibt die Zeit in Sekunden an, die eine Task ausgeführt werden darf,
           bevor sie als hängend eingestuft wird. Wird diese Angabe
           ausgelassen, werden Tasks nie als hängend eingestuft.

       --corepoolsize
           Gibt die Anzahl der Threads an, die in einem Threadpool beibehalten
           werden sollen.

           Der Standardwert ist 0.

       ---maximumpoolsize
           Gibt die maximale Anzahl von Threads an, die in einem Threadpool
           enthalten sein können.

           Der Standardwert ist 2147483647.

       --keepaliveseconds
           Gibt die Zeit in Sekunden an, die Threads im Leerlauf verbleiben
           dürfen, wenn die Anzahl der Threads größer als die Core-Größe ist.

           Der Standardwert ist 60.

       --threadlifetimeseconds
           Gibt die Zeit in Sekunden an, die die einzelnen Threads in einem
           Threadpool verbleiben, bevor sie gelöscht werden. Dies ist
           unabhängig davon, ob die Anzahl der Threads größer als die
           Core-Größe ist oder ob sich ein Thread im Leerlauf befindet. Bei
           der Ausführung von Anforderungen werden Threads nicht unterbrochen.
           Threads, die den Wert "thread-lifetime-seconds" überschreiten,
           kehren nach Abschluss der aktuellen Anforderung nicht in den
           Threadpool zurück.

           Der Standardwert ist 0.

       --taskqueuecapacity
           Gibt die Größe der Queue an, in der auf Ausführung wartende
           weitergeleitete Tasks aufbewahrt werden.

           Der Standardwert ist 2147483647.

       --description
           Text mit Einzelheiten zur verwalteten Executor-Serviceressource.

       --property
           Optionale Name/Wert-Paare für Attribute zum Konfigurieren der
           verwalteten Executor-Serviceinstanz.

       --target
           Das Ziel, für das die verwaltete Executor-Serviceressource erstellt
           wird.

OPERANDEN
       jndi_name
           Der JNDI-Name für die Suche nach der verwalteten
           Executor-Serviceinstanz.

BEISPIELE
       Example 1, Verwaltete Executor-Serviceressourcen erstellen
           In diesem Beispiel wird eine verwaltete Executor-Serviceressource
           erstellt.

               asadmin> create-managed-executor-service concurrent/SampleExecutor
               Managed executor service concurrent/SampleExecutor created successfully.
               Command create-managed-executor-service executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-managed-executor-service(1), list-managed-executor-services(1)

       asadmin(1M)

Java EE 7                        09. Feb. 20create-managed-executor-service(1)
