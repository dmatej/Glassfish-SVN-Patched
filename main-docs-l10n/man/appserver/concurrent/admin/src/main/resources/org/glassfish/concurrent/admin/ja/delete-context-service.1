delete-context-serviceasadminユーティリティのサブコマdelete-context-service(1)

名前
       delete-context-service -
       指定したJNDI名のコンテキスト・サービス・リソースを除去します

概要
           delete-context-service [--help]
           [--target target]
           context_service_name

説明
       delete-context-serviceサブコマンドは、指定したJNDI名のコンテキスト・サービス・リソースを除去します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --target
           リソースを削除するターゲットを指定します。有効なターゲットは次のとおりです:

           server
               デフォルトのサーバー・インスタンスからリソースを削除します。これがデフォルト値です。

           domain
               ドメインからリソースを削除します。

           cluster_name
               指定したクラスタ内のすべてのサーバー・インスタンスからリソースを削除します。

           instance_name
               指定したサーバー・インスタンスからリソースを削除します。

オペランド
       context_service_name
           削除されるリソースのJNDI名。

例
       例 1, コンテキスト・サービス・リソースの削除
           この例では、concurrent/myContextServiceというコンテキスト・サービス・リソースを削除します。

               asadmin> delete-context-service concurrent/myContextService
               Context service concurrent/myContextService deleted successfully.
               Command delete-context-service executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                        2013年2月9日        delete-context-service(1)
