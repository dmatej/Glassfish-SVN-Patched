list-managed-thread-factComandi)secondari delllist-managed-thread-factories(1)

NOME
       list-managed-thread-factories - elenca le risorse di thread factory
       gestito

SINTASSI
           list-managed-thread-factories [--help]
           [target]

DESCRIZIONE
       Il comando secondario list-managed-thread-factories elenca le risorse
       di thread factory gestito.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Specifica la destinazione per la quale devono essere elencate le
           risorse di thread factory gestito. Le destinazioni valide sono:

           server
               Elenca le risorse dell'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Elenca le risorse per il dominio.

           cluster-name
               Elenca le risorse di tutte le istanze server del cluster
               specificato.

           instance-name
               Elenca le risorse di un'istanza server specificata.

ESEMPI
       Esempio 1, Visualizzazione della lista di risorse di thread factory
       gestito
           In questo esempio vengono elencate le risorse di thread factory
           gestito dell'istanza server predefinita.

               asadmin> list-managed-thread-factories
               concurrent/__defaultManagedThreadFactory
               concurrent/myThreadFactory1
               concurrent/myThreadFactory2
               Command list-managed-thread-factories executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-managed-thread-factory(1), delete-managed-thread-factory(1)

       asadmin(1M)

Java EE 7                       9 febbraio 201list-managed-thread-factories(1)
