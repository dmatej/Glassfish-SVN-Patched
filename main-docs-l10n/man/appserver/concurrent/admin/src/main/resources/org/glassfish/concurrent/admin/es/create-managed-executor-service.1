create-managed-executor-Subcomandos de la Utcreate-managed-executor-service(1)

NOMBRE DE REFERENCIA
       create-managed-executor-service - creates a managed executor service
       resource

SINOPSIS
           create-managed-executor-service [--help]
           [--enabled={false|true}]
           [--contextinfoenabled={false|true}]
           [--contextinfo={Classloader|JNDI|Security|WorkArea}]
           [--threadpriority threadpriority]
           [--longrunningtasks={false|true}]
           [--hungafterseconds hungafterseconds]
           [--corepoolsize corepoolsize]
           [--maximumpoolsize maximumpoolsize]
           [--keepaliveseconds keepaliveseconds]
           [--threadlifetimeseconds threadlifetimeseconds]
           [--taskqueuecapacity taskqueuecapacity]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIPCIÓN
       El subcomando create-managed-executor-service crea un recurso de
       servicio de ejecutor gestionado.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --enabled
           Determina si el recurso está activado en tiempo de ejecución. El
           valor por defecto es true.

       --contextinfoenabled
           Determina si los contextos de contenedor se propagan a los threads.
           Si se define en true, los contextos especificados en la opción
           --contextinfo se propagan. Si se define en false, no se propaga
           ningún contexto y se ignora la opción --contextos contextinfo. El
           valor por defecto es true.

       --contextinfo
           Especifica los contextos de contenedor individuales que propagar a
           los threads. Los valores válidos son classloader, JNDI, seguridad y
           área de trabajo. Los valores se especifican en una lista separada
           por comas y no son sensibles a mayúsculas/minúsculas. Todos los
           contextos se propagan por defecto.

       --threadpriority
           Especifica la prioridad que asignar a los threads creados. El valor
           por defecto es 5.

       --longrunningtasks
           Especifica si el recurso se debe utilizar para las tareas de larga
           ejecución. El valor por defecto es false. Si se define en true, las
           tareas de larga ejecución no se notificarán como paradas.

       --hungafterseconds
           Especifica el número de segundos que se puede ejecutar una tarea
           antes de que se considere que no responde. El valor por defecto es
           0, lo que significa que las tareas nunca se consideran que no
           responden.

       --corepoolsize
           Especifica el número de threads que se deben mantener en un pool de
           threads, incluso si están inactivos. El valor por defecto es 0.

           Cuando se ejecuta una tarea nueva y el número de threads en
           ejecución es menor que corepoolsize, se crea un nuevo thread para
           manejar la solicitud. Si el valor para corepoolsize es 0 (valor por
           defecto), nunca se crean nuevos threads a menos que la cola de
           tareas esté llena o el recurso esté utilizando la función de
           introducción directa en cola. Esta función se activa cuando
           taskqueuecapacity es 0 o taskqueuecapacity es 2147483647 y
           corepoolsize equivale a 0.

       --maximumpoolsize
           Especifica el número máximo de threads que un pool de threads puede
           contener. El valor por defecto es 2147483647, lo que significa que
           el pool de threads básicamente no está enlazado y puede contener
           cualquier número de threads.

       --keepaliveseconds
           Especifica el número de segundos que los threads pueden permanecer
           inactivos cuando el número de threads es mayor que corepoolsize. El
           valor por defecto es 60.

       --threadlifetimeseconds
           Especifica el número de segundos que los threads pueden permanecer
           en un pool de threads antes de que se depuren, independientemente
           de si el número de threads es mayor que corepoolsize o si los
           threads están inactivos. El valor por defecto es 0, lo que
           significa que los threads nunca se depuran.

       --taskqueuecapacity
           Especifica el número de tareas ejecutadas que se pueden almacenar
           en la cola de tareas en espera de ejecución. El valor por defecto
           es 2147483647, lo que significa que la cola de tareas básicamente
           no está enlazada y puede almacenar cualquier número de tareas
           ejecutadas.

       --description
           Detalles descriptivos sobre el recurso.

       --property
           Pares de nombre/valor de atributo opcional para configurar el
           recurso.

           GlassFish Server no define ninguna propiedad adicional para este
           recurso. Además, este recurso actualmente no utiliza propiedades
           adicionales.

       --target
           Especifica el destino para el que se va a crear el recurso. Los
           destinos válidos son:

           server
               Crea el recurso para la instancia de servidor por defecto. Este
               es el valor por defecto.

           domain
               Crea el recurso para el dominio.

           cluster_name
               Crea el recurso para todas las instancias de servidor del
               cluster especificado.

           instance_name
               Crea el recurso para la instancia de servidor especificada.

OPERANDOS
       jndi_name
           Nombre de JNDI de este recurso.

EJEMPLOS
       Ejemplo 1, Creación de un recurso de servicio de ejecutor gestionado
           En este ejemplo se crea un recurso de servicio de ejecutor
           gestionado denominado concurrent/myExecutor.

               asadmin> create-managed-executor-service concurrent/myExecutor
               Managed executor service concurrent/myExecutor created successfully.
               Command create-managed-executor-service executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-managed-executor-service(1), list-managed-executor-services(1)

       asadmin(1M)

Java EE 7                    09 de febrero dcreate-managed-executor-service(1)
