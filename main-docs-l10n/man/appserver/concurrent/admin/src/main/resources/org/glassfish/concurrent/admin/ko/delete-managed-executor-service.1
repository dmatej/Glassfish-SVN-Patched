delete-managed-executor-seasadmin)유틸리티 하

제목
       delete-managed-executor-service - 지정된 JNDI 이름을 가진 관리 대상
       실행기 서비스 리소스 제거

시놉시스
           delete-managed-executor-service [--help]
           [--target target]
           managed_executor_service_name

설명
       delete-managed-executor-service 하위 명령은 지정된 JNDI 이름을 가진
       관리 대상 실행기 서비스 리소스를 제거합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --target
           리소스를 삭제할 대상을 지정합니다. 적합한 대상은 다음과 같습니다.

           server
               기본 서버 인스턴스에서 리소스를 삭제합니다. 이 값은
               기본값입니다.

           domain
               도메인에서 리소스를 삭제합니다.

           cluster_name
               지정된 클러스터에 있는 모든 서버 인스턴스에서 리소스를
               삭제합니다.

           instance_name
               지정된 서버 인스턴스에서 리소스를 삭제합니다.

피연산자
       managed_executor_service_name
           삭제할 리소스의 JNDI 이름입니다.

예제
       예 1, 관리 대상 실행기 서비스 리소스 삭제
           이 예제에서는 이름이 concurrent/myExecutor인 관리 대상 실행기
           서비스 리소스를 삭제합니다.

               asadmin> delete-managed-executor-service concurrent/myExecutor
               Managed executor service concurrent/myExecutor deleted successfully.
               Command delete-managed-executor-service executed successfully.

종료 상태
       0
           명령이 성공적으로 실행됨

       1
           명령 실행 중 오류 발생

참조
       create-managed-executor-service(1), list-managed-executor-services(1)

       asadmin(1M)

Java EE 7                       2013년 2월 9delete-managed-executor-service(1)
