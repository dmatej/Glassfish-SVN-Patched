list-managed-thread-factSubcomandos de la Utillist-managed-thread-factories(1)

NOMBRE DE REFERENCIA
       list-managed-thread-factories - Muestra los recursos de la fábrica de
       threads gestionados.

SINOPSIS
           list-managed-thread-factories [--help]
           [target]

DESCRIPCIÓN
       El subcomando list-managed-thread-factories muestra los recursos de la
       fábrica de threads gestionados.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

OPERANDOS
       target
           Especifica el destino para el que se van a mostrar los recursos de
           la fábrica de threads gestionados. Los destinos válidos son:

           server
               Muestra los recursos en la instancia de servidor por defecto.
               Este es el valor por defecto.

           domain
               Muestra los recursos para el dominio.

           cluster-name
               Muestra los recursos en todas las instancias de servidor del
               cluster especificado.

           instance-name
               Muestra los recursos en la instancia de servidor especificada.

EJEMPLOS
       Ejemplo 1, Lista de los recursos de la fábrica de threads gestionados
           Este ejemplo muestra los recursos de la fábrica de threads
           gestionados en la instancia de servidor por defecto.

               asadmin> list-managed-thread-factories
               concurrent/__defaultManagedThreadFactory
               concurrent/myThreadFactory1
               concurrent/myThreadFactory2
               Command list-managed-thread-factories executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       create-managed-thread-factory(1), delete-managed-thread-factory(1)

       asadmin(1M)

Java EE 7                    09 de febrero de list-managed-thread-factories(1)
