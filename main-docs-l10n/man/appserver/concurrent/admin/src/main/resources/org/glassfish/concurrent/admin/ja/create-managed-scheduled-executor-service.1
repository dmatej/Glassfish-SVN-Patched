create-managed-schedulasadminユ

名前
       create-managed-scheduled-executor-service -
       管理対象スケジュール済エグゼキュータ・サービス・リソースを作成します

概要
           create-managed-scheduled-executor-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--longrunningtasks[={false|true}]]
           [--hungafterseconds hungafterseconds]
           [--corepoolsize corepoolsize]
           [--maximumpoolsize maximumpoolsize]
           [--keepaliveseconds keepaliveseconds]
           [--threadlifetimeseconds threadlifetimeseconds]
           [--taskqueuecapacity taskqueuecapacity]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

説明
       create-managed-scheduled-executor-serviceサブコマンドは、管理対象スケジュール済エグゼキュータ・サービス・リソースを作成します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --enabled
           管理対象スケジュール済エグゼキュータ・サービス・リソースが実行時に使用可能かどうかを指定します。

           デフォルト値はtrueです。

       --contextinfo
           伝播されるコンテキスト・タイプのリストを指定するカンマ区切りの文字列。コンテキスト・タイプはセキュリティ、クラスローダー、JNDIおよび作業領域です。省略した場合、すべてのコンテキスト・タイプが伝播されます。

       --threadpriority
           管理対象スケジュール済エグゼキュータ・サービス・インスタンスに対して作成されるスレッドの優先度を指定します。

           デフォルト値は5です。

       --longrunningtasks
           コンテナでスレッドを処理する方法を指定します。trueに設定されている場合、スタック・スレッドはレポートされず、長時間実行中のタスクを待機する管理対象スケジュール済エグゼキュータ・サービスの停止はブロックされません。デフォルト値はfalseです。

       --hungafterseconds
           タスクがハングしたとみなされるまで実行可能な時間を秒単位で指定します。省略した場合、タスクがハングしたとみなされることはありません。

       --corepoolsize
           スレッド・プール内に保持するスレッド数を指定します。

           デフォルト値は0です。

       ---maximumpoolsize
           スレッド・プールに含めることができるスレッドの最大数を指定します。

           デフォルト値は2147483647です。

       --keepaliveseconds
           スレッド数がコア・サイズより大きい場合に、スレッドがアイドル状態でいられる秒数を指定します。

           デフォルト値は60です。

       --threadlifetimeseconds
           スレッド数がコア・サイズより大きいかどうか、またはスレッドがアイドル状態であるかどうかにかかわらず、各スレッドがパージされる前にスレッド・プール内に存続できる秒数を指定します。リクエストの実行中は、スレッドは中断されません。thread-lifetime-seconds値を超えるスレッドは、現在のリクエストの完了後にスレッド・プールに返されません。

           デフォルト値は0です。

       --taskqueuecapacity
           実行待ちの送信済タスクを保持するキューのサイズを指定します。

           デフォルト値は2147483647です。

       --description
           管理対象スケジュール済エグゼキュータ・サービスの詳細を説明するテキスト。

       --property
           管理対象エグゼキュータ・サービス・インスタンスを構成する、オプションの属性名と値のペア。

       --target
           管理対象スケジュール済エグゼキュータ・サービス・リソースが作成されるターゲット。

オペランド
       jndi_name
           管理対象スケジュール済エグゼキュータ・サービス・インスタンスをルックアップするためのJNDI名。

例
       例 1, 管理対象スケジュール済エグゼキュータ・サービス・リソースの作成
           この例では、管理対象スケジュール済エグゼキュータ・サービス・リソースを作成します。

               asadmin> create-managed-scheduled-executor-service concurrent/SampleScheduledExecutor
               Managed scheduled executor service concurrent/SampleScheduledExecutor created successfully.
               Command create-managed-scheduled-executor-service executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-managed-scheduled-executor-service(1),
       list-managed-scheduled-executor-services(1)

       asadmin(1M)

Java EE 7                        2create-managed-scheduled-executor-service(1)
