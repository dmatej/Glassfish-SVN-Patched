create-context-service(1) asadmin 유틸리티 하위 명령 create-context-service(1)

제목
       create-context-service - 컨텍스트 서비스 리소스 생성

시놉시스
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

설명
       create-context-service 하위 명령은 컨텍스트 서비스 리소스를 생성합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       ----enabled
           컨텍스트 서비스가 런타임에 사용으로 설정되는지 여부를 지정합니다.

           기본값은 true입니다.

       --contextinfo
           전달할 컨텍스트 유형 목록을 지정하는 쉼표로 구분된 문자열입니다.
           컨텍스트 유형은 Security, Classloader, JNDI 및 WorkArea입니다.
           생략된 경우 모든 컨텍스트 유형이 전달됩니다.

       --description
           컨텍스트 서비스 인스턴스에 대한 세부 설명을 제공하는 정보입니다.

       --property
           컨텍스트 서비스 인스턴스를 구성하기 위한 선택적 속성 이름/값
           쌍입니다.

       --target
           컨텍스트 서비스 리소스를 생성할 대상입니다.

피연산자
       jndi_name
           컨텍스트 서비스 인스턴스를 조회하기 위한 JNDI 이름입니다.

예제
       예 1, 컨텍스트 서비스 리소스 생성
           이 예제는 컨텍스트 서비스 리소스를 생성합니다.

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                       2013년 2월 9일       create-context-service(1)
