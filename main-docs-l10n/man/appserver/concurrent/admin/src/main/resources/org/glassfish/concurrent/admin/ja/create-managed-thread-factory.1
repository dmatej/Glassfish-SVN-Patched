create-managed-thread-asadminユ

名前
       create-managed-thread-factory -
       管理対象スレッド・ファクトリ・リソースを作成します

概要
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

説明
       create-managed-thread-factoryサブコマンドは、管理対象スレッド・ファクトリ・リソースを作成します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --enabled
           管理対象スレッド・ファクトリが実行時に使用可能かどうかを指定します。

           デフォルト値はtrueです。

       --contextinfo
           伝播されるコンテキスト・タイプを指定するカンマ区切りテキスト文字列。コンテキスト・タイプはセキュリティ、クラスローダー、JNDIおよび作業領域です。省略した場合、すべてのコンテキスト・タイプが伝播されます。

       --threadpriority
           管理対象スレッド・ファクトリ・インスタンスに対して作成されるスレッドの優先度を指定します。

           デフォルト値は5です。

       --description
           管理対象スレッド・ファクトリの詳細を説明するテキスト。

       --property
           管理対象スレッド・ファクトリ・インスタンスを構成する、オプションの属性名と値のペア。

       --target
           管理対象スレッド・ファクトリ・リソースが作成されるターゲット。

オペランド
       jndi_name
           管理対象スレッド・ファクトリ・インスタンスをルックアップするためのJNDI名。

例
       例 1, 管理対象スレッド・ファクトリ・リソースの作成
           この例では、管理対象スレッド・ファクトリ・リソースを作成します。

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                        2013年2月9日 create-managed-thread-factory(1)
