create-managed-thread-faSous-commandes de l'utcreate-managed-thread-factory(1)

NOM
       create-managed-thread-factory - crée une ressource de fabrique de
       threads gérée

SYNTAXE
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIPTION
       La sous-commande create-managed-thread-factory crée une ressource de
       fabrique de threads gérée.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --enabled
           Cette option indique si la fabrique de threads gérée est activée
           lors de l'exécution.

           La valeur par défaut est true.

       --contextinfo
           Chaîne de texte indiquant les types de contexte à propager (séparés
           par des virgules). Les types de contexte sont Security,
           Classloader, JNDI et WorkArea. Si cet attribut n'est pas spécifié,
           tous les types de contexte sont propagés.

       --threadpriority
           Cette option indique la priorité des threads à créer pour
           l'instance de fabrique de threads gérée.

           La valeur par défaut est 5.

       --description
           Texte décrivant la fabrique de threads gérée.

       --property
           Paires nom/valeur d'attribut facultatives pour la configuration de
           l'instance de fabrique de threads gérée.

       --target
           Cible pour laquelle la ressource de fabrique de threads gérée est
           créée.

OPÉRANDES
       jndi_name
           Nom JNDI sous lequel rechercher l'instance de fabrique de threads
           gérée.

EXEMPLES
       Exemple 1, Création d'une ressource de fabrique de threads gérée
           Cet exemple montre comment créer une ressource de fabrique de
           threads gérée.

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                         9 fév 2013  create-managed-thread-factory(1)
