delete-managed-thread-faComandi)secondari delldelete-managed-thread-factory(1)

NOME
       delete-managed-thread-factory - rimuove la risorsa di factory thread
       gestito con il nome JNDI specificato

SINTASSI
           delete-managed-thread-factory [--help]
           [--target target]
           managed_thread_factory_name

DESCRIZIONE
       Il comando secondario delete-managed-thread-factory rimuove la risorsa
       di factory thread gestito con il nome JNDI specificato.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione dalla quale viene eliminata la risorsa.
           Le destinazioni valide sono:

           server
               Elimina la risorsa dall'istanza server predefinita. Questo è il
               valore predefinito.

           domain
               Elimina la risorsa dal dominio.

           cluster_name
               Elimina la risorsa da tutte le istanze server del cluster
               specificato.

           instance_name
               Elimina la risorsa JMS dall'istanza server specificata.

OPERANDI
       managed_thread_factory_name
           Il nome JNDI della risorsa da eliminare.

ESEMPI
       Esempio 1, Eliminazione di una risorsa thread factory gestito
           In questo esempio viene eliminata la risorsa di factory thread
           gestito denominata concurrent/myThreadFactory.

               asadmin> delete-managed-thread-factory concurrent/myThreadFactory
               Managed thread factory concurrent/myThreadFactory deleted successfully.
               Command delete-managed-thread-factory executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                       9 febbraio 201delete-managed-thread-factory(1)
