delete-managed-thread-faSous-commandes de l'utdelete-managed-thread-factory(1)

NOM
       delete-managed-thread-factory - supprime une ressource de fabrique de
       threads gérée

SYNTAXE
           delete-managed-thread-factory [--help]
           [--target target]
           managed_thread_factory_name

DESCRIPTION
       La sous-commande delete-managed-thread-factory supprime une ressource
       de fabrique de threads gérée.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --target
           Cible pour laquelle la ressource de fabrique de threads gérée est
           supprimée.

OPÉRANDES
       managed_thread_factory_name
           Nom de la ressource de fabrique de threads gérée à supprimer.

EXEMPLES
       Exemple 1, Suppression d'une ressource de fabrique de threads gérée
           Dans cet exemple, la ressource de fabrique de threads gérée nommée
           est supprimée.

               asadmin> delete-managed-thread-factory

               Command delete-managed-thread-factory executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                         9 fév 2013  delete-managed-thread-factory(1)
