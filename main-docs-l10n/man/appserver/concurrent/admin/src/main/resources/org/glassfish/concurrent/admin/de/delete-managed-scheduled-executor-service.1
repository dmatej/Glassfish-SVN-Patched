delete-managed-scheduledUnterbefehdelete-managed-scheduled-executor-service(1)

NAME
       delete-managed-scheduled-executor-service - entfernt eine Managed
       Scheduled Executor-Serviceressource mit dem angegebenen JNDI-Namen

ZUSAMMENFASSUNG
           delete-managed-scheduled-executor-service [--help]
           [--target target]
           managed_scheduled_executor_service_name

BESCHREIBUNG
       Der Unterbefehl delete-managed-scheduled-executor-service entfernt eine
       Managed Scheduled Executor-Serviceressource mit dem angegebenen
       JNDI-Namen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie die Ressource löschen. Gültige Ziele
           sind:

           server
               Löscht die Ressource aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           domain
               Löscht die Ressource aus der Domain.

           cluster_name
               Löscht die Ressource aus jeder Serverinstanz im angegebenen
               Cluster.

           instance_name
               Löscht die Ressource aus der angegebenen Serverinstanz.

OPERANDEN
       managed_scheduled_executor_service_name
           Der JNDI-Name der Ressource, die gelöscht werden soll.

BEISPIELE
       Beispiel 1, Verwaltete geplante Executor-Serviceressourcen löschen
           In diesem Beispiel wird die Managed Scheduled
           Executor-Serviceressource mit dem Namen
           concurrent/myScheduledExecutor gelöscht.

               asadmin> delete-managed-scheduled-executor-service concurrent/myScheduledExecutor
               Managed scheduled executor service concurrent/myScheduledExecutor deleted successfully.
               Command delete-managed-scheduled-executor-service executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-managed-scheduled-executor-service(1),
       list-managed-scheduled-executor-services(1)

       asadmin(1M)

Java EE 7                        0delete-managed-scheduled-executor-service(1)
