create-managed-thread-faSubcomandos do utilitácreate-managed-thread-factory(1)

NOME
       create-managed-thread-factory - Cria um recurso de fábrica do thread
       gerenciado

SINOPSE
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIçãO
       O subcomando create-managed-thread-factory cria um recurso de fábrica
       do thread gerenciado.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --enabled
           Especifica se a fábrica de threads gerenciados está ativada no
           runtime.

           O valor default é verdadeiro.

       --contextinfo
           Uma string de texto separada por vírgulas que especifica os tipos
           de contextos a serem propagados. Os tipos de contexto são
           Segurança, Carregador de Classe, JNDIC e Área de Trabalho. Se
           omitido, todos os os tipos de contexto serão propagados.

       --threadpriority
           Especifica a prioridade de threads a ser criada para a instância da
           fábrica de threads gerenciados.

           O valor default é 5.

       --description
           Texto que fornece detalhes descritivos sobre a fábrica de threads
           gerenciados.

       --property
           Pares de nome/valor do atributo opcional para configurar a
           instância da fábrica de threads gerenciados.

       --target
           O alvo para o qual o recurso da fábrica de threads gerenciados está
           sendo criado.

OPERANDOS
       jndi_name
           O nome da JNDI para procurar a instância da fábrica de threads
           gerenciados.

EXEMPLOS
       Example 1, Criando um Recurso da Fábrica de Threads Gerenciados
           Este exemplo cria um recurso da fábrica de threads gerenciados.

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                   9 de fevereiro de create-managed-thread-factory(1)
