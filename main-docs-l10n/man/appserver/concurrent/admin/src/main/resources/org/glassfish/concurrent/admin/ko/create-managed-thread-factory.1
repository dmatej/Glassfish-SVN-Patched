create-managed-thread-factasadmin 유틸리티 하위

제목
       create-managed-thread-factory - 관리 대상 스레드 팩토리 리소스 생성

시놉시스
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

설명
       create-managed-thread-factory 하위 명령은 관리 대상 스레드 팩토리
       리소스를 생성합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --enabled
           관리 대상 스레드 팩토리가 런타임에 사용으로 설정되는지 여부를
           지정합니다.

           기본값은 true입니다.

       --contextinfo
           전달할 컨텍스트 유형을 지정하는 쉼표로 구분된 텍스트 문자열입니다.
           컨텍스트 유형은 Security, Classloader, JNDI 및 WorkArea입니다.
           생략된 경우 모든 컨텍스트 유형이 전달됩니다.

       --threadpriority
           관리 대상 스레드 팩토리 인스턴스에 대해 생성할 스레드의 우선순위를
           지정합니다.

           기본값은 5입니다.

       --description
           관리 대상 스레드 팩토리에 대한 세부 설명을 제공하는 텍스트입니다.

       --property
           관리 대상 스레드 팩토리 인스턴스를 구성하기 위한 선택적 속성
           이름/값 쌍입니다.

       --target
           관리 대상 스레드 팩토리 리소스를 생성할 대상입니다.

피연산자
       jndi_name
           관리 대상 스레드 팩토리 인스턴스를 조회하기 위한 JNDI 이름입니다.

예제
       예 1, 관리 대상 스레드 팩토리 리소스 생성
           이 예제는 관리 대상 스레드 팩토리 리소스를 생성합니다.

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                       2013년 2월 9일create-managed-thread-factory(1)
