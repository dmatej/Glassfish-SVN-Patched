create-context-service(1Sous-commandes de l'utilitaircreate-context-service(1)

NOM
       create-context-service - crée une ressource de service de contexte

SYNTAXE
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIPTION
       La sous-commande create-contexte-service crée une ressource de service
       de contexte.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --enabled
           Cette option indique si le service de contexte est activé lors de
           l'exécution.

           La valeur par défaut est true.

       --contextinfo
           Chaîne fournissant la liste des types de contexte à propager
           (séparés par des virgules). Les types de contexte sont Security,
           Classloader, JNDI et WorkArea. Si cet attribut n'est pas spécifié,
           tous les types de contexte sont propagés.

       --description
           Description de l'instance de service de contexte.

       --property
           Paires nom/valeur d'attribut facultatives pour la configuration de
           l'instance de service de contexte.

       --target
           Cible pour laquelle la ressource de service de contexte est créée.

OPÉRANDES
       jndi_name
           Nom JNDI sous lequel rechercher l'instance de service de contexte.

EXEMPLES
       Exemple 1, Création d'une ressource de service de contexte
           Cet exemple montre comment créer une ressource de service de
           contexte.

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                         9 fév 2013         create-context-service(1)
