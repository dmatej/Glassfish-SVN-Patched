create-context-service(1Comandi secondari della utilicreate-context-service(1)

NOME
       create-context-service - crea una risorsa servizio contesto

SINTASSI
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIZIONE
       Il comando secondario create-context-service crea una risorsa di
       servizio contesto.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       ----enabled
           Specifica se il servizio contesto è abilitato in fase di
           esecuzione.

           Il valore predefinito è true.

       --contextinfo
           Una stringa separata da virgole che specifica la lista dei tipi di
           contesto da propagare. I tipi di contesto sono Sicurezza,
           Classloader, JNDI e Area di lavoro. Se il tipo viene omesso,
           vengono propagati tutti i tipi di contesto.

       --description
           Dettagli descrittivi sull'istanza del servizio contesto.

       --property
           Le coppie nome-valore di attributo per la configurazione
           dell'istanza di servizio contesto.

       --target
           La destinazione per la quale viene creata la risorsa servizio
           contesto.

OPERANDI
       jndi_name
           Il nome JNDI per la ricerca dell'istanza di servizio contesto.

ESEMPI
       Esempio 1, Creazione di una risorsa servizio contesto
           In questo esempio viene creata una risorsa servizio contesto.

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                       9 febbraio 2013      create-context-service(1)
