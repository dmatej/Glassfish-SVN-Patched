create-context-service(1Unterbefehle des asadmin-Utilcreate-context-service(1)

NAME
       create-context-service - erstellt eine Kontextserviceressource

ZUSAMMENFASSUNG
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

BESCHREIBUNG
       Der Unterbefehl create-context-service erstellt eine
       Kontextserviceressource.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       ----enabled
           Gibt an, ob der Kontextservice zur Laufzeit aktiviert ist.

           Der Standardwert ist true.

       --contextinfo
           Eine durch Komma getrennte Zeichenfolge, die eine Liste der zu
           propagierenden Kontexttypen angibt. Die Kontexttypen lauten
           Security, Classloader, JNDI und WorkArea. Wird diese Angabe
           ausgelassen, werden alle Kontexttypen propagiert.

       --description
           Einzelheiten zur Kontextserviceinstanz.

       --property
           Optionale Name/Wert-Paare für Attribute zum Konfigurieren der
           Kontextserviceinstanz.

       --target
           Das Ziel, für das die Kontextserviceressource erstellt wird.

OPERANDEN
       jndi_name
           Der JNDI-Name für die Suche nach der Kontextserviceinstanz.

BEISPIELE
       Example 1, Kontextserviceressourcen erstellen
           In diesem Beispiel wird eine Kontextserviceressource erstellt.

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

BEENDIGUNGSSTATUS
       0
           Unterbefehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Unterbefehls

SIEHE AUCH
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                        09. Feb. 2013       create-context-service(1)
