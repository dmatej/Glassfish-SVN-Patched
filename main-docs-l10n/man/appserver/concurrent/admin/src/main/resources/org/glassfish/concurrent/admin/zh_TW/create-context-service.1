create-context-service(1)   asadmin 公用程式子命令   create-context-service(1)

參考名
       create-context-service - 建立環境服務資源

概要
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

描述
       create-context-service 子命令會建立環境服務資源。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       ----enabled
           指定是否在執行階段中啟用環境服務。

           預設值為 true。

       --contextinfo
           指定要傳輸之環境類型清單的字串 (以逗號區隔)。環境類型為
           Security、Classloader、JNDI 和
           WorkArea。如果省略，則會傳輸所有環境類型。

       --description
           有關環境服務執行處理的描述性詳細資訊。

       --property
           用於設定環境服務執行處理的選擇性屬性名/值配對。

       --target
           要建立其環境服務資源的目標。

運算元
       jndi_name
           查詢環境服務執行處理的 JNDI 名稱。

範例
       範例 1, 建立環境服務資源
           此範例會建立環境服務資源。

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

結束狀態
       0
           已成功執行子命令

       1
           執行子命令時發生錯誤

另請參閱
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 9 日     create-context-service(1)
