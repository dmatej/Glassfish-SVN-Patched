delete-context-service(1Unterbefehle des asadmin-Utildelete-context-service(1)

NAME
       delete-context-service - entfernt eine Kontextserviceressource mit dem
       angegebenen JNDI-Namen

ZUSAMMENFASSUNG
           delete-context-service [--help]
           [--target target]
           context_service_name

BESCHREIBUNG
       Der Unterbefehl delete-context-service entfernt eine
       Kontextserviceressource mit dem angegebenen JNDI-Namen.

       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --target
           Gibt das Ziel an, aus dem Sie die Ressource löschen. Gültige Ziele
           sind:

           server
               Löscht die Ressource aus der standardmäßigen Serverinstanz.
               Dies ist der Standardwert.

           domain
               Löscht die Ressource aus der Domain.

           cluster_name
               Löscht die Ressource aus jeder Serverinstanz im angegebenen
               Cluster.

           instance_name
               Löscht die Ressource aus der angegebenen Serverinstanz.

OPERANDEN
       context_service_name
           Der JNDI-Name der Ressource, die gelöscht werden soll.

BEISPIELE
       Beispiel 1, Kontextserviceressourcen löschen
           In diesem Beispiel wird die Kontextserviceressource mit dem Namen
           concurrent/myContextService gelöscht.

               asadmin> delete-context-service concurrent/myContextService
               Context service concurrent/myContextService deleted successfully.
               Command delete-context-service executed successfully.

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       create-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                        09. Feb. 2013       delete-context-service(1)
