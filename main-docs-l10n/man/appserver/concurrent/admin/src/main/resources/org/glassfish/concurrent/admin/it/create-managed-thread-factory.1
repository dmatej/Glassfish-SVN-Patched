create-managed-thread-faComandi)secondari dellcreate-managed-thread-factory(1)

NOME
       create-managed-thread-factory - crea una risorsa thread factory gestito

SINTASSI
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIZIONE
       Il comando secondario create-managed-thread-factory crea una risorsa
       thread factory gestito.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --enabled
           Specifica se la risorsa thread factory gestito è abilitata in fase
           di esecuzione.

           Il valore predefinito è true.

       --contextinfo
           Una stringa di testo separata da virgole che specifica i tipi di
           contesto da propagare. I tipi di contesto sono Sicurezza,
           Classloader, JNDI e Area di lavoro. Se il tipo viene omesso,
           vengono propagati tutti i tipi di contesto.

       --threadpriority
           Specifica la priorità dei thread da creare per l'istanza di thread
           factory gestito.

           Il valore predefinito è 5.

       --description
           Il testo della descrizione dettagliata del thread factory gestito.

       --property
           Le coppie nome-valore di attributo per la configurazione
           dell'istanza di thread factory gestito.

       --target
           La destinazione per la quale viene creata la risorsa thread factory
           gestito.

OPERANDI
       jndi_name
           Il nome JNDI per la ricerca dell'istanza di thread factory gestito.

ESEMPI
       Esempio 1, Creazione di una risorsa thread factory gestito
           In questo esempio viene creata una risorsa thread factory gestito.

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                       9 febbraio 201create-managed-thread-factory(1)
