create-managed-thread-faSubcomandos de la Utilcreate-managed-thread-factory(1)

NOMBRE DE REFERENCIA
       create-managed-thread-factory - Crea un recurso de fábrica de threads
       gestionado.

SINOPSIS
           create-managed-thread-factory [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--threadpriority threadpriority]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

DESCRIPCIÓN
       El subcomando create-managed-thread-factory crea un recurso de fábrica
       de threads gestionado.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --enabled
           Especifica la posibilidad de que la fábrica de threads gestionada
           está activada en tiempo de ejecución.

           El valor por defecto es true.

       --contextinfo
           Cadena de texto separada por comas que especifica los tipos de
           contexto que se van a propagar. Los tipos de contexto son
           Seguridad, Classloader, JNDI y Área de trabajo. Si se omite, todos
           los tipos de contexto se propagan.

       --threadpriority
           Especifica la prioridad de threads que se van a crear para la
           instancia de fábrica de threads gestionada.

           El valor por defecto es 5.

       --description
           Texto que proporciona detalles descriptivos acerca de la fábrica de
           threads gestionada.

       --property
           Pares de nombre/valor de atributo opcional para configurar la
           instancia de fábrica de threads gestionada.

       --target
           El destino para el que el recurso de fábrica de threads gestionado
           se está creando.

OPERANDOS
       jndi_name
           El nombre de JNDI para buscar la instancia de fábrica de threads
           gestionada.

EJEMPLOS
       Ejemplo 1, Creación de un recurso de fábrica de threads gestionado
           En este ejemplo se crea un recurso de fábrica de threads
           gestionado.

               asadmin> create-managed-thread-factory concurrent/SampleThreadFactory
               Managed thread factory concurrent/SampleThreadFactory created successfully.
               Command create-managed-thread-factory executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       delete-managed-thread-factory(1), list-managed-thread-factories(1)

       asadmin(1M)

Java EE 7                    09 de febrero de create-managed-thread-factory(1)
