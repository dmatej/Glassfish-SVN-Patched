create-context-service(1)   asadmin 实用程序子命令   create-context-service(1)

名称
       create-context-service - 创建上下文服务资源

提要
           create-context-service [--help]
           [--enabled[={false|true}]]
           [--contextinfo contextinfo]
           [--description description]
           [--property property]
           [--target target]
           jndi_name

说明
       create-context-service 子命令可创建上下文服务资源。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       ----enabled
           指定运行时是否启用上下文服务。

           默认值为真

       --contextinfo
           指定要传播的上下文类型列表的逗号分隔字符串。上下文类型为“Security”、“Classloader”、“JNDI”和“WorkArea”。如果省略，则传播所有上下文类型。

       --description
           关于上下文服务实例的描述性详细信息。

       --property
           用于配置上下文服务实例的可选属性名称/值对。

       --target
           要为其创建上下文服务资源的目标。

操作数
       jndi_name
           用于查找上下文服务实例的 JNDI 名称。

示例
       Example 1, 创建上下文服务资源
           此示例创建一个上下文服务资源。

               asadmin> create-context-service concurrent/SampleContextService
               Context service concurrent/SampleContextService created successfully.
               Command create-context-service executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       delete-context-service(1), list-context-services(1)

       asadmin(1M)

Java EE 7                      2013 年 2 月 9 日     create-context-service(1)
