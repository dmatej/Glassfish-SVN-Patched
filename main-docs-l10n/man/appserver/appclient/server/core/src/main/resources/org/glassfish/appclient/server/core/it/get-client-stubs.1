get-client-stubs(1)     Comandi secondari della utilit     get-client-stubs(1)

NOME
       get-client-stubs - recupera i file JAR dell'applicazione necessari per
       avviare il client dell'applicazione

SINTASSI
           get-client-stubs [--help] --appname application_name local_directory_path

DESCRIZIONE
       Il comando secondario get-client-stubs copia i file JAR richiesti per
       un modulo standalone AppClient o per ciascun modulo AppClient di
       un'applicazione dal server alla directory locale. Viene recuperato il
       file JAR generato di ogni client, oltre agli eventuali file JAR di
       supporto richiesti. Il nome del file JAR di un client è nel formato
       app-name Client.jar. Prima di eseguire il comando secondario
       get-client-stubs, è necessario distribuire l'applicazione o il modulo.
       Il file JAR del client generato è utile per eseguire l'applicazione con
       la utility appclient. Questo comando secondario è supportato solo in
       modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --appname
           Il nome dell'applicazione o del modulo client standalone.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per ulteriori
           informazioni sulle versioni di moduli e applicazioni, vedere
           "Module and Application Versions" in Oracle GlassFish Server
           Application Deployment Guide.

OPERANDI
       local_directory_path
           Il percorso della directory locale in cui deve essere memorizzato
           il file JAR degli stub di client.

ESEMPI
       Esempio 1, Uso di get-client-stubs

               asadmin> get-client-stubs --appname myapplication /sample/example
               Command get-client-stubs executed successfully

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       deploy(1), redeploy(1), undeploy(1)

       appclient(1M), asadmin(1M), package-appclient(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      16 novembre 2010            get-client-stubs(1)
