get-client-stubs(1)     Unterbefehle des asadmin-Utili     get-client-stubs(1)

NAME
       get-client-stubs - ruft die Anwendungs-JAR-Dateien ab, die zum Start
       des Anwendungsclients benötigt werden.

ZUSAMMENFASSUNG
           get-client-stubs [--help] --appname application_name local_directory_path

BESCHREIBUNG
       Der Unterbefehl get-client-stubs kopiert die erforderlichen JAR-Dateien
       für ein Standalone-AppClient-Modul oder jedes AppClient-Modul in einer
       Anwendung aus dem Serverrechner in das lokale Verzeichnis. Die
       generierte JAR-Datei jedes Clients wird jeweils zusammen mit
       erforderlichen unterstützenden JAR-Dateien abgerufen. Der Name der
       JAR-Datei des Clients muss die Form app-nameClient.jar haben. Bevor Sie
       den Unterbefehl get-client-stubs ausführen, müssen Sie die Anwendung
       oder das Modul bereitstellen. Die generierte Client-JAR-Datei ist
       nützlich, um die Anwendung mit dem Utility appclient auszuführen.
       Dieser Unterbefehl wird nur im Remote-Modus unterstützt.

OPTIONEN
       --help, -?
           Zeigt den Hilfetext für den Unterbefehl an.

       --appname
           Der Name der Anwendung oder des Standalone-Clientmoduls.

           Der Name kann eine optionale Versions-ID enthalten, die dem Namen
           folgt und von diesem durch einen Doppelpunkt (:) getrennt ist. Die
           Versions-ID muss mit einem Buchstaben oder einer Zahl beginnen. Er
           kann alphanumerische Zeichen sowie Unterstriche (_), Bindestriche
           (-) und Punkte (.) enthalten. Weitere Informationen zu Modul- und
           Anwendungsversionen finden Sie unter "Module and Application
           Versions" im Oracle GlassFish Server Application Deployment Guide.

OPERANDEN
       local_directory_path
           Der Pfad zum lokalen Verzeichnis, in dem die Client-Stub-JAR-Datei
           gespeichert werden soll.

BEISPIELE
       Beispiel 1, get-client-stubs verwenden

               asadmin> get-client-stubs --appname myapplication /sample/example
               Command get-client-stubs executed successfully

BEENDIGUNGSSTATUS
       0
           Befehl erfolgreich ausgeführt

       1
           Fehler beim Ausführen des Befehls

SIEHE AUCH
       deploy(1), redeploy(1), undeploy(1)

       appclient(1M), asadmin(1M), package-appclient(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        16. Nov. 2010             get-client-stubs(1)
