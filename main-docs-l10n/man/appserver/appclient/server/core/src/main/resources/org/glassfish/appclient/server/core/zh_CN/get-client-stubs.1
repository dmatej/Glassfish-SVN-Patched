get-client-stubs(1)         asadmin 实用程序子命令         get-client-stubs(1)

名称
       get-client-stubs - 检索启动应用程序客户机所需的应用程序 JAR 文件。

提要
           get-client-stubs [--help] --appname application_name local_directory_path

说明
       get-client-stubs 子命令将 AppClient 独立模块或应用程序中的每个
       AppClient 模块的所需 JAR
       文件从服务器计算机复制到本地目录。将会检索每个客户机的已生成 JAR
       文件以及任何所需的支持 JAR 文件。客户机 JAR 文件的名称格式为
       app-nameClient.jar。执行 get-client-stubs
       子命令之前，必须部署应用程序或模块。生成的客户机 JAR 文件对于使用
       appclient 实用程序运行应用程序非常有用。该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --appname
           应用程序或独立客户机模块的名称。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.)
           等字符。有关模块和应用程序版本的详细信息，请参阅 Oracle GlassFish
           Server Application Deployment Guide 中的 Module and Application
           Versions。

操作数
       local_directory_path
           应该存储客户机存根 JAR 文件的本地目录的路径。

示例
       Example 1, 使用 get-client-stubs

               asadmin> get-client-stubs --appname myapplication /sample/example
               Command get-client-stubs executed successfully

退出状态
       0
           已成功执行命令

       1
           执行命令时出现错误

另请参阅
       deploy(1), redeploy(1), undeploy(1)

       appclient(1M), asadmin(1M), package-appclient(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2010 年 11 月 16 日          get-client-stubs(1)
