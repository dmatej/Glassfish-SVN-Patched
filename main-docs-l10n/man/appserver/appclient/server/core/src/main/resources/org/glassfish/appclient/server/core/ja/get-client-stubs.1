get-client-stubs(1)   asadminユーティリティのサブコマンド  get-client-stubs(1)

名前
       get-client-stubs -
       アプリケーション・クライアントの起動に必要なアプリケーションJARファイルを取得します。

概要
           get-client-stubs [--help] --appname application_name local_directory_path

説明
       get-client-stubsサブコマンドは、AppClientスタンドアロン・モジュール、またはアプリケーションの各AppClientモジュールで必要なJARファイルを、サーバー・マシンからローカル・ディレクトリにコピーします。各クライアントで生成されるJARファイルは、サポートされている必要なJARファイルとともに取得されます。クライアントJARファイル名の形式はapp-name
       Client.jarです。get-client-stubsサブコマンドを実行する前に、アプリケーションまたはモジュールをデプロイする必要があります。生成されるクライアントJARファイルは、appclientユーティリティを使用してアプリケーションを実行する際に役立ちます。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --appname
           アプリケーションまたはスタンドアロンのクライアント・モジュールの名前。

           名前には、オプションのバージョン識別子を含めることができます。このバージョン識別子は名前の後に続き、コロン(:)で名前と区切ります。バージョン識別子は、文字または数値で始まる必要があります。これには、英数字とアンダースコア(_)、ダッシュ(-)、およびピリオド(.)を含めることができます。モジュールとアプリケーションのバージョンについては、Oracle
           GlassFish
           Serverア

オペランド
       local_directory_path
           クライアント・スタブJARファイルを保存するローカル・ディレクトリのパス。

例
       例 1, get-client-stubsの使用

               asadmin> get-client-stubs --appname myapplication /sample/example
               Command get-client-stubs executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       deploy(1), redeploy(1), undeploy(1)

       appclient(1M), asadmin(1M), package-appclient(1M)

       Oracle GlassFish Serverア

Java EE 7                       2010年11月16日             get-client-stubs(1)
