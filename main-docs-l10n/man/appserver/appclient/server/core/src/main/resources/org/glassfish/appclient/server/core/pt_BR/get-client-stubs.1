get-client-stubs(1)     Subcomandos do utilitário asad     get-client-stubs(1)

NOME
       get-client-stubs - recupera os arquivos JAR da aplicação necessários
       para acionar o cliente da aplicação.

SINOPSE
           get-client-stubs [--help] --appname application_name local_directory_path

DESCRIçãO
       O subcomando get-client-stubs copia os arquivos JAR para um módulo
       AppClient standalone ou cada módulo AppClient em uma aplicação da
       máquina servidora para o diretório local. Cada arquivo JAR do cliente
       gerado é recuperado, junto com quaisquer arquivos JAR de suporte. O
       nome de arquivo JAR do cliente é do formato app-name Client.jar. Antes
       de executar o subcomando
        get-client-stubs, é necessário implantar a aplicação ou o módulo. O
       arquivo cliente JAR gerado é útil para executar a aplicação usando o
       utilitário appclient. Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --appname
           O nome da aplicação ou módulo do cliente standalone.

           O nome pode incluir um identificador de versão opcional, que segue
           o nome e é separado do nome por dois-pontos (:). O identificador de
           versão deve começar com uma letra ou número. Pode conter caracteres
           alfanuméricos e sublinhado (_), traço (-), e ponto (.). Para obter
           mais informações sobre versões de módulo e da aplicação, consulte
           "Module and Application Versions" no Oracle GlassFish Server
           Application Deployment Guide.

OPERANDOS
       local_directory_path
           O caminho para o diretório local onde o fragmento do cliente do
           arquivo JAR deve estar armazenado.

EXEMPLOS
       Example 1, Usando get-client-stubs

               asadmin> get-client-stubs --appname myapplication /sample/example
               Command get-client-stubs executed successfully

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       deploy(1), redeploy(1), undeploy(1)

       appclient(1M), asadmin(1M), package-appclient(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         16 nov 2010              get-client-stubs(1)
