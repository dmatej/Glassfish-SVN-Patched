list-sub-components(1)      asadmin 实用程序子命令      list-sub-components(1)

名称
       list-sub-components - 列出已部署模块或已部署应用程序的模块中的 EJB 或
       servlet 组件

提要
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

说明
       list-sub-components 子命令列出已部署模块或已部署应用程序的模块中的 EJB
       或 Servlet 组件。如果未指定模块，则会列出所有模块。--appname
       选项仅当指定模块独立时才起作用。要显示应用程序中的特定模块，必须与
       --appname 选项一起指定模块名称。

       该子命令仅在远程模式下受支持。

选项
       --help, -?
           显示子命令的帮助文本。

       --type
           指定要列出的组件的类型。选项为 ejbs 和
           servlets。如果未指定任何类型，则会列出所有组件。

       --appname
           标识应用程序的名称。当所需输出为已部署应用程序的嵌入式模块的子组件时，此选项是必需的。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.)
           等字符。有关模块和应用程序版本的详细信息，请参阅 Oracle GlassFish
           Server Application Deployment Guide 中的 Module and Application
           Versions。

       --resources
           为每个子组件列出应用程序范围的资源。

操作数
       modulename
           指定包含子组件的模块的名称。

           名称可包含可选的版本标识符，它跟在名称后面，并与名称以冒号 (:)
           分隔。版本标识符必须以字母或数字开头。它可包含字母数字以及下划线
           (_)、短划线 (-) 和句点 (.)
           等字符。有关模块和应用程序版本的详细信息，请参阅 Oracle GlassFish
           Server Application Deployment Guide 中的 Module and Application
           Versions。

示例
       Example 1, 列出子组件
           此示例列出 mejb.jar 模块内 MEjbApp 应用程序的子组件。

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

退出状态
       0
           已成功执行子命令

       1
           执行子命令时出现错误

另请参阅
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                     2009 年 10 月 12 日       list-sub-components(1)
