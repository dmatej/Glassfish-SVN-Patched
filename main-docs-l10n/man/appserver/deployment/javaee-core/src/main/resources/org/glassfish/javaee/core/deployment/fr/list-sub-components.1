list-sub-components(1)  Sous-commandes de l'utilitaire  list-sub-components(1)

NOM
       list-sub-components - répertorie les composants de servlet ou EJB dans
       un module déployé ou dans un module d'une application déployée

SYNTAXE
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

DESCRIPTION
       La sous-commande list-sub-components répertorie les composants de
       servlet ou EJB dans un module déployé ou dans un module d'une
       application déployée. Si aucun module n'est spécifié, tous les modules
       sont répertoriés. L'option --appname fonctionne uniquement lorsque le
       module spécifié est autonome. Pour afficher un module spécifique dans
       une application, vous devez spécifier le nom du module avec l'option
       --appname.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Cette option affiche le texte d'aide de la sous-commande.

       --type
           Spécifie le type de composant à répertorier. Les options sont ejbs
           et servlets. Si aucun élément n'est spécifié, tous les composants
           sont répertoriés.

       --appname
           Identifie le nom de l'application. Cette option est obligatoire
           lorsque la sortie souhaitée correspond aux sous-composants d'un
           module imbriqué d'une application déployée.

           Le nom peut être suivi d'un identificateur de version facultatif
           dont il est séparé par un signe deux-points (:). L'identificateur
           de version doit commencer par une lettre ou un chiffre. Il peut
           contenir des caractères alphanumériques, des traits de soulignement
           (_), des tirets (-) et des points (.). Pour plus d'informations sur
           les versions des modules et des applications, reportez-vous à
           Module and Application Versions dans le manuel Oracle GlassFish
           Server Application Deployment Guide.

       --resources
           Répertorie les ressources de niveau application pour chaque
           sous-composant.

OPÉRANDES
       modulename
           Spécifie le nom du module contenant le sous-composant.

           Le nom peut être suivi d'un identificateur de version facultatif
           dont il est séparé par un signe deux-points (:). L'identificateur
           de version doit commencer par une lettre ou un chiffre. Il peut
           contenir des caractères alphanumériques, des traits de soulignement
           (_), des tirets (-) et des points (.). Pour plus d'informations sur
           les versions des modules et des applications, reportez-vous à
           Module and Application Versions dans le manuel Oracle GlassFish
           Server Application Deployment Guide.

EXEMPLES
       Exemple 1, Liste des sous-composants
           Dans cet exemple, les sous-composants de l'application MEjbApp dans
           le module mejb.jar sont répertoriés.

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                        12 oct. 2009           list-sub-components(1)
