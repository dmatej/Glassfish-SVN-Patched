list-sub-components(1)  Subcomandos do utilitário asad  list-sub-components(1)

NOME
       list-sub-components - lista componentes EJB ou de servlet em um módulo
       implantado ou em um módulo de uma aplicação implantada

SINOPSE
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

DESCRIçãO
       O subcomando list-sub-components lista componentes EJB ou um servlet em
       um módulo implantado ou em um módulo de uma aplicação implantada. Se um
       módulo não estiver especificado, todos os módulos serão listados. A
       opção --appname só funciona quando o módulo especificado é standalone.
       Para exibir um módulo específico em uma aplicação, é necessário
       especificar o nome do módulo com a opção appname.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --type
           Especifica o tipo do componente a ser listado. As opções são ejbs e
           servlets. Se nada estiver especificado, então todos os componentes
           serão listados.

       --appname
           Identifica o nome da aplicação. Essa opção é necessária quando a
           saída desejada é o subcomponente de um módulo incorporado de uma
           aplicação implantada.

           O nome pode incluir um identificador de versão opcional, que segue
           o nome e é separado do nome por dois-pontos (:). O identificador de
           versão deve começar com uma letra ou número. Pode conter caracteres
           alfanuméricos e sublinhado (_), traço (-), e ponto (.). Para obter
           mais informações sobre versões de módulo e da aplicação, consulte
           "Module and Application Versions" no Oracle GlassFish Server
           Application Deployment Guide.

       --resources
           Lista os recursos de escopo da aplicação para cada subcomponente.

OPERANDOS
       modulename
           Especifica o nome do módulo que contém o subcomponente.

           O nome pode incluir um identificador de versão opcional, que segue
           o nome e é separado do nome por dois-pontos (:). O identificador de
           versão deve começar com uma letra ou número. Pode conter caracteres
           alfanuméricos e sublinhado (_), traço (-), e ponto (.). Para obter
           mais informações sobre versões de módulo e da aplicação, consulte
           "Module and Application Versions" no Oracle GlassFish Server
           Application Deployment Guide.

EXEMPLOS
       Example 1, Listando Subcomponentes
           Este exemplo lista os subcomponentes da aplicação MEjbApp no módulo
           mejb.jar.

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro na execução do subcomando

CONSULTE TAMBéM
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                         12 out 2009           list-sub-components(1)
