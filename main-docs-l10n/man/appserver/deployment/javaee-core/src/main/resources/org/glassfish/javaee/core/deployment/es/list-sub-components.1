list-sub-components(1)  Subcomandos de la Utilidad asa  list-sub-components(1)

NOMBRE DE REFERENCIA
       list-sub-components - muestra los componentes de servlet o EJB en un
       módulo desplegado o en un módulo de una aplicación desplegada.

SINOPSIS
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

DESCRIPCIÓN
       El subcomando list-sub-components muestra los componentes EJB en un
       módulo desplegado o en un módulo de una aplicación desplegada. Si no se
       especifica un módulo, se muestran todos los módulos. La opción
       --appname actúa sólo cuando el módulo especificado es un módulo
       independiente. Para mostrar un módulo específico de una aplicación, se
       debe especificar el nombre del módulo con la opción --appname.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --type
           Especifica el tipo de componente que se debe mostrar. Las opciones
           son ejbs y servlets. Si no se especifica nada, se muestran en la
           lista todos los componentes.

       --appname
           Identifica el nombre de la aplicación. Esta opción es necesaria
           cuando la salida deseada es la lista de subcomponentes de un módulo
           embebido de una aplicación desplegada.

           El nombre puede incluir un identificador de versión opcional, que
           va a continuación del nombre y está separado de él por dos puntos
           (:). El identificador de versión debe comenzar por una letra o
           número. Puede contener caracteres alfanuméricos más los caracteres
           de subrayado (_), guión (-) y punto (.). Para obtener más
           información sobre las versiones de módulo y de aplicación, consulte
           el apartado sobre las versiones de módulo y de aplicación en Oracle
           GlassFish Server Application Deployment Guide.

       --resources
           Se muestra una lista de los recursos restringidos al ámbito de
           aplicación para cada subcomponente.

OPERANDOS
       modulename
           Especifica el nombre del módulo que contiene el subcomponente.

           El nombre puede incluir un identificador de versión opcional, que
           va a continuación del nombre y está separado de él por dos puntos
           (:). El identificador de versión debe comenzar por una letra o
           número. Puede contener caracteres alfanuméricos más los caracteres
           de subrayado (_), guión (-) y punto (.). Para obtener más
           información sobre las versiones de módulo y de aplicación, consulte
           el apartado sobre las versiones de módulo y de aplicación en Oracle
           GlassFish Server Application Deployment Guide.

EJEMPLOS
       Ejemplo 1, Lista de Subcomponentes
           En este ejemplo se muestran los subcomponentes de la aplicación
           MEjbApp dentro del módulo mejb.jar.

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

ESTADO DE SALIDA
       0
           Subcomando ejecutado correctamente

       1
           Error al ejecutar el subcomando

CONSULTE TAMBIÉN
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                    12 de octubre de 2009      list-sub-components(1)
