list-sub-components(1)    asadmin 유틸리티 하위 명령    list-sub-components(1)

제목
       list-sub-components - 배치된 모듈이나 배치된 응용 프로그램 모듈의 EJB
       또는 서블릿 구성 요소 나열

시놉시스
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

설명
       list-sub-components 하위 명령은 배치된 모듈이나 배치된 응용 프로그램
       모듈의 EJB 또는 서블릿 구성 요소를 나열합니다. 모듈을 지정하지 않으면
       모든 모듈이 나열됩니다.  --appname 옵션은 지정된 모듈이 독립형인
       경우에만 작동합니다. 응용 프로그램의 특정 모듈을 표시하려면 모듈 이름에
       --appname 옵션을 지정해야 합니다.

       이 하위 명령은 원격 모드에서만 지원됩니다.

옵션
       --help, -?
           하위 명령에 대한 도움말 텍스트를 표시합니다.

       --type
           나열할 구성 요소 유형을 지정합니다. 옵션은 ejbs 및 servlets입니다.
           옵션을 지정하지 않은 경우 모든 구성 요소가 나열됩니다.

       --appname
           응용 프로그램의 이름을 식별합니다. 이 옵션은 배치된 응용 프로그램에
           포함된 모듈의 하위 구성 요소를 출력하려는 경우에 필요합니다.

           이름에는 버전 식별자(옵션)가 포함될 수 있으며, 이 식별자는 이름
           다음에 오고 콜론(:)으로 이름과 구분됩니다. 버전 식별자는 문자 또는
           숫자로 시작해야 합니다. 영숫자 문자와 밑줄(_), 대시(-) 및 마침표(.)
           문자가 사용될 수 있습니다. 모듈 및 응용 프로그램 버전에 대한 자세한
           내용은 Oracle GlassFish Server Application Deployment Guide의
           "Module and Application Versions"를 참조하십시오.

       --resources
           각 하위 구성 요소에 대해 응용 프로그램 범위의 리소스를 나열합니다.

피연산자
       modulename
           하위 구성 요소가 들어 있는 모듈 이름을 지정합니다.

           이름에는 버전 식별자(옵션)가 포함될 수 있으며, 이 식별자는 이름
           다음에 오고 콜론(:)으로 이름과 구분됩니다. 버전 식별자는 문자 또는
           숫자로 시작해야 합니다. 영숫자 문자와 밑줄(_), 대시(-) 및 마침표(.)
           문자가 사용될 수 있습니다. 모듈 및 응용 프로그램 버전에 대한 자세한
           내용은 Oracle GlassFish Server Application Deployment Guide의
           "Module and Application Versions"를 참조하십시오.

예제
       예 1, 하위 구성 요소 나열
           이 예제는 mejb.jar 모듈에 있는 MEjbApp 응용 프로그램의 하위 구성
           요소를 나열합니다.

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

종료 상태
       0
           하위 명령이 성공적으로 실행됨

       1
           하위 명령 실행 중 오류 발생

참조
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                      2009년 10월 12일         list-sub-components(1)
