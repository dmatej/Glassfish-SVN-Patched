list-sub-components(1)  Comandi secondari della utilit  list-sub-components(1)

NOME
       list-sub-components - elenca i componenti EJB o servlet di un modulo
       distribuito o di un modulo di un'applicazione distribuita

SINTASSI
           list-sub-components [--help] [--type type]
           [--appname appname] [--resources]
           modulename

DESCRIZIONE
       Il comando secondario list-sub-components elenca i componenti EJB o
       servlet di un modulo distribuito o di un modulo di un'applicazione
       distribuita. Se non si specifica un modulo, vengono elencati tutti i
       moduli. L'opzione --appname funziona solo se si specifica un modulo
       standalone. Per visualizzare un modulo specifico di un'applicazione, è
       necessario fornire il nome del modulo con l'opzione --appname.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --type
           Specifica il tipo di componente da elencare. Le opzioni sono ejbs e
           servlets. Se non si specifica alcun valore, vengono elencati tutti
           i componenti.

       --appname
           Identifica il nome dell'applicazione. Questa opzione è obbligatoria
           se si desidera ottenere come output i componenti secondari di un
           modulo incorporato di un'applicazione distribuita.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per ulteriori
           informazioni sulle versioni di moduli e applicazioni, vedere
           "Module and Application Versions" in Oracle GlassFish Server
           Application Deployment Guide.

       --resources
           Elenca le risorse con ambito a livello di applicazione per ciascun
           componente secondario.

OPERANDI
       modulename
           Specifica il nome del modulo contenente il componente secondario.

           Il nome può includere un identificativo di versione opzionale, che
           segue il nome ed è separato dal nome mediante due punti (:).
           L'identificativo di versione deve iniziare con una lettera o un
           numero. Può contenere caratteri alfanumerici più caratteri di
           sottolineatura (_), lineette (-) e punti (.). Per ulteriori
           informazioni sulle versioni di moduli e applicazioni, vedere
           "Module and Application Versions" in Oracle GlassFish Server
           Application Deployment Guide.

ESEMPI
       Esempio 1, Visualizzazione della lista dei componenti secondari
           In questo esempio vengono elencati i componenti secondari
           dell'applicazione MEjbApp nel modulo mejb.jar.

               asadmin> list-sub-components --appname MEjbApp mejb.jar
               MEJBBean <StatelessSessionBean>
               Command list-sub-components executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable(1), disable(1), list-components(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

Java EE 7                       12 ottobre 2009         list-sub-components(1)
